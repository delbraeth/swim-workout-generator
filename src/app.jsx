    // SPA-split Phase 3: components carved out of this file live in src/components/.
    // (The server's engine extractor in lib/generator.js strips these import lines
    // before vm-eval'ing the engine prelude, so they don't affect /api/generate.)
    import { Stat } from "./components/Stat.jsx";
    import { StarRating } from "./components/StarRating.jsx";
    import { R1ProgrammingMixTab } from "./components/reports/R1ProgrammingMixTab.jsx";
    import { R2ScheduleAdherenceTab } from "./components/reports/R2ScheduleAdherenceTab.jsx";
    import { R3CurationLogTab } from "./components/reports/R3CurationLogTab.jsx";
    import { R4ProgramRecapTab } from "./components/reports/R4ProgramRecapTab.jsx";
    import { R5PlatformHealthTab } from "./components/reports/R5PlatformHealthTab.jsx";
    import { R6CurationSupportTab } from "./components/reports/R6CurationSupportTab.jsx";
    import { AdminView } from "./components/admin/AdminView.jsx";
    import { AdminPendingUgc } from "./components/admin/AdminPendingUgc.jsx";
    import { AdminPublicUgc } from "./components/admin/AdminPublicUgc.jsx";
    import { UgcGraduateModal } from "./components/admin/UgcGraduateModal.jsx";
    import { AdminFeedback } from "./components/admin/AdminFeedback.jsx";
    import { AdminUsers } from "./components/admin/AdminUsers.jsx";
    import { EditUserModal } from "./components/admin/EditUserModal.jsx";
    import { AdminInvites } from "./components/admin/AdminInvites.jsx";
    import { AdminEmailTest } from "./components/admin/AdminEmailTest.jsx";
    import { AdminBillingConfig } from "./components/admin/AdminBillingConfig.jsx";
    import { AdminVendorKit } from "./components/admin/AdminVendorKit.jsx";
    import { AdminAudit } from "./components/admin/AdminAudit.jsx";

    const { useState, useCallback, useMemo, useEffect } = React;

    // ═══════════════════════════════════════════════════════════════
    //  HISTORY BACKEND CONFIG — same origin as the page, so all paths
    //  are relative. The Node server (see server.js + Dockerfile)
    //  handles GitHub commits server-side; no secrets in this file.
    // ═══════════════════════════════════════════════════════════════
    export const API_BASE  = "/api";                 // same origin (exported for src/components/**; engine extractor strips `export`)
    const BUILD_ID  = (typeof window !== "undefined" && window.__BUILD_ID__) || "dev";   // SPA-split: the shell index.html sets window.__BUILD_ID__ (stamped by _deploy.py); the bundle reads it here (deploy stamps index.html, not the bundle).

    // CSRF token bound to the session. Fetched after auth-status confirms login,
    // sent on every write as the X-CSRF-Token header.
    const csrf = { token: null };
    async function refreshCsrf() {
      try {
        const r = await fetch(`${API_BASE}/auth/csrf`, { cache: "no-store" });
        if (!r.ok) return;
        const d = await r.json();
        csrf.token = d.token || null;
      } catch { /* leave previous value */ }
    }
    export function csrfHeaders() {   // exported for src/components/admin/*; engine extractor strips `export`
      return csrf.token ? { "X-CSRF-Token": csrf.token } : {};
    }

    // v3 impersonation client (2026-05-22 per VIEW_AS_V3_SCOPE.md).
    // Module-scope so the fetch wrapper below can read it without React
    // closure complexity. App.jsx sets/clears it via setImpersonation().
    // Shape: { target_sub, target_name, target_email, expires_at } | null.
    const impersonation = { active: null };
    function setImpersonation(state) {
      impersonation.active = state || null;
    }
    // Wrap window.fetch to auto-add X-Impersonate-Sub to every /api/* call
    // when an impersonation session is active. Targeting only /api/ keeps
    // CDN/asset fetches untouched. The header is the only signal the
    // server needs to rewrite req.userSub; the server validates the
    // header against an active session row on every request (see
    // server.js requireAuth).
    // Also makes every fetch resilient to HTTP 429. Hyperlift rate-limits
    // bursts of parallel GETs (opening a team or swimmer fans out 5-7 reads
    // at once); a 429 means the request was REJECTED before the handler ran,
    // so a short backoff-retry is safe for reads AND writes (the write never
    // executed). Without this, callers' catch blocks silently render the 429
    // as empty — e.g. the Groups tab showing "No groups yet" for a team that
    // actually has groups. Retries only on 429; all other statuses pass
    // straight through. Honors Retry-After when the server sends it.
    if (typeof window !== "undefined" && window.fetch && !window.__sfImpersonationFetchWrapped) {
      const _origFetch = window.fetch.bind(window);
      const _sfSleep = ms => new Promise(r => setTimeout(r, ms));
      const _SF_MAX_429_RETRIES = 4;
      window.fetch = async function(input, init) {
        const url = typeof input === "string" ? input : (input && input.url) || "";
        if (impersonation.active && impersonation.active.target_sub && url.startsWith("/api/")) {
          init = init || {};
          init.headers = { ...(init.headers || {}), "X-Impersonate-Sub": impersonation.active.target_sub };
        }
        let attempt = 0;
        while (true) {
          const res = await _origFetch(input, init);
          if (res.status !== 429 || attempt >= _SF_MAX_429_RETRIES) return res;
          const ra = parseFloat(res.headers.get("retry-after"));
          const backoff = Number.isFinite(ra) ? ra * 1000 : Math.min(2000, 200 * Math.pow(2, attempt));
          await _sfSleep(backoff + Math.random() * 150);
          attempt++;
        }
      };
      window.__sfImpersonationFetchWrapped = true;
    }

    // Normalize raw initials input: trim, uppercase, alphanumeric only, max 4 chars.
    function normalizeInitials(raw) {
      return (raw || "").toUpperCase().replace(/[^A-Z0-9]/g, "").slice(0, 4);
    }

    // Pool-mode → human label. Top-level so any component can use it (there's
    // also a component-local `poolLabel` with the same mapping; this is the
    // shared one for IntentForm / WeekView / facility chips).
    function poolModeLabel(m) {
      return ({ "25y": "SCY (25y)", "25m": "SCM (25m)", "50m": "LCM (50m)", "yds": "SCY (legacy)" })[m] || m;
    }

    const HISTORY_LOCAL_KEY = "swim_history_v1";
    const USER_INITIALS_KEY = "swim_user_initials";

    function loadLocalHistory() {
      try { const raw = localStorage.getItem(HISTORY_LOCAL_KEY); return raw ? JSON.parse(raw) : []; }
      catch (_) { return []; }
    }
    function saveLocalHistory(entries) {
      try { localStorage.setItem(HISTORY_LOCAL_KEY, JSON.stringify(entries)); } catch (_) {}
    }
    // Merge two entry lists by id (b wins on conflict), then sort newest-first by dateCompleted.
    function mergeById(a, b) {
      const map = new Map();
      for (const e of a) if (e && e.id) map.set(e.id, e);
      for (const e of b) if (e && e.id) map.set(e.id, e);
      return [...map.values()].sort((x, y) => (y.dateCompleted || "").localeCompare(x.dateCompleted || ""));
    }
    function makeEntryId() {
      return "w" + Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
    }

    // ═══════════════════════════════════════════════════════════════
    //  WORKOUT TYPE METADATA
    // ═══════════════════════════════════════════════════════════════
    const WORKOUT_TYPES = [
      { id: "im",        label: "IM Focus",         emoji: "🔄", description: "All four strokes — fly, back, breast, free",
        selBg: "#7c3aed", border: "#c4b5fd", badge: "#ede9fe", badgeText: "#5b21b6" },
      { id: "distance",  label: "Distance",         emoji: "🏊", description: "Longer swims, building aerobic base",
        selBg: "#2563eb", border: "var(--color-primary)", badge: "#dbeafe", badgeText: "#1e40af" },
      { id: "sprint",    label: "Sprint",           emoji: "⚡", description: "Short, fast swims with full recovery",
        selBg: "#dc2626", border: "#fca5a5", badge: "#fee2e2", badgeText: "#991b1b" },
      { id: "endurance", label: "Endurance",        emoji: "💪", description: "Steady-state, long continuous swims",
        selBg: "#16a34a", border: "#86efac", badge: "#dcfce7", badgeText: "#166534" },
      { id: "technique", label: "Technique",        emoji: "🎯", description: "Drills, kick sets, and stroke refinement",
        selBg: "#0d9488", border: "#5eead4", badge: "#ccfbf1", badgeText: "#115e59" },
      { id: "mixed",     label: "Mixed",            emoji: "🔀", description: "Sprints, distance, and drills combined",
        selBg: "#ea580c", border: "#fdba74", badge: "#ffedd5", badgeText: "#9a3412" },
      { id: "back",      label: "Specialty Back",   emoji: "🔙", description: "Backstroke focus across drills and main set",
        selBg: "#0891b2", border: "#67e8f9", badge: "#cffafe", badgeText: "#155e75" },
      { id: "breast",    label: "Specialty Breast", emoji: "🐸", description: "Breaststroke focus across drills and main set",
        selBg: "#db2777", border: "#f9a8d4", badge: "#fce7f3", badgeText: "#9d174d" },
      { id: "fly",       label: "Specialty Fly",    emoji: "🦋", description: "Butterfly focus (with free recovery throughout)",
        selBg: "#ca8a04", border: "#facc15", badge: "#fef9c3", badgeText: "#854d0e" },
    ];

    const SECTION_STYLES = {
      warmup:   { bg: "#f0f9ff", border: "#bae6fd", headerBg: "#e0f2fe", headerText: "#075985", dot: "#0ea5e9" },
      drill:    { bg: "#f0fdfa", border: "#99f6e4", headerBg: "#ccfbf1", headerText: "#134e4a", dot: "var(--color-primary)" },
      kick:     { bg: "#fffbeb", border: "#fde68a", headerBg: "#fef3c7", headerText: "#92400e", dot: "#f59e0b" },
      main:     { bg: "#f8fafc", border: "var(--color-text)", headerBg: "var(--color-border)", headerText: "#ffffff", dot: "var(--color-text-dim)" },
      cooldown: { bg: "#f9fafb", border: "#e5e7eb", headerBg: "#f3f4f6", headerText: "#374151", dot: "#9ca3af" },
    };

    // ═══════════════════════════════════════════════════════════════
    //  EFFORT-ZONE TAXONOMY (Session 2 H foundation)
    //  5 zones, green → red intensity gradient. Used for the small color
    //  pip rendered on block headers and on divergent set rows.
    //  Options can set `primary_zone` (and optional `secondary_zones`)
    //  explicitly; otherwise `inferZone()` derives a sensible default.
    // ═══════════════════════════════════════════════════════════════
    const ZONES = {
      easy:      { id: "easy",      label: "Easy",      color: "var(--color-positive)", rank: 1 },
      aerobic:   { id: "aerobic",   label: "Aerobic",   color: "var(--color-primary)", rank: 2 },
      threshold: { id: "threshold", label: "Threshold", color: "#eab308", rank: 3 },
      vo2:       { id: "vo2",       label: "VO2",       color: "#f97316", rank: 4 },
      anaerobic: { id: "anaerobic", label: "Anaerobic", color: "var(--color-destructive)", rank: 5 },
    };
    const ZONE_ORDER = ["easy", "aerobic", "threshold", "vo2", "anaerobic"];

    // ═══════════════════════════════════════════════════════════════
    //  MESOCYCLE / TRAINING PHASE (N5)
    //  Five phases. User sets one in the Profile modal; it sticks until
    //  changed. The generator multiplies main-set pick weights by the
    //  phase's per-zone multiplier (in addition to fav/repeat/recent).
    //  Naming note: "recovery" PHASE is a longer-term setting; "recovery
    //  mode" (item C) is a per-generation toggle. They compose: a recovery-
    //  phase swimmer can still flip recovery mode on for an extra-easy day.
    // ═══════════════════════════════════════════════════════════════
    const PHASES = {
      base:     { id: "base",     label: "Base",     emoji: "🌱", color: "#0ea5e9",
                  description: "Aerobic foundation — high easy/aerobic volume",
                  multipliers: { easy: 1.2, aerobic: 1.5, threshold: 0.8, vo2: 0.5, anaerobic: 0.5 } },
      build:    { id: "build",    label: "Build",    emoji: "🔨", color: "var(--color-primary)",
                  description: "Threshold/VO2 emphasis — push the ceiling",
                  multipliers: { easy: 0.8, aerobic: 1.0, threshold: 1.5, vo2: 1.3, anaerobic: 0.8 } },
      peak:     { id: "peak",     label: "Peak",     emoji: "⛰️", color: "#a855f7",
                  description: "Race-specific intensity — sharpen the sword",
                  multipliers: { easy: 0.8, aerobic: 0.8, threshold: 1.0, vo2: 1.5, anaerobic: 1.5 } },
      taper:    { id: "taper",    label: "Taper",    emoji: "📉", color: "#eab308",
                  description: "Reduce volume, preserve sharpness",
                  multipliers: { easy: 1.5, aerobic: 1.3, threshold: 0.7, vo2: 0.5, anaerobic: 0.3 } },
      recovery: { id: "recovery", label: "Recovery", emoji: "🌿", color: "var(--color-positive)",
                  description: "Active recovery — easy aerobic only",
                  multipliers: { easy: 2.0, aerobic: 1.5, threshold: 0.3, vo2: 0.2, anaerobic: 0.1 } },
    };
    const PHASE_ORDER = ["base", "build", "peak", "taper", "recovery"];

    // J — Swimmer-level presets. Persisted in settings.extra.level; picking
    // a level in Profile overwrites paceInput. No extra interval-scaling
    // multiplier (decision 2026-05-19) — level is just a calibration shortcut
    // so a new swimmer can pick "Masters" and get a sane default pace
    // instead of staring at the M:SS field with no idea what to type.
    const LEVEL_PRESETS = {
      recreational: { id: "recreational", label: "Recreational", emoji: "🏖️", pace: "2:30", description: "Fitness swimmer — long, easy intervals" },
      masters:      { id: "masters",      label: "Masters",      emoji: "🏊", pace: "2:00", description: "Adult masters / triathlete — moderate intervals" },
      competitive:  { id: "competitive",  label: "Competitive",  emoji: "🏁", pace: "1:30", description: "Trained competitive swimmer — tight intervals" },
    };
    const LEVEL_ORDER = ["recreational", "masters", "competitive"];

    // Onboarding tour — a lightweight spotlight walkthrough of the
    // generator setup flow, ending at the Generate button. Each step
    // targets a data-tour="..." anchor that already exists in the markup.
    // Only PRE-generation controls are included (favorite-toggle and
    // celebrate anchors render only after a workout exists). The anchor
    // named "step-swimmer-level" is actually on the profile button, so it
    // is intentionally not used here. Persisted-seen flag lives in
    // settings.extra.tour_seen (see applySettings / handleTourFinish).
    const TOUR_STEPS = [
      { sel: "step-pool-mode",      title: "Pick your pool",        body: "Choose your course — short-course yards or meters, or long-course. This sets the distances and the pace math for everything below." },
      { sel: "step-type-cards",     title: "Choose a workout type", body: "Pick today's focus — sprint, distance, IM and more. Each type shapes the sets the generator builds." },
      { sel: "step-pace-input",     title: "Set your pace",         body: "Enter your base pace as M:SS per 100. Intervals and target times are calculated from this number." },
      { sel: "step-equipment",      title: "Mark your equipment",   body: "Tell the generator what you have — fins, paddles, kickboard, snorkel. Sets will use or avoid gear to match." },
      { sel: "step-yardage-slider", title: "Dial in the distance",  body: "Slide to set the total distance for the session. The generator fills it with balanced warmup, main and cooldown work." },
      { sel: "step-generate-button", title: "Build your first set", body: "When you're ready, tap Generate — your workout appears below, ready to print or run on the pace clock." },
    ];

    // Heuristic zone inference from a single set's text + interval + distance.
    // Returns one of ZONE_ORDER. Keyword cues in desc/focus dominate; interval
    // tightness (work/total ratio) is the tie-breaker when text is ambiguous.
    function inferSetZone(set, sectionKey) {
      if (!set) return "aerobic";
      const text = `${set.desc || ""} ${set.focus || ""}`.toLowerCase();

      // Cooldowns are always easy unless desc says otherwise.
      if (sectionKey === "cooldown") {
        if (/sprint|fast|hard|all.?out|race/.test(text)) return "aerobic";
        return "easy";
      }

      // Strong keyword cues — explicit intensity language wins over heuristics.
      if (/all.?out|sprint|max(?!imum aerobic)|blast|empty the tank|top speed|race pace/.test(text)) {
        return "anaerobic";
      }
      if (/vo2|fast effort|fast,? hold|fast finish|fast close|fast,? fatigued/.test(text)) {
        return "vo2";
      }
      if (/threshold|t.?pace|rpe ?[78]/.test(text)) return "threshold";
      // S4 #11 — keyword set is tighter for main than for warmup/drill. In a main
      // set, "drill rotation" or "stroke technique" is often a sub-cue of a
      // harder workout (e.g. "Pull with paddles + drill rotation") and shouldn't
      // collapse the whole set to easy/aerobic. Keep those words for warmup/drill
      // sections where they genuinely signal easy work.
      const isWarmupOrDrill = (sectionKey === "warmup" || sectionKey === "drill");
      const easyRegex = isWarmupOrDrill
        ? /easy|recovery|smooth|drill|technique/
        : /easy|recovery|smooth/;
      if (easyRegex.test(text)) {
        return isWarmupOrDrill ? "easy" : "aerobic";
      }
      if (/descend|build|negative.?split|strong/.test(text)) {
        // Descend/build sets typically climb into threshold by the end.
        return "threshold";
      }
      if (/moderate|steady|aerobic/.test(text)) return "aerobic";

      // Interval-tightness fallback: estimate seconds/100yd then bucket.
      // Tighter intervals = higher zone. This is a coarse signal.
      const secs = parseIntervalSecs(set.interval);
      const dist = Number(set.dist || 0);
      if (secs && dist > 0) {
        const per100 = secs / (dist / 100);
        // S4 #13 — Stroke factor: non-free strokes naturally have higher
        // per-100 at the same effort, so the thresholds need to scale up or
        // a "moderate" back/breast gets tagged harder than a "moderate" free.
        // Heuristic detection from desc text; defaults to free (1.0) when unclear.
        const strokeFactor =
          /\bback/.test(text)                ? 1.09 :
          /\bbreast/.test(text)              ? 1.18 :
          /\bbutterfly\b|\bfly\b/.test(text) ? 1.05 :
          /\bim\b/.test(text)                ? 1.13 :
          1.0;
        // Cutoffs MUST stay monotonic (anaerobicMax < vo2Max < thresholdMax <
        // aerobicMax) or a band gets swallowed. Short reps (dist ≤ 50) run at
        // tighter per-100 send-offs, so the whole ladder shifts faster — but
        // both anaerobic AND vo2 must shift together.
        // BUG FIX (VO2 gap): the old ≤50 values were anaerobicMax 110 / vo2Max
        // 95 — inverted, so `per100 < 110 → anaerobic` ate the entire 95–110
        // vo2 band and a 50 could NEVER classify as vo2. Now ≤50 keeps real
        // sprints anaerobic (per100 < 90) and opens a vo2 band 90–110, so a
        // hard 50 (e.g. 0:52 send-off, per100 104) reads vo2 instead of being
        // over-promoted to anaerobic.
        const anaerobicMax = (dist <= 50 ? 90  : 75)  * strokeFactor;
        const vo2Max       = (dist <= 50 ? 110 : 95)  * strokeFactor;
        const thresholdMax = (dist <= 50 ? 125 : 115) * strokeFactor;
        const aerobicMax   = 145 * strokeFactor;
        if (per100 < anaerobicMax) return "anaerobic";
        if (per100 < vo2Max)       return "vo2";
        if (per100 < thresholdMax) return "threshold";
        if (per100 < aerobicMax)   return "aerobic";
        return "easy";
      }

      // Section-default fallback when text gives no signal.
      if (sectionKey === "warmup")   return "easy";
      if (sectionKey === "drill")    return "aerobic";
      if (sectionKey === "cooldown") return "easy";
      return "aerobic";
    }

    // Block-level zone: explicit `primary_zone` wins; otherwise take the
    // highest-rank zone across the block's sets (so a block with one vo2
    // finisher is shown as threshold/vo2, not aerobic).
    function inferBlockZone(block) {
      if (!block) return "aerobic";
      if (block.primary_zone && ZONES[block.primary_zone]) return block.primary_zone;
      const sets = Array.isArray(block.sets) ? block.sets : [];
      if (!sets.length) return "aerobic";
      // For warmup/cooldown, take the modal (most common) zone since they're
      // typically uniform and a single accent set shouldn't redefine the block.
      // For drill/main, take the *dominant by yardage* zone.
      const tally = {};
      for (const s of sets) {
        const z = s.zone || inferSetZone(s, block.section);
        const w = (s.reps || 1) * (s.dist || 0);
        tally[z] = (tally[z] || 0) + Math.max(1, w);
      }
      let best = null, bestW = -1;
      for (const [z, w] of Object.entries(tally)) {
        if (w > bestW) { best = z; bestW = w; }
      }
      return best || "aerobic";
    }

    // ═══════════════════════════════════════════════════════════════
    //  WORKOUT DATA — all options as flat arrays
    //  Every totalYards value is verified: sum of (reps × dist)
    // ═══════════════════════════════════════════════════════════════

    const WARMUP_OPTIONS = [
      { label: "Easy Free 400", typeAffinity: ["sprint"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_rw8rg5", reps: 1, dist: 400, desc: "Easy Freestyle", interval: "No interval — swim easy", focus: "Loosen up, steady breathing" },
        ]},
      { label: "Free Warmup (400 yd)", typeAffinity: ["technique","sprint"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_we2vne", reps: 1, dist: 200, desc: "Easy Freestyle", interval: "No interval", focus: "Settle into rhythm" },
          { id: "s_zux0lg", reps: 4, dist: 50, desc: "25 Drill / 25 Easy Swim (stroke choice)", interval: "On 1:15", focus: "Feel for the water" },
        ]},
      { label: "Free Warmup + Streamline (400 yd)", typeAffinity: ["endurance"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_5mly07", reps: 1, dist: 300, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Gradually wake up" },
          { id: "s_8tdq9n", reps: 4, dist: 25, desc: "Kick (no board, streamlined)", interval: "On :40", focus: "Loosen hips and ankles" },
        ]},
      { label: "Back Warmup + Pull (500 yd)", typeAffinity: ["endurance"], totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_j1x3k7", reps: 1, dist: 400, desc: "Freestyle — easy, focus on long stroke", interval: "No interval", focus: "Prime the aerobic engine" },
          { id: "s_t9du10", reps: 4, dist: 25, desc: "Backstroke — easy, open the shoulders", interval: "On :45", focus: "Counter the freestyle pull" },
        ]},
      { label: "Free Warmup + Pull (600 yd)", typeAffinity: ["endurance"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_bthgf9", reps: 1, dist: 400, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Aerobic warm-up, long stroke" },
          { id: "s_omazgf", reps: 4, dist: 50, desc: "Pull (buoy) — easy, focus on catch and extension", interval: "On 1:20", focus: "Upper-body prime before main set", eq: "pull" },
        ]},
      { label: "Back Warmup + Pull (700 yd)", typeAffinity: ["endurance"], totalYards: 700, types: [], strokes: [], sets: [
          { id: "s_8kz4h8", reps: 1, dist: 400, desc: "Easy Freestyle — settle in, count strokes", interval: "No interval", focus: "Efficiency awareness from the first lap" },
          { id: "s_sqezb0", reps: 2, dist: 100, desc: "Kick (board optional) — moderate, even effort", interval: "On 2:30", focus: "Activate legs", eq: "kick" },
          { id: "s_ncd89u", reps: 1, dist: 100, desc: "Easy backstroke — open the shoulders, counter freestyle pull", interval: "No interval", focus: "All-around warm-up close" },
        ]},
      { label: "Free Warmup + Build (800 yd)", typeAffinity: ["technique","endurance"], totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_vyjznr", reps: 1, dist: 400, desc: "Freestyle — very easy, nose down, minimal head movement", interval: "No interval", focus: "Prime body position and breathing pattern" },
          { id: "s_djhj9x", reps: 4, dist: 50, desc: "Stroke drills — choice, one focus per 50", interval: "On 1:20", focus: "Technical warm-up" },
          { id: "s_r43d7e", reps: 1, dist: 200, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Ramp up the aerobic engine before main set" },
        ]},
      { label: "The Dirty Dozen", typeAffinity: ["sprint"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_sm1d0q", reps: 4, dist: 50, desc: "Easy freestyle — wake up the system", interval: "On 1:15", focus: "Loosen up" },
          { id: "s_yf2a1a", reps: 4, dist: 50, desc: "Build — easy→90% by the wall, no all-out yet", interval: "On 1:15", focus: "Progressive activation of fast-twitch" },
          { id: "s_bn0zg3", reps: 4, dist: 50, desc: "Sprint — 25 fast / 25 easy float", interval: "On 1:30", focus: "Prime top-end speed without burning out" },
        ]},
      { label: "Snake Pyramid 650", typeAffinity: ["endurance"], totalYards: 650, types: [], strokes: [], sets: [
          { id: "s_vlz33w", reps: 1, dist: 50, desc: "Freestyle — easy", interval: "On 1:15", focus: "Toe in the water" },
          { id: "s_wiope6", reps: 1, dist: 100, desc: "Freestyle — easy build", interval: "On 2:15", focus: "Lengthen the stroke" },
          { id: "s_hfrg76", reps: 1, dist: 150, desc: "Freestyle — moderate, smooth tempo", interval: "On 3:15", focus: "Settle into aerobic rhythm" },
          { id: "s_jkmsxd", reps: 1, dist: 200, desc: "Freestyle — moderate, peak of the pyramid", interval: "On 4:15", focus: "Hold form across the longest piece" },
          { id: "s_o53kq2", reps: 1, dist: 100, desc: "Freestyle — moderate, faster than the way up", interval: "On 2:10", focus: "Descend the pyramid" },
          { id: "s_rdgayq", reps: 1, dist: 50, desc: "Freestyle — strong close", interval: "On 1:10", focus: "Fast finish" },
        ]},
      { label: "Build to Boil 900", typeAffinity: ["sprint","endurance"], totalYards: 900, types: [], strokes: [], sets: [
          { id: "s_lt2hhq", reps: 1, dist: 300, desc: "Freestyle — easy, find aerobic rhythm", interval: "On 6:30", focus: "Act 1: settle in" },
          { id: "s_vydoau", reps: 1, dist: 300, desc: "Freestyle — moderate, hold form throughout", interval: "On 6:15", focus: "Act 2: warm the engine" },
          { id: "s_4tt3bm", reps: 1, dist: 300, desc: "Freestyle — strong, near threshold by the end", interval: "On 6:00", focus: "Act 3: ignite for the main set" },
        ]},
      { label: "Wake & Warm Build 700", typeAffinity: ["technique","endurance"], totalYards: 700, types: [], strokes: [], sets: [
          { id: "s_96igsh", reps: 1, dist: 200, desc: "Freestyle — very easy, nose down, minimum head movement", interval: "On 4:30", focus: "Open the body, slow the heart rate" },
          { id: "s_nqaf9u", reps: 4, dist: 50, desc: "25 drill choice / 25 swim — same stroke each rep", interval: "On 1:20", focus: "Build a technique focus that carries into the swim" },
          { id: "s_ijuhyu", reps: 1, dist: 300, desc: "Freestyle — build easy → moderate by the end", interval: "On 6:30", focus: "Roll the warmth into the main set" },
        ]},
      { label: "Corkscrew Open 600", typeAffinity: ["technique"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_z7dwme", reps: 4, dist: 50, desc: "Corkscrew — alternate 25 freestyle right-side / 25 backstroke left-side (or fluid roll between them)", interval: "On 1:25", focus: "Open shoulders, hips, and rotation in one move" },
          { id: "s_thngeu", reps: 4, dist: 100, desc: "Freestyle — easy build, focus on body roll learned from the corkscrew", interval: "On 2:15", focus: "Apply rotation to flat freestyle" },
        ]},
      { label: "IM Ladder Wake-up", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_qhfsqf", reps: 1, dist: 100, desc: "Freestyle — easy", interval: "On 2:15", focus: "Settle in" },
          { id: "s_iz5gve", reps: 1, dist: 100, desc: "IM drill — 25 each stroke, easy", interval: "On 2:30", focus: "Sample all four strokes" },
          { id: "s_k4pofe", reps: 1, dist: 100, desc: "Kick (board) — freestyle, moderate tempo", interval: "On 2:30", focus: "Wake up the legs", eq: "kick" },
          { id: "s_91yb7h", reps: 1, dist: 100, desc: "IM drill — 25 each stroke, focus on transitions", interval: "On 2:30", focus: "Stroke changeovers" },
          { id: "s_c9qhfw", reps: 1, dist: 100, desc: "Freestyle — moderate, smooth tempo", interval: "On 2:00", focus: "Re-engage main stroke" },
          { id: "s_a4hhvy", reps: 1, dist: 100, desc: "100 IM — build each 25 to strong", interval: "On 2:30", focus: "Bring it home with intensity ramp" },
        ]},
      { label: "Stroke Buffet 800", totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_5lj0na", reps: 1, dist: 200, desc: "Freestyle — easy, settle in", interval: "On 4:00", focus: "Find aerobic rhythm" },
          { id: "s_pjo9pg", reps: 1, dist: 200, desc: "Backstroke — easy, open the shoulders", interval: "On 4:15", focus: "Switch on rotation" },
          { id: "s_epj258", reps: 1, dist: 200, desc: "Breaststroke or choice (not fly) — easy", interval: "On 4:30", focus: "Sample a non-free stroke" },
          { id: "s_94eyxv", reps: 1, dist: 200, desc: "Freestyle — build to moderate by the end", interval: "On 3:45", focus: "Close at warm-up tempo" },
        ]},
      { label: "Tool Time 800", totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_cgro81", reps: 1, dist: 200, desc: "Freestyle — easy swim, no gear", interval: "On 4:00", focus: "Open without gear" },
          { id: "s_3zs6ht", reps: 1, dist: 200, desc: "Pull (buoy) — easy, long stroke, high-elbow catch", interval: "On 4:00", focus: "Isolate the pull", eq: "pull" },
          { id: "s_al5gv6", reps: 1, dist: 200, desc: "Kick (board) — freestyle or choice, moderate tempo", interval: "On 4:30", focus: "Engage the legs", eq: "kick" },
          { id: "s_2ycazs", reps: 1, dist: 200, desc: "Freestyle — build the whole way, finish moderate-strong", interval: "On 3:45", focus: "Re-integrate everything" },
        ]},
      { label: "Rainbow Ladder 1000", typeAffinity: ["endurance","technique"], totalYards: 1000, types: [], strokes: [], sets: [
          { id: "s_lsq0p6", reps: 1, dist: 200, desc: "Freestyle — very easy (white zone)", interval: "On 4:30", focus: "Color 1: barely working" },
          { id: "s_dl6gq1", reps: 1, dist: 200, desc: "Freestyle — drill choice / swim by 25 (yellow zone)", interval: "On 4:15", focus: "Color 2: technique-overlay" },
          { id: "s_5ohpob", reps: 1, dist: 200, desc: "Freestyle — moderate, smooth tempo (orange zone)", interval: "On 4:00", focus: "Color 3: aerobic warmth" },
          { id: "s_u36hfq", reps: 1, dist: 200, desc: "Freestyle — moderate-strong, controlled (red zone)", interval: "On 3:45", focus: "Color 4: pre-main pressure" },
          { id: "s_s9r3e0", reps: 1, dist: 200, desc: "Freestyle — easy spin-down to reset (white zone)", interval: "On 4:30", focus: "Color 5: shake out before the main" },
        ]},
    ];

    const DRILL_OPTIONS = [
      { label: "IM 4×75", totalYards: 300, types: ["im"], strokes: [], sets: [
          { id: "s_pj7qjv", reps: 4, dist: 75, desc: "25 Fly Drill / 25 Back Drill / 25 Breast Drill", interval: "On 2:30", focus: "Feel each IM stroke before the main set" },
        ]},
      { label: "IM Kick (400 yd)", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_822pku", reps: 8, dist: 25, desc: "Stroke drills — 2 each: fly, back, breast, free", interval: "On :40", focus: "Isolate technique per stroke" },
          { id: "s_8uhmbn", reps: 4, dist: 50, desc: "IM order kick — 12.5 yards each stroke", interval: "On 1:45", focus: "Kick rhythm for each stroke", eq: "kick" },
        ]},
      { label: "IM Kick + Pull (400 yd)", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_1un10d", reps: 4, dist: 50, desc: "IM order kick — 12.5 yards each stroke", interval: "On 1:45", focus: "Wake up legs across IM strokes", eq: "kick" },
          { id: "s_7jnno4", reps: 2, dist: 100, desc: "Pull (buoy) — alternate 50 free / 50 stroke choice", interval: "On 2:15", focus: "Upper-body warm-up before IM main set", eq: "pull" },
        ]},
      { label: "IM Kick (200 yd)", totalYards: 200, types: ["im"], strokes: [], sets: [
          { id: "s_6dgh3x", reps: 4, dist: 25, desc: "Stroke drills — 1 each: fly, back, breast, free", interval: "On :40", focus: "Brief all-stroke activation" },
          { id: "s_tjpg13", reps: 4, dist: 25, desc: "IM kick — 6 yards each stroke, no board", interval: "On :45", focus: "Kick priming across all four strokes" },
        ]},
      { label: "IM 3×100", totalYards: 300, types: ["im"], strokes: [], sets: [
          { id: "s_niauzb", reps: 3, dist: 100, desc: "100 IM — 25 each stroke, easy (not race pace)", interval: "On 2:30", focus: "Stroke flow and transition practice" },
        ]},
      { label: "IM 4×100", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_8u84sb", reps: 4, dist: 100, desc: "100 IM — 25 drill each stroke, smooth transitions", interval: "On 2:30", focus: "Full-stroke drill sequence before IM main set" },
        ]},
      { label: "IM 5×100", totalYards: 500, types: ["im"], strokes: [], sets: [
          { id: "s_jk51ue", reps: 5, dist: 100, desc: "100 IM — 25 drill each stroke (fly, back, breast, free)", interval: "On 2:30", focus: "High-rep IM drill sequence before long set" },
        ]},
      { label: "IM 6×100", totalYards: 600, types: ["im"], strokes: [], sets: [
          { id: "s_6hlzdg", reps: 6, dist: 100, desc: "100 IM — 25 each stroke drill, emphasize transitions", interval: "On 2:30", focus: "Full IM drill volume" },
        ]},
      { label: "IM Transition 8×100", totalYards: 800, types: ["im"], strokes: [], sets: [
          { id: "s_zn5tbt", reps: 8, dist: 100, desc: "100 IM — focus on smooth fly→back and breast→free transitions, drill the first 25 of each stroke", interval: "On 2:15", focus: "Transitions are where IM races are won — practice them at volume" },
        ]},
      { label: "IM Stroke Block 1000", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_5cpusl", reps: 4, dist: 100, desc: "Fly drill — single-arm 25 / 3-3-3 25 / full stroke 50", interval: "On 2:30", focus: "Fly mechanics at low intensity" },
          { id: "s_plvs06", reps: 4, dist: 100, desc: "Back drill — single-arm 25 / catch-up back 25 / full stroke 50", interval: "On 2:15", focus: "Back rotation and arm timing" },
          { id: "s_4fqxhx", reps: 2, dist: 100, desc: "Breast drill — 2 kick 1 pull 25 / full stroke 75", interval: "On 2:30", focus: "Breast pull-glide cycle" },
        ]},
      { label: "Pull 3×100", totalYards: 300, types: ["distance"], strokes: [], sets: [
          { id: "s_57bhmv", reps: 3, dist: 100, desc: "Pull (buoy) — easy/moderate, focus on catch", interval: "On 2:15", focus: "Prime the pull pattern before distance work", eq: "pull" },
        ]},
      { label: "Catch-Up 4×100", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_jogiap", reps: 4, dist: 100, desc: "Pull (buoy) — moderate pace, high elbow catch", interval: "On 2:15", focus: "Upper body endurance, long stroke", eq: "pull" },
        ]},
      { label: "Kick + Build (400 yd)", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_no2z4c", reps: 6, dist: 50, desc: "Kick — moderate effort (board optional)", interval: "On 1:45", focus: "Leg conditioning and body position", eq: "kick" },
          { id: "s_7gb4j9", reps: 2, dist: 50, desc: "Freestyle — build from easy to moderate", interval: "On 1:15", focus: "Prime aerobic engine for main set" },
        ]},
      { label: "Kick + Catch-Up (400 yd)", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_vbicpy", reps: 4, dist: 50, desc: "Kick — moderate, focus on body position (board optional)", interval: "On 1:45", focus: "Leg conditioning before distance work", eq: "kick" },
          { id: "s_97iuh5", reps: 2, dist: 100, desc: "Pull (buoy) — long stroke, focus on catch", interval: "On 2:15", focus: "Upper-body endurance setup", eq: "pull" },
        ]},
      { label: "Pull 4×50", totalYards: 200, types: ["distance"], strokes: [], sets: [
          { id: "s_267dbj", reps: 4, dist: 50, desc: "Pull (buoy) — easy, focus on catch engagement", interval: "On 1:15", focus: "Quick pull priming before distance work", eq: "pull" },
        ]},
      { label: "Kick 6×50", totalYards: 300, types: ["distance"], strokes: [], sets: [
          { id: "s_mwizmw", reps: 6, dist: 50, desc: "Kick — moderate, even effort (board optional)", interval: "On 1:45", focus: "Leg priming for distance main set", eq: "kick" },
        ]},
      { label: "Free 4×100", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_zithyb", reps: 4, dist: 100, desc: "Freestyle — long stroke, nose-down position, minimal head movement", interval: "On 2:15", focus: "Head position and body alignment for distance work", eq: "snorkel" },
        ]},
      { label: "Pull 5×100", totalYards: 500, types: ["distance"], strokes: [], sets: [
          { id: "s_v5j73g", reps: 5, dist: 100, desc: "Pull (buoy) — moderate pace, high-elbow catch, long extension", interval: "On 2:15", focus: "Extended upper-body priming for long distance set", eq: "pull" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["distance"], strokes: [], sets: [
          { id: "s_zqdkud", reps: 12, dist: 50, desc: "25 catch-up drill / 25 easy swim — stay relaxed, long stroke", interval: "On 1:15", focus: "Technique-heavy priming before long distance main set" },
        ]},
      { label: "Pull 8×100", totalYards: 800, types: ["distance"], strokes: [], sets: [
          { id: "s_l0kkv3", reps: 8, dist: 100, desc: "Pull (buoy) — aerobic pace, focus on extension and high-elbow catch", interval: "On 2:15", focus: "Sustained upper-body endurance priming before distance work", eq: "pull" },
        ]},
      { label: "Catch-Up 9×100 + Pull 100", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_c19r52", reps: 9, dist: 100, desc: "25 catch-up drill / 75 swim — long stroke, glide through hand entry", interval: "On 2:00", focus: "Stroke-length emphasis at volume" },
          { id: "s_8ua1bk", reps: 1, dist: 100, desc: "Pull (buoy) — easy, recover from drill volume before the main", interval: "On 2:30", focus: "Reset and feel the long stroke from drill", eq: "pull" },
        ]},
      { label: "Build 6×50", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_um69ox", reps: 6, dist: 50, desc: "25 Easy / 25 Build (NOT all-out yet)", interval: "On 1:15", focus: "Activate fast-twitch, save energy for main set" },
        ]},
      { label: "Streamline + Build (300 yd)", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_5pnnzh", reps: 4, dist: 50, desc: "Underwater kick off wall (10m), easy swim out", interval: "On 1:00", focus: "Streamline and underwaters", eq: "underkick" },
          { id: "s_hxiktn", reps: 4, dist: 25, desc: "Tempo build — easy to fast (not all out)", interval: "On :45", focus: "Turnover prep, no sprinting yet" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_m3njh3", reps: 4, dist: 50, desc: "Kick — sharp tempo build (board optional)", interval: "On 1:45", focus: "Activate sprint leg drive", eq: "kick" },
          { id: "s_gsilk3", reps: 2, dist: 100, desc: "Pull (buoy) — fast build, focus on hand speed", interval: "On 2:15", focus: "Activate upper-body power", eq: "pull" },
        ]},
      { label: "Underwater + Build (200 yd)", totalYards: 200, types: ["sprint"], strokes: [], sets: [
          { id: "s_sxrpgf", reps: 4, dist: 25, desc: "ALL OUT 12m sprint + easy swim out", interval: "On :40", focus: "Explosive neuromuscular activation" },
          { id: "s_og9r0z", reps: 4, dist: 25, desc: "Build to 80% — not all out yet", interval: "On :40", focus: "Speed priming without early fatigue" },
        ]},
      { label: "Streamline 6×50", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_301mn0", reps: 6, dist: 50, desc: "25 underwater kick off wall + 25 easy swim", interval: "On 1:00", focus: "Streamline and breakout priming" },
        ]},
      { label: "Build 8×50", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_gzw8rm", reps: 8, dist: 50, desc: "25 sprint effort / 25 easy — ramp to 90% by rep 6–8", interval: "On 1:15", focus: "Graduated sprint activation" },
        ]},
      { label: "Build 10×50", totalYards: 500, types: ["sprint"], strokes: [], sets: [
          { id: "s_s2bk0s", reps: 10, dist: 50, desc: "25 build (easy → 85%) / 25 easy — increase intensity each rep", interval: "On 1:30", focus: "Progressive sprint activation over volume" },
        ]},
      { label: "Build 12×50", totalYards: 600, types: ["sprint"], strokes: [], sets: [
          { id: "s_ug5rw1", reps: 12, dist: 50, desc: "25 build (ramp to 90%) / 25 easy — last 4 reps at 95%", interval: "On 1:30", focus: "Full sprint priming with high volume" },
        ]},
      { label: "Pull 4×100", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_8edxvx", reps: 4, dist: 100, desc: "Pull (buoy) — easy/moderate, relaxed stroke", interval: "On 2:15", focus: "Aerobic priming, conserve energy", eq: "pull" },
        ]},
      { label: "Kick 300", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_ql8q19", reps: 1, dist: 300, desc: "Kick — choice of stroke, easy pace (board optional)", interval: "No interval", focus: "Leg warm-up, build base endurance", eq: "kick" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_a1pcnn", reps: 4, dist: 50, desc: "Kick — easy/moderate, focus on rhythm (board optional)", interval: "On 1:45", focus: "Aerobic leg priming", eq: "kick" },
          { id: "s_jzbkb7", reps: 2, dist: 100, desc: "Pull (buoy) — relaxed stroke, long extension", interval: "On 2:15", focus: "Aerobic upper-body warm-up", eq: "pull" },
        ]},
      { label: "Free 2×100", totalYards: 200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ejsqrj", reps: 2, dist: 100, desc: "Freestyle — smooth stroke, relaxed breathing, easy pace", interval: "On 2:15", focus: "Aerobic priming — stay relaxed" },
        ]},
      { label: "Kick 3×100", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_1jbsaj", reps: 3, dist: 100, desc: "Kick — easy aerobic pace (board optional)", interval: "On 2:15", focus: "Leg priming for long set", eq: "kick" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_w0zfxp", reps: 8, dist: 50, desc: "25 catch-up drill / 25 easy swim — relaxed stroke", interval: "On 1:15", focus: "Stroke efficiency before long aerobic set" },
        ]},
      { label: "Free 6×50", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_fp00v9", reps: 6, dist: 50, desc: "Freestyle — easy pace, flat head, focus on hip rotation and body line", interval: "On 1:15", focus: "Aerobic priming with body alignment focus", eq: "snorkel" },
        ]},
      { label: "Pull 5×100", totalYards: 500, types: ["endurance"], strokes: [], sets: [
          { id: "s_plakdw", reps: 5, dist: 100, desc: "Pull (buoy) — relaxed, smooth stroke, aerobic pace", interval: "On 2:15", focus: "Extended aerobic priming, save legs for long main set", eq: "pull" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["endurance"], strokes: [], sets: [
          { id: "s_k5pkzt", reps: 12, dist: 50, desc: "25 catch-up drill / 25 easy swim — stay completely relaxed throughout", interval: "On 1:15", focus: "Stroke efficiency and aerobic priming before long set" },
        ]},
      { label: "Bilateral Breathing 8×100", totalYards: 800, types: ["endurance"], strokes: [], sets: [
          { id: "s_sty7of", reps: 8, dist: 100, desc: "Freestyle — breathe every 3 strokes the first 50, every 5 strokes the second 50", interval: "On 2:00", focus: "Aerobic priming with breathing-control overlay (lung work)" },
        ]},
      { label: "Pull + Catch-Up Stack 900", totalYards: 900, types: ["endurance"], strokes: [], sets: [
          { id: "s_7tp95m", reps: 6, dist: 100, desc: "Pull (buoy) — relaxed, aerobic pace, long stroke", interval: "On 2:15", focus: "Aerobic upper-body work before endurance main", eq: "pull" },
          { id: "s_n7mxsu", reps: 6, dist: 50, desc: "Catch-up drill — full extension on every stroke, no glide skipping", interval: "On 1:15", focus: "Stroke-length reset between pull and main" },
        ]},
      { label: "Free 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_m1klqy", reps: 8, dist: 50, desc: "25 Drill / 25 Easy Swim — same stroke each rep (your choice)", interval: "On 1:25", focus: "Feel the drill translate into the swim" },
        ]},
      { label: "Kick 4×75", totalYards: 300, types: ["technique"], strokes: [], sets: [
          { id: "s_s3iyno", reps: 4, dist: 75, desc: "Kick 25 / Drill 25 / Swim 25 (same stroke)", interval: "On 1:40", focus: "Layered skill building — kick → drill → apply", eq: "kickPart" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_j9gunu", reps: 4, dist: 50, desc: "Kick — slow, focus on stroke alignment (board optional)", interval: "On 1:45", focus: "Isolate leg technique", eq: "kick" },
          { id: "s_vbrizp", reps: 2, dist: 100, desc: "Pull (buoy) — high-elbow catch, long stroke", interval: "On 2:15", focus: "Isolate upper-body technique", eq: "pull" },
        ]},
      { label: "Free 4×50", totalYards: 200, types: ["technique"], strokes: [], sets: [
          { id: "s_f7k8xv", reps: 4, dist: 50, desc: "25 drill (your choice) / 25 easy swim — feel the difference", interval: "On 1:15", focus: "Brief drill activation" },
        ]},
      { label: "Fingertip Drag 6×50", totalYards: 300, types: ["technique"], strokes: [], sets: [
          { id: "s_ks7fpm", reps: 6, dist: 50, desc: "Fingertip drag drill — 25 drill / 25 swim, same stroke", interval: "On 1:15", focus: "Hand path and elbow height awareness" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_w8ty2v", reps: 8, dist: 50, desc: "25 catch-up drill / 25 swim — focus on full extension", interval: "On 1:15", focus: "Stroke timing and long extension" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_60v8sg", reps: 8, dist: 50, desc: "Freestyle — 25 catch-up drill / 25 swim, flat head position", interval: "On 1:15", focus: "Stroke mechanics with consistent head position", eq: "snorkel" },
        ]},
      { label: "Free 10×50", totalYards: 500, types: ["technique"], strokes: [], sets: [
          { id: "s_rqn2u0", reps: 10, dist: 50, desc: "25 drill (your choice) / 25 swim — isolate and apply each rep", interval: "On 1:20", focus: "High-rep technique priming before technique set" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["technique"], strokes: [], sets: [
          { id: "s_ita50t", reps: 12, dist: 50, desc: "25 drill / 25 swim — rotate drills every 2 reps (catch-up, fingertip drag, side kick)", interval: "On 1:20", focus: "Broad technique warm-up with variety" },
        ]},
      { label: "Kick 4×75", totalYards: 300, types: ["mixed"], strokes: [], sets: [
          { id: "s_8kywgp", reps: 4, dist: 75, desc: "Kick 25 / Drill 25 / Swim 25 — stroke choice", interval: "On 1:40", focus: "All-around activation before varied main set", eq: "kickPart" },
        ]},
      { label: "Tempo (400 yd)", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_mf642o", reps: 6, dist: 50, desc: "Drill/Swim by 25 — your stroke choice", interval: "On 1:00", focus: "Skill warm-up" },
          { id: "s_x7h3i2", reps: 4, dist: 25, desc: "Fast tempo (not all out) — choice stroke", interval: "On :45", focus: "Activate speed system" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_f8zp3f", reps: 4, dist: 50, desc: "Kick — moderate, choice of stroke (board optional)", interval: "On 1:45", focus: "Leg activation", eq: "kick" },
          { id: "s_hr5ltr", reps: 2, dist: 100, desc: "Pull (buoy) — moderate, smooth rotation", interval: "On 2:15", focus: "Upper-body warm-up before mix", eq: "pull" },
        ]},
      { label: "Free (200 yd)", totalYards: 200, types: ["mixed"], strokes: [], sets: [
          { id: "s_u9nozq", reps: 4, dist: 25, desc: "Choice stroke drill — activate technique", interval: "On :45", focus: "Brief all-around drill activation" },
          { id: "s_88bd3q", reps: 4, dist: 25, desc: "Choice stroke swim — apply the drill", interval: "On :45", focus: "Drill translation before mixed set" },
        ]},
      { label: "Free 3×100", totalYards: 300, types: ["mixed"], strokes: [], sets: [
          { id: "s_c5isew", reps: 3, dist: 100, desc: "Mixed stroke — 50 drill of choice / 50 swim", interval: "On 2:15", focus: "Variety technique priming" },
        ]},
      { label: "Free 4×100", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_po8zb7", reps: 4, dist: 100, desc: "Alternating stroke — 50 stroke A drill / 50 stroke B drill", interval: "On 2:15", focus: "Cross-stroke skill work" },
        ]},
      { label: "Free 8×50", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_6qy59n", reps: 8, dist: 50, desc: "Freestyle — 25 drill of choice / 25 swim, flat head throughout", interval: "On 1:15", focus: "Technique variety with consistent head position", eq: "snorkel" },
        ]},
      { label: "Free 10×50", totalYards: 500, types: ["mixed"], strokes: [], sets: [
          { id: "s_jjg6ip", reps: 10, dist: 50, desc: "25 drill choice / 25 swim — alternate stroke every other rep", interval: "On 1:15", focus: "All-around technique priming before mixed set" },
        ]},
      { label: "Kick 8×75", totalYards: 600, types: ["mixed"], strokes: [], sets: [
          { id: "s_muar1s", reps: 8, dist: 75, desc: "25 kick / 25 drill / 25 swim — stroke choice per rep", interval: "On 2:00", focus: "Layered skill warm-up across strokes", eq: "kickPart" },
        ]},
      { label: "All-Stroke Drill 8×100", totalYards: 800, types: ["mixed"], strokes: [], sets: [
          { id: "s_8va5f7", reps: 8, dist: 100, desc: "25 fly drill / 25 back drill / 25 breast drill / 25 free smooth — round 1 = drills, round 2 = swim each 25", interval: "On 2:15", focus: "Broad-stroke skill warm-up at volume" },
        ]},
      { label: "Layered Mixed 1000", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_gtegs5", reps: 6, dist: 100, desc: "25 kick (board) / 25 drill choice / 50 swim — stroke choice per rep", interval: "On 2:15", focus: "Layered skill priming across strokes", eq: "kickPart" },
          { id: "s_t997a1", reps: 4, dist: 100, desc: "Freestyle — 25 drill of choice / 75 swim, apply the drill into the swim", interval: "On 2:00", focus: "Freestyle technique-to-swim translation" },
        ]},
      { label: "Back Single-Arm 4×75", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_wmm8ic", reps: 4, dist: 75, desc: "25 single-arm backstroke / 25 double-arm back drill / 25 backstroke swim", interval: "On 2:30", focus: "Lead arm extension and stroke timing in back" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_9734m2", reps: 6, dist: 50, desc: "Backstroke kick — 25 streamline (no board) / 25 with board", interval: "On 1:45", focus: "Body position on the back, hip drive", eq: "kick" },
        ]},
      { label: "Back Kick + Catch-Up (400 yd)", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_5nea8t", reps: 4, dist: 50, desc: "Backstroke kick — easy/moderate (board optional)", interval: "On 1:45", focus: "Leg conditioning for back", eq: "kick" },
          { id: "s_6gcq02", reps: 2, dist: 100, desc: "Pull (buoy) — backstroke pull pattern, focus on catch", interval: "On 2:15", focus: "Upper-body work for back", eq: "pull" },
        ]},
      { label: "Back Single-Arm 4×50", totalYards: 200, types: [], strokes: ["back"], sets: [
          { id: "s_il5zir", reps: 4, dist: 50, desc: "Backstroke — 25 single-arm / 25 full stroke", interval: "On 1:30", focus: "Lead arm and rotation activation" },
        ]},
      { label: "Back Single-Arm 3×100", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_g64ek2", reps: 3, dist: 100, desc: "Backstroke — 25 single-arm (L) / 25 single-arm (R) / 50 full stroke", interval: "On 2:30", focus: "Each arm isolated then integrated" },
        ]},
      { label: "Back 8×50", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_uls9dy", reps: 8, dist: 50, desc: "25 back elbow-lead drill / 25 backstroke swim", interval: "On 1:30", focus: "High-elbow catch on backstroke" },
        ]},
      { label: "Back Single-Arm 10×50", totalYards: 500, types: [], strokes: ["back"], sets: [
          { id: "s_1r6ng6", reps: 10, dist: 50, desc: "25 single-arm backstroke / 25 full backstroke — alternate lead arm each rep", interval: "On 1:30", focus: "High-rep arm isolation before back set" },
        ]},
      { label: "Back Single-Arm 6×100", totalYards: 600, types: [], strokes: ["back"], sets: [
          { id: "s_ucgisp", reps: 6, dist: 100, desc: "Backstroke — 25 single-arm (L) / 25 single-arm (R) / 50 full stroke", interval: "On 2:30", focus: "Full arm isolation and integration at volume" },
        ]},
      { label: "Breast Pull 4×75", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_xnxvxa", reps: 4, dist: 75, desc: "25 breast pull-only (no kick) / 25 breast kick (board) / 25 breast swim", interval: "On 2:00", focus: "Isolate then integrate the breast components" },
        ]},
      { label: "Breast Kick 6×50", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_j2rssc", reps: 6, dist: 50, desc: "Breaststroke kick (board) — focus on power and recovery", interval: "On 1:30", focus: "Breast kick rhythm and power", eq: "kick" },
        ]},
      { label: "Breast Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_1pyp1z", reps: 4, dist: 50, desc: "Breaststroke kick (board) — moderate effort", interval: "On 1:30", focus: "Power kick", eq: "kick" },
          { id: "s_484ta5", reps: 2, dist: 100, desc: "Pull (buoy) — breaststroke pull pattern", interval: "On 2:30", focus: "Strong pull-glide cycle", eq: "pull" },
        ]},
      { label: "Breast Pull 4×50", totalYards: 200, types: [], strokes: ["breast"], sets: [
          { id: "s_8yk2ei", reps: 4, dist: 50, desc: "25 breast pull-only / 25 full breaststroke — feel the integration", interval: "On 1:30", focus: "Pull activation before kick integration" },
        ]},
      { label: "Breast Pull 3×100", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_phxkmu", reps: 3, dist: 100, desc: "Breast — 25 kick only (board) / 25 pull only / 50 full stroke", interval: "On 2:30", focus: "Isolated-then-integrated breast work" },
        ]},
      { label: "Breast Pull 8×50", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_0mnssg", reps: 8, dist: 50, desc: "25 breast timing drill (2-kick 1-pull) / 25 full breaststroke", interval: "On 1:30", focus: "Timing drill into race stroke" },
        ]},
      { label: "Breast Pull 10×50", totalYards: 500, types: [], strokes: ["breast"], sets: [
          { id: "s_jit0io", reps: 10, dist: 50, desc: "25 breaststroke pull-only (no kick, glide) / 25 full breaststroke", interval: "On 1:30", focus: "High-rep pull activation before breast set" },
        ]},
      { label: "Breast Pull 12×50", totalYards: 600, types: [], strokes: ["breast"], sets: [
          { id: "s_bsful5", reps: 12, dist: 50, desc: "25 breaststroke pull-only / 25 full breaststroke — feel pull integration on every rep", interval: "On 1:30", focus: "High-rep pull priming for long breast set" },
        ]},
      { label: "Fly Single-Arm 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_r98mpq", reps: 6, dist: 50, desc: "25 fly drill (3-3-3 or single-arm) / 25 freestyle easy", interval: "On 1:30", focus: "Build fly tolerance with active recovery" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_mfz3u0", reps: 6, dist: 50, desc: "Dolphin kick — alternate streamline on stomach / on back", interval: "On 1:45", focus: "Dolphin rhythm without arm fatigue", eq: "kick" },
        ]},
      { label: "Fly Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_rx6xk2", reps: 4, dist: 50, desc: "Dolphin kick (board) — focus on rhythm", interval: "On 1:45", focus: "Sustained kick base for fly", eq: "kick" },
          { id: "s_4as5h3", reps: 2, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 freestyle pull (recovery in arm pattern)", interval: "On 2:00", focus: "Build fly catch with free recovery", eq: "pull" },
        ]},
      { label: "Fly Streamline 4×50", totalYards: 200, types: [], strokes: ["fly"], sets: [
          { id: "s_h8qznv", reps: 4, dist: 50, desc: "25 dolphin kick streamline / 25 easy freestyle", interval: "On 1:15", focus: "Dolphin activation without arm fatigue" },
        ]},
      { label: "Fly Single-Arm 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_2it429", reps: 6, dist: 50, desc: "25 single-arm fly (alternate L/R each rep) / 25 easy freestyle", interval: "On 1:30", focus: "Arm mechanics and undulation before fly set" },
        ]},
      { label: "Fly Build 8×50", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_qgdvgr", reps: 8, dist: 50, desc: "25 fly drill (your choice) / 25 freestyle easy", interval: "On 1:30", focus: "Build fly tolerance, recover on free" },
        ]},
      { label: "Fly Single-Arm 10×50", totalYards: 500, types: [], strokes: ["fly"], sets: [
          { id: "s_td1cm8", reps: 10, dist: 50, desc: "25 fly drill (single-arm or 3-3-3) / 25 freestyle easy", interval: "On 1:30", focus: "Extended fly drill volume with free recovery" },
        ]},
      { label: "Fly Single-Arm 12×50", totalYards: 600, types: [], strokes: ["fly"], sets: [
          { id: "s_aci739", reps: 12, dist: 50, desc: "25 fly drill (alternate: single-arm L, single-arm R, 3-3-3 each group) / 25 freestyle easy", interval: "On 1:30", focus: "High-rep fly drill variety before main set" },
        ]},
    ];

    const MAIN_OPTIONS = [
      { label: "IM Descend", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_h2kymp", reps: 2, dist: 200, desc: "IM — easy effort, focus on stroke order", interval: "On 6:00", focus: "Smooth fly→back→breast→free transitions" },
          { id: "s_szvl59", reps: 4, dist: 100, desc: "IM — moderate to strong, descend 1→4", interval: "On 3:00", focus: "Each rep a little faster" },
          { id: "s_m98b1o", reps: 4, dist: 50, desc: "IM — fast effort, hold technique under fatigue", interval: "On 1:30", focus: "Power through each stroke" },
        ]},
      { label: "Stroke Focus + IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_j5knju", reps: 4, dist: 100, desc: "IM — moderate, smooth transitions", interval: "On 3:00", focus: "Establish stroke order under load" },
          { id: "s_gdx8o7", reps: 8, dist: 50, desc: "2 each stroke: fly/back/breast/free, descend each pair", interval: "On 1:30", focus: "Individual stroke speed" },
          { id: "s_0z5ifa", reps: 4, dist: 75, desc: "50 IM + 25 Free strong", interval: "On 2:30", focus: "Combine strokes, finish fast" },
        ]},
      { label: "IM Pyramid", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_15nj9b", reps: 1, dist: 100, desc: "IM — easy", interval: "On 2:30", focus: "Set your baseline" },
          { id: "s_4t80m8", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:00", focus: "Build into it" },
          { id: "s_traa7t", reps: 1, dist: 400, desc: "IM — strong, sustained", interval: "On 12:00", focus: "Test your aerobic fitness" },
          { id: "s_0xoplm", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:00", focus: "Hang on — tired strokes" },
          { id: "s_1xhrq4", reps: 1, dist: 100, desc: "IM — ALL OUT finish", interval: "On 3:00", focus: "Empty the tank" },
        ]},
      { label: "Broken IM 200s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_g7uahw", reps: 4, dist: 200, desc: "Broken IM — 50 fly :10R, 50 back :10R, 50 breast :10R, 50 free", interval: "On 5:00", focus: "Hold race pace through each stroke with mini-rests" },
          { id: "s_rq522n", reps: 4, dist: 75, desc: "IM transitions — 50 IM + 25 Free strong", interval: "On 2:30", focus: "Sharpen the change-overs between strokes" },
        ]},
      { label: "IM Stroke Rotation", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_4hij3d", reps: 4, dist: 100, desc: "Choice non-free stroke — strong, hold form", interval: "On 2:15", focus: "Pick your weakest stroke for extra work" },
          { id: "s_y3kebp", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:00", focus: "Apply per-stroke quality to the full IM" },
          { id: "s_6liaop", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4 (close the set)", interval: "On 2:10", focus: "Finish the set with speed in the legs" },
        ]},
      { label: "Pull + IM Combo", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_vc9wt8", reps: 3, dist: 200, desc: "Pull (buoy) — alternate strokes by 50 (free / non-free)", interval: "On 4:15", focus: "Long-stroke pull pattern across IM strokes", eq: "pull" },
          { id: "s_zfoevz", reps: 5, dist: 100, desc: "IM — moderate to fast, descend 1→5", interval: "On 3:00", focus: "Build from controlled to strong, smooth transitions" },
        ]},
      { label: "Reverse-Order IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_ukwwki", reps: 4, dist: 100, desc: "REVERSE IM — free, breast, back, fly (last 25)", interval: "On 3:00", focus: "Test stroke order under reversed sequence" },
          { id: "s_5q7t1l", reps: 4, dist: 100, desc: "Normal IM — fly, back, breast, free", interval: "On 3:00", focus: "Compare to your normal order" },
          { id: "s_kzzdz5", reps: 4, dist: 50, desc: "Choice non-free stroke — moderate to fast", interval: "On 1:15", focus: "Sustain non-free stroke effort" },
        ]},
      { label: "IM Race Pace 100s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_hc316s", reps: 1, dist: 200, desc: "IM — easy/moderate warm-up to set", interval: "On 6:00", focus: "Settle into IM rhythm" },
          { id: "s_es6f6f", reps: 8, dist: 100, desc: "IM — at race pace, full recovery", interval: "On 2:30", focus: "Hit race pace consistently" },
          { id: "s_xmj497", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:30", focus: "Cool down" },
        ]},
      { label: "Broken 400 IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_ugtv2l", reps: 2, dist: 400, desc: "Broken 400 IM — :15 rest after each 100", interval: "On 12:00", focus: "Race-pace 400 IM in segments" },
          { id: "s_j6a18j", reps: 1, dist: 100, desc: "Easy choice — recovery between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_z067aq", reps: 4, dist: 50, desc: "Choice IM stroke — strong, finishing", interval: "On 1:30", focus: "Top off with stroke speed" },
        ]},
      { label: "Stroke-Drop IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_z0j97l", reps: 2, dist: 100, desc: "100 IM — full, all four strokes", interval: "On 3:00", focus: "Baseline full IM" },
          { id: "s_22wwlh", reps: 2, dist: 75, desc: "75 IM — drop the fly (back, breast, free)", interval: "On 2:30", focus: "Three strokes" },
          { id: "s_wccvq1", reps: 2, dist: 50, desc: "50 IM — back and breast only", interval: "On 1:30", focus: "Stroke transitions only" },
          { id: "s_680b69", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Reset" },
          { id: "s_y7714b", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4", interval: "On 2:15", focus: "Build freestyle finish" },
          { id: "s_lxeg92", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Top speed close" },
        ]},
      { label: "Threshold 200 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_gc7i2t", reps: 6, dist: 200, desc: "200 IM — threshold pace (RPE 7-8/10)", interval: "On 6:00", focus: "Hold threshold across IM strokes" },
        ]},
      { label: "Mini IM Pyramid", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_roc82i", reps: 1, dist: 25, desc: "Choice non-free stroke — fast", interval: "On :45", focus: "Stroke speed" },
          { id: "s_leqswr", reps: 1, dist: 50, desc: "IM — 12.5 each stroke, fast", interval: "On 1:30", focus: "All four under fatigue" },
          { id: "s_b1d4ju", reps: 1, dist: 75, desc: "75 IM — back/breast/free, fast", interval: "On 2:30", focus: "Three strokes hard" },
          { id: "s_72or27", reps: 1, dist: 100, desc: "100 IM — full, fast", interval: "On 2:30", focus: "Hold form across all four" },
          { id: "s_iy4zxw", reps: 1, dist: 200, desc: "200 IM — moderate, smooth", interval: "On 6:00", focus: "Apex of pyramid" },
          { id: "s_s300zw", reps: 1, dist: 100, desc: "100 IM — fast, fatigued", interval: "On 2:30", focus: "Sprint home" },
          { id: "s_rh69ak", reps: 1, dist: 75, desc: "75 IM — back/breast/free, fast", interval: "On 2:30", focus: "Hold the line" },
          { id: "s_kt5coy", reps: 1, dist: 50, desc: "IM — 12.5 each stroke, fast", interval: "On 1:30", focus: "Tight transitions" },
          { id: "s_i4qjuc", reps: 1, dist: 25, desc: "Choice non-free stroke — ALL OUT", interval: "On :45", focus: "Empty the tank" },
          { id: "s_04t31o", reps: 1, dist: 400, desc: "Easy choice — full cool-down recovery", interval: "On 9:00", focus: "Aerobic close-out" },
        ]},
      { label: "IM Sprint Set", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_l4l9ii", reps: 8, dist: 50, desc: "50 IM — 12.5 each stroke, fast", interval: "On 1:30", focus: "Top speed across all four strokes" },
          { id: "s_0jpkjl", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:30", focus: "Active recovery" },
          { id: "s_keolcn", reps: 8, dist: 50, desc: "Choice IM stroke — ALL OUT (rotate stroke each rep)", interval: "On 1:30", focus: "Single-stroke speed work" },
        ]},
      { label: "IM with Free Sprint Finish", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_p0j0z5", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:00", focus: "Build IM endurance" },
          { id: "s_5w6unj", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Reset" },
          { id: "s_evcvmb", reps: 4, dist: 50, desc: "Freestyle — ALL OUT, max turnover", interval: "On 1:30", focus: "Sprint freestyle after IM fatigue" },
        ]},
      { label: "Aerobic IM Volume", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_ehlgy9", reps: 12, dist: 100, desc: "100 IM — easy/moderate (RPE 6/10), focus on transitions", interval: "On 2:30", focus: "Build IM volume at aerobic pace" },
        ]},
      { label: "IM Pull + Kick Set", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_vb8xp2", reps: 4, dist: 50, desc: "IM order kick — 12.5 each stroke", interval: "On 1:45", focus: "Kick rhythm across IM strokes", eq: "kick" },
          { id: "s_o6mgld", reps: 4, dist: 100, desc: "Pull (buoy) — IM stroke focus, alternate strokes", interval: "On 2:15", focus: "Upper-body work across strokes", eq: "pull" },
          { id: "s_cb46sk", reps: 5, dist: 100, desc: "100 IM — moderate, descend 1→5", interval: "On 3:00", focus: "Apply kick + pull into full IM" },
        ]},
      { label: "100 IM Build", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_ykfkle", reps: 4, dist: 100, desc: "IM — moderate, settle into stroke order", interval: "On 3:00", focus: "Smooth transitions" },
          { id: "s_xbyvdu", reps: 4, dist: 100, desc: "IM — strong, descend 1→4 within block", interval: "On 3:00", focus: "Each rep faster" },
          { id: "s_btdqrb", reps: 1, dist: 200, desc: "IM — race pace test (200)", interval: "On 6:00", focus: "Apply earlier work to a race-distance effort" },
          { id: "s_y3m8l5", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Active recovery" },
        ]},
      { label: "Specialty + Free 75s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_bsyyo0", reps: 8, dist: 75, desc: "50 specialty stroke (your choice) + 25 free strong", interval: "On 1:45", focus: "Hone your IM weakness with free finish" },
          { id: "s_xxxv0k", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:00", focus: "Apply specialty work into full IM" },
          { id: "s_ucbqns", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "5×200 IM Descend", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_dj15s3", reps: 5, dist: 200, desc: "200 IM — descend 1→5 (each one faster)", interval: "On 6:00", focus: "Build IM pace progressively" },
          { id: "s_hn778d", reps: 2, dist: 50, desc: "Choice non-free — moderate, close-out", interval: "On 1:15", focus: "Top off stroke speed" },
        ]},
      { label: "Race-Pace 50 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_27tvix", reps: 12, dist: 50, desc: "50 IM — race pace (12.5 each stroke)", interval: "On 1:30", focus: "Race-pace IM with manageable reps" },
          { id: "s_qhy79z", reps: 4, dist: 100, desc: "Easy freestyle — recovery cycles", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Stroke Block Endurance", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_oc2x6x", reps: 1, dist: 100, desc: "Butterfly — moderate (or 25 fly + 75 free for sustainability)", interval: "On 2:30", focus: "Open fly endurance" },
          { id: "s_srfjsm", reps: 3, dist: 100, desc: "Backstroke — moderate, smooth tempo", interval: "On 2:45", focus: "Backstroke base" },
          { id: "s_7rfff3", reps: 3, dist: 100, desc: "Breaststroke — moderate, drive with kick", interval: "On 3:15", focus: "Breast tempo" },
          { id: "s_0bys91", reps: 3, dist: 100, desc: "Freestyle — strong, finish the set", interval: "On 2:15", focus: "Carry IM fitness into free" },
          { id: "s_ej2us9", reps: 2, dist: 100, desc: "100 IM — moderate, integrate all four", interval: "On 3:00", focus: "Apply block work into full IM" },
        ]},
      { label: "Even-Effort 200 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_1dmf1m", reps: 6, dist: 200, desc: "200 IM — hold the SAME time on all 6 reps", interval: "On 6:00", focus: "Pacing discipline across IM strokes" },
        ]},
      { label: "Reverse Pyramid IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_0ug0ar", reps: 1, dist: 200, desc: "IM — moderate, smooth transitions", interval: "On 6:00", focus: "Establish baseline pace" },
          { id: "s_lj01kf", reps: 1, dist: 150, desc: "IM — moderate to strong", interval: "On 4:30", focus: "Pick up pace" },
          { id: "s_7y6x54", reps: 1, dist: 100, desc: "IM — strong", interval: "On 2:30", focus: "Threshold IM" },
          { id: "s_hd4m17", reps: 1, dist: 75, desc: "IM (drop fly) — fast", interval: "On 2:30", focus: "Three strokes hard" },
          { id: "s_2nk04v", reps: 1, dist: 50, desc: "IM — fast (12.5 each stroke)", interval: "On 1:30", focus: "All four under load" },
          { id: "s_9nin3p", reps: 1, dist: 25, desc: "Choice non-free — ALL OUT", interval: "On :45", focus: "Final blast" },
          { id: "s_qal8gb", reps: 1, dist: 200, desc: "Easy choice — recovery between blocks", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_hrjdxv", reps: 4, dist: 50, desc: "Choice IM stroke — fast finishing 50s", interval: "On 1:30", focus: "Top off stroke speed" },
        ]},
      { label: "8×100 IM Fast", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_1z1qem", reps: 1, dist: 200, desc: "IM — easy, settle in", interval: "On 6:00", focus: "Open IM rhythm" },
          { id: "s_xuwnfb", reps: 8, dist: 100, desc: "100 IM — fast, descend 1-4 then 5-8", interval: "On 2:30", focus: "Two descending blocks" },
          { id: "s_vu2izd", reps: 1, dist: 100, desc: "Easy backstroke — cool down", interval: "On 2:45", focus: "Active recovery" },
        ]},
      { label: "Negative-Split 200 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_7iiw1p", reps: 5, dist: 200, desc: "200 IM — negative split (second 100 faster than first)", interval: "On 6:00", focus: "Practice closing speed across IM strokes" },
        ]},
      { label: "Sprint-Finish 100 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_1n8f9d", reps: 8, dist: 100, desc: "100 IM — first 75 moderate, last 25 free ALL OUT", interval: "On 3:00", focus: "Sprint into the wall after IM strokes" },
          { id: "s_apitr4", reps: 1, dist: 200, desc: "Easy backstroke — cool", interval: "On 5:30", focus: "Recover" },
        ]},
      { label: "Build-Then-Sprint 75s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_g6uif3", reps: 12, dist: 75, desc: "75 IM (back/breast/free) — 25 build / 25 strong / 25 fast", interval: "On 2:30", focus: "Three-pace progression on each rep" },
          { id: "s_lsjsdc", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "300 IM Repeats", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_l83qjc", reps: 4, dist: 300, desc: "300 IM — moderate, even pace through transitions", interval: "On 8:30", focus: "Mid-distance IM endurance" },
        ]},
      { label: "IM Pull Sandwich", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_5j4km6", reps: 4, dist: 100, desc: "Pull (buoy) — IM order rotation, alternate strokes by 25", interval: "On 2:15", focus: "Upper-body IM work", eq: "pull" },
          { id: "s_bl77et", reps: 4, dist: 100, desc: "100 IM — moderate, integrate pull mechanics", interval: "On 3:00", focus: "Apply pull catch into IM" },
          { id: "s_rxrrp2", reps: 4, dist: 75, desc: "Pull (buoy) — 50 stroke + 25 free", interval: "On 1:40", focus: "Bridge pull pattern across strokes", eq: "pull" },
        ]},
      { label: "IM Kick Endurance", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_dezzgp", reps: 8, dist: 50, desc: "IM order kick — 12.5 yards each stroke (board optional)", interval: "On 1:30", focus: "Kick rhythm across IM strokes", eq: "kick" },
          { id: "s_fat9v2", reps: 4, dist: 100, desc: "100 IM — moderate, drive with the legs", interval: "On 3:00", focus: "Apply kick into full IM" },
          { id: "s_3ca9ka", reps: 1, dist: 300, desc: "Easy backstroke — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Broken 200 IM Set", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_va4ual", reps: 6, dist: 200, desc: "Broken 200 IM — :10 rest after each 50", interval: "On 5:00", focus: "Race-pace IM in 50-yard segments" },
        ]},
      { label: "Stroke-Choice Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_hdici1", reps: 16, dist: 50, desc: "50 — rotate IM stroke each rep (fly, back, breast, free), ALL OUT", interval: "On 1:30", focus: "Single-stroke speed across all four" },
          { id: "s_4wcclh", reps: 1, dist: 200, desc: "Easy backstroke — close-out", interval: "On 5:30", focus: "Cool down" },
        ]},
      { label: "Tempo + Threshold IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_sfo2jr", reps: 6, dist: 100, desc: "100 IM — tempo (RPE 7/10)", interval: "On 3:00", focus: "Sustainable IM tempo" },
          { id: "s_9dc2or", reps: 4, dist: 100, desc: "100 IM — threshold (RPE 8/10)", interval: "On 3:00", focus: "Push IM threshold" },
        ]},
      { label: "75s Specialty + 200 IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_0z10pg", reps: 4, dist: 75, desc: "Specialty stroke (your choice) — strong", interval: "On 1:45", focus: "Race-stroke speed work" },
          { id: "s_oogkwz", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:00", focus: "Mid-distance IM" },
        ]},
      { label: "IM Sandwich 400s", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_cicwii", reps: 3, dist: 400, desc: "400 — 100 IM easy / 200 IM strong / 100 IM ALL OUT", interval: "On 12:00", focus: "Three-tier effort within each 400" },
        ]},
      { label: "200 IM + Free Anchors", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_zaor9h", reps: 4, dist: 300, desc: "200 IM moderate + 100 freestyle strong", interval: "On 8:30", focus: "IM into a fast freestyle anchor" },
        ]},
      { label: "Continuous 1000 IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_0dmn6q", reps: 1, dist: 1000, desc: "Continuous IM — 250 each stroke (fly→back→breast→free) at moderate pace", interval: "On 22:00", focus: "Marathon IM — manage effort across all four" },
        ]},
      { label: "400 IM Race Test", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_y67qt2", reps: 1, dist: 400, desc: "400 IM — race-pace simulation, even effort", interval: "On 12:00", focus: "Test 400 IM fitness" },
          { id: "s_3j267l", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_7etu72", reps: 4, dist: 50, desc: "Choice IM stroke — fast", interval: "On 1:30", focus: "Speed work after race effort" },
          { id: "s_l7iq01", reps: 1, dist: 300, desc: "Easy backstroke — cool", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Locomotive 100 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_mvo1w5", reps: 10, dist: 100, desc: "100 IM — first 25 easy, increase pace each 25 (locomotive)", interval: "On 3:00", focus: "Build pace within each 100 across IM strokes" },
        ]},
      { label: "Fly-Bookend IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_930mvx", reps: 8, dist: 100, desc: "25 fly fast + 50 IM (back/breast/free) + 25 fly fast", interval: "On 3:00", focus: "Fly bookends test fly endurance" },
          { id: "s_g8eah1", reps: 1, dist: 200, desc: "Easy backstroke — cool", interval: "On 5:30", focus: "Cool down" },
        ]},
      { label: "Last-50-Fast IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_cuxqb7", reps: 8, dist: 100, desc: "100 IM — first 50 moderate, last 50 ALL OUT (free)", interval: "On 3:00", focus: "Closing speed with tired strokes" },
          { id: "s_9ci7ze", reps: 4, dist: 50, desc: "Choice IM stroke — fast", interval: "On 1:30", focus: "Top off stroke speed" },
        ]},
      { label: "Threshold 150 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_xw7do2", reps: 8, dist: 150, desc: "150 IM (drop fly: back/breast/free) — threshold pace", interval: "On 3:15", focus: "Sustained threshold across three strokes" },
        ]},
      { label: "Drop-Fly 75 Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_j9i3w0", reps: 12, dist: 75, desc: "75 IM (drop fly: back/breast/free) — fast", interval: "On 2:30", focus: "Race-pace 3-stroke IM" },
          { id: "s_9f4zh7", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
        ]},
      { label: "RPE-Stepped IM", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_erh503", reps: 4, dist: 200, desc: "200 IM — RPE 6/10 (easy)", interval: "On 6:00", focus: "Aerobic IM base" },
          { id: "s_vvod9d", reps: 4, dist: 100, desc: "100 IM — RPE 7/10 (moderate)", interval: "On 3:00", focus: "Mid-zone tempo" },
          { id: "s_qfjirx", reps: 4, dist: 50, desc: "50 IM — RPE 9/10 (fast)", interval: "On 1:30", focus: "Sprint zone across IM" },
        ]},
      { label: "IM 200s Descend", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_a145ej", reps: 7, dist: 200, desc: "200 IM — descend 1→7 (each rep slightly faster)", interval: "On 6:00", focus: "Progressive IM speed over seven 200s" },
        ]},
      { label: "IM 400s + 200s", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_s9k7tz", reps: 2, dist: 400, desc: "400 IM — strong, moderate rest", interval: "On 9:30", focus: "Full-distance IM fitness" },
          { id: "s_de0uu0", reps: 3, dist: 200, desc: "200 IM — race pace, apply 400 conditioning", interval: "On 5:30", focus: "Speed after the long IMs" },
        ]},
      { label: "Reverse-Order 100s", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_ixay68", reps: 8, dist: 100, desc: "Reverse IM — free, breast, back, fly (last 25) — strong", interval: "On 3:00", focus: "Stroke order test in reverse" },
          { id: "s_n2lote", reps: 4, dist: 50, desc: "Freestyle — fast", interval: "On 1:15", focus: "Speed close-out" },
        ]},
      { label: "Power IM Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_0pqlxi", reps: 8, dist: 50, desc: "50 IM stroke choice — ALL OUT (rotate stroke)", interval: "On 1:30", focus: "Pure stroke speed" },
          { id: "s_wkhyxw", reps: 4, dist: 100, desc: "100 IM — moderate, integrate the speed", interval: "On 3:00", focus: "Apply sprint work to full IM" },
          { id: "s_rph63e", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:30", focus: "Cool down" },
        ]},
      { label: "IM Volume Ladder", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_r48swv", reps: 1, dist: 400, desc: "400 IM — aerobic effort, smooth transitions", interval: "On 9:30", focus: "Full IM distance — establish your pace" },
          { id: "s_9qox9g", reps: 2, dist: 200, desc: "200 IM — match or build on 400 pace", interval: "On 5:00", focus: "Maintain stroke quality as distance drops" },
          { id: "s_9vd8e0", reps: 4, dist: 100, desc: "100 IM — strong, best effort of the set", interval: "On 2:30", focus: "Apply full IM rhythm at shorter distance" },
        ]},
      { label: "IM Ascending Ladder", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_xbulf3", reps: 2, dist: 100, desc: "100 IM — moderate pace, smooth transitions", interval: "On 2:30", focus: "Set your baseline — hold this pace" },
          { id: "s_hzx7sb", reps: 2, dist: 200, desc: "200 IM — same effort, longer distance", interval: "On 5:00", focus: "Extend the IM as fatigue builds" },
          { id: "s_izk76q", reps: 1, dist: 400, desc: "400 IM — strong, finish with what you have", interval: "On 9:30", focus: "Full IM at the top of the ladder" },
        ]},
      { label: "IM 400s + Speed", totalYards: 1500, types: ["im"], strokes: [], sets: [
          { id: "s_ro44nr", reps: 3, dist: 400, desc: "400 IM — 100 each stroke, moderate pace, smooth transitions", interval: "On 9:30", focus: "IM distance volume" },
          { id: "s_hzbb0a", reps: 3, dist: 100, desc: "100 IM — race pace, crisp transitions", interval: "On 2:30", focus: "Speed layer after distance" },
        ]},
      { label: "IM Big Four 400s", totalYards: 2000, types: ["im"], strokes: [], sets: [
          { id: "s_q5tor0", reps: 4, dist: 400, desc: "400 IM — 100 each stroke, moderate pace, maintain form", interval: "On 9:30", focus: "Full IM distance volume" },
          { id: "s_ujiplu", reps: 4, dist: 100, desc: "100 IM — race pace, push the transitions", interval: "On 2:30", focus: "Speed finish after distance block" },
        ]},
      { label: "IM Marathon Six 400s", totalYards: 2400, types: ["im"], strokes: [], sets: [
          { id: "s_ew5wyj", reps: 6, dist: 400, desc: "400 IM — 100 each stroke, moderate, hold form throughout", interval: "On 9:30", focus: "High-volume IM endurance" },
        ]},
      { label: "IM 100s Repeat Unit", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_vi4uzd", reps: 4, dist: 100, desc: "100 IM — 25 each stroke in order, smooth transitions", interval: "On 2:30", focus: "IM efficiency and stroke transitions — designed to repeat 2–3×" },
        ]},
      { label: "IM Stroke Focus Repeat Unit", totalYards: 500, types: ["im"], strokes: [], sets: [
          { id: "s_nzc67v", reps: 4, dist: 100, desc: "100 IM — 25 each stroke, focus on weakest stroke each round", interval: "On 2:30", focus: "IM stroke balance" },
          { id: "s_vrbyq8", reps: 2, dist: 50, desc: "50 IM — race-pace, crisp transitions", interval: "On 1:15", focus: "Speed layer to close the round" },
        ]},
      { label: "IM Broken 200 Repeat Unit", totalYards: 600, types: ["im"], strokes: [], sets: [
          { id: "s_d4pl8l", reps: 2, dist: 200, desc: "200 IM — 50 each stroke, moderate pace, maintain form", interval: "On 4:45", focus: "Full IM cycle at controlled effort" },
          { id: "s_ay4wyg", reps: 4, dist: 50, desc: "50 choice stroke — not free, work your weakest", interval: "On 1:15", focus: "Specialty stroke volume each round" },
        ]},
      { label: "Five 400 IMs", totalYards: 2000, types: ["im"], strokes: [], sets: [
          { id: "s_4gk40b", reps: 5, dist: 400, desc: "400 IM — 100 of each stroke, moderate effort, hold form across all five", interval: "On 9:30", focus: "IM endurance at race-distance length" },
        ]},
      { label: "IM Big Block 1800", totalYards: 1800, types: ["im"], strokes: [], sets: [
          { id: "s_f44jf5", reps: 3, dist: 400, desc: "400 IM — moderate, smooth transitions, descend 1→3", interval: "On 9:30", focus: "Long-form IM volume" },
          { id: "s_x34jnp", reps: 6, dist: 100, desc: "100 IM — race pace, sharp transitions", interval: "On 2:30", focus: "Race-pace IM work after long-form load" },
        ]},
      { label: "IM Variety 2200", totalYards: 2200, types: ["im"], strokes: [], sets: [
          { id: "s_i3udsm", reps: 4, dist: 400, desc: "400 IM — moderate, hold form throughout", interval: "On 9:30", focus: "Long-form IM block" },
          { id: "s_1yh6ud", reps: 4, dist: 100, desc: "100 IM — strong, focus on transitions", interval: "On 2:30", focus: "Race-pace IM finishers" },
          { id: "s_u8lv16", reps: 2, dist: 100, desc: "Easy choice — recovery", interval: "On 2:30", focus: "Cool down within the main" },
        ]},
      { label: "Stroke-Count IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_6j86ds", reps: 1, dist: 200, desc: "IM — moderate, count strokes on the freestyle length (your baseline)", interval: "On 5:00", focus: "Establish your free-length stroke-count baseline" },
          { id: "s_t1hb4d", reps: 8, dist: 100, desc: "100 IM — hold your baseline ±1 on the free length; faster turnover on the others", interval: "On 2:45", focus: "Long stroke under fatigue across all four" },
          { id: "s_hpf8ft", reps: 1, dist: 100, desc: "Easy backstroke — shake out", interval: "On 2:45", focus: "Recover" },
        ]},
      { label: "IM Goldilocks 100s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_w32jyi", reps: 1, dist: 200, desc: "IM — easy/moderate, settle in", interval: "On 5:00", focus: "Open IM rhythm" },
          { id: "s_hyecn7", reps: 3, dist: 100, desc: "100 IM — easy (RPE 5)", interval: "On 2:30", focus: "Find the bottom" },
          { id: "s_vd9ryw", reps: 3, dist: 100, desc: "100 IM — moderate (RPE 7)", interval: "On 2:30", focus: "Find the middle" },
          { id: "s_roo0q4", reps: 3, dist: 100, desc: "100 IM — strong (RPE 9), feel the contrast vs the easy reps", interval: "On 2:45", focus: "Find the top — pacing literacy" },
        ]},
      { label: "30/30 IM Continuous", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_dgn9nd", reps: 1, dist: 200, desc: "IM — easy, open the set", interval: "On 5:00", focus: "Settle in" },
          { id: "s_0cd9js", reps: 12, dist: 50, desc: "50 IM (12.5 each) — alternate fast and easy reps (odd = fast, even = easy, continuous)", interval: "On 1:00", focus: "30s on / 30s off rhythm across IM strokes" },
          { id: "s_ls4wja", reps: 1, dist: 200, desc: "Easy backstroke — cool down", interval: "On 5:30", focus: "Recover" },
        ]},
      { label: "IM Bookends", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_1gchsn", reps: 1, dist: 100, desc: "100 IM — race pace, opens the set under-rested", interval: "On 3:00", focus: "Bookend #1 — race pace cold" },
          { id: "s_da1v5t", reps: 6, dist: 100, desc: "100 IM — moderate aerobic, smooth transitions", interval: "On 2:30", focus: "Aerobic fill between bookends" },
          { id: "s_rfl07c", reps: 1, dist: 100, desc: "100 IM — race pace, close the set under fatigue", interval: "On 3:00", focus: "Bookend #2 — race pace tired" },
          { id: "s_vec6hq", reps: 1, dist: 200, desc: "Easy choice — cool down", interval: "On 5:30", focus: "Compare bookend times — within :03 is the win" },
        ]},
      { label: "Build-By-Stroke 200 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_c833e7", reps: 5, dist: 200, desc: "200 IM — fly easy, back moderate, breast strong, free ALL OUT (build by stroke)", interval: "On 5:30", focus: "Finish the IM faster than you start it" },
        ]},
      { label: "Hypoxic IM 200s", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_a91cz6", reps: 1, dist: 200, desc: "IM — moderate, settle into pace", interval: "On 5:00", focus: "Open IM rhythm" },
          { id: "s_fqz7fo", reps: 4, dist: 200, desc: "200 IM — breathe every 2 on fly, every 3 on back, normal breast, breathe every 5 on free", interval: "On 5:30", focus: "CO2 tolerance across strokes (back off if dizzy)" },
        ]},
      { label: "200s Descend", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_9xo4kf", reps: 5, dist: 200, desc: "Freestyle — descend 1→5 (each one faster)", interval: "On 4:15", focus: "Find your pace early, build the whole way through" },
        ]},
      { label: "400s Descend", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_1hg3gu", reps: 3, dist: 400, desc: "Freestyle — descend 1→3 (each one faster than the last)", interval: "On 8:30", focus: "Negative split within each; fastest on #3" },
        ]},
      { label: "200s + 100s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_lct4qq", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:15", focus: "Hold form as pace increases" },
          { id: "s_qh5o5u", reps: 4, dist: 100, desc: "Freestyle — fast (near threshold)", interval: "On 2:10", focus: "Maintain stroke length at speed" },
        ]},
      { label: "Distance Ladder", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_zra6cu", reps: 1, dist: 400, desc: "Freestyle — aerobic/moderate", interval: "On 9:00", focus: "Long steady effort, build pace" },
          { id: "s_6n26l8", reps: 2, dist: 200, desc: "Freestyle — moderate/fast, descend", interval: "On 4:30", focus: "Find race pace, each one faster" },
          { id: "s_o7i20h", reps: 3, dist: 100, desc: "Freestyle — fast, under threshold", interval: "On 2:15", focus: "Sustain speed under accumulated load" },
        ]},
      { label: "Broken 500s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_2gs0ui", reps: 2, dist: 500, desc: "Broken 500 free — 5×100 with :15 rest, hold target pace", interval: "On 11:00", focus: "Race-pace simulation across long distance" },
          { id: "s_r7ix1n", reps: 2, dist: 100, desc: "Easy freestyle — active recovery between blocks", interval: "No interval — swim easy", focus: "Clear lactate, get ready for next block" },
        ]},
      { label: "Pull + Swim Threshold", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_w4zg48", reps: 4, dist: 200, desc: "Pull (buoy) — strong, hold technique under load", interval: "On 4:15", focus: "Upper-body endurance at threshold pace", eq: "pull" },
          { id: "s_l1nfvi", reps: 4, dist: 100, desc: "Freestyle swim — match the pull pace", interval: "On 2:10", focus: "Translate pull strength into full stroke" },
        ]},
      { label: "Long Aerobic Build", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_26bku0", reps: 1, dist: 800, desc: "Freestyle — negative split (second 400 faster than first)", interval: "On 17:00", focus: "Pace control over distance, build gradually" },
          { id: "s_qske9x", reps: 4, dist: 100, desc: "Freestyle — strong, finish the workout", interval: "On 2:15", focus: "Hold form despite fatigue" },
        ]},
      { label: "1000 Continuous", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_ylyqpe", reps: 1, dist: 1000, desc: "Freestyle — steady aerobic, negative split (second 500 faster)", interval: "On 22:00", focus: "Build and sustain pace over distance" },
        ]},
      { label: "Distance Pyramid 300 Apex", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_yg36oc", reps: 1, dist: 50, desc: "Freestyle — easy build", interval: "On 1:15", focus: "Open up" },
          { id: "s_v1zb14", reps: 1, dist: 100, desc: "Freestyle — moderate", interval: "On 2:15", focus: "Find your pace" },
          { id: "s_n3v2xp", reps: 1, dist: 200, desc: "Freestyle — moderate to strong", interval: "On 4:15", focus: "Hold pace longer" },
          { id: "s_38voch", reps: 1, dist: 300, desc: "Freestyle — strong", interval: "On 6:00", focus: "Apex of pyramid" },
          { id: "s_i82u1a", reps: 1, dist: 200, desc: "Freestyle — strong (match earlier pace)", interval: "On 4:15", focus: "Same pace under fatigue" },
          { id: "s_g79yzn", reps: 1, dist: 100, desc: "Freestyle — fast, near threshold", interval: "On 2:10", focus: "Pick up pace" },
          { id: "s_yfr825", reps: 1, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:00", focus: "Top speed close" },
          { id: "s_i90x53", reps: 2, dist: 100, desc: "Easy choice — active recovery", interval: "No interval — swim easy", focus: "Recover gracefully" },
        ]},
      { label: "800 + Closing 100s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_ojg47l", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, build last 200", interval: "On 17:00", focus: "Long swim with progression" },
          { id: "s_54zt82", reps: 4, dist: 100, desc: "Freestyle — fast (faster than 800 pace)", interval: "On 2:15", focus: "Pick up pace under fatigue" },
        ]},
      { label: "Distance Pull Endurance", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_h6q4ha", reps: 1, dist: 400, desc: "Pull (buoy) — steady aerobic effort", interval: "On 8:30", focus: "Long pull at endurance pace", eq: "pull" },
          { id: "s_nksy7z", reps: 4, dist: 100, desc: "Pull (buoy) — descending pace 1→4", interval: "On 2:15", focus: "Build pull intensity", eq: "pull" },
          { id: "s_372ew1", reps: 4, dist: 100, desc: "Freestyle swim — apply the pull pattern", interval: "On 2:10", focus: "Translate pull mechanics to swim" },
        ]},
      { label: "Tempo 300s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_ixjoos", reps: 4, dist: 300, desc: "Freestyle — tempo pace (RPE 7/10), hold consistent splits", interval: "On 6:30", focus: "Sustained tempo work for distance fitness" },
        ]},
      { label: "Mile Race Simulation", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_299vz5", reps: 1, dist: 1500, desc: "Freestyle — race pace simulation, even splits", interval: "On 33:00", focus: "Race-distance fitness check" },
        ]},
      { label: "Mixed-Stroke Distance", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_y6vu49", reps: 5, dist: 200, desc: "200 — first 150 free, last 50 backstroke", interval: "On 4:30", focus: "Distance with stroke variety, save the shoulders" },
          { id: "s_r6o0r2", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Distance Sprint Mix", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_4yycev", reps: 2, dist: 400, desc: "Freestyle — moderate aerobic effort", interval: "On 8:30", focus: "Distance base" },
          { id: "s_4tafwk", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:10", focus: "Distance + pace work" },
        ]},
      { label: "Descending Ladder", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_1rfzuq", reps: 1, dist: 400, desc: "Freestyle — aerobic effort, establish your pace", interval: "On 8:30", focus: "Anchor the ladder — this is your pace ceiling" },
          { id: "s_bz0ort", reps: 1, dist: 300, desc: "Freestyle — match or beat 400 pace", interval: "On 6:30", focus: "Hold as the distance drops" },
          { id: "s_r0bi3w", reps: 1, dist: 200, desc: "Freestyle — strong, find another gear", interval: "On 4:15", focus: "Negative split into the descent" },
          { id: "s_bp5uli", reps: 1, dist: 100, desc: "Freestyle — finish at best pace of the set", interval: "On 2:10", focus: "Empty the tank" },
        ]},
      { label: "Ascending Ladder", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_esp9o5", reps: 1, dist: 100, desc: "Freestyle — controlled, hold back", interval: "On 2:10", focus: "Reserve — the long stuff is coming" },
          { id: "s_sv1syb", reps: 1, dist: 200, desc: "Freestyle — moderate, find your rhythm", interval: "On 4:15", focus: "Build on the 100 pace" },
          { id: "s_v7ei3f", reps: 1, dist: 300, desc: "Freestyle — moderate to strong, hold form", interval: "On 6:30", focus: "Aerobic ceiling approaching" },
          { id: "s_rcwa05", reps: 1, dist: 400, desc: "Freestyle — strong, best sustained effort", interval: "On 8:30", focus: "Apex of the ladder — give everything left" },
        ]},
      { label: "Distance Pyramid", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_nslkhz", reps: 1, dist: 100, desc: "Freestyle — easy/moderate", interval: "On 2:10", focus: "Open up smoothly" },
          { id: "s_v4iuis", reps: 1, dist: 200, desc: "Freestyle — moderate, build from the 100", interval: "On 4:15", focus: "Climb toward the peak" },
          { id: "s_q5bxcg", reps: 1, dist: 400, desc: "Freestyle — strong, sustained effort", interval: "On 8:30", focus: "Apex — test your aerobic ceiling" },
          { id: "s_4m3trb", reps: 1, dist: 200, desc: "Freestyle — match peak pace if possible", interval: "On 4:15", focus: "Hold the line on the way back down" },
          { id: "s_8yqodi", reps: 1, dist: 100, desc: "Freestyle — near-max effort to close", interval: "On 2:10", focus: "Finish faster than you started" },
        ]},
      { label: "300s Descend", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_sc8kt7", reps: 4, dist: 300, desc: "Freestyle — descend 1→4 (each 300 faster)", interval: "On 6:30", focus: "Mid-distance descend, build pace" },
        ]},
      { label: "600 + 4×100", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_kn5oj4", reps: 1, dist: 600, desc: "Freestyle — steady aerobic base", interval: "On 13:30", focus: "Long aerobic effort" },
          { id: "s_52v9rk", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:10", focus: "Pick up the pace at the end" },
        ]},
      { label: "Mile Splits", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_0xz1x2", reps: 3, dist: 500, desc: "Freestyle — race-distance splits, hold same pace each 500", interval: "On 11:00", focus: "Mile fitness in 500-yard segments" },
        ]},
      { label: "200 Step-Down", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_eq0fqu", reps: 6, dist: 200, desc: "Freestyle — step pace down by :05 every 200", interval: "On 4:30", focus: "Progressive pace control" },
        ]},
      { label: "Distance Pull Volume", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_7s1122", reps: 1, dist: 800, desc: "Pull (buoy) — long aerobic, smooth long stroke", interval: "On 17:00", focus: "Long pull base", eq: "pull" },
          { id: "s_pojmbi", reps: 5, dist: 100, desc: "Pull (buoy) — moderate to strong, descend 1→5", interval: "On 2:15", focus: "Pull intensity build", eq: "pull" },
        ]},
      { label: "Broken 1000", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_h2f5ek", reps: 1, dist: 1000, desc: "Broken 1000 — :10 rest after every 100 (race-pace simulation)", interval: "On 22:00", focus: "Race-pace mile prep in 100-yard segments" },
          { id: "s_hmy47j", reps: 4, dist: 50, desc: "Easy freestyle — between blocks", interval: "On 1:15", focus: "Active recovery" },
        ]},
      { label: "Aerobic 250s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_b9962c", reps: 5, dist: 250, desc: "Freestyle — aerobic pace (RPE 6-7/10), even splits", interval: "On 4:45", focus: "Mid-distance aerobic volume" },
        ]},
      { label: "Negative-Split 250s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_7pl7xr", reps: 5, dist: 250, desc: "Freestyle — negative split each rep (first 125 moderate, last 125 strong)", interval: "On 4:30", focus: "Build within each 250" },
        ]},
      { label: "750 + 100s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_neeaa0", reps: 1, dist: 750, desc: "Freestyle — steady aerobic (RPE 6-7)", interval: "On 16:00", focus: "Aerobic base building" },
          { id: "s_5lbrv8", reps: 5, dist: 100, desc: "Freestyle — hold or slightly better than 750 pace", interval: "On 2:15", focus: "Volume extension after long swim" },
        ]},
      { label: "Distance + Sprint Sandwich", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_0pyiog", reps: 1, dist: 600, desc: "Freestyle — moderate aerobic effort", interval: "On 13:30", focus: "Aerobic base" },
          { id: "s_fg9gyu", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:30", focus: "Speed after distance load" },
          { id: "s_0f9xuo", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:30", focus: "Cool down" },
        ]},
      { label: "Negative-Split 400s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_rbqsvl", reps: 3, dist: 400, desc: "Freestyle — negative split (second 200 faster than first)", interval: "On 8:30", focus: "Practice closing pace within each 400" },
        ]},
      { label: "1500 Continuous", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_4h6zw7", reps: 1, dist: 1500, desc: "Freestyle — continuous, even-effort race-distance swim", interval: "On 33:00", focus: "Mile pace control" },
        ]},
      { label: "Race-Pace Mile Prep", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_sprf77", reps: 6, dist: 100, desc: "Freestyle — at goal mile pace", interval: "On 2:10", focus: "Find your sustainable mile pace" },
          { id: "s_uzvsbk", reps: 1, dist: 300, desc: "Easy backstroke — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_ouk6qe", reps: 3, dist: 100, desc: "Freestyle — slightly faster than goal pace", interval: "On 2:10", focus: "Practice closing-stage push" },
        ]},
      { label: "200 Pace Pyramid", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_vc5f0o", reps: 1, dist: 200, desc: "Freestyle — easy aerobic", interval: "On 4:00", focus: "Open up" },
          { id: "s_gabfdl", reps: 1, dist: 200, desc: "Freestyle — moderate", interval: "On 4:30", focus: "Find tempo" },
          { id: "s_lixog0", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:15", focus: "Threshold" },
          { id: "s_4qpnlf", reps: 1, dist: 200, desc: "Freestyle — fast", interval: "On 4:15", focus: "Above threshold" },
          { id: "s_fhu36x", reps: 1, dist: 200, desc: "Freestyle — strong (match 3rd)", interval: "On 4:15", focus: "Same pace under fatigue" },
          { id: "s_c9903l", reps: 1, dist: 200, desc: "Freestyle — moderate (cool)", interval: "On 4:30", focus: "Active recovery" },
        ]},
      { label: "1650 Race Day Sim", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_ewqddq", reps: 1, dist: 100, desc: "Freestyle — easy, settle in", interval: "On 2:00", focus: "Race-day warm-up feel" },
          { id: "s_2pb7mp", reps: 1, dist: 1000, desc: "Freestyle — race-pace simulation", interval: "On 22:00", focus: "First two-thirds of a 1650" },
          { id: "s_zhsg8p", reps: 1, dist: 200, desc: "Easy choice — active recovery", interval: "No interval — swim easy", focus: "Practice in-race fade management" },
        ]},
      { label: "1000 Pull Endurance", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_x9a9fj", reps: 1, dist: 1000, desc: "Pull (buoy) — continuous, smooth long stroke", interval: "On 21:00", focus: "Long-pull aerobic base", eq: "pull" },
          { id: "s_xm6pol", reps: 3, dist: 100, desc: "Freestyle — apply pull mechanics", interval: "On 2:15", focus: "Translate to swim" },
        ]},
      { label: "Long Aerobic 500", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_5i8r2j", reps: 2, dist: 500, desc: "Freestyle — steady aerobic, even effort", interval: "On 11:00", focus: "Aerobic base in mid-distance reps" },
          { id: "s_u7r6x7", reps: 1, dist: 100, desc: "Easy backstroke — close", interval: "On 2:45", focus: "Cool down" },
        ]},
      { label: "Fast Finish 200s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_7c53om", reps: 6, dist: 200, desc: "Freestyle — moderate first 100, fast second 100", interval: "On 4:30", focus: "Practice closing-half acceleration" },
        ]},
      { label: "300/200/100 Triplets", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_dr3b4j", reps: 2, dist: 300, desc: "Freestyle — aerobic", interval: "On 6:30", focus: "Distance base" },
          { id: "s_517s1l", reps: 2, dist: 200, desc: "Freestyle — moderate to fast, descend", interval: "On 4:30", focus: "Build pace" },
          { id: "s_jjqcfx", reps: 2, dist: 100, desc: "Freestyle — fast, near threshold", interval: "On 2:15", focus: "Threshold sustain" },
        ]},
      { label: "Mid-Distance Threshold", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_69snzg", reps: 4, dist: 250, desc: "Freestyle — threshold pace (RPE 7-8/10)", interval: "On 4:30", focus: "Sustained threshold mid-distance" },
          { id: "s_o1rcl8", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tempo Pull 200s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_er9obq", reps: 4, dist: 200, desc: "Pull (buoy) — tempo pace, smooth long stroke", interval: "On 4:15", focus: "Pull endurance at tempo", eq: "pull" },
          { id: "s_t6yasu", reps: 4, dist: 100, desc: "Freestyle — apply the pull tempo to full stroke", interval: "On 2:15", focus: "Carry pull pattern into swim" },
        ]},
      { label: "Overdistance 1200", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_vpnh23", reps: 1, dist: 1200, desc: "Freestyle — continuous overdistance, build last 400", interval: "On 26:00", focus: "Beyond mile-distance fitness" },
        ]},
      { label: "Distance Kick + Free", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_g8xahh", reps: 1, dist: 300, desc: "Kick — moderate (board optional)", interval: "On 7:00", focus: "Long kick base", eq: "kick" },
          { id: "s_2zx1lu", reps: 4, dist: 200, desc: "Freestyle — moderate, drive with the legs", interval: "On 4:30", focus: "Apply kick conditioning" },
        ]},
      { label: "200 + 100 Threshold", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_mf64fg", reps: 4, dist: 200, desc: "Freestyle — threshold pace", interval: "On 4:15", focus: "Threshold base" },
          { id: "s_fyejiw", reps: 4, dist: 100, desc: "Freestyle — threshold pace", interval: "On 2:10", focus: "Same pace, shorter rep" },
        ]},
      { label: "Triple 400 Endurance", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_183jpv", reps: 3, dist: 400, desc: "Freestyle — moderate aerobic, even splits", interval: "On 9:00", focus: "Aerobic base in 400-yard reps" },
        ]},
      { label: "Stroke Sprinkle Distance", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_urmdjr", reps: 4, dist: 250, desc: "First 200 freestyle steady + last 50 backstroke", interval: "On 4:45", focus: "Long swim with stroke breaks for shoulders" },
          { id: "s_be9nrf", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Mile + Sprint Finish", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_xqg4pu", reps: 1, dist: 1000, desc: "Freestyle — race-pace mile simulation", interval: "On 22:00", focus: "Race-pace fitness" },
          { id: "s_m8mhd9", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT after distance", interval: "On 1:30", focus: "Sprint after extended effort" },
        ]},
      { label: "Triple 500 Distance", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_io5ezp", reps: 3, dist: 500, desc: "Freestyle — aerobic to moderate pace, hold even splits", interval: "On 9:00", focus: "Mid-distance endurance blocks" },
        ]},
      { label: "1000 + 5×200 Distance", totalYards: 2000, types: ["distance"], strokes: [], sets: [
          { id: "s_sc9bfr", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, focus on even effort", interval: "On 21:00", focus: "Overdistance base" },
          { id: "s_q62x0q", reps: 5, dist: 200, desc: "Freestyle — moderate to strong, descend 1→5", interval: "On 4:00", focus: "Build pace after the long swim" },
        ]},
      { label: "Four 600s Distance", totalYards: 2400, types: ["distance"], strokes: [], sets: [
          { id: "s_l9vel8", reps: 4, dist: 600, desc: "Freestyle — aerobic pace, even splits, hold form to the end", interval: "On 11:00", focus: "High-volume distance endurance" },
        ]},
      { label: "Distance 200s Repeat Unit", totalYards: 500, types: ["distance"], strokes: [], sets: [
          { id: "s_lbd7hy", reps: 2, dist: 200, desc: "Freestyle — aerobic pace, negative split (second 100 faster)", interval: "On 4:15", focus: "Pacing discipline each round" },
          { id: "s_ysbmfh", reps: 1, dist: 100, desc: "Freestyle — moderate, hold form from the 200s", interval: "On 2:05", focus: "Extend endurance without rest" },
        ]},
      { label: "Distance Build Repeat Unit", totalYards: 600, types: ["distance"], strokes: [], sets: [
          { id: "s_kfhf3i", reps: 3, dist: 200, desc: "Freestyle — descend 1→3, each faster than the last", interval: "On 4:15", focus: "Progressive effort — push the last 200 each round" },
        ]},
      { label: "1800 Continuous Distance", totalYards: 1800, types: ["distance"], strokes: [], sets: [
          { id: "s_okbtr4", reps: 1, dist: 1800, desc: "Freestyle — single continuous distance swim, race-distance effort, even splits", interval: "On 38:00", focus: "Sustained distance race-pace pressure" },
        ]},
      { label: "Distance Pyramid 2000", totalYards: 2000, types: ["distance"], strokes: [], sets: [
          { id: "s_2w67vj", reps: 1, dist: 200, desc: "Freestyle — moderate, even effort", interval: "On 4:00", focus: "Open the pyramid at conversational pace" },
          { id: "s_at5dso", reps: 1, dist: 400, desc: "Freestyle — moderate, hold same pace as the 200", interval: "On 8:00", focus: "Hold pace across the longer rep" },
          { id: "s_n4jvzz", reps: 1, dist: 800, desc: "Freestyle — moderate-strong, descend the second half", interval: "On 16:00", focus: "Main piece — distance-build effort" },
          { id: "s_ozux5j", reps: 1, dist: 400, desc: "Freestyle — strong, faster than the opening 400", interval: "On 8:00", focus: "Closing pressure" },
          { id: "s_27uakk", reps: 1, dist: 200, desc: "Freestyle — strong finisher, fastest of the set", interval: "On 4:00", focus: "Drop the hammer to close" },
        ]},
      { label: "Mile + 600 + 300", totalYards: 2400, types: ["distance"], strokes: [], sets: [
          { id: "s_vlimah", reps: 1, dist: 1500, desc: "Freestyle — race-pace mile, even splits", interval: "On 33:00", focus: "Race-distance simulation" },
          { id: "s_fe3mw8", reps: 1, dist: 600, desc: "Freestyle — moderate-strong, hold the mile pace", interval: "On 12:00", focus: "Continue under fatigue" },
          { id: "s_xc4xtc", reps: 1, dist: 300, desc: "Freestyle — strong close, faster than mile pace", interval: "On 6:00", focus: "Closing effort under accumulated fatigue" },
        ]},
      { label: "The Holland Lift", totalYards: 2400, types: ["distance"], strokes: [], sets: [
          { id: "s_07cz7n", reps: 1, dist: 400, desc: "Freestyle — easy build, settle into long-distance rhythm", interval: "On 8:00", focus: "Open the engine without spiking the heart rate" },
          { id: "s_xhwvdd", reps: 1, dist: 500, desc: "Freestyle — moderate, hold one stroke count per length", interval: "On 9:30", focus: "Lock in the stroke" },
          { id: "s_t64o9w", reps: 1, dist: 600, desc: "Freestyle — moderate-strong, lift pace 5s/100 vs the 500", interval: "On 11:00", focus: "First step up — sustain it" },
          { id: "s_4sfj9m", reps: 1, dist: 500, desc: "Freestyle — strong, hold the new pace under accumulated load", interval: "On 9:30", focus: "Second step up — hold the lift" },
          { id: "s_sx4w5n", reps: 1, dist: 400, desc: "Freestyle — fastest of the set, race-pace effort", interval: "On 8:00", focus: "Top step — finish faster than you started" },
        ]},
      { label: "DPS Hold 500", totalYards: 2500, types: ["distance"], strokes: [], sets: [
          { id: "s_3sqqf7", reps: 1, dist: 500, desc: "Freestyle — moderate, count strokes on every length and record the average (your baseline)", interval: "On 9:00", focus: "Establish baseline stroke count" },
          { id: "s_olkzup", reps: 4, dist: 500, desc: "Freestyle — moderate-strong, hold baseline ±1 stroke per length on every 500", interval: "On 9:00", focus: "DPS discipline under fatigue — break form, restart the count" },
        ]},
      { label: "Step-Pace 1000", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_6bjckh", reps: 2, dist: 100, desc: "Freestyle — easy (RPE 4/10)", interval: "On 1:45", focus: "Open" },
          { id: "s_l19h73", reps: 2, dist: 100, desc: "Freestyle — moderate (RPE 6/10)", interval: "On 1:45", focus: "First pace step" },
          { id: "s_5cr6oo", reps: 2, dist: 100, desc: "Freestyle — moderate-strong (RPE 7/10)", interval: "On 1:45", focus: "Second step" },
          { id: "s_t8idsp", reps: 2, dist: 100, desc: "Freestyle — strong (RPE 8/10)", interval: "On 1:45", focus: "Third step — getting uncomfortable" },
          { id: "s_dzdbr7", reps: 2, dist: 100, desc: "Freestyle — near max (RPE 9/10)", interval: "On 1:45", focus: "Top step — fastest pair of the set" },
        ]},
      { label: "Lung Buster 500", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_ilg4ji", reps: 1, dist: 200, desc: "Freestyle — easy, breathe every 3", interval: "On 4:00", focus: "Open the lungs" },
          { id: "s_77eyvw", reps: 1, dist: 500, desc: "Freestyle — moderate, breathe by 100: 5 / 7 / 9 / 7 / 5 (back off the pattern if dizzy)", interval: "On 9:30", focus: "CO2 tolerance across distance — air discipline" },
          { id: "s_n2fet7", reps: 1, dist: 100, desc: "Easy backstroke — flush", interval: "On 2:30", focus: "Recover" },
          { id: "s_llhab2", reps: 1, dist: 500, desc: "Freestyle — moderate-strong, breathe every 5 the whole way (or 3/5 by 50 if every-5 won't hold)", interval: "On 9:30", focus: "Sustained restricted breathing — hold your stroke" },
        ]},
      { label: "Negative-Split 800", totalYards: 1600, types: ["distance"], strokes: [], sets: [
          { id: "s_ji8lck", reps: 2, dist: 800, desc: "Freestyle — first 400 controlled, second 400 must be faster (record both splits); rest enough to reset between", interval: "On 18:00", focus: "Pacing discipline — finish faster than you start, twice" },
        ]},
      { label: "Stroke-Count Descending 200s", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_mdkwxc", reps: 1, dist: 200, desc: "Freestyle — easy, count strokes on every length and average them (your baseline)", interval: "On 4:30", focus: "Lock in baseline" },
          { id: "s_edfudm", reps: 4, dist: 200, desc: "Freestyle — moderate, drop ONE stroke per length vs previous rep (baseline-1, baseline-2, etc.)", interval: "On 4:30", focus: "Forced length and glide — tempo drops as count drops" },
        ]},
      { label: "Sprint Ladder", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_ytwumo", reps: 4, dist: 100, desc: "Freestyle — 75 easy + last 25 ALL OUT", interval: "On 2:30", focus: "Save energy, unleash on last length" },
          { id: "s_60niid", reps: 8, dist: 50, desc: "Choice of stroke — 25 easy + 25 ALL OUT", interval: "On 1:45", focus: "Maximum speed on second 25" },
          { id: "s_v2m527", reps: 8, dist: 25, desc: "Freestyle — ALL OUT sprint", interval: "On 1:00", focus: "Top-end speed — best times of the workout" },
        ]},
      { label: "Pure Speed", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_ofuloi", reps: 10, dist: 50, desc: "Choice of stroke — ALL OUT every rep", interval: "On 2:00", focus: "Full effort every rep — if it isn't hard, go harder" },
          { id: "s_jvywqh", reps: 4, dist: 100, desc: "Freestyle — easy/moderate (active recovery)", interval: "No interval — swim easy", focus: "Keep moving, clear the lactic acid" },
          { id: "s_2kpded", reps: 6, dist: 25, desc: "Choice of stroke — sprint to the wall", interval: "On :55", focus: "Hold top-end speed even as fatigue builds" },
        ]},
      { label: "Race Pace Blocks", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_uepk9k", reps: 6, dist: 50, desc: "Choice — build to ALL OUT by final 25", interval: "On 2:00", focus: "Race simulation — explosive finish" },
          { id: "s_s79fk8", reps: 1, dist: 200, desc: "Easy recovery freestyle", interval: "4:00 rest before next", focus: "Full recovery" },
          { id: "s_v6hpfy", reps: 8, dist: 50, desc: "Choice — ALL OUT (full recovery between)", interval: "On 2:30", focus: "Max velocity — no holding back" },
          { id: "s_rn7slj", reps: 4, dist: 25, desc: "Choice — absolute sprint to finish", interval: "On 1:00", focus: "End on pure speed" },
        ]},
      { label: "Sprint Pyramid Up & Down", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_yiw4v5", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Wake up top-end speed" },
          { id: "s_s7rans", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Build the ladder" },
          { id: "s_s1epvh", reps: 1, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:15", focus: "Hold longer" },
          { id: "s_spmsec", reps: 1, dist: 100, desc: "Choice stroke — ALL OUT", interval: "On 3:00", focus: "Peak of pyramid" },
          { id: "s_2jbjwc", reps: 1, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:15", focus: "Speed under fatigue" },
          { id: "s_8tgd81", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Find top speed again" },
          { id: "s_eit56p", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT (best 25 of the workout)", interval: "On :45", focus: "Final blast" },
          { id: "s_y0cwe6", reps: 1, dist: 200, desc: "Easy choice — full recovery before second block", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_87huzw", reps: 4, dist: 100, desc: "Freestyle — 75 easy + last 25 ALL OUT", interval: "On 2:00", focus: "Finish with controlled speed work" },
        ]},
      { label: "Anaerobic Capacity", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_mijh96", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT, short rest (test the system)", interval: "On 1:15", focus: "Max effort with limited recovery — no holding back" },
          { id: "s_svp0x6", reps: 4, dist: 100, desc: "Easy freestyle — between sprint blocks", interval: "No interval — swim easy", focus: "Clear lactate, regroup" },
          { id: "s_bv4on1", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT, longer rest", interval: "On 2:00", focus: "Pure speed after fatigue accumulated" },
        ]},
      { label: "Sprint Pull Power", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_zbfo68", reps: 6, dist: 50, desc: "Pull (buoy) — fast, drive arm speed", interval: "On 1:15", focus: "Upper-body sprint mechanics, max stroke rate", eq: "pull" },
          { id: "s_l95gvk", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset between blocks" },
          { id: "s_fqh2n4", reps: 8, dist: 25, desc: "Freestyle — ALL OUT, max turnover", interval: "On :45", focus: "Apply pull power into full sprint" },
          { id: "s_6ix2uc", reps: 6, dist: 50, desc: "Choice — fast, descend 1→6", interval: "On 1:30", focus: "Each one faster, finish strongest" },
        ]},
      { label: "25s Lactic Tolerance", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_zo8g81", reps: 16, dist: 25, desc: "Freestyle — ALL OUT, every 25 (very tight rest)", interval: "On :40", focus: "Lactate tolerance with limited recovery" },
          { id: "s_56mszi", reps: 1, dist: 200, desc: "Easy choice — full recovery", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_d2olmq", reps: 8, dist: 25, desc: "Choice stroke — sprint with longer rest", interval: "On 1:00", focus: "Top speed under accumulated fatigue" },
          { id: "s_4zg25h", reps: 1, dist: 200, desc: "Easy freestyle — cool to next phase", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Power 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_t1uicl", reps: 12, dist: 50, desc: "Choice stroke — ALL OUT, full recovery between", interval: "On 2:00", focus: "Pure power 50s, max effort each rep" },
          { id: "s_tg67jd", reps: 4, dist: 100, desc: "Easy freestyle — between sets", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Block Sprints", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_kfh9lo", reps: 4, dist: 100, desc: "Block 1: 100s ALL OUT, descending 1→4", interval: "On 2:30", focus: "Sprint endurance" },
          { id: "s_8f0pi9", reps: 6, dist: 50, desc: "Block 2: 50s ALL OUT, descending 1→6", interval: "On 1:30", focus: "Sprint speed" },
          { id: "s_w3w8sq", reps: 1, dist: 200, desc: "Easy backstroke — full recovery", interval: "On 5:30", focus: "Active recovery" },
          { id: "s_h21e42", reps: 4, dist: 25, desc: "Block 3: 25s ALL OUT — peak velocity", interval: "On 1:00", focus: "Top-end speed" },
        ]},
      { label: "Sprint Kick Set", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_9trpfy", reps: 8, dist: 50, desc: "Kick — ALL OUT (board optional)", interval: "On 1:30", focus: "Sprint kick power", eq: "kick" },
          { id: "s_kbm138", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_alsr6s", reps: 4, dist: 100, desc: "Freestyle — ALL OUT, drive with the legs", interval: "On 2:30", focus: "Apply sprint kick to full sprint" },
        ]},
      { label: "Stroke-by-Stroke Sprint", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_p1idah", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:30", focus: "Fly speed" },
          { id: "s_nd0agt", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:30", focus: "Back speed" },
          { id: "s_9x5unn", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:30", focus: "Breast speed" },
          { id: "s_8afn4p", reps: 4, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:30", focus: "Free speed" },
          { id: "s_0r4474", reps: 1, dist: 200, desc: "Easy choice — final cool", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "75s Race Pace", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_04ylnm", reps: 10, dist: 75, desc: "Choice non-free stroke — race pace (2nd half stronger)", interval: "On 2:00", focus: "Race-distance pacing for non-free events" },
          { id: "s_776rsq", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Aerobic reset" },
          { id: "s_d58c6m", reps: 1, dist: 100, desc: "Choice — easy/moderate finishing swim", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Negative-Split 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_b9npib", reps: 16, dist: 50, desc: "First 25 build, second 25 ALL OUT", interval: "On 1:30", focus: "Practice negative-split sprint mechanics" },
          { id: "s_dj19fb", reps: 1, dist: 200, desc: "Easy choice — cool-down", interval: "No interval — swim easy", focus: "Recover" },
        ]},
      { label: "Sprint Pyramid 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_5uue00", reps: 1, dist: 25, desc: "Choice stroke — moderate (warm-up sprint)", interval: "On :45", focus: "Wake up" },
          { id: "s_hiuiti", reps: 2, dist: 25, desc: "Choice stroke — fast", interval: "On :45", focus: "Build speed" },
          { id: "s_5rvog5", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_usi0uq", reps: 8, dist: 25, desc: "Choice stroke — ALL OUT, no holding back", interval: "On :45", focus: "Maximum effort, every rep" },
          { id: "s_j1m11x", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Maintain speed under fatigue" },
          { id: "s_fl0l1j", reps: 2, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Hold the line" },
          { id: "s_ftpqdu", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT (best 25 of the day)", interval: "On :45", focus: "Final blast" },
          { id: "s_704db0", reps: 1, dist: 450, desc: "Easy choice — cool-down distance", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Speed Endurance 12×50", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_49tncc", reps: 12, dist: 50, desc: "Choice stroke — fast (race pace), hold all 12", interval: "On 1:00", focus: "Sprint endurance — same fast pace under fatigue" },
          { id: "s_wnpt47", reps: 4, dist: 100, desc: "Easy freestyle — cool-down", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Sprint Pull Set", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_6s42ub", reps: 8, dist: 50, desc: "Pull (buoy) — sprint, max stroke rate", interval: "On 1:30", focus: "Upper-body sprint power", eq: "pull" },
          { id: "s_k0yeaw", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_xrbvub", reps: 4, dist: 100, desc: "Freestyle — fast, apply pull strength", interval: "On 2:15", focus: "Carry pull power into full stroke" },
        ]},
      { label: "Sprint Ladder", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_r3p7ju", reps: 4, dist: 100, desc: "Freestyle — 75 easy build, last 25 ALL OUT", interval: "On 2:30", focus: "Controlled approach, explode at the wall" },
          { id: "s_34erb6", reps: 4, dist: 75, desc: "Choice stroke — 50 build, last 25 ALL OUT", interval: "On 2:00", focus: "Distance drops, effort rises" },
          { id: "s_bu7ph9", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT every rep", interval: "On 1:30", focus: "Pure speed, full recovery" },
          { id: "s_ojr2vb", reps: 4, dist: 25, desc: "Choice stroke — absolute top-speed sprint", interval: "On 1:00", focus: "Best 25s of the workout" },
        ]},
      { label: "Sprint Pyramid", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_jdmuyv", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :55", focus: "Open the pyramid fast" },
          { id: "s_eiojff", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Longer, still maximum effort" },
          { id: "s_4e3z9e", reps: 4, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:00", focus: "Peak of the pyramid" },
          { id: "s_ymwhta", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Speed under fatigue on the way back" },
          { id: "s_1rxq60", reps: 4, dist: 25, desc: "Choice stroke — final blast, max speed", interval: "On :55", focus: "Close the pyramid — best 25s of the set" },
          { id: "s_jvkwe5", reps: 1, dist: 100, desc: "Easy choice — full recovery", interval: "On 2:30", focus: "Active recovery" },
        ]},
      { label: "8×100 Race Pace", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_tg8u7e", reps: 8, dist: 100, desc: "Choice stroke — race pace with full recovery", interval: "On 3:00", focus: "Race-pace 100s, true effort each rep" },
          { id: "s_pgfzhe", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Sprint 25s + 75s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_lbjxho", reps: 12, dist: 25, desc: "Choice stroke — ALL OUT 25s", interval: "On 1:00", focus: "Top-end speed" },
          { id: "s_eq4ukn", reps: 4, dist: 75, desc: "Choice — first 25 easy, last 50 fast", interval: "On 2:00", focus: "Negative-split sprint" },
          { id: "s_nz93af", reps: 4, dist: 100, desc: "Easy choice — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "USRPT 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_kqg3ym", reps: 20, dist: 50, desc: "Freestyle — race pace, drop out if you miss the time", interval: "On 1:00", focus: "USRPT-style race-pace volume" },
        ]},
      { label: "Stroke Rotation Sprints", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_x2gyfl", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 2:00", focus: "Fly speed" },
          { id: "s_gby61i", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:45", focus: "Back speed" },
          { id: "s_3y8hjp", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:45", focus: "Breast speed" },
          { id: "s_2ll7up", reps: 4, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:30", focus: "Free speed" },
          { id: "s_c2vzxi", reps: 1, dist: 300, desc: "Easy backstroke — close", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "16×25 Sprint Volume", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_lno4k5", reps: 16, dist: 25, desc: "Choice stroke — ALL OUT (full recovery)", interval: "On 1:00", focus: "Pure top-end speed" },
          { id: "s_2kyws7", reps: 4, dist: 100, desc: "Easy freestyle — close-out", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_8lvej3", reps: 4, dist: 50, desc: "Choice — fast finishing 50s", interval: "On 1:30", focus: "Speed under fatigue" },
        ]},
      { label: "100s ALL OUT", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_c185bc", reps: 6, dist: 100, desc: "Choice stroke — ALL OUT (full recovery between)", interval: "On 4:00", focus: "Test 100 race-pace fitness" },
          { id: "s_yzkytp", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:30", focus: "Reset" },
          { id: "s_7ec5cg", reps: 4, dist: 50, desc: "Choice — sprint finishing 50s", interval: "On 1:30", focus: "Top-off speed" },
        ]},
      { label: "Sprint Pull Mechanics", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_psxzgb", reps: 6, dist: 50, desc: "Pull (buoy) — fast, high stroke rate", interval: "On 1:30", focus: "Pull power, max turnover", eq: "pull" },
          { id: "s_prrx0i", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_3gbjov", reps: 8, dist: 50, desc: "Freestyle — ALL OUT, apply pull mechanics", interval: "On 1:30", focus: "Translate pull power to sprint" },
          { id: "s_jke5xe", reps: 4, dist: 25, desc: "Choice — top-end speed", interval: "On 1:00", focus: "Final speed work" },
        ]},
      { label: "Lactate Tolerance 100s", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_6c1e1f", reps: 6, dist: 100, desc: "Choice stroke — ALL OUT, short rest", interval: "On 2:00", focus: "Lactate tolerance in race-distance reps" },
          { id: "s_1w9pvq", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_j6xs8x", reps: 6, dist: 50, desc: "Choice stroke — fast, longer rest", interval: "On 2:00", focus: "Top speed after lactate" },
        ]},
      { label: "Build-Then-Sprint 75s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_gr8vgq", reps: 14, dist: 75, desc: "Choice stroke — 25 build / 25 strong / 25 ALL OUT", interval: "On 1:45", focus: "Three-pace 75s, race-pace simulation" },
        ]},
      { label: "Race-Pace Sandwich", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_pm0axs", reps: 4, dist: 100, desc: "Choice stroke — race pace", interval: "On 3:00", focus: "First race-pace block" },
          { id: "s_iy4qx2", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_twflz3", reps: 4, dist: 100, desc: "Choice stroke — race pace, hold the same time", interval: "On 3:00", focus: "Second race-pace block under fatigue" },
        ]},
      { label: "16×50 Descend by 4", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_68k7dy", reps: 16, dist: 50, desc: "Choice stroke — descend by 4 (1-4 fast, 5-8 faster, 9-12 fastest, 13-16 ALL OUT)", interval: "On 1:15", focus: "Progressive sprint endurance" },
          { id: "s_3db8dk", reps: 4, dist: 50, desc: "Easy choice — between blocks", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Sprint Kick Power", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_ro5ht6", reps: 8, dist: 50, desc: "Kick — ALL OUT, focus on power off the wall (board optional)", interval: "On 1:45", focus: "Kick speed", eq: "kick" },
          { id: "s_mw8fed", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Apply kick power to swim sprint" },
          { id: "s_gpnvyu", reps: 4, dist: 50, desc: "Easy choice — close", interval: "On 1:30", focus: "Recovery" },
        ]},
      { label: "200 Sprint Test", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_ty7z8h", reps: 1, dist: 200, desc: "Choice stroke — ALL OUT (test your 200)", interval: "On 6:00", focus: "200 race-pace test" },
          { id: "s_8glz69", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_q8xmqa", reps: 4, dist: 100, desc: "Choice — strong, descend 1→4", interval: "On 2:30", focus: "Build pace post-test" },
          { id: "s_d4iaux", reps: 4, dist: 75, desc: "Choice — 25 easy / 25 build / 25 ALL OUT", interval: "On 2:00", focus: "Three-pace finishers" },
        ]},
      { label: "Power 25s on the Minute", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_9qu73q", reps: 24, dist: 25, desc: "Choice stroke — ALL OUT, on the minute (full recovery)", interval: "On 1:00", focus: "Pure speed with consistent rest" },
          { id: "s_d86rv6", reps: 4, dist: 100, desc: "Easy backstroke — close-out", interval: "On 2:45", focus: "Active recovery" },
        ]},
      { label: "Sprint Pyramid 50s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_q0adfi", reps: 1, dist: 50, desc: "Choice — moderate (warm-up sprint)", interval: "On 1:30", focus: "Wake up" },
          { id: "s_unfbku", reps: 2, dist: 50, desc: "Choice — fast", interval: "On 1:30", focus: "Build" },
          { id: "s_yikhvn", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Peak speed" },
          { id: "s_6og6h8", reps: 6, dist: 50, desc: "Choice — ALL OUT (the test)", interval: "On 1:30", focus: "Maintain top speed" },
          { id: "s_llevk8", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Hold the line" },
          { id: "s_qkl3wp", reps: 2, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Find speed" },
          { id: "s_at1a38", reps: 1, dist: 50, desc: "Choice — ALL OUT (best 50)", interval: "On 1:30", focus: "Final blast" },
          { id: "s_zeajsq", reps: 1, dist: 50, desc: "Easy — recovery", interval: "On 1:30", focus: "Recovery" },
        ]},
      { label: "Tight-Rest 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_u4w0zm", reps: 16, dist: 50, desc: "Freestyle — fast, very tight rest", interval: "On :50", focus: "Sprint endurance with limited recovery" },
          { id: "s_24k9nm", reps: 4, dist: 50, desc: "Easy choice — between blocks", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Sprint + Pull Combo", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_jx1ksr", reps: 1, dist: 200, desc: "Easy freestyle — warm into the sprint", interval: "No interval — swim easy", focus: "Open up" },
          { id: "s_hy90w4", reps: 6, dist: 50, desc: "Pull (buoy) — sprint, max turnover", interval: "On 1:30", focus: "Pull speed", eq: "pull" },
          { id: "s_1dqbsq", reps: 6, dist: 50, desc: "Choice stroke — ALL OUT swim, apply pull mechanics", interval: "On 1:30", focus: "Translate pull power" },
          { id: "s_2lgcaa", reps: 6, dist: 50, desc: "Easy choice — between sets", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Stroke-Specific 75s", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_opfyfh", reps: 4, dist: 75, desc: "Butterfly — race pace", interval: "On 2:30", focus: "Fly race-distance speed" },
          { id: "s_3eh42n", reps: 4, dist: 75, desc: "Backstroke — race pace", interval: "On 2:15", focus: "Back race-distance speed" },
          { id: "s_tcwx61", reps: 4, dist: 75, desc: "Breaststroke — race pace", interval: "On 2:15", focus: "Breast race-distance speed" },
          { id: "s_p251na", reps: 2, dist: 75, desc: "Freestyle — race pace", interval: "On 1:30", focus: "Free race-distance speed" },
          { id: "s_xed14n", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint 24×50", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_6timwc", reps: 24, dist: 50, desc: "Choice stroke — ALL OUT, full rest between reps", interval: "On 1:30", focus: "Max speed across 24 sprint reps" },
        ]},
      { label: "Speed Blocks 100s", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_6lse3e", reps: 6, dist: 100, desc: "Choice stroke — sprint first 25, build last 75", interval: "On 2:15", focus: "Open with speed then hold mechanics" },
          { id: "s_enrf3d", reps: 6, dist: 100, desc: "Freestyle — strong, reinforce sprint mechanics", interval: "On 2:00", focus: "Speed under aerobic load" },
        ]},
      { label: "Sprint + Recovery 100s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_szacoy", reps: 10, dist: 100, desc: "50 ALL OUT / 50 easy (immediate)", interval: "On 2:30", focus: "Hard/easy alternation across reps" },
        ]},
      { label: "Power-Pull 75s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_on7vd5", reps: 6, dist: 75, desc: "Pull (buoy) — fast, max stroke power", interval: "On 1:45", focus: "Upper-body sprint", eq: "pull" },
          { id: "s_i716r1", reps: 6, dist: 75, desc: "Freestyle — fast, integrate pull power", interval: "On 1:30", focus: "Apply pull strength to swim" },
          { id: "s_die2fc", reps: 1, dist: 150, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "30-Second Repeats", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_g37dkt", reps: 20, dist: 25, desc: "Choice stroke — ALL OUT, on :30", interval: "On :30", focus: "Maximum effort with race-pace cycle" },
          { id: "s_c84ceb", reps: 1, dist: 200, desc: "Easy freestyle — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_9fdqga", reps: 4, dist: 75, desc: "Choice — easy/build/sprint", interval: "On 2:00", focus: "Three-pace finishers" },
        ]},
      { label: "Pace 50/100/50", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_k2x1vq", reps: 5, dist: 200, desc: "50 ALL OUT / 100 easy / 50 ALL OUT", interval: "On 4:00", focus: "Speed bookends with active recovery in middle" },
        ]},
      { label: "Sprint Kick Endurance", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_erq0zq", reps: 12, dist: 50, desc: "Kick — fast, descend 1-4 then 5-8 then 9-12 (board optional)", interval: "On 1:30", focus: "Kick speed across volume", eq: "kick" },
          { id: "s_91pdtg", reps: 4, dist: 100, desc: "Choice stroke — fast swim, drive with the legs", interval: "On 2:00", focus: "Apply kick to swim sprint" },
          { id: "s_0gxu05", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Sprint Day", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_m5tzn2", reps: 8, dist: 50, desc: "Backstroke — ALL OUT (full recovery)", interval: "On 2:00", focus: "Back sprint speed" },
          { id: "s_t4iv4b", reps: 4, dist: 100, desc: "Backstroke — fast, descend 1→4", interval: "On 2:45", focus: "Back race-pace endurance" },
          { id: "s_lf4dk4", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 2:00", focus: "Top off" },
        ]},
      { label: "Ladder Sprints", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_8kcfqs", reps: 1, dist: 25, desc: "Choice — ALL OUT", interval: "On :45", focus: "Open up" },
          { id: "s_p2px79", reps: 1, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Build" },
          { id: "s_76so04", reps: 1, dist: 75, desc: "Choice — ALL OUT", interval: "On 2:15", focus: "Peak" },
          { id: "s_y2ozcc", reps: 1, dist: 100, desc: "Choice — ALL OUT", interval: "On 3:00", focus: "Hold" },
          { id: "s_sjf2xg", reps: 1, dist: 150, desc: "Choice — ALL OUT", interval: "On 4:30", focus: "Test" },
          { id: "s_jpnkon", reps: 1, dist: 100, desc: "Choice — ALL OUT", interval: "On 3:00", focus: "Match earlier" },
          { id: "s_k81qwt", reps: 1, dist: 75, desc: "Choice — ALL OUT", interval: "On 2:15", focus: "Find speed" },
          { id: "s_fnc7uh", reps: 1, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Final 50" },
          { id: "s_u99wv4", reps: 1, dist: 25, desc: "Choice — ALL OUT", interval: "On :45", focus: "Best 25" },
          { id: "s_gg906k", reps: 1, dist: 350, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Quad-Strap 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_qwdcm4", reps: 4, dist: 50, desc: "Round 1 — choice stroke ALL OUT", interval: "On 1:30", focus: "First peak" },
          { id: "s_ryujls", reps: 4, dist: 50, desc: "Round 2 — choice stroke ALL OUT", interval: "On 1:30", focus: "Hold speed" },
          { id: "s_lw95ik", reps: 4, dist: 50, desc: "Round 3 — choice stroke ALL OUT", interval: "On 1:30", focus: "Find speed" },
          { id: "s_rcr57s", reps: 4, dist: 50, desc: "Round 4 — choice stroke ALL OUT", interval: "On 1:30", focus: "Final round, biggest effort" },
          { id: "s_0xqqki", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint Recovery Mix", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_gfquqn", reps: 4, dist: 100, desc: "Choice stroke — ALL OUT", interval: "On 4:00", focus: "Race-pace 100s" },
          { id: "s_rg87rm", reps: 4, dist: 75, desc: "Choice — ALL OUT", interval: "On 3:00", focus: "Sprint 75s" },
          { id: "s_bt7nxy", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 2:00", focus: "Sprint 50s" },
          { id: "s_5dth9m", reps: 4, dist: 25, desc: "Choice — ALL OUT", interval: "On 1:00", focus: "Top-end 25s" },
        ]},
      { label: "Pull Speed 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_mfwq5l", reps: 16, dist: 25, desc: "Pull (buoy) — ALL OUT, max turnover", interval: "On :45", focus: "Upper-body top speed", eq: "pull" },
          { id: "s_m4c6or", reps: 4, dist: 100, desc: "Freestyle — fast, apply pull mechanics", interval: "On 2:00", focus: "Translate to sprint" },
          { id: "s_4rozt2", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Race-Day Sprint Sims", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_w6apef", reps: 1, dist: 50, desc: "Choice — race-pace 50 simulation", interval: "On 3:00", focus: "Race 1 — sprint 50" },
          { id: "s_3t2368", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_sjvxo3", reps: 1, dist: 100, desc: "Choice — race-pace 100 simulation", interval: "On 4:00", focus: "Race 2 — sprint 100" },
          { id: "s_9viff2", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_ftk76s", reps: 1, dist: 50, desc: "Choice — second race 50", interval: "On 3:00", focus: "Race 3 — back-to-back sprint" },
          { id: "s_mu4uj3", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_gpl3ey", reps: 1, dist: 100, desc: "Choice — second race 100", interval: "On 4:00", focus: "Race 4 — final hard" },
          { id: "s_t7ao2u", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_cxyqfa", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tabata-Style 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_b32oxc", reps: 16, dist: 25, desc: "Choice stroke — ALL OUT, on :30 (8 rounds × 2 reps, race-recovery cycle)", interval: "On :30", focus: "Lactate-tolerance sprint blocks" },
          { id: "s_lozru9", reps: 4, dist: 100, desc: "Easy freestyle — between rounds", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_iuw74z", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint Volume 20×75", totalYards: 1500, types: ["sprint"], strokes: [], sets: [
          { id: "s_g8vk63", reps: 20, dist: 75, desc: "Choice stroke — 50 strong build / 25 ALL OUT (sprint the last 25)", interval: "On 2:00", focus: "High-volume speed endurance" },
        ]},
      { label: "Sprint Double Block 2000", totalYards: 2000, types: ["sprint"], strokes: [], sets: [
          { id: "s_xhi6mb", reps: 20, dist: 50, desc: "Choice stroke — ALL OUT or near-max (full recovery)", interval: "On 1:30", focus: "Sprint capacity block" },
          { id: "s_ysbpof", reps: 10, dist: 100, desc: "Freestyle — strong (not sprinting), hold pace", interval: "On 2:30", focus: "Aerobic close after sprint block" },
        ]},
      { label: "Sprint 50s Repeat Unit", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_hcyhms", reps: 8, dist: 50, desc: "Freestyle or choice — ALL OUT every rep, leave it on the wall", interval: "On 1:45", focus: "Max speed — designed to repeat 2–3×" },
        ]},
      { label: "Sprint 50+100 Repeat Unit", totalYards: 500, types: ["sprint"], strokes: [], sets: [
          { id: "s_fncrhc", reps: 6, dist: 50, desc: "Freestyle — ALL OUT sprint", interval: "On 1:45", focus: "Top-end speed" },
          { id: "s_ysmtpi", reps: 2, dist: 100, desc: "Freestyle — easy active recovery, long stroke", interval: "On 2:30", focus: "Flush and reset before next round" },
        ]},
      { label: "Sprint Build Repeat Unit", totalYards: 600, types: ["sprint"], strokes: [], sets: [
          { id: "s_olzz33", reps: 4, dist: 100, desc: "Freestyle — build to strong, last 25 sprint", interval: "On 2:00", focus: "Controlled build with sprint finish" },
          { id: "s_qp98ls", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Max effort to close each round" },
        ]},
      { label: "30/30 Race Pace", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_1awalq", reps: 1, dist: 200, desc: "Easy choice — open up, long stroke", interval: "On 4:30", focus: "Prime the system" },
          { id: "s_zqccx5", reps: 16, dist: 50, desc: "Freestyle — alternate: odd 50s ALL OUT, even 50s easy active recovery (no rest between — the easy 50 IS the rest)", interval: "On 1:00", focus: "Race-pace repeatability — controlled recovery is the skill" },
          { id: "s_7qwpgz", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Bookend 50s", totalYards: 800, types: ["sprint"], strokes: [], sets: [
          { id: "s_qhyoi4", reps: 8, dist: 50, desc: "Freestyle — ALL OUT 25 + ALL OUT 25 with a 5-second pause at the wall (two max-effort 25s back-to-back as one 50)", interval: "On 2:00", focus: "Tolerate the second 25 — race finish under fatigue" },
          { id: "s_xfqm29", reps: 8, dist: 50, desc: "Choice stroke — easy active recovery, long stroke", interval: "On 1:30", focus: "Flush between sprint reps" },
        ]},
      { label: "Hot/Cold Ladder", totalYards: 800, types: ["sprint"], strokes: [], sets: [
          { id: "s_dzpbkc", reps: 1, dist: 25, desc: "ALL OUT sprint — choice", interval: "On 1:00", focus: "Hot" },
          { id: "s_limfkn", reps: 1, dist: 100, desc: "Easy — active recovery", interval: "On 2:30", focus: "Cold" },
          { id: "s_hrs1nn", reps: 1, dist: 50, desc: "ALL OUT sprint — choice", interval: "On 1:30", focus: "Hot" },
          { id: "s_bcc85y", reps: 1, dist: 100, desc: "Easy — active recovery", interval: "On 2:30", focus: "Cold" },
          { id: "s_8fdb5q", reps: 1, dist: 75, desc: "ALL OUT sprint — choice", interval: "On 2:00", focus: "Hot" },
          { id: "s_r1s0eh", reps: 1, dist: 100, desc: "Easy — active recovery", interval: "On 2:30", focus: "Cold" },
          { id: "s_xgoxxh", reps: 1, dist: 100, desc: "ALL OUT — choice (the test)", interval: "On 2:30", focus: "Hottest — full distance" },
          { id: "s_xxr86x", reps: 1, dist: 100, desc: "Easy — flush", interval: "On 2:30", focus: "Cool down between cycles" },
          { id: "s_d3fr2g", reps: 1, dist: 75, desc: "ALL OUT sprint — choice", interval: "On 2:00", focus: "Climb back down" },
          { id: "s_zzmjey", reps: 1, dist: 50, desc: "ALL OUT sprint — choice", interval: "On 1:30", focus: "Hot" },
          { id: "s_xu15op", reps: 1, dist: 25, desc: "ALL OUT sprint — choice", interval: "On 1:00", focus: "Top of the lap counter" },
        ]},
      { label: "The 5/35", totalYards: 1500, types: ["sprint"], strokes: [], sets: [
          { id: "s_a8t8gl", reps: 1, dist: 200, desc: "Easy choice — open up", interval: "On 4:30", focus: "Prime" },
          { id: "s_3etag2", reps: 5, dist: 50, desc: "Freestyle — ALL OUT, on extra rest (full recovery between)", interval: "On 1:45", focus: "Top-end speed when fresh — note your time" },
          { id: "s_h9kj6q", reps: 35, dist: 25, desc: "Freestyle — descend through the set: first 5 easy, next 10 moderate, next 10 strong, last 10 ALL OUT", interval: "On 0:30", focus: "Sprint repeatability — hold the last 10 within 1s of the opening 5x50 pace" },
          { id: "s_li4ekm", reps: 1, dist: 175, desc: "Easy choice — flush", interval: "On 4:30", focus: "Cool down" },
        ]},
      { label: "Underwater Sprint 25s", totalYards: 600, types: ["sprint"], strokes: [], sets: [
          { id: "s_jb22c0", reps: 1, dist: 200, desc: "Easy choice — open up, long stroke", interval: "On 5:00", focus: "Prime" },
          { id: "s_yw5g9k", reps: 8, dist: 25, desc: "Freestyle ALL OUT — 7-10m underwater dolphin kick off the wall, then sprint to finish (back off underwaters if you get lightheaded)", interval: "On 0:45", focus: "Breakout speed — wall plus first strokes wins the race" },
          { id: "s_ds19zi", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Reverse-Broken 100", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_i2sdvf", reps: 1, dist: 200, desc: "Easy choice — open up", interval: "On 4:30", focus: "Prime" },
          { id: "s_udk93p", reps: 6, dist: 100, desc: "Freestyle — sprint, but broken IN REVERSE: 10s rest after the FIRST 25, then 25 + 25 + 25 straight through (front-loaded recovery)", interval: "On 3:00", focus: "Build pain tolerance — the back 75 carries the work" },
          { id: "s_i98pgv", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Aerobic Ladder", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_liurib", reps: 1, dist: 400, desc: "Freestyle — easy (RPE 5/10)", interval: "On 9:00", focus: "Settle in, controlled breathing" },
          { id: "s_fp4qk1", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6/10)", interval: "On 5:45", focus: "Find your sustainable tempo" },
          { id: "s_cvlpbo", reps: 1, dist: 200, desc: "Freestyle — strong (RPE 7/10)", interval: "On 4:30", focus: "Push the pace — hold your form" },
          { id: "s_w7uch9", reps: 1, dist: 100, desc: "Freestyle — near max (RPE 8+/10)", interval: "On 2:00", focus: "Empty the aerobic tank" },
        ]},
      { label: "Long Steady State", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_atuhsz", reps: 2, dist: 600, desc: "Freestyle — steady aerobic pace (RPE 6/10), no intervals", interval: "On 11:30", focus: "Comfortable effort you can sustain 20+ min" },
        ]},
      { label: "Progressive Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_orvrk8", reps: 1, dist: 400, desc: "Freestyle — easy (RPE 5/10)", interval: "On 9:00", focus: "Aerobic base — controlled breathing" },
          { id: "s_trc725", reps: 1, dist: 400, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 7:45", focus: "Find your tempo" },
          { id: "s_8iguq8", reps: 1, dist: 400, desc: "Freestyle — strong (RPE 7-8/10)", interval: "On 8:30", focus: "Push lactate threshold — hold stroke form" },
        ]},
      { label: "Threshold Tempo", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_hg4dp9", reps: 4, dist: 300, desc: "Freestyle — threshold pace (RPE 7-8/10). Hold SAME pace all 4 reps.", interval: "On 6:30", focus: "Aerobic ceiling — maintain form under sustained effort" },
        ]},
      { label: "Pull Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_iqpnvv", reps: 1, dist: 600, desc: "Pull (buoy) — steady aerobic, focus on long stroke", interval: "On 11:00", focus: "Build pull endurance, smooth and rhythmic", eq: "pull" },
          { id: "s_7pdl05", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend 1→4", interval: "On 2:00", focus: "Add intensity to the same pull pattern", eq: "pull" },
          { id: "s_53n8yl", reps: 1, dist: 200, desc: "Freestyle swim — apply pull catch into full stroke", interval: "On 3:45", focus: "Carry the catch over to swimming" },
        ]},
      { label: "Mile Pace Builder", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_hasalf", reps: 1, dist: 400, desc: "Freestyle — find your mile pace (RPE 7/10)", interval: "On 8:30", focus: "Establish your sustainable race pace" },
          { id: "s_5sfw21", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Reset between efforts" },
          { id: "s_ehr1cv", reps: 1, dist: 400, desc: "Freestyle — match the first 400 pace", interval: "On 8:30", focus: "Repeat the same effort fatigued" },
          { id: "s_afjoxf", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Final reset" },
          { id: "s_uwp4ky", reps: 2, dist: 100, desc: "Freestyle — slightly faster than 400 pace", interval: "On 2:15", focus: "Practice closing speed at the end of a mile" },
        ]},
      { label: "Aerobic Capacity Set", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_7lcdqa", reps: 6, dist: 150, desc: "Freestyle — strong aerobic effort (RPE 7-8/10)", interval: "On 2:45", focus: "Sustained mid-distance work, hold form throughout" },
          { id: "s_hbs2kc", reps: 4, dist: 100, desc: "Freestyle — finish strong (RPE 8/10)", interval: "On 2:10", focus: "Push lactate threshold under accumulated fatigue" },
        ]},
      { label: "Heart Rate Set", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_yz73vk", reps: 4, dist: 100, desc: "Freestyle — RPE 5/10 (easy aerobic)", interval: "On 2:15", focus: "Active recovery zone" },
          { id: "s_nopc6i", reps: 4, dist: 100, desc: "Freestyle — RPE 7/10 (tempo)", interval: "On 2:10", focus: "Mid-zone tempo" },
          { id: "s_psl7mr", reps: 4, dist: 100, desc: "Freestyle — RPE 8/10 (threshold)", interval: "On 2:10", focus: "Threshold zone" },
        ]},
      { label: "Long Kick Endurance", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_aeuflq", reps: 1, dist: 400, desc: "Kick — easy/moderate, choice of stroke (board optional)", interval: "On 8:00", focus: "Long kick base, build endurance", eq: "kick" },
          { id: "s_7yz1m7", reps: 6, dist: 100, desc: "Freestyle — moderate aerobic, focus on legs driving", interval: "On 2:15", focus: "Apply kick conditioning into stroke" },
          { id: "s_q2pcxx", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Cool down" },
        ]},
      { label: "Threshold Ladder", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_9qogd6", reps: 1, dist: 100, desc: "Freestyle — RPE 6/10 (build into threshold)", interval: "On 2:15", focus: "Open up" },
          { id: "s_fgvss9", reps: 1, dist: 200, desc: "Freestyle — RPE 7/10", interval: "On 4:30", focus: "Approach threshold" },
          { id: "s_h5s8kf", reps: 1, dist: 300, desc: "Freestyle — RPE 8/10 (at threshold)", interval: "On 6:30", focus: "Hold threshold" },
          { id: "s_gengia", reps: 1, dist: 200, desc: "Freestyle — RPE 8/10 (still threshold)", interval: "On 4:30", focus: "Match earlier 200 pace under fatigue" },
          { id: "s_bwa54t", reps: 1, dist: 100, desc: "Freestyle — RPE 9/10 (just over threshold)", interval: "On 2:15", focus: "Push past threshold" },
          { id: "s_0e9u2g", reps: 1, dist: 300, desc: "Easy choice — full recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sub-Threshold Volume", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_7zg12e", reps: 13, dist: 100, desc: "Freestyle — sub-threshold (RPE 7-7.5/10), even pace", interval: "On 2:10", focus: "High-volume sustained effort just below threshold" },
        ]},
      { label: "Aerobic IM Set", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_wcnk1e", reps: 6, dist: 200, desc: "200 IM — aerobic effort (RPE 6/10), smooth transitions", interval: "On 6:00", focus: "Endurance across IM strokes at aerobic pace" },
        ]},
      { label: "Pull-Heavy Endurance", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_xspzss", reps: 1, dist: 500, desc: "Pull (buoy) — steady aerobic, focus on long stroke", interval: "On 9:30", focus: "Long pull base", eq: "pull" },
          { id: "s_zg91x3", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend pace 1→4", interval: "On 2:15", focus: "Build pull intensity", eq: "pull" },
          { id: "s_d02gk3", reps: 4, dist: 100, desc: "Freestyle — apply pull mechanics", interval: "On 2:15", focus: "Translate to swim" },
        ]},
      { label: "Race-Pace 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_p8kefc", reps: 6, dist: 200, desc: "Freestyle — at goal race pace, hold all 6 reps", interval: "On 4:00", focus: "Race-distance pace under accumulating fatigue" },
        ]},
      { label: "400 Repeats Aerobic", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_kd5ir6", reps: 3, dist: 400, desc: "Freestyle — aerobic pace (RPE 7/10), even effort", interval: "On 9:00", focus: "Distance work at aerobic ceiling" },
        ]},
      { label: "Recovery Intervals", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_50yd9x", reps: 8, dist: 150, desc: "75 strong / 75 easy alternating", interval: "On 2:45", focus: "Practice recovery between hard efforts" },
        ]},
      { label: "Long Continuous + Sprint Finish", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_pu1ag4", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, even splits", interval: "On 17:00", focus: "Long aerobic base" },
          { id: "s_1y6tcz", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (max sprint after distance)", interval: "On 1:30", focus: "Sprint after fatigue — race-finish prep" },
        ]},
      { label: "Ascending Ladder", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_tl5vea", reps: 1, dist: 100, desc: "Freestyle — easy (RPE 5/10)", interval: "On 2:10", focus: "Reserve — the long stuff is coming" },
          { id: "s_mhb9v1", reps: 1, dist: 200, desc: "Freestyle — easy/moderate (RPE 6/10)", interval: "On 4:15", focus: "Find your rhythm" },
          { id: "s_cafjlx", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 6:30", focus: "Build into sustained effort" },
          { id: "s_l8t301", reps: 1, dist: 400, desc: "Freestyle — strong aerobic (RPE 7/10)", interval: "On 8:30", focus: "Apex — hold your best aerobic pace" },
        ]},
      { label: "Endurance Pyramid", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_uyw2as", reps: 1, dist: 200, desc: "Freestyle — easy/moderate (RPE 6/10)", interval: "On 4:15", focus: "Build in from the base" },
          { id: "s_28m2qr", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 6:30", focus: "Climb toward the apex" },
          { id: "s_nhmc2h", reps: 1, dist: 400, desc: "Freestyle — strong aerobic (RPE 7-8/10)", interval: "On 8:30", focus: "Apex — aerobic ceiling, hold your form" },
          { id: "s_e50mlk", reps: 1, dist: 300, desc: "Freestyle — hold the 300 pace on the way down", interval: "On 6:30", focus: "Same effort under fatigue" },
          { id: "s_5aezi9", reps: 1, dist: 200, desc: "Freestyle — close strong", interval: "On 4:15", focus: "Finish faster than you started" },
        ]},
      { label: "Aerobic 7×200", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_yw0sew", reps: 7, dist: 200, desc: "Freestyle — aerobic pace, even splits throughout", interval: "On 4:15", focus: "High-volume aerobic accumulation" },
        ]},
      { label: "1000 + 4×100", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_a7hss0", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, focus on stroke efficiency", interval: "On 22:00", focus: "Distance foundation" },
          { id: "s_8kvuco", reps: 4, dist: 100, desc: "Freestyle — slightly faster than 1000 pace", interval: "On 2:15", focus: "Speed layer after distance work" },
        ]},
      { label: "8×150 Steady", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_im1kkx", reps: 8, dist: 150, desc: "Freestyle — aerobic pace (RPE 6-7/10), hold all 8 the same", interval: "On 3:10", focus: "Classic Masters repeat — even splits every rep" },
        ]},
      { label: "Mile Pace Set", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_h8a4ov", reps: 11, dist: 100, desc: "Freestyle — at goal mile pace, hold even splits", interval: "On 2:10", focus: "Race-pace mile fitness" },
        ]},
      { label: "60-Minute Aerobic", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_mnoldn", reps: 1, dist: 1500, desc: "Freestyle — comfortable aerobic pace, smooth all the way", interval: "On 33:00", focus: "Long aerobic capacity work" },
        ]},
      { label: "Threshold 250s", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_nrvqet", reps: 5, dist: 250, desc: "Freestyle — threshold pace (RPE 7-8/10), even effort", interval: "On 4:30", focus: "Mid-distance threshold work" },
        ]},
      { label: "Hold-Pace 1250", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_acx701", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, aim for even 250 splits", interval: "On 22:00", focus: "Sustained aerobic discipline" },
          { id: "s_q8s1c6", reps: 5, dist: 50, desc: "Freestyle — slightly faster than 1000 pace", interval: "On 1:15", focus: "Speed finish after the long swim" },
        ]},
      { label: "Negative-Split 250s Endurance", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_l5yzbs", reps: 5, dist: 250, desc: "Freestyle — negative split (hold effort steady, let times drop rep to rep)", interval: "On 5:00", focus: "Learn to finish stronger than you start" },
        ]},
      { label: "Aerobic Build 400s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_hssneg", reps: 3, dist: 400, desc: "Freestyle — build pace within each 400 (easy → strong)", interval: "On 8:30", focus: "Internal pace progression" },
        ]},
      { label: "RPE Stairstep", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_8llrdv", reps: 2, dist: 200, desc: "Freestyle — RPE 6/10", interval: "On 4:30", focus: "Settle in" },
          { id: "s_bltgoi", reps: 2, dist: 200, desc: "Freestyle — RPE 7/10", interval: "On 4:15", focus: "Build" },
          { id: "s_cu18af", reps: 2, dist: 200, desc: "Freestyle — RPE 8/10", interval: "On 4:15", focus: "Threshold" },
        ]},
      { label: "Long Pull + Build", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_gse5ll", reps: 1, dist: 800, desc: "Pull (buoy) — steady aerobic, smooth long stroke", interval: "On 17:00", focus: "Long pull endurance", eq: "pull" },
          { id: "s_y4uivl", reps: 5, dist: 100, desc: "Freestyle — descend 1→5, build closing pace", interval: "On 2:15", focus: "Apply pull conditioning" },
        ]},
      { label: "Heart Rate Recovery", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ajni7y", reps: 6, dist: 200, desc: "Freestyle — moderate aerobic, focus on staying in HR zone 2", interval: "On 4:30", focus: "Build aerobic fitness with HR feedback" },
        ]},
      { label: "Long Choice 1100", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_d874u0", reps: 1, dist: 1100, desc: "Freestyle — continuous, comfortable aerobic pace, even effort", interval: "On 20:00", focus: "Long-form aerobic engine work" },
        ]},
      { label: "Cruise 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_utiesa", reps: 6, dist: 200, desc: "Freestyle — cruise pace (between aerobic and threshold)", interval: "On 4:15", focus: "Cruise pace for distance fitness" },
        ]},
      { label: "Mile Negative Split", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_513r7e", reps: 1, dist: 1500, desc: "Freestyle — second 750 faster than first 750", interval: "On 33:00", focus: "Practice negative splitting a mile" },
        ]},
      { label: "Lactate Buffering", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_is40j1", reps: 4, dist: 200, desc: "Freestyle — at lactate threshold (RPE 8/10)", interval: "On 4:15", focus: "Threshold blocks" },
          { id: "s_inmpxe", reps: 1, dist: 200, desc: "Easy backstroke — recovery between blocks", interval: "On 5:30", focus: "Practice lactate clearance" },
          { id: "s_g0mesk", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_sjnohi", reps: 8, dist: 150, desc: "Freestyle — aerobic pace (RPE 6/10), smooth long stroke", interval: "On 3:00", focus: "Aerobic volume in shorter reps" },
        ]},
      { label: "Pull-Heavy 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_8dinql", reps: 6, dist: 200, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 4:15", focus: "Pull endurance volume", eq: "pull" },
        ]},
      { label: "200/100 Endurance Mix", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_8hh46l", reps: 3, dist: 200, desc: "Freestyle — moderate aerobic", interval: "On 4:30", focus: "Aerobic base" },
          { id: "s_3tsyjj", reps: 6, dist: 100, desc: "Freestyle — sub-threshold pace", interval: "On 2:10", focus: "Sustained sub-threshold" },
        ]},
      { label: "Anaerobic Threshold Test", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_9i352t", reps: 1, dist: 200, desc: "Freestyle — easy warm-up", interval: "On 4:00", focus: "Open up" },
          { id: "s_fixewn", reps: 1, dist: 800, desc: "Freestyle — at anaerobic threshold (RPE 8/10), even splits", interval: "On 14:00", focus: "Threshold test piece" },
          { id: "s_ikl3qc", reps: 2, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Cool down" },
        ]},
      { label: "Aerobic IM Volume", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ctcrc5", reps: 4, dist: 300, desc: "300 IM — aerobic pace, smooth transitions", interval: "On 6:00", focus: "Endurance across all four strokes" },
        ]},
      { label: "Steady 500s", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_7sqvwm", reps: 3, dist: 500, desc: "Freestyle — steady aerobic, hold same time each 500", interval: "On 11:00", focus: "Even-effort distance volume" },
        ]},
      { label: "Pull + Kick Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_cslm1i", reps: 1, dist: 400, desc: "Pull (buoy) — steady aerobic", interval: "On 8:30", focus: "Pull base", eq: "pull" },
          { id: "s_0tipqr", reps: 1, dist: 400, desc: "Kick — moderate (board optional)", interval: "On 8:30", focus: "Kick base", eq: "kick" },
          { id: "s_4taxma", reps: 4, dist: 100, desc: "Freestyle — moderate, integrate pull and kick work", interval: "On 2:15", focus: "Apply both into full stroke" },
        ]},
      { label: "Tempo + Recovery 100s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_iymom4", reps: 12, dist: 100, desc: "Freestyle — odd-numbered tempo (RPE 7), even-numbered easy", interval: "On 2:15", focus: "Tempo with built-in recovery" },
        ]},
      { label: "Long Open Water Sim", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_tnxxh8", reps: 1, dist: 100, desc: "Freestyle — easy warm-up", interval: "On 2:00", focus: "Open up" },
          { id: "s_jb7ool", reps: 1, dist: 1000, desc: "Freestyle — sighting every 6-8 strokes (open water simulation)", interval: "On 22:00", focus: "Practice open-water mechanics with sighting" },
          { id: "s_6dvmtl", reps: 1, dist: 200, desc: "Easy choice — cool", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic Step-Up", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_bhyx2o", reps: 1, dist: 100, desc: "Freestyle — RPE 5/10", interval: "On 2:15", focus: "Easy" },
          { id: "s_wwusuz", reps: 1, dist: 200, desc: "Freestyle — RPE 6/10", interval: "On 4:30", focus: "Aerobic" },
          { id: "s_82po6i", reps: 1, dist: 300, desc: "Freestyle — RPE 7/10", interval: "On 6:30", focus: "Tempo" },
          { id: "s_4p6afe", reps: 1, dist: 400, desc: "Freestyle — RPE 8/10", interval: "On 7:15", focus: "Threshold" },
          { id: "s_2kpai3", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Distance Recovery Set", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_6c687q", reps: 11, dist: 100, desc: "Freestyle — easy/moderate (RPE 5-6/10), recovery-day pace", interval: "No interval — swim easy", focus: "Aerobic recovery volume" },
        ]},
      { label: "Race-Pace Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_p0zet0", reps: 8, dist: 150, desc: "Freestyle — at goal race pace, hold even splits", interval: "On 2:45", focus: "Race-pace volume in shorter reps" },
        ]},
      { label: "Threshold Pull 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_lwr4sc", reps: 8, dist: 150, desc: "Pull (buoy) — threshold pace, smooth long stroke", interval: "On 2:45", focus: "Pull threshold volume", eq: "pull" },
        ]},
      { label: "1×800 Steady + Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_x6zckq", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, even effort throughout", interval: "On 17:00", focus: "Long aerobic effort" },
          { id: "s_7gfabp", reps: 4, dist: 100, desc: "Freestyle — build pace 1→4 (each one slightly faster)", interval: "On 2:15", focus: "Closing build after distance" },
        ]},
      { label: "Aerobic Stroke Mix", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_fpfpta", reps: 4, dist: 200, desc: "Freestyle — aerobic", interval: "On 4:30", focus: "Free aerobic base" },
          { id: "s_pod71k", reps: 2, dist: 200, desc: "Backstroke — aerobic", interval: "On 5:30", focus: "Back aerobic" },
        ]},
      { label: "Long Kick Aerobic", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_h1rxrs", reps: 1, dist: 600, desc: "Kick — choice stroke, aerobic effort (board optional)", interval: "On 13:00", focus: "Long kick base", eq: "kick" },
          { id: "s_sd7ysd", reps: 6, dist: 100, desc: "Freestyle — aerobic, drive with the legs", interval: "On 2:15", focus: "Apply kick conditioning" },
        ]},
      { label: "Aerobic 6×200", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_mhkniq", reps: 6, dist: 200, desc: "Freestyle — aerobic pace, descend 1→3 then 4→6", interval: "On 4:30", focus: "Two-block descend across aerobic volume" },
        ]},
      { label: "Long Pull + Free Mix", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_7lak93", reps: 4, dist: 200, desc: "Pull (buoy) — aerobic pace, smooth long stroke", interval: "On 4:15", focus: "Pull aerobic volume", eq: "pull" },
          { id: "s_hivt9d", reps: 5, dist: 100, desc: "Freestyle — moderate, apply pull mechanics", interval: "On 2:15", focus: "Translate to swim" },
        ]},
      { label: "Marathon-Style 12×100", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_81zt5t", reps: 12, dist: 100, desc: "Freestyle — all reps the same time, easy aerobic effort", interval: "On 2:00", focus: "Discipline of even-effort high-volume aerobic" },
        ]},
      { label: "Straight 1500 Endurance", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_6524hg", reps: 1, dist: 1500, desc: "Freestyle — aerobic pace, focus on even effort and form throughout", interval: "On 32:00", focus: "Long continuous endurance" },
        ]},
      { label: "Double 1000 Endurance", totalYards: 2000, types: ["endurance"], strokes: [], sets: [
          { id: "s_9cog22", reps: 2, dist: 1000, desc: "Freestyle — aerobic pace, negative split (second 500 faster than first)", interval: "On 21:00", focus: "Long-form aerobic endurance with pacing discipline" },
        ]},
      { label: "Triple 800 Endurance", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_g7hvoi", reps: 3, dist: 800, desc: "Freestyle — aerobic pace, descend 1→3 (each one faster)", interval: "On 17:00", focus: "High-volume endurance with progressive effort" },
        ]},
      { label: "Endurance 100s Repeat Unit", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_ixg3uh", reps: 4, dist: 100, desc: "Freestyle — aerobic pace, RPE 6/10, long stroke throughout", interval: "On 2:10", focus: "Sustained aerobic effort — designed to repeat 2–3×" },
        ]},
      { label: "Endurance 200+100 Repeat Unit", totalYards: 500, types: ["endurance"], strokes: [], sets: [
          { id: "s_f8sk7z", reps: 2, dist: 200, desc: "Freestyle — moderate aerobic pace, even splits", interval: "On 4:15", focus: "Aerobic base work" },
          { id: "s_03mw6o", reps: 1, dist: 100, desc: "Freestyle — slightly stronger, hold form from 200s", interval: "On 2:05", focus: "Push the last 100 each round" },
        ]},
      { label: "Endurance 200s Repeat Unit", totalYards: 600, types: ["endurance"], strokes: [], sets: [
          { id: "s_d5ugr2", reps: 3, dist: 200, desc: "Freestyle — aerobic pace, aim for identical splits each rep", interval: "On 4:15", focus: "Pacing consistency — make each round match the last" },
        ]},
      { label: "CSS 10×100", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_txpwje", reps: 10, dist: 100, desc: "Freestyle — Critical Swim Speed pace, hold ALL 10 reps the same", interval: "On 2:10", focus: "Find and hold your CSS — sustainable for 1500m/1650y" },
        ]},
      { label: "50s on the Minute", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_7jmttc", reps: 24, dist: 50, desc: "Freestyle — steady pace, on the :60 (test endurance)", interval: "On 1:00", focus: "Sustain pace and form across high-volume 50s" },
        ]},
      { label: "8×150 Repeats", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_q3xkxv", reps: 8, dist: 150, desc: "Freestyle — steady aerobic pace, hold all 8 identical", interval: "On 3:10", focus: "Classic Masters repeat set — consistency over speed" },
        ]},
      { label: "Lactate-Threshold 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_est7wl", reps: 8, dist: 150, desc: "Freestyle — threshold pace (RPE 8/10), even effort", interval: "On 2:45", focus: "Lactate threshold in mid-distance reps" },
        ]},
      { label: "CSS Holdover", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_7xy5vr", reps: 12, dist: 100, desc: "Freestyle — CSS pace, hold ALL 12 reps the same", interval: "On 2:10", focus: "Critical Swim Speed under accumulated load" },
        ]},
      { label: "Race-Pace 100s", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_5z7r4s", reps: 10, dist: 100, desc: "Freestyle — at race-distance pace, hold all 10", interval: "On 2:10", focus: "Race pace under volume" },
        ]},
      { label: "8×150 Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_l4dg9b", reps: 8, dist: 150, desc: "Freestyle — build pace 1-4, then descend 5-8", interval: "On 2:45", focus: "Two-block build into the threshold" },
        ]},
      { label: "Build-Per-100", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_ymo1yo", reps: 10, dist: 100, desc: "Freestyle — first 25 easy, build through 100", interval: "On 2:00", focus: "Internal build per rep" },
        ]},
      { label: "Distance 100s Repeat Unit", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_9gmat0", reps: 4, dist: 100, desc: "Freestyle — aerobic pace, even effort, count strokes on last 25", interval: "On 2:10", focus: "Steady-state aerobic — designed to repeat 2–3×" },
        ]},
      { label: "Sprint Marathon 2400", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_mj371u", reps: 8, dist: 200, desc: "Freestyle — strong, descend 1→4 then repeat (1→4)", interval: "On 4:30", focus: "Sustained speed over distance" },
          { id: "s_z5uofw", reps: 8, dist: 100, desc: "Choice stroke — fast (near race pace)", interval: "On 2:00", focus: "Speed layer after distance block" },
        ]},
      { label: "Continuous 1800 Aerobic", totalYards: 1800, types: ["endurance"], strokes: [], sets: [
          { id: "s_1rdzhp", reps: 1, dist: 1800, desc: "Freestyle — single continuous swim at aerobic pace, smooth all the way", interval: "On 39:00", focus: "Single-piece aerobic capacity build" },
        ]},
      { label: "Six 300s Aerobic", totalYards: 1800, types: ["endurance"], strokes: [], sets: [
          { id: "s_vlf4sq", reps: 6, dist: 300, desc: "Freestyle — aerobic pace, even effort across all six, controlled breathing", interval: "On 6:30", focus: "Aerobic volume at a long-rep length that's neither sprint nor distance" },
        ]},
      { label: "Aerobic Build 2400", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_mo1xs3", reps: 1, dist: 400, desc: "Freestyle — easy, find aerobic rhythm", interval: "On 8:30", focus: "Warm into the aerobic build" },
          { id: "s_2sbq4c", reps: 1, dist: 800, desc: "Freestyle — moderate, hold same pace throughout", interval: "On 17:00", focus: "Sustained aerobic effort" },
          { id: "s_foxg7c", reps: 1, dist: 800, desc: "Freestyle — strong, faster than the previous 800", interval: "On 16:30", focus: "Closing pressure on aerobic threshold" },
          { id: "s_xeqj2b", reps: 1, dist: 400, desc: "Freestyle — strong, hold the new pace", interval: "On 8:00", focus: "Finish at threshold" },
        ]},
      { label: "Kick + Drill + Swim", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_qppklj", reps: 8, dist: 75, desc: "25 Kick / 25 Drill / 25 Swim — same stroke each rep (your choice)", interval: "On 1:45", focus: "Apply the drill directly into the swim", eq: "kickPart" },
          { id: "s_p59tp4", reps: 4, dist: 100, desc: "Pull (buoy) — focus on high-elbow catch, long stroke", interval: "On 2:15", focus: "Sculpt the perfect pull pattern", eq: "pull" },
        ]},
      { label: "Freestyle Drill Progression", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_c9g7td", reps: 4, dist: 50, desc: "Kick only — no board, streamlined off wall", interval: "On 1:45", focus: "Core rotation, balanced two-beat kick" },
          { id: "s_482ujh", reps: 4, dist: 50, desc: "Single-arm drill (alternate arms by 25)", interval: "On 1:20", focus: "Lead arm extension, shoulder-driven rotation" },
          { id: "s_le9va9", reps: 4, dist: 50, desc: "Catch-up drill — touch hands at extension", interval: "On 1:25", focus: "Full extension before pulling" },
          { id: "s_2etkjd", reps: 4, dist: 100, desc: "Freestyle swim — integrate all drilled elements", interval: "On 2:10", focus: "Swim slowly, feel everything" },
        ]},
      { label: "All-Stroke Technique", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_ujmaw2", reps: 4, dist: 100, desc: "Choice of stroke — SLOW, perfect form over speed", interval: "On 2:30", focus: "No rushing — technique over pace" },
          { id: "s_il7v1m", reps: 8, dist: 50, desc: "2 each: fly drill / back drill / breast drill / free drill", interval: "On 1:30", focus: "Key isolation drill for each stroke" },
          { id: "s_runt2v", reps: 4, dist: 75, desc: "25 Drill + 50 Swim (same stroke — apply what you drilled)", interval: "On 2:30", focus: "Bridge drill into real swimming" },
        ]},
      { label: "Kick-Heavy Day", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_fpj2ko", reps: 8, dist: 50, desc: "Kick — alternate stroke choice every 50", interval: "On 1:30", focus: "Develop kick across all strokes", eq: "kick" },
          { id: "s_fis6zt", reps: 4, dist: 75, desc: "25 Kick / 25 Drill / 25 Swim — same stroke", interval: "On 1:40", focus: "Translate kick rhythm into swim", eq: "kickPart" },
          { id: "s_6yt512", reps: 4, dist: 100, desc: "Freestyle — relaxed swim, apply leg work", interval: "On 2:00", focus: "Apply leg conditioning into the stroke" },
        ]},
      { label: "Catch + Pull Focus", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_snq2nz", reps: 4, dist: 50, desc: "Sculling drill — head-first, focus on hand pressure", interval: "On 1:15", focus: "Feel the water against your forearm" },
          { id: "s_na35y4", reps: 4, dist: 100, desc: "Pull (buoy) — count strokes per 25, minimize", interval: "On 2:15", focus: "Long efficient pull, max distance per stroke", eq: "pull" },
          { id: "s_xugpfa", reps: 4, dist: 75, desc: "25 Pull + 50 Swim — carry the catch into the stroke", interval: "On 1:40", focus: "Bridge pull mechanics into full freestyle", eq: "pull" },
          { id: "s_kd3cx4", reps: 2, dist: 100, desc: "Freestyle — relaxed, smooth, apply everything", interval: "On 2:00", focus: "Integrate the work into normal swimming" },
        ]},
      { label: "Stroke Count Awareness", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_a0nypy", reps: 4, dist: 50, desc: "Freestyle — count strokes per 25, set baseline", interval: "On 1:20", focus: "Establish your normal stroke count" },
          { id: "s_wowrju", reps: 4, dist: 50, desc: "Freestyle — same speed, REDUCE strokes by 1-2", interval: "On 1:20", focus: "Reach further, swim taller" },
          { id: "s_4b7chd", reps: 4, dist: 50, desc: "Freestyle — go FASTER without adding strokes", interval: "On 1:20", focus: "Faster turnover, same length" },
          { id: "s_wu8i6a", reps: 4, dist: 100, desc: "Freestyle — apply the count target across longer swim", interval: "On 2:10", focus: "Sustain efficiency over distance" },
        ]},
      { label: "Backstroke Focus", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_quzr11", reps: 4, dist: 50, desc: "Backstroke — single-arm drill (alternate arms by 25)", interval: "On 1:30", focus: "Lead arm extension and rotation" },
          { id: "s_hjww3u", reps: 4, dist: 75, desc: "Backstroke — count strokes per 25, minimize", interval: "On 2:30", focus: "Long efficient back stroke" },
          { id: "s_58aluy", reps: 4, dist: 50, desc: "Backstroke — focus on hip rotation and kick", interval: "On 1:30", focus: "Drive rotation from the hips" },
          { id: "s_ltgvgw", reps: 4, dist: 100, desc: "Backstroke — full swim, integrate all elements", interval: "On 3:00", focus: "Apply drill work to full backstroke" },
        ]},
      { label: "Breaststroke Mechanics", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_a2bw1l", reps: 4, dist: 50, desc: "Breaststroke — pull-only drill (no kick, just glide)", interval: "On 1:30", focus: "Isolate the pull and glide" },
          { id: "s_xom7j8", reps: 4, dist: 50, desc: "Breaststroke — kick-only drill (board optional)", interval: "On 1:30", focus: "Power kick and timing" },
          { id: "s_kxp9kb", reps: 4, dist: 75, desc: "Breaststroke — 2 kicks per pull (slow pace)", interval: "On 2:30", focus: "Extend the glide phase" },
          { id: "s_acw5rd", reps: 4, dist: 100, desc: "Breaststroke — full stroke, integrate timing", interval: "On 3:15", focus: "Smooth pull-glide-kick rhythm" },
        ]},
      { label: "Butterfly Sustainability", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_d54lxk", reps: 8, dist: 25, desc: "Butterfly — 3-3-3 drill (3 right arm / 3 left arm / 3 full)", interval: "On 1:00", focus: "Build fly endurance with drill" },
          { id: "s_sibz7m", reps: 6, dist: 50, desc: "Butterfly — 25 drill / 25 swim", interval: "On 1:30", focus: "Bridge drill into full fly" },
          { id: "s_r4u1l1", reps: 4, dist: 100, desc: "Butterfly + Free — 25 fly / 75 free", interval: "On 3:00", focus: "Practice fly recovery in workout context" },
          { id: "s_2twmn5", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Body Position Mastery", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_x7vp5t", reps: 4, dist: 50, desc: "Streamline kick on back — 12.5 underwater + 37.5 swim", interval: "On 1:30", focus: "Tight streamline, head still" },
          { id: "s_abuc3o", reps: 4, dist: 50, desc: "Side-lying kick — alternate sides every 25", interval: "On 1:30", focus: "Lateral body position with rotation" },
          { id: "s_qqv1tc", reps: 4, dist: 75, desc: "Freestyle — focus on head position (water at hairline)", interval: "On 1:45", focus: "Neutral head reduces drag" },
          { id: "s_p0gt9j", reps: 3, dist: 100, desc: "Freestyle — body height, keep hips high", interval: "On 2:00", focus: "Whole-body alignment over the surface" },
        ]},
      { label: "Underwater Dolphin Focus", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_01vyqb", reps: 8, dist: 25, desc: "Underwater dolphin kick off wall (target 10m), easy swim out", interval: "On 1:00", focus: "Streamline + dolphin kick power", eq: "underkick" },
          { id: "s_9g5rrj", reps: 4, dist: 50, desc: "Butterfly with focus on undulation", interval: "On 1:30", focus: "Apply dolphin rhythm to fly" },
          { id: "s_q6w1f2", reps: 4, dist: 75, desc: "25 underwater dolphin / 50 freestyle", interval: "On 1:45", focus: "Combine underwater work with swim", eq: "underkick" },
          { id: "s_2euphk", reps: 3, dist: 100, desc: "Freestyle — focus on push-off and underwater work", interval: "On 2:00", focus: "Underwater work in freestyle context" },
        ]},
      { label: "Bilateral Breathing", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_hrxep5", reps: 4, dist: 50, desc: "Freestyle — breathe every 3 strokes", interval: "On 1:20", focus: "Bilateral breathing baseline" },
          { id: "s_dwsbrv", reps: 4, dist: 75, desc: "Freestyle — breathe every 5 strokes", interval: "On 1:45", focus: "Lung capacity and breath control" },
          { id: "s_o6edu8", reps: 4, dist: 100, desc: "Freestyle — alternate 3-stroke / 5-stroke patterns", interval: "On 2:00", focus: "Switch breathing patterns mid-swim" },
          { id: "s_paeacg", reps: 1, dist: 100, desc: "Freestyle — breathe every 7 strokes (lung challenge)", interval: "On 2:00", focus: "Breath control under aerobic stress" },
        ]},
      { label: "High-Elbow Catch", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_zk8o4u", reps: 4, dist: 50, desc: "Pull (buoy) — sculling: head-first then feet-first", interval: "On 1:30", focus: "Feel water pressure on the forearm", eq: "pull" },
          { id: "s_pgkrm1", reps: 4, dist: 50, desc: "Pull (buoy) — fingertip drag (slow)", interval: "On 1:15", focus: "Engage the elbow in recovery", eq: "pull" },
          { id: "s_8ylevm", reps: 4, dist: 100, desc: "Pull (buoy) — focus on early vertical forearm", interval: "On 2:15", focus: "High elbow during catch", eq: "pull" },
          { id: "s_u710cx", reps: 3, dist: 100, desc: "Freestyle — apply high-elbow catch", interval: "On 2:15", focus: "Carry technique into swim" },
        ]},
      { label: "Streamline Off Walls", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_irr5ay", reps: 8, dist: 25, desc: "Streamline kick off wall — count strokes after surfacing", interval: "On 1:00", focus: "Maximize underwater distance off each wall" },
          { id: "s_sw7h8g", reps: 4, dist: 50, desc: "Freestyle — strong push-off, underwater dolphin to 10m", interval: "On 1:15", focus: "Apply streamline to freestyle turns" },
          { id: "s_90ugrq", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off and turn", interval: "On 2:00", focus: "Build streamline habit across distance" },
          { id: "s_vlxmmu", reps: 2, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Recovery" },
        ]},
      { label: "Stroke Count Reduction", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_44d0s1", reps: 4, dist: 50, desc: "Freestyle — count strokes (set baseline)", interval: "On 1:20", focus: "Establish baseline count per 25" },
          { id: "s_ptx4my", reps: 4, dist: 50, desc: "Freestyle — same speed, count -1", interval: "On 1:20", focus: "Lengthen the stroke" },
          { id: "s_6za37m", reps: 4, dist: 50, desc: "Freestyle — same speed, count -2", interval: "On 1:20", focus: "Maximize distance per stroke" },
          { id: "s_x495bh", reps: 4, dist: 100, desc: "Freestyle — apply minimum count to full 100s", interval: "On 2:15", focus: "Sustain efficient count over distance" },
          { id: "s_t90b8s", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Slow Swim Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_96jmkr", reps: 8, dist: 100, desc: "Choice of stroke — SLOW (longer than normal interval)", interval: "On 2:30", focus: "Deliberate practice — every stroke perfect" },
          { id: "s_fnspzs", reps: 4, dist: 50, desc: "Choice non-free — slow, focus on connection", interval: "On 1:30", focus: "Feel the catch, push, finish" },
        ]},
      { label: "20×50 Drill-Swim", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_7my7dn", reps: 20, dist: 50, desc: "25 Drill / 25 Swim — rotate strokes every 4", interval: "On 1:05", focus: "High-rep technique: drill it, then immediately apply it" },
        ]},
      { label: "Nose-Down Stroke Focus", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_7wzbzl", reps: 8, dist: 50, desc: "Freestyle — 25 catch-up drill / 25 swim, flat head position", interval: "On 1:15", focus: "Stroke mechanics with consistent head position", eq: "snorkel" },
          { id: "s_yjo7xh", reps: 8, dist: 100, desc: "Freestyle — long stroke, neutral head, moderate pace", interval: "On 2:10", focus: "Apply flat head technique to longer reps" },
        ]},
      { label: "Single-Arm Progression", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_qfdkdc", reps: 4, dist: 50, desc: "Single-arm freestyle — left only, lead arm extension", interval: "On 1:15", focus: "Left-side mechanics" },
          { id: "s_4zp298", reps: 4, dist: 50, desc: "Single-arm freestyle — right only, lead arm extension", interval: "On 1:15", focus: "Right-side mechanics" },
          { id: "s_xk8rxg", reps: 4, dist: 75, desc: "25 single-arm + 50 swim — apply per-arm focus", interval: "On 2:30", focus: "Bridge drill into stroke" },
          { id: "s_0hydmf", reps: 4, dist: 125, desc: "Freestyle — slow, focus on per-arm consistency", interval: "On 2:30", focus: "Apply mechanics to longer reps" },
        ]},
      { label: "Catch-Up Combination", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_nxz36b", reps: 8, dist: 50, desc: "Catch-up drill — touch hands at extension, focus on lead arm hold", interval: "On 1:20", focus: "Full extension before stroking" },
          { id: "s_137t6t", reps: 4, dist: 100, desc: "Freestyle — long, smooth, slight catch-up timing", interval: "On 2:00", focus: "Apply timing into normal stroke" },
          { id: "s_pr5x6z", reps: 4, dist: 50, desc: "Catch-up drill — faster pace, maintain timing", interval: "On 1:20", focus: "Translate timing to faster swimming" },
        ]},
      { label: "Stroke Rate Awareness", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_78x0d4", reps: 4, dist: 50, desc: "Freestyle — count strokes per 25 (baseline)", interval: "On 1:20", focus: "Establish baseline" },
          { id: "s_buzhtt", reps: 4, dist: 50, desc: "Freestyle — same speed, count -1 (lengthen stroke)", interval: "On 1:20", focus: "Lengthen, fewer strokes" },
          { id: "s_lheaii", reps: 4, dist: 50, desc: "Freestyle — same count, faster pace", interval: "On 1:20", focus: "Faster turnover, same length" },
          { id: "s_3oswzm", reps: 4, dist: 100, desc: "Freestyle — apply min count to longer reps", interval: "On 2:00", focus: "Hold efficient count over distance" },
          { id: "s_8j2inu", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fingertip Drag Day", totalYards: 1050, types: ["technique"], strokes: [], sets: [
          { id: "s_1s9x1r", reps: 4, dist: 50, desc: "Fingertip drag drill — slow, controlled recovery", interval: "On 1:15", focus: "Engaged elbow during recovery" },
          { id: "s_82zaha", reps: 4, dist: 75, desc: "25 fingertip drag + 50 swim", interval: "On 2:30", focus: "Apply recovery pattern" },
          { id: "s_sqb8q1", reps: 4, dist: 100, desc: "Freestyle — keep fingertips low through recovery", interval: "On 2:00", focus: "Maintain elbow engagement at speed" },
          { id: "s_3lyezi", reps: 1, dist: 150, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sculling Sequences", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_twqc2f", reps: 4, dist: 50, desc: "Front scull (head-first) — focus on hand pressure", interval: "On 1:30", focus: "Forward scull mechanics" },
          { id: "s_5bynm7", reps: 4, dist: 50, desc: "Mid-pull scull — under chest", interval: "On 1:30", focus: "Mid-stroke pressure" },
          { id: "s_6s7xml", reps: 4, dist: 50, desc: "Reverse scull (feet-first)", interval: "On 1:30", focus: "Reverse mechanics" },
          { id: "s_bay9gg", reps: 4, dist: 100, desc: "Freestyle — apply sculling sensation to catch", interval: "On 2:00", focus: "Translate scull pressure to pull" },
        ]},
      { label: "Kick on Side Drill", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_dn3b01", reps: 4, dist: 50, desc: "Kick on left side — top arm at side, lead arm extended", interval: "On 1:30", focus: "Left-side rotation", eq: "kick" },
          { id: "s_ypmist", reps: 4, dist: 50, desc: "Kick on right side — top arm at side, lead arm extended", interval: "On 1:30", focus: "Right-side rotation", eq: "kick" },
          { id: "s_m1ecc4", reps: 4, dist: 50, desc: "6-kick switch drill (6 kicks each side, then swap)", interval: "On 1:20", focus: "Connect rotation to stroke", eq: "kickPart" },
          { id: "s_r2lsw7", reps: 4, dist: 100, desc: "Freestyle — apply rotation pattern", interval: "On 2:00", focus: "Translate to swimming" },
        ]},
      { label: "3/5/7 Breathing Build", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_kltso0", reps: 4, dist: 75, desc: "Freestyle — breathe every 3 strokes", interval: "On 1:30", focus: "Bilateral baseline" },
          { id: "s_fu0e62", reps: 4, dist: 75, desc: "Freestyle — breathe every 5 strokes", interval: "On 1:30", focus: "Lung control" },
          { id: "s_xvo3z8", reps: 4, dist: 75, desc: "Freestyle — breathe every 7 strokes", interval: "On 1:30", focus: "Hypoxic build" },
          { id: "s_nmfo1k", reps: 1, dist: 200, desc: "Freestyle — return to bilateral, smooth", interval: "On 4:00", focus: "Reset normal breathing" },
        ]},
      { label: "Vertical Forearm Set", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_j66pln", reps: 4, dist: 50, desc: "Pull (buoy) — slow, exaggerate early vertical forearm", interval: "On 1:30", focus: "Engage the elbow at the top of the catch", eq: "pull" },
          { id: "s_v5ucbx", reps: 4, dist: 100, desc: "Pull (buoy) — apply EVF at moderate pace", interval: "On 2:15", focus: "Maintain EVF over longer reps", eq: "pull" },
          { id: "s_b52quc", reps: 4, dist: 75, desc: "Freestyle — 25 EVF focus / 50 swim", interval: "On 2:30", focus: "Bridge into normal stroke" },
          { id: "s_re6lwa", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Technique Day", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_jiz1cd", reps: 4, dist: 50, desc: "Backstroke — single-arm drill (alternate arms by 25)", interval: "On 1:30", focus: "Lead arm extension" },
          { id: "s_t5ek3b", reps: 4, dist: 50, desc: "Backstroke — double-arm drill (both arms simultaneously)", interval: "On 1:30", focus: "Symmetry and rotation" },
          { id: "s_1mqjp9", reps: 4, dist: 75, desc: "Backstroke — 25 drill + 50 swim", interval: "On 2:30", focus: "Apply drill into stroke" },
          { id: "s_eu1inj", reps: 4, dist: 100, desc: "Backstroke — full swim, focus on hip rotation", interval: "On 3:00", focus: "Rotate from the core" },
          { id: "s_yxt4q5", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breaststroke Timing", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_e6m7oi", reps: 4, dist: 50, desc: "Breaststroke — pull-pull-kick drill (2 pulls per kick)", interval: "On 1:30", focus: "Isolate pull power" },
          { id: "s_0mztt5", reps: 4, dist: 50, desc: "Breaststroke — kick-kick-pull drill (2 kicks per pull)", interval: "On 1:30", focus: "Extend the glide" },
          { id: "s_8qin91", reps: 4, dist: 75, desc: "Breaststroke — full stroke, slow tempo", interval: "On 2:30", focus: "Smooth pull-glide-kick rhythm" },
          { id: "s_777dpe", reps: 4, dist: 100, desc: "Breaststroke — full stroke, normal tempo", interval: "On 3:15", focus: "Apply timing at race tempo" },
          { id: "s_yvufl7", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Butterfly Drill Series", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_9d6vmm", reps: 4, dist: 25, desc: "Fly — single-arm drill (left)", interval: "On :50", focus: "Left-arm fly mechanics" },
          { id: "s_3uv2zk", reps: 4, dist: 25, desc: "Fly — single-arm drill (right)", interval: "On :50", focus: "Right-arm fly mechanics" },
          { id: "s_63s0z9", reps: 4, dist: 50, desc: "Fly — 3-3-3 drill (3R / 3L / 3 full)", interval: "On 1:30", focus: "Build to full stroke" },
          { id: "s_7p1flz", reps: 4, dist: 50, desc: "Fly — 25 drill / 25 swim", interval: "On 1:30", focus: "Bridge to full fly" },
          { id: "s_hbarfb", reps: 4, dist: 100, desc: "Fly + Free — 25 fly / 75 free", interval: "On 2:30", focus: "Sustainable fly" },
          { id: "s_va8ewk", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Push-Off Underwater Focus", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_9igf95", reps: 8, dist: 25, desc: "Streamline kick on back — 12.5 yd underwater + 12.5 swim", interval: "On 1:00", focus: "Tight streamline + dolphin kick", eq: "underkick" },
          { id: "s_4mpxcs", reps: 4, dist: 50, desc: "Streamline kick on back — go for max distance underwater", interval: "On 1:30", focus: "Push the underwater envelope", eq: "underkick" },
          { id: "s_t3wu35", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off, count UW kicks", interval: "On 2:00", focus: "Build streamline habit" },
          { id: "s_041ad3", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Stroke Length Test", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_xsfsbr", reps: 4, dist: 50, desc: "Freestyle — slowest possible (max stroke length)", interval: "On 1:30", focus: "Maximum distance per stroke" },
          { id: "s_h9zut2", reps: 4, dist: 50, desc: "Freestyle — moderate, hold long stroke", interval: "On 1:15", focus: "Maintain length at tempo" },
          { id: "s_vs60tm", reps: 4, dist: 50, desc: "Freestyle — fast, hold long stroke", interval: "On 1:15", focus: "Length at speed" },
          { id: "s_g6nars", reps: 4, dist: 100, desc: "Freestyle — apply length at strong pace", interval: "On 2:15", focus: "Length over distance" },
          { id: "s_ozxq36", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Hip Rotation Day", totalYards: 1150, types: ["technique"], strokes: [], sets: [
          { id: "s_vuuppf", reps: 4, dist: 50, desc: "Freestyle — exaggerated hip rotation, slow", interval: "On 1:15", focus: "Drive rotation from the hips" },
          { id: "s_jr55jm", reps: 4, dist: 50, desc: "Freestyle — kick on side, 6 kicks per arm-stroke", interval: "On 1:30", focus: "Connect rotation timing" },
          { id: "s_a27ipv", reps: 4, dist: 75, desc: "Freestyle — 25 hip rotation drill + 50 swim", interval: "On 2:30", focus: "Apply rotation pattern" },
          { id: "s_87f1og", reps: 4, dist: 100, desc: "Freestyle — moderate pace, maintain hip rotation", interval: "On 2:00", focus: "Sustained rotation under pace" },
          { id: "s_pwulm5", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "Catch & Finish", totalYards: 1150, types: ["technique"], strokes: [], sets: [
          { id: "s_o7w3b1", reps: 4, dist: 50, desc: "Freestyle — fingertip drag drill, focus on high elbow recovery", interval: "On 1:20", focus: "Set up the catch" },
          { id: "s_t2hu6w", reps: 4, dist: 50, desc: "Freestyle — fist swimming, feel the forearm pressure", interval: "On 1:15", focus: "Develop forearm catch surface", eq: "fins" },
          { id: "s_wik3rm", reps: 4, dist: 75, desc: "Freestyle — 25 catch drill + 50 swim, lock in early vertical forearm", interval: "On 2:15", focus: "Transfer drill feel to swim" },
          { id: "s_f51bxk", reps: 4, dist: 100, desc: "Freestyle — moderate, focus on full finish past hip each stroke", interval: "On 2:15", focus: "Complete the pull cycle" },
          { id: "s_g50my1", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "DPS (Distance Per Stroke)", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_fey9v8", reps: 4, dist: 50, desc: "Freestyle — count strokes (baseline)", interval: "On 1:20", focus: "Baseline" },
          { id: "s_bhkrj3", reps: 4, dist: 50, desc: "Freestyle — same speed, target -2 strokes", interval: "On 1:15", focus: "Increase DPS" },
          { id: "s_pvqez0", reps: 4, dist: 50, desc: "Freestyle — same speed, target -3 strokes", interval: "On 1:20", focus: "Maximize DPS" },
          { id: "s_9zn7gr", reps: 4, dist: 100, desc: "Freestyle — strong pace, hold target stroke count", interval: "On 2:15", focus: "DPS under pace" },
          { id: "s_13uc8z", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Pull Pattern Detail", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_779ka3", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on early catch", interval: "On 1:30", focus: "Early vertical forearm", eq: "pull" },
          { id: "s_ktzvs0", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on power phase", interval: "On 1:30", focus: "Mid-pull pressure", eq: "pull" },
          { id: "s_e3fcce", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on finish/release", interval: "On 1:30", focus: "Complete the stroke", eq: "pull" },
          { id: "s_jcdnek", reps: 4, dist: 100, desc: "Freestyle — integrate the full pull pattern", interval: "On 2:00", focus: "Apply detail to swim" },
          { id: "s_8ipwac", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Body Position", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_ja0cja", reps: 4, dist: 50, desc: "Backstroke kick — head still, hips up", interval: "On 1:30", focus: "Body line on back", eq: "kick" },
          { id: "s_p6wyu1", reps: 4, dist: 50, desc: "Backstroke — single-arm drill", interval: "On 1:30", focus: "Rotation drives stroke" },
          { id: "s_gt3j53", reps: 4, dist: 75, desc: "Backstroke — 25 drill + 50 swim", interval: "On 2:30", focus: "Bridge drill to stroke" },
          { id: "s_c6klil", reps: 4, dist: 75, desc: "Backstroke — full stroke, hold tight body line", interval: "On 2:30", focus: "Maintain position under pace" },
          { id: "s_wevt8m", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Open-Turn Practice", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_xiwfoq", reps: 8, dist: 50, desc: "Breaststroke — focus on legal touch + open turn", interval: "On 1:30", focus: "Sharp open-turn mechanics" },
          { id: "s_ltm6je", reps: 8, dist: 50, desc: "Butterfly — focus on legal touch + open turn", interval: "On 1:30", focus: "Fly open-turn mechanics" },
          { id: "s_tfjwzf", reps: 4, dist: 50, desc: "Choice — IM-transition turn (e.g., back→breast)", interval: "On 1:30", focus: "IM transition turns" },
          { id: "s_utognj", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Flip Turn Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_yxethl", reps: 12, dist: 50, desc: "Freestyle — focus on tight flip turns, fast push-off", interval: "On 1:15", focus: "Fast efficient flip turns" },
          { id: "s_vq8gql", reps: 4, dist: 75, desc: "Freestyle — turn into 3 strong strokes off wall", interval: "On 1:45", focus: "Explosive push-off + first strokes" },
          { id: "s_q4xkpk", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Slow-Motion Stroke", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_mfaj64", reps: 4, dist: 100, desc: "Freestyle — SLOWEST possible, perfect mechanics", interval: "On 3:00", focus: "Deliberate practice — every motion intentional" },
          { id: "s_wofx4v", reps: 4, dist: 75, desc: "Choice non-free — slow, perfect form", interval: "On 2:00", focus: "Apply slowness to stroke" },
          { id: "s_l1domm", reps: 4, dist: 50, desc: "Choice — slow, focus on connection", interval: "On 1:30", focus: "Feel the catch" },
          { id: "s_xtxvqb", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Side-Kick + Switch", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_jac75z", reps: 6, dist: 50, desc: "Kick on side — 25 left, 25 right", interval: "On 1:30", focus: "Side balance", eq: "kick" },
          { id: "s_40besq", reps: 6, dist: 50, desc: "6-kick switch drill (6 kicks before each switch)", interval: "On 1:20", focus: "Connect kick to stroke", eq: "kickPart" },
          { id: "s_6ogoia", reps: 4, dist: 100, desc: "Freestyle — apply rotation timing", interval: "On 2:00", focus: "Translate to swim" },
        ]},
      { label: "All-Stroke Slow Day", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_gn3m6w", reps: 4, dist: 100, desc: "Butterfly + Free (25 fly / 75 free) — slow", interval: "On 2:30", focus: "Fly form at slow tempo" },
          { id: "s_x7gvy9", reps: 4, dist: 100, desc: "Backstroke — slow, perfect form", interval: "On 2:30", focus: "Back form" },
          { id: "s_34cylv", reps: 2, dist: 100, desc: "Breaststroke — slow, glide-focused", interval: "On 3:15", focus: "Breast form" },
          { id: "s_dlq4zj", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Counted-Stroke 100s", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_hr3f2u", reps: 8, dist: 100, desc: "Freestyle — count strokes per 25, hold same count all 4 lengths", interval: "On 2:00", focus: "Stroke count discipline" },
          { id: "s_o2xg77", reps: 4, dist: 50, desc: "Freestyle — count strokes -1 from 100s baseline", interval: "On 1:15", focus: "Reduced stroke count" },
        ]},
      { label: "Catch + Kick Mix", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_hui6g1", reps: 4, dist: 50, desc: "Pull (buoy) — focus on catch", interval: "On 1:30", focus: "Catch detail", eq: "pull" },
          { id: "s_2hv7ih", reps: 4, dist: 50, desc: "Kick — moderate (board optional)", interval: "On 1:45", focus: "Leg engagement", eq: "kick" },
          { id: "s_qhsi1d", reps: 4, dist: 75, desc: "25 pull / 25 kick / 25 swim — same stroke", interval: "On 2:30", focus: "Apply both into swim" },
          { id: "s_dl56e5", reps: 4, dist: 100, desc: "Freestyle — full stroke, integrate everything", interval: "On 2:00", focus: "Whole-stroke integration" },
          { id: "s_nkq3w3", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Reverse Stroke Practice", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_dv5xs6", reps: 4, dist: 50, desc: "Freestyle — reverse stroke (start at finish, work backward through phases)", interval: "On 1:30", focus: "Understand stroke phases by reversing" },
          { id: "s_xim3en", reps: 4, dist: 50, desc: "Freestyle — exaggerated normal-direction stroke", interval: "On 1:15", focus: "Apply phase awareness forward" },
          { id: "s_84b2ih", reps: 4, dist: 100, desc: "Freestyle — moderate pace, mindful of each phase", interval: "On 2:15", focus: "Phase-aware swimming" },
          { id: "s_si3wgn", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tempo Trainer Mock", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_zlluup", reps: 4, dist: 50, desc: "Freestyle — slow, deliberate tempo (count seconds per stroke)", interval: "On 1:15", focus: "Slow tempo" },
          { id: "s_562mjc", reps: 4, dist: 50, desc: "Freestyle — increase tempo by 1 'click' (faster turnover)", interval: "On 1:15", focus: "Faster tempo" },
          { id: "s_gpiei0", reps: 4, dist: 50, desc: "Freestyle — race tempo, hold count over longer reps", interval: "On 1:15", focus: "Race tempo" },
          { id: "s_qubg9z", reps: 4, dist: 100, desc: "Freestyle — moderate, tempo-aware", interval: "On 2:00", focus: "Apply tempo awareness" },
          { id: "s_kzqv0i", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Streamline Volume", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_7hpu3q", reps: 12, dist: 25, desc: "Streamline kick + 1 stroke (10m UW + freestyle finish)", interval: "On :45", focus: "Long underwater off every wall", eq: "underkick" },
          { id: "s_2m8yat", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off and underwater", interval: "On 2:00", focus: "Build streamline habit" },
          { id: "s_m2ycte", reps: 4, dist: 75, desc: "Freestyle — 25 underwater dolphin + 50 swim", interval: "On 1:45", focus: "Underwater + swim", eq: "underkick" },
          { id: "s_wwiiaz", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Connection Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_st4iap", reps: 4, dist: 50, desc: "Freestyle — feel hand-to-hip connection (one stroke at a time)", interval: "On 1:30", focus: "Whole-body connection" },
          { id: "s_ifswbr", reps: 4, dist: 75, desc: "Freestyle — apply connection at moderate tempo", interval: "On 1:45", focus: "Connection at pace" },
          { id: "s_i7nio1", reps: 4, dist: 100, desc: "Freestyle — apply connection at strong pace", interval: "On 2:00", focus: "Connection at speed" },
          { id: "s_s117lt", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Front Quadrant Drill", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_nhxoww", reps: 8, dist: 50, desc: "Catch-up freestyle — exaggerate front-quadrant timing", interval: "On 1:30", focus: "Both arms in front quadrant" },
          { id: "s_1fbdf4", reps: 4, dist: 75, desc: "Freestyle — slight catch-up timing at moderate pace", interval: "On 2:30", focus: "Apply timing in stroke" },
          { id: "s_uu9h8o", reps: 3, dist: 100, desc: "Freestyle — front-quadrant aware at strong pace", interval: "On 2:00", focus: "Sustain timing under speed" },
        ]},
      { label: "Body Line Mastery", totalYards: 1050, types: ["technique"], strokes: [], sets: [
          { id: "s_69onlz", reps: 4, dist: 50, desc: "Streamline kick on back — head still, hips up", interval: "On 1:30", focus: "Body line on back", eq: "kick" },
          { id: "s_foj4q7", reps: 4, dist: 50, desc: "Streamline kick on front — head down", interval: "On 1:30", focus: "Body line on front", eq: "kick" },
          { id: "s_j0bnh6", reps: 4, dist: 75, desc: "Freestyle — focus on hairline at the surface", interval: "On 1:45", focus: "Neutral head position reduces drag" },
          { id: "s_vkh6b8", reps: 3, dist: 100, desc: "Freestyle — strong pace, hold body line", interval: "On 2:00", focus: "Body line under speed" },
          { id: "s_yikxgm", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "Technique 10×150", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_jpfjdl", reps: 10, dist: 150, desc: "Freestyle — 50 drill (choice) / 50 swim / 50 strong swim — apply drill into speed", interval: "On 3:15", focus: "High-rep drill-to-swim transfer across volume" },
        ]},
      { label: "Technique 20×100", totalYards: 2000, types: ["technique"], strokes: [], sets: [
          { id: "s_2pt7ls", reps: 20, dist: 100, desc: "Freestyle — 50 drill / 50 swim — one technical focus for the entire set", interval: "On 2:15", focus: "Sustained drill-to-swim practice at volume" },
        ]},
      { label: "Technique 8×300", totalYards: 2400, types: ["technique"], strokes: [], sets: [
          { id: "s_vf2jzc", reps: 8, dist: 300, desc: "Freestyle — 100 drill / 100 build / 100 strong — escalate quality each section", interval: "On 6:30", focus: "Extended technique integration at high volume" },
        ]},
      { label: "Drill-Swim 50s Repeat Unit", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_emjxny", reps: 8, dist: 50, desc: "25 drill / 25 swim — one focus per rep (catch, rotation, reach, finish)", interval: "On 1:20", focus: "Apply each drill point directly into the swim — designed to repeat 2–3×" },
        ]},
      { label: "Technique 75s Repeat Unit", totalYards: 525, types: ["technique"], strokes: [], sets: [
          { id: "s_83zfc1", reps: 7, dist: 75, desc: "25 drill / 25 build / 25 strong — escalate quality within each 75", interval: "On 1:45", focus: "Drill-to-swim integration at low yardage" },
        ]},
      { label: "Catch-Up Build Repeat Unit", totalYards: 600, types: ["technique"], strokes: [], sets: [
          { id: "s_ztpj3y", reps: 4, dist: 100, desc: "Freestyle — 50 catch-up drill / 50 swim, focus on full extension before pull", interval: "On 2:20", focus: "Extension and catch mechanics" },
          { id: "s_5ited0", reps: 4, dist: 50, desc: "Freestyle — swim normal, hold the feeling from the drill", interval: "On 1:15", focus: "Transfer drill to full swimming" },
        ]},
      { label: "30/30 Aerobic", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_bhrhzu", reps: 1, dist: 200, desc: "Easy choice — settle in", interval: "On 4:30", focus: "Open the aerobic engine" },
          { id: "s_o9gnbm", reps: 20, dist: 50, desc: "Freestyle — alternate: odd 50s moderate-strong, even 50s easy (the easy 50 IS the rest, no wall rest)", interval: "On 1:00", focus: "Continuous aerobic — 1000 yards without leaving the water" },
          { id: "s_7zbft9", reps: 1, dist: 300, desc: "Easy backstroke — long cooldown", interval: "On 7:00", focus: "Flush the legs" },
        ]},
      { label: "Rolling 50s", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_7n06j3", reps: 24, dist: 50, desc: "Freestyle — every 4th 50 is strong; the rest are moderate. No rest between (rolling, continuous).", interval: "On 1:00", focus: "Pace shifts inside continuous aerobic — train surge recovery" },
        ]},
      { label: "Aerobic Goldilocks", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_on80ig", reps: 5, dist: 100, desc: "Freestyle — too easy (RPE 4/10) — almost lazy long stroke", interval: "On 2:00", focus: "Bottom of the band" },
          { id: "s_7yomtu", reps: 5, dist: 100, desc: "Freestyle — too hard (RPE 8/10) — uncomfortable but sustainable", interval: "On 2:00", focus: "Top of the band" },
          { id: "s_y4cxog", reps: 5, dist: 100, desc: "Freestyle — JUST RIGHT (RPE 6/10) — find the middle, hold it", interval: "On 2:00", focus: "Sweet-spot aerobic — the pace you should know by feel" },
        ]},
      { label: "Hypoxic Pull 500", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_1n4sjx", reps: 1, dist: 300, desc: "Easy pull (buoy + paddles optional) — breathe every 3, open the lungs", interval: "On 6:30", focus: "Prime" },
          { id: "s_s1fw1n", reps: 1, dist: 500, desc: "Pull — moderate, breathe every 5 the whole way (drop to 3/5 by 50 if you can't hold every 5)", interval: "On 11:00", focus: "Restricted-breathing endurance — keep the stroke long" },
          { id: "s_4e6kgc", reps: 1, dist: 200, desc: "Easy free — every 3 — flush", interval: "On 4:30", focus: "Recover" },
          { id: "s_3j5wwe", reps: 1, dist: 500, desc: "Pull — moderate, breathe by 100: 3 / 5 / 7 / 5 / 3 (ladder — back off if dizzy)", interval: "On 11:00", focus: "Air-control variability under fatigue" },
        ]},
      { label: "Tempo Ladder 100s", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_6zhm58", reps: 3, dist: 100, desc: "Freestyle — easy tempo (RPE 5)", interval: "On 2:00", focus: "Lowest tempo — long stroke" },
          { id: "s_jr0gde", reps: 3, dist: 100, desc: "Freestyle — moderate tempo (RPE 6)", interval: "On 2:00", focus: "Step 1" },
          { id: "s_tg13g1", reps: 3, dist: 100, desc: "Freestyle — moderate-strong tempo (RPE 7)", interval: "On 2:00", focus: "Step 2" },
          { id: "s_uh861c", reps: 3, dist: 100, desc: "Freestyle — strong tempo (RPE 8)", interval: "On 2:00", focus: "Step 3 — hold form as turnover climbs" },
          { id: "s_vv5dnk", reps: 3, dist: 100, desc: "Freestyle — drop back to moderate (RPE 6)", interval: "On 2:00", focus: "Recover the tempo on the way down" },
        ]},
      { label: "Step-Down 400s", totalYards: 1600, types: ["technique"], strokes: [], sets: [
          { id: "s_ba1trk", reps: 1, dist: 400, desc: "Freestyle — RPE 5, easy aerobic", interval: "On 8:30", focus: "Bottom step" },
          { id: "s_m1v4ig", reps: 1, dist: 400, desc: "Freestyle — RPE 6, drop 5s/100 vs the previous", interval: "On 8:00", focus: "Step up" },
          { id: "s_2743iq", reps: 1, dist: 400, desc: "Freestyle — RPE 7, drop another 5s/100 (10s total vs first)", interval: "On 7:30", focus: "Step up again" },
          { id: "s_wc1sdk", reps: 1, dist: 400, desc: "Freestyle — RPE 8, drop another 5s/100 (15s total vs first)", interval: "On 7:00", focus: "Top step — finish faster, with shorter rest" },
        ]},
      { label: "Compact Mix", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_06rs1h", reps: 3, dist: 200, desc: "Freestyle — aerobic, descend 1→3", interval: "On 4:15", focus: "Distance base at controlled effort" },
          { id: "s_jui6k2", reps: 4, dist: 100, desc: "IM or choice — moderate to fast, descend", interval: "On 2:00", focus: "Mix strokes, build intensity" },
        ]},
      { label: "Speed + Distance Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_t8sz52", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:30", focus: "Distance base — stay aerobic" },
          { id: "s_33yah9", reps: 1, dist: 200, desc: "Easy backstroke — active recovery", interval: "4:00 before next block", focus: "Recover while moving" },
          { id: "s_30ysg2", reps: 8, dist: 50, desc: "Choice of stroke — ALL OUT sprint", interval: "On 1:45", focus: "Speed work after accumulated fatigue" },
        ]},
      { label: "3-Part Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_w175or", reps: 3, dist: 200, desc: "Freestyle — strong aerobic", interval: "On 4:15", focus: "Build aerobic capacity" },
          { id: "s_wbdp27", reps: 6, dist: 50, desc: "25 Drill / 25 Swim — stroke choice", interval: "On 1:25", focus: "Technique reset mid-workout" },
          { id: "s_n1dzhn", reps: 3, dist: 100, desc: "IM or stroke choice — moderate to fast, descend 1→3", interval: "On 2:00", focus: "Finish strong on the last one" },
        ]},
      { label: "Mixed Pyramid", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_8plqrc", reps: 1, dist: 400, desc: "Freestyle — aerobic, controlled effort", interval: "On 8:30", focus: "Establish aerobic base" },
          { id: "s_kj00hn", reps: 2, dist: 200, desc: "Choice of stroke — moderate to fast", interval: "On 4:15", focus: "Pick up the pace, find threshold" },
          { id: "s_hsru4l", reps: 4, dist: 100, desc: "IM or choice — hard effort", interval: "On 2:00", focus: "Push lactate threshold" },
          { id: "s_3hmtz8", reps: 4, dist: 50, desc: "Sprint — choice stroke, ALL OUT", interval: "On 1:30", focus: "Top-end speed" },
        ]},
      { label: "Pull + Kick Mix", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_fr9zo7", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:15", focus: "Upper-body block", eq: "pull" },
          { id: "s_xrrrog", reps: 1, dist: 100, desc: "Easy freestyle — transition", interval: "On 2:00", focus: "Reset between blocks" },
          { id: "s_g3jdhk", reps: 4, dist: 50, desc: "Kick — moderate effort (board optional)", interval: "On 1:45", focus: "Lower-body block", eq: "kick" },
          { id: "s_rpe446", reps: 4, dist: 100, desc: "Freestyle — strong, integrate both", interval: "On 2:15", focus: "Apply pull + kick into full stroke" },
        ]},
      { label: "Sprint Recovery Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_oz3nb9", reps: 6, dist: 100, desc: "50 ALL OUT / 50 easy (immediately)", interval: "On 2:00", focus: "Practice rapid recovery between hard efforts" },
          { id: "s_ewso3z", reps: 1, dist: 200, desc: "Easy backstroke — full recovery", interval: "On 5:30", focus: "Active recovery between blocks" },
          { id: "s_ynijhi", reps: 4, dist: 100, desc: "Freestyle — strong, hold near-threshold pace", interval: "On 2:15", focus: "Sustained effort with the lactate already there" },
        ]},
      { label: "IM-Free-Choice Triplets", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_d6r8sa", reps: 4, dist: 100, desc: "IM — moderate, focus on transitions", interval: "On 3:00", focus: "Smooth stroke order under control" },
          { id: "s_mvv86r", reps: 4, dist: 100, desc: "Freestyle — descend 1→4 (each faster)", interval: "On 2:10", focus: "Build pace round to round" },
          { id: "s_jng27o", reps: 4, dist: 100, desc: "Choice stroke (non-free) — strong, hold form", interval: "On 2:10", focus: "Pick your weakest stroke for variety" },
        ]},
      { label: "Race Day Simulation", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_ljj0vs", reps: 1, dist: 200, desc: "Freestyle — easy warm-up swim", interval: "On 6:00", focus: "Settle in" },
          { id: "s_cxf188", reps: 1, dist: 100, desc: "Choice stroke — race pace test", interval: "On 3:00", focus: "First race effort" },
          { id: "s_yy2cyk", reps: 1, dist: 200, desc: "Easy choice — recovery between races", interval: "No interval — swim easy", focus: "Reset between events" },
          { id: "s_lqhfrq", reps: 1, dist: 200, desc: "IM — race pace simulation", interval: "On 5:00", focus: "Second race effort" },
          { id: "s_odlimr", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_i0l57e", reps: 1, dist: 50, desc: "Choice stroke — sprint finish", interval: "On 2:00", focus: "Final race" },
          { id: "s_8e8bno", reps: 1, dist: 250, desc: "Easy choice — final cool", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Quad Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_z6wh5z", reps: 6, dist: 50, desc: "Kick — choice stroke (board optional)", interval: "On 1:45", focus: "Leg block", eq: "kick" },
          { id: "s_x15wc3", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:15", focus: "Pull block", eq: "pull" },
          { id: "s_nmcbz2", reps: 4, dist: 50, desc: "25 Drill / 25 Swim — choice stroke", interval: "On 1:30", focus: "Drill block" },
          { id: "s_9fgne1", reps: 3, dist: 100, desc: "Freestyle — strong, integrate all", interval: "On 2:15", focus: "Apply all four phases into stroke" },
        ]},
      { label: "Variety Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_mlrv6v", reps: 1, dist: 50, desc: "Butterfly — easy/moderate", interval: "On 1:30", focus: "Open up fly" },
          { id: "s_kl65na", reps: 2, dist: 100, desc: "Backstroke — moderate", interval: "On 2:45", focus: "Settle into back" },
          { id: "s_agh3w2", reps: 2, dist: 150, desc: "Breaststroke — moderate", interval: "On 3:00", focus: "Hold breast tempo" },
          { id: "s_dwazmx", reps: 2, dist: 200, desc: "Freestyle — strong", interval: "On 4:15", focus: "Push the freestyle" },
          { id: "s_i5gmsq", reps: 1, dist: 250, desc: "IM — moderate to strong", interval: "On 5:30", focus: "All four strokes under fatigue" },
        ]},
      { label: "Speed + Recovery Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_nouqtk", reps: 4, dist: 200, desc: "100 ALL OUT / 100 easy (immediate)", interval: "On 6:00", focus: "Hard/easy alternation" },
          { id: "s_06d9bl", reps: 4, dist: 100, desc: "Choice — strong, hold quality stroke", interval: "On 2:00", focus: "Quality after fatigue" },
        ]},
      { label: "Threshold + Sprint", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_cxsr9r", reps: 4, dist: 200, desc: "Freestyle — threshold pace (RPE 8/10)", interval: "On 4:15", focus: "Build threshold base" },
          { id: "s_7o6dfe", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_s3a8ba", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:30", focus: "Sprint after threshold accumulation" },
        ]},
      { label: "Aerobic + IM Combo", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_s56u1o", reps: 1, dist: 400, desc: "Freestyle — steady aerobic effort", interval: "On 8:30", focus: "Aerobic base" },
          { id: "s_cendwx", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:00", focus: "IM after aerobic load" },
          { id: "s_t6gjwf", reps: 4, dist: 100, desc: "Freestyle — descending pace 1→4", interval: "On 2:10", focus: "Build pace round to round" },
        ]},
      { label: "Pull + Sprint Day", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_a7v96j", reps: 6, dist: 100, desc: "Pull (buoy) — moderate to strong, build pace", interval: "On 2:15", focus: "Build upper-body strength", eq: "pull" },
          { id: "s_8198us", reps: 1, dist: 200, desc: "Easy choice — transition", interval: "On 4:00", focus: "Reset" },
          { id: "s_3af2hw", reps: 6, dist: 50, desc: "Choice stroke — fast sprints", interval: "On 1:30", focus: "Apply pull strength to sprints" },
        ]},
      { label: "Long + Short Day", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_gg51ig", reps: 1, dist: 500, desc: "Freestyle — steady aerobic", interval: "On 11:00", focus: "Long base" },
          { id: "s_ynjoju", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_38s7yq", reps: 12, dist: 50, desc: "Choice stroke — descend by 4 (1-4 fast, 5-8 faster, 9-12 fastest)", interval: "On 1:15", focus: "Short fast 50s" },
        ]},
      { label: "Distance + Drill Combo", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_yx0d7s", reps: 1, dist: 400, desc: "Freestyle — steady aerobic", interval: "On 8:30", focus: "Distance base" },
          { id: "s_wa5wkc", reps: 8, dist: 50, desc: "25 Drill / 25 Swim — choice stroke", interval: "On 1:30", focus: "Mid-workout technique reset" },
          { id: "s_2km5t8", reps: 3, dist: 100, desc: "Freestyle — strong, apply drill into stroke", interval: "On 2:15", focus: "Carry technique to fast swimming" },
        ]},
      { label: "All-Stroke Tour", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_144q4p", reps: 3, dist: 100, desc: "Butterfly (or 25 fly + 75 free)", interval: "On 2:30", focus: "Fly endurance" },
          { id: "s_hnw1ox", reps: 3, dist: 100, desc: "Backstroke — moderate, smooth pace", interval: "On 2:45", focus: "Back tempo" },
          { id: "s_b0rac2", reps: 3, dist: 100, desc: "Breaststroke — moderate to strong", interval: "On 3:15", focus: "Breast tempo" },
          { id: "s_375v0z", reps: 3, dist: 100, desc: "Freestyle — strong finishing pace", interval: "On 2:15", focus: "Free endurance" },
        ]},
      { label: "Descending Mixed Ladder", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_7lem01", reps: 1, dist: 400, desc: "Freestyle — aerobic, establish your pace", interval: "On 8:30", focus: "Anchor the ladder — steady aerobic effort" },
          { id: "s_cyf4w0", reps: 1, dist: 300, desc: "Backstroke or choice — match free pace", interval: "On 6:30", focus: "Stroke change keeps it honest" },
          { id: "s_fim5tj", reps: 1, dist: 200, desc: "Freestyle — strong, find another gear", interval: "On 4:15", focus: "Negative split into the descent" },
          { id: "s_ye7msg", reps: 1, dist: 100, desc: "Choice stroke — sprint, finish strongest", interval: "On 2:15", focus: "Empty the tank" },
        ]},
      { label: "Mixed Ascending Ladder", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_d8uxjl", reps: 1, dist: 100, desc: "Choice stroke — controlled sprint, wake up fast-twitch", interval: "On 2:15", focus: "Start sharp before the distance builds" },
          { id: "s_lr74z8", reps: 1, dist: 200, desc: "100 IM + 100 free — moderate, smooth transitions", interval: "On 4:30", focus: "Add IM complexity as volume grows" },
          { id: "s_et0g6o", reps: 1, dist: 300, desc: "Freestyle — moderate to strong, 3 × 100 in your head", interval: "On 6:30", focus: "Sustain pace over longer distance" },
          { id: "s_roc3ge", reps: 1, dist: 400, desc: "Choice stroke — strong, finish better than you started", interval: "On 9:00", focus: "Top of the ladder — best effort of the set" },
        ]},
      { label: "Sprint + Distance Bookends", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_849uzp", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:30", focus: "Open with speed" },
          { id: "s_i4a0cq", reps: 1, dist: 400, desc: "Freestyle — moderate aerobic", interval: "On 8:30", focus: "Distance middle" },
          { id: "s_ojxctx", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (after distance)", interval: "On 1:30", focus: "Speed under fatigue" },
        ]},
      { label: "Threshold + Sprint Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_znmbpx", reps: 4, dist: 200, desc: "Freestyle — threshold pace", interval: "On 4:15", focus: "Threshold base" },
          { id: "s_i22d6l", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (after threshold)", interval: "On 1:30", focus: "Sprint after lactate accumulation" },
        ]},
      { label: "Pull-Drill-Sprint Block", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_ypcgko", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:15", focus: "Pull block", eq: "pull" },
          { id: "s_veeesa", reps: 4, dist: 50, desc: "Drill — choice stroke, technique focus", interval: "On 1:15", focus: "Drill block" },
          { id: "s_6neirj", reps: 1, dist: 100, desc: "Easy freestyle — transition", interval: "On 2:00", focus: "Reset" },
          { id: "s_wifeq1", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:30", focus: "Sprint block" },
        ]},
      { label: "All-Stroke Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_r9b8n2", reps: 1, dist: 50, desc: "Butterfly — moderate", interval: "On 1:30", focus: "Open fly" },
          { id: "s_ucnwoj", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:45", focus: "Back" },
          { id: "s_w5tk60", reps: 1, dist: 150, desc: "Breaststroke — moderate", interval: "On 3:00", focus: "Breast" },
          { id: "s_012g0g", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:15", focus: "Free" },
          { id: "s_8x3npc", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:00", focus: "All four" },
          { id: "s_iqwacl", reps: 1, dist: 150, desc: "Breaststroke — strong (match earlier)", interval: "On 3:00", focus: "Same pace under fatigue" },
          { id: "s_kxvrjl", reps: 1, dist: 100, desc: "Backstroke — strong", interval: "On 2:45", focus: "Pick up pace" },
          { id: "s_3wrk8r", reps: 1, dist: 50, desc: "Butterfly — ALL OUT (best 50)", interval: "On 1:30", focus: "Final blast" },
          { id: "s_5olzx8", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "5-Way Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_dp6jex", reps: 1, dist: 200, desc: "Freestyle — moderate aerobic", interval: "On 4:30", focus: "Aerobic phase" },
          { id: "s_byjh34", reps: 1, dist: 200, desc: "IM — moderate, smooth transitions", interval: "On 6:00", focus: "IM phase" },
          { id: "s_ufsaon", reps: 1, dist: 200, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 4:15", focus: "Pull phase", eq: "pull" },
          { id: "s_e3pfqg", reps: 1, dist: 200, desc: "Kick — choice stroke (board optional)", interval: "On 4:30", focus: "Kick phase", eq: "kick" },
          { id: "s_sagumj", reps: 1, dist: 200, desc: "Choice stroke — strong, integrate it all", interval: "On 4:00", focus: "Apply phase" },
          { id: "s_8bqit8", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Negative-Split Distance + Sprint", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_aco42k", reps: 2, dist: 400, desc: "Freestyle — negative split (second 200 faster)", interval: "On 8:30", focus: "Practice closing acceleration" },
          { id: "s_0aesuw", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:30", focus: "Sprint after distance" },
        ]},
      { label: "IM + Free Sprints", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_psl4pk", reps: 3, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:00", focus: "IM base" },
          { id: "s_8bqfal", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Reset" },
          { id: "s_m3nnsv", reps: 8, dist: 50, desc: "Freestyle — fast, descend 1-4 then 5-8", interval: "On 1:15", focus: "Two-block free sprints" },
        ]},
      { label: "Distance + Stroke Variety", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_3f1nyd", reps: 1, dist: 400, desc: "Freestyle — aerobic", interval: "On 8:30", focus: "Aerobic base" },
          { id: "s_wmmxnb", reps: 4, dist: 100, desc: "Choice non-free stroke — moderate", interval: "On 2:30", focus: "Stroke variety" },
          { id: "s_je8vpi", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:00", focus: "Mid-set IM" },
          { id: "s_0xwt5i", reps: 3, dist: 100, desc: "Freestyle — strong, descend", interval: "On 2:15", focus: "Closing pace" },
        ]},
      { label: "Hard-Easy 200s", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_4ignmd", reps: 6, dist: 200, desc: "Odd reps strong (RPE 8) / even reps easy (RPE 5)", interval: "On 4:30", focus: "Practice rapid-recovery alternation" },
        ]},
      { label: "Fartlek Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_sz47jj", reps: 1, dist: 1200, desc: "Continuous freestyle — fartlek (vary pace every 100: easy/moderate/hard, your choice each rep)", interval: "On 26:00", focus: "Self-paced variety over distance" },
        ]},
      { label: "Pull + IM Sandwich", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_1p8jhi", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, smooth", interval: "On 2:15", focus: "Pull base", eq: "pull" },
          { id: "s_8ozq50", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:00", focus: "IM middle" },
          { id: "s_md3eos", reps: 4, dist: 100, desc: "Pull (buoy) — strong, descend 1→4", interval: "On 2:15", focus: "Pull build", eq: "pull" },
        ]},
      { label: "Specialty + Sprint Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_wgflmz", reps: 4, dist: 100, desc: "Specialty stroke (your choice) — race-pace test", interval: "On 2:30", focus: "Race-distance specialty" },
          { id: "s_eewdeh", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_m7x141", reps: 4, dist: 75, desc: "Choice — first 50 fast / last 25 ALL OUT", interval: "On 2:00", focus: "Negative-split sprint" },
          { id: "s_hujadx", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:30", focus: "Top-off speed" },
          { id: "s_hckxhm", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Race-Day Three Events", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_nf30sp", reps: 1, dist: 200, desc: "Easy warm-up swim", interval: "On 6:00", focus: "Race-day feel" },
          { id: "s_lkkejk", reps: 1, dist: 200, desc: "Choice stroke — race pace (Event 1)", interval: "On 5:00", focus: "First race effort" },
          { id: "s_4we2v5", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Inter-event recovery" },
          { id: "s_xmkp1z", reps: 1, dist: 100, desc: "Choice stroke — race pace (Event 2)", interval: "On 3:00", focus: "Sprint race" },
          { id: "s_eqjw7f", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_zsamtf", reps: 1, dist: 200, desc: "IM — race-pace simulation (Event 3)", interval: "On 5:00", focus: "IM race" },
          { id: "s_y6vu4s", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic + Threshold Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_guofu9", reps: 1, dist: 600, desc: "Freestyle — aerobic, even effort", interval: "On 13:30", focus: "Aerobic base" },
          { id: "s_692vgs", reps: 4, dist: 100, desc: "Freestyle — threshold pace, descend 1→4", interval: "On 2:15", focus: "Build threshold" },
          { id: "s_gttoxt", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Three-Stroke Day (no fly)", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_p2o5ow", reps: 4, dist: 100, desc: "Backstroke — moderate to strong, descend 1→4", interval: "On 2:45", focus: "Back" },
          { id: "s_2a8ihh", reps: 4, dist: 100, desc: "Breaststroke — moderate to strong, descend 1→4", interval: "On 3:15", focus: "Breast" },
          { id: "s_7baxnn", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4", interval: "On 2:15", focus: "Free" },
        ]},
      { label: "Drill + Pull + Swim", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_f0dmkm", reps: 4, dist: 50, desc: "Drill — choice stroke, technique focus", interval: "On 1:15", focus: "Drill block" },
          { id: "s_g95kt4", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 2:15", focus: "Pull block", eq: "pull" },
          { id: "s_1fenjq", reps: 4, dist: 50, desc: "Drill — same stroke as pull", interval: "On 1:15", focus: "Reapply drill after pull" },
          { id: "s_prs5zy", reps: 4, dist: 100, desc: "Choice stroke — strong, integrate everything", interval: "On 2:00", focus: "Whole-stroke integration" },
        ]},
      { label: "Race-Pace Mix", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_02b649", reps: 4, dist: 100, desc: "Choice stroke — race pace (full recovery)", interval: "On 3:00", focus: "Race-pace 100s" },
          { id: "s_92nw0n", reps: 4, dist: 50, desc: "Choice stroke — race pace", interval: "On 1:30", focus: "Race-pace 50s" },
          { id: "s_2akhrv", reps: 4, dist: 75, desc: "Choice stroke — race pace", interval: "On 2:00", focus: "Race-pace 75s" },
          { id: "s_4fe1tk", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Long + Sprint Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_w2d819", reps: 4, dist: 250, desc: "Freestyle 200 moderate + 50 ALL OUT (immediate)", interval: "On 5:00", focus: "Sprint after each distance segment" },
          { id: "s_rxrxps", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint + Pull + Free", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_t09e4l", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Open sprint" },
          { id: "s_snezw1", reps: 4, dist: 100, desc: "Pull (buoy) — strong, sustain pace", interval: "On 2:15", focus: "Pull middle", eq: "pull" },
          { id: "s_py0ci6", reps: 4, dist: 75, desc: "Freestyle — fast, integrate", interval: "On 1:45", focus: "Apply pull power" },
          { id: "s_iptbhh", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Quad-Set Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_fid3y9", reps: 4, dist: 50, desc: "Round 1: choice stroke — ALL OUT 50s", interval: "On 1:30", focus: "Sprint round" },
          { id: "s_cqoswj", reps: 4, dist: 75, desc: "Round 2: choice stroke — fast 75s", interval: "On 1:45", focus: "Race-distance round" },
          { id: "s_c2rrot", reps: 4, dist: 100, desc: "Round 3: choice stroke — strong 100s", interval: "On 2:00", focus: "Endurance round" },
          { id: "s_csz52u", reps: 4, dist: 75, desc: "Round 4: choice stroke — moderate 75s", interval: "On 1:45", focus: "Recovery round" },
          { id: "s_asrbac", reps: 4, dist: 50, desc: "Round 5: choice stroke — easy 50s", interval: "On 1:30", focus: "Cool round" },
        ]},
      { label: "IM Threshold + Sprints", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_5c5bjc", reps: 4, dist: 200, desc: "200 IM — threshold pace", interval: "On 6:00", focus: "IM threshold" },
          { id: "s_zx5ccw", reps: 4, dist: 50, desc: "Freestyle — ALL OUT, max turnover", interval: "On 1:30", focus: "Sprint after IM" },
          { id: "s_yvpuyk", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:30", focus: "Cool down" },
        ]},
      { label: "Long-to-Short Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_qw3nry", reps: 1, dist: 400, desc: "Freestyle — aerobic", interval: "On 8:30", focus: "Aerobic base" },
          { id: "s_e2utv1", reps: 1, dist: 300, desc: "Freestyle — moderate", interval: "On 6:30", focus: "Build" },
          { id: "s_u318pz", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:15", focus: "Threshold" },
          { id: "s_uate81", reps: 1, dist: 100, desc: "Freestyle — fast", interval: "On 2:15", focus: "Sub-threshold" },
          { id: "s_x2bk34", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:30", focus: "Sprint close-out" },
        ]},
      { label: "Stroke Tempo Day", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_7ef4xm", reps: 4, dist: 100, desc: "Backstroke — moderate", interval: "On 2:45", focus: "Back tempo" },
          { id: "s_0u7be6", reps: 4, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:15", focus: "Breast tempo" },
          { id: "s_pqw8lf", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:15", focus: "Free tempo" },
          { id: "s_wvtv7m", reps: 4, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Fly speed bursts" },
          { id: "s_6qhjgo", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Pull/Kick Alternation", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_mmbk7f", reps: 6, dist: 100, desc: "Pull (buoy) — moderate", interval: "On 2:15", focus: "Pull volume", eq: "pull" },
          { id: "s_tzqz1w", reps: 6, dist: 100, desc: "Kick — choice stroke (board optional)", interval: "On 2:30", focus: "Kick volume", eq: "kick" },
        ]},
      { label: "Test-Set Sandwich", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_tqvev1", reps: 1, dist: 200, desc: "Freestyle — easy warm-up", interval: "On 4:00", focus: "Open up" },
          { id: "s_8whryl", reps: 1, dist: 500, desc: "Freestyle — TEST (race-pace 500)", interval: "On 11:00", focus: "Race-pace test" },
          { id: "s_r4yj6x", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:30", focus: "Reset" },
          { id: "s_4rzdoa", reps: 4, dist: 75, desc: "Choice stroke — fast finishers", interval: "On 1:45", focus: "Speed work after test" },
        ]},
      { label: "Sprint Endurance Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_ynboy5", reps: 12, dist: 75, desc: "Choice stroke — fast (race pace), short rest", interval: "On 1:30", focus: "Sprint endurance volume" },
          { id: "s_b4jy0v", reps: 4, dist: 75, desc: "Easy choice — close", interval: "On 1:45", focus: "Cool down" },
        ]},
      { label: "All-Around Day", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_vhz56g", reps: 1, dist: 300, desc: "Freestyle — moderate aerobic", interval: "On 6:30", focus: "Aerobic" },
          { id: "s_0eih4x", reps: 4, dist: 100, desc: "100 IM — moderate", interval: "On 3:00", focus: "IM" },
          { id: "s_nyhaf5", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend 1→4", interval: "On 2:15", focus: "Pull build", eq: "pull" },
          { id: "s_pvma1n", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On 1:00", focus: "Sprint" },
          { id: "s_xlx3mw", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Distance Endurance + Speed", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_kbismg", reps: 1, dist: 800, desc: "Freestyle — steady aerobic", interval: "On 17:00", focus: "Long base" },
          { id: "s_tl079c", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:15", focus: "Sub-threshold finish" },
          { id: "s_x70578", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Stroke 50/100 Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_7tmoss", reps: 4, dist: 50, desc: "Butterfly — fast (or 25 fly + 25 free)", interval: "On 1:30", focus: "Fly speed" },
          { id: "s_4f9h0e", reps: 4, dist: 100, desc: "Backstroke — moderate to strong", interval: "On 2:45", focus: "Back tempo" },
          { id: "s_fc4mn8", reps: 4, dist: 50, desc: "Breaststroke — fast", interval: "On 1:30", focus: "Breast speed" },
          { id: "s_8anc2t", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:15", focus: "Free tempo" },
          { id: "s_7p75zm", reps: 4, dist: 50, desc: "Easy choice — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "Pyramid + Kick Finish", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_q4dt00", reps: 1, dist: 50, desc: "Freestyle — easy build", interval: "On 1:15", focus: "Open up" },
          { id: "s_xcbpjp", reps: 1, dist: 100, desc: "Freestyle — moderate", interval: "On 2:15", focus: "Build" },
          { id: "s_3eizda", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:15", focus: "Threshold" },
          { id: "s_nudabb", reps: 1, dist: 100, desc: "Freestyle — fast", interval: "On 2:15", focus: "Sub-threshold" },
          { id: "s_8xycyy", reps: 1, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:00", focus: "Sprint" },
          { id: "s_x9anlx", reps: 6, dist: 100, desc: "Kick — choice stroke (board optional), moderate", interval: "On 2:30", focus: "Kick close-out", eq: "kick" },
        ]},
      { label: "Mixed 6×250", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_ddogcw", reps: 6, dist: 250, desc: "Choice stroke or IM — alternate between freestyle and choice stroke every rep", interval: "On 5:15", focus: "Sustained variety across mixed distance" },
        ]},
      { label: "Mixed Pyramid Volume 2000", totalYards: 2000, types: ["mixed"], strokes: [], sets: [
          { id: "s_hms63p", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:15", focus: "Distance base" },
          { id: "s_756sju", reps: 4, dist: 150, desc: "Choice stroke — moderate to strong", interval: "On 3:15", focus: "Stroke variety mid-set" },
          { id: "s_n3ne6c", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:15", focus: "Push the pace" },
          { id: "s_wbn0m7", reps: 4, dist: 50, desc: "Choice stroke — fast finisher", interval: "On 1:15", focus: "Speed close-out" },
        ]},
      { label: "Mixed Triple Block 2400", totalYards: 2400, types: ["mixed"], strokes: [], sets: [
          { id: "s_ahpe8x", reps: 4, dist: 300, desc: "Freestyle — aerobic to moderate, even effort", interval: "On 6:00", focus: "Distance base" },
          { id: "s_li81d8", reps: 4, dist: 200, desc: "Choice stroke or IM — moderate to strong", interval: "On 4:15", focus: "Variety and intensity build" },
          { id: "s_x1vleg", reps: 4, dist: 100, desc: "Freestyle — strong, hold pace to the end", interval: "On 2:15", focus: "Aerobic capacity close-out" },
        ]},
      { label: "Mixed 100s Repeat Unit", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_1mu8kt", reps: 1, dist: 100, desc: "Freestyle — aerobic, long stroke", interval: "On 2:10", focus: "Base" },
          { id: "s_ohqj6w", reps: 1, dist: 100, desc: "Backstroke or IM — moderate", interval: "On 2:30", focus: "Stroke variety" },
          { id: "s_aqzofw", reps: 1, dist: 100, desc: "Freestyle — build to strong", interval: "On 2:10", focus: "Intensity build" },
          { id: "s_9lzi3c", reps: 1, dist: 100, desc: "Choice stroke — strong, not sprinting", interval: "On 2:15", focus: "Mixed effort close — designed to repeat 2–3×" },
        ]},
      { label: "Mixed Kick-Pull-Swim Repeat Unit", totalYards: 500, types: ["mixed"], strokes: [], sets: [
          { id: "s_1cioax", reps: 4, dist: 50, desc: "Kick (board optional) — moderate effort", interval: "On 1:15", focus: "Leg activation", eq: "kickPart" },
          { id: "s_iyn0n4", reps: 2, dist: 100, desc: "Pull (buoy) — long stroke, high elbow catch", interval: "On 2:15", focus: "Upper-body isolation", eq: "pull" },
          { id: "s_rfuz04", reps: 2, dist: 50, desc: "Freestyle swim — put it all together", interval: "On 1:05", focus: "Full-body integration each round" },
        ]},
      { label: "Mixed Sprint-Recover Repeat Unit", totalYards: 600, types: ["mixed"], strokes: [], sets: [
          { id: "s_r15ruw", reps: 4, dist: 100, desc: "Freestyle — build to strong, last 25 all-out", interval: "On 2:15", focus: "Controlled build with sprint close" },
          { id: "s_sspqto", reps: 4, dist: 50, desc: "Choice stroke (not free) — moderate recovery", interval: "On 1:20", focus: "Stroke variety and active recovery between rounds" },
        ]},
      { label: "Mixed Five 400s", totalYards: 2000, types: ["mixed"], strokes: [], sets: [
          { id: "s_2co3l3", reps: 5, dist: 400, desc: "Rotate strokes: 1) freestyle, 2) IM, 3) backstroke, 4) freestyle, 5) choice stroke — moderate to strong, descend 1→5", interval: "On 8:00", focus: "Multi-stroke endurance at race-distance length" },
        ]},
      { label: "Mile + Stroke + Sprint", totalYards: 2100, types: ["mixed"], strokes: [], sets: [
          { id: "s_djfng7", reps: 1, dist: 1500, desc: "Freestyle — moderate aerobic mile, even effort", interval: "On 33:00", focus: "Base aerobic block" },
          { id: "s_j20goh", reps: 4, dist: 100, desc: "100 IM — moderate-strong, crisp transitions", interval: "On 2:15", focus: "IM technique after aerobic load" },
          { id: "s_chhqsj", reps: 4, dist: 50, desc: "Choice stroke — sprint, ALL OUT", interval: "On 1:30", focus: "Closing speed under fatigue" },
        ]},
      { label: "Mixed Variety Stack 2200", totalYards: 2200, types: ["mixed"], strokes: [], sets: [
          { id: "s_voei23", reps: 4, dist: 300, desc: "Freestyle — moderate, even effort across all four", interval: "On 6:30", focus: "Aerobic anchor" },
          { id: "s_acplno", reps: 4, dist: 150, desc: "Choice stroke — moderate-strong, switch strokes between reps", interval: "On 3:30", focus: "Stroke variety under aerobic load" },
          { id: "s_c87jy4", reps: 4, dist: 100, desc: "Freestyle — strong, hold target pace", interval: "On 2:15", focus: "Build pressure" },
          { id: "s_j30dxz", reps: 4, dist: 50, desc: "Choice stroke — sprint", interval: "On 1:15", focus: "Sprint finisher" },
        ]},
      { label: "Garbage 1500", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_i3w12w", reps: 1, dist: 1500, desc: "Freestyle — continuous, NO target pace. Mix it up freely: switch strokes by 100, drop in 25s of kick or drill whenever you want, change tempo, breathe how you like. The only rule is don't stop.", interval: "On 35:00", focus: "Garbage yardage — aerobic volume without judgment. Used by elite distance swimmers between hard sets." },
        ]},
      { label: "The 30/30", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_v21k3g", reps: 1, dist: 200, desc: "Easy choice — settle in", interval: "On 4:30", focus: "Prime" },
          { id: "s_vzoxd8", reps: 20, dist: 50, desc: "Alternate by 50: odd 50s = strong freestyle, even 50s = easy choice stroke. Continuous — no wall rest. The easy 50 IS the recovery.", interval: "On 1:00", focus: "Stroke variety + pace shifts inside a continuous swim — train the surge-recover muscle" },
          { id: "s_q64xm6", reps: 1, dist: 300, desc: "Easy backstroke — cooldown", interval: "On 7:00", focus: "Flush" },
        ]},
      { label: "Goldilocks 100s", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_yqy652", reps: 5, dist: 100, desc: "Freestyle — too easy (RPE 4) — almost lazy", interval: "On 2:00", focus: "Bottom of the band — feel how slow is slow" },
          { id: "s_tntue7", reps: 5, dist: 100, desc: "Choice stroke — too hard (RPE 8) — uncomfortable", interval: "On 2:15", focus: "Top of the band — feel how hard is hard" },
          { id: "s_hmo9r6", reps: 5, dist: 100, desc: "IM order (fly/back/breast/free/choice) — JUST RIGHT (RPE 6) — find the middle", interval: "On 2:15", focus: "Sweet-spot pace across multiple strokes — the one you should know by feel" },
        ]},
      { label: "Rolling 25s", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_dd5f7h", reps: 1, dist: 200, desc: "Easy choice — open up", interval: "On 4:30", focus: "Prime" },
          { id: "s_qwkd60", reps: 24, dist: 25, desc: "Continuous (no wall rest) — rotate by 25: free / back / breast / free / back / breast / ... — strong tempo throughout", interval: "On 0:35", focus: "Stroke rotation under continuous load — find rhythm in the rotation" },
          { id: "s_tj1ekj", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Mike's Mile (broken 1650)", totalYards: 1650, types: ["mixed"], strokes: [], sets: [
          { id: "s_7erb08", reps: 1, dist: 500, desc: "Freestyle — race pace, even splits", interval: "On 9:30", focus: "First piece — establish race pace" },
          { id: "s_lgb7lt", reps: 1, dist: 500, desc: "Freestyle — race pace, no slower than #1", interval: "On 9:30", focus: "Second piece — pace discipline" },
          { id: "s_7odm46", reps: 1, dist: 400, desc: "Freestyle — race pace, no slower than #1", interval: "On 7:30", focus: "Third piece — hold form" },
          { id: "s_pmp6e8", reps: 1, dist: 250, desc: "Freestyle — faster than race pace (this is your closing speed in a real mile)", interval: "On 5:30", focus: "Race finish — empty the tank" },
        ]},
      { label: "Race Sim 500", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_hpyybv", reps: 1, dist: 300, desc: "Easy choice — open up + 2x25 fast off the wall (dive + breakout simulation)", interval: "On 7:00", focus: "Race warmup — primed and loose" },
          { id: "s_dw40f3", reps: 1, dist: 100, desc: "Freestyle — fast 50 / easy 50 (open the legs without burning them)", interval: "On 3:00", focus: "Final prime — wake up the speed" },
          { id: "s_qwq9v7", reps: 1, dist: 500, desc: "Freestyle — RACE: go-from-the-gun 500. Even splits or negative split. Record every 100.", interval: "On 12:00", focus: "Race simulation — this is the workout, treat it like the meet" },
          { id: "s_om3i4o", reps: 1, dist: 200, desc: "Very easy backstroke — flush the lactate", interval: "On 5:30", focus: "Recover" },
          { id: "s_iic9mr", reps: 1, dist: 400, desc: "Freestyle — moderate, hold form (this is your fitness check — can you swim well after racing?)", interval: "On 9:00", focus: "Post-race aerobic — race-day fitness" },
        ]},
      { label: "Back 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_frl3en", reps: 10, dist: 100, desc: "Backstroke — moderate to strong, descend each pair (1-2, 3-4, etc.)", interval: "On 2:45", focus: "Build backstroke fitness across volume" },
        ]},
      { label: "Back Pyramid", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_my23u7", reps: 1, dist: 50, desc: "Backstroke — easy build", interval: "On 1:30", focus: "Open up" },
          { id: "s_9ur2dh", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:45", focus: "Find your pace" },
          { id: "s_5k043g", reps: 1, dist: 150, desc: "Backstroke — moderate to strong", interval: "On 3:15", focus: "Hold longer" },
          { id: "s_2ps71w", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 4:15", focus: "Apex of pyramid" },
          { id: "s_zklhek", reps: 1, dist: 150, desc: "Backstroke — strong, match earlier 150 pace", interval: "On 3:15", focus: "Same pace, fatigued" },
          { id: "s_e0efkm", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:45", focus: "Pick up pace" },
          { id: "s_hp0yqf", reps: 1, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:30", focus: "Top speed" },
          { id: "s_kfaamh", reps: 1, dist: 300, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back + Free Cycles", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_koq521", reps: 8, dist: 100, desc: "50 backstroke strong / 50 freestyle easy", interval: "On 2:45", focus: "Hard back / easy free alternation for recovery" },
          { id: "s_7295n6", reps: 4, dist: 50, desc: "Backstroke — fast finishing 50s", interval: "On 1:30", focus: "Speed at the end" },
        ]},
      { label: "Back Pull Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_o3frmh", reps: 6, dist: 100, desc: "Pull (buoy) — backstroke, focus on catch", interval: "On 2:15", focus: "Upper-body backstroke work", eq: "pull" },
          { id: "s_fe00cy", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:45", focus: "Reset" },
          { id: "s_gnivvb", reps: 6, dist: 50, desc: "Backstroke — strong, apply pull mechanics", interval: "On 1:30", focus: "Translate pull strength to swim" },
        ]},
      { label: "Back Sprint Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_9yh3k4", reps: 16, dist: 50, desc: "Backstroke — descend 1→4 within each set of 4", interval: "On 1:30", focus: "Sprint backstroke with consistent rest" },
          { id: "s_7h051p", reps: 4, dist: 50, desc: "Backstroke — ALL OUT, full recovery", interval: "On 2:00", focus: "Top-end back speed" },
        ]},
      { label: "Back 200s Descend", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_ni8s4r", reps: 6, dist: 200, desc: "Backstroke — descend 1→6 (each one faster)", interval: "On 5:30", focus: "Build back pace progressively" },
        ]},
      { label: "Back 50s Volume", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_6htis1", reps: 20, dist: 50, desc: "Backstroke — moderate to strong, descend within each set of 5 (1-5, 6-10, 11-15, 16-20)", interval: "On 1:30", focus: "Back endurance with cyclical descend" },
        ]},
      { label: "Back Race-Pace 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_b7vfsi", reps: 8, dist: 100, desc: "Backstroke — at race pace (full recovery)", interval: "On 3:00", focus: "Race-pace 100s" },
          { id: "s_vcs5fm", reps: 4, dist: 50, desc: "Backstroke — easy, recovery", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Back Mile Pace", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_9peece", reps: 11, dist: 100, desc: "Backstroke — at goal mile pace, hold even", interval: "On 2:45", focus: "Back endurance at goal pace" },
        ]},
      { label: "Back Sprint Pyramid", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_tcxmxn", reps: 1, dist: 25, desc: "Backstroke — moderate (open up)", interval: "On :45", focus: "Wake up" },
          { id: "s_lqk4yi", reps: 2, dist: 25, desc: "Backstroke — fast", interval: "On :45", focus: "Build" },
          { id: "s_uxa5gb", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_ac7zht", reps: 6, dist: 25, desc: "Backstroke — ALL OUT (the test)", interval: "On :45", focus: "Maintain speed" },
          { id: "s_rmqxvo", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Hold" },
          { id: "s_ltyt3m", reps: 2, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Find speed" },
          { id: "s_0bijk7", reps: 1, dist: 25, desc: "Backstroke — ALL OUT (best 25)", interval: "On :45", focus: "Final blast" },
          { id: "s_roi4vg", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Build 75s", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_s3ogdu", reps: 14, dist: 75, desc: "Backstroke — 25 build / 25 strong / 25 fast", interval: "On 2:30", focus: "Three-pace 75s" },
        ]},
      { label: "Back 7×150", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_fpo0h8", reps: 7, dist: 150, desc: "Backstroke — aerobic pace, focus on catch and rotation", interval: "On 3:30", focus: "Mid-distance backstroke volume" },
        ]},
      { label: "Back 100s + 50s", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_vrlqmn", reps: 6, dist: 100, desc: "Backstroke — moderate, even effort", interval: "On 2:45", focus: "Backstroke aerobic base" },
          { id: "s_kkhw4h", reps: 9, dist: 50, desc: "Backstroke — descend in groups of 3 (faster on rep 3)", interval: "On 1:30", focus: "Speed layered onto base" },
        ]},
      { label: "Back Threshold 200s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_bo22gw", reps: 6, dist: 200, desc: "Backstroke — threshold pace (RPE 8/10), even effort", interval: "On 5:30", focus: "Back threshold endurance" },
        ]},
      { label: "Back IM Transition", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_3n8k6c", reps: 4, dist: 200, desc: "Back+Breast 200 (100 back / 100 breast) — moderate, smooth transition", interval: "On 4:30", focus: "IM transition focus" },
          { id: "s_9e4ssu", reps: 6, dist: 50, desc: "Backstroke — strong, race-pace finishers", interval: "On 1:30", focus: "Top off back speed" },
        ]},
      { label: "Back Pull Set Extended", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_cfhc3o", reps: 6, dist: 100, desc: "Pull (buoy) — backstroke, focus on catch", interval: "On 2:00", focus: "Back pull mechanics", eq: "pull" },
          { id: "s_m1widb", reps: 1, dist: 100, desc: "Easy back — recovery", interval: "On 2:15", focus: "Reset" },
          { id: "s_6aa15t", reps: 8, dist: 50, desc: "Backstroke — strong, integrate pull", interval: "On 1:30", focus: "Apply pull to swim" },
        ]},
      { label: "Back 100/50 Mix", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_55ehax", reps: 6, dist: 100, desc: "Backstroke — moderate, descend 1→6", interval: "On 2:45", focus: "Back tempo" },
          { id: "s_1epsco", reps: 8, dist: 50, desc: "Backstroke — fast, descend 1-4 then 5-8", interval: "On 1:30", focus: "Back sprint" },
        ]},
      { label: "Back Underwater Day", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_d2neqe", reps: 8, dist: 25, desc: "Streamline backstroke kick — 12.5 yd UW + 12.5 swim", interval: "On 1:00", focus: "Underwater work on back", eq: "underkick" },
          { id: "s_j7cv2q", reps: 4, dist: 50, desc: "Backstroke — focus on push-off + UW dolphins", interval: "On 1:15", focus: "Underwater off every wall", eq: "underkick" },
          { id: "s_4gfwhc", reps: 6, dist: 100, desc: "Backstroke — moderate, focus on every push-off", interval: "On 2:45", focus: "Build streamline habit" },
        ]},
      { label: "Back Negative Splits", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_n2negn", reps: 6, dist: 200, desc: "Backstroke — negative split (second 100 faster than first)", interval: "On 5:30", focus: "Practice closing speed on back" },
        ]},
      { label: "Back Pyramid Up & Down", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_7mwlfl", reps: 1, dist: 50, desc: "Backstroke — easy", interval: "On 1:30", focus: "Open" },
          { id: "s_ovkdb0", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:45", focus: "Build" },
          { id: "s_p6m6nz", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 5:30", focus: "Peak" },
          { id: "s_czxb2s", reps: 1, dist: 300, desc: "Backstroke — moderate, smooth", interval: "On 6:00", focus: "Distance" },
          { id: "s_vi6bhu", reps: 1, dist: 200, desc: "Backstroke — strong, match earlier", interval: "On 5:30", focus: "Same pace fatigued" },
          { id: "s_6my24r", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:45", focus: "Pick up" },
          { id: "s_adqz1v", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Continuous 1000", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_49gsd1", reps: 1, dist: 1000, desc: "Backstroke — continuous, even-effort aerobic swim", interval: "On 22:00", focus: "Long-form back endurance" },
        ]},
      { label: "Back Test 200", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_h3rydz", reps: 1, dist: 200, desc: "Backstroke — easy warm-up", interval: "On 4:30", focus: "Open up back" },
          { id: "s_sxpfsb", reps: 1, dist: 200, desc: "Backstroke — TEST 200 (race-pace effort)", interval: "On 5:00", focus: "200 back race test" },
          { id: "s_e34eeg", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_3p8ja0", reps: 6, dist: 50, desc: "Backstroke — fast finishers", interval: "On 1:30", focus: "Top-off speed" },
          { id: "s_ll3qs3", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Endurance + Sprint", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_vccn82", reps: 1, dist: 600, desc: "Backstroke — steady aerobic, smooth tempo", interval: "On 12:00", focus: "Long back base" },
          { id: "s_gyh5id", reps: 8, dist: 50, desc: "Backstroke — fast, descend 1-4 then 5-8", interval: "On 1:30", focus: "Sprint after distance" },
          { id: "s_357ex0", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Tempo + Threshold", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_6cwwy3", reps: 6, dist: 100, desc: "Backstroke — tempo (RPE 7/10)", interval: "On 2:45", focus: "Tempo zone" },
          { id: "s_6wt92c", reps: 6, dist: 100, desc: "Backstroke — threshold (RPE 8/10)", interval: "On 2:45", focus: "Threshold zone" },
        ]},
      { label: "Back Race-Distance Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_immv45", reps: 5, dist: 200, desc: "Backstroke — at race pace (200), full recovery", interval: "On 5:00", focus: "200 back race-pace volume" },
        ]},
      { label: "Back Stroke Drop", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_h9j77b", reps: 4, dist: 100, desc: "Backstroke — full stroke, count strokes", interval: "On 2:45", focus: "Baseline stroke count" },
          { id: "s_f58b2v", reps: 4, dist: 100, desc: "Backstroke — drop 1 stroke, same speed", interval: "On 2:45", focus: "Reduce count" },
          { id: "s_rrg50i", reps: 4, dist: 100, desc: "Backstroke — drop 2 strokes, same speed", interval: "On 2:45", focus: "Maximize DPS" },
          { id: "s_qn0o22", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back 7×200", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_5q8pfx", reps: 7, dist: 200, desc: "Backstroke — aerobic pace, smooth rotation and catch", interval: "On 5:00", focus: "Mid-distance back volume" },
        ]},
      { label: "Back 200s + 100s", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_3u9thm", reps: 4, dist: 200, desc: "Backstroke — moderate aerobic, even effort", interval: "On 5:00", focus: "Backstroke aerobic base" },
          { id: "s_bmpsv2", reps: 6, dist: 100, desc: "Backstroke — descend in groups of 3 (faster each rep)", interval: "On 2:45", focus: "Speed layered onto base" },
        ]},
      { label: "Back Power 25s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_7fddkn", reps: 16, dist: 25, desc: "Backstroke — ALL OUT (full recovery)", interval: "On :45", focus: "Pure back speed" },
          { id: "s_cvtyji", reps: 4, dist: 100, desc: "Backstroke — moderate, recovery", interval: "On 2:45", focus: "Active recovery" },
          { id: "s_pnn360", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Stroke Count", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_xdfitw", reps: 4, dist: 50, desc: "Backstroke — count strokes per 25, baseline", interval: "On 1:30", focus: "Establish baseline" },
          { id: "s_dxeic3", reps: 4, dist: 50, desc: "Backstroke — same speed, count -1", interval: "On 1:30", focus: "Reduce strokes" },
          { id: "s_h0kkyv", reps: 4, dist: 50, desc: "Backstroke — faster pace, hold count", interval: "On 1:30", focus: "Speed without adding strokes" },
          { id: "s_95dej5", reps: 4, dist: 100, desc: "Backstroke — apply count over distance", interval: "On 2:45", focus: "Sustain efficiency" },
          { id: "s_svkp48", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back IM-Free Cycles", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_bvs8y3", reps: 4, dist: 200, desc: "100 backstroke strong + 100 freestyle moderate", interval: "On 5:30", focus: "Hard back / easy free for sustainability" },
          { id: "s_6jt0vv", reps: 4, dist: 50, desc: "Backstroke — fast finishing 50s", interval: "On 1:30", focus: "Top off back speed" },
        ]},
      { label: "Back Kick Endurance", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_q57wu5", reps: 1, dist: 400, desc: "Backstroke kick — easy aerobic, head still", interval: "On 9:00", focus: "Long back kick base", eq: "kick" },
          { id: "s_0btgn1", reps: 4, dist: 100, desc: "Backstroke kick — moderate, descend 1→4", interval: "On 3:15", focus: "Build kick intensity", eq: "kick" },
          { id: "s_2sh0xq", reps: 3, dist: 100, desc: "Backstroke — strong, drive with the legs", interval: "On 2:45", focus: "Apply kick to swim" },
        ]},
      { label: "Back Long + Sprint", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_95mpo4", reps: 1, dist: 800, desc: "Backstroke — steady aerobic, smooth tempo", interval: "On 17:00", focus: "Long back endurance" },
          { id: "s_sbwvk2", reps: 8, dist: 50, desc: "Backstroke — ALL OUT, full recovery", interval: "On 2:00", focus: "Sprint after distance" },
        ]},
      { label: "Back Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_31k1an", reps: 10, dist: 100, desc: "Backstroke — alternate 1 fast / 1 easy", interval: "On 2:45", focus: "Hard-easy pattern across volume" },
        ]},
      { label: "Back Race Test Day", totalYards: 1300, types: [], strokes: ["back"], sets: [
          { id: "s_u5gjso", reps: 1, dist: 100, desc: "Backstroke — easy warm-up", interval: "On 2:45", focus: "Open up" },
          { id: "s_6j5bzh", reps: 1, dist: 100, desc: "Backstroke — TEST 100 (race pace)", interval: "On 3:00", focus: "100 back test" },
          { id: "s_0zsch9", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_93egyq", reps: 1, dist: 200, desc: "Backstroke — TEST 200 (race pace)", interval: "On 5:00", focus: "200 back test" },
          { id: "s_r2e5wd", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_f6sfnn", reps: 4, dist: 100, desc: "Backstroke — moderate, hold form", interval: "On 2:45", focus: "Aerobic close" },
          { id: "s_c47ofi", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Aerobic 150s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_setiri", reps: 8, dist: 150, desc: "Backstroke — aerobic pace (RPE 6/10), smooth", interval: "On 3:00", focus: "Back aerobic volume" },
        ]},
      { label: "Back Mid-Distance Build", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_zdtnf2", reps: 1, dist: 400, desc: "Backstroke — easy aerobic", interval: "On 9:00", focus: "Aerobic base" },
          { id: "s_wo0zft", reps: 1, dist: 300, desc: "Backstroke — moderate", interval: "On 6:30", focus: "Build" },
          { id: "s_02yw1w", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 5:30", focus: "Threshold" },
          { id: "s_dn25ud", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:45", focus: "Sub-threshold" },
          { id: "s_qvuz5j", reps: 2, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:30", focus: "Sprint close" },
        ]},
      { label: "Back 8×125 Threshold", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_9mg3dn", reps: 8, dist: 125, desc: "Backstroke — threshold pace, hold even", interval: "On 2:30", focus: "Threshold in odd-distance reps" },
        ]},
      { label: "Back Sprint + Endurance", totalYards: 1300, types: [], strokes: ["back"], sets: [
          { id: "s_uzqogo", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Wake up speed" },
          { id: "s_jlgrtt", reps: 1, dist: 200, desc: "Backstroke — easy", interval: "On 4:30", focus: "Recover" },
          { id: "s_f63xfu", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:30", focus: "Speed" },
          { id: "s_rimxal", reps: 1, dist: 200, desc: "Backstroke — easy", interval: "On 4:30", focus: "Recover" },
          { id: "s_ogp0h7", reps: 4, dist: 100, desc: "Backstroke — fast, hold race pace", interval: "On 2:45", focus: "Race-distance speed" },
          { id: "s_280tbk", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back 10×150", totalYards: 1500, types: [], strokes: ["back"], sets: [
          { id: "s_pyzgd5", reps: 10, dist: 150, desc: "Backstroke — aerobic to moderate pace, descend 1→5 then repeat", interval: "On 3:30", focus: "Back aerobic volume with progressive effort" },
        ]},
      { label: "Back 10×200", totalYards: 2000, types: [], strokes: ["back"], sets: [
          { id: "s_1r4fr9", reps: 10, dist: 200, desc: "Backstroke — aerobic pace, hold even splits throughout", interval: "On 4:30", focus: "High-volume back endurance" },
        ]},
      { label: "Back 8×300", totalYards: 2400, types: [], strokes: ["back"], sets: [
          { id: "s_7ieole", reps: 8, dist: 300, desc: "Backstroke — aerobic pace, descend 1→4 then repeat (each set faster)", interval: "On 7:00", focus: "Extended back endurance at high volume" },
        ]},
      { label: "Back 50s Repeat Unit", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_unuekd", reps: 8, dist: 50, desc: "Backstroke — moderate to strong, focus on high arm recovery and hip rotation", interval: "On 1:30", focus: "Back volume and mechanics — designed to repeat 2–3×" },
        ]},
      { label: "Back 100s Repeat Unit", totalYards: 500, types: [], strokes: ["back"], sets: [
          { id: "s_gwgasz", reps: 4, dist: 100, desc: "Backstroke — moderate, count strokes per length to monitor efficiency", interval: "On 2:45", focus: "Back aerobic with stroke awareness" },
          { id: "s_aw3nzg", reps: 2, dist: 50, desc: "Backstroke — strong, hold stroke count from 100s", interval: "On 1:20", focus: "Speed with maintained mechanics" },
        ]},
      { label: "Back Build Repeat Unit", totalYards: 600, types: [], strokes: ["back"], sets: [
          { id: "s_sjz85n", reps: 3, dist: 200, desc: "Backstroke — descend 1→3, each 200 faster than the last", interval: "On 5:00", focus: "Progressive backstroke effort — push the last 200 each round" },
        ]},
      { label: "Back Underwater Chain", totalYards: 800, types: [], strokes: ["back"], sets: [
          { id: "s_9zysvh", reps: 1, dist: 200, desc: "Easy backstroke — open up, no underwater focus yet", interval: "On 4:30", focus: "Prime" },
          { id: "s_k9qein", reps: 8, dist: 50, desc: "Backstroke — 15m dolphin underwater off EVERY wall (push and turn), then surface and swim strong; count underwater kicks each rep", interval: "On 1:30", focus: "Underwater kick endurance — the part of the race most masters skip" },
          { id: "s_97wyfa", reps: 1, dist: 200, desc: "Easy choice — flush", interval: "On 5:00", focus: "Recover" },
        ]},
      { label: "Head-Still 200s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_7woxli", reps: 5, dist: 200, desc: "Backstroke — moderate, focus on a stationary head (imagine a full water glass on your forehead); body rotates, head does not", interval: "On 4:30", focus: "Eliminate head bobble — the fastest fix for crooked backstroke" },
        ]},
      { label: "Back Hip-Drive Build", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_87olfx", reps: 4, dist: 100, desc: "Backstroke — easy, count your strokes per length (your baseline)", interval: "On 2:30", focus: "Establish stroke count" },
          { id: "s_tau9y2", reps: 4, dist: 100, desc: "Backstroke — moderate, drive each stroke from the hip rotation (\"one hip up, one hip up\"); hold baseline ±1", interval: "On 2:30", focus: "Power source: hips, not arms" },
          { id: "s_wyh2pm", reps: 4, dist: 100, desc: "Backstroke — strong, drive the hip AND lengthen the catch; count should hold even as effort climbs", interval: "On 2:30", focus: "Translate hip drive to speed without shortening the stroke" },
        ]},
      { label: "Back Crossover Killer", totalYards: 700, types: [], strokes: ["back"], sets: [
          { id: "s_0wshck", reps: 1, dist: 200, desc: "Easy backstroke — open up", interval: "On 4:30", focus: "Prime" },
          { id: "s_6t5cer", reps: 8, dist: 50, desc: "Backstroke — moderate-strong, every hand enters PINKY-FIRST at the 11 o'clock / 1 o'clock line (no thumb-first, no crossing over centerline); count strokes", interval: "On 1:30", focus: "Eliminate crossover — the source of S-curve backstroke" },
          { id: "s_0a0l7n", reps: 1, dist: 100, desc: "Easy choice — flush", interval: "On 2:30", focus: "Reset" },
        ]},
      { label: "Back Rolling Under/Swim 50s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_1atklg", reps: 1, dist: 200, desc: "Easy backstroke — open up + 4×15m dolphin off the wall", interval: "On 4:30", focus: "Prime underwater work" },
          { id: "s_df5yu1", reps: 16, dist: 50, desc: "Backstroke — alternate by 50: odd 50s = 25 dolphin underwater + 25 swim; even 50s = full swim, moderate. Continuous, no wall rest (the easy 50 is recovery)", interval: "On 1:15", focus: "Train underwater-to-surface transition under aerobic load" },
          { id: "s_nn6vsx", reps: 1, dist: 200, desc: "Easy choice — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Back Turn-Focus 200s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_k7rlty", reps: 6, dist: 200, desc: "Backstroke — moderate-strong, but EVERY turn (3 per 200) is a back-to-front flip with a 10m dolphin underwater off the wall; rebuild the swim each length", interval: "On 5:00", focus: "Back-to-front turn — most masters never train it; the race-saving skill" },
        ]},
      { label: "Breast 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_ghpohl", reps: 10, dist: 100, desc: "Breaststroke — moderate to strong, count strokes per 25", interval: "On 3:15", focus: "Sustain breast pace, monitor stroke count for efficiency" },
        ]},
      { label: "Breast Pyramid", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_onix85", reps: 1, dist: 50, desc: "Breaststroke — easy build", interval: "On 1:30", focus: "Open up" },
          { id: "s_1h7cdf", reps: 1, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:15", focus: "Find your pace" },
          { id: "s_dwlgii", reps: 1, dist: 150, desc: "Breaststroke — moderate to strong", interval: "On 3:45", focus: "Hold longer" },
          { id: "s_zse7ma", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 5:00", focus: "Apex of pyramid" },
          { id: "s_z8zo74", reps: 1, dist: 150, desc: "Breaststroke — strong, match earlier pace", interval: "On 3:45", focus: "Same pace, fatigued" },
          { id: "s_mx4o4e", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:15", focus: "Pick up pace" },
          { id: "s_kmtrna", reps: 1, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:30", focus: "Top speed" },
          { id: "s_qojopv", reps: 1, dist: 300, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Kick Endurance", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_yh9l0x", reps: 1, dist: 300, desc: "Breaststroke kick (board) — long aerobic effort", interval: "On 6:30", focus: "Build kick endurance", eq: "kick" },
          { id: "s_y0c49w", reps: 4, dist: 100, desc: "Breaststroke kick — moderate, descend pace 1→4", interval: "On 3:15", focus: "Build kick intensity", eq: "kick" },
          { id: "s_kj1hty", reps: 3, dist: 100, desc: "Breaststroke swim — strong, drive with the kick", interval: "On 3:15", focus: "Apply kick power to full stroke" },
        ]},
      { label: "Breast Pull Set", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_3jta5g", reps: 6, dist: 100, desc: "Pull (buoy) — breaststroke pull, no kick (just glide)", interval: "On 2:30", focus: "Strong pull pattern", eq: "pull" },
          { id: "s_q781an", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_me3cyc", reps: 6, dist: 50, desc: "Breaststroke — strong, integrate pull with kick", interval: "On 1:30", focus: "Apply pull mechanics" },
        ]},
      { label: "Breast Sprint Set", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_86x7pw", reps: 12, dist: 50, desc: "Breaststroke — descend 1→4 within each set of 4", interval: "On 1:30", focus: "Sprint breast with controlled cadence" },
          { id: "s_b0a1ai", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_t6bswd", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT, fast turns", interval: "On 1:45", focus: "Peak breast speed" },
        ]},
      { label: "Breast 200s Descend", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_5fwtsj", reps: 6, dist: 200, desc: "Breaststroke — descend 1→6 (each one faster)", interval: "On 6:15", focus: "Build breast pace progressively" },
        ]},
      { label: "Breast 50s Sprint Volume", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_ahxpcj", reps: 16, dist: 50, desc: "Breaststroke — descend 1→4 within each set of 4", interval: "On 1:30", focus: "Sprint breast cyclically" },
          { id: "s_hrbax3", reps: 4, dist: 50, desc: "Breaststroke — easy, recovery", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Breast Race-Pace 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_etocgn", reps: 8, dist: 100, desc: "Breaststroke — at race pace (full recovery)", interval: "On 3:00", focus: "Race-pace 100s" },
          { id: "s_0pj12u", reps: 4, dist: 50, desc: "Breaststroke — moderate, recovery", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Breast Mile Pace", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_k597g0", reps: 11, dist: 100, desc: "Breaststroke — at goal mile pace, hold even splits", interval: "On 3:15", focus: "Breast endurance at goal pace" },
        ]},
      { label: "Breast Sprint Pyramid", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_anorb0", reps: 1, dist: 25, desc: "Breaststroke — moderate (open)", interval: "On :50", focus: "Wake up" },
          { id: "s_pt61ep", reps: 2, dist: 25, desc: "Breaststroke — fast", interval: "On :50", focus: "Build" },
          { id: "s_8sx7b3", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Peak speed" },
          { id: "s_loixk4", reps: 6, dist: 25, desc: "Breaststroke — ALL OUT (the test)", interval: "On :50", focus: "Maintain speed" },
          { id: "s_erxhmw", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Hold" },
          { id: "s_2883lu", reps: 2, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Find speed" },
          { id: "s_kxzlr1", reps: 1, dist: 25, desc: "Breaststroke — ALL OUT (best 25)", interval: "On :50", focus: "Final blast" },
          { id: "s_bdwrlk", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Build 75s", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_b8j7k5", reps: 14, dist: 75, desc: "Breaststroke — 25 build / 25 strong / 25 fast", interval: "On 2:30", focus: "Three-pace 75s" },
        ]},
      { label: "Breast 7×150", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_2lzyh5", reps: 7, dist: 150, desc: "Breaststroke — aerobic pace, focus on timing of pull and kick", interval: "On 4:30", focus: "Mid-distance breast volume" },
        ]},
      { label: "Breast 100s + 50s", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_pxhnd6", reps: 6, dist: 100, desc: "Breaststroke — moderate, even effort", interval: "On 3:15", focus: "Breast aerobic base" },
          { id: "s_1u0jo6", reps: 9, dist: 50, desc: "Breaststroke — descend in groups of 3 (faster on rep 3)", interval: "On 1:30", focus: "Speed layered onto base" },
        ]},
      { label: "Breast Threshold 200s", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_pyr36t", reps: 6, dist: 200, desc: "Breaststroke — threshold pace (RPE 8/10)", interval: "On 6:15", focus: "Breast threshold endurance" },
        ]},
      { label: "Breast IM Transition", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_dimurh", reps: 4, dist: 200, desc: "Breast+Free 200 (100 breast / 100 free) — moderate, smooth transition", interval: "On 4:30", focus: "IM transition focus" },
          { id: "s_n7pphh", reps: 6, dist: 50, desc: "Breaststroke — strong, race-pace finishers", interval: "On 1:30", focus: "Top off breast speed" },
        ]},
      { label: "Breast Pull-Only Set", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_ngbhxr", reps: 6, dist: 100, desc: "Breaststroke pull — no kick (just glide), focus on pull power", interval: "On 2:30", focus: "Isolate pull mechanics", eq: "pull" },
          { id: "s_b0k38r", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_ue8w27", reps: 8, dist: 50, desc: "Breaststroke — full stroke, integrate pull", interval: "On 1:30", focus: "Apply pull to full stroke" },
        ]},
      { label: "Breast Negative Splits", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_qhwi48", reps: 6, dist: 200, desc: "Breaststroke — negative split (second 100 faster than first)", interval: "On 6:15", focus: "Practice closing speed on breast" },
        ]},
      { label: "Breast Pyramid Up & Down", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_nhm8ul", reps: 1, dist: 50, desc: "Breaststroke — easy", interval: "On 1:30", focus: "Open" },
          { id: "s_uxlsgy", reps: 1, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:15", focus: "Build" },
          { id: "s_rpkyvw", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 6:15", focus: "Peak" },
          { id: "s_fdins2", reps: 1, dist: 300, desc: "Breaststroke — moderate, smooth", interval: "On 6:30", focus: "Distance" },
          { id: "s_kp39qd", reps: 1, dist: 200, desc: "Breaststroke — strong, match earlier", interval: "On 6:15", focus: "Same pace fatigued" },
          { id: "s_upvwjr", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:15", focus: "Pick up" },
          { id: "s_g76ym4", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Continuous 1000", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_26d3uk", reps: 1, dist: 1000, desc: "Breaststroke — continuous, aerobic effort, even pace", interval: "On 25:00", focus: "Long-form breast endurance" },
        ]},
      { label: "Breast Test 200", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_fdlgga", reps: 1, dist: 200, desc: "Breaststroke — easy warm-up", interval: "On 5:00", focus: "Open up breast" },
          { id: "s_ce293r", reps: 1, dist: 200, desc: "Breaststroke — TEST 200 (race-pace effort)", interval: "On 5:30", focus: "200 breast race test" },
          { id: "s_owghw3", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_gr95a7", reps: 6, dist: 50, desc: "Breaststroke — fast finishers", interval: "On 1:30", focus: "Top-off speed" },
          { id: "s_iy7spp", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Endurance + Sprint", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_7bhmoe", reps: 1, dist: 600, desc: "Breaststroke — steady aerobic, drive with kick", interval: "On 13:00", focus: "Long breast base" },
          { id: "s_9dbjza", reps: 8, dist: 50, desc: "Breaststroke — fast, descend 1-4 then 5-8", interval: "On 1:30", focus: "Sprint after distance" },
          { id: "s_efjb62", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Tempo + Threshold", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_yb26gu", reps: 6, dist: 100, desc: "Breaststroke — tempo (RPE 7/10)", interval: "On 3:15", focus: "Tempo zone" },
          { id: "s_bevvcc", reps: 6, dist: 100, desc: "Breaststroke — threshold (RPE 8/10)", interval: "On 3:15", focus: "Threshold zone" },
        ]},
      { label: "Breast Race-Distance 200s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_0euahx", reps: 5, dist: 200, desc: "Breaststroke — at race pace (200), full recovery", interval: "On 5:30", focus: "200 breast race-pace volume" },
        ]},
      { label: "Breast Power 50s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_7w6247", reps: 12, dist: 50, desc: "Breaststroke — ALL OUT (full recovery)", interval: "On 1:45", focus: "Pure breast speed" },
          { id: "s_ot1x0f", reps: 4, dist: 100, desc: "Breaststroke — moderate, recovery", interval: "On 3:15", focus: "Active recovery" },
        ]},
      { label: "Breast Pull + Kick + Swim", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_ior6zi", reps: 4, dist: 50, desc: "Breaststroke — pull only (no kick, glide)", interval: "On 1:30", focus: "Pull power", eq: "pull" },
          { id: "s_ml8gt8", reps: 4, dist: 50, desc: "Breaststroke kick (board) — strong", interval: "On 1:30", focus: "Kick power", eq: "kick" },
          { id: "s_u75nnj", reps: 4, dist: 50, desc: "Breaststroke — full stroke, integrate", interval: "On 1:30", focus: "Apply both" },
          { id: "s_k82sqv", reps: 4, dist: 100, desc: "Breaststroke — full stroke, race-pace tempo", interval: "On 3:15", focus: "Race-pace endurance" },
          { id: "s_l60vqn", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_8aghy4", reps: 10, dist: 100, desc: "Breaststroke — alternate 1 fast / 1 easy", interval: "On 3:15", focus: "Hard-easy pattern across volume" },
        ]},
      { label: "Breast Long Kick Day", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_2ybasq", reps: 1, dist: 500, desc: "Breaststroke kick (board) — long aerobic", interval: "On 11:00", focus: "Long breast kick base", eq: "kick" },
          { id: "s_c8ivar", reps: 4, dist: 100, desc: "Breaststroke kick — descend 1→4", interval: "On 3:15", focus: "Build kick intensity", eq: "kick" },
          { id: "s_rnhc8m", reps: 3, dist: 100, desc: "Breaststroke — strong, drive with kick", interval: "On 3:15", focus: "Apply kick power" },
        ]},
      { label: "Breast Race Test Day", totalYards: 1300, types: [], strokes: ["breast"], sets: [
          { id: "s_iqxyce", reps: 1, dist: 100, desc: "Breaststroke — easy warm-up", interval: "On 3:15", focus: "Open up" },
          { id: "s_bqf8j9", reps: 1, dist: 100, desc: "Breaststroke — TEST 100 (race pace)", interval: "On 3:00", focus: "100 breast test" },
          { id: "s_sgc9n9", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_x1qhbf", reps: 1, dist: 200, desc: "Breaststroke — TEST 200 (race pace)", interval: "On 5:30", focus: "200 breast test" },
          { id: "s_3yj8ry", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_iu7dkq", reps: 4, dist: 100, desc: "Breaststroke — moderate, hold form", interval: "On 3:15", focus: "Aerobic close" },
          { id: "s_unrqbx", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Aerobic 150s", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_pm0s1j", reps: 8, dist: 150, desc: "Breaststroke — aerobic pace (RPE 6/10), smooth", interval: "On 3:30", focus: "Breast aerobic volume" },
        ]},
      { label: "Breast Mid-Distance Build", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_u3yswx", reps: 1, dist: 400, desc: "Breaststroke — easy aerobic", interval: "On 10:00", focus: "Aerobic base" },
          { id: "s_tee7ym", reps: 1, dist: 300, desc: "Breaststroke — moderate", interval: "On 7:00", focus: "Build" },
          { id: "s_83cuec", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 6:15", focus: "Threshold" },
          { id: "s_usa1op", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:15", focus: "Sub-threshold" },
          { id: "s_k212od", reps: 2, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:30", focus: "Sprint close" },
        ]},
      { label: "Breast 8×125 Threshold", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_y4weec", reps: 8, dist: 125, desc: "Breaststroke — threshold pace, hold even", interval: "On 2:45", focus: "Threshold in odd-distance reps" },
        ]},
      { label: "Breast 50/100 Mix", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_9c22i1", reps: 6, dist: 100, desc: "Breaststroke — moderate, descend 1→6", interval: "On 3:15", focus: "Breast tempo" },
          { id: "s_oufxxs", reps: 8, dist: 50, desc: "Breaststroke — fast, descend 1-4 then 5-8", interval: "On 1:30", focus: "Breast sprint" },
        ]},
      { label: "Breast Stroke Count", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_si8xq9", reps: 4, dist: 50, desc: "Breaststroke — count strokes per 25, baseline", interval: "On 1:30", focus: "Establish baseline" },
          { id: "s_eshfd4", reps: 4, dist: 50, desc: "Breaststroke — same speed, count -1", interval: "On 1:30", focus: "Reduce strokes" },
          { id: "s_6qgm81", reps: 4, dist: 50, desc: "Breaststroke — faster pace, hold count", interval: "On 1:30", focus: "Speed without adding strokes" },
          { id: "s_e9dlce", reps: 4, dist: 100, desc: "Breaststroke — apply count over distance", interval: "On 3:15", focus: "Sustain efficiency" },
          { id: "s_zefj9a", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Long + Sprint", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_8t5n29", reps: 1, dist: 800, desc: "Breaststroke — steady aerobic, smooth", interval: "On 18:00", focus: "Long breast endurance" },
          { id: "s_7go2hi", reps: 8, dist: 50, desc: "Breaststroke — ALL OUT, full recovery", interval: "On 2:00", focus: "Sprint after distance" },
        ]},
      { label: "Breast Stroke-Drop Pyramid", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_094zko", reps: 4, dist: 75, desc: "Breaststroke — full stroke", interval: "On 2:30", focus: "Build" },
          { id: "s_o6tne7", reps: 4, dist: 100, desc: "Breaststroke — race pace, count strokes", interval: "On 3:15", focus: "Race-pace stroke count" },
          { id: "s_1pipac", reps: 6, dist: 50, desc: "Breaststroke — fast, fewer strokes than baseline", interval: "On 1:30", focus: "Speed with efficiency" },
        ]},
      { label: "Breast IM-Free Cycles", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_clqf9i", reps: 4, dist: 200, desc: "100 breaststroke strong + 100 freestyle moderate", interval: "On 6:15", focus: "Hard breast / easy free for sustainability" },
          { id: "s_w43esh", reps: 4, dist: 50, desc: "Breaststroke — fast finishing 50s", interval: "On 1:30", focus: "Top off breast speed" },
        ]},
      { label: "Breast Sprint + Endurance", totalYards: 1300, types: [], strokes: ["breast"], sets: [
          { id: "s_lq0s8c", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Wake up speed" },
          { id: "s_a0v573", reps: 1, dist: 200, desc: "Breaststroke — easy", interval: "On 5:00", focus: "Recover" },
          { id: "s_hw65us", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:30", focus: "Speed" },
          { id: "s_b6c6nx", reps: 1, dist: 200, desc: "Breaststroke — easy", interval: "On 5:00", focus: "Recover" },
          { id: "s_hj1hej", reps: 4, dist: 100, desc: "Breaststroke — fast, hold race pace", interval: "On 3:15", focus: "Race-distance speed" },
          { id: "s_owtgec", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast 10×150", totalYards: 1500, types: [], strokes: ["breast"], sets: [
          { id: "s_olkn6z", reps: 10, dist: 150, desc: "Breaststroke — aerobic pace, descend 1→5 then repeat, count strokes per length", interval: "On 4:30", focus: "Breast aerobic volume with stroke efficiency focus" },
        ]},
      { label: "Breast 10×200", totalYards: 2000, types: [], strokes: ["breast"], sets: [
          { id: "s_p7t412", reps: 10, dist: 200, desc: "Breaststroke — aerobic to moderate pace, hold even splits", interval: "On 6:15", focus: "High-volume breast endurance" },
        ]},
      { label: "Breast 8×300", totalYards: 2400, types: [], strokes: ["breast"], sets: [
          { id: "s_regq86", reps: 8, dist: 300, desc: "Breaststroke — aerobic pace, descend 1→4 then repeat", interval: "On 9:00", focus: "Extended breast endurance at high volume" },
        ]},
      { label: "Breast 50s Repeat Unit", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_v064bd", reps: 8, dist: 50, desc: "Breaststroke — moderate, focus on tight glide and sharp kick-pull-glide cycle", interval: "On 1:45", focus: "Breast mechanics at repeatable effort — designed to repeat 2–3×" },
        ]},
      { label: "Breast 100s Repeat Unit", totalYards: 500, types: [], strokes: ["breast"], sets: [
          { id: "s_l72m79", reps: 4, dist: 100, desc: "Breaststroke — moderate to strong, count pull-out glide distance on turn", interval: "On 3:15", focus: "Breaststroke aerobic with technical focus" },
          { id: "s_39vv51", reps: 2, dist: 50, desc: "Breaststroke — strong, crisp cycle rhythm", interval: "On 1:30", focus: "Finish each round with speed" },
        ]},
      { label: "Breast Build Repeat Unit", totalYards: 600, types: [], strokes: ["breast"], sets: [
          { id: "s_h4dksi", reps: 3, dist: 200, desc: "Breaststroke — descend 1→3, each faster than the last", interval: "On 6:00", focus: "Progressive breast effort — each round builds on the last" },
        ]},
      { label: "Breast Pullout Discipline", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_c2ae0r", reps: 1, dist: 200, desc: "Easy breast — open up, normal short pullouts", interval: "On 5:30", focus: "Prime" },
          { id: "s_58f1wm", reps: 10, dist: 75, desc: "Breaststroke — FULL legal pullout (one pull-down + one dolphin kick + one breaststroke kick) off the wall before first stroke; moderate-strong tempo", interval: "On 2:30", focus: "Pullout endurance — the defining race skill most masters underuse" },
          { id: "s_qzayal", reps: 1, dist: 50, desc: "Easy choice — flush", interval: "On 2:30", focus: "Recover" },
        ]},
      { label: "2-Kicks-Per-Pull", totalYards: 800, types: [], strokes: ["breast"], sets: [
          { id: "s_31mpf4", reps: 8, dist: 100, desc: "Breaststroke — exaggerated 2 KICKS between every pull (illegal for racing — this is a training drill). Hold long glide after the second kick.", interval: "On 3:30", focus: "Strengthen the second kick + force a long, patient glide" },
        ]},
      { label: "Breast DPS Hold", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_apnaao", reps: 1, dist: 200, desc: "Easy breast — count strokes per length, find your minimum sustainable count", interval: "On 5:30", focus: "Establish minimum-strokes baseline" },
          { id: "s_qd9v6z", reps: 8, dist: 100, desc: "Breaststroke — moderate, hold baseline ±1 stroke per length; if you exceed baseline+1 on any length, restart the count", interval: "On 3:15", focus: "Distance per stroke under fatigue — the long-stroke discipline" },
        ]},
      { label: "Breast Broken 200 (Race Sim)", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_agnwjf", reps: 1, dist: 200, desc: "Easy breast — open up + 2× full pullouts (no swimming, just pullout off the wall)", interval: "On 5:30", focus: "Prime the pullout muscles" },
          { id: "s_tq3a8v", reps: 4, dist: 200, desc: "Breaststroke — BROKEN race-pace 200: 4×50 with 10s rest between, full pullout off EVERY wall, target your 200 race-pace ±2s per 50. Record splits.", interval: "On 7:30", focus: "Race-pace simulation with pullout focus across the broken rest" },
          { id: "s_f2zigj", reps: 1, dist: 200, desc: "Very easy choice — flush", interval: "On 6:00", focus: "Recover lactate" },
        ]},
      { label: "Breast Tempo Ladder 100s", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_yvgkxg", reps: 3, dist: 100, desc: "Breaststroke — SLOW tempo (~14 strokes/100), long glide after kick", interval: "On 3:30", focus: "Bottom turnover — feel the glide" },
          { id: "s_eun7w5", reps: 3, dist: 100, desc: "Breaststroke — moderate tempo (~16 strokes/100), shorten glide slightly", interval: "On 3:15", focus: "Step up" },
          { id: "s_x10kto", reps: 3, dist: 100, desc: "Breaststroke — fast tempo (~18-19 strokes/100), minimal glide, race-style turnover", interval: "On 3:00", focus: "Top turnover — find your race tempo" },
          { id: "s_k8rnap", reps: 3, dist: 100, desc: "Breaststroke — drop back to moderate tempo, hold the feeling of fast catch from the previous set", interval: "On 3:15", focus: "Carry tempo discipline back down" },
        ]},
      { label: "Breast Pull-Down Power 25s", totalYards: 700, types: [], strokes: ["breast"], sets: [
          { id: "s_oohk9y", reps: 1, dist: 200, desc: "Easy breast — open the shoulders", interval: "On 5:30", focus: "Prime" },
          { id: "s_98hkiv", reps: 12, dist: 25, desc: "EXPLOSIVE pull-down only: push off, full underwater pullout (one pull-down + one dolphin + breaststroke kick), then ONE stroke and finish on stretch", interval: "On 0:50", focus: "Pull-down power and depth — the most-trained skill of elite breaststrokers" },
          { id: "s_va8mjk", reps: 1, dist: 200, desc: "Easy choice — flush", interval: "On 5:00", focus: "Recover" },
        ]},
      { label: "Fly 25s with Free Recovery", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_jfvtrg", reps: 16, dist: 50, desc: "25 fly fast / 25 freestyle easy (immediate)", interval: "On 1:30", focus: "Build fly tolerance with active recovery" },
          { id: "s_8y47co", reps: 4, dist: 50, desc: "25 fly ALL OUT / 25 free easy", interval: "On 1:30", focus: "Maximum fly speed with recovery" },
        ]},
      { label: "Fly + Free Pattern 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_pvadlb", reps: 8, dist: 100, desc: "25 fly / 75 freestyle — recover into the free", interval: "On 2:00", focus: "Sustainable fly with free recovery built in" },
          { id: "s_06mx71", reps: 4, dist: 50, desc: "Butterfly — fast (full stroke, no recovery)", interval: "On 1:30", focus: "Pure fly speed at the end" },
        ]},
      { label: "Fly Sprint Pyramid", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_3io9y2", reps: 1, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Open up" },
          { id: "s_uld110", reps: 2, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Build" },
          { id: "s_iixb1t", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_759z6w", reps: 8, dist: 25, desc: "Butterfly — ALL OUT (this is the test)", interval: "On 1:00", focus: "Maintain speed under fatigue" },
          { id: "s_r3ild0", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Hold the line" },
          { id: "s_ibx35p", reps: 2, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Find top speed" },
          { id: "s_918zkq", reps: 1, dist: 25, desc: "Butterfly — ALL OUT (best 25 of the day)", interval: "On :45", focus: "Final blast" },
          { id: "s_m96w7k", reps: 1, dist: 550, desc: "Easy freestyle — full recovery", interval: "No interval — swim easy", focus: "Aerobic close-out" },
        ]},
      { label: "Fly Pull Set", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_3bbkpd", reps: 8, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 freestyle pull", interval: "On 2:15", focus: "Build fly catch with free recovery", eq: "pull" },
          { id: "s_94lr9h", reps: 4, dist: 50, desc: "Butterfly — fast (apply pull mechanics)", interval: "On 1:30", focus: "Translate pull strength to fly swim" },
        ]},
      { label: "Fly Kick + Swim", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_q2wiba", reps: 1, dist: 300, desc: "Dolphin kick (board) — easy aerobic", interval: "On 7:00", focus: "Long dolphin base", eq: "kick" },
          { id: "s_jn2rbe", reps: 4, dist: 100, desc: "Dolphin kick — moderate, descend pace 1→4", interval: "On 3:00", focus: "Build kick intensity", eq: "kick" },
          { id: "s_j75wk5", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:15", focus: "Apply dolphin rhythm to fly with active recovery" },
        ]},
      { label: "Fly 50s Volume", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_0iesk4", reps: 16, dist: 50, desc: "25 fly fast / 25 freestyle easy (immediate)", interval: "On 1:30", focus: "Fly tolerance with active recovery" },
          { id: "s_tqisqz", reps: 4, dist: 50, desc: "Butterfly — fast (full stroke)", interval: "On 1:30", focus: "Pure fly speed" },
        ]},
      { label: "Fly 25s Sprint", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_j1t1oh", reps: 16, dist: 25, desc: "Butterfly — ALL OUT 25s (full recovery)", interval: "On 1:00", focus: "Pure fly speed" },
          { id: "s_b3odpr", reps: 6, dist: 100, desc: "Easy freestyle — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Fly + Free Cycles", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_dxjo9o", reps: 8, dist: 100, desc: "25 fly / 75 free — sustainable fly fitness", interval: "On 2:00", focus: "Build fly endurance with free recovery" },
          { id: "s_l27e2j", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:30", focus: "Fly speed with free recovery" },
        ]},
      { label: "Fly Pull Mix", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_6rkadl", reps: 4, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 free pull", interval: "On 2:15", focus: "Build fly catch with free recovery", eq: "pull" },
          { id: "s_rq3g8p", reps: 4, dist: 100, desc: "Freestyle — strong, apply pull mechanics", interval: "On 2:00", focus: "Translate pull pattern" },
          { id: "s_7g6oxs", reps: 4, dist: 50, desc: "Butterfly — fast, full stroke", interval: "On 1:30", focus: "Apply pull strength to fly" },
          { id: "s_1m58g2", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Mile Sustainability", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_3skfak", reps: 12, dist: 100, desc: "25 fly fast / 75 free moderate (mile pace) — repeat", interval: "On 2:00", focus: "Long-form fly tolerance with free for distance" },
        ]},
      { label: "Fly Build 75s", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_me2yub", reps: 14, dist: 75, desc: "25 fly easy / 25 free / 25 fly ALL OUT", interval: "On 2:30", focus: "Three-pace 75s, fly bookends" },
        ]},
      { label: "Fly 7×150", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_1r746c", reps: 7, dist: 150, desc: "50 fly / 100 free — aerobic pace, preserve fly form across all reps", interval: "On 3:30", focus: "Mid-distance fly volume with free recovery" },
        ]},
      { label: "Fly 100s + 50s", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_13wlhr", reps: 6, dist: 100, desc: "25 fly / 75 free — moderate, focus on fly quality on the lead 25", interval: "On 2:30", focus: "Fly lead with free base" },
          { id: "s_jwo7j4", reps: 9, dist: 50, desc: "25 fly fast / 25 free easy — descend fly effort in groups of 3", interval: "On 1:30", focus: "Speed layered onto base" },
        ]},
      { label: "Fly Endurance Test", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_4s1px3", reps: 4, dist: 100, desc: "Butterfly — full stroke, race pace (full recovery)", interval: "On 3:30", focus: "Race-pace 100 fly volume" },
          { id: "s_87zcg6", reps: 4, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:30", focus: "Maintain fly speed with recovery" },
          { id: "s_qpydg7", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down after fly volume" },
        ]},
      { label: "Fly Race-Pace 200", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_98ko43", reps: 1, dist: 200, desc: "Butterfly — TEST 200 (race-pace simulation; 50 fly + 50 free + 50 fly + 50 free if needed for sustainability)", interval: "On 6:00", focus: "200 fly fitness check" },
          { id: "s_o29nd1", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:30", focus: "Reset after test" },
          { id: "s_wuwvjl", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:30", focus: "Apply fly volume" },
          { id: "s_8nbimk", reps: 1, dist: 300, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Threshold 50s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_hgoxrw", reps: 8, dist: 50, desc: "Butterfly — at threshold (RPE 8/10), full stroke", interval: "On 1:45", focus: "Sustained fly threshold" },
          { id: "s_bpn31j", reps: 8, dist: 50, desc: "25 fly / 25 free — moderate", interval: "On 1:15", focus: "Active recovery between threshold reps" },
          { id: "s_j5hye2", reps: 4, dist: 50, desc: "Easy freestyle — close", interval: "On 1:15", focus: "Cool down" },
        ]},
      { label: "Fly Negative Splits", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_7zuz29", reps: 8, dist: 100, desc: "100 fly — negative split (50 fly easy / 50 fly fast)", interval: "On 2:30", focus: "Practice closing speed in fly" },
          { id: "s_f9p5fv", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Fly Pyramid", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_86jh5w", reps: 1, dist: 50, desc: "Butterfly — moderate (or 25 fly + 25 free)", interval: "On 1:30", focus: "Open up fly" },
          { id: "s_fg1phb", reps: 1, dist: 75, desc: "Butterfly — moderate (or 25 fly + 50 free)", interval: "On 2:00", focus: "Build" },
          { id: "s_839tj2", reps: 1, dist: 100, desc: "Butterfly — moderate (or 25 fly + 75 free)", interval: "On 2:30", focus: "Peak fly" },
          { id: "s_3kb6bs", reps: 1, dist: 75, desc: "Butterfly — fast (or 25 fly + 50 free)", interval: "On 2:00", focus: "Pick up pace" },
          { id: "s_jikjbq", reps: 1, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:30", focus: "Top speed" },
          { id: "s_wcssoc", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_zyj5my", reps: 1, dist: 550, desc: "Easy freestyle — full close-out", interval: "No interval — swim easy", focus: "Cool down after fly volume" },
        ]},
      { label: "Fly + IM Mix", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_902rzw", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:00", focus: "Apply fly into IM context" },
          { id: "s_724psp", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:30", focus: "Fly speed with active recovery" },
        ]},
      { label: "Fly Pull-Heavy Day", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_q8ao4p", reps: 8, dist: 100, desc: "Pull (buoy) — 50 fly pull / 50 free pull", interval: "On 2:15", focus: "Build fly catch endurance", eq: "pull" },
          { id: "s_d5b5ra", reps: 4, dist: 50, desc: "Butterfly — full stroke, apply pull mechanics", interval: "On 1:30", focus: "Translate pull power to fly" },
        ]},
      { label: "Fly Power 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_6xz5gj", reps: 4, dist: 100, desc: "Butterfly — race pace, full stroke (long recovery)", interval: "On 4:00", focus: "Race-pace fly 100s" },
          { id: "s_dxufls", reps: 4, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:30", focus: "Maintain speed with recovery" },
          { id: "s_6r80wa", reps: 4, dist: 100, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Continuous 1000", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_15ew43", reps: 1, dist: 1000, desc: "Continuous swim — alternate 25 fly / 75 free for 1000 (test fly endurance over distance)", interval: "On 22:00", focus: "Long-form fly tolerance" },
        ]},
      { label: "Fly Tempo + Threshold", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_orn8cy", reps: 5, dist: 100, desc: "25 fly / 75 free — tempo (RPE 7/10)", interval: "On 2:00", focus: "Tempo zone" },
          { id: "s_0cd8ez", reps: 5, dist: 100, desc: "25 fly / 75 free — threshold (RPE 8/10)", interval: "On 2:00", focus: "Threshold zone" },
        ]},
      { label: "Fly Race Test Day", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_h1m12x", reps: 1, dist: 100, desc: "25 fly + 75 free — easy warm-up", interval: "On 2:30", focus: "Open up" },
          { id: "s_47mmlr", reps: 1, dist: 100, desc: "Butterfly — TEST 100 (race pace)", interval: "On 3:30", focus: "100 fly test" },
          { id: "s_acuqjx", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:30", focus: "Reset" },
          { id: "s_uwoj73", reps: 1, dist: 50, desc: "Butterfly — TEST 50 (ALL OUT)", interval: "On 2:00", focus: "Sprint fly test" },
          { id: "s_3lzhxk", reps: 1, dist: 250, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_zd76za", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:30", focus: "Apply fly volume" },
          { id: "s_fu0ewy", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Sprint + Endurance", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_e66wy6", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On 1:00", focus: "Wake up speed" },
          { id: "s_57sxqg", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_k892my", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:45", focus: "Sprint" },
          { id: "s_1jwnuz", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_1sslc0", reps: 8, dist: 50, desc: "25 fly fast / 25 free moderate", interval: "On 1:30", focus: "Apply speed under fatigue" },
          { id: "s_w37heu", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Sandwich", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_mtri6f", reps: 4, dist: 200, desc: "25 fly / 150 free / 25 fly (fly bookends)", interval: "On 6:00", focus: "Test fly endurance with bookend pattern" },
          { id: "s_q00j1z", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:30", focus: "Active recovery" },
        ]},
      { label: "Fly Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_dbl944", reps: 10, dist: 100, desc: "Alternate: 25 fly fast + 75 free / 100 free easy", interval: "On 2:00", focus: "Hard fly / easy free pattern" },
        ]},
      { label: "Fly Aerobic Volume", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_04iu6g", reps: 11, dist: 100, desc: "25 fly / 75 free — aerobic pace (RPE 6/10), smooth", interval: "On 2:00", focus: "Long-form fly tolerance at aerobic effort" },
        ]},
      { label: "Fly Pull + Sprint", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_5kl79t", reps: 4, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 free pull, focus on catch", interval: "On 2:15", focus: "Build fly catch", eq: "pull" },
          { id: "s_wncoud", reps: 8, dist: 50, desc: "Butterfly — fast (full stroke)", interval: "On 1:30", focus: "Apply pull power to fly" },
          { id: "s_ovq0qx", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Stroke Drop", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_gm0spy", reps: 4, dist: 50, desc: "Butterfly — full stroke, count strokes per 25", interval: "On 1:30", focus: "Baseline fly stroke count" },
          { id: "s_0z93v1", reps: 4, dist: 50, desc: "Butterfly — same speed, count -1", interval: "On 1:30", focus: "Reduce strokes" },
          { id: "s_gylhq3", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:30", focus: "Apply efficiency to fast fly" },
          { id: "s_b8omsq", reps: 4, dist: 50, desc: "Easy freestyle — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "Fly Long Kick Day", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_26mprh", reps: 1, dist: 400, desc: "Dolphin kick (board) — easy aerobic", interval: "On 9:00", focus: "Long dolphin base", eq: "kick" },
          { id: "s_67mzyp", reps: 4, dist: 100, desc: "Dolphin kick — moderate, descend 1→4", interval: "On 3:00", focus: "Build kick intensity", eq: "kick" },
          { id: "s_etevdv", reps: 4, dist: 50, desc: "Butterfly — fast, drive with the kick", interval: "On 1:30", focus: "Apply kick to swim" },
          { id: "s_374jim", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Mid-Distance Build", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_pykyh8", reps: 1, dist: 400, desc: "Continuous: 25 fly / 75 free — easy aerobic", interval: "On 9:00", focus: "Aerobic fly base" },
          { id: "s_n8tnjc", reps: 1, dist: 300, desc: "Continuous: 25 fly / 75 free — moderate", interval: "On 6:00", focus: "Build" },
          { id: "s_4nfa7h", reps: 1, dist: 200, desc: "Continuous: 25 fly / 75 free — strong", interval: "On 6:00", focus: "Threshold" },
          { id: "s_keex8y", reps: 1, dist: 100, desc: "Butterfly — fast (full stroke)", interval: "On 2:30", focus: "Sub-threshold fly" },
          { id: "s_mza9gi", reps: 2, dist: 50, desc: "Easy freestyle — close", interval: "On 1:30", focus: "Cool down" },
        ]},
      { label: "Fly 8×125", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_jjmh2h", reps: 8, dist: 125, desc: "25 fly / 100 free — moderate to strong, descend", interval: "On 2:30", focus: "Build fly tolerance with free for distance" },
        ]},
      { label: "Fly Speed Endurance", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_fi3gle", reps: 8, dist: 75, desc: "25 fly fast / 50 free moderate", interval: "On 2:30", focus: "Repeated fly bursts" },
          { id: "s_zw3lmv", reps: 4, dist: 100, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Underwater Day", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_wj4u4k", reps: 8, dist: 25, desc: "Streamline dolphin kick — 12.5 yd UW + 12.5 swim out", interval: "On 1:00", focus: "Underwater work", eq: "underkick" },
          { id: "s_u3gw10", reps: 4, dist: 50, desc: "Butterfly — focus on push-off + UW dolphins", interval: "On 1:30", focus: "UW into fly", eq: "underkick" },
          { id: "s_41go1g", reps: 6, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:30", focus: "Apply UW work to swim" },
          { id: "s_jg5swp", reps: 4, dist: 75, desc: "Easy freestyle — close", interval: "On 1:45", focus: "Cool down" },
        ]},
      { label: "Fly + Sprint Free", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_odg5wg", reps: 4, dist: 100, desc: "25 fly fast / 75 free moderate", interval: "On 2:00", focus: "Build fly volume with free recovery" },
          { id: "s_piequs", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_fu709a", reps: 8, dist: 50, desc: "Freestyle — ALL OUT (apply fly fitness)", interval: "On 1:30", focus: "Free sprint after fly volume" },
          { id: "s_523gsm", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Single-Arm Day", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_gx229w", reps: 4, dist: 25, desc: "Single-arm fly drill (left)", interval: "On :50", focus: "Left-arm fly mechanics" },
          { id: "s_tb39tc", reps: 4, dist: 25, desc: "Single-arm fly drill (right)", interval: "On :50", focus: "Right-arm fly mechanics" },
          { id: "s_cxz5wh", reps: 4, dist: 50, desc: "Fly — 3-3-3 drill (3R / 3L / 3 full)", interval: "On 1:30", focus: "Build to full stroke" },
          { id: "s_zx35gr", reps: 4, dist: 75, desc: "25 fly drill / 50 fly swim", interval: "On 2:30", focus: "Apply drill into fly" },
          { id: "s_mny3pq", reps: 4, dist: 100, desc: "25 fly fast / 75 free moderate", interval: "On 2:00", focus: "Volume with technique" },
          { id: "s_t7z07q", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly 15×100 Volume", totalYards: 1500, types: [], strokes: ["fly"], sets: [
          { id: "s_vluhkz", reps: 15, dist: 100, desc: "25 fly fast / 75 free moderate — maintain fly quality each rep", interval: "On 2:15", focus: "High-rep fly tolerance with free recovery" },
        ]},
      { label: "Fly 20×100 Marathon", totalYards: 2000, types: [], strokes: ["fly"], sets: [
          { id: "s_4gpaux", reps: 20, dist: 100, desc: "25 fly / 75 free — aerobic pace throughout, focus on fly form on every lead 25", interval: "On 2:15", focus: "Extended fly volume at aerobic effort" },
        ]},
      { label: "Fly 8×300 Volume", totalYards: 2400, types: [], strokes: ["fly"], sets: [
          { id: "s_kmymqc", reps: 8, dist: 300, desc: "50 fly / 250 free — strong fly lead, aerobic free, hold fly form on every rep", interval: "On 5:30", focus: "High-volume fly tolerance with free for distance" },
        ]},
      { label: "Fly 50s Repeat Unit", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_79v5ez", reps: 8, dist: 50, desc: "25 fly / 25 freestyle — full fly effort on lead 25, easy free recovery", interval: "On 1:30", focus: "Fly tolerance with built-in recovery — designed to repeat 2–3×" },
        ]},
      { label: "Fly 25+Free Repeat Unit", totalYards: 500, types: [], strokes: ["fly"], sets: [
          { id: "s_3ev37a", reps: 6, dist: 50, desc: "25 fly ALL OUT / 25 freestyle easy", interval: "On 1:30", focus: "Max fly speed with active recovery" },
          { id: "s_v6xldt", reps: 2, dist: 100, desc: "Freestyle — moderate, keep breathing relaxed", interval: "On 2:10", focus: "Aerobic flush before the next round" },
        ]},
      { label: "Fly Build Repeat Unit", totalYards: 600, types: [], strokes: ["fly"], sets: [
          { id: "s_yi2hav", reps: 4, dist: 100, desc: "25 fly / 75 free — fly strong on lead 25, hold rhythm on the free", interval: "On 2:15", focus: "Fly volume without burnout — build fly quality each round" },
          { id: "s_6qxz9v", reps: 4, dist: 50, desc: "25 fly ALL OUT / 25 free easy", interval: "On 1:20", focus: "Top-speed fly to close each round" },
        ]},
      { label: "3-and-3 Fly", totalYards: 800, types: [], strokes: ["fly"], sets: [
          { id: "s_ersioh", reps: 1, dist: 200, desc: "Easy choice — open up, 4×15m dolphin off the wall", interval: "On 4:30", focus: "Prime dolphin kick" },
          { id: "s_2x1dgr", reps: 8, dist: 50, desc: "Alternate stroke by 3-count throughout the length: 3 strokes fly / 3 strokes freestyle / 3 fly / 3 free / etc. Hold quality fly form on every fly stroke", interval: "On 1:30", focus: "Train fly rhythm with built-in recovery — tolerable fly volume" },
          { id: "s_9ml36x", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Recover" },
        ]},
      { label: "Reverse 3-and-3 Fly Pyramid", totalYards: 700, types: [], strokes: ["fly"], sets: [
          { id: "s_x67zrw", reps: 1, dist: 200, desc: "Easy choice — open up, last 25 of each 100 with 2-3 fly strokes", interval: "On 4:30", focus: "Prime" },
          { id: "s_84jumw", reps: 1, dist: 50, desc: "Fly count: 1 stroke fly / 7 strokes free per length (low fly volume)", interval: "On 1:30", focus: "Step 1 — fly cameo" },
          { id: "s_sehqmm", reps: 1, dist: 50, desc: "Fly count: 2 fly / 6 free per length", interval: "On 1:30", focus: "Step 2" },
          { id: "s_0v9gch", reps: 1, dist: 50, desc: "Fly count: 3 fly / 5 free per length", interval: "On 1:30", focus: "Step 3 — fly equal partner" },
          { id: "s_9980r2", reps: 1, dist: 50, desc: "Fly count: 4 fly / 4 free per length", interval: "On 1:30", focus: "Step 4 — half and half" },
          { id: "s_bkdl14", reps: 1, dist: 50, desc: "Fly count: 5 fly / 3 free per length", interval: "On 1:30", focus: "Step 5 — fly dominant" },
          { id: "s_gg3v8t", reps: 1, dist: 50, desc: "Fly count: 6 fly / 2 free per length", interval: "On 1:30", focus: "Step 6 — fly with a free breath cameo" },
          { id: "s_guuanc", reps: 1, dist: 50, desc: "Fly count: 7 fly / 1 free per length", interval: "On 1:30", focus: "Step 7 — almost full fly" },
          { id: "s_flkncp", reps: 1, dist: 50, desc: "FULL fly — top step", interval: "On 1:30", focus: "Top step — earned" },
          { id: "s_owgx22", reps: 1, dist: 100, desc: "Easy backstroke — flush", interval: "On 2:30", focus: "Recover" },
        ]},
      { label: "Underwater + Swim Fly 50s", totalYards: 900, types: [], strokes: ["fly"], sets: [
          { id: "s_egdaqj", reps: 1, dist: 200, desc: "Easy choice — open up, 4×15m dolphin off the wall", interval: "On 4:30", focus: "Prime dolphin kick + lungs" },
          { id: "s_p33pf0", reps: 10, dist: 50, desc: "Butterfly — 25 dolphin underwater (head down, streamline) + 25 fly swim (strong); back off underwater length if you get lightheaded", interval: "On 1:45", focus: "Race-style fly: long underwaters powering surface speed" },
          { id: "s_qoi2wy", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Fly Stroke-Count Cap", totalYards: 700, types: [], strokes: ["fly"], sets: [
          { id: "s_wcynn9", reps: 1, dist: 100, desc: "Easy fly — count your strokes per 25 (baseline)", interval: "On 3:00", focus: "Establish stroke count" },
          { id: "s_khaqxr", reps: 8, dist: 50, desc: "Butterfly — moderate-strong, must stay AT OR UNDER baseline-1 strokes per 25. Take a longer underwater off the wall if needed.", interval: "On 2:00", focus: "Force long underwaters + late breakouts to keep count down" },
          { id: "s_8ryqox", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Recover" },
        ]},
      { label: "Crocodile Fly", totalYards: 800, types: [], strokes: ["fly"], sets: [
          { id: "s_9ndp8m", reps: 1, dist: 200, desc: "Easy choice — open up; last 50 with low-profile fly recovery (hands barely clear water)", interval: "On 5:00", focus: "Prime low-profile fly" },
          { id: "s_clvt11", reps: 8, dist: 50, desc: "Butterfly — race form: flat body line, low recovery (hands skim water on the way forward), forward press of the chest. Strong tempo.", interval: "On 1:45", focus: "Eliminate up-and-down fly — train flat, fast race-style fly" },
          { id: "s_vfu72m", reps: 1, dist: 200, desc: "Easy backstroke — flush", interval: "On 5:00", focus: "Cool down" },
        ]},
      { label: "Broken 100 Fly", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_n2c24b", reps: 1, dist: 200, desc: "Easy choice — open up + 4×15m dolphin off the wall", interval: "On 4:30", focus: "Prime" },
          { id: "s_91gpuh", reps: 1, dist: 100, desc: "Easy fly — moderate, find rhythm", interval: "On 3:00", focus: "Wake up the stroke" },
          { id: "s_t846eu", reps: 4, dist: 100, desc: "BROKEN 100 fly: race-pace 25 + 10s rest + race-pace 25 + 10s rest + race-pace 25 + 10s rest + race-pace 25. Target your 100 fly race-pace per 25. Record splits.", interval: "On 5:00", focus: "Race-pace 100 fly with rest you'd never get in a meet — train the speed" },
          { id: "s_r1akco", reps: 1, dist: 400, desc: "Easy choice — long flush", interval: "On 9:00", focus: "Cool down — lactate recovery" },
        ]},
      // ── VO2max mains (SCY) — aerobic-power sets, ~95% effort, short rest.
      //    Tagged so they surface for endurance/distance/mixed (+ im/stroke);
      //    "VO2" keyword + vo2-band send-offs classify them as vo2.
      { label: "VO2 8×100 Free", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y01", reps: 8, dist: 100, desc: "Freestyle — VO2 effort, hold strong tempo to the wall", interval: "On 1:30", focus: "Aerobic power — short rest, repeatable just under race pace" },
        ]},
      { label: "VO2 6×150 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y02", reps: 6, dist: 150, desc: "Freestyle — fast, VO2 effort, hold form under fatigue", interval: "On 2:15", focus: "Sustained aerobic power across the 150" },
        ]},
      { label: "VO2 5×200 Free", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y03", reps: 5, dist: 200, desc: "Freestyle — VO2, even pace just under 200 race speed", interval: "On 3:00", focus: "High-end aerobic — negative-split each 200 if you can" },
        ]},
      { label: "VO2 10×100 Free Descend", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y04", reps: 10, dist: 100, desc: "Freestyle — descend 1 to 5, then hold all of 6 to 10 fast (VO2)", interval: "On 1:30", focus: "Find VO2 pace, then hold it fatigued" },
        ]},
      { label: "VO2 12×75 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y05", reps: 12, dist: 75, desc: "Freestyle — fast 75s, VO2 effort, strong final 25", interval: "On 1:05", focus: "Aerobic power with a close — short rest" },
        ]},
      { label: "VO2 16×50 Free", totalYards: 800, types: ["endurance","mixed","distance"], strokes: [], sets: [
          { id: "s_vo2y06", reps: 16, dist: 50, desc: "Freestyle — fast 50s, VO2 effort, hold stroke length under fatigue", interval: "On 0:45", focus: "High turnover at short rest — aerobic power" },
        ]},
      { label: "VO2 4×200 Free Build", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y07", reps: 4, dist: 200, desc: "Freestyle — build each 200 to VO2 by the third 50, fast final 50", interval: "On 3:00", focus: "Progressive aerobic-power 200s" },
        ]},
      { label: "VO2 8×125 Free", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y08", reps: 8, dist: 125, desc: "Freestyle — fast effort, VO2, hold tempo through the odd distance", interval: "On 1:50", focus: "Aerobic power over an off-distance — stay long" },
        ]},
      { label: "VO2 8×100 IM", totalYards: 800, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2y09", reps: 8, dist: 100, desc: "100 IM — VO2 effort, hold legal stroke and tempo under fatigue", interval: "On 1:40", focus: "Aerobic power across all four strokes" },
        ]},
      { label: "VO2 6×150 IM", totalYards: 900, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2y10", reps: 6, dist: 150, desc: "150 IM (no free) — fast effort, VO2, strong transitions", interval: "On 2:30", focus: "Stroke-change power at high heart rate" },
        ]},
      { label: "VO2 8×100 Back", totalYards: 800, types: ["endurance","mixed"], strokes: ["back"], sets: [
          { id: "s_vo2y11", reps: 8, dist: 100, desc: "Backstroke — VO2 effort, steady fast tempo, hold hip rotation", interval: "On 1:40", focus: "Aerobic power on back — short rest" },
        ]},
      { label: "VO2 8×100 Breast", totalYards: 800, types: ["endurance","mixed"], strokes: ["breast"], sets: [
          { id: "s_vo2y12", reps: 8, dist: 100, desc: "Breaststroke — VO2 effort, strong pullouts, hold stroke length", interval: "On 1:50", focus: "Aerobic power on breast — repeatable just under race" },
        ]},
      { label: "VO2 6×100 Fly", totalYards: 600, types: ["endurance","mixed"], strokes: ["fly"], sets: [
          { id: "s_vo2y13", reps: 6, dist: 100, desc: "Butterfly — VO2 effort, swim fly while form holds then strong free finish", interval: "On 1:40", focus: "Fly aerobic power without form breakdown" },
        ]},
      { label: "VO2 20×50 Free", totalYards: 1000, types: ["endurance","mixed","distance"], strokes: [], sets: [
          { id: "s_vo2y14", reps: 20, dist: 50, desc: "Freestyle — fast 50s, VO2 effort, consistent send-off across all 20", interval: "On 0:45", focus: "Aerobic-power volume — hold pace, no fade" },
        ]},
      { label: "VO2 6×100 + 6×50 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2y15a", reps: 6, dist: 100, desc: "Freestyle — VO2 effort, hold tempo", interval: "On 1:30", focus: "Aerobic power" },
          { id: "s_vo2y15b", reps: 6, dist: 50, desc: "Freestyle — fast, VO2, quicker than the 100 pace", interval: "On 0:45", focus: "Speed-up to close the set" },
        ]},
    ];

    const COOLDOWN_OPTIONS = [
      { label: "Cool-Down — Easy Free 200", totalYards: 200, types: [], strokes: [], sets: [
          { id: "s_ivqcr4", reps: 1, dist: 200, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Bring heart rate down, flush the muscles" },
        ]},
      { label: "Cool-Down — Back (250 yd)", totalYards: 250, types: [], strokes: [], sets: [
          { id: "s_kepoej", reps: 1, dist: 150, desc: "Easy Backstroke or choice", interval: "No interval", focus: "Let lactic acid clear, open the chest" },
          { id: "s_0qruig", reps: 1, dist: 100, desc: "Easy Freestyle — progressively slower", interval: "No interval", focus: "Full cool-down — slowest laps of the day" },
        ]},
      { label: "Cool-Down — Easy Back 50", totalYards: 200, types: [], strokes: [], sets: [
          { id: "s_lgglxj", reps: 4, dist: 50, desc: "Easy alternating: Freestyle / Backstroke (swap each length)", interval: "On 1:20", focus: "Gentle recovery — no effort, just move" },
        ]},
      { label: "Cool-Down — Back (300 yd)", totalYards: 300, types: [], strokes: [], sets: [
          { id: "s_l34uea", reps: 1, dist: 200, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Flush the muscles, lower heart rate" },
          { id: "s_vcvtmb", reps: 1, dist: 100, desc: "Easy backstroke — gentle on the shoulders", interval: "No interval", focus: "Open the chest, counter freestyle pull" },
        ]},
      { label: "Cool-Down — Back (400 yd)", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_i47ru2", reps: 1, dist: 200, desc: "Easy backstroke or choice — progressively slower", interval: "No interval", focus: "Let lactic acid clear, open the chest" },
          { id: "s_8h7zkh", reps: 1, dist: 100, desc: "Easy freestyle — very easy, focus on long glide", interval: "No interval", focus: "Wind down heart rate" },
          { id: "s_b4dryx", reps: 4, dist: 25, desc: "Easy kick choice — no board, streamlined", interval: "On :45", focus: "Gentle leg flush after a big set" },
        ]},
    ];

    // ═══════════════════════════════════════════════════════════════
    //  50-METER POOL OPTION BANKS
    //  All distances are in meters. totalYards property is re-used
    //  (value is meters) to keep the generator logic unchanged.
    //  IM / back / breast / fly types fall back to yard banks.
    // ═══════════════════════════════════════════════════════════════

    const WARMUP_OPTIONS_50M = [
      { label: "Easy Free 400", typeAffinity: ["sprint"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_1iks9m", reps: 1, dist: 400, desc: "Easy Freestyle", interval: "No interval — swim easy", focus: "Loosen up, settle into long-course rhythm" },
        ]},
      { label: "Free Warmup (400 yd)", typeAffinity: ["sprint"], totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_rle277", reps: 1, dist: 300, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Ease into the long-course pool" },
          { id: "s_z6yznj", reps: 4, dist: 50, desc: "Build: easy first 25m, strong second 25m", interval: "On 1:20", focus: "Wake up fast-twitch before sprint work" },
        ]},
      { label: "Free Warmup + Streamline (400 yd)", typeAffinity: ["sprint"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_g19c00", reps: 1, dist: 400, desc: "Easy Freestyle — steady pace, long stroke", interval: "No interval", focus: "Full-body warm-up before fast work" },
          { id: "s_wvtftb", reps: 4, dist: 50, desc: "Descend 1→4: add speed each length", interval: "On 1:20", focus: "Gradual race-pace activation" },
        ]},
      { label: "Back Warmup + Pull (500 yd)", typeAffinity: ["technique"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_obaign", reps: 1, dist: 200, desc: "Easy Freestyle — focus on long, smooth stroke", interval: "No interval", focus: "Settle into good mechanics" },
          { id: "s_e3pwfv", reps: 4, dist: 50, desc: "Drill choice — fingertip drag or catch-up drill", interval: "On 1:20", focus: "Isolate recovery and front-end catch" },
        ]},
      { label: "Free Warmup + Pull (600 yd)", typeAffinity: ["technique"], totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_ndil0z", reps: 1, dist: 300, desc: "Easy Freestyle", interval: "No interval", focus: "Settle into the long-course pool" },
          { id: "s_9dgtg4", reps: 4, dist: 50, desc: "Stroke drill choice — one focus per length", interval: "On 1:20", focus: "Feel for the water" },
        ]},
      { label: "Back Warmup + Pull (700 yd)", typeAffinity: ["technique"], totalYards: 700, types: [], strokes: [], sets: [
          { id: "s_h42e4i", reps: 1, dist: 500, desc: "Easy Freestyle — breathe every 3, focus on rotation", interval: "No interval", focus: "Long warm-up before detailed drill work" },
          { id: "s_317etk", reps: 4, dist: 50, desc: "Drill choice — one focus per length, reset between", interval: "On 1:30", focus: "Prime stroke mechanics before technique main" },
        ]},
      { label: "Free Warmup + Build (800 yd)", typeAffinity: ["endurance"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_891grf", reps: 1, dist: 400, desc: "Easy Freestyle — build effort from 30% to 60%", interval: "No interval", focus: "Gradual aerobic engine warm-up" },
        ]},
      { label: "Free Warmup + Kick (500 yd)", typeAffinity: ["endurance"], totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_i7tez8", reps: 1, dist: 300, desc: "Easy Freestyle — relaxed, long-course rhythm", interval: "No interval", focus: "Ease into aerobic work" },
          { id: "s_sn4ipv", reps: 4, dist: 50, desc: "Kick (board) — steady pace, hips up", interval: "On 1:30", focus: "Engage legs before endurance main", eq: "kick" },
        ]},
      { label: "Free Warmup + Streamline (600 yd)", typeAffinity: ["endurance"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_jobqvx", reps: 1, dist: 400, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Warm up the aerobic engine gradually" },
          { id: "s_xujl6t", reps: 4, dist: 50, desc: "Kick (no board, streamlined) — 1 length each", interval: "On 1:30", focus: "Loosen hips and ankles", eq: "kick" },
        ]},
      { label: "Free Warmup + Catch-Up (800 yd)", typeAffinity: ["endurance"], totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_8eyef5", reps: 1, dist: 400, desc: "Easy Freestyle — aerobic pace, breathe every 3", interval: "No interval", focus: "Thorough warm-up for long-distance work" },
          { id: "s_6j56sd", reps: 2, dist: 100, desc: "Pull (buoy) — moderate, high elbow catch", interval: "On 2:30", focus: "Upper body activation", eq: "pull" },
          { id: "s_0taw8k", reps: 4, dist: 50, desc: "Kick (board) — easy to moderate", interval: "On 1:30", focus: "Leg conditioning before the long main set", eq: "kick" },
        ]},
    ];

    const COOLDOWN_OPTIONS_50M = [
      { label: "Cool-Down — Easy Free 200", totalYards: 200, types: [], strokes: [], sets: [
          { id: "s_0ydr4k", reps: 1, dist: 200, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Bring heart rate down, flush the muscles" },
        ]},
      { label: "Cool-Down — Back (250 yd)", totalYards: 300, types: [], strokes: [], sets: [
          { id: "s_qjn3qz", reps: 1, dist: 200, desc: "Easy Backstroke — open the chest, decelerate", interval: "No interval", focus: "Clear the lactic acid" },
          { id: "s_ataq28", reps: 1, dist: 100, desc: "Easy Freestyle — progressively slower", interval: "No interval", focus: "Slowest lengths of the day" },
        ]},
      { label: "Cool-Down — Easy Back 50", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_7g96ym", reps: 4, dist: 100, desc: "Easy alternating: Freestyle / Backstroke (swap each 100)", interval: "On 2:30", focus: "Gentle recovery — no effort, just move" },
        ]},
    ];

    const DRILL_OPTIONS_50M = [
      { label: "Pull 3×100", totalYards: 300, types: ["distance"], strokes: [], sets: [
          { id: "s_yzz1ru", reps: 3, dist: 100, desc: "Pull (buoy) — easy/moderate, focus on catch", interval: "On 2:30", focus: "Prime the pull pattern before distance work", eq: "pull" },
        ]},
      { label: "Catch-Up 4×100", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_5ocokx", reps: 4, dist: 100, desc: "Pull (buoy) — moderate pace, high elbow catch", interval: "On 2:30", focus: "Upper body endurance, long stroke", eq: "pull" },
        ]},
      { label: "Kick + Build (400 yd)", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_lhn391", reps: 2, dist: 200, desc: "Pull (buoy) — steady aerobic, focus on long stroke", interval: "On 5:00", focus: "Build the aerobic base before distance main set", eq: "pull" },
        ]},
      { label: "Pull 4×100", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_emtmiw", reps: 3, dist: 100, desc: "Pull (buoy) — easy/moderate, focus on catch", interval: "On 2:30", focus: "Prime the pull pattern before endurance work", eq: "pull" },
        ]},
      { label: "Kick 300", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_6xd4ya", reps: 4, dist: 100, desc: "Pull (buoy) — moderate pace, high elbow catch", interval: "On 2:30", focus: "Upper body endurance before long main set", eq: "pull" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_ctesam", reps: 4, dist: 100, desc: "Kick (board) — steady pace, even split", interval: "On 2:45", focus: "Leg conditioning before endurance main", eq: "kick" },
        ]},
      { label: "Build 6×50", totalYards: 200, types: ["sprint"], strokes: [], sets: [
          { id: "s_7x07yr", reps: 4, dist: 50, desc: "Build: easy first 25m / fast second 25m", interval: "On 1:30", focus: "Prime fast-twitch before sprint main" },
        ]},
      { label: "Streamline + Build (300 yd)", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_xiiuqc", reps: 6, dist: 50, desc: "Moderate to strong — 1 length each, crisp turns", interval: "On 1:20", focus: "Sharpen stroke rate for sprint main" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_15iern", reps: 4, dist: 100, desc: "Build: 50m easy / 50m strong — 2 lengths each", interval: "On 2:30", focus: "Controlled activation into sprint mode" },
        ]},
      { label: "Free 8×50", totalYards: 300, types: ["technique"], strokes: [], sets: [
          { id: "s_o6c0qa", reps: 6, dist: 50, desc: "Stroke drills — choice, one focus per length", interval: "On 1:20", focus: "Isolate weak points before technique main" },
        ]},
      { label: "Kick 4×75", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_vtnv6b", reps: 4, dist: 100, desc: "50m drill / 50m swim — alternate focus", interval: "On 2:30", focus: "Bridge from drill isolation to full-stroke application" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_b0f8ao", reps: 8, dist: 50, desc: "Single-arm or catch-up drill — alternate focus each length", interval: "On 1:20", focus: "Rotation control and high-elbow catch" },
        ]},
      { label: "Kick 4×75", totalYards: 300, types: ["mixed"], strokes: [], sets: [
          { id: "s_wmwk9b", reps: 3, dist: 100, desc: "50m drill / 50m strong swim", interval: "On 2:30", focus: "Sharpen technique then apply it at speed" },
        ]},
      { label: "Tempo (400 yd)", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_gxiags", reps: 4, dist: 100, desc: "Alternating: Pull (odd reps) / Free strong (even reps)", interval: "On 2:30", focus: "Upper body endurance with speed contrast", eq: "pull" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 500, types: ["mixed"], strokes: [], sets: [
          { id: "s_i7c1lf", reps: 2, dist: 50, desc: "Sprint — 1 length, max effort", interval: "On 1:30", focus: "Wake up fast-twitch" },
          { id: "s_y8j2vg", reps: 2, dist: 100, desc: "Pull (buoy) — easy recovery between sprints", interval: "On 2:30", focus: "Active recovery, reinforce catch", eq: "pull" },
          { id: "s_app5na", reps: 2, dist: 50, desc: "Sprint — 1 length, hold top-end speed", interval: "On 1:30", focus: "Close the drill with speed" },
          { id: "s_mybfv5", reps: 1, dist: 100, desc: "Pull (buoy) — relaxed, flush the sprint work", interval: "On 2:30", focus: "Transition smoothly to main set", eq: "pull" },
        ]},
      { label: "IM 4×75", totalYards: 200, types: ["im"], strokes: [], sets: [
          { id: "s_460be2", reps: 4, dist: 50, desc: "IM stroke sequence — one stroke per 50m: fly / back / breast / free", interval: "On 1:30", focus: "Prime each stroke before IM main set" },
        ]},
      { label: "IM Kick (400 yd)", totalYards: 300, types: ["im"], strokes: [], sets: [
          { id: "s_36kd7y", reps: 6, dist: 50, desc: "Stroke drill rotation — fly drill / back drill / breast drill / free easy / repeat", interval: "On 1:20", focus: "Isolate weak points across all four strokes" },
        ]},
      { label: "IM Kick + Pull (400 yd)", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_9ondyc", reps: 4, dist: 100, desc: "IM — one stroke per 50m (drill first 50m, swim second 50m)", interval: "On 2:45", focus: "Bridge drill feel into full-stroke IM rhythm" },
        ]},
      { label: "Back Single-Arm 4×75", totalYards: 200, types: [], strokes: ["back"], sets: [
          { id: "s_uzkvei", reps: 4, dist: 50, desc: "Backstroke — alternating single-arm / full-stroke each length", interval: "On 1:20", focus: "Isolate the pulling pattern and high-elbow catch" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_ewjg7c", reps: 6, dist: 50, desc: "Backstroke — alternating elbow-lead drill / full-stroke swim", interval: "On 1:20", focus: "Sharpen rotation and catch before backstroke main" },
        ]},
      { label: "Back Kick + Catch-Up (400 yd)", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_xfvp99", reps: 4, dist: 100, desc: "Backstroke — 50m drill (count strokes) / 50m swim (hold your count)", interval: "On 2:30", focus: "Transfer stroke efficiency to full backstroke" },
        ]},
      { label: "Breast Pull 4×75", totalYards: 200, types: [], strokes: ["breast"], sets: [
          { id: "s_jiaml9", reps: 4, dist: 50, desc: "Breaststroke — alternating pull-only / full-stroke each length", interval: "On 1:20", focus: "Isolate the pull phase and high elbow recovery" },
        ]},
      { label: "Breast Kick 6×50", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_wej8hl", reps: 6, dist: 50, desc: "Breaststroke — alternating kick-only / full-stroke each length", interval: "On 1:30", focus: "Build kick power and timing before breaststroke main" },
        ]},
      { label: "Breast Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_0xb0d6", reps: 4, dist: 100, desc: "Breaststroke — 50m pull-only / 50m full-stroke, focus on glide", interval: "On 2:30", focus: "Connect the pull to a patient, powerful stroke cycle" },
        ]},
      { label: "Fly Single-Arm 6×50", totalYards: 200, types: [], strokes: ["fly"], sets: [
          { id: "s_f5vxpd", reps: 4, dist: 50, desc: "Alternating: fly drill (single-arm or finger-drag) / easy freestyle", interval: "On 1:20", focus: "Activate butterfly without fatiguing before the main set" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_dn1kzd", reps: 6, dist: 50, desc: "Alternating: underwater dolphin kick / easy freestyle recovery", interval: "On 1:20", focus: "Build the undulation rhythm and kick power for fly" },
        ]},
      { label: "Fly Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_e1idso", reps: 4, dist: 100, desc: "50m single-arm fly (alternate arms each length) / 50m easy freestyle", interval: "On 2:30", focus: "Isolate each arm's pull before full-butterfly main set" },
        ]},
    ];

    const MAIN_OPTIONS_50M = [
      { label: "Distance Threshold", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_khqbh5", reps: 3, dist: 400, desc: "Freestyle — threshold pace (RPE 7-8/10)", interval: "On 9:00", focus: "Hold pace across long-course 400s" },
        ]},
      { label: "Long Intervals", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_2w2nbn", reps: 5, dist: 200, desc: "Freestyle — strong, even-split each 200", interval: "On 5:00", focus: "Sustain aerobic threshold in a 50m pool" },
        ]},
      { label: "Distance Build", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_dqw9dz", reps: 1, dist: 400, desc: "Freestyle — steady threshold, set your long-course pace", interval: "On 9:00", focus: "Establish 50m rhythm" },
          { id: "s_7i4x4b", reps: 4, dist: 200, desc: "Freestyle — slightly faster than the 400, descend 1→4", interval: "On 5:00", focus: "Build to top-end aerobic speed" },
        ]},
      { label: "Over-Distance 400s", totalYards: 800, types: ["distance"], strokes: [], sets: [
          { id: "s_gxzz9i", reps: 2, dist: 400, desc: "Freestyle — easy to moderate, focus on efficiency", interval: "On 10:00", focus: "Comfortable long-course aerobic effort" },
        ]},
      { label: "Steady 400s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_kv6phk", reps: 3, dist: 400, desc: "Freestyle — steady aerobic pace (conversational effort)", interval: "On 10:00", focus: "Build long-course aerobic base" },
        ]},
      { label: "Continuous 800s", totalYards: 1600, types: ["endurance"], strokes: [], sets: [
          { id: "s_x47uwz", reps: 2, dist: 800, desc: "Freestyle — steady, negative split (2nd 400m faster)", interval: "On 18:00", focus: "Long-course aerobic endurance" },
        ]},
      { label: "Endurance Ladder", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_3g3bon", reps: 1, dist: 400, desc: "Freestyle — steady, comfortably hard", interval: "On 10:00", focus: "Prime the aerobic engine for the shorter pieces" },
          { id: "s_5p0ktv", reps: 3, dist: 200, desc: "Freestyle — slightly faster than the 400, hold pace", interval: "On 5:00", focus: "Sustain speed after the long effort" },
        ]},
      { label: "Long Pull Set", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_64hged", reps: 3, dist: 400, desc: "Pull (buoy) — steady long-course pull, high elbow catch", interval: "On 9:30", focus: "Upper-body endurance for long-distance swimming", eq: "pull" },
        ]},
      { label: "100m Sprint Set", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_12zvwe", reps: 10, dist: 100, desc: "Sprint — 2 lengths at race pace, full recovery", interval: "On 3:00", focus: "Long-course sprint quality in short repeats" },
        ]},
      { label: "Pure 50s", totalYards: 800, types: ["sprint"], strokes: [], sets: [
          { id: "s_h2vhk5", reps: 16, dist: 50, desc: "Sprint — 1 length full effort, full recovery", interval: "On 1:30", focus: "Maximum velocity over a single 50m length" },
        ]},
      { label: "Sprint Pyramid", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_qb0ovv", reps: 4, dist: 100, desc: "Moderate sprint — build into race pace over 2 lengths", interval: "On 2:30", focus: "Dial in pacing" },
          { id: "s_tr90yy", reps: 4, dist: 50, desc: "Fast sprint — 1 length maximum effort", interval: "On 1:30", focus: "Peak velocity" },
          { id: "s_7asevh", reps: 4, dist: 100, desc: "Strong sprint — hold race pace under fatigue", interval: "On 2:30", focus: "Sustain speed when tired" },
        ]},
      { label: "Stroke Refinement", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_lor9ah", reps: 10, dist: 100, desc: "Controlled pace — count strokes, exaggerate one drill element per rep", interval: "On 2:45", focus: "Build muscle memory at moderate effort" },
        ]},
      { label: "Efficiency Ladder", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_41nx2l", reps: 2, dist: 400, desc: "Steady swim — count strokes per length, hold your count", interval: "On 10:00", focus: "Long-course stroke efficiency" },
          { id: "s_bxc4z3", reps: 4, dist: 100, desc: "Control swim — maintain stroke count from the 400s at higher effort", interval: "On 2:45", focus: "Apply long-stroke efficiency at faster pace" },
        ]},
      { label: "Catch and Pull Focus", totalYards: 900, types: ["technique"], strokes: [], sets: [
          { id: "s_b5cj83", reps: 6, dist: 100, desc: "Pull (buoy) — high-elbow catch, early vertical forearm", interval: "On 2:30", focus: "Isolate the catch pattern over 2 lengths", eq: "pull" },
          { id: "s_rlh1gl", reps: 6, dist: 50, desc: "Free swim — carry the catch into full stroke, no buoy", interval: "On 1:20", focus: "Transfer drill feel to race stroke" },
        ]},
      { label: "Mixed Threshold", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_2df978", reps: 4, dist: 200, desc: "Threshold pace — strong aerobic effort (RPE 7/10)", interval: "On 5:00", focus: "Establish steady long-course pace" },
          { id: "s_ah6ju0", reps: 6, dist: 50, desc: "Sprint — race pace, maximum turnover", interval: "On 1:30", focus: "Top-end speed after threshold work" },
        ]},
      { label: "Long and Short", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_fa7ppn", reps: 2, dist: 400, desc: "Steady threshold — own your pace over 8 lengths", interval: "On 9:30", focus: "Long-course aerobic quality" },
          { id: "s_dvagv6", reps: 2, dist: 100, desc: "Moderate-strong — hold threshold pace from the 400s", interval: "On 2:30", focus: "Shorter repeats, sharper feel" },
          { id: "s_izgrx4", reps: 4, dist: 50, desc: "Sprint — 1 length full effort, finish the set fast", interval: "On 1:20", focus: "Close with speed" },
        ]},
      { label: "Mixed Variety", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_oqsqpf", reps: 4, dist: 100, desc: "Moderate — 2 lengths steady aerobic", interval: "On 2:30", focus: "Get the engine running" },
          { id: "s_ct9iei", reps: 4, dist: 50, desc: "Fast — 1 length strong pace", interval: "On 1:20", focus: "Speed layer" },
          { id: "s_cdwhew", reps: 2, dist: 200, desc: "Threshold — 4 lengths, hold pace under fatigue", interval: "On 5:00", focus: "Endurance on top of the speed work" },
        ]},
      { label: "IM 200s", totalYards: 800, types: ["im"], strokes: [], sets: [
          { id: "s_l6rest", reps: 4, dist: 200, desc: "Individual Medley — one stroke per 50m, hold each stroke strong", interval: "On 6:00", focus: "Build IM rhythm and transition efficiency" },
        ]},
      { label: "IM Volume", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_4l5hub", reps: 5, dist: 200, desc: "Individual Medley — steady effort, focus on clean transitions", interval: "On 6:00", focus: "Long-course IM aerobic base" },
        ]},
      { label: "IM 400s", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_1t6g4x", reps: 3, dist: 400, desc: "Individual Medley — one stroke per 100m, controlled effort", interval: "On 12:00", focus: "Sustain all four strokes over a long-course 400 IM" },
        ]},
      { label: "IM Endurance", totalYards: 1600, types: ["im"], strokes: [], sets: [
          { id: "s_s35y7c", reps: 4, dist: 400, desc: "Individual Medley — moderate effort, negative-split the back half", interval: "On 12:00", focus: "Long-course IM aerobic endurance" },
        ]},
      { label: "IM Ladder", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_36svlv", reps: 2, dist: 400, desc: "Individual Medley — steady, establish your IM pace", interval: "On 12:00", focus: "Long IM aerobic effort" },
          { id: "s_47wrhn", reps: 1, dist: 200, desc: "Individual Medley — push the pace slightly vs. the 400s", interval: "On 6:00", focus: "Close the set faster" },
        ]},
      { label: "IM 200s Repeat Unit", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_izxy5d", reps: 2, dist: 200, desc: "Individual Medley — strong, clean transitions", interval: "On 6:00", focus: "Repeatable IM quality — designed to repeat 2–3×" },
        ]},
      { label: "IM 200s Long Repeat Unit", totalYards: 600, types: ["im"], strokes: [], sets: [
          { id: "s_kq58q1", reps: 3, dist: 200, desc: "Individual Medley — consistent effort each round", interval: "On 6:00", focus: "IM endurance volume — designed to repeat 2–3×" },
        ]},
      { label: "Backstroke 100s", totalYards: 800, types: [], strokes: ["back"], sets: [
          { id: "s_p3fjjs", reps: 8, dist: 100, desc: "Backstroke — moderate pace, count strokes per length", interval: "On 2:45", focus: "Build stroke efficiency over 2 lengths" },
        ]},
      { label: "Backstroke 200s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_bs44hl", reps: 5, dist: 200, desc: "Backstroke — steady aerobic, hold stroke count from length to length", interval: "On 5:30", focus: "Long-course backstroke aerobic base" },
        ]},
      { label: "Backstroke Volume", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_zgwgt6", reps: 6, dist: 200, desc: "Backstroke — comfortably hard (RPE 7/10), negative-split each 200", interval: "On 5:30", focus: "Backstroke endurance at long-course pace" },
        ]},
      { label: "Backstroke 400s", totalYards: 1600, types: [], strokes: ["back"], sets: [
          { id: "s_mxuk8j", reps: 4, dist: 400, desc: "Backstroke — steady threshold pace, focus on long-axis rotation", interval: "On 11:00", focus: "Backstroke aerobic power over 8 lengths" },
        ]},
      { label: "Backstroke Ladder", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_mcte5w", reps: 4, dist: 100, desc: "Backstroke — moderate, establish pace", interval: "On 2:45", focus: "Find your long-course backstroke rhythm" },
          { id: "s_6ujiwz", reps: 3, dist: 200, desc: "Backstroke — slightly faster than the 100s, hold pace", interval: "On 5:30", focus: "Sustain speed at longer distance" },
        ]},
      { label: "Back 100s Repeat Unit", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_crulfe", reps: 4, dist: 100, desc: "Backstroke — strong, count strokes each length", interval: "On 2:45", focus: "Repeatable backstroke quality — designed to repeat 2–3×" },
        ]},
      { label: "Back 200s Repeat Unit", totalYards: 600, types: [], strokes: ["back"], sets: [
          { id: "s_dpwsrz", reps: 3, dist: 200, desc: "Backstroke — consistent effort, hold stroke rate", interval: "On 5:30", focus: "Backstroke endurance volume — designed to repeat 2–3×" },
        ]},
      { label: "Breaststroke 100s", totalYards: 800, types: [], strokes: ["breast"], sets: [
          { id: "s_nk3syf", reps: 8, dist: 100, desc: "Breaststroke — moderate pace, focus on patient glide and powerful pull", interval: "On 3:00", focus: "Build stroke timing over 2 lengths" },
        ]},
      { label: "Breaststroke 200s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_mblgof", reps: 5, dist: 200, desc: "Breaststroke — steady aerobic, hold consistent cycle rhythm", interval: "On 6:00", focus: "Long-course breaststroke aerobic base" },
        ]},
      { label: "Breaststroke Volume", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_ygmd3e", reps: 6, dist: 200, desc: "Breaststroke — comfortably hard (RPE 7/10), strong pull each cycle", interval: "On 6:00", focus: "Breaststroke endurance at long-course pace" },
        ]},
      { label: "Breaststroke 400s", totalYards: 1600, types: [], strokes: ["breast"], sets: [
          { id: "s_2utflf", reps: 4, dist: 400, desc: "Breaststroke — steady threshold pace, focus on efficient pull-out on turns", interval: "On 12:00", focus: "Breaststroke aerobic power over 8 lengths" },
        ]},
      { label: "Breaststroke Ladder", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_1yfbbu", reps: 4, dist: 100, desc: "Breaststroke — moderate, establish pace and glide timing", interval: "On 3:00", focus: "Find your long-course breaststroke rhythm" },
          { id: "s_3sipzk", reps: 3, dist: 200, desc: "Breaststroke — slightly faster than the 100s, hold cycle timing", interval: "On 6:00", focus: "Sustain breaststroke speed at longer distance" },
        ]},
      { label: "Breast 100s Repeat Unit", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_fv3y76", reps: 4, dist: 100, desc: "Breaststroke — strong pull and patient glide", interval: "On 3:00", focus: "Repeatable breaststroke quality — designed to repeat 2–3×" },
        ]},
      { label: "Breast 200s Repeat Unit", totalYards: 600, types: [], strokes: ["breast"], sets: [
          { id: "s_rdbjxg", reps: 3, dist: 200, desc: "Breaststroke — consistent cycle rhythm each round", interval: "On 6:00", focus: "Breaststroke endurance volume — designed to repeat 2–3×" },
        ]},
      { label: "Fly/Free 100s", totalYards: 800, types: [], strokes: ["fly"], sets: [
          { id: "s_yiten3", reps: 8, dist: 100, desc: "50m butterfly / 50m easy freestyle — full effort on the fly, recover on free", interval: "On 3:00", focus: "Build butterfly volume with active recovery" },
        ]},
      { label: "Fly Sprint 50s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_3rpaqh", reps: 20, dist: 50, desc: "Butterfly — maximum effort, long underwater off the wall, full recovery", interval: "On 1:30", focus: "Maintain stroke quality on each 50m fly length" },
        ]},
      { label: "Fly/Free 200s", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_123ez8", reps: 6, dist: 200, desc: "100m butterfly / 100m easy freestyle — push the fly, use the free to recover", interval: "On 6:00", focus: "Long-course butterfly endurance with built-in recovery" },
        ]},
      { label: "Fly Volume", totalYards: 1600, types: [], strokes: ["fly"], sets: [
          { id: "s_a0mbk8", reps: 8, dist: 200, desc: "100m butterfly / 100m moderate freestyle — maintain fly technique on each half", interval: "On 6:00", focus: "Build butterfly aerobic base through fly-dominant 200s" },
        ]},
      { label: "Fly Ladder", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_1qzh1d", reps: 6, dist: 100, desc: "50m butterfly / 50m easy freestyle — hold fly technique", interval: "On 3:00", focus: "Establish fly rhythm at long-course pace" },
          { id: "s_fixj2b", reps: 2, dist: 200, desc: "100m butterfly / 100m moderate freestyle — sustain quality", interval: "On 6:00", focus: "Extend fly endurance after the shorter reps" },
        ]},
      { label: "Fly 100s Repeat Unit", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_t7l02i", reps: 4, dist: 100, desc: "50m butterfly / 50m easy freestyle — quality fly on each rep", interval: "On 3:00", focus: "Repeatable butterfly quality — designed to repeat 2–3×" },
        ]},
      { label: "Fly 100s Long Repeat Unit", totalYards: 600, types: [], strokes: ["fly"], sets: [
          { id: "s_tvni5r", reps: 6, dist: 100, desc: "50m butterfly / 50m easy freestyle — consistent effort each round", interval: "On 3:00", focus: "Butterfly endurance volume — designed to repeat 2–3×" },
        ]},
      // ── VO2max mains (LCM, long-course meters) — aerobic-power sets.
      { label: "VO2 8×100 Free", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2l01", reps: 8, dist: 100, desc: "Freestyle — VO2 effort, hold strong tempo through the long-course 100", interval: "On 1:35", focus: "Aerobic power — short rest, just under race pace" },
        ]},
      { label: "VO2 6×150 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2l02", reps: 6, dist: 150, desc: "Freestyle — fast, VO2 effort, hold form on the back half", interval: "On 2:20", focus: "Sustained aerobic power, long-course tempo" },
        ]},
      { label: "VO2 5×200 Free", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2l03", reps: 5, dist: 200, desc: "Freestyle — VO2, even pace just under 200 race speed", interval: "On 3:10", focus: "High-end aerobic — hold pace with no walls to rest on" },
        ]},
      { label: "VO2 10×100 Free Descend", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2l04", reps: 10, dist: 100, desc: "Freestyle — descend 1 to 5, then hold all of 6 to 10 fast (VO2)", interval: "On 1:35", focus: "Find VO2 pace, then hold it fatigued" },
        ]},
      { label: "VO2 16×50 Free", totalYards: 800, types: ["endurance","mixed","distance"], strokes: [], sets: [
          { id: "s_vo2l05", reps: 16, dist: 50, desc: "Freestyle — fast long-course 50s, VO2 effort, hold stroke length", interval: "On 0:48", focus: "High turnover at short rest — aerobic power" },
        ]},
      { label: "VO2 4×200 Free Build", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2l06", reps: 4, dist: 200, desc: "Freestyle — build each 200 to VO2 by the back 100, fast final 50", interval: "On 3:10", focus: "Progressive aerobic-power 200s" },
        ]},
      { label: "VO2 8×100 IM", totalYards: 800, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2l07", reps: 8, dist: 100, desc: "100 IM — VO2 effort, hold legal stroke and tempo under fatigue", interval: "On 1:45", focus: "Aerobic power across all four strokes" },
        ]},
      { label: "VO2 6×150 IM", totalYards: 900, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2l08", reps: 6, dist: 150, desc: "150 IM (fly/back/breast) — fast effort, VO2, strong transitions", interval: "On 2:35", focus: "Stroke-change power at high heart rate" },
        ]},
      { label: "VO2 8×100 Back", totalYards: 800, types: ["endurance","mixed"], strokes: ["back"], sets: [
          { id: "s_vo2l09", reps: 8, dist: 100, desc: "Backstroke — VO2 effort, steady fast tempo, hold hip rotation", interval: "On 1:45", focus: "Aerobic power on back — short rest" },
        ]},
      { label: "VO2 8×100 Breast", totalYards: 800, types: ["endurance","mixed"], strokes: ["breast"], sets: [
          { id: "s_vo2l10", reps: 8, dist: 100, desc: "Breaststroke — VO2 effort, strong pullouts, hold stroke length", interval: "On 1:55", focus: "Aerobic power on breast — repeatable just under race" },
        ]},
      { label: "VO2 6×100 Fly", totalYards: 600, types: ["endurance","mixed"], strokes: ["fly"], sets: [
          { id: "s_vo2l11", reps: 6, dist: 100, desc: "Butterfly — VO2 effort, swim fly while form holds then strong free finish", interval: "On 1:45", focus: "Fly aerobic power without form breakdown" },
        ]},
    ];

    // ═══════════════════════════════════════════════════════════════
    //  SCM (25m short-course meters) option banks.
    //  Ported from the SCY (25y) banks: same distances (lengths match),
    //  intervals scaled +5% to reflect meter-vs-yard pace difference.
    //  Specialty types fall back to LCM (50m) if a type entry is missing.
    // ═══════════════════════════════════════════════════════════════

    const WARMUP_OPTIONS_SCM = [
      { label: "Easy Free 400", typeAffinity: ["sprint"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_d0qsmt", reps: 1, dist: 400, desc: "Easy Freestyle", interval: "No interval — swim easy", focus: "Loosen up, steady breathing" },
        ]},
      { label: "Free Warmup (400 yd)", typeAffinity: ["technique","sprint"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_mi6k7b", reps: 1, dist: 200, desc: "Easy Freestyle", interval: "No interval", focus: "Settle into rhythm" },
          { id: "s_wk0q4k", reps: 4, dist: 50, desc: "25 Drill / 25 Easy Swim (stroke choice)", interval: "On 1:20", focus: "Feel for the water" },
        ]},
      { label: "Free Warmup + Streamline (400 yd)", typeAffinity: ["endurance"], totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_yz2k8e", reps: 1, dist: 300, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Gradually wake up" },
          { id: "s_31ipl5", reps: 4, dist: 25, desc: "Kick (no board, streamlined)", interval: "On :40", focus: "Loosen hips and ankles" },
        ]},
      { label: "Back Warmup + Pull (500 yd)", typeAffinity: ["endurance"], totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_po8kz8", reps: 1, dist: 400, desc: "Freestyle — easy, focus on long stroke", interval: "No interval", focus: "Prime the aerobic engine" },
          { id: "s_ecjlpp", reps: 4, dist: 25, desc: "Backstroke — easy, open the shoulders", interval: "On :45", focus: "Counter the freestyle pull" },
        ]},
      { label: "Free Warmup + Pull (600 yd)", typeAffinity: ["endurance"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_30mov4", reps: 1, dist: 400, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Aerobic warm-up, long stroke" },
          { id: "s_hvips7", reps: 4, dist: 50, desc: "Pull (buoy) — easy, focus on catch and extension", interval: "On 1:25", focus: "Upper-body prime before main set", eq: "pull" },
        ]},
      { label: "Back Warmup + Pull (700 yd)", typeAffinity: ["endurance"], totalYards: 700, types: [], strokes: [], sets: [
          { id: "s_im23ei", reps: 1, dist: 400, desc: "Easy Freestyle — settle in, count strokes", interval: "No interval", focus: "Efficiency awareness from the first lap" },
          { id: "s_hwy32h", reps: 2, dist: 100, desc: "Kick (board optional) — moderate, even effort", interval: "On 2:40", focus: "Activate legs", eq: "kick" },
          { id: "s_r12f1e", reps: 1, dist: 100, desc: "Easy backstroke — open the shoulders, counter freestyle pull", interval: "No interval", focus: "All-around warm-up close" },
        ]},
      { label: "Free Warmup + Build (800 yd)", typeAffinity: ["technique","endurance"], totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_qjwkl8", reps: 1, dist: 400, desc: "Freestyle — very easy, nose down, minimal head movement", interval: "No interval", focus: "Prime body position and breathing pattern" },
          { id: "s_4e8t8k", reps: 4, dist: 50, desc: "Stroke drills — choice, one focus per 50", interval: "On 1:25", focus: "Technical warm-up" },
          { id: "s_0br41r", reps: 1, dist: 200, desc: "Freestyle — build from easy to moderate", interval: "No interval", focus: "Ramp up the aerobic engine before main set" },
        ]},
      { label: "The Dirty Dozen", typeAffinity: ["sprint"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_389rli", reps: 4, dist: 50, desc: "Easy freestyle — wake up the system", interval: "On 1:20", focus: "Loosen up" },
          { id: "s_rs6moi", reps: 4, dist: 50, desc: "Build — easy→90% by the wall, no all-out yet", interval: "On 1:20", focus: "Progressive activation of fast-twitch" },
          { id: "s_i1aq4z", reps: 4, dist: 50, desc: "Sprint — 25 fast / 25 easy float", interval: "On 1:35", focus: "Prime top-end speed without burning out" },
        ]},
      { label: "Snake Pyramid 650", typeAffinity: ["endurance"], totalYards: 650, types: [], strokes: [], sets: [
          { id: "s_jssmlm", reps: 1, dist: 50, desc: "Freestyle — easy", interval: "On 1:20", focus: "Toe in the water" },
          { id: "s_uod33p", reps: 1, dist: 100, desc: "Freestyle — easy build", interval: "On 2:25", focus: "Lengthen the stroke" },
          { id: "s_j0trbn", reps: 1, dist: 150, desc: "Freestyle — moderate, smooth tempo", interval: "On 3:30", focus: "Settle into aerobic rhythm" },
          { id: "s_n49h1f", reps: 1, dist: 200, desc: "Freestyle — moderate, peak of the pyramid", interval: "On 4:30", focus: "Hold form across the longest piece" },
          { id: "s_4zgz45", reps: 1, dist: 100, desc: "Freestyle — moderate, faster than the way up", interval: "On 2:20", focus: "Descend the pyramid" },
          { id: "s_xyb681", reps: 1, dist: 50, desc: "Freestyle — strong close", interval: "On 1:15", focus: "Fast finish" },
        ]},
      { label: "Build to Boil 900", typeAffinity: ["sprint","endurance"], totalYards: 900, types: [], strokes: [], sets: [
          { id: "s_ti0nby", reps: 1, dist: 300, desc: "Freestyle — easy, find aerobic rhythm", interval: "On 6:50", focus: "Act 1: settle in" },
          { id: "s_pe7az8", reps: 1, dist: 300, desc: "Freestyle — moderate, hold form throughout", interval: "On 6:35", focus: "Act 2: warm the engine" },
          { id: "s_0vsu3m", reps: 1, dist: 300, desc: "Freestyle — strong, near threshold by the end", interval: "On 6:20", focus: "Act 3: ignite for the main set" },
        ]},
      { label: "Wake & Warm Build 700", typeAffinity: ["technique","endurance"], totalYards: 700, types: [], strokes: [], sets: [
          { id: "s_zx8g38", reps: 1, dist: 200, desc: "Freestyle — very easy, nose down, minimum head movement", interval: "On 4:45", focus: "Open the body, slow the heart rate" },
          { id: "s_hkjxt5", reps: 4, dist: 50, desc: "25 drill choice / 25 swim — same stroke each rep", interval: "On 1:25", focus: "Build a technique focus that carries into the swim" },
          { id: "s_epaz3r", reps: 1, dist: 300, desc: "Freestyle — build easy → moderate by the end", interval: "On 6:50", focus: "Roll the warmth into the main set" },
        ]},
      { label: "Corkscrew Open 600", typeAffinity: ["technique"], totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_worllv", reps: 4, dist: 50, desc: "Corkscrew — alternate 25 freestyle right-side / 25 backstroke left-side (or fluid roll between them)", interval: "On 1:30", focus: "Open shoulders, hips, and rotation in one move" },
          { id: "s_pwqg7q", reps: 4, dist: 100, desc: "Freestyle — easy build, focus on body roll learned from the corkscrew", interval: "On 2:25", focus: "Apply rotation to flat freestyle" },
        ]},
      { label: "IM Ladder Wake-up", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_t9zk5e", reps: 1, dist: 100, desc: "Freestyle — easy", interval: "On 2:25", focus: "Settle in" },
          { id: "s_wjvl6t", reps: 1, dist: 100, desc: "IM drill — 25 each stroke, easy", interval: "On 2:40", focus: "Sample all four strokes" },
          { id: "s_kukk49", reps: 1, dist: 100, desc: "Kick (board) — freestyle, moderate tempo", interval: "On 2:40", focus: "Wake up the legs", eq: "kick" },
          { id: "s_dhr403", reps: 1, dist: 100, desc: "IM drill — 25 each stroke, focus on transitions", interval: "On 2:40", focus: "Stroke changeovers" },
          { id: "s_5bw6da", reps: 1, dist: 100, desc: "Freestyle — moderate, smooth tempo", interval: "On 2:10", focus: "Re-engage main stroke" },
          { id: "s_k1nkt0", reps: 1, dist: 100, desc: "100 IM — build each 25 to strong", interval: "On 2:40", focus: "Bring it home with intensity ramp" },
        ]},
      { label: "Stroke Buffet 800", totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_s1568t", reps: 1, dist: 200, desc: "Freestyle — easy, settle in", interval: "On 4:15", focus: "Find aerobic rhythm" },
          { id: "s_0t64np", reps: 1, dist: 200, desc: "Backstroke — easy, open the shoulders", interval: "On 4:30", focus: "Switch on rotation" },
          { id: "s_e8ka41", reps: 1, dist: 200, desc: "Breaststroke or choice (not fly) — easy", interval: "On 4:45", focus: "Sample a non-free stroke" },
          { id: "s_9ghga6", reps: 1, dist: 200, desc: "Freestyle — build to moderate by the end", interval: "On 4:00", focus: "Close at warm-up tempo" },
        ]},
      { label: "Tool Time 800", totalYards: 800, types: [], strokes: [], sets: [
          { id: "s_gc0wjy", reps: 1, dist: 200, desc: "Freestyle — easy swim, no gear", interval: "On 4:15", focus: "Open without gear" },
          { id: "s_x5473y", reps: 1, dist: 200, desc: "Pull (buoy) — easy, long stroke, high-elbow catch", interval: "On 4:15", focus: "Isolate the pull", eq: "pull" },
          { id: "s_gz3ccy", reps: 1, dist: 200, desc: "Kick (board) — freestyle or choice, moderate tempo", interval: "On 4:45", focus: "Engage the legs", eq: "kick" },
          { id: "s_kl3zwc", reps: 1, dist: 200, desc: "Freestyle — build the whole way, finish moderate-strong", interval: "On 4:00", focus: "Re-integrate everything" },
        ]},
      { label: "Rainbow Ladder 1000", typeAffinity: ["endurance","technique"], totalYards: 1000, types: [], strokes: [], sets: [
          { id: "s_rp8qme", reps: 1, dist: 200, desc: "Freestyle — very easy (white zone)", interval: "On 4:45", focus: "Color 1: barely working" },
          { id: "s_9s5aav", reps: 1, dist: 200, desc: "Freestyle — drill choice / swim by 25 (yellow zone)", interval: "On 4:30", focus: "Color 2: technique-overlay" },
          { id: "s_4blymz", reps: 1, dist: 200, desc: "Freestyle — moderate, smooth tempo (orange zone)", interval: "On 4:15", focus: "Color 3: aerobic warmth" },
          { id: "s_ox731h", reps: 1, dist: 200, desc: "Freestyle — moderate-strong, controlled (red zone)", interval: "On 4:00", focus: "Color 4: pre-main pressure" },
          { id: "s_k586re", reps: 1, dist: 200, desc: "Freestyle — easy spin-down to reset (white zone)", interval: "On 4:45", focus: "Color 5: shake out before the main" },
        ]},
    ];

    const COOLDOWN_OPTIONS_SCM = [
      { label: "Cool-Down — Easy Free 200", totalYards: 200, types: [], strokes: [], sets: [
          { id: "s_k1uvby", reps: 1, dist: 200, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Bring heart rate down, flush the muscles" },
        ]},
      { label: "Cool-Down — Back (250 yd)", totalYards: 250, types: [], strokes: [], sets: [
          { id: "s_xl27fz", reps: 1, dist: 150, desc: "Easy Backstroke or choice", interval: "No interval", focus: "Let lactic acid clear, open the chest" },
          { id: "s_e3qyad", reps: 1, dist: 100, desc: "Easy Freestyle — progressively slower", interval: "No interval", focus: "Full cool-down — slowest laps of the day" },
        ]},
      { label: "Cool-Down — Easy Back 50", totalYards: 200, types: [], strokes: [], sets: [
          { id: "s_n1xanh", reps: 4, dist: 50, desc: "Easy alternating: Freestyle / Backstroke (swap each length)", interval: "On 1:25", focus: "Gentle recovery — no effort, just move" },
        ]},
      { label: "Cool-Down — Back (300 yd)", totalYards: 300, types: [], strokes: [], sets: [
          { id: "s_7lfpfe", reps: 1, dist: 200, desc: "Easy Freestyle — long, relaxed stroke", interval: "No interval", focus: "Flush the muscles, lower heart rate" },
          { id: "s_zzw92l", reps: 1, dist: 100, desc: "Easy backstroke — gentle on the shoulders", interval: "No interval", focus: "Open the chest, counter freestyle pull" },
        ]},
      { label: "Cool-Down — Back (400 yd)", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_7u0c2q", reps: 1, dist: 200, desc: "Easy backstroke or choice — progressively slower", interval: "No interval", focus: "Let lactic acid clear, open the chest" },
          { id: "s_w4dazp", reps: 1, dist: 100, desc: "Easy freestyle — very easy, focus on long glide", interval: "No interval", focus: "Wind down heart rate" },
          { id: "s_6cmnys", reps: 4, dist: 25, desc: "Easy kick choice — no board, streamlined", interval: "On :45", focus: "Gentle leg flush after a big set" },
        ]},
    ];

    const DRILL_OPTIONS_SCM = [
      { label: "IM 4×75", totalYards: 300, types: ["im"], strokes: [], sets: [
          { id: "s_ccaxsw", reps: 4, dist: 75, desc: "25 Fly Drill / 25 Back Drill / 25 Breast Drill", interval: "On 2:40", focus: "Feel each IM stroke before the main set" },
        ]},
      { label: "IM Kick (400 yd)", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_4fta3s", reps: 8, dist: 25, desc: "Stroke drills — 2 each: fly, back, breast, free", interval: "On :40", focus: "Isolate technique per stroke" },
          { id: "s_h1chn4", reps: 4, dist: 50, desc: "IM order kick — 12.5 yards each stroke", interval: "On 1:50", focus: "Kick rhythm for each stroke", eq: "kick" },
        ]},
      { label: "IM Kick + Pull (400 yd)", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_pkuvds", reps: 4, dist: 50, desc: "IM order kick — 12.5 yards each stroke", interval: "On 1:50", focus: "Wake up legs across IM strokes", eq: "kick" },
          { id: "s_umqsxh", reps: 2, dist: 100, desc: "Pull (buoy) — alternate 50 free / 50 stroke choice", interval: "On 2:20", focus: "Upper-body warm-up before IM main set", eq: "pull" },
        ]},
      { label: "IM Kick (200 yd)", totalYards: 200, types: ["im"], strokes: [], sets: [
          { id: "s_ui08c0", reps: 4, dist: 25, desc: "Stroke drills — 1 each: fly, back, breast, free", interval: "On :40", focus: "Brief all-stroke activation" },
          { id: "s_jm3ard", reps: 4, dist: 25, desc: "IM kick — 6 yards each stroke, no board", interval: "On :45", focus: "Kick priming across all four strokes" },
        ]},
      { label: "IM 3×100", totalYards: 300, types: ["im"], strokes: [], sets: [
          { id: "s_yjd1bv", reps: 3, dist: 100, desc: "100 IM — 25 each stroke, easy (not race pace)", interval: "On 2:40", focus: "Stroke flow and transition practice" },
        ]},
      { label: "IM 4×100", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_yhcy81", reps: 4, dist: 100, desc: "100 IM — 25 drill each stroke, smooth transitions", interval: "On 2:40", focus: "Full-stroke drill sequence before IM main set" },
        ]},
      { label: "IM 5×100", totalYards: 500, types: ["im"], strokes: [], sets: [
          { id: "s_ovlsh7", reps: 5, dist: 100, desc: "100 IM — 25 drill each stroke (fly, back, breast, free)", interval: "On 2:40", focus: "High-rep IM drill sequence before long set" },
        ]},
      { label: "IM 6×100", totalYards: 600, types: ["im"], strokes: [], sets: [
          { id: "s_ys7pu5", reps: 6, dist: 100, desc: "100 IM — 25 each stroke drill, emphasize transitions", interval: "On 2:40", focus: "Full IM drill volume" },
        ]},
      { label: "IM Transition 8×100", totalYards: 800, types: ["im"], strokes: [], sets: [
          { id: "s_pt0r79", reps: 8, dist: 100, desc: "100 IM — focus on smooth fly→back and breast→free transitions, drill the first 25 of each stroke", interval: "On 2:25", focus: "Transitions are where IM races are won — practice them at volume" },
        ]},
      { label: "IM Stroke Block 1000", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_cezaeu", reps: 4, dist: 100, desc: "Fly drill — single-arm 25 / 3-3-3 25 / full stroke 50", interval: "On 2:40", focus: "Fly mechanics at low intensity" },
          { id: "s_kdiuho", reps: 4, dist: 100, desc: "Back drill — single-arm 25 / catch-up back 25 / full stroke 50", interval: "On 2:25", focus: "Back rotation and arm timing" },
          { id: "s_95rau8", reps: 2, dist: 100, desc: "Breast drill — 2 kick 1 pull 25 / full stroke 75", interval: "On 2:40", focus: "Breast pull-glide cycle" },
        ]},
      { label: "Pull 3×100", totalYards: 300, types: ["distance"], strokes: [], sets: [
          { id: "s_si34om", reps: 3, dist: 100, desc: "Pull (buoy) — easy/moderate, focus on catch", interval: "On 2:20", focus: "Prime the pull pattern before distance work", eq: "pull" },
        ]},
      { label: "Catch-Up 4×100", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_j2lkfj", reps: 4, dist: 100, desc: "Pull (buoy) — moderate pace, high elbow catch", interval: "On 2:20", focus: "Upper body endurance, long stroke", eq: "pull" },
        ]},
      { label: "Kick + Build (400 yd)", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_fpvmsl", reps: 6, dist: 50, desc: "Kick — moderate effort (board optional)", interval: "On 1:50", focus: "Leg conditioning and body position", eq: "kick" },
          { id: "s_yha6vi", reps: 2, dist: 50, desc: "Freestyle — build from easy to moderate", interval: "On 1:20", focus: "Prime aerobic engine for main set" },
        ]},
      { label: "Kick + Catch-Up (400 yd)", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_mxkteq", reps: 4, dist: 50, desc: "Kick — moderate, focus on body position (board optional)", interval: "On 1:50", focus: "Leg conditioning before distance work", eq: "kick" },
          { id: "s_f0tchk", reps: 2, dist: 100, desc: "Pull (buoy) — long stroke, focus on catch", interval: "On 2:20", focus: "Upper-body endurance setup", eq: "pull" },
        ]},
      { label: "Pull 4×50", totalYards: 200, types: ["distance"], strokes: [], sets: [
          { id: "s_hsige1", reps: 4, dist: 50, desc: "Pull (buoy) — easy, focus on catch engagement", interval: "On 1:20", focus: "Quick pull priming before distance work", eq: "pull" },
        ]},
      { label: "Kick 6×50", totalYards: 300, types: ["distance"], strokes: [], sets: [
          { id: "s_18yg9r", reps: 6, dist: 50, desc: "Kick — moderate, even effort (board optional)", interval: "On 1:50", focus: "Leg priming for distance main set", eq: "kick" },
        ]},
      { label: "Free 4×100", totalYards: 400, types: ["distance"], strokes: [], sets: [
          { id: "s_eaidz3", reps: 4, dist: 100, desc: "Freestyle — long stroke, nose-down position, minimal head movement", interval: "On 2:20", focus: "Head position and body alignment for distance work", eq: "snorkel" },
        ]},
      { label: "Pull 5×100", totalYards: 500, types: ["distance"], strokes: [], sets: [
          { id: "s_0dadwr", reps: 5, dist: 100, desc: "Pull (buoy) — moderate pace, high-elbow catch, long extension", interval: "On 2:20", focus: "Extended upper-body priming for long distance set", eq: "pull" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["distance"], strokes: [], sets: [
          { id: "s_uxqx51", reps: 12, dist: 50, desc: "25 catch-up drill / 25 easy swim — stay relaxed, long stroke", interval: "On 1:20", focus: "Technique-heavy priming before long distance main set" },
        ]},
      { label: "Pull 8×100", totalYards: 800, types: ["distance"], strokes: [], sets: [
          { id: "s_lacvwk", reps: 8, dist: 100, desc: "Pull (buoy) — aerobic pace, focus on extension and high-elbow catch", interval: "On 2:20", focus: "Sustained upper-body endurance priming before distance work", eq: "pull" },
        ]},
      { label: "Catch-Up 9×100 + Pull 100", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_cnwdjm", reps: 9, dist: 100, desc: "25 catch-up drill / 75 swim — long stroke, glide through hand entry", interval: "On 2:10", focus: "Stroke-length emphasis at volume" },
          { id: "s_5som9a", reps: 1, dist: 100, desc: "Pull (buoy) — easy, recover from drill volume before the main", interval: "On 2:35", focus: "Reset and feel the long stroke from drill", eq: "pull" },
        ]},
      { label: "Build 6×50", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_jqr9tz", reps: 6, dist: 50, desc: "25 Easy / 25 Build (NOT all-out yet)", interval: "On 1:20", focus: "Activate fast-twitch, save energy for main set" },
        ]},
      { label: "Streamline + Build (300 yd)", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_m15q1e", reps: 4, dist: 50, desc: "Underwater kick off wall (10m), easy swim out", interval: "On 1:05", focus: "Streamline and underwaters", eq: "underkick" },
          { id: "s_on1o8a", reps: 4, dist: 25, desc: "Tempo build — easy to fast (not all out)", interval: "On :45", focus: "Turnover prep, no sprinting yet" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_shljk4", reps: 4, dist: 50, desc: "Kick — sharp tempo build (board optional)", interval: "On 1:50", focus: "Activate sprint leg drive", eq: "kick" },
          { id: "s_x8a993", reps: 2, dist: 100, desc: "Pull (buoy) — fast build, focus on hand speed", interval: "On 2:20", focus: "Activate upper-body power", eq: "pull" },
        ]},
      { label: "Underwater + Build (200 yd)", totalYards: 200, types: ["sprint"], strokes: [], sets: [
          { id: "s_wnf21u", reps: 4, dist: 25, desc: "ALL OUT 12m sprint + easy swim out", interval: "On :40", focus: "Explosive neuromuscular activation" },
          { id: "s_vw4mhl", reps: 4, dist: 25, desc: "Build to 80% — not all out yet", interval: "On :40", focus: "Speed priming without early fatigue" },
        ]},
      { label: "Streamline 6×50", totalYards: 300, types: ["sprint"], strokes: [], sets: [
          { id: "s_ie5pai", reps: 6, dist: 50, desc: "25 underwater kick off wall + 25 easy swim", interval: "On 1:05", focus: "Streamline and breakout priming" },
        ]},
      { label: "Build 8×50", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_91qzvx", reps: 8, dist: 50, desc: "25 sprint effort / 25 easy — ramp to 90% by rep 6–8", interval: "On 1:20", focus: "Graduated sprint activation" },
        ]},
      { label: "Build 10×50", totalYards: 500, types: ["sprint"], strokes: [], sets: [
          { id: "s_f1h2px", reps: 10, dist: 50, desc: "25 build (easy → 85%) / 25 easy — increase intensity each rep", interval: "On 1:35", focus: "Progressive sprint activation over volume" },
        ]},
      { label: "Build 12×50", totalYards: 600, types: ["sprint"], strokes: [], sets: [
          { id: "s_clzi2y", reps: 12, dist: 50, desc: "25 build (ramp to 90%) / 25 easy — last 4 reps at 95%", interval: "On 1:35", focus: "Full sprint priming with high volume" },
        ]},
      { label: "Pull 4×100", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_mr09wo", reps: 4, dist: 100, desc: "Pull (buoy) — easy/moderate, relaxed stroke", interval: "On 2:20", focus: "Aerobic priming, conserve energy", eq: "pull" },
        ]},
      { label: "Kick 300", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_1tkoqa", reps: 1, dist: 300, desc: "Kick — choice of stroke, easy pace (board optional)", interval: "No interval", focus: "Leg warm-up, build base endurance", eq: "kick" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_obb1u0", reps: 4, dist: 50, desc: "Kick — easy/moderate, focus on rhythm (board optional)", interval: "On 1:50", focus: "Aerobic leg priming", eq: "kick" },
          { id: "s_q7ebdb", reps: 2, dist: 100, desc: "Pull (buoy) — relaxed stroke, long extension", interval: "On 2:20", focus: "Aerobic upper-body warm-up", eq: "pull" },
        ]},
      { label: "Free 2×100", totalYards: 200, types: ["endurance"], strokes: [], sets: [
          { id: "s_lgnnwj", reps: 2, dist: 100, desc: "Freestyle — smooth stroke, relaxed breathing, easy pace", interval: "On 2:20", focus: "Aerobic priming — stay relaxed" },
        ]},
      { label: "Kick 3×100", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_x4f7co", reps: 3, dist: 100, desc: "Kick — easy aerobic pace (board optional)", interval: "On 2:20", focus: "Leg priming for long set", eq: "kick" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_j04inl", reps: 8, dist: 50, desc: "25 catch-up drill / 25 easy swim — relaxed stroke", interval: "On 1:20", focus: "Stroke efficiency before long aerobic set" },
        ]},
      { label: "Free 6×50", totalYards: 300, types: ["endurance"], strokes: [], sets: [
          { id: "s_g2m8di", reps: 6, dist: 50, desc: "Freestyle — easy pace, flat head, focus on hip rotation and body line", interval: "On 1:20", focus: "Aerobic priming with body alignment focus", eq: "snorkel" },
        ]},
      { label: "Pull 5×100", totalYards: 500, types: ["endurance"], strokes: [], sets: [
          { id: "s_ej3949", reps: 5, dist: 100, desc: "Pull (buoy) — relaxed, smooth stroke, aerobic pace", interval: "On 2:20", focus: "Extended aerobic priming, save legs for long main set", eq: "pull" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["endurance"], strokes: [], sets: [
          { id: "s_tiwpvx", reps: 12, dist: 50, desc: "25 catch-up drill / 25 easy swim — stay completely relaxed throughout", interval: "On 1:20", focus: "Stroke efficiency and aerobic priming before long set" },
        ]},
      { label: "Bilateral Breathing 8×100", totalYards: 800, types: ["endurance"], strokes: [], sets: [
          { id: "s_g4rlas", reps: 8, dist: 100, desc: "Freestyle — breathe every 3 strokes the first 50, every 5 strokes the second 50", interval: "On 2:10", focus: "Aerobic priming with breathing-control overlay (lung work)" },
        ]},
      { label: "Pull + Catch-Up Stack 900", totalYards: 900, types: ["endurance"], strokes: [], sets: [
          { id: "s_qx1jgp", reps: 6, dist: 100, desc: "Pull (buoy) — relaxed, aerobic pace, long stroke", interval: "On 2:20", focus: "Aerobic upper-body work before endurance main", eq: "pull" },
          { id: "s_wfit61", reps: 6, dist: 50, desc: "Catch-up drill — full extension on every stroke, no glide skipping", interval: "On 1:20", focus: "Stroke-length reset between pull and main" },
        ]},
      { label: "Free 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_fu81vq", reps: 8, dist: 50, desc: "25 Drill / 25 Easy Swim — same stroke each rep (your choice)", interval: "On 1:30", focus: "Feel the drill translate into the swim" },
        ]},
      { label: "Kick 4×75", totalYards: 300, types: ["technique"], strokes: [], sets: [
          { id: "s_pcegsj", reps: 4, dist: 75, desc: "Kick 25 / Drill 25 / Swim 25 (same stroke)", interval: "On 1:45", focus: "Layered skill building — kick → drill → apply", eq: "kickPart" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_lcbo8j", reps: 4, dist: 50, desc: "Kick — slow, focus on stroke alignment (board optional)", interval: "On 1:50", focus: "Isolate leg technique", eq: "kick" },
          { id: "s_ye7cmb", reps: 2, dist: 100, desc: "Pull (buoy) — high-elbow catch, long stroke", interval: "On 2:20", focus: "Isolate upper-body technique", eq: "pull" },
        ]},
      { label: "Free 4×50", totalYards: 200, types: ["technique"], strokes: [], sets: [
          { id: "s_m9hndf", reps: 4, dist: 50, desc: "25 drill (your choice) / 25 easy swim — feel the difference", interval: "On 1:20", focus: "Brief drill activation" },
        ]},
      { label: "Fingertip Drag 6×50", totalYards: 300, types: ["technique"], strokes: [], sets: [
          { id: "s_y173uv", reps: 6, dist: 50, desc: "Fingertip drag drill — 25 drill / 25 swim, same stroke", interval: "On 1:20", focus: "Hand path and elbow height awareness" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_jcd2v8", reps: 8, dist: 50, desc: "25 catch-up drill / 25 swim — focus on full extension", interval: "On 1:20", focus: "Stroke timing and long extension" },
        ]},
      { label: "Catch-Up 8×50", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_v7o7u9", reps: 8, dist: 50, desc: "Freestyle — 25 catch-up drill / 25 swim, flat head position", interval: "On 1:20", focus: "Stroke mechanics with consistent head position", eq: "snorkel" },
        ]},
      { label: "Free 10×50", totalYards: 500, types: ["technique"], strokes: [], sets: [
          { id: "s_v91ivt", reps: 10, dist: 50, desc: "25 drill (your choice) / 25 swim — isolate and apply each rep", interval: "On 1:25", focus: "High-rep technique priming before technique set" },
        ]},
      { label: "Catch-Up 12×50", totalYards: 600, types: ["technique"], strokes: [], sets: [
          { id: "s_7bksh9", reps: 12, dist: 50, desc: "25 drill / 25 swim — rotate drills every 2 reps (catch-up, fingertip drag, side kick)", interval: "On 1:25", focus: "Broad technique warm-up with variety" },
        ]},
      { label: "Kick 4×75", totalYards: 300, types: ["mixed"], strokes: [], sets: [
          { id: "s_y9k1q3", reps: 4, dist: 75, desc: "Kick 25 / Drill 25 / Swim 25 — stroke choice", interval: "On 1:45", focus: "All-around activation before varied main set", eq: "kickPart" },
        ]},
      { label: "Tempo (400 yd)", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_89rvso", reps: 6, dist: 50, desc: "Drill/Swim by 25 — your stroke choice", interval: "On 1:05", focus: "Skill warm-up" },
          { id: "s_ohzyei", reps: 4, dist: 25, desc: "Fast tempo (not all out) — choice stroke", interval: "On :45", focus: "Activate speed system" },
        ]},
      { label: "Kick + Pull (400 yd)", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_ia810h", reps: 4, dist: 50, desc: "Kick — moderate, choice of stroke (board optional)", interval: "On 1:50", focus: "Leg activation", eq: "kick" },
          { id: "s_ljtemc", reps: 2, dist: 100, desc: "Pull (buoy) — moderate, smooth rotation", interval: "On 2:20", focus: "Upper-body warm-up before mix", eq: "pull" },
        ]},
      { label: "Free (200 yd)", totalYards: 200, types: ["mixed"], strokes: [], sets: [
          { id: "s_b52il1", reps: 4, dist: 25, desc: "Choice stroke drill — activate technique", interval: "On :45", focus: "Brief all-around drill activation" },
          { id: "s_oe04fe", reps: 4, dist: 25, desc: "Choice stroke swim — apply the drill", interval: "On :45", focus: "Drill translation before mixed set" },
        ]},
      { label: "Free 3×100", totalYards: 300, types: ["mixed"], strokes: [], sets: [
          { id: "s_shfui0", reps: 3, dist: 100, desc: "Mixed stroke — 50 drill of choice / 50 swim", interval: "On 2:20", focus: "Variety technique priming" },
        ]},
      { label: "Free 4×100", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_taodii", reps: 4, dist: 100, desc: "Alternating stroke — 50 stroke A drill / 50 stroke B drill", interval: "On 2:20", focus: "Cross-stroke skill work" },
        ]},
      { label: "Free 8×50", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_u3tjw3", reps: 8, dist: 50, desc: "Freestyle — 25 drill of choice / 25 swim, flat head throughout", interval: "On 1:20", focus: "Technique variety with consistent head position", eq: "snorkel" },
        ]},
      { label: "Free 10×50", totalYards: 500, types: ["mixed"], strokes: [], sets: [
          { id: "s_t53i8l", reps: 10, dist: 50, desc: "25 drill choice / 25 swim — alternate stroke every other rep", interval: "On 1:20", focus: "All-around technique priming before mixed set" },
        ]},
      { label: "Kick 8×75", totalYards: 600, types: ["mixed"], strokes: [], sets: [
          { id: "s_bil6xy", reps: 8, dist: 75, desc: "25 kick / 25 drill / 25 swim — stroke choice per rep", interval: "On 2:05", focus: "Layered skill warm-up across strokes", eq: "kickPart" },
        ]},
      { label: "All-Stroke Drill 8×100", totalYards: 800, types: ["mixed"], strokes: [], sets: [
          { id: "s_vauvdf", reps: 8, dist: 100, desc: "25 fly drill / 25 back drill / 25 breast drill / 25 free smooth — round 1 = drills, round 2 = swim each 25", interval: "On 2:25", focus: "Broad-stroke skill warm-up at volume" },
        ]},
      { label: "Layered Mixed 1000", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_q7y8ku", reps: 6, dist: 100, desc: "25 kick (board) / 25 drill choice / 50 swim — stroke choice per rep", interval: "On 2:25", focus: "Layered skill priming across strokes", eq: "kickPart" },
          { id: "s_4h0jar", reps: 4, dist: 100, desc: "Freestyle — 25 drill of choice / 75 swim, apply the drill into the swim", interval: "On 2:10", focus: "Freestyle technique-to-swim translation" },
        ]},
      { label: "Back Single-Arm 4×75", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_cn9csc", reps: 4, dist: 75, desc: "25 single-arm backstroke / 25 double-arm back drill / 25 backstroke swim", interval: "On 2:40", focus: "Lead arm extension and stroke timing in back" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_v282dw", reps: 6, dist: 50, desc: "Backstroke kick — 25 streamline (no board) / 25 with board", interval: "On 1:50", focus: "Body position on the back, hip drive", eq: "kick" },
        ]},
      { label: "Back Kick + Catch-Up (400 yd)", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_wt07kn", reps: 4, dist: 50, desc: "Backstroke kick — easy/moderate (board optional)", interval: "On 1:50", focus: "Leg conditioning for back", eq: "kick" },
          { id: "s_6rtglt", reps: 2, dist: 100, desc: "Pull (buoy) — backstroke pull pattern, focus on catch", interval: "On 2:20", focus: "Upper-body work for back", eq: "pull" },
        ]},
      { label: "Back Single-Arm 4×50", totalYards: 200, types: [], strokes: ["back"], sets: [
          { id: "s_msp4cn", reps: 4, dist: 50, desc: "Backstroke — 25 single-arm / 25 full stroke", interval: "On 1:35", focus: "Lead arm and rotation activation" },
        ]},
      { label: "Back Single-Arm 3×100", totalYards: 300, types: [], strokes: ["back"], sets: [
          { id: "s_5huohn", reps: 3, dist: 100, desc: "Backstroke — 25 single-arm (L) / 25 single-arm (R) / 50 full stroke", interval: "On 2:40", focus: "Each arm isolated then integrated" },
        ]},
      { label: "Back 8×50", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_4e8pas", reps: 8, dist: 50, desc: "25 back elbow-lead drill / 25 backstroke swim", interval: "On 1:35", focus: "High-elbow catch on backstroke" },
        ]},
      { label: "Back Single-Arm 10×50", totalYards: 500, types: [], strokes: ["back"], sets: [
          { id: "s_0lkjv5", reps: 10, dist: 50, desc: "25 single-arm backstroke / 25 full backstroke — alternate lead arm each rep", interval: "On 1:35", focus: "High-rep arm isolation before back set" },
        ]},
      { label: "Back Single-Arm 6×100", totalYards: 600, types: [], strokes: ["back"], sets: [
          { id: "s_a2g0mx", reps: 6, dist: 100, desc: "Backstroke — 25 single-arm (L) / 25 single-arm (R) / 50 full stroke", interval: "On 2:40", focus: "Full arm isolation and integration at volume" },
        ]},
      { label: "Breast Pull 4×75", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_4h899r", reps: 4, dist: 75, desc: "25 breast pull-only (no kick) / 25 breast kick (board) / 25 breast swim", interval: "On 2:05", focus: "Isolate then integrate the breast components" },
        ]},
      { label: "Breast Kick 6×50", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_jaqcui", reps: 6, dist: 50, desc: "Breaststroke kick (board) — focus on power and recovery", interval: "On 1:35", focus: "Breast kick rhythm and power", eq: "kick" },
        ]},
      { label: "Breast Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_8i9ztc", reps: 4, dist: 50, desc: "Breaststroke kick (board) — moderate effort", interval: "On 1:35", focus: "Power kick", eq: "kick" },
          { id: "s_4fsgrl", reps: 2, dist: 100, desc: "Pull (buoy) — breaststroke pull pattern", interval: "On 2:40", focus: "Strong pull-glide cycle", eq: "pull" },
        ]},
      { label: "Breast Pull 4×50", totalYards: 200, types: [], strokes: ["breast"], sets: [
          { id: "s_kogkv3", reps: 4, dist: 50, desc: "25 breast pull-only / 25 full breaststroke — feel the integration", interval: "On 1:35", focus: "Pull activation before kick integration" },
        ]},
      { label: "Breast Pull 3×100", totalYards: 300, types: [], strokes: ["breast"], sets: [
          { id: "s_84aplc", reps: 3, dist: 100, desc: "Breast — 25 kick only (board) / 25 pull only / 50 full stroke", interval: "On 2:40", focus: "Isolated-then-integrated breast work" },
        ]},
      { label: "Breast Pull 8×50", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_gbzpm1", reps: 8, dist: 50, desc: "25 breast timing drill (2-kick 1-pull) / 25 full breaststroke", interval: "On 1:35", focus: "Timing drill into race stroke" },
        ]},
      { label: "Breast Pull 10×50", totalYards: 500, types: [], strokes: ["breast"], sets: [
          { id: "s_zmxg6y", reps: 10, dist: 50, desc: "25 breaststroke pull-only (no kick, glide) / 25 full breaststroke", interval: "On 1:35", focus: "High-rep pull activation before breast set" },
        ]},
      { label: "Breast Pull 12×50", totalYards: 600, types: [], strokes: ["breast"], sets: [
          { id: "s_p6ojoy", reps: 12, dist: 50, desc: "25 breaststroke pull-only / 25 full breaststroke — feel pull integration on every rep", interval: "On 1:35", focus: "High-rep pull priming for long breast set" },
        ]},
      { label: "Fly Single-Arm 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_8sr3nx", reps: 6, dist: 50, desc: "25 fly drill (3-3-3 or single-arm) / 25 freestyle easy", interval: "On 1:35", focus: "Build fly tolerance with active recovery" },
        ]},
      { label: "Back Streamline 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_awlclu", reps: 6, dist: 50, desc: "Dolphin kick — alternate streamline on stomach / on back", interval: "On 1:50", focus: "Dolphin rhythm without arm fatigue", eq: "kick" },
        ]},
      { label: "Fly Kick + Pull (400 yd)", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_ucs6u2", reps: 4, dist: 50, desc: "Dolphin kick (board) — focus on rhythm", interval: "On 1:50", focus: "Sustained kick base for fly", eq: "kick" },
          { id: "s_7vuix3", reps: 2, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 freestyle pull (recovery in arm pattern)", interval: "On 2:05", focus: "Build fly catch with free recovery", eq: "pull" },
        ]},
      { label: "Fly Streamline 4×50", totalYards: 200, types: [], strokes: ["fly"], sets: [
          { id: "s_o0b21k", reps: 4, dist: 50, desc: "25 dolphin kick streamline / 25 easy freestyle", interval: "On 1:20", focus: "Dolphin activation without arm fatigue" },
        ]},
      { label: "Fly Single-Arm 6×50", totalYards: 300, types: [], strokes: ["fly"], sets: [
          { id: "s_u0ri6w", reps: 6, dist: 50, desc: "25 single-arm fly (alternate L/R each rep) / 25 easy freestyle", interval: "On 1:35", focus: "Arm mechanics and undulation before fly set" },
        ]},
      { label: "Fly Build 8×50", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_sx45md", reps: 8, dist: 50, desc: "25 fly drill (your choice) / 25 freestyle easy", interval: "On 1:35", focus: "Build fly tolerance, recover on free" },
        ]},
      { label: "Fly Single-Arm 10×50", totalYards: 500, types: [], strokes: ["fly"], sets: [
          { id: "s_6abovy", reps: 10, dist: 50, desc: "25 fly drill (single-arm or 3-3-3) / 25 freestyle easy", interval: "On 1:35", focus: "Extended fly drill volume with free recovery" },
        ]},
      { label: "Fly Single-Arm 12×50", totalYards: 600, types: [], strokes: ["fly"], sets: [
          { id: "s_v704kz", reps: 12, dist: 50, desc: "25 fly drill (alternate: single-arm L, single-arm R, 3-3-3 each group) / 25 freestyle easy", interval: "On 1:35", focus: "High-rep fly drill variety before main set" },
        ]},
    ];

    const MAIN_OPTIONS_SCM = [
      { label: "IM Descend", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_aq83wp", reps: 2, dist: 200, desc: "IM — easy effort, focus on stroke order", interval: "On 6:20", focus: "Smooth fly→back→breast→free transitions" },
          { id: "s_vluqdc", reps: 4, dist: 100, desc: "IM — moderate to strong, descend 1→4", interval: "On 3:10", focus: "Each rep a little faster" },
          { id: "s_evnffg", reps: 4, dist: 50, desc: "IM — fast effort, hold technique under fatigue", interval: "On 1:35", focus: "Power through each stroke" },
        ]},
      { label: "Stroke Focus + IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_g3ypr8", reps: 4, dist: 100, desc: "IM — moderate, smooth transitions", interval: "On 3:10", focus: "Establish stroke order under load" },
          { id: "s_svs1yh", reps: 8, dist: 50, desc: "2 each stroke: fly/back/breast/free, descend each pair", interval: "On 1:35", focus: "Individual stroke speed" },
          { id: "s_bhoh2e", reps: 4, dist: 75, desc: "50 IM + 25 Free strong", interval: "On 2:40", focus: "Combine strokes, finish fast" },
        ]},
      { label: "IM Pyramid", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_e1mipa", reps: 1, dist: 100, desc: "IM — easy", interval: "On 2:40", focus: "Set your baseline" },
          { id: "s_paw2p9", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:20", focus: "Build into it" },
          { id: "s_90f0tc", reps: 1, dist: 400, desc: "IM — strong, sustained", interval: "On 12:35", focus: "Test your aerobic fitness" },
          { id: "s_eia092", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:20", focus: "Hang on — tired strokes" },
          { id: "s_you1fh", reps: 1, dist: 100, desc: "IM — ALL OUT finish", interval: "On 3:10", focus: "Empty the tank" },
        ]},
      { label: "Broken IM 200s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_afounb", reps: 4, dist: 200, desc: "Broken IM — 50 fly :10R, 50 back :10R, 50 breast :10R, 50 free", interval: "On 5:15", focus: "Hold race pace through each stroke with mini-rests" },
          { id: "s_fjkn6k", reps: 4, dist: 75, desc: "IM transitions — 50 IM + 25 Free strong", interval: "On 2:40", focus: "Sharpen the change-overs between strokes" },
        ]},
      { label: "IM Stroke Rotation", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_j4pyni", reps: 4, dist: 100, desc: "Choice non-free stroke — strong, hold form", interval: "On 2:20", focus: "Pick your weakest stroke for extra work" },
          { id: "s_g48lz4", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:10", focus: "Apply per-stroke quality to the full IM" },
          { id: "s_14mykq", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4 (close the set)", interval: "On 2:15", focus: "Finish the set with speed in the legs" },
        ]},
      { label: "Pull + IM Combo", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_ppeafy", reps: 3, dist: 200, desc: "Pull (buoy) — alternate strokes by 50 (free / non-free)", interval: "On 4:30", focus: "Long-stroke pull pattern across IM strokes", eq: "pull" },
          { id: "s_xkcffv", reps: 5, dist: 100, desc: "IM — moderate to fast, descend 1→5", interval: "On 3:10", focus: "Build from controlled to strong, smooth transitions" },
        ]},
      { label: "Reverse-Order IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_rvw8da", reps: 4, dist: 100, desc: "REVERSE IM — free, breast, back, fly (last 25)", interval: "On 3:10", focus: "Test stroke order under reversed sequence" },
          { id: "s_w3sd5a", reps: 4, dist: 100, desc: "Normal IM — fly, back, breast, free", interval: "On 3:10", focus: "Compare to your normal order" },
          { id: "s_i4t341", reps: 4, dist: 50, desc: "Choice non-free stroke — moderate to fast", interval: "On 1:20", focus: "Sustain non-free stroke effort" },
        ]},
      { label: "IM Race Pace 100s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_o76rag", reps: 1, dist: 200, desc: "IM — easy/moderate warm-up to set", interval: "On 6:20", focus: "Settle into IM rhythm" },
          { id: "s_ygqgb8", reps: 8, dist: 100, desc: "IM — at race pace, full recovery", interval: "On 2:40", focus: "Hit race pace consistently" },
          { id: "s_3yj4cr", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:40", focus: "Cool down" },
        ]},
      { label: "Broken 400 IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_d55kyp", reps: 2, dist: 400, desc: "Broken 400 IM — :15 rest after each 100", interval: "On 12:35", focus: "Race-pace 400 IM in segments" },
          { id: "s_ddz6gj", reps: 1, dist: 100, desc: "Easy choice — recovery between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_0uyfhn", reps: 4, dist: 50, desc: "Choice IM stroke — strong, finishing", interval: "On 1:35", focus: "Top off with stroke speed" },
        ]},
      { label: "Stroke-Drop IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_wue7q3", reps: 2, dist: 100, desc: "100 IM — full, all four strokes", interval: "On 3:10", focus: "Baseline full IM" },
          { id: "s_p6owxg", reps: 2, dist: 75, desc: "75 IM — drop the fly (back, breast, free)", interval: "On 2:40", focus: "Three strokes" },
          { id: "s_j9wej8", reps: 2, dist: 50, desc: "50 IM — back and breast only", interval: "On 1:35", focus: "Stroke transitions only" },
          { id: "s_0mbz68", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Reset" },
          { id: "s_0ijtio", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4", interval: "On 2:20", focus: "Build freestyle finish" },
          { id: "s_cf593h", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Top speed close" },
        ]},
      { label: "Threshold 200 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_qjnvv3", reps: 6, dist: 200, desc: "200 IM — threshold pace (RPE 7-8/10)", interval: "On 6:20", focus: "Hold threshold across IM strokes" },
        ]},
      { label: "Mini IM Pyramid", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_lcsd1t", reps: 1, dist: 25, desc: "Choice non-free stroke — fast", interval: "On :45", focus: "Stroke speed" },
          { id: "s_dshqma", reps: 1, dist: 50, desc: "IM — 12.5 each stroke, fast", interval: "On 1:35", focus: "All four under fatigue" },
          { id: "s_zvka6i", reps: 1, dist: 75, desc: "75 IM — back/breast/free, fast", interval: "On 2:40", focus: "Three strokes hard" },
          { id: "s_odiiij", reps: 1, dist: 100, desc: "100 IM — full, fast", interval: "On 2:40", focus: "Hold form across all four" },
          { id: "s_r37t08", reps: 1, dist: 200, desc: "200 IM — moderate, smooth", interval: "On 6:20", focus: "Apex of pyramid" },
          { id: "s_dgxhvn", reps: 1, dist: 100, desc: "100 IM — fast, fatigued", interval: "On 2:40", focus: "Sprint home" },
          { id: "s_tig9q8", reps: 1, dist: 75, desc: "75 IM — back/breast/free, fast", interval: "On 2:40", focus: "Hold the line" },
          { id: "s_1oukf8", reps: 1, dist: 50, desc: "IM — 12.5 each stroke, fast", interval: "On 1:35", focus: "Tight transitions" },
          { id: "s_q2bvbc", reps: 1, dist: 25, desc: "Choice non-free stroke — ALL OUT", interval: "On :45", focus: "Empty the tank" },
          { id: "s_s75dj7", reps: 1, dist: 400, desc: "Easy choice — full cool-down recovery", interval: "On 9:25", focus: "Aerobic close-out" },
        ]},
      { label: "IM Sprint Set", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_k8msuh", reps: 8, dist: 50, desc: "50 IM — 12.5 each stroke, fast", interval: "On 1:35", focus: "Top speed across all four strokes" },
          { id: "s_opyl0x", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:45", focus: "Active recovery" },
          { id: "s_ghcvnm", reps: 8, dist: 50, desc: "Choice IM stroke — ALL OUT (rotate stroke each rep)", interval: "On 1:35", focus: "Single-stroke speed work" },
        ]},
      { label: "IM with Free Sprint Finish", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_eltn35", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:20", focus: "Build IM endurance" },
          { id: "s_06eek0", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Reset" },
          { id: "s_69wxka", reps: 4, dist: 50, desc: "Freestyle — ALL OUT, max turnover", interval: "On 1:35", focus: "Sprint freestyle after IM fatigue" },
        ]},
      { label: "Aerobic IM Volume", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_h49vic", reps: 12, dist: 100, desc: "100 IM — easy/moderate (RPE 6/10), focus on transitions", interval: "On 2:40", focus: "Build IM volume at aerobic pace" },
        ]},
      { label: "IM Pull + Kick Set", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_4j81oe", reps: 4, dist: 50, desc: "IM order kick — 12.5 each stroke", interval: "On 1:50", focus: "Kick rhythm across IM strokes", eq: "kick" },
          { id: "s_1owimd", reps: 4, dist: 100, desc: "Pull (buoy) — IM stroke focus, alternate strokes", interval: "On 2:20", focus: "Upper-body work across strokes", eq: "pull" },
          { id: "s_fvli7o", reps: 5, dist: 100, desc: "100 IM — moderate, descend 1→5", interval: "On 3:10", focus: "Apply kick + pull into full IM" },
        ]},
      { label: "100 IM Build", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_vb14qc", reps: 4, dist: 100, desc: "IM — moderate, settle into stroke order", interval: "On 3:10", focus: "Smooth transitions" },
          { id: "s_svt818", reps: 4, dist: 100, desc: "IM — strong, descend 1→4 within block", interval: "On 3:10", focus: "Each rep faster" },
          { id: "s_p7iwzc", reps: 1, dist: 200, desc: "IM — race pace test (200)", interval: "On 6:20", focus: "Apply earlier work to a race-distance effort" },
          { id: "s_f29ooj", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Active recovery" },
        ]},
      { label: "Specialty + Free 75s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_li0mhn", reps: 8, dist: 75, desc: "50 specialty stroke (your choice) + 25 free strong", interval: "On 1:50", focus: "Hone your IM weakness with free finish" },
          { id: "s_fbppup", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:10", focus: "Apply specialty work into full IM" },
          { id: "s_ikmd38", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "5×200 IM Descend", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_uz49g9", reps: 5, dist: 200, desc: "200 IM — descend 1→5 (each one faster)", interval: "On 6:20", focus: "Build IM pace progressively" },
          { id: "s_ezn1ju", reps: 2, dist: 50, desc: "Choice non-free — moderate, close-out", interval: "On 1:20", focus: "Top off stroke speed" },
        ]},
      { label: "Race-Pace 50 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_sn6nxg", reps: 12, dist: 50, desc: "50 IM — race pace (12.5 each stroke)", interval: "On 1:35", focus: "Race-pace IM with manageable reps" },
          { id: "s_4g6fps", reps: 4, dist: 100, desc: "Easy freestyle — recovery cycles", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Stroke Block Endurance", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_dn3zph", reps: 1, dist: 100, desc: "Butterfly — moderate (or 25 fly + 75 free for sustainability)", interval: "On 2:40", focus: "Open fly endurance" },
          { id: "s_e8lgj6", reps: 3, dist: 100, desc: "Backstroke — moderate, smooth tempo", interval: "On 2:55", focus: "Backstroke base" },
          { id: "s_kbx0c4", reps: 3, dist: 100, desc: "Breaststroke — moderate, drive with kick", interval: "On 3:25", focus: "Breast tempo" },
          { id: "s_41l3wy", reps: 3, dist: 100, desc: "Freestyle — strong, finish the set", interval: "On 2:20", focus: "Carry IM fitness into free" },
          { id: "s_m24epn", reps: 2, dist: 100, desc: "100 IM — moderate, integrate all four", interval: "On 3:10", focus: "Apply block work into full IM" },
        ]},
      { label: "Even-Effort 200 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_6uocl8", reps: 6, dist: 200, desc: "200 IM — hold the SAME time on all 6 reps", interval: "On 6:20", focus: "Pacing discipline across IM strokes" },
        ]},
      { label: "Reverse Pyramid IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_onq232", reps: 1, dist: 200, desc: "IM — moderate, smooth transitions", interval: "On 6:20", focus: "Establish baseline pace" },
          { id: "s_lnl3ob", reps: 1, dist: 150, desc: "IM — moderate to strong", interval: "On 4:45", focus: "Pick up pace" },
          { id: "s_241jx4", reps: 1, dist: 100, desc: "IM — strong", interval: "On 2:40", focus: "Threshold IM" },
          { id: "s_ns46bi", reps: 1, dist: 75, desc: "IM (drop fly) — fast", interval: "On 2:40", focus: "Three strokes hard" },
          { id: "s_u4m62g", reps: 1, dist: 50, desc: "IM — fast (12.5 each stroke)", interval: "On 1:35", focus: "All four under load" },
          { id: "s_qc7r7d", reps: 1, dist: 25, desc: "Choice non-free — ALL OUT", interval: "On :45", focus: "Final blast" },
          { id: "s_dptbs8", reps: 1, dist: 200, desc: "Easy choice — recovery between blocks", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_4ae4qh", reps: 4, dist: 50, desc: "Choice IM stroke — fast finishing 50s", interval: "On 1:35", focus: "Top off stroke speed" },
        ]},
      { label: "8×100 IM Fast", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_teylcj", reps: 1, dist: 200, desc: "IM — easy, settle in", interval: "On 6:20", focus: "Open IM rhythm" },
          { id: "s_62khzx", reps: 8, dist: 100, desc: "100 IM — fast, descend 1-4 then 5-8", interval: "On 2:40", focus: "Two descending blocks" },
          { id: "s_q1ti24", reps: 1, dist: 100, desc: "Easy backstroke — cool down", interval: "On 2:55", focus: "Active recovery" },
        ]},
      { label: "Negative-Split 200 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_8kezho", reps: 5, dist: 200, desc: "200 IM — negative split (second 100 faster than first)", interval: "On 6:20", focus: "Practice closing speed across IM strokes" },
        ]},
      { label: "Sprint-Finish 100 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_g8zujv", reps: 8, dist: 100, desc: "100 IM — first 75 moderate, last 25 free ALL OUT", interval: "On 3:10", focus: "Sprint into the wall after IM strokes" },
          { id: "s_c6vks3", reps: 1, dist: 200, desc: "Easy backstroke — cool", interval: "On 5:45", focus: "Recover" },
        ]},
      { label: "Build-Then-Sprint 75s", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_ne4pmw", reps: 12, dist: 75, desc: "75 IM (back/breast/free) — 25 build / 25 strong / 25 fast", interval: "On 2:40", focus: "Three-pace progression on each rep" },
          { id: "s_znqxnf", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "300 IM Repeats", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_cfa4x6", reps: 4, dist: 300, desc: "300 IM — moderate, even pace through transitions", interval: "On 8:55", focus: "Mid-distance IM endurance" },
        ]},
      { label: "IM Pull Sandwich", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_8qhdtw", reps: 4, dist: 100, desc: "Pull (buoy) — IM order rotation, alternate strokes by 25", interval: "On 2:20", focus: "Upper-body IM work", eq: "pull" },
          { id: "s_9lrej2", reps: 4, dist: 100, desc: "100 IM — moderate, integrate pull mechanics", interval: "On 3:10", focus: "Apply pull catch into IM" },
          { id: "s_vq97al", reps: 4, dist: 75, desc: "Pull (buoy) — 50 stroke + 25 free", interval: "On 1:45", focus: "Bridge pull pattern across strokes", eq: "pull" },
        ]},
      { label: "IM Kick Endurance", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_0x9gjr", reps: 8, dist: 50, desc: "IM order kick — 12.5 yards each stroke (board optional)", interval: "On 1:35", focus: "Kick rhythm across IM strokes", eq: "kick" },
          { id: "s_febbwu", reps: 4, dist: 100, desc: "100 IM — moderate, drive with the legs", interval: "On 3:10", focus: "Apply kick into full IM" },
          { id: "s_xsxdww", reps: 1, dist: 300, desc: "Easy backstroke — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Broken 200 IM Set", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_n1rcga", reps: 6, dist: 200, desc: "Broken 200 IM — :10 rest after each 50", interval: "On 5:15", focus: "Race-pace IM in 50-yard segments" },
        ]},
      { label: "Stroke-Choice Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_5x1oub", reps: 16, dist: 50, desc: "50 — rotate IM stroke each rep (fly, back, breast, free), ALL OUT", interval: "On 1:35", focus: "Single-stroke speed across all four" },
          { id: "s_wa3fi5", reps: 1, dist: 200, desc: "Easy backstroke — close-out", interval: "On 5:45", focus: "Cool down" },
        ]},
      { label: "Tempo + Threshold IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_o216vc", reps: 6, dist: 100, desc: "100 IM — tempo (RPE 7/10)", interval: "On 3:10", focus: "Sustainable IM tempo" },
          { id: "s_rgvnaz", reps: 4, dist: 100, desc: "100 IM — threshold (RPE 8/10)", interval: "On 3:10", focus: "Push IM threshold" },
        ]},
      { label: "75s Specialty + 200 IM", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_pd396d", reps: 4, dist: 75, desc: "Specialty stroke (your choice) — strong", interval: "On 1:50", focus: "Race-stroke speed work" },
          { id: "s_b648vj", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:20", focus: "Mid-distance IM" },
        ]},
      { label: "IM Sandwich 400s", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_a596au", reps: 3, dist: 400, desc: "400 — 100 IM easy / 200 IM strong / 100 IM ALL OUT", interval: "On 12:35", focus: "Three-tier effort within each 400" },
        ]},
      { label: "200 IM + Free Anchors", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_avu40l", reps: 4, dist: 300, desc: "200 IM moderate + 100 freestyle strong", interval: "On 8:55", focus: "IM into a fast freestyle anchor" },
        ]},
      { label: "Continuous 1000 IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_633xqr", reps: 1, dist: 1000, desc: "Continuous IM — 250 each stroke (fly→back→breast→free) at moderate pace", interval: "On 23:05", focus: "Marathon IM — manage effort across all four" },
        ]},
      { label: "400 IM Race Test", totalYards: 1100, types: ["im"], strokes: [], sets: [
          { id: "s_fca0l4", reps: 1, dist: 400, desc: "400 IM — race-pace simulation, even effort", interval: "On 12:35", focus: "Test 400 IM fitness" },
          { id: "s_fb0vyh", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_5hlwlq", reps: 4, dist: 50, desc: "Choice IM stroke — fast", interval: "On 1:35", focus: "Speed work after race effort" },
          { id: "s_mlo0oi", reps: 1, dist: 300, desc: "Easy backstroke — cool", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Locomotive 100 IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_npzg6f", reps: 10, dist: 100, desc: "100 IM — first 25 easy, increase pace each 25 (locomotive)", interval: "On 3:10", focus: "Build pace within each 100 across IM strokes" },
        ]},
      { label: "Fly-Bookend IMs", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_1drmr7", reps: 8, dist: 100, desc: "25 fly fast + 50 IM (back/breast/free) + 25 fly fast", interval: "On 3:10", focus: "Fly bookends test fly endurance" },
          { id: "s_9hkoas", reps: 1, dist: 200, desc: "Easy backstroke — cool", interval: "On 5:45", focus: "Cool down" },
        ]},
      { label: "Last-50-Fast IM", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_f1wfoe", reps: 8, dist: 100, desc: "100 IM — first 50 moderate, last 50 ALL OUT (free)", interval: "On 3:10", focus: "Closing speed with tired strokes" },
          { id: "s_mx5s38", reps: 4, dist: 50, desc: "Choice IM stroke — fast", interval: "On 1:35", focus: "Top off stroke speed" },
        ]},
      { label: "Threshold 150 IMs", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_n1ay6x", reps: 8, dist: 150, desc: "150 IM (drop fly: back/breast/free) — threshold pace", interval: "On 3:25", focus: "Sustained threshold across three strokes" },
        ]},
      { label: "Drop-Fly 75 Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_zt5a2x", reps: 12, dist: 75, desc: "75 IM (drop fly: back/breast/free) — fast", interval: "On 2:40", focus: "Race-pace 3-stroke IM" },
          { id: "s_jm35p7", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
        ]},
      { label: "RPE-Stepped IM", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_h7nvwt", reps: 4, dist: 200, desc: "200 IM — RPE 6/10 (easy)", interval: "On 6:20", focus: "Aerobic IM base" },
          { id: "s_qlhpwq", reps: 4, dist: 100, desc: "100 IM — RPE 7/10 (moderate)", interval: "On 3:10", focus: "Mid-zone tempo" },
          { id: "s_oafh6e", reps: 4, dist: 50, desc: "50 IM — RPE 9/10 (fast)", interval: "On 1:35", focus: "Sprint zone across IM" },
        ]},
      { label: "IM 200s Descend", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_w9l9ct", reps: 7, dist: 200, desc: "200 IM — descend 1→7 (each rep slightly faster)", interval: "On 6:20", focus: "Progressive IM speed over seven 200s" },
        ]},
      { label: "IM 400s + 200s", totalYards: 1400, types: ["im"], strokes: [], sets: [
          { id: "s_prxumu", reps: 2, dist: 400, desc: "400 IM — strong, moderate rest", interval: "On 10:00", focus: "Full-distance IM fitness" },
          { id: "s_3neaza", reps: 3, dist: 200, desc: "200 IM — race pace, apply 400 conditioning", interval: "On 5:45", focus: "Speed after the long IMs" },
        ]},
      { label: "Reverse-Order 100s", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_vhr3hz", reps: 8, dist: 100, desc: "Reverse IM — free, breast, back, fly (last 25) — strong", interval: "On 3:10", focus: "Stroke order test in reverse" },
          { id: "s_4wc56n", reps: 4, dist: 50, desc: "Freestyle — fast", interval: "On 1:20", focus: "Speed close-out" },
        ]},
      { label: "Power IM Sprints", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_e7ay9l", reps: 8, dist: 50, desc: "50 IM stroke choice — ALL OUT (rotate stroke)", interval: "On 1:35", focus: "Pure stroke speed" },
          { id: "s_zok72b", reps: 4, dist: 100, desc: "100 IM — moderate, integrate the speed", interval: "On 3:10", focus: "Apply sprint work to full IM" },
          { id: "s_ye8b7i", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:45", focus: "Cool down" },
        ]},
      { label: "IM Volume Ladder", totalYards: 1200, types: ["im"], strokes: [], sets: [
          { id: "s_vc1bf8", reps: 1, dist: 400, desc: "400 IM — aerobic effort, smooth transitions", interval: "On 10:00", focus: "Full IM distance — establish your pace" },
          { id: "s_m5cudn", reps: 2, dist: 200, desc: "200 IM — match or build on 400 pace", interval: "On 5:15", focus: "Maintain stroke quality as distance drops" },
          { id: "s_amchkp", reps: 4, dist: 100, desc: "100 IM — strong, best effort of the set", interval: "On 2:40", focus: "Apply full IM rhythm at shorter distance" },
        ]},
      { label: "IM Ascending Ladder", totalYards: 1000, types: ["im"], strokes: [], sets: [
          { id: "s_r062of", reps: 2, dist: 100, desc: "100 IM — moderate pace, smooth transitions", interval: "On 2:40", focus: "Set your baseline — hold this pace" },
          { id: "s_6bw7u6", reps: 2, dist: 200, desc: "200 IM — same effort, longer distance", interval: "On 5:15", focus: "Extend the IM as fatigue builds" },
          { id: "s_qxoc0y", reps: 1, dist: 400, desc: "400 IM — strong, finish with what you have", interval: "On 10:00", focus: "Full IM at the top of the ladder" },
        ]},
      { label: "IM 400s + Speed", totalYards: 1500, types: ["im"], strokes: [], sets: [
          { id: "s_p1h2km", reps: 3, dist: 400, desc: "400 IM — 100 each stroke, moderate pace, smooth transitions", interval: "On 10:00", focus: "IM distance volume" },
          { id: "s_1whtsg", reps: 3, dist: 100, desc: "100 IM — race pace, crisp transitions", interval: "On 2:40", focus: "Speed layer after distance" },
        ]},
      { label: "IM Big Four 400s", totalYards: 2000, types: ["im"], strokes: [], sets: [
          { id: "s_xcnje9", reps: 4, dist: 400, desc: "400 IM — 100 each stroke, moderate pace, maintain form", interval: "On 10:00", focus: "Full IM distance volume" },
          { id: "s_zj654o", reps: 4, dist: 100, desc: "100 IM — race pace, push the transitions", interval: "On 2:40", focus: "Speed finish after distance block" },
        ]},
      { label: "IM Marathon Six 400s", totalYards: 2400, types: ["im"], strokes: [], sets: [
          { id: "s_agf9kb", reps: 6, dist: 400, desc: "400 IM — 100 each stroke, moderate, hold form throughout", interval: "On 10:00", focus: "High-volume IM endurance" },
        ]},
      { label: "IM 100s Repeat Unit", totalYards: 400, types: ["im"], strokes: [], sets: [
          { id: "s_rz6vux", reps: 4, dist: 100, desc: "100 IM — 25 each stroke in order, smooth transitions", interval: "On 2:40", focus: "IM efficiency and stroke transitions — designed to repeat 2–3×" },
        ]},
      { label: "IM Stroke Focus Repeat Unit", totalYards: 500, types: ["im"], strokes: [], sets: [
          { id: "s_lgfx03", reps: 4, dist: 100, desc: "100 IM — 25 each stroke, focus on weakest stroke each round", interval: "On 2:40", focus: "IM stroke balance" },
          { id: "s_2myfxt", reps: 2, dist: 50, desc: "50 IM — race-pace, crisp transitions", interval: "On 1:20", focus: "Speed layer to close the round" },
        ]},
      { label: "IM Broken 200 Repeat Unit", totalYards: 600, types: ["im"], strokes: [], sets: [
          { id: "s_pz0p59", reps: 2, dist: 200, desc: "200 IM — 50 each stroke, moderate pace, maintain form", interval: "On 5:00", focus: "Full IM cycle at controlled effort" },
          { id: "s_gd7h3w", reps: 4, dist: 50, desc: "50 choice stroke — not free, work your weakest", interval: "On 1:20", focus: "Specialty stroke volume each round" },
        ]},
      { label: "Five 400 IMs", totalYards: 2000, types: ["im"], strokes: [], sets: [
          { id: "s_c400r5", reps: 5, dist: 400, desc: "400 IM — 100 of each stroke, moderate effort, hold form across all five", interval: "On 10:00", focus: "IM endurance at race-distance length" },
        ]},
      { label: "IM Big Block 1800", totalYards: 1800, types: ["im"], strokes: [], sets: [
          { id: "s_xrbv6s", reps: 3, dist: 400, desc: "400 IM — moderate, smooth transitions, descend 1→3", interval: "On 10:00", focus: "Long-form IM volume" },
          { id: "s_g31yej", reps: 6, dist: 100, desc: "100 IM — race pace, sharp transitions", interval: "On 2:40", focus: "Race-pace IM work after long-form load" },
        ]},
      { label: "IM Variety 2200", totalYards: 2200, types: ["im"], strokes: [], sets: [
          { id: "s_nxp9mq", reps: 4, dist: 400, desc: "400 IM — moderate, hold form throughout", interval: "On 10:00", focus: "Long-form IM block" },
          { id: "s_f8f8l2", reps: 4, dist: 100, desc: "100 IM — strong, focus on transitions", interval: "On 2:40", focus: "Race-pace IM finishers" },
          { id: "s_cupdil", reps: 2, dist: 100, desc: "Easy choice — recovery", interval: "On 2:40", focus: "Cool down within the main" },
        ]},
      { label: "200s Descend", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_xsceo5", reps: 5, dist: 200, desc: "Freestyle — descend 1→5 (each one faster)", interval: "On 4:30", focus: "Find your pace early, build the whole way through" },
        ]},
      { label: "400s Descend", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_sk7476", reps: 3, dist: 400, desc: "Freestyle — descend 1→3 (each one faster than the last)", interval: "On 8:55", focus: "Negative split within each; fastest on #3" },
        ]},
      { label: "200s + 100s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_98l4fi", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:30", focus: "Hold form as pace increases" },
          { id: "s_wnf9az", reps: 4, dist: 100, desc: "Freestyle — fast (near threshold)", interval: "On 2:15", focus: "Maintain stroke length at speed" },
        ]},
      { label: "Distance Ladder", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_oyio0n", reps: 1, dist: 400, desc: "Freestyle — aerobic/moderate", interval: "On 9:25", focus: "Long steady effort, build pace" },
          { id: "s_8i7aka", reps: 2, dist: 200, desc: "Freestyle — moderate/fast, descend", interval: "On 4:45", focus: "Find race pace, each one faster" },
          { id: "s_uv18yg", reps: 3, dist: 100, desc: "Freestyle — fast, under threshold", interval: "On 2:20", focus: "Sustain speed under accumulated load" },
        ]},
      { label: "Broken 500s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_lrt5ih", reps: 2, dist: 500, desc: "Broken 500 free — 5×100 with :15 rest, hold target pace", interval: "On 11:35", focus: "Race-pace simulation across long distance" },
          { id: "s_tc5wk8", reps: 2, dist: 100, desc: "Easy freestyle — active recovery between blocks", interval: "No interval — swim easy", focus: "Clear lactate, get ready for next block" },
        ]},
      { label: "Pull + Swim Threshold", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_g9a6jt", reps: 4, dist: 200, desc: "Pull (buoy) — strong, hold technique under load", interval: "On 4:30", focus: "Upper-body endurance at threshold pace", eq: "pull" },
          { id: "s_tcr9zw", reps: 4, dist: 100, desc: "Freestyle swim — match the pull pace", interval: "On 2:15", focus: "Translate pull strength into full stroke" },
        ]},
      { label: "Long Aerobic Build", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_61v6uy", reps: 1, dist: 800, desc: "Freestyle — negative split (second 400 faster than first)", interval: "On 17:50", focus: "Pace control over distance, build gradually" },
          { id: "s_hesr3y", reps: 4, dist: 100, desc: "Freestyle — strong, finish the workout", interval: "On 2:20", focus: "Hold form despite fatigue" },
        ]},
      { label: "1000 Continuous", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_qyszbi", reps: 1, dist: 1000, desc: "Freestyle — steady aerobic, negative split (second 500 faster)", interval: "On 23:05", focus: "Build and sustain pace over distance" },
        ]},
      { label: "Distance Pyramid 300 Apex", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_p84bgp", reps: 1, dist: 50, desc: "Freestyle — easy build", interval: "On 1:20", focus: "Open up" },
          { id: "s_7chs9t", reps: 1, dist: 100, desc: "Freestyle — moderate", interval: "On 2:20", focus: "Find your pace" },
          { id: "s_3973mb", reps: 1, dist: 200, desc: "Freestyle — moderate to strong", interval: "On 4:30", focus: "Hold pace longer" },
          { id: "s_2mtioz", reps: 1, dist: 300, desc: "Freestyle — strong", interval: "On 6:20", focus: "Apex of pyramid" },
          { id: "s_yxnfo0", reps: 1, dist: 200, desc: "Freestyle — strong (match earlier pace)", interval: "On 4:30", focus: "Same pace under fatigue" },
          { id: "s_h4imp1", reps: 1, dist: 100, desc: "Freestyle — fast, near threshold", interval: "On 2:15", focus: "Pick up pace" },
          { id: "s_vhm5ep", reps: 1, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:05", focus: "Top speed close" },
          { id: "s_swhhzb", reps: 2, dist: 100, desc: "Easy choice — active recovery", interval: "No interval — swim easy", focus: "Recover gracefully" },
        ]},
      { label: "800 + Closing 100s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_of3k0g", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, build last 200", interval: "On 17:50", focus: "Long swim with progression" },
          { id: "s_swuy72", reps: 4, dist: 100, desc: "Freestyle — fast (faster than 800 pace)", interval: "On 2:20", focus: "Pick up pace under fatigue" },
        ]},
      { label: "Distance Pull Endurance", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_qlrb9c", reps: 1, dist: 400, desc: "Pull (buoy) — steady aerobic effort", interval: "On 8:55", focus: "Long pull at endurance pace", eq: "pull" },
          { id: "s_p2sxe8", reps: 4, dist: 100, desc: "Pull (buoy) — descending pace 1→4", interval: "On 2:20", focus: "Build pull intensity", eq: "pull" },
          { id: "s_jyh7o6", reps: 4, dist: 100, desc: "Freestyle swim — apply the pull pattern", interval: "On 2:15", focus: "Translate pull mechanics to swim" },
        ]},
      { label: "Tempo 300s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_u1olgs", reps: 4, dist: 300, desc: "Freestyle — tempo pace (RPE 7/10), hold consistent splits", interval: "On 6:50", focus: "Sustained tempo work for distance fitness" },
        ]},
      { label: "Mile Race Simulation", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_dw8rfb", reps: 1, dist: 1500, desc: "Freestyle — race pace simulation, even splits", interval: "On 34:40", focus: "Race-distance fitness check" },
        ]},
      { label: "Mixed-Stroke Distance", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_wrspre", reps: 5, dist: 200, desc: "200 — first 150 free, last 50 backstroke", interval: "On 4:45", focus: "Distance with stroke variety, save the shoulders" },
          { id: "s_ufmtxj", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Distance Sprint Mix", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_0wcsja", reps: 2, dist: 400, desc: "Freestyle — moderate aerobic effort", interval: "On 8:55", focus: "Distance base" },
          { id: "s_yk7yxa", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:15", focus: "Distance + pace work" },
        ]},
      { label: "Descending Ladder", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_ut13xc", reps: 1, dist: 400, desc: "Freestyle — aerobic effort, establish your pace", interval: "On 8:55", focus: "Anchor the ladder — this is your pace ceiling" },
          { id: "s_8idsqj", reps: 1, dist: 300, desc: "Freestyle — match or beat 400 pace", interval: "On 6:50", focus: "Hold as the distance drops" },
          { id: "s_y6fq8i", reps: 1, dist: 200, desc: "Freestyle — strong, find another gear", interval: "On 4:30", focus: "Negative split into the descent" },
          { id: "s_mk6ohf", reps: 1, dist: 100, desc: "Freestyle — finish at best pace of the set", interval: "On 2:15", focus: "Empty the tank" },
        ]},
      { label: "Ascending Ladder", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_p34xyc", reps: 1, dist: 100, desc: "Freestyle — controlled, hold back", interval: "On 2:15", focus: "Reserve — the long stuff is coming" },
          { id: "s_ovoxoa", reps: 1, dist: 200, desc: "Freestyle — moderate, find your rhythm", interval: "On 4:30", focus: "Build on the 100 pace" },
          { id: "s_qj3yaw", reps: 1, dist: 300, desc: "Freestyle — moderate to strong, hold form", interval: "On 6:50", focus: "Aerobic ceiling approaching" },
          { id: "s_qbh9jt", reps: 1, dist: 400, desc: "Freestyle — strong, best sustained effort", interval: "On 8:55", focus: "Apex of the ladder — give everything left" },
        ]},
      { label: "Distance Pyramid", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_8wu7q6", reps: 1, dist: 100, desc: "Freestyle — easy/moderate", interval: "On 2:15", focus: "Open up smoothly" },
          { id: "s_7dcmx4", reps: 1, dist: 200, desc: "Freestyle — moderate, build from the 100", interval: "On 4:30", focus: "Climb toward the peak" },
          { id: "s_amx0us", reps: 1, dist: 400, desc: "Freestyle — strong, sustained effort", interval: "On 8:55", focus: "Apex — test your aerobic ceiling" },
          { id: "s_ap2mjo", reps: 1, dist: 200, desc: "Freestyle — match peak pace if possible", interval: "On 4:30", focus: "Hold the line on the way back down" },
          { id: "s_ikmqqy", reps: 1, dist: 100, desc: "Freestyle — near-max effort to close", interval: "On 2:15", focus: "Finish faster than you started" },
        ]},
      { label: "300s Descend", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_xaqaud", reps: 4, dist: 300, desc: "Freestyle — descend 1→4 (each 300 faster)", interval: "On 6:50", focus: "Mid-distance descend, build pace" },
        ]},
      { label: "600 + 4×100", totalYards: 1000, types: ["distance"], strokes: [], sets: [
          { id: "s_43c4do", reps: 1, dist: 600, desc: "Freestyle — steady aerobic base", interval: "On 14:10", focus: "Long aerobic effort" },
          { id: "s_4r9qy3", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:15", focus: "Pick up the pace at the end" },
        ]},
      { label: "Mile Splits", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_kt2pah", reps: 3, dist: 500, desc: "Freestyle — race-distance splits, hold same pace each 500", interval: "On 11:35", focus: "Mile fitness in 500-yard segments" },
        ]},
      { label: "200 Step-Down", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_uovpc6", reps: 6, dist: 200, desc: "Freestyle — step pace down by :05 every 200", interval: "On 4:45", focus: "Progressive pace control" },
        ]},
      { label: "Distance Pull Volume", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_vnmoiw", reps: 1, dist: 800, desc: "Pull (buoy) — long aerobic, smooth long stroke", interval: "On 17:50", focus: "Long pull base", eq: "pull" },
          { id: "s_6ypyjr", reps: 5, dist: 100, desc: "Pull (buoy) — moderate to strong, descend 1→5", interval: "On 2:20", focus: "Pull intensity build", eq: "pull" },
        ]},
      { label: "Broken 1000", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_c1nsri", reps: 1, dist: 1000, desc: "Broken 1000 — :10 rest after every 100 (race-pace simulation)", interval: "On 23:05", focus: "Race-pace mile prep in 100-yard segments" },
          { id: "s_mdtpjq", reps: 4, dist: 50, desc: "Easy freestyle — between blocks", interval: "On 1:20", focus: "Active recovery" },
        ]},
      { label: "Aerobic 250s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_fohd2x", reps: 5, dist: 250, desc: "Freestyle — aerobic pace (RPE 6-7/10), even splits", interval: "On 5:00", focus: "Mid-distance aerobic volume" },
        ]},
      { label: "Negative-Split 250s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_8wxdvr", reps: 5, dist: 250, desc: "Freestyle — negative split each rep (first 125 moderate, last 125 strong)", interval: "On 4:45", focus: "Build within each 250" },
        ]},
      { label: "750 + 100s", totalYards: 1250, types: ["distance"], strokes: [], sets: [
          { id: "s_wgycza", reps: 1, dist: 750, desc: "Freestyle — steady aerobic (RPE 6-7)", interval: "On 16:50", focus: "Aerobic base building" },
          { id: "s_3ya0bp", reps: 5, dist: 100, desc: "Freestyle — hold or slightly better than 750 pace", interval: "On 2:20", focus: "Volume extension after long swim" },
        ]},
      { label: "Distance + Sprint Sandwich", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_71gl1g", reps: 1, dist: 600, desc: "Freestyle — moderate aerobic effort", interval: "On 14:10", focus: "Aerobic base" },
          { id: "s_jav6yd", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:35", focus: "Speed after distance load" },
          { id: "s_i3w1rm", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:45", focus: "Cool down" },
        ]},
      { label: "Negative-Split 400s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_fsb199", reps: 3, dist: 400, desc: "Freestyle — negative split (second 200 faster than first)", interval: "On 8:55", focus: "Practice closing pace within each 400" },
        ]},
      { label: "1500 Continuous", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_l1rwg8", reps: 1, dist: 1500, desc: "Freestyle — continuous, even-effort race-distance swim", interval: "On 34:40", focus: "Mile pace control" },
        ]},
      { label: "Race-Pace Mile Prep", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_amouj1", reps: 6, dist: 100, desc: "Freestyle — at goal mile pace", interval: "On 2:15", focus: "Find your sustainable mile pace" },
          { id: "s_g37z3g", reps: 1, dist: 300, desc: "Easy backstroke — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_wsxuuw", reps: 3, dist: 100, desc: "Freestyle — slightly faster than goal pace", interval: "On 2:15", focus: "Practice closing-stage push" },
        ]},
      { label: "200 Pace Pyramid", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_kj8dk4", reps: 1, dist: 200, desc: "Freestyle — easy aerobic", interval: "On 4:10", focus: "Open up" },
          { id: "s_dgtklr", reps: 1, dist: 200, desc: "Freestyle — moderate", interval: "On 4:45", focus: "Find tempo" },
          { id: "s_beh3pl", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:30", focus: "Threshold" },
          { id: "s_66bq1j", reps: 1, dist: 200, desc: "Freestyle — fast", interval: "On 4:30", focus: "Above threshold" },
          { id: "s_ci00ud", reps: 1, dist: 200, desc: "Freestyle — strong (match 3rd)", interval: "On 4:30", focus: "Same pace under fatigue" },
          { id: "s_aw4wis", reps: 1, dist: 200, desc: "Freestyle — moderate (cool)", interval: "On 4:45", focus: "Active recovery" },
        ]},
      { label: "1650 Race Day Sim", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_s1amwv", reps: 1, dist: 100, desc: "Freestyle — easy, settle in", interval: "On 2:05", focus: "Race-day warm-up feel" },
          { id: "s_6j9bzm", reps: 1, dist: 1000, desc: "Freestyle — race-pace simulation", interval: "On 23:05", focus: "First two-thirds of a 1650" },
          { id: "s_m0s3wx", reps: 1, dist: 200, desc: "Easy choice — active recovery", interval: "No interval — swim easy", focus: "Practice in-race fade management" },
        ]},
      { label: "1000 Pull Endurance", totalYards: 1300, types: ["distance"], strokes: [], sets: [
          { id: "s_ff61hp", reps: 1, dist: 1000, desc: "Pull (buoy) — continuous, smooth long stroke", interval: "On 22:05", focus: "Long-pull aerobic base", eq: "pull" },
          { id: "s_u2ahlv", reps: 3, dist: 100, desc: "Freestyle — apply pull mechanics", interval: "On 2:20", focus: "Translate to swim" },
        ]},
      { label: "Long Aerobic 500", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_qp9fnx", reps: 2, dist: 500, desc: "Freestyle — steady aerobic, even effort", interval: "On 11:35", focus: "Aerobic base in mid-distance reps" },
          { id: "s_qucy6k", reps: 1, dist: 100, desc: "Easy backstroke — close", interval: "On 2:55", focus: "Cool down" },
        ]},
      { label: "Fast Finish 200s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_dawk2z", reps: 6, dist: 200, desc: "Freestyle — moderate first 100, fast second 100", interval: "On 4:45", focus: "Practice closing-half acceleration" },
        ]},
      { label: "300/200/100 Triplets", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_65meo0", reps: 2, dist: 300, desc: "Freestyle — aerobic", interval: "On 6:50", focus: "Distance base" },
          { id: "s_dms3xu", reps: 2, dist: 200, desc: "Freestyle — moderate to fast, descend", interval: "On 4:45", focus: "Build pace" },
          { id: "s_5dypcv", reps: 2, dist: 100, desc: "Freestyle — fast, near threshold", interval: "On 2:20", focus: "Threshold sustain" },
        ]},
      { label: "Mid-Distance Threshold", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_uuqag0", reps: 4, dist: 250, desc: "Freestyle — threshold pace (RPE 7-8/10)", interval: "On 4:45", focus: "Sustained threshold mid-distance" },
          { id: "s_p2k9kh", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tempo Pull 200s", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_nxqdw3", reps: 4, dist: 200, desc: "Pull (buoy) — tempo pace, smooth long stroke", interval: "On 4:30", focus: "Pull endurance at tempo", eq: "pull" },
          { id: "s_6fg747", reps: 4, dist: 100, desc: "Freestyle — apply the pull tempo to full stroke", interval: "On 2:20", focus: "Carry pull pattern into swim" },
        ]},
      { label: "Overdistance 1200", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_59iyvx", reps: 1, dist: 1200, desc: "Freestyle — continuous overdistance, build last 400", interval: "On 27:20", focus: "Beyond mile-distance fitness" },
        ]},
      { label: "Distance Kick + Free", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_6xogo9", reps: 1, dist: 300, desc: "Kick — moderate (board optional)", interval: "On 7:20", focus: "Long kick base", eq: "kick" },
          { id: "s_jejk3r", reps: 4, dist: 200, desc: "Freestyle — moderate, drive with the legs", interval: "On 4:45", focus: "Apply kick conditioning" },
        ]},
      { label: "200 + 100 Threshold", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_ew8e69", reps: 4, dist: 200, desc: "Freestyle — threshold pace", interval: "On 4:30", focus: "Threshold base" },
          { id: "s_i94aqz", reps: 4, dist: 100, desc: "Freestyle — threshold pace", interval: "On 2:15", focus: "Same pace, shorter rep" },
        ]},
      { label: "Triple 400 Endurance", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_hqb5mc", reps: 3, dist: 400, desc: "Freestyle — moderate aerobic, even splits", interval: "On 9:25", focus: "Aerobic base in 400-yard reps" },
        ]},
      { label: "Stroke Sprinkle Distance", totalYards: 1100, types: ["distance"], strokes: [], sets: [
          { id: "s_0dm8lm", reps: 4, dist: 250, desc: "First 200 freestyle steady + last 50 backstroke", interval: "On 5:00", focus: "Long swim with stroke breaks for shoulders" },
          { id: "s_117h6n", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Mile + Sprint Finish", totalYards: 1200, types: ["distance"], strokes: [], sets: [
          { id: "s_wr7ed4", reps: 1, dist: 1000, desc: "Freestyle — race-pace mile simulation", interval: "On 23:05", focus: "Race-pace fitness" },
          { id: "s_cb4400", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT after distance", interval: "On 1:35", focus: "Sprint after extended effort" },
        ]},
      { label: "Triple 500 Distance", totalYards: 1500, types: ["distance"], strokes: [], sets: [
          { id: "s_odz6pi", reps: 3, dist: 500, desc: "Freestyle — aerobic to moderate pace, hold even splits", interval: "On 9:25", focus: "Mid-distance endurance blocks" },
        ]},
      { label: "1000 + 5×200 Distance", totalYards: 2000, types: ["distance"], strokes: [], sets: [
          { id: "s_zgymmy", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, focus on even effort", interval: "On 22:05", focus: "Overdistance base" },
          { id: "s_93gtyg", reps: 5, dist: 200, desc: "Freestyle — moderate to strong, descend 1→5", interval: "On 4:10", focus: "Build pace after the long swim" },
        ]},
      { label: "Four 600s Distance", totalYards: 2400, types: ["distance"], strokes: [], sets: [
          { id: "s_399vwi", reps: 4, dist: 600, desc: "Freestyle — aerobic pace, even splits, hold form to the end", interval: "On 11:35", focus: "High-volume distance endurance" },
        ]},
      { label: "Distance 200s Repeat Unit", totalYards: 500, types: ["distance"], strokes: [], sets: [
          { id: "s_c9byfu", reps: 2, dist: 200, desc: "Freestyle — aerobic pace, negative split (second 100 faster)", interval: "On 4:30", focus: "Pacing discipline each round" },
          { id: "s_hwwcpa", reps: 1, dist: 100, desc: "Freestyle — moderate, hold form from the 200s", interval: "On 2:10", focus: "Extend endurance without rest" },
        ]},
      { label: "Distance Build Repeat Unit", totalYards: 600, types: ["distance"], strokes: [], sets: [
          { id: "s_mfikpt", reps: 3, dist: 200, desc: "Freestyle — descend 1→3, each faster than the last", interval: "On 4:30", focus: "Progressive effort — push the last 200 each round" },
        ]},
      { label: "1800 Continuous Distance", totalYards: 1800, types: ["distance"], strokes: [], sets: [
          { id: "s_vjcyfe", reps: 1, dist: 1800, desc: "Freestyle — single continuous distance swim, race-distance effort, even splits", interval: "On 41:00", focus: "Sustained distance race-pace pressure" },
        ]},
      { label: "Distance Pyramid 2000", totalYards: 2000, types: ["distance"], strokes: [], sets: [
          { id: "s_c1i8r8", reps: 1, dist: 200, desc: "Freestyle — moderate, even effort", interval: "On 4:15", focus: "Open the pyramid at conversational pace" },
          { id: "s_e60jmf", reps: 1, dist: 400, desc: "Freestyle — moderate, hold same pace as the 200", interval: "On 8:30", focus: "Hold pace across the longer rep" },
          { id: "s_wqvpku", reps: 1, dist: 800, desc: "Freestyle — moderate-strong, descend the second half", interval: "On 17:00", focus: "Main piece — distance-build effort" },
          { id: "s_fn6xc6", reps: 1, dist: 400, desc: "Freestyle — strong, faster than the opening 400", interval: "On 8:30", focus: "Closing pressure" },
          { id: "s_4c8r4s", reps: 1, dist: 200, desc: "Freestyle — strong finisher, fastest of the set", interval: "On 4:15", focus: "Drop the hammer to close" },
        ]},
      { label: "Mile + 600 + 300", totalYards: 2400, types: ["distance"], strokes: [], sets: [
          { id: "s_3619qm", reps: 1, dist: 1500, desc: "Freestyle — race-pace mile, even splits", interval: "On 35:00", focus: "Race-distance simulation" },
          { id: "s_ntr39o", reps: 1, dist: 600, desc: "Freestyle — moderate-strong, hold the mile pace", interval: "On 12:45", focus: "Continue under fatigue" },
          { id: "s_fbocz8", reps: 1, dist: 300, desc: "Freestyle — strong close, faster than mile pace", interval: "On 6:20", focus: "Closing effort under accumulated fatigue" },
        ]},
      { label: "Sprint Ladder", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_yb1o71", reps: 4, dist: 100, desc: "Freestyle — 75 easy + last 25 ALL OUT", interval: "On 2:40", focus: "Save energy, unleash on last length" },
          { id: "s_n7p91w", reps: 8, dist: 50, desc: "Choice of stroke — 25 easy + 25 ALL OUT", interval: "On 1:50", focus: "Maximum speed on second 25" },
          { id: "s_cujv9i", reps: 8, dist: 25, desc: "Freestyle — ALL OUT sprint", interval: "On 1:05", focus: "Top-end speed — best times of the workout" },
        ]},
      { label: "Pure Speed", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_eyt4g4", reps: 10, dist: 50, desc: "Choice of stroke — ALL OUT every rep", interval: "On 2:05", focus: "Full effort every rep — if it isn't hard, go harder" },
          { id: "s_3hke75", reps: 4, dist: 100, desc: "Freestyle — easy/moderate (active recovery)", interval: "No interval — swim easy", focus: "Keep moving, clear the lactic acid" },
          { id: "s_modmdj", reps: 6, dist: 25, desc: "Choice of stroke — sprint to the wall", interval: "On 1:00", focus: "Hold top-end speed even as fatigue builds" },
        ]},
      { label: "Race Pace Blocks", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_o3e02x", reps: 6, dist: 50, desc: "Choice — build to ALL OUT by final 25", interval: "On 2:05", focus: "Race simulation — explosive finish" },
          { id: "s_3ohp40", reps: 1, dist: 200, desc: "Easy recovery freestyle", interval: "4:00 rest before next", focus: "Full recovery" },
          { id: "s_16c6we", reps: 8, dist: 50, desc: "Choice — ALL OUT (full recovery between)", interval: "On 2:40", focus: "Max velocity — no holding back" },
          { id: "s_x40536", reps: 4, dist: 25, desc: "Choice — absolute sprint to finish", interval: "On 1:05", focus: "End on pure speed" },
        ]},
      { label: "Sprint Pyramid Up & Down", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_n3dnto", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Wake up top-end speed" },
          { id: "s_xsvrps", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Build the ladder" },
          { id: "s_t5h3m3", reps: 1, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:20", focus: "Hold longer" },
          { id: "s_795unk", reps: 1, dist: 100, desc: "Choice stroke — ALL OUT", interval: "On 3:10", focus: "Peak of pyramid" },
          { id: "s_lghahy", reps: 1, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:20", focus: "Speed under fatigue" },
          { id: "s_3pcytr", reps: 1, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Find top speed again" },
          { id: "s_ogs9nt", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT (best 25 of the workout)", interval: "On :45", focus: "Final blast" },
          { id: "s_f4gozf", reps: 1, dist: 200, desc: "Easy choice — full recovery before second block", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_4oz0yz", reps: 4, dist: 100, desc: "Freestyle — 75 easy + last 25 ALL OUT", interval: "On 2:05", focus: "Finish with controlled speed work" },
        ]},
      { label: "Anaerobic Capacity", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_q1phrl", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT, short rest (test the system)", interval: "On 1:20", focus: "Max effort with limited recovery — no holding back" },
          { id: "s_0mh8ob", reps: 4, dist: 100, desc: "Easy freestyle — between sprint blocks", interval: "No interval — swim easy", focus: "Clear lactate, regroup" },
          { id: "s_vnl625", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT, longer rest", interval: "On 2:05", focus: "Pure speed after fatigue accumulated" },
        ]},
      { label: "Sprint Pull Power", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_9skp3o", reps: 6, dist: 50, desc: "Pull (buoy) — fast, drive arm speed", interval: "On 1:20", focus: "Upper-body sprint mechanics, max stroke rate", eq: "pull" },
          { id: "s_2k8r5k", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset between blocks" },
          { id: "s_03swx2", reps: 8, dist: 25, desc: "Freestyle — ALL OUT, max turnover", interval: "On :45", focus: "Apply pull power into full sprint" },
          { id: "s_spq7ku", reps: 6, dist: 50, desc: "Choice — fast, descend 1→6", interval: "On 1:35", focus: "Each one faster, finish strongest" },
        ]},
      { label: "25s Lactic Tolerance", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_hsbj88", reps: 16, dist: 25, desc: "Freestyle — ALL OUT, every 25 (very tight rest)", interval: "On :40", focus: "Lactate tolerance with limited recovery" },
          { id: "s_2ak0a4", reps: 1, dist: 200, desc: "Easy choice — full recovery", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_rgo2ag", reps: 8, dist: 25, desc: "Choice stroke — sprint with longer rest", interval: "On 1:05", focus: "Top speed under accumulated fatigue" },
          { id: "s_c3tfgk", reps: 1, dist: 200, desc: "Easy freestyle — cool to next phase", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Power 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_y41zbo", reps: 12, dist: 50, desc: "Choice stroke — ALL OUT, full recovery between", interval: "On 2:05", focus: "Pure power 50s, max effort each rep" },
          { id: "s_h7c8n7", reps: 4, dist: 100, desc: "Easy freestyle — between sets", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Block Sprints", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_9d733w", reps: 4, dist: 100, desc: "Block 1: 100s ALL OUT, descending 1→4", interval: "On 2:40", focus: "Sprint endurance" },
          { id: "s_cytced", reps: 6, dist: 50, desc: "Block 2: 50s ALL OUT, descending 1→6", interval: "On 1:35", focus: "Sprint speed" },
          { id: "s_bahr1h", reps: 1, dist: 200, desc: "Easy backstroke — full recovery", interval: "On 5:45", focus: "Active recovery" },
          { id: "s_775sw1", reps: 4, dist: 25, desc: "Block 3: 25s ALL OUT — peak velocity", interval: "On 1:05", focus: "Top-end speed" },
        ]},
      { label: "Sprint Kick Set", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_2ukhif", reps: 8, dist: 50, desc: "Kick — ALL OUT (board optional)", interval: "On 1:35", focus: "Sprint kick power", eq: "kick" },
          { id: "s_mvppxv", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_c02lvh", reps: 4, dist: 100, desc: "Freestyle — ALL OUT, drive with the legs", interval: "On 2:40", focus: "Apply sprint kick to full sprint" },
        ]},
      { label: "Stroke-by-Stroke Sprint", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_3qanh3", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:35", focus: "Fly speed" },
          { id: "s_og6uw1", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:35", focus: "Back speed" },
          { id: "s_rorqv8", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:35", focus: "Breast speed" },
          { id: "s_0q3l43", reps: 4, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:35", focus: "Free speed" },
          { id: "s_tkzb4i", reps: 1, dist: 200, desc: "Easy choice — final cool", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "75s Race Pace", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_4y69rw", reps: 10, dist: 75, desc: "Choice non-free stroke — race pace (2nd half stronger)", interval: "On 2:05", focus: "Race-distance pacing for non-free events" },
          { id: "s_mzuaee", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Aerobic reset" },
          { id: "s_ao7an2", reps: 1, dist: 100, desc: "Choice — easy/moderate finishing swim", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Negative-Split 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_8lde1f", reps: 16, dist: 50, desc: "First 25 build, second 25 ALL OUT", interval: "On 1:35", focus: "Practice negative-split sprint mechanics" },
          { id: "s_j85bcc", reps: 1, dist: 200, desc: "Easy choice — cool-down", interval: "No interval — swim easy", focus: "Recover" },
        ]},
      { label: "Sprint Pyramid 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_sx8h14", reps: 1, dist: 25, desc: "Choice stroke — moderate (warm-up sprint)", interval: "On :45", focus: "Wake up" },
          { id: "s_86y4np", reps: 2, dist: 25, desc: "Choice stroke — fast", interval: "On :45", focus: "Build speed" },
          { id: "s_epqwim", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_rlzhiv", reps: 8, dist: 25, desc: "Choice stroke — ALL OUT, no holding back", interval: "On :45", focus: "Maximum effort, every rep" },
          { id: "s_wkitjb", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Maintain speed under fatigue" },
          { id: "s_12lls8", reps: 2, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On :45", focus: "Hold the line" },
          { id: "s_3aobft", reps: 1, dist: 25, desc: "Choice stroke — ALL OUT (best 25 of the day)", interval: "On :45", focus: "Final blast" },
          { id: "s_fdag37", reps: 1, dist: 450, desc: "Easy choice — cool-down distance", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Speed Endurance 12×50", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_7v2ytc", reps: 12, dist: 50, desc: "Choice stroke — fast (race pace), hold all 12", interval: "On 1:05", focus: "Sprint endurance — same fast pace under fatigue" },
          { id: "s_dwpxo6", reps: 4, dist: 100, desc: "Easy freestyle — cool-down", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Sprint Pull Set", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_aws7qs", reps: 8, dist: 50, desc: "Pull (buoy) — sprint, max stroke rate", interval: "On 1:35", focus: "Upper-body sprint power", eq: "pull" },
          { id: "s_8vf2sw", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_wir3d6", reps: 4, dist: 100, desc: "Freestyle — fast, apply pull strength", interval: "On 2:20", focus: "Carry pull power into full stroke" },
        ]},
      { label: "Sprint Ladder", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_mxv5p8", reps: 4, dist: 100, desc: "Freestyle — 75 easy build, last 25 ALL OUT", interval: "On 2:40", focus: "Controlled approach, explode at the wall" },
          { id: "s_f5fnij", reps: 4, dist: 75, desc: "Choice stroke — 50 build, last 25 ALL OUT", interval: "On 2:05", focus: "Distance drops, effort rises" },
          { id: "s_z0u4tb", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT every rep", interval: "On 1:35", focus: "Pure speed, full recovery" },
          { id: "s_ve6fdz", reps: 4, dist: 25, desc: "Choice stroke — absolute top-speed sprint", interval: "On 1:05", focus: "Best 25s of the workout" },
        ]},
      { label: "Sprint Pyramid", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_o8saki", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On 1:00", focus: "Open the pyramid fast" },
          { id: "s_sfsrcl", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Longer, still maximum effort" },
          { id: "s_ag4v31", reps: 4, dist: 75, desc: "Choice stroke — ALL OUT", interval: "On 2:05", focus: "Peak of the pyramid" },
          { id: "s_jpl739", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Speed under fatigue on the way back" },
          { id: "s_q7yjwx", reps: 4, dist: 25, desc: "Choice stroke — final blast, max speed", interval: "On 1:00", focus: "Close the pyramid — best 25s of the set" },
          { id: "s_dzewuq", reps: 1, dist: 100, desc: "Easy choice — full recovery", interval: "On 2:40", focus: "Active recovery" },
        ]},
      { label: "8×100 Race Pace", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_g1l776", reps: 8, dist: 100, desc: "Choice stroke — race pace with full recovery", interval: "On 3:10", focus: "Race-pace 100s, true effort each rep" },
          { id: "s_2skuau", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Sprint 25s + 75s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_fk0bu4", reps: 12, dist: 25, desc: "Choice stroke — ALL OUT 25s", interval: "On 1:05", focus: "Top-end speed" },
          { id: "s_4tlvj0", reps: 4, dist: 75, desc: "Choice — first 25 easy, last 50 fast", interval: "On 2:05", focus: "Negative-split sprint" },
          { id: "s_i5qhpk", reps: 4, dist: 100, desc: "Easy choice — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "USRPT 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_31rk8n", reps: 20, dist: 50, desc: "Freestyle — race pace, drop out if you miss the time", interval: "On 1:05", focus: "USRPT-style race-pace volume" },
        ]},
      { label: "Stroke Rotation Sprints", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_3py6ta", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 2:05", focus: "Fly speed" },
          { id: "s_euhx2t", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:50", focus: "Back speed" },
          { id: "s_f58u2g", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:50", focus: "Breast speed" },
          { id: "s_2vmkb6", reps: 4, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:35", focus: "Free speed" },
          { id: "s_g1urs3", reps: 1, dist: 300, desc: "Easy backstroke — close", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "16×25 Sprint Volume", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_u60bof", reps: 16, dist: 25, desc: "Choice stroke — ALL OUT (full recovery)", interval: "On 1:05", focus: "Pure top-end speed" },
          { id: "s_c7jq9b", reps: 4, dist: 100, desc: "Easy freestyle — close-out", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_7ckju6", reps: 4, dist: 50, desc: "Choice — fast finishing 50s", interval: "On 1:35", focus: "Speed under fatigue" },
        ]},
      { label: "100s ALL OUT", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_3f8mbl", reps: 6, dist: 100, desc: "Choice stroke — ALL OUT (full recovery between)", interval: "On 4:10", focus: "Test 100 race-pace fitness" },
          { id: "s_8ch7po", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:45", focus: "Reset" },
          { id: "s_8k9of8", reps: 4, dist: 50, desc: "Choice — sprint finishing 50s", interval: "On 1:35", focus: "Top-off speed" },
        ]},
      { label: "Sprint Pull Mechanics", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_jtkb1h", reps: 6, dist: 50, desc: "Pull (buoy) — fast, high stroke rate", interval: "On 1:35", focus: "Pull power, max turnover", eq: "pull" },
          { id: "s_kttezd", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_6jmvm4", reps: 8, dist: 50, desc: "Freestyle — ALL OUT, apply pull mechanics", interval: "On 1:35", focus: "Translate pull power to sprint" },
          { id: "s_f3jrrr", reps: 4, dist: 25, desc: "Choice — top-end speed", interval: "On 1:05", focus: "Final speed work" },
        ]},
      { label: "Lactate Tolerance 100s", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_6axx56", reps: 6, dist: 100, desc: "Choice stroke — ALL OUT, short rest", interval: "On 2:05", focus: "Lactate tolerance in race-distance reps" },
          { id: "s_hxmz6r", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_pzmv24", reps: 6, dist: 50, desc: "Choice stroke — fast, longer rest", interval: "On 2:05", focus: "Top speed after lactate" },
        ]},
      { label: "Build-Then-Sprint 75s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_w1271n", reps: 14, dist: 75, desc: "Choice stroke — 25 build / 25 strong / 25 ALL OUT", interval: "On 1:50", focus: "Three-pace 75s, race-pace simulation" },
        ]},
      { label: "Race-Pace Sandwich", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_vq0sk4", reps: 4, dist: 100, desc: "Choice stroke — race pace", interval: "On 3:10", focus: "First race-pace block" },
          { id: "s_ejm202", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_sm7dna", reps: 4, dist: 100, desc: "Choice stroke — race pace, hold the same time", interval: "On 3:10", focus: "Second race-pace block under fatigue" },
        ]},
      { label: "16×50 Descend by 4", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_8ab765", reps: 16, dist: 50, desc: "Choice stroke — descend by 4 (1-4 fast, 5-8 faster, 9-12 fastest, 13-16 ALL OUT)", interval: "On 1:20", focus: "Progressive sprint endurance" },
          { id: "s_c2p8pw", reps: 4, dist: 50, desc: "Easy choice — between blocks", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Sprint Kick Power", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_vnenyo", reps: 8, dist: 50, desc: "Kick — ALL OUT, focus on power off the wall (board optional)", interval: "On 1:50", focus: "Kick speed", eq: "kick" },
          { id: "s_fba7xh", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Apply kick power to swim sprint" },
          { id: "s_qwyb1g", reps: 4, dist: 50, desc: "Easy choice — close", interval: "On 1:35", focus: "Recovery" },
        ]},
      { label: "200 Sprint Test", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_69uday", reps: 1, dist: 200, desc: "Choice stroke — ALL OUT (test your 200)", interval: "On 6:20", focus: "200 race-pace test" },
          { id: "s_r6t6v3", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_8xz4wb", reps: 4, dist: 100, desc: "Choice — strong, descend 1→4", interval: "On 2:40", focus: "Build pace post-test" },
          { id: "s_79sni1", reps: 4, dist: 75, desc: "Choice — 25 easy / 25 build / 25 ALL OUT", interval: "On 2:05", focus: "Three-pace finishers" },
        ]},
      { label: "Power 25s on the Minute", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_htnrgt", reps: 24, dist: 25, desc: "Choice stroke — ALL OUT, on the minute (full recovery)", interval: "On 1:05", focus: "Pure speed with consistent rest" },
          { id: "s_bk0u3e", reps: 4, dist: 100, desc: "Easy backstroke — close-out", interval: "On 2:55", focus: "Active recovery" },
        ]},
      { label: "Sprint Pyramid 50s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_tgc67u", reps: 1, dist: 50, desc: "Choice — moderate (warm-up sprint)", interval: "On 1:35", focus: "Wake up" },
          { id: "s_82dmcl", reps: 2, dist: 50, desc: "Choice — fast", interval: "On 1:35", focus: "Build" },
          { id: "s_39uptd", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Peak speed" },
          { id: "s_a435zs", reps: 6, dist: 50, desc: "Choice — ALL OUT (the test)", interval: "On 1:35", focus: "Maintain top speed" },
          { id: "s_2obtoh", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Hold the line" },
          { id: "s_3yzhjj", reps: 2, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Find speed" },
          { id: "s_e90d6v", reps: 1, dist: 50, desc: "Choice — ALL OUT (best 50)", interval: "On 1:35", focus: "Final blast" },
          { id: "s_jieu02", reps: 1, dist: 50, desc: "Easy — recovery", interval: "On 1:35", focus: "Recovery" },
        ]},
      { label: "Tight-Rest 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_ivpj1l", reps: 16, dist: 50, desc: "Freestyle — fast, very tight rest", interval: "On :50", focus: "Sprint endurance with limited recovery" },
          { id: "s_23iljt", reps: 4, dist: 50, desc: "Easy choice — between blocks", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Sprint + Pull Combo", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_2sw9dk", reps: 1, dist: 200, desc: "Easy freestyle — warm into the sprint", interval: "No interval — swim easy", focus: "Open up" },
          { id: "s_2kqgyi", reps: 6, dist: 50, desc: "Pull (buoy) — sprint, max turnover", interval: "On 1:35", focus: "Pull speed", eq: "pull" },
          { id: "s_x6gjxl", reps: 6, dist: 50, desc: "Choice stroke — ALL OUT swim, apply pull mechanics", interval: "On 1:35", focus: "Translate pull power" },
          { id: "s_ssf8y2", reps: 6, dist: 50, desc: "Easy choice — between sets", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Stroke-Specific 75s", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_bvl2al", reps: 4, dist: 75, desc: "Butterfly — race pace", interval: "On 2:40", focus: "Fly race-distance speed" },
          { id: "s_f3bdfz", reps: 4, dist: 75, desc: "Backstroke — race pace", interval: "On 2:20", focus: "Back race-distance speed" },
          { id: "s_apawnj", reps: 4, dist: 75, desc: "Breaststroke — race pace", interval: "On 2:20", focus: "Breast race-distance speed" },
          { id: "s_9i062t", reps: 2, dist: 75, desc: "Freestyle — race pace", interval: "On 1:35", focus: "Free race-distance speed" },
          { id: "s_cbb8gk", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint 24×50", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_47hswa", reps: 24, dist: 50, desc: "Choice stroke — ALL OUT, full rest between reps", interval: "On 1:35", focus: "Max speed across 24 sprint reps" },
        ]},
      { label: "Speed Blocks 100s", totalYards: 1200, types: ["sprint"], strokes: [], sets: [
          { id: "s_5hqw28", reps: 6, dist: 100, desc: "Choice stroke — sprint first 25, build last 75", interval: "On 2:20", focus: "Open with speed then hold mechanics" },
          { id: "s_7i3yrg", reps: 6, dist: 100, desc: "Freestyle — strong, reinforce sprint mechanics", interval: "On 2:05", focus: "Speed under aerobic load" },
        ]},
      { label: "Sprint + Recovery 100s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_rrsvx2", reps: 10, dist: 100, desc: "50 ALL OUT / 50 easy (immediate)", interval: "On 2:40", focus: "Hard/easy alternation across reps" },
        ]},
      { label: "Power-Pull 75s", totalYards: 1050, types: ["sprint"], strokes: [], sets: [
          { id: "s_dursds", reps: 6, dist: 75, desc: "Pull (buoy) — fast, max stroke power", interval: "On 1:50", focus: "Upper-body sprint", eq: "pull" },
          { id: "s_mt9bvw", reps: 6, dist: 75, desc: "Freestyle — fast, integrate pull power", interval: "On 1:35", focus: "Apply pull strength to swim" },
          { id: "s_5v9p5u", reps: 1, dist: 150, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "30-Second Repeats", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_rsdnle", reps: 20, dist: 25, desc: "Choice stroke — ALL OUT, on :30", interval: "On :30", focus: "Maximum effort with race-pace cycle" },
          { id: "s_6whewh", reps: 1, dist: 200, desc: "Easy freestyle — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_a6lmpv", reps: 4, dist: 75, desc: "Choice — easy/build/sprint", interval: "On 2:05", focus: "Three-pace finishers" },
        ]},
      { label: "Pace 50/100/50", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_kmg5ou", reps: 5, dist: 200, desc: "50 ALL OUT / 100 easy / 50 ALL OUT", interval: "On 4:10", focus: "Speed bookends with active recovery in middle" },
        ]},
      { label: "Sprint Kick Endurance", totalYards: 1100, types: ["sprint"], strokes: [], sets: [
          { id: "s_f8bmq2", reps: 12, dist: 50, desc: "Kick — fast, descend 1-4 then 5-8 then 9-12 (board optional)", interval: "On 1:35", focus: "Kick speed across volume", eq: "kick" },
          { id: "s_o0yf1l", reps: 4, dist: 100, desc: "Choice stroke — fast swim, drive with the legs", interval: "On 2:05", focus: "Apply kick to swim sprint" },
          { id: "s_xwwtem", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Sprint Day", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_i428c1", reps: 8, dist: 50, desc: "Backstroke — ALL OUT (full recovery)", interval: "On 2:05", focus: "Back sprint speed" },
          { id: "s_w0439h", reps: 4, dist: 100, desc: "Backstroke — fast, descend 1→4", interval: "On 2:55", focus: "Back race-pace endurance" },
          { id: "s_4qjyka", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 2:05", focus: "Top off" },
        ]},
      { label: "Ladder Sprints", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_4v74ec", reps: 1, dist: 25, desc: "Choice — ALL OUT", interval: "On :45", focus: "Open up" },
          { id: "s_u6cdcf", reps: 1, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Build" },
          { id: "s_ch95t9", reps: 1, dist: 75, desc: "Choice — ALL OUT", interval: "On 2:20", focus: "Peak" },
          { id: "s_bw6e6s", reps: 1, dist: 100, desc: "Choice — ALL OUT", interval: "On 3:10", focus: "Hold" },
          { id: "s_k8mpll", reps: 1, dist: 150, desc: "Choice — ALL OUT", interval: "On 4:45", focus: "Test" },
          { id: "s_qu06gq", reps: 1, dist: 100, desc: "Choice — ALL OUT", interval: "On 3:10", focus: "Match earlier" },
          { id: "s_24hc3q", reps: 1, dist: 75, desc: "Choice — ALL OUT", interval: "On 2:20", focus: "Find speed" },
          { id: "s_n0nlm8", reps: 1, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Final 50" },
          { id: "s_z5unq6", reps: 1, dist: 25, desc: "Choice — ALL OUT", interval: "On :45", focus: "Best 25" },
          { id: "s_mrh2xs", reps: 1, dist: 350, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Quad-Strap 50s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_5xn5rn", reps: 4, dist: 50, desc: "Round 1 — choice stroke ALL OUT", interval: "On 1:35", focus: "First peak" },
          { id: "s_7dliyz", reps: 4, dist: 50, desc: "Round 2 — choice stroke ALL OUT", interval: "On 1:35", focus: "Hold speed" },
          { id: "s_dneykp", reps: 4, dist: 50, desc: "Round 3 — choice stroke ALL OUT", interval: "On 1:35", focus: "Find speed" },
          { id: "s_nu4f4z", reps: 4, dist: 50, desc: "Round 4 — choice stroke ALL OUT", interval: "On 1:35", focus: "Final round, biggest effort" },
          { id: "s_0y49ur", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint Recovery Mix", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_qu4wt2", reps: 4, dist: 100, desc: "Choice stroke — ALL OUT", interval: "On 4:10", focus: "Race-pace 100s" },
          { id: "s_8zqaoa", reps: 4, dist: 75, desc: "Choice — ALL OUT", interval: "On 3:10", focus: "Sprint 75s" },
          { id: "s_5cl0mu", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 2:05", focus: "Sprint 50s" },
          { id: "s_rfmm11", reps: 4, dist: 25, desc: "Choice — ALL OUT", interval: "On 1:05", focus: "Top-end 25s" },
        ]},
      { label: "Pull Speed 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_gbyqua", reps: 16, dist: 25, desc: "Pull (buoy) — ALL OUT, max turnover", interval: "On :45", focus: "Upper-body top speed", eq: "pull" },
          { id: "s_1nysv9", reps: 4, dist: 100, desc: "Freestyle — fast, apply pull mechanics", interval: "On 2:05", focus: "Translate to sprint" },
          { id: "s_telodp", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Race-Day Sprint Sims", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_sbpvnk", reps: 1, dist: 50, desc: "Choice — race-pace 50 simulation", interval: "On 3:10", focus: "Race 1 — sprint 50" },
          { id: "s_oz1io4", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_no703r", reps: 1, dist: 100, desc: "Choice — race-pace 100 simulation", interval: "On 4:10", focus: "Race 2 — sprint 100" },
          { id: "s_ej03nn", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_38bf64", reps: 1, dist: 50, desc: "Choice — second race 50", interval: "On 3:10", focus: "Race 3 — back-to-back sprint" },
          { id: "s_7wku2o", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_u12c02", reps: 1, dist: 100, desc: "Choice — second race 100", interval: "On 4:10", focus: "Race 4 — final hard" },
          { id: "s_1ctmv0", reps: 1, dist: 100, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_ohdbkk", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tabata-Style 25s", totalYards: 1000, types: ["sprint"], strokes: [], sets: [
          { id: "s_crcvwm", reps: 16, dist: 25, desc: "Choice stroke — ALL OUT, on :30 (8 rounds × 2 reps, race-recovery cycle)", interval: "On :30", focus: "Lactate-tolerance sprint blocks" },
          { id: "s_0eu8ww", reps: 4, dist: 100, desc: "Easy freestyle — between rounds", interval: "No interval — swim easy", focus: "Active recovery" },
          { id: "s_sghke1", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint Volume 20×75", totalYards: 1500, types: ["sprint"], strokes: [], sets: [
          { id: "s_d8aoeg", reps: 20, dist: 75, desc: "Choice stroke — 50 strong build / 25 ALL OUT (sprint the last 25)", interval: "On 2:05", focus: "High-volume speed endurance" },
        ]},
      { label: "Sprint Double Block 2000", totalYards: 2000, types: ["sprint"], strokes: [], sets: [
          { id: "s_o05soe", reps: 20, dist: 50, desc: "Choice stroke — ALL OUT or near-max (full recovery)", interval: "On 1:35", focus: "Sprint capacity block" },
          { id: "s_n6vkmj", reps: 10, dist: 100, desc: "Freestyle — strong (not sprinting), hold pace", interval: "On 2:40", focus: "Aerobic close after sprint block" },
        ]},
      { label: "Sprint 50s Repeat Unit", totalYards: 400, types: ["sprint"], strokes: [], sets: [
          { id: "s_u2cety", reps: 8, dist: 50, desc: "Freestyle or choice — ALL OUT every rep, leave it on the wall", interval: "On 1:50", focus: "Max speed — designed to repeat 2–3×" },
        ]},
      { label: "Sprint 50+100 Repeat Unit", totalYards: 500, types: ["sprint"], strokes: [], sets: [
          { id: "s_gyrep4", reps: 6, dist: 50, desc: "Freestyle — ALL OUT sprint", interval: "On 1:50", focus: "Top-end speed" },
          { id: "s_5hkle0", reps: 2, dist: 100, desc: "Freestyle — easy active recovery, long stroke", interval: "On 2:40", focus: "Flush and reset before next round" },
        ]},
      { label: "Sprint Build Repeat Unit", totalYards: 600, types: ["sprint"], strokes: [], sets: [
          { id: "s_ou62px", reps: 4, dist: 100, desc: "Freestyle — build to strong, last 25 sprint", interval: "On 2:05", focus: "Controlled build with sprint finish" },
          { id: "s_vbp0br", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Max effort to close each round" },
        ]},
      { label: "Aerobic Ladder", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_m2mwd8", reps: 1, dist: 400, desc: "Freestyle — easy (RPE 5/10)", interval: "On 9:25", focus: "Settle in, controlled breathing" },
          { id: "s_848fsc", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6/10)", interval: "On 6:00", focus: "Find your sustainable tempo" },
          { id: "s_6zcp9z", reps: 1, dist: 200, desc: "Freestyle — strong (RPE 7/10)", interval: "On 4:45", focus: "Push the pace — hold your form" },
          { id: "s_24zw93", reps: 1, dist: 100, desc: "Freestyle — near max (RPE 8+/10)", interval: "On 2:05", focus: "Empty the aerobic tank" },
        ]},
      { label: "Long Steady State", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_7ou85f", reps: 2, dist: 600, desc: "Freestyle — steady aerobic pace (RPE 6/10), no intervals", interval: "On 12:05", focus: "Comfortable effort you can sustain 20+ min" },
        ]},
      { label: "Progressive Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_gavc6e", reps: 1, dist: 400, desc: "Freestyle — easy (RPE 5/10)", interval: "On 9:25", focus: "Aerobic base — controlled breathing" },
          { id: "s_dtr1du", reps: 1, dist: 400, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 8:10", focus: "Find your tempo" },
          { id: "s_o3dkn6", reps: 1, dist: 400, desc: "Freestyle — strong (RPE 7-8/10)", interval: "On 8:55", focus: "Push lactate threshold — hold stroke form" },
        ]},
      { label: "Threshold Tempo", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ubbdcr", reps: 4, dist: 300, desc: "Freestyle — threshold pace (RPE 7-8/10). Hold SAME pace all 4 reps.", interval: "On 6:50", focus: "Aerobic ceiling — maintain form under sustained effort" },
        ]},
      { label: "Pull Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_vmgfqn", reps: 1, dist: 600, desc: "Pull (buoy) — steady aerobic, focus on long stroke", interval: "On 11:35", focus: "Build pull endurance, smooth and rhythmic", eq: "pull" },
          { id: "s_yqvtni", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend 1→4", interval: "On 2:05", focus: "Add intensity to the same pull pattern", eq: "pull" },
          { id: "s_u2nbzt", reps: 1, dist: 200, desc: "Freestyle swim — apply pull catch into full stroke", interval: "On 3:55", focus: "Carry the catch over to swimming" },
        ]},
      { label: "Mile Pace Builder", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_cnxqpc", reps: 1, dist: 400, desc: "Freestyle — find your mile pace (RPE 7/10)", interval: "On 8:55", focus: "Establish your sustainable race pace" },
          { id: "s_gp4upx", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Reset between efforts" },
          { id: "s_n0vjm8", reps: 1, dist: 400, desc: "Freestyle — match the first 400 pace", interval: "On 8:55", focus: "Repeat the same effort fatigued" },
          { id: "s_vxiin4", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Final reset" },
          { id: "s_h5xw4h", reps: 2, dist: 100, desc: "Freestyle — slightly faster than 400 pace", interval: "On 2:20", focus: "Practice closing speed at the end of a mile" },
        ]},
      { label: "Aerobic Capacity Set", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_1qzuax", reps: 6, dist: 150, desc: "Freestyle — strong aerobic effort (RPE 7-8/10)", interval: "On 2:55", focus: "Sustained mid-distance work, hold form throughout" },
          { id: "s_8u4hvy", reps: 4, dist: 100, desc: "Freestyle — finish strong (RPE 8/10)", interval: "On 2:15", focus: "Push lactate threshold under accumulated fatigue" },
        ]},
      { label: "Heart Rate Set", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_6swbww", reps: 4, dist: 100, desc: "Freestyle — RPE 5/10 (easy aerobic)", interval: "On 2:20", focus: "Active recovery zone" },
          { id: "s_d2loga", reps: 4, dist: 100, desc: "Freestyle — RPE 7/10 (tempo)", interval: "On 2:15", focus: "Mid-zone tempo" },
          { id: "s_ugkta4", reps: 4, dist: 100, desc: "Freestyle — RPE 8/10 (threshold)", interval: "On 2:15", focus: "Threshold zone" },
        ]},
      { label: "Long Kick Endurance", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_n6a18d", reps: 1, dist: 400, desc: "Kick — easy/moderate, choice of stroke (board optional)", interval: "On 8:25", focus: "Long kick base, build endurance", eq: "kick" },
          { id: "s_pv5ok2", reps: 6, dist: 100, desc: "Freestyle — moderate aerobic, focus on legs driving", interval: "On 2:20", focus: "Apply kick conditioning into stroke" },
          { id: "s_2irn55", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Cool down" },
        ]},
      { label: "Threshold Ladder", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_oym2y2", reps: 1, dist: 100, desc: "Freestyle — RPE 6/10 (build into threshold)", interval: "On 2:20", focus: "Open up" },
          { id: "s_asnao4", reps: 1, dist: 200, desc: "Freestyle — RPE 7/10", interval: "On 4:45", focus: "Approach threshold" },
          { id: "s_qvgjg3", reps: 1, dist: 300, desc: "Freestyle — RPE 8/10 (at threshold)", interval: "On 6:50", focus: "Hold threshold" },
          { id: "s_nu530d", reps: 1, dist: 200, desc: "Freestyle — RPE 8/10 (still threshold)", interval: "On 4:45", focus: "Match earlier 200 pace under fatigue" },
          { id: "s_8hkkli", reps: 1, dist: 100, desc: "Freestyle — RPE 9/10 (just over threshold)", interval: "On 2:20", focus: "Push past threshold" },
          { id: "s_k6r68y", reps: 1, dist: 300, desc: "Easy choice — full recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sub-Threshold Volume", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_1rpxf4", reps: 13, dist: 100, desc: "Freestyle — sub-threshold (RPE 7-7.5/10), even pace", interval: "On 2:15", focus: "High-volume sustained effort just below threshold" },
        ]},
      { label: "Aerobic IM Set", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_jrf81r", reps: 6, dist: 200, desc: "200 IM — aerobic effort (RPE 6/10), smooth transitions", interval: "On 6:20", focus: "Endurance across IM strokes at aerobic pace" },
        ]},
      { label: "Pull-Heavy Endurance", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_elhwwf", reps: 1, dist: 500, desc: "Pull (buoy) — steady aerobic, focus on long stroke", interval: "On 10:00", focus: "Long pull base", eq: "pull" },
          { id: "s_9416oy", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend pace 1→4", interval: "On 2:20", focus: "Build pull intensity", eq: "pull" },
          { id: "s_yvyd97", reps: 4, dist: 100, desc: "Freestyle — apply pull mechanics", interval: "On 2:20", focus: "Translate to swim" },
        ]},
      { label: "Race-Pace 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_l7x1i5", reps: 6, dist: 200, desc: "Freestyle — at goal race pace, hold all 6 reps", interval: "On 4:10", focus: "Race-distance pace under accumulating fatigue" },
        ]},
      { label: "400 Repeats Aerobic", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_6fmuor", reps: 3, dist: 400, desc: "Freestyle — aerobic pace (RPE 7/10), even effort", interval: "On 9:25", focus: "Distance work at aerobic ceiling" },
        ]},
      { label: "Recovery Intervals", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_398a6a", reps: 8, dist: 150, desc: "75 strong / 75 easy alternating", interval: "On 2:55", focus: "Practice recovery between hard efforts" },
        ]},
      { label: "Long Continuous + Sprint Finish", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ein5pn", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, even splits", interval: "On 17:50", focus: "Long aerobic base" },
          { id: "s_dwlaeg", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (max sprint after distance)", interval: "On 1:35", focus: "Sprint after fatigue — race-finish prep" },
        ]},
      { label: "Ascending Ladder", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_9v2fyj", reps: 1, dist: 100, desc: "Freestyle — easy (RPE 5/10)", interval: "On 2:15", focus: "Reserve — the long stuff is coming" },
          { id: "s_6y8wun", reps: 1, dist: 200, desc: "Freestyle — easy/moderate (RPE 6/10)", interval: "On 4:30", focus: "Find your rhythm" },
          { id: "s_r8ix4m", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 6:50", focus: "Build into sustained effort" },
          { id: "s_mcqjux", reps: 1, dist: 400, desc: "Freestyle — strong aerobic (RPE 7/10)", interval: "On 8:55", focus: "Apex — hold your best aerobic pace" },
        ]},
      { label: "Endurance Pyramid", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_lbwry1", reps: 1, dist: 200, desc: "Freestyle — easy/moderate (RPE 6/10)", interval: "On 4:30", focus: "Build in from the base" },
          { id: "s_gj2mys", reps: 1, dist: 300, desc: "Freestyle — moderate (RPE 6-7/10)", interval: "On 6:50", focus: "Climb toward the apex" },
          { id: "s_oq27ll", reps: 1, dist: 400, desc: "Freestyle — strong aerobic (RPE 7-8/10)", interval: "On 8:55", focus: "Apex — aerobic ceiling, hold your form" },
          { id: "s_aetj3g", reps: 1, dist: 300, desc: "Freestyle — hold the 300 pace on the way down", interval: "On 6:50", focus: "Same effort under fatigue" },
          { id: "s_ohibar", reps: 1, dist: 200, desc: "Freestyle — close strong", interval: "On 4:30", focus: "Finish faster than you started" },
        ]},
      { label: "Aerobic 7×200", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_48zp6z", reps: 7, dist: 200, desc: "Freestyle — aerobic pace, even splits throughout", interval: "On 4:30", focus: "High-volume aerobic accumulation" },
        ]},
      { label: "1000 + 4×100", totalYards: 1400, types: ["endurance"], strokes: [], sets: [
          { id: "s_5z7kmi", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, focus on stroke efficiency", interval: "On 23:05", focus: "Distance foundation" },
          { id: "s_s99z4x", reps: 4, dist: 100, desc: "Freestyle — slightly faster than 1000 pace", interval: "On 2:20", focus: "Speed layer after distance work" },
        ]},
      { label: "8×150 Steady", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_5pcch6", reps: 8, dist: 150, desc: "Freestyle — aerobic pace (RPE 6-7/10), hold all 8 the same", interval: "On 3:20", focus: "Classic Masters repeat — even splits every rep" },
        ]},
      { label: "Mile Pace Set", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_9vg3j6", reps: 11, dist: 100, desc: "Freestyle — at goal mile pace, hold even splits", interval: "On 2:15", focus: "Race-pace mile fitness" },
        ]},
      { label: "60-Minute Aerobic", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_ol1ua0", reps: 1, dist: 1500, desc: "Freestyle — comfortable aerobic pace, smooth all the way", interval: "On 34:40", focus: "Long aerobic capacity work" },
        ]},
      { label: "Threshold 250s", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_ewrpf8", reps: 5, dist: 250, desc: "Freestyle — threshold pace (RPE 7-8/10), even effort", interval: "On 4:45", focus: "Mid-distance threshold work" },
        ]},
      { label: "Hold-Pace 1250", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_vof3u2", reps: 1, dist: 1000, desc: "Freestyle — aerobic pace, aim for even 250 splits", interval: "On 23:05", focus: "Sustained aerobic discipline" },
          { id: "s_movx39", reps: 5, dist: 50, desc: "Freestyle — slightly faster than 1000 pace", interval: "On 1:20", focus: "Speed finish after the long swim" },
        ]},
      { label: "Negative-Split 250s Endurance", totalYards: 1250, types: ["endurance"], strokes: [], sets: [
          { id: "s_53jj1q", reps: 5, dist: 250, desc: "Freestyle — negative split (hold effort steady, let times drop rep to rep)", interval: "On 5:15", focus: "Learn to finish stronger than you start" },
        ]},
      { label: "Aerobic Build 400s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_6kpyry", reps: 3, dist: 400, desc: "Freestyle — build pace within each 400 (easy → strong)", interval: "On 8:55", focus: "Internal pace progression" },
        ]},
      { label: "RPE Stairstep", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_cji3k3", reps: 2, dist: 200, desc: "Freestyle — RPE 6/10", interval: "On 4:45", focus: "Settle in" },
          { id: "s_d8txj6", reps: 2, dist: 200, desc: "Freestyle — RPE 7/10", interval: "On 4:30", focus: "Build" },
          { id: "s_nc2yrm", reps: 2, dist: 200, desc: "Freestyle — RPE 8/10", interval: "On 4:30", focus: "Threshold" },
        ]},
      { label: "Long Pull + Build", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_p7fppv", reps: 1, dist: 800, desc: "Pull (buoy) — steady aerobic, smooth long stroke", interval: "On 17:50", focus: "Long pull endurance", eq: "pull" },
          { id: "s_zh48ym", reps: 5, dist: 100, desc: "Freestyle — descend 1→5, build closing pace", interval: "On 2:20", focus: "Apply pull conditioning" },
        ]},
      { label: "Heart Rate Recovery", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_kef17i", reps: 6, dist: 200, desc: "Freestyle — moderate aerobic, focus on staying in HR zone 2", interval: "On 4:45", focus: "Build aerobic fitness with HR feedback" },
        ]},
      { label: "Long Choice 1100", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_ngotjf", reps: 1, dist: 1100, desc: "Freestyle — continuous, comfortable aerobic pace, even effort", interval: "On 21:00", focus: "Long-form aerobic engine work" },
        ]},
      { label: "Cruise 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_w5bv0w", reps: 6, dist: 200, desc: "Freestyle — cruise pace (between aerobic and threshold)", interval: "On 4:30", focus: "Cruise pace for distance fitness" },
        ]},
      { label: "Mile Negative Split", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_w1sdfh", reps: 1, dist: 1500, desc: "Freestyle — second 750 faster than first 750", interval: "On 34:40", focus: "Practice negative splitting a mile" },
        ]},
      { label: "Lactate Buffering", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_7tk5r9", reps: 4, dist: 200, desc: "Freestyle — at lactate threshold (RPE 8/10)", interval: "On 4:30", focus: "Threshold blocks" },
          { id: "s_45z4be", reps: 1, dist: 200, desc: "Easy backstroke — recovery between blocks", interval: "On 5:45", focus: "Practice lactate clearance" },
          { id: "s_jc85vc", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_b7xpl7", reps: 8, dist: 150, desc: "Freestyle — aerobic pace (RPE 6/10), smooth long stroke", interval: "On 3:10", focus: "Aerobic volume in shorter reps" },
        ]},
      { label: "Pull-Heavy 200s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_y25798", reps: 6, dist: 200, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 4:30", focus: "Pull endurance volume", eq: "pull" },
        ]},
      { label: "200/100 Endurance Mix", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ua2upo", reps: 3, dist: 200, desc: "Freestyle — moderate aerobic", interval: "On 4:45", focus: "Aerobic base" },
          { id: "s_4ek6i5", reps: 6, dist: 100, desc: "Freestyle — sub-threshold pace", interval: "On 2:15", focus: "Sustained sub-threshold" },
        ]},
      { label: "Anaerobic Threshold Test", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_7l3gk0", reps: 1, dist: 200, desc: "Freestyle — easy warm-up", interval: "On 4:10", focus: "Open up" },
          { id: "s_kg68ck", reps: 1, dist: 800, desc: "Freestyle — at anaerobic threshold (RPE 8/10), even splits", interval: "On 14:40", focus: "Threshold test piece" },
          { id: "s_wtehgr", reps: 2, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Cool down" },
        ]},
      { label: "Aerobic IM Volume", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_m32d96", reps: 4, dist: 300, desc: "300 IM — aerobic pace, smooth transitions", interval: "On 6:20", focus: "Endurance across all four strokes" },
        ]},
      { label: "Steady 500s", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_ksa1jk", reps: 3, dist: 500, desc: "Freestyle — steady aerobic, hold same time each 500", interval: "On 11:35", focus: "Even-effort distance volume" },
        ]},
      { label: "Pull + Kick Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_hqu2zr", reps: 1, dist: 400, desc: "Pull (buoy) — steady aerobic", interval: "On 8:55", focus: "Pull base", eq: "pull" },
          { id: "s_bd2m3q", reps: 1, dist: 400, desc: "Kick — moderate (board optional)", interval: "On 8:55", focus: "Kick base", eq: "kick" },
          { id: "s_ccyhn9", reps: 4, dist: 100, desc: "Freestyle — moderate, integrate pull and kick work", interval: "On 2:20", focus: "Apply both into full stroke" },
        ]},
      { label: "Tempo + Recovery 100s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_f6f43m", reps: 12, dist: 100, desc: "Freestyle — odd-numbered tempo (RPE 7), even-numbered easy", interval: "On 2:20", focus: "Tempo with built-in recovery" },
        ]},
      { label: "Long Open Water Sim", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_3sgt4s", reps: 1, dist: 100, desc: "Freestyle — easy warm-up", interval: "On 2:05", focus: "Open up" },
          { id: "s_89oe2d", reps: 1, dist: 1000, desc: "Freestyle — sighting every 6-8 strokes (open water simulation)", interval: "On 23:05", focus: "Practice open-water mechanics with sighting" },
          { id: "s_9xlk3q", reps: 1, dist: 200, desc: "Easy choice — cool", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic Step-Up", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_d674dy", reps: 1, dist: 100, desc: "Freestyle — RPE 5/10", interval: "On 2:20", focus: "Easy" },
          { id: "s_8grb6h", reps: 1, dist: 200, desc: "Freestyle — RPE 6/10", interval: "On 4:45", focus: "Aerobic" },
          { id: "s_hqpphq", reps: 1, dist: 300, desc: "Freestyle — RPE 7/10", interval: "On 6:50", focus: "Tempo" },
          { id: "s_0ayd6k", reps: 1, dist: 400, desc: "Freestyle — RPE 8/10", interval: "On 7:35", focus: "Threshold" },
          { id: "s_1qdea1", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Distance Recovery Set", totalYards: 1100, types: ["endurance"], strokes: [], sets: [
          { id: "s_gg8hfp", reps: 11, dist: 100, desc: "Freestyle — easy/moderate (RPE 5-6/10), recovery-day pace", interval: "No interval — swim easy", focus: "Aerobic recovery volume" },
        ]},
      { label: "Race-Pace Endurance", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ro1pq6", reps: 8, dist: 150, desc: "Freestyle — at goal race pace, hold even splits", interval: "On 2:55", focus: "Race-pace volume in shorter reps" },
        ]},
      { label: "Threshold Pull 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_hrykew", reps: 8, dist: 150, desc: "Pull (buoy) — threshold pace, smooth long stroke", interval: "On 2:55", focus: "Pull threshold volume", eq: "pull" },
        ]},
      { label: "1×800 Steady + Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_rybhgh", reps: 1, dist: 800, desc: "Freestyle — steady aerobic, even effort throughout", interval: "On 17:50", focus: "Long aerobic effort" },
          { id: "s_hi7oci", reps: 4, dist: 100, desc: "Freestyle — build pace 1→4 (each one slightly faster)", interval: "On 2:20", focus: "Closing build after distance" },
        ]},
      { label: "Aerobic Stroke Mix", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_qjxp7a", reps: 4, dist: 200, desc: "Freestyle — aerobic", interval: "On 4:45", focus: "Free aerobic base" },
          { id: "s_mzg7hx", reps: 2, dist: 200, desc: "Backstroke — aerobic", interval: "On 5:45", focus: "Back aerobic" },
        ]},
      { label: "Long Kick Aerobic", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_ctbdrn", reps: 1, dist: 600, desc: "Kick — choice stroke, aerobic effort (board optional)", interval: "On 13:40", focus: "Long kick base", eq: "kick" },
          { id: "s_a07qmu", reps: 6, dist: 100, desc: "Freestyle — aerobic, drive with the legs", interval: "On 2:20", focus: "Apply kick conditioning" },
        ]},
      { label: "Aerobic 6×200", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_pwf1ga", reps: 6, dist: 200, desc: "Freestyle — aerobic pace, descend 1→3 then 4→6", interval: "On 4:45", focus: "Two-block descend across aerobic volume" },
        ]},
      { label: "Long Pull + Free Mix", totalYards: 1300, types: ["endurance"], strokes: [], sets: [
          { id: "s_gfc60d", reps: 4, dist: 200, desc: "Pull (buoy) — aerobic pace, smooth long stroke", interval: "On 4:30", focus: "Pull aerobic volume", eq: "pull" },
          { id: "s_qbbe1d", reps: 5, dist: 100, desc: "Freestyle — moderate, apply pull mechanics", interval: "On 2:20", focus: "Translate to swim" },
        ]},
      { label: "Marathon-Style 12×100", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_dqr19w", reps: 12, dist: 100, desc: "Freestyle — all reps the same time, easy aerobic effort", interval: "On 2:05", focus: "Discipline of even-effort high-volume aerobic" },
        ]},
      { label: "Straight 1500 Endurance", totalYards: 1500, types: ["endurance"], strokes: [], sets: [
          { id: "s_auz71o", reps: 1, dist: 1500, desc: "Freestyle — aerobic pace, focus on even effort and form throughout", interval: "On 33:35", focus: "Long continuous endurance" },
        ]},
      { label: "Double 1000 Endurance", totalYards: 2000, types: ["endurance"], strokes: [], sets: [
          { id: "s_7vo32p", reps: 2, dist: 1000, desc: "Freestyle — aerobic pace, negative split (second 500 faster than first)", interval: "On 22:05", focus: "Long-form aerobic endurance with pacing discipline" },
        ]},
      { label: "Triple 800 Endurance", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_eoh8ij", reps: 3, dist: 800, desc: "Freestyle — aerobic pace, descend 1→3 (each one faster)", interval: "On 17:50", focus: "High-volume endurance with progressive effort" },
        ]},
      { label: "Endurance 100s Repeat Unit", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_vfjmzh", reps: 4, dist: 100, desc: "Freestyle — aerobic pace, RPE 6/10, long stroke throughout", interval: "On 2:15", focus: "Sustained aerobic effort — designed to repeat 2–3×" },
        ]},
      { label: "Endurance 200+100 Repeat Unit", totalYards: 500, types: ["endurance"], strokes: [], sets: [
          { id: "s_ulu11s", reps: 2, dist: 200, desc: "Freestyle — moderate aerobic pace, even splits", interval: "On 4:30", focus: "Aerobic base work" },
          { id: "s_vqsrjq", reps: 1, dist: 100, desc: "Freestyle — slightly stronger, hold form from 200s", interval: "On 2:10", focus: "Push the last 100 each round" },
        ]},
      { label: "Endurance 200s Repeat Unit", totalYards: 600, types: ["endurance"], strokes: [], sets: [
          { id: "s_54hit9", reps: 3, dist: 200, desc: "Freestyle — aerobic pace, aim for identical splits each rep", interval: "On 4:30", focus: "Pacing consistency — make each round match the last" },
        ]},
      { label: "CSS 10×100", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_0tsf4o", reps: 10, dist: 100, desc: "Freestyle — Critical Swim Speed pace, hold ALL 10 reps the same", interval: "On 2:15", focus: "Find and hold your CSS — sustainable for 1500m/1650y" },
        ]},
      { label: "50s on the Minute", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_u8t7eu", reps: 24, dist: 50, desc: "Freestyle — steady pace, on the :60 (test endurance)", interval: "On 1:05", focus: "Sustain pace and form across high-volume 50s" },
        ]},
      { label: "8×150 Repeats", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_euwse1", reps: 8, dist: 150, desc: "Freestyle — steady aerobic pace, hold all 8 identical", interval: "On 3:20", focus: "Classic Masters repeat set — consistency over speed" },
        ]},
      { label: "Lactate-Threshold 150s", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_kng2pr", reps: 8, dist: 150, desc: "Freestyle — threshold pace (RPE 8/10), even effort", interval: "On 2:55", focus: "Lactate threshold in mid-distance reps" },
        ]},
      { label: "CSS Holdover", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_vfj85w", reps: 12, dist: 100, desc: "Freestyle — CSS pace, hold ALL 12 reps the same", interval: "On 2:15", focus: "Critical Swim Speed under accumulated load" },
        ]},
      { label: "Race-Pace 100s", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_otc9t1", reps: 10, dist: 100, desc: "Freestyle — at race-distance pace, hold all 10", interval: "On 2:15", focus: "Race pace under volume" },
        ]},
      { label: "8×150 Build", totalYards: 1200, types: ["endurance"], strokes: [], sets: [
          { id: "s_yu285o", reps: 8, dist: 150, desc: "Freestyle — build pace 1-4, then descend 5-8", interval: "On 2:55", focus: "Two-block build into the threshold" },
        ]},
      { label: "Build-Per-100", totalYards: 1000, types: ["endurance"], strokes: [], sets: [
          { id: "s_ecs3a1", reps: 10, dist: 100, desc: "Freestyle — first 25 easy, build through 100", interval: "On 2:05", focus: "Internal build per rep" },
        ]},
      { label: "Distance 100s Repeat Unit", totalYards: 400, types: ["endurance"], strokes: [], sets: [
          { id: "s_ct5may", reps: 4, dist: 100, desc: "Freestyle — aerobic pace, even effort, count strokes on last 25", interval: "On 2:15", focus: "Steady-state aerobic — designed to repeat 2–3×" },
        ]},
      { label: "Sprint Marathon 2400", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_c7xmlu", reps: 8, dist: 200, desc: "Freestyle — strong, descend 1→4 then repeat (1→4)", interval: "On 4:45", focus: "Sustained speed over distance" },
          { id: "s_7jyyj0", reps: 8, dist: 100, desc: "Choice stroke — fast (near race pace)", interval: "On 2:05", focus: "Speed layer after distance block" },
        ]},
      { label: "Continuous 1800 Aerobic", totalYards: 1800, types: ["endurance"], strokes: [], sets: [
          { id: "s_t6as21", reps: 1, dist: 1800, desc: "Freestyle — single continuous swim at aerobic pace, smooth all the way", interval: "On 42:00", focus: "Single-piece aerobic capacity build" },
        ]},
      { label: "Six 300s Aerobic", totalYards: 1800, types: ["endurance"], strokes: [], sets: [
          { id: "s_9737gk", reps: 6, dist: 300, desc: "Freestyle — aerobic pace, even effort across all six, controlled breathing", interval: "On 6:50", focus: "Aerobic volume at a long-rep length that's neither sprint nor distance" },
        ]},
      { label: "Aerobic Build 2400", totalYards: 2400, types: ["endurance"], strokes: [], sets: [
          { id: "s_fi9gy8", reps: 1, dist: 400, desc: "Freestyle — easy, find aerobic rhythm", interval: "On 9:00", focus: "Warm into the aerobic build" },
          { id: "s_bn7655", reps: 1, dist: 800, desc: "Freestyle — moderate, hold same pace throughout", interval: "On 18:00", focus: "Sustained aerobic effort" },
          { id: "s_4mnxo1", reps: 1, dist: 800, desc: "Freestyle — strong, faster than the previous 800", interval: "On 17:30", focus: "Closing pressure on aerobic threshold" },
          { id: "s_8tmvtd", reps: 1, dist: 400, desc: "Freestyle — strong, hold the new pace", interval: "On 8:30", focus: "Finish at threshold" },
        ]},
      { label: "Kick + Drill + Swim", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_v3ggwj", reps: 8, dist: 75, desc: "25 Kick / 25 Drill / 25 Swim — same stroke each rep (your choice)", interval: "On 1:50", focus: "Apply the drill directly into the swim", eq: "kickPart" },
          { id: "s_a0vy3r", reps: 4, dist: 100, desc: "Pull (buoy) — focus on high-elbow catch, long stroke", interval: "On 2:20", focus: "Sculpt the perfect pull pattern", eq: "pull" },
        ]},
      { label: "Freestyle Drill Progression", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_sdgtvf", reps: 4, dist: 50, desc: "Kick only — no board, streamlined off wall", interval: "On 1:50", focus: "Core rotation, balanced two-beat kick" },
          { id: "s_9q9t12", reps: 4, dist: 50, desc: "Single-arm drill (alternate arms by 25)", interval: "On 1:25", focus: "Lead arm extension, shoulder-driven rotation" },
          { id: "s_7yll1p", reps: 4, dist: 50, desc: "Catch-up drill — touch hands at extension", interval: "On 1:30", focus: "Full extension before pulling" },
          { id: "s_2svy2n", reps: 4, dist: 100, desc: "Freestyle swim — integrate all drilled elements", interval: "On 2:15", focus: "Swim slowly, feel everything" },
        ]},
      { label: "All-Stroke Technique", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_h2ukoa", reps: 4, dist: 100, desc: "Choice of stroke — SLOW, perfect form over speed", interval: "On 2:40", focus: "No rushing — technique over pace" },
          { id: "s_8wl3hf", reps: 8, dist: 50, desc: "2 each: fly drill / back drill / breast drill / free drill", interval: "On 1:35", focus: "Key isolation drill for each stroke" },
          { id: "s_qns7ph", reps: 4, dist: 75, desc: "25 Drill + 50 Swim (same stroke — apply what you drilled)", interval: "On 2:40", focus: "Bridge drill into real swimming" },
        ]},
      { label: "Kick-Heavy Day", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_cf0p0o", reps: 8, dist: 50, desc: "Kick — alternate stroke choice every 50", interval: "On 1:35", focus: "Develop kick across all strokes", eq: "kick" },
          { id: "s_1e093w", reps: 4, dist: 75, desc: "25 Kick / 25 Drill / 25 Swim — same stroke", interval: "On 1:45", focus: "Translate kick rhythm into swim", eq: "kickPart" },
          { id: "s_m605m0", reps: 4, dist: 100, desc: "Freestyle — relaxed swim, apply leg work", interval: "On 2:05", focus: "Apply leg conditioning into the stroke" },
        ]},
      { label: "Catch + Pull Focus", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_8q2iob", reps: 4, dist: 50, desc: "Sculling drill — head-first, focus on hand pressure", interval: "On 1:20", focus: "Feel the water against your forearm" },
          { id: "s_9jyh7z", reps: 4, dist: 100, desc: "Pull (buoy) — count strokes per 25, minimize", interval: "On 2:20", focus: "Long efficient pull, max distance per stroke", eq: "pull" },
          { id: "s_ux8h9q", reps: 4, dist: 75, desc: "25 Pull + 50 Swim — carry the catch into the stroke", interval: "On 1:45", focus: "Bridge pull mechanics into full freestyle", eq: "pull" },
          { id: "s_vd74n1", reps: 2, dist: 100, desc: "Freestyle — relaxed, smooth, apply everything", interval: "On 2:05", focus: "Integrate the work into normal swimming" },
        ]},
      { label: "Stroke Count Awareness", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_69i498", reps: 4, dist: 50, desc: "Freestyle — count strokes per 25, set baseline", interval: "On 1:25", focus: "Establish your normal stroke count" },
          { id: "s_kxjztk", reps: 4, dist: 50, desc: "Freestyle — same speed, REDUCE strokes by 1-2", interval: "On 1:25", focus: "Reach further, swim taller" },
          { id: "s_chvie1", reps: 4, dist: 50, desc: "Freestyle — go FASTER without adding strokes", interval: "On 1:25", focus: "Faster turnover, same length" },
          { id: "s_reo8fg", reps: 4, dist: 100, desc: "Freestyle — apply the count target across longer swim", interval: "On 2:15", focus: "Sustain efficiency over distance" },
        ]},
      { label: "Backstroke Focus", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_4tjd1p", reps: 4, dist: 50, desc: "Backstroke — single-arm drill (alternate arms by 25)", interval: "On 1:35", focus: "Lead arm extension and rotation" },
          { id: "s_ufvc6s", reps: 4, dist: 75, desc: "Backstroke — count strokes per 25, minimize", interval: "On 2:40", focus: "Long efficient back stroke" },
          { id: "s_30mmtl", reps: 4, dist: 50, desc: "Backstroke — focus on hip rotation and kick", interval: "On 1:35", focus: "Drive rotation from the hips" },
          { id: "s_jxj19r", reps: 4, dist: 100, desc: "Backstroke — full swim, integrate all elements", interval: "On 3:10", focus: "Apply drill work to full backstroke" },
        ]},
      { label: "Breaststroke Mechanics", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_uf3l8w", reps: 4, dist: 50, desc: "Breaststroke — pull-only drill (no kick, just glide)", interval: "On 1:35", focus: "Isolate the pull and glide" },
          { id: "s_2jpllq", reps: 4, dist: 50, desc: "Breaststroke — kick-only drill (board optional)", interval: "On 1:35", focus: "Power kick and timing" },
          { id: "s_33qck2", reps: 4, dist: 75, desc: "Breaststroke — 2 kicks per pull (slow pace)", interval: "On 2:40", focus: "Extend the glide phase" },
          { id: "s_bz80kp", reps: 4, dist: 100, desc: "Breaststroke — full stroke, integrate timing", interval: "On 3:25", focus: "Smooth pull-glide-kick rhythm" },
        ]},
      { label: "Butterfly Sustainability", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_k4o3l5", reps: 8, dist: 25, desc: "Butterfly — 3-3-3 drill (3 right arm / 3 left arm / 3 full)", interval: "On 1:05", focus: "Build fly endurance with drill" },
          { id: "s_rffrem", reps: 6, dist: 50, desc: "Butterfly — 25 drill / 25 swim", interval: "On 1:35", focus: "Bridge drill into full fly" },
          { id: "s_t5cv2s", reps: 4, dist: 100, desc: "Butterfly + Free — 25 fly / 75 free", interval: "On 3:10", focus: "Practice fly recovery in workout context" },
          { id: "s_aorkvz", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Body Position Mastery", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_irc3fn", reps: 4, dist: 50, desc: "Streamline kick on back — 12.5 underwater + 37.5 swim", interval: "On 1:35", focus: "Tight streamline, head still" },
          { id: "s_44og2i", reps: 4, dist: 50, desc: "Side-lying kick — alternate sides every 25", interval: "On 1:35", focus: "Lateral body position with rotation" },
          { id: "s_4tr4s6", reps: 4, dist: 75, desc: "Freestyle — focus on head position (water at hairline)", interval: "On 1:50", focus: "Neutral head reduces drag" },
          { id: "s_8e1093", reps: 3, dist: 100, desc: "Freestyle — body height, keep hips high", interval: "On 2:05", focus: "Whole-body alignment over the surface" },
        ]},
      { label: "Underwater Dolphin Focus", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_0zsuqr", reps: 8, dist: 25, desc: "Underwater dolphin kick off wall (target 10m), easy swim out", interval: "On 1:05", focus: "Streamline + dolphin kick power", eq: "underkick" },
          { id: "s_rv9ap3", reps: 4, dist: 50, desc: "Butterfly with focus on undulation", interval: "On 1:35", focus: "Apply dolphin rhythm to fly" },
          { id: "s_u03rma", reps: 4, dist: 75, desc: "25 underwater dolphin / 50 freestyle", interval: "On 1:50", focus: "Combine underwater work with swim", eq: "underkick" },
          { id: "s_rm787y", reps: 3, dist: 100, desc: "Freestyle — focus on push-off and underwater work", interval: "On 2:05", focus: "Underwater work in freestyle context" },
        ]},
      { label: "Bilateral Breathing", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_l938gn", reps: 4, dist: 50, desc: "Freestyle — breathe every 3 strokes", interval: "On 1:25", focus: "Bilateral breathing baseline" },
          { id: "s_q15ni7", reps: 4, dist: 75, desc: "Freestyle — breathe every 5 strokes", interval: "On 1:50", focus: "Lung capacity and breath control" },
          { id: "s_xowayl", reps: 4, dist: 100, desc: "Freestyle — alternate 3-stroke / 5-stroke patterns", interval: "On 2:05", focus: "Switch breathing patterns mid-swim" },
          { id: "s_wwodi0", reps: 1, dist: 100, desc: "Freestyle — breathe every 7 strokes (lung challenge)", interval: "On 2:05", focus: "Breath control under aerobic stress" },
        ]},
      { label: "High-Elbow Catch", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_63df0a", reps: 4, dist: 50, desc: "Pull (buoy) — sculling: head-first then feet-first", interval: "On 1:35", focus: "Feel water pressure on the forearm", eq: "pull" },
          { id: "s_m2vmow", reps: 4, dist: 50, desc: "Pull (buoy) — fingertip drag (slow)", interval: "On 1:20", focus: "Engage the elbow in recovery", eq: "pull" },
          { id: "s_184k1c", reps: 4, dist: 100, desc: "Pull (buoy) — focus on early vertical forearm", interval: "On 2:20", focus: "High elbow during catch", eq: "pull" },
          { id: "s_suexkh", reps: 3, dist: 100, desc: "Freestyle — apply high-elbow catch", interval: "On 2:20", focus: "Carry technique into swim" },
        ]},
      { label: "Streamline Off Walls", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_atfoq1", reps: 8, dist: 25, desc: "Streamline kick off wall — count strokes after surfacing", interval: "On 1:05", focus: "Maximize underwater distance off each wall" },
          { id: "s_se999t", reps: 4, dist: 50, desc: "Freestyle — strong push-off, underwater dolphin to 10m", interval: "On 1:20", focus: "Apply streamline to freestyle turns" },
          { id: "s_f4kfhk", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off and turn", interval: "On 2:05", focus: "Build streamline habit across distance" },
          { id: "s_4j3stk", reps: 2, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Recovery" },
        ]},
      { label: "Stroke Count Reduction", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_3euuik", reps: 4, dist: 50, desc: "Freestyle — count strokes (set baseline)", interval: "On 1:25", focus: "Establish baseline count per 25" },
          { id: "s_44a07w", reps: 4, dist: 50, desc: "Freestyle — same speed, count -1", interval: "On 1:25", focus: "Lengthen the stroke" },
          { id: "s_e7ydu2", reps: 4, dist: 50, desc: "Freestyle — same speed, count -2", interval: "On 1:25", focus: "Maximize distance per stroke" },
          { id: "s_8vjits", reps: 4, dist: 100, desc: "Freestyle — apply minimum count to full 100s", interval: "On 2:20", focus: "Sustain efficient count over distance" },
          { id: "s_1l4lcx", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Slow Swim Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_cvwvjt", reps: 8, dist: 100, desc: "Choice of stroke — SLOW (longer than normal interval)", interval: "On 2:40", focus: "Deliberate practice — every stroke perfect" },
          { id: "s_w7sgpo", reps: 4, dist: 50, desc: "Choice non-free — slow, focus on connection", interval: "On 1:35", focus: "Feel the catch, push, finish" },
        ]},
      { label: "20×50 Drill-Swim", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_bzrzkl", reps: 20, dist: 50, desc: "25 Drill / 25 Swim — rotate strokes every 4", interval: "On 1:10", focus: "High-rep technique: drill it, then immediately apply it" },
        ]},
      { label: "Nose-Down Stroke Focus", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_cl5r2f", reps: 8, dist: 50, desc: "Freestyle — 25 catch-up drill / 25 swim, flat head position", interval: "On 1:20", focus: "Stroke mechanics with consistent head position", eq: "snorkel" },
          { id: "s_6g4c9i", reps: 8, dist: 100, desc: "Freestyle — long stroke, neutral head, moderate pace", interval: "On 2:15", focus: "Apply flat head technique to longer reps" },
        ]},
      { label: "Single-Arm Progression", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_aph8t9", reps: 4, dist: 50, desc: "Single-arm freestyle — left only, lead arm extension", interval: "On 1:20", focus: "Left-side mechanics" },
          { id: "s_cqw80k", reps: 4, dist: 50, desc: "Single-arm freestyle — right only, lead arm extension", interval: "On 1:20", focus: "Right-side mechanics" },
          { id: "s_ufx1vp", reps: 4, dist: 75, desc: "25 single-arm + 50 swim — apply per-arm focus", interval: "On 2:40", focus: "Bridge drill into stroke" },
          { id: "s_go0ewj", reps: 4, dist: 125, desc: "Freestyle — slow, focus on per-arm consistency", interval: "On 2:40", focus: "Apply mechanics to longer reps" },
        ]},
      { label: "Catch-Up Combination", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_jb54ny", reps: 8, dist: 50, desc: "Catch-up drill — touch hands at extension, focus on lead arm hold", interval: "On 1:25", focus: "Full extension before stroking" },
          { id: "s_zn1lnj", reps: 4, dist: 100, desc: "Freestyle — long, smooth, slight catch-up timing", interval: "On 2:05", focus: "Apply timing into normal stroke" },
          { id: "s_f3obxv", reps: 4, dist: 50, desc: "Catch-up drill — faster pace, maintain timing", interval: "On 1:25", focus: "Translate timing to faster swimming" },
        ]},
      { label: "Stroke Rate Awareness", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_khx5o9", reps: 4, dist: 50, desc: "Freestyle — count strokes per 25 (baseline)", interval: "On 1:25", focus: "Establish baseline" },
          { id: "s_y186zs", reps: 4, dist: 50, desc: "Freestyle — same speed, count -1 (lengthen stroke)", interval: "On 1:25", focus: "Lengthen, fewer strokes" },
          { id: "s_25bzs8", reps: 4, dist: 50, desc: "Freestyle — same count, faster pace", interval: "On 1:25", focus: "Faster turnover, same length" },
          { id: "s_wh5is7", reps: 4, dist: 100, desc: "Freestyle — apply min count to longer reps", interval: "On 2:05", focus: "Hold efficient count over distance" },
          { id: "s_40jcj7", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fingertip Drag Day", totalYards: 1050, types: ["technique"], strokes: [], sets: [
          { id: "s_4zpsr1", reps: 4, dist: 50, desc: "Fingertip drag drill — slow, controlled recovery", interval: "On 1:20", focus: "Engaged elbow during recovery" },
          { id: "s_ltv7nv", reps: 4, dist: 75, desc: "25 fingertip drag + 50 swim", interval: "On 2:40", focus: "Apply recovery pattern" },
          { id: "s_jlzkl4", reps: 4, dist: 100, desc: "Freestyle — keep fingertips low through recovery", interval: "On 2:05", focus: "Maintain elbow engagement at speed" },
          { id: "s_bcszgr", reps: 1, dist: 150, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sculling Sequences", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_5ktpp5", reps: 4, dist: 50, desc: "Front scull (head-first) — focus on hand pressure", interval: "On 1:35", focus: "Forward scull mechanics" },
          { id: "s_voyqho", reps: 4, dist: 50, desc: "Mid-pull scull — under chest", interval: "On 1:35", focus: "Mid-stroke pressure" },
          { id: "s_t8ye19", reps: 4, dist: 50, desc: "Reverse scull (feet-first)", interval: "On 1:35", focus: "Reverse mechanics" },
          { id: "s_vod49p", reps: 4, dist: 100, desc: "Freestyle — apply sculling sensation to catch", interval: "On 2:05", focus: "Translate scull pressure to pull" },
        ]},
      { label: "Kick on Side Drill", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_aleyv2", reps: 4, dist: 50, desc: "Kick on left side — top arm at side, lead arm extended", interval: "On 1:35", focus: "Left-side rotation", eq: "kick" },
          { id: "s_dhvtlg", reps: 4, dist: 50, desc: "Kick on right side — top arm at side, lead arm extended", interval: "On 1:35", focus: "Right-side rotation", eq: "kick" },
          { id: "s_9nxcvd", reps: 4, dist: 50, desc: "6-kick switch drill (6 kicks each side, then swap)", interval: "On 1:25", focus: "Connect rotation to stroke", eq: "kickPart" },
          { id: "s_v1n6yo", reps: 4, dist: 100, desc: "Freestyle — apply rotation pattern", interval: "On 2:05", focus: "Translate to swimming" },
        ]},
      { label: "3/5/7 Breathing Build", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_trlrxq", reps: 4, dist: 75, desc: "Freestyle — breathe every 3 strokes", interval: "On 1:35", focus: "Bilateral baseline" },
          { id: "s_e1c5cc", reps: 4, dist: 75, desc: "Freestyle — breathe every 5 strokes", interval: "On 1:35", focus: "Lung control" },
          { id: "s_b5kjfg", reps: 4, dist: 75, desc: "Freestyle — breathe every 7 strokes", interval: "On 1:35", focus: "Hypoxic build" },
          { id: "s_zyrp6n", reps: 1, dist: 200, desc: "Freestyle — return to bilateral, smooth", interval: "On 4:10", focus: "Reset normal breathing" },
        ]},
      { label: "Vertical Forearm Set", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_m026yr", reps: 4, dist: 50, desc: "Pull (buoy) — slow, exaggerate early vertical forearm", interval: "On 1:35", focus: "Engage the elbow at the top of the catch", eq: "pull" },
          { id: "s_0y07hn", reps: 4, dist: 100, desc: "Pull (buoy) — apply EVF at moderate pace", interval: "On 2:20", focus: "Maintain EVF over longer reps", eq: "pull" },
          { id: "s_calfpr", reps: 4, dist: 75, desc: "Freestyle — 25 EVF focus / 50 swim", interval: "On 2:40", focus: "Bridge into normal stroke" },
          { id: "s_fhd5mr", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Technique Day", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_4avf15", reps: 4, dist: 50, desc: "Backstroke — single-arm drill (alternate arms by 25)", interval: "On 1:35", focus: "Lead arm extension" },
          { id: "s_73b24r", reps: 4, dist: 50, desc: "Backstroke — double-arm drill (both arms simultaneously)", interval: "On 1:35", focus: "Symmetry and rotation" },
          { id: "s_8dxd1l", reps: 4, dist: 75, desc: "Backstroke — 25 drill + 50 swim", interval: "On 2:40", focus: "Apply drill into stroke" },
          { id: "s_1te8ww", reps: 4, dist: 100, desc: "Backstroke — full swim, focus on hip rotation", interval: "On 3:10", focus: "Rotate from the core" },
          { id: "s_tqoazq", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breaststroke Timing", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_kyn805", reps: 4, dist: 50, desc: "Breaststroke — pull-pull-kick drill (2 pulls per kick)", interval: "On 1:35", focus: "Isolate pull power" },
          { id: "s_bdcrgo", reps: 4, dist: 50, desc: "Breaststroke — kick-kick-pull drill (2 kicks per pull)", interval: "On 1:35", focus: "Extend the glide" },
          { id: "s_gbzs9t", reps: 4, dist: 75, desc: "Breaststroke — full stroke, slow tempo", interval: "On 2:40", focus: "Smooth pull-glide-kick rhythm" },
          { id: "s_2w35e4", reps: 4, dist: 100, desc: "Breaststroke — full stroke, normal tempo", interval: "On 3:25", focus: "Apply timing at race tempo" },
          { id: "s_rxpq1t", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Butterfly Drill Series", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_i09bvj", reps: 4, dist: 25, desc: "Fly — single-arm drill (left)", interval: "On :50", focus: "Left-arm fly mechanics" },
          { id: "s_usodj8", reps: 4, dist: 25, desc: "Fly — single-arm drill (right)", interval: "On :50", focus: "Right-arm fly mechanics" },
          { id: "s_g96yb7", reps: 4, dist: 50, desc: "Fly — 3-3-3 drill (3R / 3L / 3 full)", interval: "On 1:35", focus: "Build to full stroke" },
          { id: "s_zhz5vk", reps: 4, dist: 50, desc: "Fly — 25 drill / 25 swim", interval: "On 1:35", focus: "Bridge to full fly" },
          { id: "s_lzcjrm", reps: 4, dist: 100, desc: "Fly + Free — 25 fly / 75 free", interval: "On 2:40", focus: "Sustainable fly" },
          { id: "s_e5dqxh", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Push-Off Underwater Focus", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_n1qekk", reps: 8, dist: 25, desc: "Streamline kick on back — 12.5 yd underwater + 12.5 swim", interval: "On 1:05", focus: "Tight streamline + dolphin kick", eq: "underkick" },
          { id: "s_5zggd6", reps: 4, dist: 50, desc: "Streamline kick on back — go for max distance underwater", interval: "On 1:35", focus: "Push the underwater envelope", eq: "underkick" },
          { id: "s_msa1c1", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off, count UW kicks", interval: "On 2:05", focus: "Build streamline habit" },
          { id: "s_8vzeqe", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Stroke Length Test", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_b7mms5", reps: 4, dist: 50, desc: "Freestyle — slowest possible (max stroke length)", interval: "On 1:35", focus: "Maximum distance per stroke" },
          { id: "s_jv0gia", reps: 4, dist: 50, desc: "Freestyle — moderate, hold long stroke", interval: "On 1:20", focus: "Maintain length at tempo" },
          { id: "s_yeogbe", reps: 4, dist: 50, desc: "Freestyle — fast, hold long stroke", interval: "On 1:20", focus: "Length at speed" },
          { id: "s_1a7d5s", reps: 4, dist: 100, desc: "Freestyle — apply length at strong pace", interval: "On 2:20", focus: "Length over distance" },
          { id: "s_urncad", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Hip Rotation Day", totalYards: 1150, types: ["technique"], strokes: [], sets: [
          { id: "s_rjjp1r", reps: 4, dist: 50, desc: "Freestyle — exaggerated hip rotation, slow", interval: "On 1:20", focus: "Drive rotation from the hips" },
          { id: "s_npzae4", reps: 4, dist: 50, desc: "Freestyle — kick on side, 6 kicks per arm-stroke", interval: "On 1:35", focus: "Connect rotation timing" },
          { id: "s_w4mxp0", reps: 4, dist: 75, desc: "Freestyle — 25 hip rotation drill + 50 swim", interval: "On 2:40", focus: "Apply rotation pattern" },
          { id: "s_10du4o", reps: 4, dist: 100, desc: "Freestyle — moderate pace, maintain hip rotation", interval: "On 2:05", focus: "Sustained rotation under pace" },
          { id: "s_ix3uqr", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "Catch & Finish", totalYards: 1150, types: ["technique"], strokes: [], sets: [
          { id: "s_zf7s3y", reps: 4, dist: 50, desc: "Freestyle — fingertip drag drill, focus on high elbow recovery", interval: "On 1:25", focus: "Set up the catch" },
          { id: "s_h8y9km", reps: 4, dist: 50, desc: "Freestyle — fist swimming, feel the forearm pressure", interval: "On 1:20", focus: "Develop forearm catch surface", eq: "fins" },
          { id: "s_lpl0so", reps: 4, dist: 75, desc: "Freestyle — 25 catch drill + 50 swim, lock in early vertical forearm", interval: "On 2:20", focus: "Transfer drill feel to swim" },
          { id: "s_2ji4ak", reps: 4, dist: 100, desc: "Freestyle — moderate, focus on full finish past hip each stroke", interval: "On 2:20", focus: "Complete the pull cycle" },
          { id: "s_esr7b3", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "DPS (Distance Per Stroke)", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_ercyq7", reps: 4, dist: 50, desc: "Freestyle — count strokes (baseline)", interval: "On 1:25", focus: "Baseline" },
          { id: "s_98ca8h", reps: 4, dist: 50, desc: "Freestyle — same speed, target -2 strokes", interval: "On 1:20", focus: "Increase DPS" },
          { id: "s_of4w3o", reps: 4, dist: 50, desc: "Freestyle — same speed, target -3 strokes", interval: "On 1:25", focus: "Maximize DPS" },
          { id: "s_jk9kch", reps: 4, dist: 100, desc: "Freestyle — strong pace, hold target stroke count", interval: "On 2:20", focus: "DPS under pace" },
          { id: "s_6oj3vq", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Pull Pattern Detail", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_lrl4l4", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on early catch", interval: "On 1:35", focus: "Early vertical forearm", eq: "pull" },
          { id: "s_s86d92", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on power phase", interval: "On 1:35", focus: "Mid-pull pressure", eq: "pull" },
          { id: "s_kuna3u", reps: 4, dist: 50, desc: "Pull (buoy) — slow, focus on finish/release", interval: "On 1:35", focus: "Complete the stroke", eq: "pull" },
          { id: "s_mss2du", reps: 4, dist: 100, desc: "Freestyle — integrate the full pull pattern", interval: "On 2:05", focus: "Apply detail to swim" },
          { id: "s_ykwqqh", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Backstroke Body Position", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_v6zeop", reps: 4, dist: 50, desc: "Backstroke kick — head still, hips up", interval: "On 1:35", focus: "Body line on back", eq: "kick" },
          { id: "s_scv4ln", reps: 4, dist: 50, desc: "Backstroke — single-arm drill", interval: "On 1:35", focus: "Rotation drives stroke" },
          { id: "s_fh8n4j", reps: 4, dist: 75, desc: "Backstroke — 25 drill + 50 swim", interval: "On 2:40", focus: "Bridge drill to stroke" },
          { id: "s_ghv5gu", reps: 4, dist: 75, desc: "Backstroke — full stroke, hold tight body line", interval: "On 2:40", focus: "Maintain position under pace" },
          { id: "s_to3q5y", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Open-Turn Practice", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_ivhu3n", reps: 8, dist: 50, desc: "Breaststroke — focus on legal touch + open turn", interval: "On 1:35", focus: "Sharp open-turn mechanics" },
          { id: "s_e2b6uc", reps: 8, dist: 50, desc: "Butterfly — focus on legal touch + open turn", interval: "On 1:35", focus: "Fly open-turn mechanics" },
          { id: "s_g6damr", reps: 4, dist: 50, desc: "Choice — IM-transition turn (e.g., back→breast)", interval: "On 1:35", focus: "IM transition turns" },
          { id: "s_nq4che", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Flip Turn Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_9hfsjg", reps: 12, dist: 50, desc: "Freestyle — focus on tight flip turns, fast push-off", interval: "On 1:20", focus: "Fast efficient flip turns" },
          { id: "s_cr5gc6", reps: 4, dist: 75, desc: "Freestyle — turn into 3 strong strokes off wall", interval: "On 1:50", focus: "Explosive push-off + first strokes" },
          { id: "s_hofjbh", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Slow-Motion Stroke", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_z94x87", reps: 4, dist: 100, desc: "Freestyle — SLOWEST possible, perfect mechanics", interval: "On 3:10", focus: "Deliberate practice — every motion intentional" },
          { id: "s_xx0kc4", reps: 4, dist: 75, desc: "Choice non-free — slow, perfect form", interval: "On 2:05", focus: "Apply slowness to stroke" },
          { id: "s_79p59l", reps: 4, dist: 50, desc: "Choice — slow, focus on connection", interval: "On 1:35", focus: "Feel the catch" },
          { id: "s_f36ieb", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Side-Kick + Switch", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_20qa9x", reps: 6, dist: 50, desc: "Kick on side — 25 left, 25 right", interval: "On 1:35", focus: "Side balance", eq: "kick" },
          { id: "s_kqvy33", reps: 6, dist: 50, desc: "6-kick switch drill (6 kicks before each switch)", interval: "On 1:25", focus: "Connect kick to stroke", eq: "kickPart" },
          { id: "s_v73i77", reps: 4, dist: 100, desc: "Freestyle — apply rotation timing", interval: "On 2:05", focus: "Translate to swim" },
        ]},
      { label: "All-Stroke Slow Day", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_a8qxo5", reps: 4, dist: 100, desc: "Butterfly + Free (25 fly / 75 free) — slow", interval: "On 2:40", focus: "Fly form at slow tempo" },
          { id: "s_8nt36r", reps: 4, dist: 100, desc: "Backstroke — slow, perfect form", interval: "On 2:40", focus: "Back form" },
          { id: "s_135s69", reps: 2, dist: 100, desc: "Breaststroke — slow, glide-focused", interval: "On 3:25", focus: "Breast form" },
          { id: "s_cn0neo", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Counted-Stroke 100s", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_b3or4b", reps: 8, dist: 100, desc: "Freestyle — count strokes per 25, hold same count all 4 lengths", interval: "On 2:05", focus: "Stroke count discipline" },
          { id: "s_lvjjaq", reps: 4, dist: 50, desc: "Freestyle — count strokes -1 from 100s baseline", interval: "On 1:20", focus: "Reduced stroke count" },
        ]},
      { label: "Catch + Kick Mix", totalYards: 1200, types: ["technique"], strokes: [], sets: [
          { id: "s_b17tvr", reps: 4, dist: 50, desc: "Pull (buoy) — focus on catch", interval: "On 1:35", focus: "Catch detail", eq: "pull" },
          { id: "s_xkiogd", reps: 4, dist: 50, desc: "Kick — moderate (board optional)", interval: "On 1:50", focus: "Leg engagement", eq: "kick" },
          { id: "s_euc1aa", reps: 4, dist: 75, desc: "25 pull / 25 kick / 25 swim — same stroke", interval: "On 2:40", focus: "Apply both into swim" },
          { id: "s_0g0jgs", reps: 4, dist: 100, desc: "Freestyle — full stroke, integrate everything", interval: "On 2:05", focus: "Whole-stroke integration" },
          { id: "s_0knxbt", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Reverse Stroke Practice", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_uav3dg", reps: 4, dist: 50, desc: "Freestyle — reverse stroke (start at finish, work backward through phases)", interval: "On 1:35", focus: "Understand stroke phases by reversing" },
          { id: "s_wbiqcc", reps: 4, dist: 50, desc: "Freestyle — exaggerated normal-direction stroke", interval: "On 1:20", focus: "Apply phase awareness forward" },
          { id: "s_4vsu8q", reps: 4, dist: 100, desc: "Freestyle — moderate pace, mindful of each phase", interval: "On 2:20", focus: "Phase-aware swimming" },
          { id: "s_22nkwo", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Tempo Trainer Mock", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_85bvvh", reps: 4, dist: 50, desc: "Freestyle — slow, deliberate tempo (count seconds per stroke)", interval: "On 1:20", focus: "Slow tempo" },
          { id: "s_gklc1t", reps: 4, dist: 50, desc: "Freestyle — increase tempo by 1 'click' (faster turnover)", interval: "On 1:20", focus: "Faster tempo" },
          { id: "s_kp90h2", reps: 4, dist: 50, desc: "Freestyle — race tempo, hold count over longer reps", interval: "On 1:20", focus: "Race tempo" },
          { id: "s_9bw1td", reps: 4, dist: 100, desc: "Freestyle — moderate, tempo-aware", interval: "On 2:05", focus: "Apply tempo awareness" },
          { id: "s_i2n9lw", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Streamline Volume", totalYards: 1100, types: ["technique"], strokes: [], sets: [
          { id: "s_cs4c2g", reps: 12, dist: 25, desc: "Streamline kick + 1 stroke (10m UW + freestyle finish)", interval: "On :45", focus: "Long underwater off every wall", eq: "underkick" },
          { id: "s_7pcyif", reps: 4, dist: 100, desc: "Freestyle — focus on every push-off and underwater", interval: "On 2:05", focus: "Build streamline habit" },
          { id: "s_d77ed6", reps: 4, dist: 75, desc: "Freestyle — 25 underwater dolphin + 50 swim", interval: "On 1:50", focus: "Underwater + swim", eq: "underkick" },
          { id: "s_t6rl3v", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Connection Day", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_yj9n6f", reps: 4, dist: 50, desc: "Freestyle — feel hand-to-hip connection (one stroke at a time)", interval: "On 1:35", focus: "Whole-body connection" },
          { id: "s_9990q0", reps: 4, dist: 75, desc: "Freestyle — apply connection at moderate tempo", interval: "On 1:50", focus: "Connection at pace" },
          { id: "s_l7st2a", reps: 4, dist: 100, desc: "Freestyle — apply connection at strong pace", interval: "On 2:05", focus: "Connection at speed" },
          { id: "s_8vm9jf", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Front Quadrant Drill", totalYards: 1000, types: ["technique"], strokes: [], sets: [
          { id: "s_q0e07l", reps: 8, dist: 50, desc: "Catch-up freestyle — exaggerate front-quadrant timing", interval: "On 1:35", focus: "Both arms in front quadrant" },
          { id: "s_qquxni", reps: 4, dist: 75, desc: "Freestyle — slight catch-up timing at moderate pace", interval: "On 2:40", focus: "Apply timing in stroke" },
          { id: "s_g973n4", reps: 3, dist: 100, desc: "Freestyle — front-quadrant aware at strong pace", interval: "On 2:05", focus: "Sustain timing under speed" },
        ]},
      { label: "Body Line Mastery", totalYards: 1050, types: ["technique"], strokes: [], sets: [
          { id: "s_pyqte8", reps: 4, dist: 50, desc: "Streamline kick on back — head still, hips up", interval: "On 1:35", focus: "Body line on back", eq: "kick" },
          { id: "s_6sutf2", reps: 4, dist: 50, desc: "Streamline kick on front — head down", interval: "On 1:35", focus: "Body line on front", eq: "kick" },
          { id: "s_infa3t", reps: 4, dist: 75, desc: "Freestyle — focus on hairline at the surface", interval: "On 1:50", focus: "Neutral head position reduces drag" },
          { id: "s_16co6p", reps: 3, dist: 100, desc: "Freestyle — strong pace, hold body line", interval: "On 2:05", focus: "Body line under speed" },
          { id: "s_cq02op", reps: 1, dist: 50, desc: "Easy choice — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "Technique 10×150", totalYards: 1500, types: ["technique"], strokes: [], sets: [
          { id: "s_rei9c4", reps: 10, dist: 150, desc: "Freestyle — 50 drill (choice) / 50 swim / 50 strong swim — apply drill into speed", interval: "On 3:25", focus: "High-rep drill-to-swim transfer across volume" },
        ]},
      { label: "Technique 20×100", totalYards: 2000, types: ["technique"], strokes: [], sets: [
          { id: "s_38o8b0", reps: 20, dist: 100, desc: "Freestyle — 50 drill / 50 swim — one technical focus for the entire set", interval: "On 2:20", focus: "Sustained drill-to-swim practice at volume" },
        ]},
      { label: "Technique 8×300", totalYards: 2400, types: ["technique"], strokes: [], sets: [
          { id: "s_asyl9w", reps: 8, dist: 300, desc: "Freestyle — 100 drill / 100 build / 100 strong — escalate quality each section", interval: "On 6:50", focus: "Extended technique integration at high volume" },
        ]},
      { label: "Drill-Swim 50s Repeat Unit", totalYards: 400, types: ["technique"], strokes: [], sets: [
          { id: "s_nht1f3", reps: 8, dist: 50, desc: "25 drill / 25 swim — one focus per rep (catch, rotation, reach, finish)", interval: "On 1:25", focus: "Apply each drill point directly into the swim — designed to repeat 2–3×" },
        ]},
      { label: "Technique 75s Repeat Unit", totalYards: 525, types: ["technique"], strokes: [], sets: [
          { id: "s_0fp0ps", reps: 7, dist: 75, desc: "25 drill / 25 build / 25 strong — escalate quality within each 75", interval: "On 1:50", focus: "Drill-to-swim integration at low yardage" },
        ]},
      { label: "Catch-Up Build Repeat Unit", totalYards: 600, types: ["technique"], strokes: [], sets: [
          { id: "s_71juua", reps: 4, dist: 100, desc: "Freestyle — 50 catch-up drill / 50 swim, focus on full extension before pull", interval: "On 2:25", focus: "Extension and catch mechanics" },
          { id: "s_kn7r2u", reps: 4, dist: 50, desc: "Freestyle — swim normal, hold the feeling from the drill", interval: "On 1:20", focus: "Transfer drill to full swimming" },
        ]},
      { label: "Compact Mix", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_moyf60", reps: 3, dist: 200, desc: "Freestyle — aerobic, descend 1→3", interval: "On 4:30", focus: "Distance base at controlled effort" },
          { id: "s_3mefe9", reps: 4, dist: 100, desc: "IM or choice — moderate to fast, descend", interval: "On 2:05", focus: "Mix strokes, build intensity" },
        ]},
      { label: "Speed + Distance Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_lvj519", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:45", focus: "Distance base — stay aerobic" },
          { id: "s_2ckrfv", reps: 1, dist: 200, desc: "Easy backstroke — active recovery", interval: "4:00 before next block", focus: "Recover while moving" },
          { id: "s_0bk7dz", reps: 8, dist: 50, desc: "Choice of stroke — ALL OUT sprint", interval: "On 1:50", focus: "Speed work after accumulated fatigue" },
        ]},
      { label: "3-Part Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_1tt9vu", reps: 3, dist: 200, desc: "Freestyle — strong aerobic", interval: "On 4:30", focus: "Build aerobic capacity" },
          { id: "s_s4ofok", reps: 6, dist: 50, desc: "25 Drill / 25 Swim — stroke choice", interval: "On 1:30", focus: "Technique reset mid-workout" },
          { id: "s_eea78b", reps: 3, dist: 100, desc: "IM or stroke choice — moderate to fast, descend 1→3", interval: "On 2:05", focus: "Finish strong on the last one" },
        ]},
      { label: "Mixed Pyramid", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_acysie", reps: 1, dist: 400, desc: "Freestyle — aerobic, controlled effort", interval: "On 8:55", focus: "Establish aerobic base" },
          { id: "s_yypg97", reps: 2, dist: 200, desc: "Choice of stroke — moderate to fast", interval: "On 4:30", focus: "Pick up the pace, find threshold" },
          { id: "s_0smp10", reps: 4, dist: 100, desc: "IM or choice — hard effort", interval: "On 2:05", focus: "Push lactate threshold" },
          { id: "s_ii5mn4", reps: 4, dist: 50, desc: "Sprint — choice stroke, ALL OUT", interval: "On 1:35", focus: "Top-end speed" },
        ]},
      { label: "Pull + Kick Mix", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_hguotl", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:20", focus: "Upper-body block", eq: "pull" },
          { id: "s_zf19yn", reps: 1, dist: 100, desc: "Easy freestyle — transition", interval: "On 2:05", focus: "Reset between blocks" },
          { id: "s_g2sbcz", reps: 4, dist: 50, desc: "Kick — moderate effort (board optional)", interval: "On 1:50", focus: "Lower-body block", eq: "kick" },
          { id: "s_idt9ju", reps: 4, dist: 100, desc: "Freestyle — strong, integrate both", interval: "On 2:20", focus: "Apply pull + kick into full stroke" },
        ]},
      { label: "Sprint Recovery Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_ef0bbr", reps: 6, dist: 100, desc: "50 ALL OUT / 50 easy (immediately)", interval: "On 2:05", focus: "Practice rapid recovery between hard efforts" },
          { id: "s_j9s4kx", reps: 1, dist: 200, desc: "Easy backstroke — full recovery", interval: "On 5:45", focus: "Active recovery between blocks" },
          { id: "s_sf42ex", reps: 4, dist: 100, desc: "Freestyle — strong, hold near-threshold pace", interval: "On 2:20", focus: "Sustained effort with the lactate already there" },
        ]},
      { label: "IM-Free-Choice Triplets", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_d5slra", reps: 4, dist: 100, desc: "IM — moderate, focus on transitions", interval: "On 3:10", focus: "Smooth stroke order under control" },
          { id: "s_cd1xoz", reps: 4, dist: 100, desc: "Freestyle — descend 1→4 (each faster)", interval: "On 2:15", focus: "Build pace round to round" },
          { id: "s_q046l3", reps: 4, dist: 100, desc: "Choice stroke (non-free) — strong, hold form", interval: "On 2:15", focus: "Pick your weakest stroke for variety" },
        ]},
      { label: "Race Day Simulation", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_qgzac0", reps: 1, dist: 200, desc: "Freestyle — easy warm-up swim", interval: "On 6:20", focus: "Settle in" },
          { id: "s_eyebf7", reps: 1, dist: 100, desc: "Choice stroke — race pace test", interval: "On 3:10", focus: "First race effort" },
          { id: "s_5sqmre", reps: 1, dist: 200, desc: "Easy choice — recovery between races", interval: "No interval — swim easy", focus: "Reset between events" },
          { id: "s_e66yxu", reps: 1, dist: 200, desc: "IM — race pace simulation", interval: "On 5:15", focus: "Second race effort" },
          { id: "s_n2mkjy", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_0tbhsz", reps: 1, dist: 50, desc: "Choice stroke — sprint finish", interval: "On 2:05", focus: "Final race" },
          { id: "s_ccxn3a", reps: 1, dist: 250, desc: "Easy choice — final cool", interval: "No interval — swim easy", focus: "Aerobic recovery" },
        ]},
      { label: "Quad Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_wgzddj", reps: 6, dist: 50, desc: "Kick — choice stroke (board optional)", interval: "On 1:50", focus: "Leg block", eq: "kick" },
          { id: "s_wi36vj", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:20", focus: "Pull block", eq: "pull" },
          { id: "s_dsdnuf", reps: 4, dist: 50, desc: "25 Drill / 25 Swim — choice stroke", interval: "On 1:35", focus: "Drill block" },
          { id: "s_bfep0o", reps: 3, dist: 100, desc: "Freestyle — strong, integrate all", interval: "On 2:20", focus: "Apply all four phases into stroke" },
        ]},
      { label: "Variety Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_4kkqro", reps: 1, dist: 50, desc: "Butterfly — easy/moderate", interval: "On 1:35", focus: "Open up fly" },
          { id: "s_txrdve", reps: 2, dist: 100, desc: "Backstroke — moderate", interval: "On 2:55", focus: "Settle into back" },
          { id: "s_ixuc39", reps: 2, dist: 150, desc: "Breaststroke — moderate", interval: "On 3:10", focus: "Hold breast tempo" },
          { id: "s_kntnxu", reps: 2, dist: 200, desc: "Freestyle — strong", interval: "On 4:30", focus: "Push the freestyle" },
          { id: "s_qaly6s", reps: 1, dist: 250, desc: "IM — moderate to strong", interval: "On 5:45", focus: "All four strokes under fatigue" },
        ]},
      { label: "Speed + Recovery Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_4kugef", reps: 4, dist: 200, desc: "100 ALL OUT / 100 easy (immediate)", interval: "On 6:20", focus: "Hard/easy alternation" },
          { id: "s_nchi5n", reps: 4, dist: 100, desc: "Choice — strong, hold quality stroke", interval: "On 2:05", focus: "Quality after fatigue" },
        ]},
      { label: "Threshold + Sprint", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_a6ydmd", reps: 4, dist: 200, desc: "Freestyle — threshold pace (RPE 8/10)", interval: "On 4:30", focus: "Build threshold base" },
          { id: "s_c4iyh2", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_l0921v", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:35", focus: "Sprint after threshold accumulation" },
        ]},
      { label: "Aerobic + IM Combo", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_honvhh", reps: 1, dist: 400, desc: "Freestyle — steady aerobic effort", interval: "On 8:55", focus: "Aerobic base" },
          { id: "s_g0vr92", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:10", focus: "IM after aerobic load" },
          { id: "s_s8jzr2", reps: 4, dist: 100, desc: "Freestyle — descending pace 1→4", interval: "On 2:15", focus: "Build pace round to round" },
        ]},
      { label: "Pull + Sprint Day", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_p6xzms", reps: 6, dist: 100, desc: "Pull (buoy) — moderate to strong, build pace", interval: "On 2:20", focus: "Build upper-body strength", eq: "pull" },
          { id: "s_7jmyvz", reps: 1, dist: 200, desc: "Easy choice — transition", interval: "On 4:10", focus: "Reset" },
          { id: "s_fdimp0", reps: 6, dist: 50, desc: "Choice stroke — fast sprints", interval: "On 1:35", focus: "Apply pull strength to sprints" },
        ]},
      { label: "Long + Short Day", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_lse2bw", reps: 1, dist: 500, desc: "Freestyle — steady aerobic", interval: "On 11:35", focus: "Long base" },
          { id: "s_7e6kmb", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_hf9tl0", reps: 12, dist: 50, desc: "Choice stroke — descend by 4 (1-4 fast, 5-8 faster, 9-12 fastest)", interval: "On 1:20", focus: "Short fast 50s" },
        ]},
      { label: "Distance + Drill Combo", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_8ifms7", reps: 1, dist: 400, desc: "Freestyle — steady aerobic", interval: "On 8:55", focus: "Distance base" },
          { id: "s_u3einy", reps: 8, dist: 50, desc: "25 Drill / 25 Swim — choice stroke", interval: "On 1:35", focus: "Mid-workout technique reset" },
          { id: "s_4re3ok", reps: 3, dist: 100, desc: "Freestyle — strong, apply drill into stroke", interval: "On 2:20", focus: "Carry technique to fast swimming" },
        ]},
      { label: "All-Stroke Tour", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_sc448r", reps: 3, dist: 100, desc: "Butterfly (or 25 fly + 75 free)", interval: "On 2:40", focus: "Fly endurance" },
          { id: "s_bkyuiq", reps: 3, dist: 100, desc: "Backstroke — moderate, smooth pace", interval: "On 2:55", focus: "Back tempo" },
          { id: "s_iusw9k", reps: 3, dist: 100, desc: "Breaststroke — moderate to strong", interval: "On 3:25", focus: "Breast tempo" },
          { id: "s_f2x8gd", reps: 3, dist: 100, desc: "Freestyle — strong finishing pace", interval: "On 2:20", focus: "Free endurance" },
        ]},
      { label: "Descending Mixed Ladder", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_idbs3y", reps: 1, dist: 400, desc: "Freestyle — aerobic, establish your pace", interval: "On 8:55", focus: "Anchor the ladder — steady aerobic effort" },
          { id: "s_05yp8s", reps: 1, dist: 300, desc: "Backstroke or choice — match free pace", interval: "On 6:50", focus: "Stroke change keeps it honest" },
          { id: "s_0em77x", reps: 1, dist: 200, desc: "Freestyle — strong, find another gear", interval: "On 4:30", focus: "Negative split into the descent" },
          { id: "s_a600wp", reps: 1, dist: 100, desc: "Choice stroke — sprint, finish strongest", interval: "On 2:20", focus: "Empty the tank" },
        ]},
      { label: "Mixed Ascending Ladder", totalYards: 1000, types: ["mixed"], strokes: [], sets: [
          { id: "s_0jn6cz", reps: 1, dist: 100, desc: "Choice stroke — controlled sprint, wake up fast-twitch", interval: "On 2:20", focus: "Start sharp before the distance builds" },
          { id: "s_ua5dg6", reps: 1, dist: 200, desc: "100 IM + 100 free — moderate, smooth transitions", interval: "On 4:45", focus: "Add IM complexity as volume grows" },
          { id: "s_ionyun", reps: 1, dist: 300, desc: "Freestyle — moderate to strong, 3 × 100 in your head", interval: "On 6:50", focus: "Sustain pace over longer distance" },
          { id: "s_mx8zn6", reps: 1, dist: 400, desc: "Choice stroke — strong, finish better than you started", interval: "On 9:25", focus: "Top of the ladder — best effort of the set" },
        ]},
      { label: "Sprint + Distance Bookends", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_8ls2tr", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:35", focus: "Open with speed" },
          { id: "s_wqfvu6", reps: 1, dist: 400, desc: "Freestyle — moderate aerobic", interval: "On 8:55", focus: "Distance middle" },
          { id: "s_vuafex", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (after distance)", interval: "On 1:35", focus: "Speed under fatigue" },
        ]},
      { label: "Threshold + Sprint Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_kg8yop", reps: 4, dist: 200, desc: "Freestyle — threshold pace", interval: "On 4:30", focus: "Threshold base" },
          { id: "s_3jxli5", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT (after threshold)", interval: "On 1:35", focus: "Sprint after lactate accumulation" },
        ]},
      { label: "Pull-Drill-Sprint Block", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_nupa4j", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, focus on catch", interval: "On 2:20", focus: "Pull block", eq: "pull" },
          { id: "s_c6md5a", reps: 4, dist: 50, desc: "Drill — choice stroke, technique focus", interval: "On 1:20", focus: "Drill block" },
          { id: "s_36duei", reps: 1, dist: 100, desc: "Easy freestyle — transition", interval: "On 2:05", focus: "Reset" },
          { id: "s_4h2e7t", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:35", focus: "Sprint block" },
        ]},
      { label: "All-Stroke Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_aj3r0j", reps: 1, dist: 50, desc: "Butterfly — moderate", interval: "On 1:35", focus: "Open fly" },
          { id: "s_u7dxlo", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:55", focus: "Back" },
          { id: "s_j9j0u3", reps: 1, dist: 150, desc: "Breaststroke — moderate", interval: "On 3:10", focus: "Breast" },
          { id: "s_g15ijm", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:30", focus: "Free" },
          { id: "s_xdrqr3", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:20", focus: "All four" },
          { id: "s_lx8qlf", reps: 1, dist: 150, desc: "Breaststroke — strong (match earlier)", interval: "On 3:10", focus: "Same pace under fatigue" },
          { id: "s_ekknkr", reps: 1, dist: 100, desc: "Backstroke — strong", interval: "On 2:55", focus: "Pick up pace" },
          { id: "s_id79tc", reps: 1, dist: 50, desc: "Butterfly — ALL OUT (best 50)", interval: "On 1:35", focus: "Final blast" },
          { id: "s_8m1l7i", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "5-Way Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_or6ae8", reps: 1, dist: 200, desc: "Freestyle — moderate aerobic", interval: "On 4:45", focus: "Aerobic phase" },
          { id: "s_8nua55", reps: 1, dist: 200, desc: "IM — moderate, smooth transitions", interval: "On 6:20", focus: "IM phase" },
          { id: "s_zyt1v6", reps: 1, dist: 200, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 4:30", focus: "Pull phase", eq: "pull" },
          { id: "s_f06a24", reps: 1, dist: 200, desc: "Kick — choice stroke (board optional)", interval: "On 4:45", focus: "Kick phase", eq: "kick" },
          { id: "s_oatlq1", reps: 1, dist: 200, desc: "Choice stroke — strong, integrate it all", interval: "On 4:10", focus: "Apply phase" },
          { id: "s_uxlkpe", reps: 1, dist: 200, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Negative-Split Distance + Sprint", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_wjq7uc", reps: 2, dist: 400, desc: "Freestyle — negative split (second 200 faster)", interval: "On 8:55", focus: "Practice closing acceleration" },
          { id: "s_ptueko", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT sprint", interval: "On 1:35", focus: "Sprint after distance" },
        ]},
      { label: "IM + Free Sprints", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_3czeux", reps: 3, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:20", focus: "IM base" },
          { id: "s_wmo8k1", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Reset" },
          { id: "s_l5ghxh", reps: 8, dist: 50, desc: "Freestyle — fast, descend 1-4 then 5-8", interval: "On 1:20", focus: "Two-block free sprints" },
        ]},
      { label: "Distance + Stroke Variety", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_sxyrod", reps: 1, dist: 400, desc: "Freestyle — aerobic", interval: "On 8:55", focus: "Aerobic base" },
          { id: "s_9mpw85", reps: 4, dist: 100, desc: "Choice non-free stroke — moderate", interval: "On 2:40", focus: "Stroke variety" },
          { id: "s_lz9145", reps: 1, dist: 200, desc: "IM — moderate", interval: "On 6:20", focus: "Mid-set IM" },
          { id: "s_3xqhx2", reps: 3, dist: 100, desc: "Freestyle — strong, descend", interval: "On 2:20", focus: "Closing pace" },
        ]},
      { label: "Hard-Easy 200s", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_5kkedu", reps: 6, dist: 200, desc: "Odd reps strong (RPE 8) / even reps easy (RPE 5)", interval: "On 4:45", focus: "Practice rapid-recovery alternation" },
        ]},
      { label: "Fartlek Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_2a6cdy", reps: 1, dist: 1200, desc: "Continuous freestyle — fartlek (vary pace every 100: easy/moderate/hard, your choice each rep)", interval: "On 27:20", focus: "Self-paced variety over distance" },
        ]},
      { label: "Pull + IM Sandwich", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_3z1rdo", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, smooth", interval: "On 2:20", focus: "Pull base", eq: "pull" },
          { id: "s_b9j82z", reps: 4, dist: 100, desc: "100 IM — moderate, smooth transitions", interval: "On 3:10", focus: "IM middle" },
          { id: "s_kh4op6", reps: 4, dist: 100, desc: "Pull (buoy) — strong, descend 1→4", interval: "On 2:20", focus: "Pull build", eq: "pull" },
        ]},
      { label: "Specialty + Sprint Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_181rlp", reps: 4, dist: 100, desc: "Specialty stroke (your choice) — race-pace test", interval: "On 2:40", focus: "Race-distance specialty" },
          { id: "s_om6sw3", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_prvx3i", reps: 4, dist: 75, desc: "Choice — first 50 fast / last 25 ALL OUT", interval: "On 2:05", focus: "Negative-split sprint" },
          { id: "s_qwabg9", reps: 4, dist: 50, desc: "Choice — ALL OUT", interval: "On 1:35", focus: "Top-off speed" },
          { id: "s_o4oy31", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Race-Day Three Events", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_4ab526", reps: 1, dist: 200, desc: "Easy warm-up swim", interval: "On 6:20", focus: "Race-day feel" },
          { id: "s_crqs9n", reps: 1, dist: 200, desc: "Choice stroke — race pace (Event 1)", interval: "On 5:15", focus: "First race effort" },
          { id: "s_omk5o4", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Inter-event recovery" },
          { id: "s_f9ij2x", reps: 1, dist: 100, desc: "Choice stroke — race pace (Event 2)", interval: "On 3:10", focus: "Sprint race" },
          { id: "s_ak57iq", reps: 1, dist: 200, desc: "Easy choice — between events", interval: "No interval — swim easy", focus: "Recover" },
          { id: "s_5k6e7r", reps: 1, dist: 200, desc: "IM — race-pace simulation (Event 3)", interval: "On 5:15", focus: "IM race" },
          { id: "s_ull1fw", reps: 1, dist: 100, desc: "Easy choice — close-out", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Aerobic + Threshold Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_3hh1vi", reps: 1, dist: 600, desc: "Freestyle — aerobic, even effort", interval: "On 14:10", focus: "Aerobic base" },
          { id: "s_i5sny8", reps: 4, dist: 100, desc: "Freestyle — threshold pace, descend 1→4", interval: "On 2:20", focus: "Build threshold" },
          { id: "s_fobo93", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Three-Stroke Day (no fly)", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_zt21in", reps: 4, dist: 100, desc: "Backstroke — moderate to strong, descend 1→4", interval: "On 2:55", focus: "Back" },
          { id: "s_v9c1mb", reps: 4, dist: 100, desc: "Breaststroke — moderate to strong, descend 1→4", interval: "On 3:25", focus: "Breast" },
          { id: "s_72tsey", reps: 4, dist: 100, desc: "Freestyle — strong, descend 1→4", interval: "On 2:20", focus: "Free" },
        ]},
      { label: "Drill + Pull + Swim", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_ndnedy", reps: 4, dist: 50, desc: "Drill — choice stroke, technique focus", interval: "On 1:20", focus: "Drill block" },
          { id: "s_mlqjfl", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, smooth long stroke", interval: "On 2:20", focus: "Pull block", eq: "pull" },
          { id: "s_dwxnrs", reps: 4, dist: 50, desc: "Drill — same stroke as pull", interval: "On 1:20", focus: "Reapply drill after pull" },
          { id: "s_53efor", reps: 4, dist: 100, desc: "Choice stroke — strong, integrate everything", interval: "On 2:05", focus: "Whole-stroke integration" },
        ]},
      { label: "Race-Pace Mix", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_3cmfpo", reps: 4, dist: 100, desc: "Choice stroke — race pace (full recovery)", interval: "On 3:10", focus: "Race-pace 100s" },
          { id: "s_g2t2vq", reps: 4, dist: 50, desc: "Choice stroke — race pace", interval: "On 1:35", focus: "Race-pace 50s" },
          { id: "s_bccn4w", reps: 4, dist: 75, desc: "Choice stroke — race pace", interval: "On 2:05", focus: "Race-pace 75s" },
          { id: "s_fqzt80", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Long + Sprint Cycles", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_0z5hi2", reps: 4, dist: 250, desc: "Freestyle 200 moderate + 50 ALL OUT (immediate)", interval: "On 5:15", focus: "Sprint after each distance segment" },
          { id: "s_8mplaf", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Sprint + Pull + Free", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_2yhpsq", reps: 8, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Open sprint" },
          { id: "s_foji32", reps: 4, dist: 100, desc: "Pull (buoy) — strong, sustain pace", interval: "On 2:20", focus: "Pull middle", eq: "pull" },
          { id: "s_0xjmqs", reps: 4, dist: 75, desc: "Freestyle — fast, integrate", interval: "On 1:50", focus: "Apply pull power" },
          { id: "s_rk4841", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Quad-Set Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_wud4qv", reps: 4, dist: 50, desc: "Round 1: choice stroke — ALL OUT 50s", interval: "On 1:35", focus: "Sprint round" },
          { id: "s_t1x99o", reps: 4, dist: 75, desc: "Round 2: choice stroke — fast 75s", interval: "On 1:50", focus: "Race-distance round" },
          { id: "s_ee8xwq", reps: 4, dist: 100, desc: "Round 3: choice stroke — strong 100s", interval: "On 2:05", focus: "Endurance round" },
          { id: "s_lncti8", reps: 4, dist: 75, desc: "Round 4: choice stroke — moderate 75s", interval: "On 1:50", focus: "Recovery round" },
          { id: "s_ylxct2", reps: 4, dist: 50, desc: "Round 5: choice stroke — easy 50s", interval: "On 1:35", focus: "Cool round" },
        ]},
      { label: "IM Threshold + Sprints", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_gllqn0", reps: 4, dist: 200, desc: "200 IM — threshold pace", interval: "On 6:20", focus: "IM threshold" },
          { id: "s_xyvg2u", reps: 4, dist: 50, desc: "Freestyle — ALL OUT, max turnover", interval: "On 1:35", focus: "Sprint after IM" },
          { id: "s_umbjec", reps: 1, dist: 200, desc: "Easy backstroke — close", interval: "On 5:45", focus: "Cool down" },
        ]},
      { label: "Long-to-Short Pyramid", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_mmo2ug", reps: 1, dist: 400, desc: "Freestyle — aerobic", interval: "On 8:55", focus: "Aerobic base" },
          { id: "s_gean58", reps: 1, dist: 300, desc: "Freestyle — moderate", interval: "On 6:50", focus: "Build" },
          { id: "s_8b65e7", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:30", focus: "Threshold" },
          { id: "s_jfc12q", reps: 1, dist: 100, desc: "Freestyle — fast", interval: "On 2:20", focus: "Sub-threshold" },
          { id: "s_av3gxj", reps: 4, dist: 50, desc: "Choice stroke — ALL OUT", interval: "On 1:35", focus: "Sprint close-out" },
        ]},
      { label: "Stroke Tempo Day", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_wdbxhr", reps: 4, dist: 100, desc: "Backstroke — moderate", interval: "On 2:55", focus: "Back tempo" },
          { id: "s_lvs1ge", reps: 4, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:25", focus: "Breast tempo" },
          { id: "s_tm3zcd", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:20", focus: "Free tempo" },
          { id: "s_vl5mt2", reps: 4, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Fly speed bursts" },
          { id: "s_zlson6", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Pull/Kick Alternation", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_2hea7p", reps: 6, dist: 100, desc: "Pull (buoy) — moderate", interval: "On 2:20", focus: "Pull volume", eq: "pull" },
          { id: "s_4xqolm", reps: 6, dist: 100, desc: "Kick — choice stroke (board optional)", interval: "On 2:40", focus: "Kick volume", eq: "kick" },
        ]},
      { label: "Test-Set Sandwich", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_rincyg", reps: 1, dist: 200, desc: "Freestyle — easy warm-up", interval: "On 4:10", focus: "Open up" },
          { id: "s_lqzmou", reps: 1, dist: 500, desc: "Freestyle — TEST (race-pace 500)", interval: "On 11:35", focus: "Race-pace test" },
          { id: "s_wws0bi", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:45", focus: "Reset" },
          { id: "s_org9di", reps: 4, dist: 75, desc: "Choice stroke — fast finishers", interval: "On 1:50", focus: "Speed work after test" },
        ]},
      { label: "Sprint Endurance Mix", totalYards: 1200, types: ["mixed"], strokes: [], sets: [
          { id: "s_nh0bcr", reps: 12, dist: 75, desc: "Choice stroke — fast (race pace), short rest", interval: "On 1:35", focus: "Sprint endurance volume" },
          { id: "s_dshr1t", reps: 4, dist: 75, desc: "Easy choice — close", interval: "On 1:50", focus: "Cool down" },
        ]},
      { label: "All-Around Day", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_23un2b", reps: 1, dist: 300, desc: "Freestyle — moderate aerobic", interval: "On 6:50", focus: "Aerobic" },
          { id: "s_73qo6r", reps: 4, dist: 100, desc: "100 IM — moderate", interval: "On 3:10", focus: "IM" },
          { id: "s_c9zzbk", reps: 4, dist: 100, desc: "Pull (buoy) — moderate, descend 1→4", interval: "On 2:20", focus: "Pull build", eq: "pull" },
          { id: "s_o3c9ec", reps: 4, dist: 25, desc: "Choice stroke — ALL OUT", interval: "On 1:05", focus: "Sprint" },
          { id: "s_kj2dr8", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Distance Endurance + Speed", totalYards: 1300, types: ["mixed"], strokes: [], sets: [
          { id: "s_ettzm9", reps: 1, dist: 800, desc: "Freestyle — steady aerobic", interval: "On 17:50", focus: "Long base" },
          { id: "s_n8vh4n", reps: 4, dist: 100, desc: "Freestyle — fast, descend 1→4", interval: "On 2:20", focus: "Sub-threshold finish" },
          { id: "s_tkhaoa", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Stroke 50/100 Mix", totalYards: 1400, types: ["mixed"], strokes: [], sets: [
          { id: "s_g657va", reps: 4, dist: 50, desc: "Butterfly — fast (or 25 fly + 25 free)", interval: "On 1:35", focus: "Fly speed" },
          { id: "s_auak6a", reps: 4, dist: 100, desc: "Backstroke — moderate to strong", interval: "On 2:55", focus: "Back tempo" },
          { id: "s_ytzt38", reps: 4, dist: 50, desc: "Breaststroke — fast", interval: "On 1:35", focus: "Breast speed" },
          { id: "s_y9v4fe", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:20", focus: "Free tempo" },
          { id: "s_hks4eo", reps: 4, dist: 50, desc: "Easy choice — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "Pyramid + Kick Finish", totalYards: 1100, types: ["mixed"], strokes: [], sets: [
          { id: "s_v6n6w6", reps: 1, dist: 50, desc: "Freestyle — easy build", interval: "On 1:20", focus: "Open up" },
          { id: "s_oqhekh", reps: 1, dist: 100, desc: "Freestyle — moderate", interval: "On 2:20", focus: "Build" },
          { id: "s_xk7f75", reps: 1, dist: 200, desc: "Freestyle — strong", interval: "On 4:30", focus: "Threshold" },
          { id: "s_jdl2gj", reps: 1, dist: 100, desc: "Freestyle — fast", interval: "On 2:20", focus: "Sub-threshold" },
          { id: "s_6c9b1w", reps: 1, dist: 50, desc: "Freestyle — ALL OUT", interval: "On 1:05", focus: "Sprint" },
          { id: "s_fhbtfh", reps: 6, dist: 100, desc: "Kick — choice stroke (board optional), moderate", interval: "On 2:40", focus: "Kick close-out", eq: "kick" },
        ]},
      { label: "Mixed 6×250", totalYards: 1500, types: ["mixed"], strokes: [], sets: [
          { id: "s_vi3bgw", reps: 6, dist: 250, desc: "Choice stroke or IM — alternate between freestyle and choice stroke every rep", interval: "On 5:30", focus: "Sustained variety across mixed distance" },
        ]},
      { label: "Mixed Pyramid Volume 2000", totalYards: 2000, types: ["mixed"], strokes: [], sets: [
          { id: "s_i1w2zn", reps: 4, dist: 200, desc: "Freestyle — moderate to strong, descend 1→4", interval: "On 4:30", focus: "Distance base" },
          { id: "s_2yvlwl", reps: 4, dist: 150, desc: "Choice stroke — moderate to strong", interval: "On 3:25", focus: "Stroke variety mid-set" },
          { id: "s_cd47fs", reps: 4, dist: 100, desc: "Freestyle — strong", interval: "On 2:20", focus: "Push the pace" },
          { id: "s_fsjfwp", reps: 4, dist: 50, desc: "Choice stroke — fast finisher", interval: "On 1:20", focus: "Speed close-out" },
        ]},
      { label: "Mixed Triple Block 2400", totalYards: 2400, types: ["mixed"], strokes: [], sets: [
          { id: "s_isbg9a", reps: 4, dist: 300, desc: "Freestyle — aerobic to moderate, even effort", interval: "On 6:20", focus: "Distance base" },
          { id: "s_koyuic", reps: 4, dist: 200, desc: "Choice stroke or IM — moderate to strong", interval: "On 4:30", focus: "Variety and intensity build" },
          { id: "s_uivw4c", reps: 4, dist: 100, desc: "Freestyle — strong, hold pace to the end", interval: "On 2:20", focus: "Aerobic capacity close-out" },
        ]},
      { label: "Mixed 100s Repeat Unit", totalYards: 400, types: ["mixed"], strokes: [], sets: [
          { id: "s_vy6xy9", reps: 1, dist: 100, desc: "Freestyle — aerobic, long stroke", interval: "On 2:15", focus: "Base" },
          { id: "s_1j5c7u", reps: 1, dist: 100, desc: "Backstroke or IM — moderate", interval: "On 2:40", focus: "Stroke variety" },
          { id: "s_xdte2t", reps: 1, dist: 100, desc: "Freestyle — build to strong", interval: "On 2:15", focus: "Intensity build" },
          { id: "s_0uma5m", reps: 1, dist: 100, desc: "Choice stroke — strong, not sprinting", interval: "On 2:20", focus: "Mixed effort close — designed to repeat 2–3×" },
        ]},
      { label: "Mixed Kick-Pull-Swim Repeat Unit", totalYards: 500, types: ["mixed"], strokes: [], sets: [
          { id: "s_kbbq5u", reps: 4, dist: 50, desc: "Kick (board optional) — moderate effort", interval: "On 1:20", focus: "Leg activation", eq: "kickPart" },
          { id: "s_u6efp3", reps: 2, dist: 100, desc: "Pull (buoy) — long stroke, high elbow catch", interval: "On 2:20", focus: "Upper-body isolation", eq: "pull" },
          { id: "s_k05j4k", reps: 2, dist: 50, desc: "Freestyle swim — put it all together", interval: "On 1:10", focus: "Full-body integration each round" },
        ]},
      { label: "Mixed Sprint-Recover Repeat Unit", totalYards: 600, types: ["mixed"], strokes: [], sets: [
          { id: "s_3bmli3", reps: 4, dist: 100, desc: "Freestyle — build to strong, last 25 all-out", interval: "On 2:20", focus: "Controlled build with sprint close" },
          { id: "s_o3741v", reps: 4, dist: 50, desc: "Choice stroke (not free) — moderate recovery", interval: "On 1:25", focus: "Stroke variety and active recovery between rounds" },
        ]},
      { label: "Mixed Five 400s", totalYards: 2000, types: ["mixed"], strokes: [], sets: [
          { id: "s_ndh1i0", reps: 5, dist: 400, desc: "Rotate strokes: 1) freestyle, 2) IM, 3) backstroke, 4) freestyle, 5) choice stroke — moderate to strong, descend 1→5", interval: "On 8:30", focus: "Multi-stroke endurance at race-distance length" },
        ]},
      { label: "Mile + Stroke + Sprint", totalYards: 2100, types: ["mixed"], strokes: [], sets: [
          { id: "s_ghuccf", reps: 1, dist: 1500, desc: "Freestyle — moderate aerobic mile, even effort", interval: "On 35:00", focus: "Base aerobic block" },
          { id: "s_fofg81", reps: 4, dist: 100, desc: "100 IM — moderate-strong, crisp transitions", interval: "On 2:25", focus: "IM technique after aerobic load" },
          { id: "s_b5vfkp", reps: 4, dist: 50, desc: "Choice stroke — sprint, ALL OUT", interval: "On 1:35", focus: "Closing speed under fatigue" },
        ]},
      { label: "Mixed Variety Stack 2200", totalYards: 2200, types: ["mixed"], strokes: [], sets: [
          { id: "s_m2qj0s", reps: 4, dist: 300, desc: "Freestyle — moderate, even effort across all four", interval: "On 6:50", focus: "Aerobic anchor" },
          { id: "s_96onaz", reps: 4, dist: 150, desc: "Choice stroke — moderate-strong, switch strokes between reps", interval: "On 3:45", focus: "Stroke variety under aerobic load" },
          { id: "s_scwpgr", reps: 4, dist: 100, desc: "Freestyle — strong, hold target pace", interval: "On 2:25", focus: "Build pressure" },
          { id: "s_o9m31v", reps: 4, dist: 50, desc: "Choice stroke — sprint", interval: "On 1:20", focus: "Sprint finisher" },
        ]},
      { label: "Back 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_aly3dl", reps: 10, dist: 100, desc: "Backstroke — moderate to strong, descend each pair (1-2, 3-4, etc.)", interval: "On 2:55", focus: "Build backstroke fitness across volume" },
        ]},
      { label: "Back Pyramid", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_0rvqod", reps: 1, dist: 50, desc: "Backstroke — easy build", interval: "On 1:35", focus: "Open up" },
          { id: "s_lfxbj7", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:55", focus: "Find your pace" },
          { id: "s_ow30ax", reps: 1, dist: 150, desc: "Backstroke — moderate to strong", interval: "On 3:25", focus: "Hold longer" },
          { id: "s_kb9qem", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 4:30", focus: "Apex of pyramid" },
          { id: "s_h92x06", reps: 1, dist: 150, desc: "Backstroke — strong, match earlier 150 pace", interval: "On 3:25", focus: "Same pace, fatigued" },
          { id: "s_dpgf1m", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:55", focus: "Pick up pace" },
          { id: "s_9x6nni", reps: 1, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:35", focus: "Top speed" },
          { id: "s_5m6k58", reps: 1, dist: 300, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back + Free Cycles", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_fodk4h", reps: 8, dist: 100, desc: "50 backstroke strong / 50 freestyle easy", interval: "On 2:55", focus: "Hard back / easy free alternation for recovery" },
          { id: "s_gwx1vh", reps: 4, dist: 50, desc: "Backstroke — fast finishing 50s", interval: "On 1:35", focus: "Speed at the end" },
        ]},
      { label: "Back Pull Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_zfmael", reps: 6, dist: 100, desc: "Pull (buoy) — backstroke, focus on catch", interval: "On 2:20", focus: "Upper-body backstroke work", eq: "pull" },
          { id: "s_t9h8yv", reps: 1, dist: 100, desc: "Easy backstroke — recovery", interval: "On 2:55", focus: "Reset" },
          { id: "s_p7uuf0", reps: 6, dist: 50, desc: "Backstroke — strong, apply pull mechanics", interval: "On 1:35", focus: "Translate pull strength to swim" },
        ]},
      { label: "Back Sprint Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_x7k9x7", reps: 16, dist: 50, desc: "Backstroke — descend 1→4 within each set of 4", interval: "On 1:35", focus: "Sprint backstroke with consistent rest" },
          { id: "s_2qyw02", reps: 4, dist: 50, desc: "Backstroke — ALL OUT, full recovery", interval: "On 2:05", focus: "Top-end back speed" },
        ]},
      { label: "Back 200s Descend", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_wcw8hu", reps: 6, dist: 200, desc: "Backstroke — descend 1→6 (each one faster)", interval: "On 5:45", focus: "Build back pace progressively" },
        ]},
      { label: "Back 50s Volume", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_u1idjs", reps: 20, dist: 50, desc: "Backstroke — moderate to strong, descend within each set of 5 (1-5, 6-10, 11-15, 16-20)", interval: "On 1:35", focus: "Back endurance with cyclical descend" },
        ]},
      { label: "Back Race-Pace 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_aiz9p7", reps: 8, dist: 100, desc: "Backstroke — at race pace (full recovery)", interval: "On 3:10", focus: "Race-pace 100s" },
          { id: "s_mf7q23", reps: 4, dist: 50, desc: "Backstroke — easy, recovery", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Back Mile Pace", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_a6trf4", reps: 11, dist: 100, desc: "Backstroke — at goal mile pace, hold even", interval: "On 2:55", focus: "Back endurance at goal pace" },
        ]},
      { label: "Back Sprint Pyramid", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_fpf53a", reps: 1, dist: 25, desc: "Backstroke — moderate (open up)", interval: "On :45", focus: "Wake up" },
          { id: "s_gwbsx1", reps: 2, dist: 25, desc: "Backstroke — fast", interval: "On :45", focus: "Build" },
          { id: "s_n5d7l8", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_xcl0rf", reps: 6, dist: 25, desc: "Backstroke — ALL OUT (the test)", interval: "On :45", focus: "Maintain speed" },
          { id: "s_cs77qf", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Hold" },
          { id: "s_dhru7m", reps: 2, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Find speed" },
          { id: "s_ciwib7", reps: 1, dist: 25, desc: "Backstroke — ALL OUT (best 25)", interval: "On :45", focus: "Final blast" },
          { id: "s_8xl8f7", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Build 75s", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_yhr6o0", reps: 14, dist: 75, desc: "Backstroke — 25 build / 25 strong / 25 fast", interval: "On 2:40", focus: "Three-pace 75s" },
        ]},
      { label: "Back 7×150", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_p5qnp0", reps: 7, dist: 150, desc: "Backstroke — aerobic pace, focus on catch and rotation", interval: "On 3:40", focus: "Mid-distance backstroke volume" },
        ]},
      { label: "Back 100s + 50s", totalYards: 1050, types: [], strokes: ["back"], sets: [
          { id: "s_hubgzv", reps: 6, dist: 100, desc: "Backstroke — moderate, even effort", interval: "On 2:55", focus: "Backstroke aerobic base" },
          { id: "s_sxc92a", reps: 9, dist: 50, desc: "Backstroke — descend in groups of 3 (faster on rep 3)", interval: "On 1:35", focus: "Speed layered onto base" },
        ]},
      { label: "Back Threshold 200s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_r10li1", reps: 6, dist: 200, desc: "Backstroke — threshold pace (RPE 8/10), even effort", interval: "On 5:45", focus: "Back threshold endurance" },
        ]},
      { label: "Back IM Transition", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_fnvqgh", reps: 4, dist: 200, desc: "Back+Breast 200 (100 back / 100 breast) — moderate, smooth transition", interval: "On 4:45", focus: "IM transition focus" },
          { id: "s_jaos98", reps: 6, dist: 50, desc: "Backstroke — strong, race-pace finishers", interval: "On 1:35", focus: "Top off back speed" },
        ]},
      { label: "Back Pull Set Extended", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_52aigc", reps: 6, dist: 100, desc: "Pull (buoy) — backstroke, focus on catch", interval: "On 2:05", focus: "Back pull mechanics", eq: "pull" },
          { id: "s_rv8q5e", reps: 1, dist: 100, desc: "Easy back — recovery", interval: "On 2:20", focus: "Reset" },
          { id: "s_65qqxu", reps: 8, dist: 50, desc: "Backstroke — strong, integrate pull", interval: "On 1:35", focus: "Apply pull to swim" },
        ]},
      { label: "Back 100/50 Mix", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_z8hkng", reps: 6, dist: 100, desc: "Backstroke — moderate, descend 1→6", interval: "On 2:55", focus: "Back tempo" },
          { id: "s_cn2joo", reps: 8, dist: 50, desc: "Backstroke — fast, descend 1-4 then 5-8", interval: "On 1:35", focus: "Back sprint" },
        ]},
      { label: "Back Underwater Day", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_tza9br", reps: 8, dist: 25, desc: "Streamline backstroke kick — 12.5 yd UW + 12.5 swim", interval: "On 1:05", focus: "Underwater work on back", eq: "underkick" },
          { id: "s_mzpkkf", reps: 4, dist: 50, desc: "Backstroke — focus on push-off + UW dolphins", interval: "On 1:20", focus: "Underwater off every wall", eq: "underkick" },
          { id: "s_vjv425", reps: 6, dist: 100, desc: "Backstroke — moderate, focus on every push-off", interval: "On 2:55", focus: "Build streamline habit" },
        ]},
      { label: "Back Negative Splits", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_2z22xa", reps: 6, dist: 200, desc: "Backstroke — negative split (second 100 faster than first)", interval: "On 5:45", focus: "Practice closing speed on back" },
        ]},
      { label: "Back Pyramid Up & Down", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_cv0lyj", reps: 1, dist: 50, desc: "Backstroke — easy", interval: "On 1:35", focus: "Open" },
          { id: "s_0m05lq", reps: 1, dist: 100, desc: "Backstroke — moderate", interval: "On 2:55", focus: "Build" },
          { id: "s_yezlut", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 5:45", focus: "Peak" },
          { id: "s_m4mjaw", reps: 1, dist: 300, desc: "Backstroke — moderate, smooth", interval: "On 6:20", focus: "Distance" },
          { id: "s_4swwgf", reps: 1, dist: 200, desc: "Backstroke — strong, match earlier", interval: "On 5:45", focus: "Same pace fatigued" },
          { id: "s_2q2woo", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:55", focus: "Pick up" },
          { id: "s_a7x9p4", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Continuous 1000", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_bmst2t", reps: 1, dist: 1000, desc: "Backstroke — continuous, even-effort aerobic swim", interval: "On 23:05", focus: "Long-form back endurance" },
        ]},
      { label: "Back Test 200", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_9bphu2", reps: 1, dist: 200, desc: "Backstroke — easy warm-up", interval: "On 4:45", focus: "Open up back" },
          { id: "s_h1he6u", reps: 1, dist: 200, desc: "Backstroke — TEST 200 (race-pace effort)", interval: "On 5:15", focus: "200 back race test" },
          { id: "s_1eujgl", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_olcap1", reps: 6, dist: 50, desc: "Backstroke — fast finishers", interval: "On 1:35", focus: "Top-off speed" },
          { id: "s_oxlj55", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Endurance + Sprint", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_cyjqlc", reps: 1, dist: 600, desc: "Backstroke — steady aerobic, smooth tempo", interval: "On 12:35", focus: "Long back base" },
          { id: "s_mg0984", reps: 8, dist: 50, desc: "Backstroke — fast, descend 1-4 then 5-8", interval: "On 1:35", focus: "Sprint after distance" },
          { id: "s_181ato", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Tempo + Threshold", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_nnoiug", reps: 6, dist: 100, desc: "Backstroke — tempo (RPE 7/10)", interval: "On 2:55", focus: "Tempo zone" },
          { id: "s_20wchz", reps: 6, dist: 100, desc: "Backstroke — threshold (RPE 8/10)", interval: "On 2:55", focus: "Threshold zone" },
        ]},
      { label: "Back Race-Distance Set", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_8ru52r", reps: 5, dist: 200, desc: "Backstroke — at race pace (200), full recovery", interval: "On 5:15", focus: "200 back race-pace volume" },
        ]},
      { label: "Back Stroke Drop", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_vu8j4t", reps: 4, dist: 100, desc: "Backstroke — full stroke, count strokes", interval: "On 2:55", focus: "Baseline stroke count" },
          { id: "s_k8j960", reps: 4, dist: 100, desc: "Backstroke — drop 1 stroke, same speed", interval: "On 2:55", focus: "Reduce count" },
          { id: "s_ehfuxr", reps: 4, dist: 100, desc: "Backstroke — drop 2 strokes, same speed", interval: "On 2:55", focus: "Maximize DPS" },
          { id: "s_gzbpy0", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back 7×200", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_4sfdm9", reps: 7, dist: 200, desc: "Backstroke — aerobic pace, smooth rotation and catch", interval: "On 5:15", focus: "Mid-distance back volume" },
        ]},
      { label: "Back 200s + 100s", totalYards: 1400, types: [], strokes: ["back"], sets: [
          { id: "s_gu1sof", reps: 4, dist: 200, desc: "Backstroke — moderate aerobic, even effort", interval: "On 5:15", focus: "Backstroke aerobic base" },
          { id: "s_r9cet9", reps: 6, dist: 100, desc: "Backstroke — descend in groups of 3 (faster each rep)", interval: "On 2:55", focus: "Speed layered onto base" },
        ]},
      { label: "Back Power 25s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_fslyss", reps: 16, dist: 25, desc: "Backstroke — ALL OUT (full recovery)", interval: "On :45", focus: "Pure back speed" },
          { id: "s_jvq6px", reps: 4, dist: 100, desc: "Backstroke — moderate, recovery", interval: "On 2:55", focus: "Active recovery" },
          { id: "s_iupgdo", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Stroke Count", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_chosrs", reps: 4, dist: 50, desc: "Backstroke — count strokes per 25, baseline", interval: "On 1:35", focus: "Establish baseline" },
          { id: "s_vmzatm", reps: 4, dist: 50, desc: "Backstroke — same speed, count -1", interval: "On 1:35", focus: "Reduce strokes" },
          { id: "s_jka1gm", reps: 4, dist: 50, desc: "Backstroke — faster pace, hold count", interval: "On 1:35", focus: "Speed without adding strokes" },
          { id: "s_y4yayi", reps: 4, dist: 100, desc: "Backstroke — apply count over distance", interval: "On 2:55", focus: "Sustain efficiency" },
          { id: "s_aytug0", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back IM-Free Cycles", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_n2fig7", reps: 4, dist: 200, desc: "100 backstroke strong + 100 freestyle moderate", interval: "On 5:45", focus: "Hard back / easy free for sustainability" },
          { id: "s_sezlrp", reps: 4, dist: 50, desc: "Backstroke — fast finishing 50s", interval: "On 1:35", focus: "Top off back speed" },
        ]},
      { label: "Back Kick Endurance", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_0cb9bf", reps: 1, dist: 400, desc: "Backstroke kick — easy aerobic, head still", interval: "On 9:25", focus: "Long back kick base", eq: "kick" },
          { id: "s_41epjt", reps: 4, dist: 100, desc: "Backstroke kick — moderate, descend 1→4", interval: "On 3:25", focus: "Build kick intensity", eq: "kick" },
          { id: "s_gaphux", reps: 3, dist: 100, desc: "Backstroke — strong, drive with the legs", interval: "On 2:55", focus: "Apply kick to swim" },
        ]},
      { label: "Back Long + Sprint", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_g7nbge", reps: 1, dist: 800, desc: "Backstroke — steady aerobic, smooth tempo", interval: "On 17:50", focus: "Long back endurance" },
          { id: "s_el17vn", reps: 8, dist: 50, desc: "Backstroke — ALL OUT, full recovery", interval: "On 2:05", focus: "Sprint after distance" },
        ]},
      { label: "Back Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_d1mw3l", reps: 10, dist: 100, desc: "Backstroke — alternate 1 fast / 1 easy", interval: "On 2:55", focus: "Hard-easy pattern across volume" },
        ]},
      { label: "Back Race Test Day", totalYards: 1300, types: [], strokes: ["back"], sets: [
          { id: "s_ds6uzd", reps: 1, dist: 100, desc: "Backstroke — easy warm-up", interval: "On 2:55", focus: "Open up" },
          { id: "s_v8a2hp", reps: 1, dist: 100, desc: "Backstroke — TEST 100 (race pace)", interval: "On 3:10", focus: "100 back test" },
          { id: "s_n8d5rt", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_y92dbe", reps: 1, dist: 200, desc: "Backstroke — TEST 200 (race pace)", interval: "On 5:15", focus: "200 back test" },
          { id: "s_vgk34b", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_ddsctn", reps: 4, dist: 100, desc: "Backstroke — moderate, hold form", interval: "On 2:55", focus: "Aerobic close" },
          { id: "s_apmgsh", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back Aerobic 150s", totalYards: 1200, types: [], strokes: ["back"], sets: [
          { id: "s_l65q9h", reps: 8, dist: 150, desc: "Backstroke — aerobic pace (RPE 6/10), smooth", interval: "On 3:10", focus: "Back aerobic volume" },
        ]},
      { label: "Back Mid-Distance Build", totalYards: 1100, types: [], strokes: ["back"], sets: [
          { id: "s_q9vuiw", reps: 1, dist: 400, desc: "Backstroke — easy aerobic", interval: "On 9:25", focus: "Aerobic base" },
          { id: "s_76zigh", reps: 1, dist: 300, desc: "Backstroke — moderate", interval: "On 6:50", focus: "Build" },
          { id: "s_67agl0", reps: 1, dist: 200, desc: "Backstroke — strong", interval: "On 5:45", focus: "Threshold" },
          { id: "s_1tv33n", reps: 1, dist: 100, desc: "Backstroke — fast", interval: "On 2:55", focus: "Sub-threshold" },
          { id: "s_8vntbs", reps: 2, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:35", focus: "Sprint close" },
        ]},
      { label: "Back 8×125 Threshold", totalYards: 1000, types: [], strokes: ["back"], sets: [
          { id: "s_k48txo", reps: 8, dist: 125, desc: "Backstroke — threshold pace, hold even", interval: "On 2:40", focus: "Threshold in odd-distance reps" },
        ]},
      { label: "Back Sprint + Endurance", totalYards: 1300, types: [], strokes: ["back"], sets: [
          { id: "s_3yv49y", reps: 4, dist: 25, desc: "Backstroke — ALL OUT", interval: "On :45", focus: "Wake up speed" },
          { id: "s_88evw1", reps: 1, dist: 200, desc: "Backstroke — easy", interval: "On 4:45", focus: "Recover" },
          { id: "s_81bmj2", reps: 4, dist: 50, desc: "Backstroke — ALL OUT", interval: "On 1:35", focus: "Speed" },
          { id: "s_3xuyg3", reps: 1, dist: 200, desc: "Backstroke — easy", interval: "On 4:45", focus: "Recover" },
          { id: "s_9r9tq5", reps: 4, dist: 100, desc: "Backstroke — fast, hold race pace", interval: "On 2:55", focus: "Race-distance speed" },
          { id: "s_ihysyw", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Back 10×150", totalYards: 1500, types: [], strokes: ["back"], sets: [
          { id: "s_2ta3cy", reps: 10, dist: 150, desc: "Backstroke — aerobic to moderate pace, descend 1→5 then repeat", interval: "On 3:40", focus: "Back aerobic volume with progressive effort" },
        ]},
      { label: "Back 10×200", totalYards: 2000, types: [], strokes: ["back"], sets: [
          { id: "s_c85osq", reps: 10, dist: 200, desc: "Backstroke — aerobic pace, hold even splits throughout", interval: "On 4:45", focus: "High-volume back endurance" },
        ]},
      { label: "Back 8×300", totalYards: 2400, types: [], strokes: ["back"], sets: [
          { id: "s_lw605m", reps: 8, dist: 300, desc: "Backstroke — aerobic pace, descend 1→4 then repeat (each set faster)", interval: "On 7:20", focus: "Extended back endurance at high volume" },
        ]},
      { label: "Back 50s Repeat Unit", totalYards: 400, types: [], strokes: ["back"], sets: [
          { id: "s_s6prkm", reps: 8, dist: 50, desc: "Backstroke — moderate to strong, focus on high arm recovery and hip rotation", interval: "On 1:35", focus: "Back volume and mechanics — designed to repeat 2–3×" },
        ]},
      { label: "Back 100s Repeat Unit", totalYards: 500, types: [], strokes: ["back"], sets: [
          { id: "s_u22udu", reps: 4, dist: 100, desc: "Backstroke — moderate, count strokes per length to monitor efficiency", interval: "On 2:55", focus: "Back aerobic with stroke awareness" },
          { id: "s_i38cld", reps: 2, dist: 50, desc: "Backstroke — strong, hold stroke count from 100s", interval: "On 1:25", focus: "Speed with maintained mechanics" },
        ]},
      { label: "Back Build Repeat Unit", totalYards: 600, types: [], strokes: ["back"], sets: [
          { id: "s_gs6qul", reps: 3, dist: 200, desc: "Backstroke — descend 1→3, each 200 faster than the last", interval: "On 5:15", focus: "Progressive backstroke effort — push the last 200 each round" },
        ]},
      { label: "Breast 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_08570l", reps: 10, dist: 100, desc: "Breaststroke — moderate to strong, count strokes per 25", interval: "On 3:25", focus: "Sustain breast pace, monitor stroke count for efficiency" },
        ]},
      { label: "Breast Pyramid", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_q8yhoh", reps: 1, dist: 50, desc: "Breaststroke — easy build", interval: "On 1:35", focus: "Open up" },
          { id: "s_ry3z8j", reps: 1, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:25", focus: "Find your pace" },
          { id: "s_wq4ret", reps: 1, dist: 150, desc: "Breaststroke — moderate to strong", interval: "On 3:55", focus: "Hold longer" },
          { id: "s_1s0c0k", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 5:15", focus: "Apex of pyramid" },
          { id: "s_zby4nl", reps: 1, dist: 150, desc: "Breaststroke — strong, match earlier pace", interval: "On 3:55", focus: "Same pace, fatigued" },
          { id: "s_ssluop", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:25", focus: "Pick up pace" },
          { id: "s_jbfrtc", reps: 1, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:35", focus: "Top speed" },
          { id: "s_mde7tj", reps: 1, dist: 300, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Kick Endurance", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_mx2v3x", reps: 1, dist: 300, desc: "Breaststroke kick (board) — long aerobic effort", interval: "On 6:50", focus: "Build kick endurance", eq: "kick" },
          { id: "s_fq3k0e", reps: 4, dist: 100, desc: "Breaststroke kick — moderate, descend pace 1→4", interval: "On 3:25", focus: "Build kick intensity", eq: "kick" },
          { id: "s_upasyg", reps: 3, dist: 100, desc: "Breaststroke swim — strong, drive with the kick", interval: "On 3:25", focus: "Apply kick power to full stroke" },
        ]},
      { label: "Breast Pull Set", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_74x5we", reps: 6, dist: 100, desc: "Pull (buoy) — breaststroke pull, no kick (just glide)", interval: "On 2:40", focus: "Strong pull pattern", eq: "pull" },
          { id: "s_3wezan", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_kt7l0a", reps: 6, dist: 50, desc: "Breaststroke — strong, integrate pull with kick", interval: "On 1:35", focus: "Apply pull mechanics" },
        ]},
      { label: "Breast Sprint Set", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_23jdt1", reps: 12, dist: 50, desc: "Breaststroke — descend 1→4 within each set of 4", interval: "On 1:35", focus: "Sprint breast with controlled cadence" },
          { id: "s_mogfm7", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_clzczm", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT, fast turns", interval: "On 1:50", focus: "Peak breast speed" },
        ]},
      { label: "Breast 200s Descend", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_bgil0k", reps: 6, dist: 200, desc: "Breaststroke — descend 1→6 (each one faster)", interval: "On 6:35", focus: "Build breast pace progressively" },
        ]},
      { label: "Breast 50s Sprint Volume", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_zzso19", reps: 16, dist: 50, desc: "Breaststroke — descend 1→4 within each set of 4", interval: "On 1:35", focus: "Sprint breast cyclically" },
          { id: "s_clcyit", reps: 4, dist: 50, desc: "Breaststroke — easy, recovery", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Breast Race-Pace 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_lmvl3d", reps: 8, dist: 100, desc: "Breaststroke — at race pace (full recovery)", interval: "On 3:10", focus: "Race-pace 100s" },
          { id: "s_ggn6v5", reps: 4, dist: 50, desc: "Breaststroke — moderate, recovery", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Breast Mile Pace", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_n39u2s", reps: 11, dist: 100, desc: "Breaststroke — at goal mile pace, hold even splits", interval: "On 3:25", focus: "Breast endurance at goal pace" },
        ]},
      { label: "Breast Sprint Pyramid", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_f9wtis", reps: 1, dist: 25, desc: "Breaststroke — moderate (open)", interval: "On :50", focus: "Wake up" },
          { id: "s_sgz0vh", reps: 2, dist: 25, desc: "Breaststroke — fast", interval: "On :50", focus: "Build" },
          { id: "s_c8dxf5", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Peak speed" },
          { id: "s_chvlgl", reps: 6, dist: 25, desc: "Breaststroke — ALL OUT (the test)", interval: "On :50", focus: "Maintain speed" },
          { id: "s_k35lvi", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Hold" },
          { id: "s_a1jtkx", reps: 2, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Find speed" },
          { id: "s_cz5jgb", reps: 1, dist: 25, desc: "Breaststroke — ALL OUT (best 25)", interval: "On :50", focus: "Final blast" },
          { id: "s_1oz2gn", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Build 75s", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_pi1fhg", reps: 14, dist: 75, desc: "Breaststroke — 25 build / 25 strong / 25 fast", interval: "On 2:40", focus: "Three-pace 75s" },
        ]},
      { label: "Breast 7×150", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_uaj9zq", reps: 7, dist: 150, desc: "Breaststroke — aerobic pace, focus on timing of pull and kick", interval: "On 4:45", focus: "Mid-distance breast volume" },
        ]},
      { label: "Breast 100s + 50s", totalYards: 1050, types: [], strokes: ["breast"], sets: [
          { id: "s_netbvr", reps: 6, dist: 100, desc: "Breaststroke — moderate, even effort", interval: "On 3:25", focus: "Breast aerobic base" },
          { id: "s_2uqx2z", reps: 9, dist: 50, desc: "Breaststroke — descend in groups of 3 (faster on rep 3)", interval: "On 1:35", focus: "Speed layered onto base" },
        ]},
      { label: "Breast Threshold 200s", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_hlpori", reps: 6, dist: 200, desc: "Breaststroke — threshold pace (RPE 8/10)", interval: "On 6:35", focus: "Breast threshold endurance" },
        ]},
      { label: "Breast IM Transition", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_7zjnl8", reps: 4, dist: 200, desc: "Breast+Free 200 (100 breast / 100 free) — moderate, smooth transition", interval: "On 4:45", focus: "IM transition focus" },
          { id: "s_qxiu3f", reps: 6, dist: 50, desc: "Breaststroke — strong, race-pace finishers", interval: "On 1:35", focus: "Top off breast speed" },
        ]},
      { label: "Breast Pull-Only Set", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_mrvpse", reps: 6, dist: 100, desc: "Breaststroke pull — no kick (just glide), focus on pull power", interval: "On 2:40", focus: "Isolate pull mechanics", eq: "pull" },
          { id: "s_rk2huv", reps: 1, dist: 100, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_40qmtx", reps: 8, dist: 50, desc: "Breaststroke — full stroke, integrate pull", interval: "On 1:35", focus: "Apply pull to full stroke" },
        ]},
      { label: "Breast Negative Splits", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_eau1s5", reps: 6, dist: 200, desc: "Breaststroke — negative split (second 100 faster than first)", interval: "On 6:35", focus: "Practice closing speed on breast" },
        ]},
      { label: "Breast Pyramid Up & Down", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_95ekdv", reps: 1, dist: 50, desc: "Breaststroke — easy", interval: "On 1:35", focus: "Open" },
          { id: "s_znepv7", reps: 1, dist: 100, desc: "Breaststroke — moderate", interval: "On 3:25", focus: "Build" },
          { id: "s_hgbyvw", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 6:35", focus: "Peak" },
          { id: "s_tbfqmo", reps: 1, dist: 300, desc: "Breaststroke — moderate, smooth", interval: "On 6:50", focus: "Distance" },
          { id: "s_h1j88u", reps: 1, dist: 200, desc: "Breaststroke — strong, match earlier", interval: "On 6:35", focus: "Same pace fatigued" },
          { id: "s_vpzj42", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:25", focus: "Pick up" },
          { id: "s_51c30y", reps: 1, dist: 150, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Continuous 1000", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_xakmo4", reps: 1, dist: 1000, desc: "Breaststroke — continuous, aerobic effort, even pace", interval: "On 26:15", focus: "Long-form breast endurance" },
        ]},
      { label: "Breast Test 200", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_miy5g5", reps: 1, dist: 200, desc: "Breaststroke — easy warm-up", interval: "On 5:15", focus: "Open up breast" },
          { id: "s_17b5gu", reps: 1, dist: 200, desc: "Breaststroke — TEST 200 (race-pace effort)", interval: "On 5:45", focus: "200 breast race test" },
          { id: "s_u83ccl", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_gd3tmd", reps: 6, dist: 50, desc: "Breaststroke — fast finishers", interval: "On 1:35", focus: "Top-off speed" },
          { id: "s_vnrcvi", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Endurance + Sprint", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_78yljf", reps: 1, dist: 600, desc: "Breaststroke — steady aerobic, drive with kick", interval: "On 13:40", focus: "Long breast base" },
          { id: "s_m2wie4", reps: 8, dist: 50, desc: "Breaststroke — fast, descend 1-4 then 5-8", interval: "On 1:35", focus: "Sprint after distance" },
          { id: "s_7r9mhq", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Tempo + Threshold", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_38go78", reps: 6, dist: 100, desc: "Breaststroke — tempo (RPE 7/10)", interval: "On 3:25", focus: "Tempo zone" },
          { id: "s_n5tgrq", reps: 6, dist: 100, desc: "Breaststroke — threshold (RPE 8/10)", interval: "On 3:25", focus: "Threshold zone" },
        ]},
      { label: "Breast Race-Distance 200s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_3jrkqa", reps: 5, dist: 200, desc: "Breaststroke — at race pace (200), full recovery", interval: "On 5:45", focus: "200 breast race-pace volume" },
        ]},
      { label: "Breast Power 50s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_6obl2p", reps: 12, dist: 50, desc: "Breaststroke — ALL OUT (full recovery)", interval: "On 1:50", focus: "Pure breast speed" },
          { id: "s_o5hno4", reps: 4, dist: 100, desc: "Breaststroke — moderate, recovery", interval: "On 3:25", focus: "Active recovery" },
        ]},
      { label: "Breast Pull + Kick + Swim", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_caocd3", reps: 4, dist: 50, desc: "Breaststroke — pull only (no kick, glide)", interval: "On 1:35", focus: "Pull power", eq: "pull" },
          { id: "s_rjvuro", reps: 4, dist: 50, desc: "Breaststroke kick (board) — strong", interval: "On 1:35", focus: "Kick power", eq: "kick" },
          { id: "s_39ohte", reps: 4, dist: 50, desc: "Breaststroke — full stroke, integrate", interval: "On 1:35", focus: "Apply both" },
          { id: "s_m8sgje", reps: 4, dist: 100, desc: "Breaststroke — full stroke, race-pace tempo", interval: "On 3:25", focus: "Race-pace endurance" },
          { id: "s_1ym87r", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_gfxf3k", reps: 10, dist: 100, desc: "Breaststroke — alternate 1 fast / 1 easy", interval: "On 3:25", focus: "Hard-easy pattern across volume" },
        ]},
      { label: "Breast Long Kick Day", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_u37blx", reps: 1, dist: 500, desc: "Breaststroke kick (board) — long aerobic", interval: "On 11:35", focus: "Long breast kick base", eq: "kick" },
          { id: "s_bcgc0o", reps: 4, dist: 100, desc: "Breaststroke kick — descend 1→4", interval: "On 3:25", focus: "Build kick intensity", eq: "kick" },
          { id: "s_6kke30", reps: 3, dist: 100, desc: "Breaststroke — strong, drive with kick", interval: "On 3:25", focus: "Apply kick power" },
        ]},
      { label: "Breast Race Test Day", totalYards: 1300, types: [], strokes: ["breast"], sets: [
          { id: "s_fblj9j", reps: 1, dist: 100, desc: "Breaststroke — easy warm-up", interval: "On 3:25", focus: "Open up" },
          { id: "s_9ptiuf", reps: 1, dist: 100, desc: "Breaststroke — TEST 100 (race pace)", interval: "On 3:10", focus: "100 breast test" },
          { id: "s_wf689j", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_dw4pof", reps: 1, dist: 200, desc: "Breaststroke — TEST 200 (race pace)", interval: "On 5:45", focus: "200 breast test" },
          { id: "s_nxws54", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_ti66xi", reps: 4, dist: 100, desc: "Breaststroke — moderate, hold form", interval: "On 3:25", focus: "Aerobic close" },
          { id: "s_mqdqll", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Aerobic 150s", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_czwa63", reps: 8, dist: 150, desc: "Breaststroke — aerobic pace (RPE 6/10), smooth", interval: "On 3:40", focus: "Breast aerobic volume" },
        ]},
      { label: "Breast Mid-Distance Build", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_206ws9", reps: 1, dist: 400, desc: "Breaststroke — easy aerobic", interval: "On 10:30", focus: "Aerobic base" },
          { id: "s_6odmkm", reps: 1, dist: 300, desc: "Breaststroke — moderate", interval: "On 7:20", focus: "Build" },
          { id: "s_tg2s6u", reps: 1, dist: 200, desc: "Breaststroke — strong", interval: "On 6:35", focus: "Threshold" },
          { id: "s_s28t0p", reps: 1, dist: 100, desc: "Breaststroke — fast", interval: "On 3:25", focus: "Sub-threshold" },
          { id: "s_x8xg71", reps: 2, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:35", focus: "Sprint close" },
        ]},
      { label: "Breast 8×125 Threshold", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_mokezy", reps: 8, dist: 125, desc: "Breaststroke — threshold pace, hold even", interval: "On 2:55", focus: "Threshold in odd-distance reps" },
        ]},
      { label: "Breast 50/100 Mix", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_yime3c", reps: 6, dist: 100, desc: "Breaststroke — moderate, descend 1→6", interval: "On 3:25", focus: "Breast tempo" },
          { id: "s_jeddsz", reps: 8, dist: 50, desc: "Breaststroke — fast, descend 1-4 then 5-8", interval: "On 1:35", focus: "Breast sprint" },
        ]},
      { label: "Breast Stroke Count", totalYards: 1100, types: [], strokes: ["breast"], sets: [
          { id: "s_0w801h", reps: 4, dist: 50, desc: "Breaststroke — count strokes per 25, baseline", interval: "On 1:35", focus: "Establish baseline" },
          { id: "s_vrmruy", reps: 4, dist: 50, desc: "Breaststroke — same speed, count -1", interval: "On 1:35", focus: "Reduce strokes" },
          { id: "s_s4526x", reps: 4, dist: 50, desc: "Breaststroke — faster pace, hold count", interval: "On 1:35", focus: "Speed without adding strokes" },
          { id: "s_masyx1", reps: 4, dist: 100, desc: "Breaststroke — apply count over distance", interval: "On 3:25", focus: "Sustain efficiency" },
          { id: "s_j7lxwt", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast Long + Sprint", totalYards: 1200, types: [], strokes: ["breast"], sets: [
          { id: "s_jwihkt", reps: 1, dist: 800, desc: "Breaststroke — steady aerobic, smooth", interval: "On 18:55", focus: "Long breast endurance" },
          { id: "s_sj5foi", reps: 8, dist: 50, desc: "Breaststroke — ALL OUT, full recovery", interval: "On 2:05", focus: "Sprint after distance" },
        ]},
      { label: "Breast Stroke-Drop Pyramid", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_2hwznw", reps: 4, dist: 75, desc: "Breaststroke — full stroke", interval: "On 2:40", focus: "Build" },
          { id: "s_5ak4oy", reps: 4, dist: 100, desc: "Breaststroke — race pace, count strokes", interval: "On 3:25", focus: "Race-pace stroke count" },
          { id: "s_5hqsor", reps: 6, dist: 50, desc: "Breaststroke — fast, fewer strokes than baseline", interval: "On 1:35", focus: "Speed with efficiency" },
        ]},
      { label: "Breast IM-Free Cycles", totalYards: 1000, types: [], strokes: ["breast"], sets: [
          { id: "s_o07x2b", reps: 4, dist: 200, desc: "100 breaststroke strong + 100 freestyle moderate", interval: "On 6:35", focus: "Hard breast / easy free for sustainability" },
          { id: "s_hjnlyj", reps: 4, dist: 50, desc: "Breaststroke — fast finishing 50s", interval: "On 1:35", focus: "Top off breast speed" },
        ]},
      { label: "Breast Sprint + Endurance", totalYards: 1300, types: [], strokes: ["breast"], sets: [
          { id: "s_qtaeyt", reps: 4, dist: 25, desc: "Breaststroke — ALL OUT", interval: "On :50", focus: "Wake up speed" },
          { id: "s_tlz1yb", reps: 1, dist: 200, desc: "Breaststroke — easy", interval: "On 5:15", focus: "Recover" },
          { id: "s_8e7qb0", reps: 4, dist: 50, desc: "Breaststroke — ALL OUT", interval: "On 1:35", focus: "Speed" },
          { id: "s_xw9nge", reps: 1, dist: 200, desc: "Breaststroke — easy", interval: "On 5:15", focus: "Recover" },
          { id: "s_d90izi", reps: 4, dist: 100, desc: "Breaststroke — fast, hold race pace", interval: "On 3:25", focus: "Race-distance speed" },
          { id: "s_venp1f", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Breast 10×150", totalYards: 1500, types: [], strokes: ["breast"], sets: [
          { id: "s_92oivw", reps: 10, dist: 150, desc: "Breaststroke — aerobic pace, descend 1→5 then repeat, count strokes per length", interval: "On 4:45", focus: "Breast aerobic volume with stroke efficiency focus" },
        ]},
      { label: "Breast 10×200", totalYards: 2000, types: [], strokes: ["breast"], sets: [
          { id: "s_2a48wx", reps: 10, dist: 200, desc: "Breaststroke — aerobic to moderate pace, hold even splits", interval: "On 6:35", focus: "High-volume breast endurance" },
        ]},
      { label: "Breast 8×300", totalYards: 2400, types: [], strokes: ["breast"], sets: [
          { id: "s_9tu3yq", reps: 8, dist: 300, desc: "Breaststroke — aerobic pace, descend 1→4 then repeat", interval: "On 9:25", focus: "Extended breast endurance at high volume" },
        ]},
      { label: "Breast 50s Repeat Unit", totalYards: 400, types: [], strokes: ["breast"], sets: [
          { id: "s_0ekuei", reps: 8, dist: 50, desc: "Breaststroke — moderate, focus on tight glide and sharp kick-pull-glide cycle", interval: "On 1:50", focus: "Breast mechanics at repeatable effort — designed to repeat 2–3×" },
        ]},
      { label: "Breast 100s Repeat Unit", totalYards: 500, types: [], strokes: ["breast"], sets: [
          { id: "s_8g1u9t", reps: 4, dist: 100, desc: "Breaststroke — moderate to strong, count pull-out glide distance on turn", interval: "On 3:25", focus: "Breaststroke aerobic with technical focus" },
          { id: "s_9rin2h", reps: 2, dist: 50, desc: "Breaststroke — strong, crisp cycle rhythm", interval: "On 1:35", focus: "Finish each round with speed" },
        ]},
      { label: "Breast Build Repeat Unit", totalYards: 600, types: [], strokes: ["breast"], sets: [
          { id: "s_iwmkx2", reps: 3, dist: 200, desc: "Breaststroke — descend 1→3, each faster than the last", interval: "On 6:20", focus: "Progressive breast effort — each round builds on the last" },
        ]},
      { label: "Fly 25s with Free Recovery", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_4yiib7", reps: 16, dist: 50, desc: "25 fly fast / 25 freestyle easy (immediate)", interval: "On 1:35", focus: "Build fly tolerance with active recovery" },
          { id: "s_ij8ee7", reps: 4, dist: 50, desc: "25 fly ALL OUT / 25 free easy", interval: "On 1:35", focus: "Maximum fly speed with recovery" },
        ]},
      { label: "Fly + Free Pattern 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_ikwngj", reps: 8, dist: 100, desc: "25 fly / 75 freestyle — recover into the free", interval: "On 2:05", focus: "Sustainable fly with free recovery built in" },
          { id: "s_c6xrli", reps: 4, dist: 50, desc: "Butterfly — fast (full stroke, no recovery)", interval: "On 1:35", focus: "Pure fly speed at the end" },
        ]},
      { label: "Fly Sprint Pyramid", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_hc78m5", reps: 1, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Open up" },
          { id: "s_wsula1", reps: 2, dist: 25, desc: "Butterfly — fast", interval: "On :45", focus: "Build" },
          { id: "s_bk5ii8", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Peak speed" },
          { id: "s_uxah1w", reps: 8, dist: 25, desc: "Butterfly — ALL OUT (this is the test)", interval: "On 1:05", focus: "Maintain speed under fatigue" },
          { id: "s_l9xpn3", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Hold the line" },
          { id: "s_cmxgwe", reps: 2, dist: 25, desc: "Butterfly — ALL OUT", interval: "On :45", focus: "Find top speed" },
          { id: "s_wc94ad", reps: 1, dist: 25, desc: "Butterfly — ALL OUT (best 25 of the day)", interval: "On :45", focus: "Final blast" },
          { id: "s_gdui4z", reps: 1, dist: 550, desc: "Easy freestyle — full recovery", interval: "No interval — swim easy", focus: "Aerobic close-out" },
        ]},
      { label: "Fly Pull Set", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_ds9st9", reps: 8, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 freestyle pull", interval: "On 2:20", focus: "Build fly catch with free recovery", eq: "pull" },
          { id: "s_iivjhk", reps: 4, dist: 50, desc: "Butterfly — fast (apply pull mechanics)", interval: "On 1:35", focus: "Translate pull strength to fly swim" },
        ]},
      { label: "Fly Kick + Swim", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_ckomz3", reps: 1, dist: 300, desc: "Dolphin kick (board) — easy aerobic", interval: "On 7:20", focus: "Long dolphin base", eq: "kick" },
          { id: "s_7zim31", reps: 4, dist: 100, desc: "Dolphin kick — moderate, descend pace 1→4", interval: "On 3:10", focus: "Build kick intensity", eq: "kick" },
          { id: "s_3gp2sz", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:20", focus: "Apply dolphin rhythm to fly with active recovery" },
        ]},
      { label: "Fly 50s Volume", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_5cv5r9", reps: 16, dist: 50, desc: "25 fly fast / 25 freestyle easy (immediate)", interval: "On 1:35", focus: "Fly tolerance with active recovery" },
          { id: "s_g35my0", reps: 4, dist: 50, desc: "Butterfly — fast (full stroke)", interval: "On 1:35", focus: "Pure fly speed" },
        ]},
      { label: "Fly 25s Sprint", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_gabp1d", reps: 16, dist: 25, desc: "Butterfly — ALL OUT 25s (full recovery)", interval: "On 1:05", focus: "Pure fly speed" },
          { id: "s_9r8ds9", reps: 6, dist: 100, desc: "Easy freestyle — between blocks", interval: "No interval — swim easy", focus: "Active recovery" },
        ]},
      { label: "Fly + Free Cycles", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_c4wz6j", reps: 8, dist: 100, desc: "25 fly / 75 free — sustainable fly fitness", interval: "On 2:05", focus: "Build fly endurance with free recovery" },
          { id: "s_zhm8f8", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:40", focus: "Fly speed with free recovery" },
        ]},
      { label: "Fly Pull Mix", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_99tlrw", reps: 4, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 free pull", interval: "On 2:20", focus: "Build fly catch with free recovery", eq: "pull" },
          { id: "s_b1954i", reps: 4, dist: 100, desc: "Freestyle — strong, apply pull mechanics", interval: "On 2:05", focus: "Translate pull pattern" },
          { id: "s_kh3ylb", reps: 4, dist: 50, desc: "Butterfly — fast, full stroke", interval: "On 1:35", focus: "Apply pull strength to fly" },
          { id: "s_91w91u", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Mile Sustainability", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_v6ehds", reps: 12, dist: 100, desc: "25 fly fast / 75 free moderate (mile pace) — repeat", interval: "On 2:05", focus: "Long-form fly tolerance with free for distance" },
        ]},
      { label: "Fly Build 75s", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_d6ihsp", reps: 14, dist: 75, desc: "25 fly easy / 25 free / 25 fly ALL OUT", interval: "On 2:40", focus: "Three-pace 75s, fly bookends" },
        ]},
      { label: "Fly 7×150", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_gfowoa", reps: 7, dist: 150, desc: "50 fly / 100 free — aerobic pace, preserve fly form across all reps", interval: "On 3:40", focus: "Mid-distance fly volume with free recovery" },
        ]},
      { label: "Fly 100s + 50s", totalYards: 1050, types: [], strokes: ["fly"], sets: [
          { id: "s_rbiwxw", reps: 6, dist: 100, desc: "25 fly / 75 free — moderate, focus on fly quality on the lead 25", interval: "On 2:40", focus: "Fly lead with free base" },
          { id: "s_xkmebt", reps: 9, dist: 50, desc: "25 fly fast / 25 free easy — descend fly effort in groups of 3", interval: "On 1:35", focus: "Speed layered onto base" },
        ]},
      { label: "Fly Endurance Test", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_yo6n26", reps: 4, dist: 100, desc: "Butterfly — full stroke, race pace (full recovery)", interval: "On 3:40", focus: "Race-pace 100 fly volume" },
          { id: "s_l2y9mk", reps: 4, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:35", focus: "Maintain fly speed with recovery" },
          { id: "s_642lgh", reps: 1, dist: 500, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down after fly volume" },
        ]},
      { label: "Fly Race-Pace 200", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_htfkc4", reps: 1, dist: 200, desc: "Butterfly — TEST 200 (race-pace simulation; 50 fly + 50 free + 50 fly + 50 free if needed for sustainability)", interval: "On 6:20", focus: "200 fly fitness check" },
          { id: "s_yy0sdp", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:45", focus: "Reset after test" },
          { id: "s_j60n1n", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:35", focus: "Apply fly volume" },
          { id: "s_lnx1e9", reps: 1, dist: 300, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Threshold 50s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_i71ppf", reps: 8, dist: 50, desc: "Butterfly — at threshold (RPE 8/10), full stroke", interval: "On 1:50", focus: "Sustained fly threshold" },
          { id: "s_upccy2", reps: 8, dist: 50, desc: "25 fly / 25 free — moderate", interval: "On 1:20", focus: "Active recovery between threshold reps" },
          { id: "s_hg2gvs", reps: 4, dist: 50, desc: "Easy freestyle — close", interval: "On 1:20", focus: "Cool down" },
        ]},
      { label: "Fly Negative Splits", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_ngui44", reps: 8, dist: 100, desc: "100 fly — negative split (50 fly easy / 50 fly fast)", interval: "On 2:40", focus: "Practice closing speed in fly" },
          { id: "s_8kwm6a", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Fly Pyramid", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_twac4q", reps: 1, dist: 50, desc: "Butterfly — moderate (or 25 fly + 25 free)", interval: "On 1:35", focus: "Open up fly" },
          { id: "s_6op40g", reps: 1, dist: 75, desc: "Butterfly — moderate (or 25 fly + 50 free)", interval: "On 2:05", focus: "Build" },
          { id: "s_2d8rv4", reps: 1, dist: 100, desc: "Butterfly — moderate (or 25 fly + 75 free)", interval: "On 2:40", focus: "Peak fly" },
          { id: "s_rnewhj", reps: 1, dist: 75, desc: "Butterfly — fast (or 25 fly + 50 free)", interval: "On 2:05", focus: "Pick up pace" },
          { id: "s_b8fa9a", reps: 1, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:35", focus: "Top speed" },
          { id: "s_8iz6qr", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_tapu38", reps: 1, dist: 550, desc: "Easy freestyle — full close-out", interval: "No interval — swim easy", focus: "Cool down after fly volume" },
        ]},
      { label: "Fly + IM Mix", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_541yub", reps: 4, dist: 200, desc: "200 IM — moderate, smooth transitions", interval: "On 6:20", focus: "Apply fly into IM context" },
          { id: "s_l7875x", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:40", focus: "Fly speed with active recovery" },
        ]},
      { label: "Fly Pull-Heavy Day", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_f4xli1", reps: 8, dist: 100, desc: "Pull (buoy) — 50 fly pull / 50 free pull", interval: "On 2:20", focus: "Build fly catch endurance", eq: "pull" },
          { id: "s_weknai", reps: 4, dist: 50, desc: "Butterfly — full stroke, apply pull mechanics", interval: "On 1:35", focus: "Translate pull power to fly" },
        ]},
      { label: "Fly Power 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_610h4r", reps: 4, dist: 100, desc: "Butterfly — race pace, full stroke (long recovery)", interval: "On 4:10", focus: "Race-pace fly 100s" },
          { id: "s_4a3bn4", reps: 4, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:35", focus: "Maintain speed with recovery" },
          { id: "s_f9zde0", reps: 4, dist: 100, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Continuous 1000", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_myhchi", reps: 1, dist: 1000, desc: "Continuous swim — alternate 25 fly / 75 free for 1000 (test fly endurance over distance)", interval: "On 23:05", focus: "Long-form fly tolerance" },
        ]},
      { label: "Fly Tempo + Threshold", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_o9uye8", reps: 5, dist: 100, desc: "25 fly / 75 free — tempo (RPE 7/10)", interval: "On 2:05", focus: "Tempo zone" },
          { id: "s_nmsnhu", reps: 5, dist: 100, desc: "25 fly / 75 free — threshold (RPE 8/10)", interval: "On 2:05", focus: "Threshold zone" },
        ]},
      { label: "Fly Race Test Day", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_hds4bk", reps: 1, dist: 100, desc: "25 fly + 75 free — easy warm-up", interval: "On 2:40", focus: "Open up" },
          { id: "s_euls56", reps: 1, dist: 100, desc: "Butterfly — TEST 100 (race pace)", interval: "On 3:40", focus: "100 fly test" },
          { id: "s_myohko", reps: 1, dist: 200, desc: "Easy backstroke — recovery", interval: "On 5:45", focus: "Reset" },
          { id: "s_2548f2", reps: 1, dist: 50, desc: "Butterfly — TEST 50 (ALL OUT)", interval: "On 2:05", focus: "Sprint fly test" },
          { id: "s_qn81e4", reps: 1, dist: 250, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_5zpxol", reps: 4, dist: 75, desc: "25 fly fast / 50 free easy", interval: "On 2:40", focus: "Apply fly volume" },
          { id: "s_b1go83", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Sprint + Endurance", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_mqbw85", reps: 4, dist: 25, desc: "Butterfly — ALL OUT", interval: "On 1:05", focus: "Wake up speed" },
          { id: "s_kieajj", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_31to9n", reps: 4, dist: 50, desc: "Butterfly — ALL OUT", interval: "On 1:50", focus: "Sprint" },
          { id: "s_9tf63a", reps: 1, dist: 200, desc: "Easy freestyle — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_4z784w", reps: 8, dist: 50, desc: "25 fly fast / 25 free moderate", interval: "On 1:35", focus: "Apply speed under fatigue" },
          { id: "s_8sapo1", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Sandwich", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_tzsxql", reps: 4, dist: 200, desc: "25 fly / 150 free / 25 fly (fly bookends)", interval: "On 6:20", focus: "Test fly endurance with bookend pattern" },
          { id: "s_wexbpy", reps: 4, dist: 50, desc: "Easy freestyle — between sets", interval: "On 1:35", focus: "Active recovery" },
        ]},
      { label: "Fly Hard-Easy 100s", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_2urq45", reps: 10, dist: 100, desc: "Alternate: 25 fly fast + 75 free / 100 free easy", interval: "On 2:05", focus: "Hard fly / easy free pattern" },
        ]},
      { label: "Fly Aerobic Volume", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_mea2u8", reps: 11, dist: 100, desc: "25 fly / 75 free — aerobic pace (RPE 6/10), smooth", interval: "On 2:05", focus: "Long-form fly tolerance at aerobic effort" },
        ]},
      { label: "Fly Pull + Sprint", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_byb433", reps: 4, dist: 100, desc: "Pull (buoy) — 25 fly pull / 75 free pull, focus on catch", interval: "On 2:20", focus: "Build fly catch", eq: "pull" },
          { id: "s_kp5qsw", reps: 8, dist: 50, desc: "Butterfly — fast (full stroke)", interval: "On 1:35", focus: "Apply pull power to fly" },
          { id: "s_prcvbc", reps: 1, dist: 200, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Stroke Drop", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_o6ft4c", reps: 4, dist: 50, desc: "Butterfly — full stroke, count strokes per 25", interval: "On 1:35", focus: "Baseline fly stroke count" },
          { id: "s_txpihr", reps: 4, dist: 50, desc: "Butterfly — same speed, count -1", interval: "On 1:35", focus: "Reduce strokes" },
          { id: "s_c4nl7w", reps: 8, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:35", focus: "Apply efficiency to fast fly" },
          { id: "s_6hmpza", reps: 4, dist: 50, desc: "Easy freestyle — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "Fly Long Kick Day", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_m2qs19", reps: 1, dist: 400, desc: "Dolphin kick (board) — easy aerobic", interval: "On 9:25", focus: "Long dolphin base", eq: "kick" },
          { id: "s_i746d5", reps: 4, dist: 100, desc: "Dolphin kick — moderate, descend 1→4", interval: "On 3:10", focus: "Build kick intensity", eq: "kick" },
          { id: "s_txcykz", reps: 4, dist: 50, desc: "Butterfly — fast, drive with the kick", interval: "On 1:35", focus: "Apply kick to swim" },
          { id: "s_9aebm7", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Mid-Distance Build", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_h414ai", reps: 1, dist: 400, desc: "Continuous: 25 fly / 75 free — easy aerobic", interval: "On 9:25", focus: "Aerobic fly base" },
          { id: "s_61mwtw", reps: 1, dist: 300, desc: "Continuous: 25 fly / 75 free — moderate", interval: "On 6:20", focus: "Build" },
          { id: "s_fd2se5", reps: 1, dist: 200, desc: "Continuous: 25 fly / 75 free — strong", interval: "On 6:20", focus: "Threshold" },
          { id: "s_6k8pie", reps: 1, dist: 100, desc: "Butterfly — fast (full stroke)", interval: "On 2:40", focus: "Sub-threshold fly" },
          { id: "s_urairm", reps: 2, dist: 50, desc: "Easy freestyle — close", interval: "On 1:35", focus: "Cool down" },
        ]},
      { label: "Fly 8×125", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_0ydiyh", reps: 8, dist: 125, desc: "25 fly / 100 free — moderate to strong, descend", interval: "On 2:40", focus: "Build fly tolerance with free for distance" },
        ]},
      { label: "Fly Speed Endurance", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_dbnsgc", reps: 8, dist: 75, desc: "25 fly fast / 50 free moderate", interval: "On 2:40", focus: "Repeated fly bursts" },
          { id: "s_4c7c01", reps: 4, dist: 100, desc: "Easy freestyle — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Underwater Day", totalYards: 1000, types: [], strokes: ["fly"], sets: [
          { id: "s_wk2h0p", reps: 8, dist: 25, desc: "Streamline dolphin kick — 12.5 yd UW + 12.5 swim out", interval: "On 1:05", focus: "Underwater work", eq: "underkick" },
          { id: "s_azi07a", reps: 4, dist: 50, desc: "Butterfly — focus on push-off + UW dolphins", interval: "On 1:35", focus: "UW into fly", eq: "underkick" },
          { id: "s_ao2et4", reps: 6, dist: 50, desc: "25 fly fast / 25 free easy", interval: "On 1:35", focus: "Apply UW work to swim" },
          { id: "s_awt30e", reps: 4, dist: 75, desc: "Easy freestyle — close", interval: "On 1:50", focus: "Cool down" },
        ]},
      { label: "Fly + Sprint Free", totalYards: 1100, types: [], strokes: ["fly"], sets: [
          { id: "s_51f5sx", reps: 4, dist: 100, desc: "25 fly fast / 75 free moderate", interval: "On 2:05", focus: "Build fly volume with free recovery" },
          { id: "s_zna1xo", reps: 1, dist: 200, desc: "Easy choice — recovery", interval: "No interval — swim easy", focus: "Reset" },
          { id: "s_lmayzp", reps: 8, dist: 50, desc: "Freestyle — ALL OUT (apply fly fitness)", interval: "On 1:35", focus: "Free sprint after fly volume" },
          { id: "s_mkp5w4", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly Single-Arm Day", totalYards: 1200, types: [], strokes: ["fly"], sets: [
          { id: "s_92uc5t", reps: 4, dist: 25, desc: "Single-arm fly drill (left)", interval: "On :50", focus: "Left-arm fly mechanics" },
          { id: "s_l195rs", reps: 4, dist: 25, desc: "Single-arm fly drill (right)", interval: "On :50", focus: "Right-arm fly mechanics" },
          { id: "s_us4mwh", reps: 4, dist: 50, desc: "Fly — 3-3-3 drill (3R / 3L / 3 full)", interval: "On 1:35", focus: "Build to full stroke" },
          { id: "s_0uh35w", reps: 4, dist: 75, desc: "25 fly drill / 50 fly swim", interval: "On 2:40", focus: "Apply drill into fly" },
          { id: "s_6tunuw", reps: 4, dist: 100, desc: "25 fly fast / 75 free moderate", interval: "On 2:05", focus: "Volume with technique" },
          { id: "s_7da9od", reps: 1, dist: 100, desc: "Easy choice — close", interval: "No interval — swim easy", focus: "Cool down" },
        ]},
      { label: "Fly 15×100 Volume", totalYards: 1500, types: [], strokes: ["fly"], sets: [
          { id: "s_jxhe0k", reps: 15, dist: 100, desc: "25 fly fast / 75 free moderate — maintain fly quality each rep", interval: "On 2:20", focus: "High-rep fly tolerance with free recovery" },
        ]},
      { label: "Fly 20×100 Marathon", totalYards: 2000, types: [], strokes: ["fly"], sets: [
          { id: "s_deskf1", reps: 20, dist: 100, desc: "25 fly / 75 free — aerobic pace throughout, focus on fly form on every lead 25", interval: "On 2:20", focus: "Extended fly volume at aerobic effort" },
        ]},
      { label: "Fly 8×300 Volume", totalYards: 2400, types: [], strokes: ["fly"], sets: [
          { id: "s_obq76z", reps: 8, dist: 300, desc: "50 fly / 250 free — strong fly lead, aerobic free, hold fly form on every rep", interval: "On 5:45", focus: "High-volume fly tolerance with free for distance" },
        ]},
      { label: "Fly 50s Repeat Unit", totalYards: 400, types: [], strokes: ["fly"], sets: [
          { id: "s_da9co2", reps: 8, dist: 50, desc: "25 fly / 25 freestyle — full fly effort on lead 25, easy free recovery", interval: "On 1:35", focus: "Fly tolerance with built-in recovery — designed to repeat 2–3×" },
        ]},
      { label: "Fly 25+Free Repeat Unit", totalYards: 500, types: [], strokes: ["fly"], sets: [
          { id: "s_jzgyuf", reps: 6, dist: 50, desc: "25 fly ALL OUT / 25 freestyle easy", interval: "On 1:35", focus: "Max fly speed with active recovery" },
          { id: "s_6ae4x6", reps: 2, dist: 100, desc: "Freestyle — moderate, keep breathing relaxed", interval: "On 2:15", focus: "Aerobic flush before the next round" },
        ]},
      { label: "Fly Build Repeat Unit", totalYards: 600, types: [], strokes: ["fly"], sets: [
          { id: "s_uiq9nn", reps: 4, dist: 100, desc: "25 fly / 75 free — fly strong on lead 25, hold rhythm on the free", interval: "On 2:20", focus: "Fly volume without burnout — build fly quality each round" },
          { id: "s_sn9rvg", reps: 4, dist: 50, desc: "25 fly ALL OUT / 25 free easy", interval: "On 1:25", focus: "Top-speed fly to close each round" },
        ]},
      // ── VO2max mains (SCM, meters) — aerobic-power sets, ~95% effort.
      { label: "VO2 8×100 Free", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s01", reps: 8, dist: 100, desc: "Freestyle — VO2 effort, hold strong tempo to the wall", interval: "On 1:35", focus: "Aerobic power — short rest, just under race pace" },
        ]},
      { label: "VO2 6×150 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s02", reps: 6, dist: 150, desc: "Freestyle — fast, VO2 effort, hold form under fatigue", interval: "On 2:20", focus: "Sustained aerobic power across the 150" },
        ]},
      { label: "VO2 5×200 Free", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s03", reps: 5, dist: 200, desc: "Freestyle — VO2, even pace just under 200 race speed", interval: "On 3:10", focus: "High-end aerobic — negative-split each 200 if you can" },
        ]},
      { label: "VO2 10×100 Free Descend", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s04", reps: 10, dist: 100, desc: "Freestyle — descend 1 to 5, then hold all of 6 to 10 fast (VO2)", interval: "On 1:35", focus: "Find VO2 pace, then hold it fatigued" },
        ]},
      { label: "VO2 12×75 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s05", reps: 12, dist: 75, desc: "Freestyle — fast 75s, VO2 effort, strong final 25", interval: "On 1:08", focus: "Aerobic power with a close — short rest" },
        ]},
      { label: "VO2 16×50 Free", totalYards: 800, types: ["endurance","mixed","distance"], strokes: [], sets: [
          { id: "s_vo2s06", reps: 16, dist: 50, desc: "Freestyle — fast 50s, VO2 effort, hold stroke length under fatigue", interval: "On 0:48", focus: "High turnover at short rest — aerobic power" },
        ]},
      { label: "VO2 4×200 Free Build", totalYards: 800, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s07", reps: 4, dist: 200, desc: "Freestyle — build each 200 to VO2 by the third 50, fast final 50", interval: "On 3:10", focus: "Progressive aerobic-power 200s" },
        ]},
      { label: "VO2 8×125 Free", totalYards: 1000, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s08", reps: 8, dist: 125, desc: "Freestyle — fast effort, VO2, hold tempo through the odd distance", interval: "On 1:55", focus: "Aerobic power over an off-distance — stay long" },
        ]},
      { label: "VO2 8×100 IM", totalYards: 800, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2s09", reps: 8, dist: 100, desc: "100 IM — VO2 effort, hold legal stroke and tempo under fatigue", interval: "On 1:45", focus: "Aerobic power across all four strokes" },
        ]},
      { label: "VO2 6×150 IM", totalYards: 900, types: ["im","mixed"], strokes: [], sets: [
          { id: "s_vo2s10", reps: 6, dist: 150, desc: "150 IM (no free) — fast effort, VO2, strong transitions", interval: "On 2:35", focus: "Stroke-change power at high heart rate" },
        ]},
      { label: "VO2 8×100 Back", totalYards: 800, types: ["endurance","mixed"], strokes: ["back"], sets: [
          { id: "s_vo2s11", reps: 8, dist: 100, desc: "Backstroke — VO2 effort, steady fast tempo, hold hip rotation", interval: "On 1:45", focus: "Aerobic power on back — short rest" },
        ]},
      { label: "VO2 8×100 Breast", totalYards: 800, types: ["endurance","mixed"], strokes: ["breast"], sets: [
          { id: "s_vo2s12", reps: 8, dist: 100, desc: "Breaststroke — VO2 effort, strong pullouts, hold stroke length", interval: "On 1:55", focus: "Aerobic power on breast — repeatable just under race" },
        ]},
      { label: "VO2 6×100 Fly", totalYards: 600, types: ["endurance","mixed"], strokes: ["fly"], sets: [
          { id: "s_vo2s13", reps: 6, dist: 100, desc: "Butterfly — VO2 effort, swim fly while form holds then strong free finish", interval: "On 1:45", focus: "Fly aerobic power without form breakdown" },
        ]},
      { label: "VO2 20×50 Free", totalYards: 1000, types: ["endurance","mixed","distance"], strokes: [], sets: [
          { id: "s_vo2s14", reps: 20, dist: 50, desc: "Freestyle — fast 50s, VO2 effort, consistent send-off across all 20", interval: "On 0:48", focus: "Aerobic-power volume — hold pace, no fade" },
        ]},
      { label: "VO2 6×100 + 6×50 Free", totalYards: 900, types: ["distance","endurance","mixed"], strokes: [], sets: [
          { id: "s_vo2s15a", reps: 6, dist: 100, desc: "Freestyle — VO2 effort, hold tempo", interval: "On 1:35", focus: "Aerobic power" },
          { id: "s_vo2s15b", reps: 6, dist: 50, desc: "Freestyle — fast, VO2, quicker than the 100 pace", interval: "On 0:48", focus: "Speed-up to close the set" },
        ]},
    ];

    // ═══════════════════════════════════════════════════════════════
    //  KICK OPTIONS — standalone 5th-section kick banks. Non-typed
    //  (types:[]/strokes:[]) like warmup/cooldown so getBankOptions
    //  returns all options regardless of workout type. Yards bank +
    //  meters banks (50m / SCM) so unit-aware lookup never crosses
    //  banks needlessly. eq:["kickboard"] is a PREFERRED tag (bonus
    //  weight in the picker), never a hard requirement.
    // ═══════════════════════════════════════════════════════════════
    const KICK_OPTIONS = [
      { label: "Flutter Kick Builder", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_kk001", reps: 8, dist: 50, desc: "Flutter kick — alternate 25 moderate / 25 fast", interval: "On 1:05", focus: "Leg endurance with a speed kicker", eq: "kick" },
        ]},
      { label: "IM Kick Rotation", totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_kk002", reps: 4, dist: 50, desc: "Kick IM order — fly / back / breast / free by 50", interval: "On 1:10", focus: "Touch all four kicks", eq: "kick" },
          { id: "s_kk003", reps: 6, dist: 50, desc: "Choice kick — strong, streamline off each wall", interval: "On 1:05", focus: "Underwater push-offs", eq: "kick" },
        ]},
      { label: "Dolphin & Free Kick", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_kk004", reps: 6, dist: 50, desc: "Underwater dolphin kick — streamline off each wall", interval: "On 1:10", focus: "Underwater power", eq: "underkick" },
          { id: "s_kk005", reps: 6, dist: 50, desc: "Flutter kick on back — steady, hips up", interval: "On 1:05", focus: "Body position", eq: "kick" },
        ]},
    ];
    const KICK_OPTIONS_50M = [
      { label: "Flutter Kick Builder", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_kk001m", reps: 8, dist: 50, desc: "Flutter kick — alternate 25 moderate / 25 fast", interval: "On 1:10", focus: "Leg endurance with a speed kicker", eq: "kick" },
        ]},
      { label: "IM Kick Rotation", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_kk002m", reps: 4, dist: 50, desc: "Kick IM order — fly / back / breast / free by 50", interval: "On 1:15", focus: "Touch all four kicks", eq: "kick" },
          { id: "s_kk003m", reps: 4, dist: 100, desc: "Choice kick — strong, streamline off each wall", interval: "On 2:25", focus: "Underwater push-offs", eq: "kick" },
        ]},
      { label: "Dolphin & Free Kick", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_kk004m", reps: 6, dist: 50, desc: "Underwater dolphin kick — streamline off each wall", interval: "On 1:15", focus: "Underwater power", eq: "underkick" },
          { id: "s_kk005m", reps: 6, dist: 50, desc: "Flutter kick on back — steady, hips up", interval: "On 1:10", focus: "Body position", eq: "kick" },
        ]},
    ];
    const KICK_OPTIONS_SCM = [
      { label: "Flutter Kick Builder", totalYards: 400, types: [], strokes: [], sets: [
          { id: "s_kk001s", reps: 8, dist: 50, desc: "Flutter kick — alternate 25 moderate / 25 fast", interval: "On 1:10", focus: "Leg endurance with a speed kicker", eq: "kick" },
        ]},
      { label: "IM Kick Rotation", totalYards: 500, types: [], strokes: [], sets: [
          { id: "s_kk002s", reps: 4, dist: 50, desc: "Kick IM order — fly / back / breast / free by 50", interval: "On 1:12", focus: "Touch all four kicks", eq: "kick" },
          { id: "s_kk003s", reps: 6, dist: 50, desc: "Choice kick — strong, streamline off each wall", interval: "On 1:08", focus: "Underwater push-offs", eq: "kick" },
        ]},
      { label: "Dolphin & Free Kick", totalYards: 600, types: [], strokes: [], sets: [
          { id: "s_kk004s", reps: 6, dist: 50, desc: "Underwater dolphin kick — streamline off each wall", interval: "On 1:12", focus: "Underwater power", eq: "underkick" },
          { id: "s_kk005s", reps: 6, dist: 50, desc: "Flutter kick on back — steady, hips up", interval: "On 1:08", focus: "Body position", eq: "kick" },
        ]},
    ];

    // ═══════════════════════════════════════════════════════════════
    //  SET-ID LOOKUP MAP — set_id → "reps×dist – parent label"
    //  Built once at script load by walking every *_OPTIONS bank.
    //  Used by R3 Curation Log to render human-readable set names
    //  instead of raw "s_xxxxxx" identifiers. Old IDs no longer in
    //  the bank (e.g., a user favorited a set that's since been
    //  removed) fall back to the raw ID at the render site.
    // ═══════════════════════════════════════════════════════════════
    const SET_ID_NAME_MAP = (() => {
      const m = new Map();
      // Phase H Stage 2: all 12 bank constants are flat arrays now.
      const walkArray = (arr) => {
        if (!Array.isArray(arr)) return;
        for (const opt of arr) {
          if (!opt || !Array.isArray(opt.sets)) continue;
          for (const s of opt.sets) {
            if (s && s.id) m.set(s.id, `${s.reps || 1}×${s.dist} – ${opt.label}`);
          }
        }
      };
      for (const bank of [
        WARMUP_OPTIONS, COOLDOWN_OPTIONS, DRILL_OPTIONS, MAIN_OPTIONS,
        WARMUP_OPTIONS_50M, COOLDOWN_OPTIONS_50M, DRILL_OPTIONS_50M, MAIN_OPTIONS_50M,
        WARMUP_OPTIONS_SCM, COOLDOWN_OPTIONS_SCM, DRILL_OPTIONS_SCM, MAIN_OPTIONS_SCM,
        KICK_OPTIONS, KICK_OPTIONS_50M, KICK_OPTIONS_SCM,
      ]) walkArray(bank);
      return m;
    })();
    export function setIdToName(setId) {   // exported for src/components/reports/R3CurationLogTab.jsx (engine extractor strips `export` before vm-eval)
      return SET_ID_NAME_MAP.get(setId) || setId;
    }

    // ═══════════════════════════════════════════════════════════════
    //  BUDGET-AWARE GENERATOR
    //  Filters options so the workout stays within [1900, maxYards]
    // ═══════════════════════════════════════════════════════════════

    const pick = (arr) => arr[Math.floor(Math.random() * arr.length)];

    // Weighted random pick. weightFn(item) returns a positive number.
    // Items with weight 0 are excluded. Falls back to uniform pick if total is 0.
    // ─── Audio cues (W1) ─────────────────────────────────────────────
    // Web Audio API tone generator for rest-timer cues.
    // Lazy-init the AudioContext on first call. iOS requires the first
    // call to happen inside a user gesture; we prime it from the rest
    // picker's Start button (and from any user click that opens Run mode).
    let _audioCtx = null;
    function primeAudioCtx() {
      if (!_audioCtx) {
        try {
          const AC = window.AudioContext || window.webkitAudioContext;
          if (!AC) return null;
          _audioCtx = new AC();
        } catch (_) { return null; }
      }
      if (_audioCtx.state === "suspended") {
        _audioCtx.resume().catch(() => {});
      }
      return _audioCtx;
    }
    function playRestCue(kind) {
      const ctx = primeAudioCtx();
      if (!ctx) return;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = "sine";
      const now = ctx.currentTime;
      if (kind === "go") {
        osc.frequency.value = 1320;
        gain.gain.setValueAtTime(0.001, now);
        gain.gain.exponentialRampToValueAtTime(0.35, now + 0.01);
        gain.gain.exponentialRampToValueAtTime(0.001, now + 0.60);
        osc.connect(gain).connect(ctx.destination);
        osc.start(now);
        osc.stop(now + 0.60);
      } else {
        // Short beep at T-3/T-2/T-1
        osc.frequency.value = 880;
        gain.gain.setValueAtTime(0.001, now);
        gain.gain.exponentialRampToValueAtTime(0.25, now + 0.01);
        gain.gain.exponentialRampToValueAtTime(0.001, now + 0.13);
        osc.connect(gain).connect(ctx.destination);
        osc.start(now);
        osc.stop(now + 0.13);
      }
    }

    function pickWeighted(arr, weightFn) {
      const weights = arr.map(weightFn);
      const total = weights.reduce((s, w) => s + (w > 0 ? w : 0), 0);
      if (total <= 0) {
        // S4 #10 — defensive guard: if the weight function zeroed everything
        // (e.g. phase × recovery filter collapsed every option's score), we
        // fall back to uniform pick. That's a safe degradation but masks an
        // upstream bug where the filter should have left at least one option
        // with positive weight. Surface it in the console so the issue is
        // visible during development.
        if (typeof console !== "undefined" && console.warn) {
          console.warn("[pickWeighted] all weights non-positive — falling back to uniform pick", { arrLen: arr.length });
        }
        return pick(arr);
      }
      let r = Math.random() * total;
      for (let i = 0; i < arr.length; i++) {
        const w = weights[i] > 0 ? weights[i] : 0;
        r -= w;
        // Strict `<` so each weight gets a half-open interval [a, b). The old
        // `<=` boundary biased toward index 0 when r landed exactly at 0 after
        // subtracting the first weight — measurable only for integer weights
        // (e.g. [1,1,1] gave arr[0] a slight edge at r=1.0 exactly).
        if (r < 0) return arr[i];
      }
      return arr[arr.length - 1];
    }

    // Returns the main-set label for a block, whether the block came from a
    // freshly generated workout (label preserved by spread), a regenerated
    // section, or a history-loaded entry (handleSave persists `label` now;
    // older rows fall back to parsing the block name).
    function extractMainLabel(block) {
      if (!block || block.section !== "main") return null;
      if (block.label) return block.label;
      const m = (block.name || "").match(/^Main Set — (.+?)(?:\s+×\d+)?$/);
      return m ? m[1] : null;
    }

    const MIN_YARDS  = 1900;
    const MIN_METERS = 2000;

    // Each checked equipment item must be satisfied by ≥ 1 set in the drill+main blocks.
    // satisfiedBy lists the eq tags whose presence counts for that equipment.
    const EQUIP_REQUIREMENTS = {
      kickboard: ["kick", "kickPart"],
      fins:      ["kick", "kickPart", "underkick"],
      paddles:   ["pull"],
      pullBuoy:  ["pull"],
      snorkel:   ["snorkel"],
    };

    // F: equipment can be in three modes: "off" | "preferred" | "required".
    // For backward compatibility, treat truthy bools as "required" and falsy /
    // missing as "off". These helpers centralize the read so the rest of the
    // pipeline doesn't need to care about the wire format.
    function equipMode(equipment, k) {
      if (!equipment) return "off";
      const v = equipment[k];
      if (v === "required" || v === true)  return "required";
      if (v === "preferred")               return "preferred";
      return "off";
    }
    function isEquipOn(equipment, k)       { return equipMode(equipment, k) !== "off"; }
    function isEquipRequired(equipment, k) { return equipMode(equipment, k) === "required"; }
    function isEquipPreferred(equipment, k){ return equipMode(equipment, k) === "preferred"; }

    function eqTagsIn(option) {
      return new Set(option.sets.filter(s => s.eq).map(s => s.eq));
    }

    // Hard-filter check: only "required" equipment enforces the constraint.
    // "Preferred" is handled by weight-bias at pick time (see preferredEquipBoost).
    function combinationSatisfies(dr, ma, equipment) {
      if (!equipment) return true;
      const tags = new Set([...eqTagsIn(dr), ...eqTagsIn(ma)]);
      for (const k of Object.keys(EQUIP_REQUIREMENTS)) {
        if (isEquipRequired(equipment, k) && !EQUIP_REQUIREMENTS[k].some(t => tags.has(t))) return false;
      }
      return true;
    }

    // Returns the count of "preferred" equipment items whose required `eq`
    // tags appear in this option's sets. Used to multiply pick weights.
    function preferredEquipMatches(option, equipment) {
      if (!equipment || !option || !option.sets) return 0;
      const tags = eqTagsIn(option);
      let n = 0;
      for (const k of Object.keys(EQUIP_REQUIREMENTS)) {
        if (!isEquipPreferred(equipment, k)) continue;
        if (EQUIP_REQUIREMENTS[k].some(t => tags.has(t))) n += 1;
      }
      return n;
    }

    // pinnedBlocks: { warmup?, drill?, main?, cooldown? } — sections to hold fixed.
    // For each pinned section the block is used as-is; unpinned sections are generated
    // to fill the remaining budget. Budget math uses freeBudget = maxYards - pinnedTotal.
    // Maps workout type → warmup affinity family. Types not listed get no filter (use all warmups).
    const TYPE_WARMUP_FAMILY = {
      sprint:    "sprint",
      endurance: "endurance",
      distance:  "endurance",
      technique: "technique",
    };

    // ═══════════════════════════════════════════════════════════════
    //  W2 — NATURAL-LANGUAGE INTENT PARSER (keyword, no LLM)
    //  ───────────────────────────────────────────────────────────────
    //  Coach types something like "easy 2k aerobic" or "threshold 25
    //  min @ 2:00 with paddles" — parser extracts known tokens, leaves
    //  the rest as `unparsed` for the UI to display.
    //
    //  Order of consume() passes matters:
    //    1. pace (so @2:00 doesn't collide with bare distance)
    //    2. distance with explicit unit suffix (k/y/yd/m/meters/yards)
    //    3. duration (min/hr)
    //    4. workout type (im/distance/sprint/etc — strip BEFORE zone
    //       so standalone "sprint" maps to type, not zone)
    //    5. recovery mode toggle ("recovery" / "easy day")
    //    6. zones (easy/aerobic/threshold/vo2/anaerobic)
    //    7. equipment (paddles/fins/snorkel/kickboard/pull buoy)
    //
    //  Returns { tokens: [...], unparsed: "..." }. Vocabulary expansion
    //  is intentional — add categories opportunistically as coaches
    //  surface phrases that should parse.
    function parseIntent(text) {
      const tokens = [];
      // Surrounding spaces simplify \b boundary matches at start/end.
      let remaining = " " + (text || "").toLowerCase().trim() + " ";

      function consume(re, mkToken) {
        let m;
        while ((m = remaining.match(re)) !== null) {
          const tok = mkToken(m);
          if (tok) tokens.push(tok);
          remaining = remaining.slice(0, m.index) + " " + remaining.slice(m.index + m[0].length);
        }
      }

      // 1. Pace: "@2:00", "@ 2:00"
      consume(/@\s*(\d+):(\d{2})\b/, m => {
        const secs = parseInt(m[1], 10) * 60 + parseInt(m[2], 10);
        if (secs < 30 || secs > 300) return null;  // outside realistic 100-pace range
        return { kind: "pace", value: secs, raw: m[0].trim() };
      });

      // 2. Distance with explicit unit: "2k" → 2000 yd (idiomatic), "1500m" → meters, "2400 yd"
      consume(/\b(\d+)\s*(k|yd|yards|m|meters|y)\b/, m => {
        const n = parseInt(m[1], 10);
        const u = m[2];
        let value, unit;
        if (u === "k")                                       { value = n * 1000; unit = "y"; }
        else if (u === "y" || u === "yd" || u === "yards")   { value = n;        unit = "y"; }
        else if (u === "m" || u === "meters")                { value = n;        unit = "m"; }
        if (value < 500 || value > 10000) return null;       // outside realistic workout range
        return { kind: "distance", value, unit, raw: m[0].trim() };
      });

      // 2b. Bare-number distance fallback. Catches "2400 with fins" where
      //     coach drops the unit. Default unit is yards; handleApplyIntent
      //     converts to current pool unit if needed. Range gate (500-10000)
      //     is loose enough to cover all swim distances; tight enough to
      //     reject incidental small numbers and the @M:SS pace pattern
      //     (which has been consumed in step 1 already).
      consume(/\b(\d{3,5})\b/, m => {
        const n = parseInt(m[1], 10);
        if (n < 500 || n > 10000) return null;
        return { kind: "distance", value: n, unit: "y", raw: m[0] };
      });

      // 3. Duration: "25 min", "1 hr", "2 hours"
      consume(/\b(\d+)\s*(minutes?|min|hours?|hrs?|hr)\b/, m => {
        const n = parseInt(m[1], 10);
        const isHr = /^(hr|hrs|hour|hours)$/.test(m[2]);
        const mins = isHr ? n * 60 : n;
        if (mins < 15 || mins > 240) return null;
        return { kind: "duration", value: mins, raw: m[0].trim() };
      });

      // 4. Workout types — strip BEFORE zones so "sprint" maps to type
      const TYPES = ["im","distance","sprint","endurance","technique","mixed","back","breast","fly"];
      for (const t of TYPES) {
        const re = new RegExp(`\\b${t}\\b`, "i");
        const m  = remaining.match(re);
        if (m) {
          tokens.push({ kind: "type", value: t, raw: t });
          remaining = remaining.slice(0, m.index) + " " + remaining.slice(m.index + m[0].length);
        }
      }

      // 5. Recovery mode
      const recoveryMatch = remaining.match(/\brecovery\b|\beasy day\b/);
      if (recoveryMatch) {
        tokens.push({ kind: "recovery", value: true, raw: recoveryMatch[0] });
        remaining = remaining.slice(0, recoveryMatch.index) + " " + remaining.slice(recoveryMatch.index + recoveryMatch[0].length);
      }

      // 6. T-pace (informational pace reference — no numeric value)
      const tpaceMatch = remaining.match(/@?\s*\bt-?pace\b/i);
      if (tpaceMatch) {
        tokens.push({ kind: "pace_ref", value: "T-pace", raw: tpaceMatch[0].trim() });
        remaining = remaining.slice(0, tpaceMatch.index) + " " + remaining.slice(tpaceMatch.index + tpaceMatch[0].length);
      }

      // 7. Equipment — multi-word phrases first ("pull buoy" before "buoy"
      //    AND before stroke "pull") so equipment matches consume the phrase.
      const EQUIP = [
        ["paddles",   "paddles"],
        ["fins",      "fins"],
        ["snorkel",   "snorkel"],
        ["kickboard", "kickboard"],
        ["pull buoy", "pullBuoy"],
        ["pullbuoy",  "pullBuoy"],
        ["buoy",      "pullBuoy"],
      ];
      for (const [phrase, key] of EQUIP) {
        const re = new RegExp(`\\b${phrase}\\b`, "i");
        const m  = remaining.match(re);
        if (m) {
          tokens.push({ kind: "equipment", value: key, raw: phrase });
          remaining = remaining.slice(0, m.index) + " " + remaining.slice(m.index + m[0].length);
        }
      }

      // 8. Stroke actions (kick/pull/drill/swim/scull and gerund/plural forms).
      //    Informational only — no form-state mapping today. Future: could
      //    bias the picker toward sets containing the matched stroke.
      //    Runs AFTER equipment so "pull buoy" is consumed first; "pull"
      //    on its own (or "pulling") still matches here.
      consume(/\b(kick|pull|drill|swim|scull)(ing|s)?\b/i, m => {
        return { kind: "stroke", value: m[1].toLowerCase(), raw: m[0] };
      });

      // 9. Zones — primary names + common effort synonyms coaches use.
      //    Effort synonyms map to the closest zone so the chip preview shows
      //    the canonical zone name. Run AFTER type so standalone "sprint"
      //    has already been consumed as a type.
      const ZONE_SYNONYMS = [
        ["easy",      "easy"],
        ["aerobic",   "aerobic"],
        ["threshold", "threshold"],
        ["vo2",       "vo2"],
        ["anaerobic", "anaerobic"],
        ["strong",    "threshold"],
        ["hard",      "anaerobic"],
        ["all-out",   "anaerobic"],
        ["all out",   "anaerobic"],
        ["fast",      "vo2"],
        ["smooth",    "aerobic"],
        ["steady",    "aerobic"],
        ["light",     "easy"],
        ["moderate",  "aerobic"],
      ];
      for (const [phrase, zone] of ZONE_SYNONYMS) {
        const re = new RegExp(`\\b${phrase}\\b`, "i");
        const m  = remaining.match(re);
        if (m) {
          tokens.push({ kind: "zone", value: zone, raw: phrase });
          remaining = remaining.slice(0, m.index) + " " + remaining.slice(m.index + m[0].length);
        }
      }

      // Strip trivial connectors / punctuation from unparsed remainder.
      // Expanded list captures coach filler ("today", "session", "workout",
      // "set", "doing", "yards" stray, etc.) so the unparsed display is clean.
      const unparsed = remaining
        .replace(/\b(with|and|a|an|the|some|just|please|also|plus|of|on|at|for|to|today|session|workout|set|doing|yards|yard|meters|meter|only)\b/gi, " ")
        .replace(/[,.!?]/g, " ")
        .replace(/\s+/g, " ")
        .trim();

      // Dedupe (kind, value) tuples — happens when synonyms collide with
      // canonical names (e.g. "strong threshold" → strong→threshold + threshold).
      // Preserves first-seen ordering. Tokens with structured `value` (objects)
      // get their JSON used as the dedup key — currently no token kind has
      // object values so this is a future-safety guard.
      const seen = new Set();
      const dedupedTokens = [];
      for (const t of tokens) {
        const key = `${t.kind}:${typeof t.value === "object" ? JSON.stringify(t.value) : t.value}`;
        if (seen.has(key)) continue;
        seen.add(key);
        dedupedTokens.push(t);
      }

      return { tokens: dedupedTokens, unparsed };
    }

    // 2026-05-15: refactored to single options object for readability and to
    // make adding/removing optional knobs safe (no positional ordering pitfalls).
    // All previous positional args are now keys on the opts object — the
    // function body still references them by name via destructuring.
    // Section-proportion controls (real-shift). Each bias mode declares
    // target ratios of freeBudget per section. "balanced" is sentinel for
    // no-change-to-current-allocator. Non-balanced modes:
    //   - compute target_section = freeBudget * ratio
    //   - bias warmup + drill pickers toward options nearest the target
    //     (inverse-distance weighting on top of existing favBoost/equip)
    //   - override main's hardcoded 65% floor with the bias-specific ratio
    //   - cooldown left as-is (always small, near-floor)
    // Targets are guidance, not hard floors — the existing attempts-loop +
    // bank-availability fallbacks still apply.
    const SECTION_BIAS_RATIOS = {
      balanced:     null,                                                       // sentinel: keep current behavior
      warmup_heavy: { warmup: 0.22, drill: 0.12, main: 0.62, cooldown: 0.04 },
      drill_heavy:  { warmup: 0.09, drill: 0.26, main: 0.61, cooldown: 0.04 },
      long_main:    { warmup: 0.06, drill: 0.07, main: 0.83, cooldown: 0.04 },
    };

    // ── S3: helpers for engine routing per section ───────────────────
    // generateEngineForSection — pick an applicable template (weighted
    // among those applicable to section × typeKey; v1.3 applies 0.25×
    // weight to disfavored (template_id, stroke) tuples) and invoke the
    // engine's full pipeline (validate + retry-3 + anti-repeat).
    // Returns { option, templateUsed, attempts } or { option: null } on
    // no-applicable-template or full-failure (caller silently falls
    // back to bank).
    function generateEngineForSection(section, typeId, budgetYd, recent, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, activeConstraints = []) {
      if (typeof TEMPLATE_ENGINE === "undefined") return { option: null };
      const applicable = TEMPLATE_ENGINE.applicable(section, typeId);
      if (!applicable.length) return { option: null };
      // v1.3 + v1.8 + v1.13 — weighted pick.
      //   Favorite (template_id, stroke) → 3× weight (UNIVERSAL precedence:
      //     favorite wins over disfavor when both apply to same tuple).
      //   Disfavored (template_id, stroke) → disfavorMultiplier (0.25 in
      //     downweight mode, 0 in exclude mode) — IF not also favorited.
      //   If all weights collapse to zero, fall back to uniform random.
      const stroke = templateDefaultStroke(typeId);
      const favSet = new Set((engineFavorites    || []).map(f => `${f.template_id}:${f.stroke}`));
      const disSet = new Set((engineDisfavorites || []).map(d => `${d.template_id}:${d.stroke}`));
      const disfavorMultiplier = disfavorMode === "exclude" ? 0 : 0.25;
      const weights = applicable.map(t => {
        const k = `${t.id}:${stroke}`;
        if (favSet.has(k)) return 3;                  // v1.13: favorite wins
        if (disSet.has(k)) return disfavorMultiplier; // disfavor downweight
        return 1;
      });
      const totalW = weights.reduce((s, w) => s + w, 0);
      let tpl;
      if (totalW <= 0) {
        tpl = applicable[Math.floor(Math.random() * applicable.length)];
      } else {
        let r = Math.random() * totalW;
        let pickIdx = applicable.length - 1;
        for (let i = 0; i < applicable.length; i++) {
          r -= weights[i];
          if (r < 0) { pickIdx = i; break; }
        }
        tpl = applicable[pickIdx];
      }
      return TEMPLATE_ENGINE.generateWithRetry({
        templateId: tpl.id, typeKey: typeId, section, budgetYd, recent: recent || [], lanesPaceSecs, activeConstraints,
      });
    }

    // applyEngineOverrides — replace bank-picked sections with engine
    // output when sectionSources[sect] === "engine" (or "mix" — TODO S3.4).
    // The bank picker still chooses an option per section; if engine is
    // requested, we replace that option AFTER the picker succeeds, sizing
    // the engine output to the bank option's totalYards. This preserves
    // the picker's combination validation (equipment, yardage min/max)
    // while substituting content per section. Engine output gets a
    // __engineMeta marker so the renderer can show the ⚡ badge.
    function applyEngineOverrides(typeId, sections, sources, recent, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, activeConstraints = []) {
      if (!sources) return sections;
      const out = { ...sections };
      // kick is bank-only (sources never names it from the UI), so its
      // `sources[sect] || "bank"` short-circuits to bank → no override.
      // Listed here only so a future kick-source would route generically;
      // generateEngineForSection degrades to { option: null } for sections
      // with no applicable template, so an unknown source can't throw.
      for (const sect of ["warmup", "drill", "kick", "main", "cooldown"]) {
        const src = sources[sect] || "bank";
        if (src === "engine") {
          const original = sections[sect];
          if (!original || !original.totalYards) continue;
          const res = generateEngineForSection(sect, typeId, original.totalYards, recent, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, activeConstraints);
          if (res && res.option) {
            out[sect] = {
              ...res.option,
              __engineMeta: {
                template_id: res.templateUsed,
                stroke: templateDefaultStroke(typeId),
              },
            };
          }
          // else: engine had no applicable template or all attempts failed —
          // silent bank fallback (original section stays unchanged)
        } else if (src === "mix") {
          // Mix: bank prefix + engine suffix, two parented sub-blocks under
          // one section header (per spec §7.3). Engine sub-block sized at
          // ~50% of the bank prefix's totalYards. Total section yards =
          // bank + engine, which exceeds the picker's per-section
          // allocation; documented as known limitation (mix is opt-in
          // extra variety, not a strict budget mode).
          const original = sections[sect];
          if (!original || !original.totalYards) continue;
          const engineBudget = Math.max(100, Math.round(original.totalYards * 0.5));
          const res = generateEngineForSection(sect, typeId, engineBudget, recent, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, activeConstraints);
          if (res && res.option) {
            const stroke = templateDefaultStroke(typeId);
            out[sect] = {
              // Note: deliberately omit `section` so buildWorkout's
              // hardcoded section value applies (passing undefined here
              // would override the hardcode via the spread).
              label: `${original.label} + Engine`,
              totalYards: original.totalYards + res.option.totalYards,
              // Renderer (WorkoutBlock) checks for subBlocks first and
              // renders each as its own labeled box if present. Engine
              // sub-block carries __engineMeta for the ⚡ badge.
              subBlocks: [
                {
                  label: original.label,
                  totalYards: original.totalYards,
                  sets: original.sets,
                },
                {
                  label: res.option.label,
                  totalYards: res.option.totalYards,
                  sets: res.option.sets,
                  __engineMeta: { template_id: res.templateUsed, stroke },
                },
              ],
              // Pass-through `sets` (concatenated) for backward compat
              // with code that reads block.sets directly (totals, print).
              sets: [...original.sets, ...res.option.sets],
              // Top-level engineMeta marks "this section used the engine"
              // for the write-side path that prepends to recent[].
              __engineMeta: { template_id: res.templateUsed, stroke },
            };
          }
          // else: no applicable engine template → silent bank fallback
        }
      }
      return out;
    }

    function generateWorkout({
      typeId,
      maxYards,
      equipment,
      favorites = [],
      poolMode = "25y",
      userMin = null,
      pinnedBlocks = {},
      recentLabels = [],
      recoveryMode = false,
      phase = null,
      favoriteSetIds = null,
      sectionBias = "balanced",
      sectionSources = null,            // S3 — { warmup, drill, main, cooldown } ∈ { bank, engine, mix }
      recentEngineTemplates = [],       // S3 — anti-repeat memory (last 10 { template_id, stroke, ts })
      disfavorites = [],                // v1.2 — labels with 0.25× pick weight (mutex with favorites)
      engineDisfavorites = [],          // v1.3 — (template_id, stroke) tuples with 0.25× engine pick weight
      engineFavorites = [],             // v1.13 — (template_id, stroke) tuples with 3× engine pick weight
      disfavorSetIds = null,            // v1.5 — Set<set_id> with 0.25× weight per match (mutex with favoriteSetIds)
      disfavorMode = "downweight",      // v1.8 — "downweight" (0.25×) or "exclude" (hard-exclude, weight 0)
      lanesPaceSecs = null,             // v2.0 — array of lane paces (sec/100). When set, picker filters options that don't fit all lanes (see optionFitsAllLanes). null/empty = single-pace path unchanged.
      ugcOverlay = null,                // UGC Phase B — caller's UGC overlay (12-key shape); merged into bank picker per exact pool mode.
      myConstraints = [],               // Phase 3 PSC slice 2 — caller's active swimmer_constraints rows. Step-0 hard-exclude. Skipped entirely in multi-lane mode (per scope §3.5; per-lane sub lands in slice 3).
      tonightOverrides = [],             // Phase 3 PSC slice 3 — in-memory constraint rows added at Generate time from the per-practice checklist. Unioned with myConstraints; never persisted.
      includedSections = ["warmup", "drill", "main", "cooldown"],   // Section model A1 — which sections to build. Default = all 4 (byte-identical to pre-A1). `main` is always required. Subsetting wired in A2.
    } = {}) {
      // Section model A1 — per-section inclusion flags. With the default all-4
      // list every flag is true, so all downstream guards are inert.
      const _incl = {
        warmup:   includedSections.includes("warmup"),
        drill:    includedSections.includes("drill"),
        kick:     includedSections.includes("kick"),        // kick — opt-in 5th section (defaults excluded)
        main:     true,                                     // main is never skippable
        cooldown: includedSections.includes("cooldown"),
      };
      // Section model A3 — excluded sections behave as pinned EMPTY blocks: the
      // solver's pinned path uses them as-is (0 yards, never picked from the
      // pool), applyEngineOverrides leaves 0-yard blocks untouched, and they
      // contribute 0 to pinnedTotal — so freeBudget is unchanged and flows to
      // the remaining sections (main absorbs the slack), holding the target.
      // buildWorkout then filters the empty blocks out of the output.
      const _EMPTY_SECTION = { label: "", totalYards: 0, sets: [] };
      pinnedBlocks = {
        ...pinnedBlocks,
        ...(_incl.warmup   ? {} : { warmup:   _EMPTY_SECTION }),
        ...(_incl.drill    ? {} : { drill:    _EMPTY_SECTION }),
        ...(_incl.kick     ? {} : { kick:     _EMPTY_SECTION }),
        ...(_incl.cooldown ? {} : { cooldown: _EMPTY_SECTION }),
      };
      // v1.8 — Resolve the multiplier once. "exclude" zeros the weight;
      // pickWeighted's existing "all weights zero → uniform fallback" is the
      // silent failsafe when hard-exclude would empty the eligible pool.
      const disfavorMultiplier = disfavorMode === "exclude" ? 0 : 0.25;
      let biasRatios = SECTION_BIAS_RATIOS[sectionBias] || null;
      // Section model A3 — when a section is skipped, renormalize the Mix bias
      // shares over the INCLUDED sections so the freed share redistributes
      // proportionally (without this, a skipped section's reserved % is lost).
      // Guarded to the skipped case: the all-4 path keeps the literal ratios
      // (no float drift), preserving byte-identical output.
      if (biasRatios && (!_incl.warmup || !_incl.drill || !_incl.cooldown)) {
        const _sum = ["warmup", "drill", "main", "cooldown"]
          .reduce((s, k) => s + (_incl[k] ? (biasRatios[k] || 0) : 0), 0);
        if (_sum > 0) {
          biasRatios = {
            warmup:   _incl.warmup   ? biasRatios.warmup   / _sum : 0,
            drill:    _incl.drill    ? biasRatios.drill    / _sum : 0,
            main:                      biasRatios.main      / _sum,
            cooldown: _incl.cooldown ? biasRatios.cooldown / _sum : 0,
          };
        }
      }
      const is25m    = poolMode === "25m";
      const is50m    = poolMode === "50m";
      const isMeters = is25m || is50m;
      // Unit-aware bank lookup. Each call returns options already in the
      // user's pool unit (converted from yd↔m if the fallback crossed banks).
      // UGC overlay (Phase B+) is appended in getBankOptions per exact pool mode.
      const _allWarmupsRaw   = getBankOptions("warmup",   typeId, poolMode, ugcOverlay);
      const _allCooldownsRaw = getBankOptions("cooldown", typeId, poolMode, ugcOverlay);
      const _drillListRaw    = getBankOptions("drill",    typeId, poolMode, ugcOverlay);
      const _kickListRaw     = getBankOptions("kick",     typeId, poolMode, ugcOverlay);
      const _mainListRaw     = getBankOptions("main",     typeId, poolMode, ugcOverlay);

      // Phase 3 PSC slice 2/3 — hard-exclude step 0. Per
      // PER_SWIMMER_CONSTRAINTS_SCOPE.md §3.4: drop options before weight
      // computation. SKIPPED entirely in multi-lane mode (fork 1 with Cap'n
      // 2026-05-26: per-lane substitution rendering lands in slice 3+).
      // Bank fallback: if PSC empties a pool, use the unfiltered pool so
      // generation never stalls (silent per fork 3; substitution rendering
      // is slice 3+). Tracked in `pscFilterFallback` for debug only.
      // Slice 3: tonightOverrides (per-practice checklist) unions with
      // persistent myConstraints — both feed the same picker filter.
      const _effectiveConstraints = [...(myConstraints || []), ...(tonightOverrides || [])];
      const usePsc = _effectiveConstraints.length > 0 && !(Array.isArray(lanesPaceSecs) && lanesPaceSecs.length > 0);
      const pscFilterFallback = { warmup: false, drill: false, kick: false, main: false, cooldown: false };
      const _narrowByPsc = (pool, sectionKey) => {
        if (!usePsc) return pool;
        const filtered = pool.filter(o => !optionViolatesPSC(o, _effectiveConstraints));
        if (filtered.length === 0) {
          pscFilterFallback[sectionKey] = true;
          return pool;
        }
        return filtered;
      };
      const _allWarmupsPsc   = _narrowByPsc(_allWarmupsRaw,   "warmup");
      const _allCooldownsPsc = _narrowByPsc(_allCooldownsRaw, "cooldown");
      const _drillListPsc    = _narrowByPsc(_drillListRaw,    "drill");
      const _kickListPsc     = _narrowByPsc(_kickListRaw,     "kick");
      const _mainListPsc     = _narrowByPsc(_mainListRaw,     "main");

      // v2.0 — Multi-lane lane-fit pre-filter. When lanesPaceSecs is non-empty,
      // narrow each pool to options whose intervals fit ALL lanes (rest ∈
      // [MIN_REST, MAX_REST_RATIO × swimTime]). Graceful fallback per section:
      // if the filter empties a pool, fall back to the unfiltered pool and
      // flag laneFitFallback so the UI can surface a "lanes too wide" warning.
      // When lanesPaceSecs is null/empty, the filter is a no-op and the path
      // is byte-identical to the single-pace flow.
      const useLaneFit = Array.isArray(lanesPaceSecs) && lanesPaceSecs.length > 0;
      const laneFitFallback = { warmup: false, drill: false, kick: false, main: false, cooldown: false };
      const _narrowByLanes = (pool, sectionKey) => {
        if (!useLaneFit) return pool;
        const filtered = pool.filter(o => optionFitsAllLanes(o, lanesPaceSecs));
        if (filtered.length === 0) {
          laneFitFallback[sectionKey] = true;
          return pool;
        }
        return filtered;
      };
      const allWarmups   = _narrowByLanes(_allWarmupsPsc,   "warmup");
      const allCooldowns = _narrowByLanes(_allCooldownsPsc, "cooldown");
      const drillList    = _narrowByLanes(_drillListPsc,    "drill");
      const kickList     = _narrowByLanes(_kickListPsc,     "kick");
      let   mainList     = _narrowByLanes(_mainListPsc,     "main");

      // C: recovery mode — filter mains down to easy/aerobic zone options.
      // If the filter would empty the pool, fall back to all mains and let
      // the +10% interval stretch + repeat-suppression carry the recovery
      // intent on its own.
      if (recoveryMode) {
        const recoveryMains = mainList.filter(o => {
          const z = inferBlockZone({ section: "main", sets: o.sets, primary_zone: o.primary_zone });
          return z === "easy" || z === "aerobic";
        });
        if (recoveryMains.length) mainList = recoveryMains;
      }

      // Fixed yardage from pinned sections; free budget is what remains for unpinned picks
      const pinnedTotal = (pinnedBlocks.warmup   ? pinnedBlocks.warmup.totalYards   : 0)
                        + (pinnedBlocks.drill    ? pinnedBlocks.drill.totalYards    : 0)
                        + (pinnedBlocks.kick     ? pinnedBlocks.kick.totalYards     : 0)
                        + (pinnedBlocks.main     ? pinnedBlocks.main.totalYards     : 0)
                        + (pinnedBlocks.cooldown ? pinnedBlocks.cooldown.totalYards : 0);
      const freeBudget  = maxYards - pinnedTotal;
      const hasPins     = pinnedTotal > 0;

      // Per-section minimums (0 for pinned sections — they don't consume free budget)
      const minWu = (pinnedBlocks.warmup   || !_incl.warmup)   ? 0 : Math.min(...allWarmups.map(o => o.totalYards));
      const minDr = (pinnedBlocks.drill    || !_incl.drill)    ? 0 : Math.min(...drillList.map(o => o.totalYards));
      const minKt = (pinnedBlocks.kick || !_incl.kick || !kickList.length) ? 0 : Math.min(...kickList.map(o => o.totalYards));
      const minMa = pinnedBlocks.main     ? 0 : Math.min(...mainList.map(o => o.totalYards));
      const minCd = (pinnedBlocks.cooldown || !_incl.cooldown) ? 0 : Math.min(...allCooldowns.map(o => o.totalYards));

      // C: recovery mode drops the absolute floor to 1200y (1300m) so a short
      // easy day is allowed regardless of the type's normal minimum.
      const recoveryFloor = isMeters ? 1300 : 1200;
      const baseAbsMin = recoveryMode
        ? Math.min(recoveryFloor, isMeters ? MIN_METERS : MIN_YARDS)
        : (isMeters ? MIN_METERS : MIN_YARDS);
      const absoluteMin = Math.max(baseAbsMin, userMin || 0);
      // S3 #4 — Floor scales with target so unpinned workouts stay close to maxYards.
      // Relaxed when (a) pins are present (user chose those blocks intentionally),
      // or (b) recovery mode is on — recovery wants a SHORT easy day, so the
      // -400 "stay near max" rule is dropped to let the absoluteMin (= 1200y /
      // 1300m floor from baseAbsMin) actually bite.
      const minTotal = (hasPins || recoveryMode)
        ? absoluteMin
        : Math.max(absoluteMin, maxYards - 400);

      const hasEquipReq = equipment && Object.keys(EQUIP_REQUIREMENTS).some(k => isEquipRequired(equipment, k));

      const attempts = (maxYards >= 3000 || hasEquipReq) ? 80 : 30;

      // Favorite-label boost shared by all section pickers. Returns 3 when
      // option's label is in the user's favorites (matching the existing
      // mainWeight × 3 bias), 1 otherwise. Cross-mode parallels share labels,
      // so favoriting "IM Warmup" in SCY also boosts the SCM/LCM versions.
      const favSet = new Set(favorites || []);
      // v1.2 — disfavor: 0.25× weight, mutually exclusive with favorite
      // (enforced at the DB layer; defensive check here in case a label is
      // somehow in both sets — favorite wins).
      const disfavorSet = new Set(disfavorites || []);
      const favBoost = (o) => {
        if (!o.label) return 1;
        if (favSet.has(o.label))      return 3;                  // favorite × 3
        if (disfavorSet.has(o.label)) return disfavorMultiplier; // v1.8: downweight (0.25) or exclude (0)
        return 1;
      };

      function pickBiased(arr, floor) {
        const preferred = arr.filter(o => o.totalYards >= floor);
        const source = preferred.length > 0 ? preferred : arr;
        // Apply favorite × 3 bias even on the floor-filtered pool.
        return pickWeighted(source, favBoost);
      }

      // Section-proportion helper. Picks from arr with weights that combine
      // a base weight function (e.g. favBoost, equipment-match) and an
      // inverse-quadratic distance penalty from a target yardage. Used by
      // warmup + drill pickers when sectionBias != "balanced".
      //   weight(opt) = base(opt) / (1 + |opt.totalYards - target| / target)^2
      // distance=0 → 1.0× base; distance=target → 0.25× base; distance=2×target → ~0.11× base.
      function pickWithTargetYards(arr, target, baseFn) {
        return pickWeighted(arr, (o) => {
          const distRatio = Math.abs((o.totalYards || 0) - target) / Math.max(1, target);
          const penalty = 1 / Math.pow(1 + distRatio, 2);
          return (baseFn ? baseFn(o) : 1) * penalty;
        });
      }

      for (let attempt = 0; attempt < attempts; attempt++) {
        let freeUsed = 0; // yards committed to unpinned sections this attempt

        // 1. Warmup
        let wu;
        if (pinnedBlocks.warmup) {
          wu = pinnedBlocks.warmup;
        } else {
          const validWarmups = allWarmups.filter(o =>
            o.totalYards <= freeBudget - minDr - minKt - minMa - minCd
          );
          if (!validWarmups.length) break;
          // Prefer warmups whose typeAffinity matches the workout type family
          const warmupFamily = TYPE_WARMUP_FAMILY[typeId] || null;
          const affinityPool = warmupFamily
            ? validWarmups.filter(o => o.typeAffinity && o.typeAffinity.includes(warmupFamily))
            : validWarmups;
          // S4 #12 — Use total maxYards (not freeBudget) for the floor. With
          // pinned drill+main+cooldown, freeBudget shrinks to just the warmup's
          // remaining share, so a 12%-of-freeBudget floor becomes essentially
          // zero. Tying to maxYards keeps the floor proportional to the
          // intended workout size regardless of pin state.
          const wuPool = affinityPool.length > 0 ? affinityPool : validWarmups;
          if (biasRatios) {
            // Real-shift: target yardage from bias ratios, distance-weighted.
            wu = pickWithTargetYards(wuPool, freeBudget * biasRatios.warmup, favBoost);
          } else {
            wu = pickBiased(wuPool, maxYards * 0.12);
          }
          freeUsed += wu.totalYards;
        }

        // 2. Drill — F: bias picks toward drills that satisfy a "preferred"
        // equipment item (one match = ×2 weight per match). Required-equipment
        // hard filter still happens in combinationSatisfies after main picks.
        let dr;
        if (pinnedBlocks.drill) {
          dr = pinnedBlocks.drill;
        } else {
          const validDrills = drillList.filter(o =>
            o.totalYards <= freeBudget - freeUsed - minKt - minMa - minCd
          );
          if (!validDrills.length) continue;
          const drillBase = (o) => Math.pow(2, preferredEquipMatches(o, equipment)) * favBoost(o);
          if (biasRatios) {
            // Real-shift: target by bias; no 10% floor (target weighting handles spread).
            dr = pickWithTargetYards(validDrills, freeBudget * biasRatios.drill, drillBase);
          } else {
            const drFloor = (freeBudget - freeUsed) * 0.10;
            const aboveFloor = validDrills.filter(o => o.totalYards >= drFloor);
            const drPool = aboveFloor.length > 0 ? aboveFloor : validDrills;
            dr = pickWeighted(drPool, drillBase);
          }
          freeUsed += dr.totalYards;
        }

        // 2b. Kick (optional 5th section — defaults excluded). Drawn from free
        // budget after drill; no bias target (kick isn't in SECTION_BIAS_RATIOS).
        let kt;
        if (pinnedBlocks.kick) {
          kt = pinnedBlocks.kick;
        } else {
          const validKicks = kickList.filter(o =>
            o.totalYards <= freeBudget - freeUsed - minMa - minCd
          );
          if (!validKicks.length) continue;
          const kickBase = (o) => Math.pow(2, preferredEquipMatches(o, equipment)) * favBoost(o);
          kt = pickWeighted(validKicks, kickBase);
          freeUsed += kt.totalYards;
        }

        // 3. Main — expand pool with ×2 and ×3 repeat variants
        //    (C: recovery mode skips the repeat expansion — short and sweet)
        let ma;
        if (pinnedBlocks.main) {
          ma = pinnedBlocks.main;
        } else {
          const mainBudget = freeBudget - freeUsed - minCd;
          const mainPoolExpanded = recoveryMode
            ? mainList.slice()
            : mainList.flatMap(o => {
                const variants = [o];
                for (const r of [2, 3]) {
                  const rep = { ...o, rounds: r, totalYards: o.totalYards * r };
                  if (rep.totalYards <= mainBudget) variants.push(rep);
                }
                return variants;
              });
          const validMain = mainPoolExpanded.filter(o => o.totalYards <= mainBudget);
          if (!validMain.length) continue;
          // S3 #4 — main set normally takes ≥65% of the remaining budget so
          // workouts feel substantive. In recovery mode we want a SHORT day,
          // so the 65% rule is dropped: any main that fits the budget is fair
          // game, down to the absolute minimum (minMa).
          // Real-shift bias: override the 65% floor with the bias-specific
          // main ratio applied to freeBudget (target absolute, not relative
          // to whatever's left after warmup+drill grabbed). long_main pushes
          // floor to ~83%; drill/warmup-heavy modes lower it so the other
          // sections can absorb more.
          let mainFloorRatio;
          if (recoveryMode)       mainFloorRatio = null;
          else if (biasRatios)    mainFloorRatio = biasRatios.main;
          else                    mainFloorRatio = 0.65;
          const maFloor = (mainFloorRatio == null)
            ? minMa
            : Math.max(minMa, Math.min(mainBudget, freeBudget * mainFloorRatio));
          // Weighting:
          //   base    = 1
          //   favorite× 3
          //   repeat(rounds > 1) × 2
          //   recently-used label (last 7d) × 0.25  ← N1 anti-repeat
          //   phase-zone multiplier (e.g. taper: easy×1.5, vo2×0.5) ← N5
          //   preferred-equipment match × 2 per item ← F
          //
          // S4 #9 — Label-keyed lookups (favorites, recentSet) are working as
          // "anti-repeat by family": when the same conceptual workout (e.g.
          // "IM Descend") is authored across pool modes (SCY/SCM/LCM) or types
          // (im / mixed), they share a label, so down-weighting one down-weights
          // all variants. That's the intended behavior. Labels that *aren't*
          // meant to share semantics MUST stay globally unique.
          // 2026-05-15: catalog favorites now expose warmup/drill/cooldown
          // labels to the favorites array too. Verified zero cross-section
          // collisions (618 unique labels). New bank entries must keep this
          // invariant — re-run tools/check_label_collisions.py if added.
          const recentSet = new Set(recentLabels);
          const phaseSpec = phase && PHASES[phase] ? PHASES[phase] : null;
          const mainWeight = (o) => {
            let w = 1;
            // v1.13 — UNIVERSAL precedence rule: presence in ANY favorite list
            // (own or coach-propagated, since `favorites` is the unioned set
            // by the time it reaches here) skips the disfavor multiplier
            // entirely. Same per-set: if a set is favorited, it does NOT
            // also get the disfavor multiplier even if its ID is in the
            // disfavor set. Previously (≤ v1.12) these multiplied, so a
            // favorite+disfavor collision yielded 0.75×; now favorite wins.
            const isLabelFav = !!(favorites.length && o.label && favorites.includes(o.label));
            if (isLabelFav) {
              w *= 3;
            } else if (disfavorites.length && o.label && disfavorites.includes(o.label)) {
              w *= disfavorMultiplier;
            }
            // Per-set: count fav vs disfavor matches PER SET and apply 3× per
            // favorited set OR disfavorMultiplier per disfavored set — never
            // both for the same set (favorite wins on collisions). Mutex at
            // DB layer already prevents own collisions; coach-propagated
            // values can still collide with own, hence the per-set check.
            if (Array.isArray(o.sets) && ((favoriteSetIds && favoriteSetIds.size) || (disfavorSetIds && disfavorSetIds.size))) {
              let favSetMatches = 0;
              let disSetMatches = 0;
              for (const s of o.sets) {
                if (!s.id) continue;
                if (favoriteSetIds && favoriteSetIds.has(s.id))      favSetMatches++;
                else if (disfavorSetIds && disfavorSetIds.has(s.id)) disSetMatches++;
              }
              if (favSetMatches > 0) w *= Math.pow(3, favSetMatches);
              if (disSetMatches > 0) w *= Math.pow(disfavorMultiplier, disSetMatches);
            }
            if ((o.rounds || 1) > 1) w *= 2;
            if (o.label && recentSet.has(o.label)) w *= 0.25;
            if (phaseSpec) {
              const optionZone = inferBlockZone({ section: "main", sets: o.sets, primary_zone: o.primary_zone });
              const m = phaseSpec.multipliers[optionZone];
              if (typeof m === "number") w *= m;
            }
            const prefMatches = preferredEquipMatches(o, equipment);
            if (prefMatches > 0) w *= Math.pow(2, prefMatches);
            return w;
          };
          const filteredByFloor = validMain.filter(o => o.totalYards >= maFloor);
          const mainSource = filteredByFloor.length > 0 ? filteredByFloor : validMain;
          ma = pickWeighted(mainSource, mainWeight);
          freeUsed += ma.totalYards;
        }

        // 4. Equipment satisfaction check
        if (hasEquipReq && !combinationSatisfies(dr, ma, equipment)) continue;

        // 5. Cooldown
        let cd;
        if (pinnedBlocks.cooldown) {
          cd = pinnedBlocks.cooldown;
        } else {
          const remaining = freeBudget - freeUsed;
          const validCooldowns = allCooldowns.filter(o => o.totalYards <= remaining);
          if (!validCooldowns.length) continue;
          cd = pickWeighted(validCooldowns, favBoost);
        }

        const total = wu.totalYards + dr.totalYards + kt.totalYards + ma.totalYards + cd.totalYards;
        if (total >= minTotal && total <= maxYards) {
          // S3 — apply engine overrides per section, then recompute total
          const overridden = applyEngineOverrides(typeId, { warmup: wu, drill: dr, kick: kt, main: ma, cooldown: cd }, sectionSources, recentEngineTemplates, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, usePsc ? _effectiveConstraints : []);
          const finalTotal = overridden.warmup.totalYards + overridden.drill.totalYards + overridden.kick.totalYards + overridden.main.totalYards + overridden.cooldown.totalYards;
          // S3 #19 — telemetry: how often does the random-attempt loop succeed?
          // v2.0 — attach laneFit fallback marker (closure vars live in generateWorkout scope).
          const _laneFitFallbackSections = useLaneFit
            ? Object.entries(laneFitFallback).filter(([_, v]) => v).map(([k]) => k)
            : [];
          const _loopResult = { ...buildWorkout(typeId, overridden.warmup, overridden.drill, overridden.kick, overridden.main, overridden.cooldown, finalTotal, includedSections), __genPath: "loop", __genAttempts: attempt + 1 };
          if (_laneFitFallbackSections.length) _loopResult.__laneFitFallback = _laneFitFallbackSections;
          return _loopResult;
        }
      }

      // Fallback: largest valid combination, respecting pins
      const byYardsDesc = (a, b) => b.totalYards - a.totalYards;
      let fbFree = 0;

      let wu;
      if (pinnedBlocks.warmup) {
        wu = pinnedBlocks.warmup;
      } else {
        const wuPool = allWarmups.filter(o => o.totalYards <= freeBudget - minDr - minMa - minCd);
        wu = (wuPool.length ? wuPool.slice().sort(byYardsDesc) : allWarmups.slice().sort(byYardsDesc))[0];
        fbFree += wu.totalYards;
      }

      let dr;
      if (pinnedBlocks.drill) {
        dr = pinnedBlocks.drill;
      } else {
        let drCandidates = drillList.filter(o => o.totalYards <= freeBudget - fbFree - minMa - minCd);
        if (!drCandidates.length) drCandidates = drillList;
        drCandidates = drCandidates.slice().sort(byYardsDesc);
        if (hasEquipReq) {
          const eqPreferred = drCandidates.filter(d =>
            mainList.some(m =>
              m.totalYards <= freeBudget - fbFree - d.totalYards - minKt - minCd &&
              combinationSatisfies(d, m, equipment)
            )
          );
          // S3 #3 — if NO drill+main combination satisfies the required-equipment
          // set, return a failure sentinel instead of silently dropping the filter.
          // The user asked for that equipment to be required; honor it.
          if (!eqPreferred.length) {
            return { __generateFailure: true, reason: "equipment_unsatisfiable",
                     error: "No drill + main combination covers all your required equipment at this max-yardage. Try lowering an item to 'preferred', or raising the max-yardage." };
          }
          drCandidates = eqPreferred;
        }
        dr = drCandidates[0];
        fbFree += dr.totalYards;
      }

      // Kick (optional 5th section). Excluded → pinnedBlocks.kick is the empty
      // section (0 yards). Included with options → largest that fits. If
      // included but no kick options exist, fall back to an empty section.
      let kt;
      if (pinnedBlocks.kick) {
        kt = pinnedBlocks.kick;
      } else if (_incl.kick && kickList.length) {
        let ktCandidates = kickList.filter(o => o.totalYards <= freeBudget - fbFree - minMa - minCd);
        if (!ktCandidates.length) ktCandidates = kickList;
        kt = ktCandidates.slice().sort(byYardsDesc)[0];
        fbFree += kt.totalYards;
      } else {
        kt = { label: "", totalYards: 0, sets: [] };
      }

      let ma;
      if (pinnedBlocks.main) {
        ma = pinnedBlocks.main;
      } else {
        const fbMainBudget = freeBudget - fbFree - minCd;
        const fbMainExpanded = recoveryMode
          ? mainList.slice()
          : mainList.flatMap(o => {
              const variants = [o];
              for (const r of [2, 3]) {
                const rep = { ...o, rounds: r, totalYards: o.totalYards * r };
                if (rep.totalYards <= fbMainBudget) variants.push(rep);
              }
              return variants;
            });
        let mainCandidates = fbMainExpanded.filter(o => o.totalYards <= fbMainBudget);
        if (!mainCandidates.length) mainCandidates = mainList;
        mainCandidates = mainCandidates.slice().sort(byYardsDesc);
        if (hasEquipReq) {
          const eqMain = mainCandidates.filter(m => combinationSatisfies(dr, m, equipment));
          // S3 #3 — defensive: the eqPreferred check above guarantees a matching
          // main exists for the chosen drill, so this should never be empty. If
          // it is, the invariant has been violated — fail rather than mis-satisfy.
          if (!eqMain.length) {
            return { __generateFailure: true, reason: "equipment_unsatisfiable_for_drill",
                     error: "Selected drill has no compatible main set for your required equipment. Try regenerating or adjusting equipment." };
          }
          mainCandidates = eqMain;
        }
        ma = mainCandidates[0];
        fbFree += ma.totalYards;
      }

      let cd;
      if (pinnedBlocks.cooldown) {
        cd = pinnedBlocks.cooldown;
      } else {
        const cdPool = allCooldowns.filter(o => o.totalYards <= freeBudget - fbFree);
        cd = (cdPool.length ? cdPool.slice().sort(byYardsDesc) : allCooldowns.slice().sort(byYardsDesc))[0];
      }

      const total = wu.totalYards + dr.totalYards + kt.totalYards + ma.totalYards + cd.totalYards;
      // S3 — apply engine overrides per section, then recompute total
      const overridden = applyEngineOverrides(typeId, { warmup: wu, drill: dr, kick: kt, main: ma, cooldown: cd }, sectionSources, recentEngineTemplates, engineDisfavorites, disfavorMode, engineFavorites, lanesPaceSecs, usePsc ? _effectiveConstraints : []);
      const finalTotal = overridden.warmup.totalYards + overridden.drill.totalYards + overridden.kick.totalYards + overridden.main.totalYards + overridden.cooldown.totalYards;
      // S3 #19 — telemetry: fallback path fired (random loop exhausted attempts).
      // v2.0 — same laneFit attachment as the loop-success path above.
      const _fallbackLaneFitSections = useLaneFit
        ? Object.entries(laneFitFallback).filter(([_, v]) => v).map(([k]) => k)
        : [];
      const _fallbackResult = { ...buildWorkout(typeId, overridden.warmup, overridden.drill, overridden.kick, overridden.main, overridden.cooldown, finalTotal, includedSections), __genPath: "fallback", __genAttempts: attempts };
      if (_fallbackLaneFitSections.length) _fallbackResult.__laneFitFallback = _fallbackLaneFitSections;
      return _fallbackResult;
    }

    function buildWorkout(typeId, wu, dr, kt, ma, cd, total, includedSections = ["warmup", "drill", "main", "cooldown"]) {
      const mainName = ma.rounds > 1
        ? `Main Set — ${ma.label} ×${ma.rounds}`
        : `Main Set — ${ma.label}`;
      // Section model A1 — assemble the candidate blocks, then keep only
      // included sections (default = warmup/drill/main/cooldown → byte-identical
      // to pre-A1; kick is opt-in and dropped here unless includedSections names it).
      const blocks = [
        { name: "Warm-Up",              section: "warmup",   ...wu },
        { name: "Drill / Pre-Main Set", section: "drill",    ...dr },
        { name: "Kick Set",             section: "kick",     ...kt },
        { name: mainName, section: "main", ...ma, roundRestSecs: ma.roundRestSecs ?? 30 },
        { name: "Cool-Down",            section: "cooldown", ...cd },
      ].filter(b => includedSections.includes(b.section));
      const estimatedMin = calcEstimatedMin(blocks);
      // Section model A2 — total reflects only the INCLUDED blocks: honest when
      // a section is skipped, and identical to the passed `total` when all four
      // are present (sum of the four blocks === finalTotal).
      const includedTotal = blocks.reduce((s, b) => s + (b.totalYards || 0), 0);
      return { blocks, totalYards: includedTotal, estimatedMin, typeId };
      // NOTE: __laneFitFallback attachment happens at the buildWorkout
      // call sites inside generateWorkout — buildWorkout is module-scope
      // and can't see generateWorkout's locals (useLaneFit, laneFitFallback).
    }

    // ─── Per-section regeneration ────────────────────────────────────
    // Replace ONE section of an existing workout. The other three sections
    // are held fixed, so the new pick must (a) keep total yardage in
    // [MIN_YARDS, maxYards] and (b) preserve the equipment-coverage invariant
    // across drill+main if any equipment is checked.
    const SECTION_KEYS = ["warmup", "drill", "kick", "main", "cooldown"];

    // Stable signature so we can exclude the current pick when alternatives exist.
    // Built from the full sets array so it matches across (a) fresh blocks
    // (which spread the option, including `label`), (b) raw pool options, and
    // (c) history-loaded blocks (which have `label` stripped by handleSave).
    function sectionFingerprint(b) {
      if (!b || !Array.isArray(b.sets)) return "";
      const sig = b.sets.map(s => `${s.reps}x${s.dist}|${s.desc}`).join("/");
      return `${b.totalYards}|r${b.rounds || 1}|${sig}`;
    }

    // Returns { workout, error }. On success, workout is a fresh object with the
    // requested section replaced. On failure, error is a human-readable reason
    // and the caller should keep the prior workout unchanged.
    // 2026-05-15: refactored to options-object — same rationale as generateWorkout.
    function regenerateSection({
      workout,
      typeId,
      sectionKey,
      maxYards,
      equipment,
      poolMode = "25y",
      userMin = null,
      recentLabels = [],
      recoveryMode = false,
      phase = null,
      favorites = [],
      favoriteSetIds = null,         // Gap-1 (2026-05-22) — Set<set_id> with 3× weight per match on main section
      engineFavorites = [],          // v1.13 — (template_id, stroke) tuples with 3× engine pick weight
      sectionSource = "bank",        // S3 — bank | engine | mix
      recentEngineTemplates = [],    // S3 — anti-repeat memory
      disfavorites = [],             // v1.2 — labels with 0.25× pick weight
      engineDisfavorites = [],       // v1.3 — (template_id, stroke) tuples with 0.25× engine pick weight
      disfavorSetIds = null,         // Gap-1 (2026-05-22) — Set<set_id> with 0.25× weight per match on main section
      disfavorMode = "downweight",   // v1.8 — "downweight" (0.25) or "exclude" (0)
      lanesPaceSecs = null,          // v2.0 — multi-lane fit filter on the section's pool
      ugcOverlay = null,             // UGC Phase B — caller's UGC overlay merged into bank picker per exact pool mode
      myConstraints = [],            // Phase 3 PSC slice 2 — caller's active swimmer_constraints rows. Step-0 hard-exclude. Skipped in multi-lane mode.
      tonightOverrides = [],          // Phase 3 PSC slice 3 — in-memory per-practice overrides; unioned with myConstraints.
    } = {}) {
      // v1.8 — Resolve once for use below.
      const disfavorMultiplier = disfavorMode === "exclude" ? 0 : 0.25;
      // Phase 3 PSC slice 2/3 — same skip-in-multi-lane rule as generateWorkout.
      const _useLaneFitRegen     = Array.isArray(lanesPaceSecs) && lanesPaceSecs.length > 0;
      const _effectiveCRegen     = [...(myConstraints || []), ...(tonightOverrides || [])];
      const _usePscRegen         = _effectiveCRegen.length > 0 && !_useLaneFitRegen;
      // Section lookup below is by section key (findIndex), not by position or
      // count, so we only require a non-empty blocks array here. A fixed 4/5
      // bound would wrongly reject workouts with skipped sections (fewer swim
      // blocks) or added dryland blocks (which also live in `blocks`).
      if (!workout || !Array.isArray(workout.blocks) || workout.blocks.length === 0) {
        return { workout: null, error: "No workout to update." };
      }
      if (!SECTION_KEYS.includes(sectionKey)) return { workout: null, error: "Unknown section." };

      const is25m    = poolMode === "25m";
      const is50m    = poolMode === "50m";
      const isMeters = is25m || is50m;
      const blocks = workout.blocks;
      // Match the target block by section, not positional index — kick can be
      // inserted between drill and main, shifting positions.
      const idx = blocks.findIndex(b => b.section === sectionKey);
      if (idx < 0) return { workout: null, error: "Unknown section." };
      const current = blocks[idx];
      const others = blocks.reduce((sum, b, i) => i === idx ? sum : sum + b.totalYards, 0);
      const lower = Math.max(0, Math.max(isMeters ? MIN_METERS : MIN_YARDS, userMin || 0) - others);
      const upper = maxYards - others;

      let pool;
      if (sectionKey === "warmup")        pool = getBankOptions("warmup",   typeId, poolMode, ugcOverlay);
      else if (sectionKey === "cooldown") pool = getBankOptions("cooldown", typeId, poolMode, ugcOverlay);
      else if (sectionKey === "drill")    pool = getBankOptions("drill",    typeId, poolMode, ugcOverlay);
      else if (sectionKey === "kick")     pool = getBankOptions("kick",     typeId, poolMode, ugcOverlay);
      else {
        let basePool = getBankOptions("main", typeId, poolMode, ugcOverlay);
        // C: in recovery mode, only consider easy/aerobic mains, no repeat variants.
        if (recoveryMode) {
          const recoveryMains = basePool.filter(o => {
            const z = inferBlockZone({ section: "main", sets: o.sets, primary_zone: o.primary_zone });
            return z === "easy" || z === "aerobic";
          });
          if (recoveryMains.length) basePool = recoveryMains;
          pool = basePool.slice();
        } else {
          // Expand with ×2 and ×3 repeat variants
          pool = basePool.flatMap(o => {
            const variants = [o];
            for (const r of [2, 3]) {
              variants.push({ ...o, rounds: r, totalYards: o.totalYards * r });
            }
            return variants;
          });
        }
      }
      // Phase 3 PSC slice 2 — hard-exclude step 0, before the lane-fit
      // pre-filter. Skipped in multi-lane mode (per fork 1). Bank-fallback
      // on empty filtered pool — silent (per fork 3).
      if (_usePscRegen) {
        const pscFiltered = pool.filter(o => !optionViolatesPSC(o, _effectiveCRegen));
        if (pscFiltered.length > 0) pool = pscFiltered;
        // else: silent bank fallback (caller has no actionable recovery)
      }
      // v2.0 — Multi-lane fit pre-filter on the section's pool. Same
      // graceful fallback as generateWorkout (use unfiltered if empty).
      let _laneFitFallback = false;
      if (_useLaneFitRegen) {
        const filtered = pool.filter(o => optionFitsAllLanes(o, lanesPaceSecs));
        if (filtered.length > 0) pool = filtered;
        else                     _laneFitFallback = true;
      }

      const hasEquipReq = equipment && Object.keys(EQUIP_REQUIREMENTS).some(k => isEquipRequired(equipment, k));
      const partnerDrill = blocks.find(b => b.section === "drill") || blocks[1]; // used when regenerating main
      const partnerMain  = blocks.find(b => b.section === "main")  || blocks[2]; // used when regenerating drill

      const passesEquip = (candidate) => {
        if (!hasEquipReq) return true;
        if (sectionKey === "drill") return combinationSatisfies(candidate, partnerMain, equipment);
        if (sectionKey === "main")  return combinationSatisfies(partnerDrill, candidate, equipment);
        return true; // warmup/cooldown carry no equipment tags
      };

      const fitsBudget = (o) => o.totalYards >= lower && o.totalYards <= upper;
      let validAll = pool.filter(o => fitsBudget(o) && passesEquip(o));
      if (!validAll.length) {
        // v1.11 fix — for engine/mix regen, bank validation isn't a hard
        // gate. Engine output replaces the bank pick anyway, and engine
        // generates at the section's target yardage independent of the
        // bank pool. Fall back to bank options that fit equipment but
        // ignore the strict yardage gate (engine will size correctly).
        // If even THAT pool is empty (no equipment-compatible bank option
        // exists at all), engine has nothing to seed from — return error.
        if (sectionSource === "engine" || sectionSource === "mix") {
          validAll = pool.filter(passesEquip);
          if (!validAll.length) {
            return { workout: null, error: hasEquipReq
              ? "No bank option exists for this section to seed the engine and satisfy equipment. Try unrequiring equipment."
              : "No bank option exists for this section to seed the engine." };
          }
        } else {
          return { workout: null, error: hasEquipReq
            ? "No option for this section fits the remaining yardage and your equipment selection."
            : "No option for this section fits the remaining yardage budget." };
        }
      }

      // Prefer type-matched warmups on regen (same logic as generateWorkout)
      if (sectionKey === "warmup") {
        const warmupFamily = TYPE_WARMUP_FAMILY[typeId] || null;
        if (warmupFamily) {
          const affinityPool = validAll.filter(o => o.typeAffinity && o.typeAffinity.includes(warmupFamily));
          if (affinityPool.length > 0) validAll = affinityPool;
        }
      }

      const curFp = sectionFingerprint(current);
      let novel = validAll.filter(o => sectionFingerprint(o) !== curFp);
      if (!novel.length) {
        // v1.11 fix — the novelty check guards bank-mode regen from picking
        // the same bank option again. Engine/Mix regens will produce a
        // freshly-generated engine block downstream regardless of the bank
        // pool size, so requiring bank novelty here mis-blocks them. When
        // engine/mix source is set, fall back to validAll (skip novelty
        // gate). The engine override below replaces the bankBlock.
        if (sectionSource === "engine" || sectionSource === "mix") {
          if (!validAll.length) {
            return { workout: null, error: "No option for this section fits the remaining yardage budget." };
          }
          novel = validAll;  // skip the novelty gate; engine will produce fresh content
        } else {
          return { workout: null, error: "Only one option fits these constraints — adjust max yards or equipment for more variety." };
        }
      }
      // N1: down-weight recently-used labels (main section only — no labels elsewhere).
      // N5: layer phase multipliers on top of the recent-label down-weight.
      // F: boost options whose sets cover any "preferred" equipment (drill+main).
      // FAV: favorite-label × 3 across all section regens (matches generateWorkout).
      const recentSet = new Set(recentLabels);
      const favSet    = new Set(favorites || []);
      // v1.2 — disfavor: 0.25× pick weight, mutex with favorite at DB layer.
      const disfavorSet = new Set(disfavorites || []);
      const favBoost  = (o) => {
        if (!o.label) return 1;
        if (favSet.has(o.label))      return 3;
        if (disfavorSet.has(o.label)) return disfavorMultiplier; // v1.8: 0.25 or 0
        return 1;
      };
      const phaseSpec = phase && PHASES[phase] ? PHASES[phase] : null;
      const picked = sectionKey === "main"
        ? pickWeighted(novel, (o) => {
            let w = (o.label && recentSet.has(o.label)) ? 0.25 : 1;
            if (phaseSpec) {
              const optionZone = inferBlockZone({ section: "main", sets: o.sets, primary_zone: o.primary_zone });
              const m = phaseSpec.multipliers[optionZone];
              if (typeof m === "number") w *= m;
            }
            const prefMatches = preferredEquipMatches(o, equipment);
            if (prefMatches > 0) w *= Math.pow(2, prefMatches);
            w *= favBoost(o);
            // Gap-1 (2026-05-22) — set-level fav/disfavor on regen. Mirrors
            // mainWeight's per-set logic in generateWorkout: per-set,
            // favorite hit short-circuits disfavor (v1.13 universal rule).
            // Multiplicative across multiple matching sets within one option.
            if (Array.isArray(o.sets) && ((favoriteSetIds && favoriteSetIds.size) || (disfavorSetIds && disfavorSetIds.size))) {
              let favSetMatches = 0;
              let disSetMatches = 0;
              for (const s of o.sets) {
                if (!s.id) continue;
                if (favoriteSetIds && favoriteSetIds.has(s.id))      favSetMatches++;
                else if (disfavorSetIds && disfavorSetIds.has(s.id)) disSetMatches++;
              }
              if (favSetMatches > 0) w *= Math.pow(3, favSetMatches);
              if (disSetMatches > 0) w *= Math.pow(disfavorMultiplier, disSetMatches);
            }
            return w;
          })
        : sectionKey === "drill"
        ? pickWeighted(novel, (o) => Math.pow(2, preferredEquipMatches(o, equipment)) * favBoost(o))
        : pickWeighted(novel, favBoost);

      const NAME_BY_SECTION = {
        warmup:   "Warm-Up",
        drill:    "Drill / Pre-Main Set",
        kick:     "Kick Set",
        cooldown: "Cool-Down",
      };
      const bankBlock = sectionKey === "main"
        ? { name: `Main Set — ${picked.label}`, section: "main", ...picked }
        : { name: NAME_BY_SECTION[sectionKey], section: sectionKey, ...picked };

      // S3 — engine override per section source
      let newBlock = bankBlock;
      if (sectionSource === "engine" || sectionSource === "mix") {
        const overridden = applyEngineOverrides(
          typeId,
          { [sectionKey]: bankBlock },
          { [sectionKey]: sectionSource },
          recentEngineTemplates,
          engineDisfavorites,
          disfavorMode,
          engineFavorites,
          lanesPaceSecs,
          _usePscRegen ? _effectiveCRegen : [],
        );
        newBlock = { ...bankBlock, ...overridden[sectionKey] };
      }

      const newBlocks = blocks.map((b, i) => i === idx ? newBlock : b);
      const total = newBlocks.reduce((s, b) => s + b.totalYards, 0);

      const regenResult = { blocks: newBlocks, totalYards: total, estimatedMin: calcEstimatedMin(newBlocks), typeId };
      if (_laneFitFallback) regenResult.__laneFitFallback = [sectionKey];
      return { workout: regenResult, error: null };
    }

    // ═══════════════════════════════════════════════════════════════
    //  EQUIPMENT — checkbox state shapes set descriptions at render time
    //  (yardage, intervals, and focus are unchanged — equipment is a
    //   "what gear you use" detail, not a different exercise)
    // ═══════════════════════════════════════════════════════════════

    const EQUIPMENT_LIST = [
      { id: "kickboard", label: "Kickboard", icon: "🟦", description: "Use a board for kick sets" },
      { id: "fins",      label: "Fins",      icon: "🐬", description: "Add fins to kick & underwater sets" },
      { id: "paddles",   label: "Paddles",   icon: "🤚", description: "Add paddles to pull sets" },
      { id: "pullBuoy",  label: "Pull Buoy", icon: "🛟", description: "Confirms buoy use on pull sets" },
      { id: "snorkel",   label: "Snorkel",   icon: "🤿", description: "Adds snorkel cue to freestyle drill and technique sets" },
    ];

    // Returns the equipment items (in display order) that apply to a given set
    // based on the set's `eq` tag and the user's current checkbox state.
    function equipmentForSet(set, equip) {
      if (!set.eq) return [];
      const items = [];
      // F: treat "preferred" and "required" both as "use this gear in description".
      // Only "off" suppresses the badge/desc rewrite.
      if (set.eq === "kick" || set.eq === "kickPart") {
        if (isEquipOn(equip, "kickboard")) items.push("kickboard");
        if (isEquipOn(equip, "fins"))      items.push("fins");
      } else if (set.eq === "underkick") {
        // Underwater kick is from streamline — kickboard doesn't apply, fins do
        if (isEquipOn(equip, "fins"))      items.push("fins");
      } else if (set.eq === "pull") {
        if (isEquipOn(equip, "paddles"))   items.push("paddles");
        if (isEquipOn(equip, "pullBuoy"))  items.push("buoy");
      } else if (set.eq === "snorkel") {
        if (isEquipOn(equip, "snorkel"))   items.push("snorkel");
      }
      return items;
    }

    // Rewrite a set's description based on which compatible equipment is checked.
    // Yardage and intervals never change — only the prose updates to be explicit
    // about which gear to grab.
    function enhanceDesc(set, equip) {
      const items = equipmentForSet(set, equip);
      if (items.length === 0) return set.desc;
      const eqText = items.join(" + ");
      if (set.eq === "kick") {
        return set.desc
          .replace(/\s*\(board optional\)/i, "")
          .replace(/^IM order kick\b/, `IM order kick with ${eqText}`)
          .replace(/^Kick\b/, `Kick with ${eqText}`);
      }
      if (set.eq === "underkick") {
        return set.desc.replace(/^Underwater kick\b/, `Underwater kick with ${eqText}`);
      }
      if (set.eq === "kickPart") {
        // Multi-part set ("Kick 25 / Drill 25 / Swim 25") — annotate kick portions
        return `${set.desc}  ·  kick portions with ${eqText}`;
      }
      if (set.eq === "pull") {
        return set.desc.replace(/Pull\s*\(buoy\)/i, `Pull with ${eqText}`);
      }
      if (set.eq === "snorkel") {
        return set.desc + "  ·  use snorkel";
      }
      return set.desc;
    }

    // ═══════════════════════════════════════════════════════════════
    //  SET SWAP HELPERS
    // ═══════════════════════════════════════════════════════════════

    // Strict interval parser — the single source of truth for "how many seconds
    // is this interval string?". Returns null for empty / no-interval / unparseable.
    // Only accepts the canonical authored form `On M:SS` (with `M` optional, e.g.
    // `On :45`). User-typed input from the per-set editor is canonicalized via
    // normalizeIntervalInput BEFORE being stored, so the math only ever sees
    // canonical strings here.
    function parseIntervalSecs(str) {
      if (!str) return null;
      // Explicit "no interval" markers — both the canonical sentinel and any
      // legacy / loose variant. Treated identically to unparseable: caller
      // falls back to default behavior (yd/min for time estimate, etc.).
      if (/no\s*interval/i.test(str)) return null;
      const m = str.match(/^On\s+(\d*):(\d{1,2})/i);
      if (!m) return null;
      const mins = m[1] === "" ? 0 : parseInt(m[1], 10);
      const secs = parseInt(m[2], 10);
      if (isNaN(mins) || isNaN(secs)) return null;
      return mins * 60 + secs;
    }

    function formatIntervalSecs(secs) {
      const m = Math.floor(secs / 60);
      const s = secs % 60;
      // Authored data uses the `On :SS` form for sub-minute intervals (e.g. "On :40").
      // Match that convention so edited / scaled cells don't visually diverge from
      // their authored neighbors. Math is identical either way.
      const mPart = m === 0 ? "" : String(m);
      return `On ${mPart}:${String(s).padStart(2, "0")}`;
    }

    // Canonical sentinel for an interval-less set. Authored data uses this
    // exact string; clear-to-no-interval and "no interval" typed input both
    // normalize to it.
    const NO_INTERVAL_CANONICAL = "No interval — swim easy";

    // Normalize raw user input from the per-set interval editor into either
    // a canonical interval string or an explicit error. Permissive about
    // input form, strict about what it stores. Returns:
    //   { value: "On M:SS" | NO_INTERVAL_CANONICAL, secs: number | null, error: null }  // valid
    //   { value: null, secs: null, error: null }                                          // empty → no-op
    //   { value: null, secs: null, error: "<friendly message>" }                          // unparseable
    //
    // Accepted forms (case-insensitive, leading/trailing whitespace ignored):
    //   "On 1:30"   → On 1:30   (canonical pass-through)
    //   "1:30"      → On 1:30
    //   ":45"       → On 0:45
    //   "0:45"      → On 0:45
    //   "90"        → On 1:30   (bare integer = seconds; 5–3600 range)
    //   "no interval" / "none" / "off" → NO_INTERVAL_CANONICAL
    //   ""          → no-op (caller closes editor without changes)
    //
    // Rejected: decimals, negative numbers, hour notation, anything else.
    function normalizeIntervalInput(raw) {
      const v = (raw == null ? "" : String(raw)).trim();
      if (v === "") return { value: null, secs: null, error: null };

      // "no interval" family — accept several common phrasings.
      if (/^(no\s*interval|none|off|n\/a)$/i.test(v)) {
        return { value: NO_INTERVAL_CANONICAL, secs: null, error: null };
      }

      // Strip an optional leading "On " (case-insensitive) so the rest of the
      // logic sees only the time part. Also handle the case where the user
      // backspaced everything except the "On" prefix — treat as empty no-op.
      const stripped = v.replace(/^on\s*/i, "").trim();
      if (stripped === "") return { value: null, secs: null, error: null };

      // M:SS or :SS form (M optional, SS must be 1–2 digits).
      const colonMatch = stripped.match(/^(\d*):(\d{1,2})$/);
      if (colonMatch) {
        const mins = colonMatch[1] === "" ? 0 : parseInt(colonMatch[1], 10);
        const secs = parseInt(colonMatch[2], 10);
        if (secs >= 60) {
          return { value: null, secs: null, error: "seconds must be 0–59 (use M:SS, not S:SS)" };
        }
        const total = mins * 60 + secs;
        if (total < 5)    return { value: null, secs: null, error: "interval too short (minimum 5s)" };
        if (total > 3600) return { value: null, secs: null, error: "interval too long (max 1 hour)" };
        return { value: formatIntervalSecs(total), secs: total, error: null };
      }

      // Bare integer = total seconds (e.g. "90" → On 1:30).
      const intMatch = stripped.match(/^(\d+)$/);
      if (intMatch) {
        const total = parseInt(intMatch[1], 10);
        if (total < 5)    return { value: null, secs: null, error: "interval too short (minimum 5s)" };
        if (total > 3600) return { value: null, secs: null, error: "interval too long (max 1 hour)" };
        return { value: formatIntervalSecs(total), secs: total, error: null };
      }

      return { value: null, secs: null, error: `cannot parse "${v}" — try "1:30" or "90" or "no interval"` };
    }

    function scaleInterval(intervalStr, ratio) {
      const secs = parseIntervalSecs(intervalStr);
      if (secs === null) return intervalStr;
      const scaled = Math.max(30, Math.round((secs * ratio) / 5) * 5);
      return formatIntervalSecs(scaled);
    }

    // v2.0 — Multi-lane fit check. Given an option (warmup/drill/main/cooldown
    // shape) and an array of lane pace strings (e.g. ["1:30", "1:45", "2:00"]),
    // returns true iff every set with a parseable interval leaves every lane
    // a rest time in [minRest, maxRestRatio * swimTime].
    //
    // The math:
    //   swimTime_L(set) = (paceL / paceBaseline) * baseSwimTime(set)
    //   rest_L(set)     = interval(set) - swimTime_L(set)
    // baseSwimTime is the "swim time at baseline pace" for this set —
    // approximated as (dist / 100) * paceBaseline since intervals are
    // authored relative to a 100-yd reference. This is the same model
    // rescaleBlocksForPace uses.
    //
    // Sets without an interval (NO_INTERVAL_CANONICAL, warmups, cooldowns)
    // are skipped (return true for that set — they're not interval-driven).
    // An option with NO interval-driven sets passes trivially.
    //
    // Edge cases:
    //   - Empty/null lanesPace: returns true (single-pace path, no filter)
    //   - Unparseable pace: skipped (treated as "any pace" — defensive)
    //   - dist <= 0 or missing: set skipped (defensive against bad bank rows)
    // Phase 3 / PSC slice 2 — hard-exclude check.
    // Returns true if `option` violates ANY constraint in `constraints`.
    // Used as step-0 of the picker (before favorites + disfavor) per
    // PER_SWIMMER_CONSTRAINTS_SCOPE.md §3.4.
    //
    // Constraint types handled here (12 of 14 — option-level):
    //   stroke_no_{fly,breast,back,free}     — checks option.strokes[] + label/desc fallback
    //   equip_no_{paddles,fins,snorkel,kickboard,buoy} — checks set.eq across all sets
    // Section + cap constraints are NOT option-level; caller handles those
    // separately at the section/post-assembly level.
    //
    // Equipment mapping (bank eq vocab → constraint type):
    //   "paddles" → equip_no_paddles  (rare; bank uses paddles in desc text only)
    //   "fins"    → equip_no_fins
    //   "snorkel" → equip_no_snorkel
    //   "kick"    → equip_no_kickboard  (eq="kick" implies kickboard-assisted)
    //   "pull"    → equip_no_buoy       (eq="pull" implies pull-buoy)
    function optionViolatesPSC(option, constraints) {
      if (!option || !Array.isArray(constraints) || constraints.length === 0) return false;
      const STROKE_REGEX = {
        fly:    /\b(fly|butterfly)\b/i,
        breast: /\bbreast(?:stroke)?\b/i,
        back:   /\bback(?:stroke)?\b/i,
        free:   /\b(free(?:style)?|FREE)\b/i,
      };
      const EQ_TO_BANNED = {
        paddles:   "equip_no_paddles",
        fins:      "equip_no_fins",
        snorkel:   "equip_no_snorkel",
        kick:      "equip_no_kickboard",
        pull:      "equip_no_buoy",
      };
      const optStrokes = Array.isArray(option.strokes) ? option.strokes : [];
      const sets       = Array.isArray(option.sets)    ? option.sets    : [];
      const blob       = ((option.label || "") + " " + sets.map(s => (s.desc || "") + " " + (s.focus || "")).join(" "));
      for (const c of constraints) {
        const t = c && c.constraint_type;
        if (!t) continue;
        // Stroke exclusions — option-level: explicit strokes tag wins; fallback to text match.
        if (t === "stroke_no_fly"    && (optStrokes.includes("fly")    || STROKE_REGEX.fly.test(blob)))    return true;
        if (t === "stroke_no_breast" && (optStrokes.includes("breast") || STROKE_REGEX.breast.test(blob))) return true;
        if (t === "stroke_no_back"   && (optStrokes.includes("back")   || STROKE_REGEX.back.test(blob)))   return true;
        if (t === "stroke_no_free"   && (optStrokes.includes("free")   || STROKE_REGEX.free.test(blob)))   return true;
        // Equipment exclusions — set-level: any set with the matching eq disqualifies the whole option.
        if (t.startsWith("equip_no_")) {
          for (const s of sets) {
            if (!s.eq) continue;
            if (EQ_TO_BANNED[s.eq] === t) return true;
          }
        }
        // Paddles edge case: bank eq vocab doesn't have "paddles" — fall back to text.
        if (t === "equip_no_paddles" && /\bpaddles?\b/i.test(blob)) return true;
      }
      return false;
    }

    // Phase 3 PSC slice 4 — substitution policy v1 (scope §3.5).
    // Pure function. Walks workout.blocks/sets against constraints, returns
    // per-set annotations describing what would be substituted FOR THIS
    // SWIMMER. Used by AssignedToMeView per-card details and by
    // MultiPacePrintView per-lane substitution rendering.
    //
    // Output shape:
    //   [
    //     { block_idx, set_idx, kind: "stroke_sub", from, to, constraint_type },
    //     { block_idx, set_idx, kind: "equip_drop", eq, constraint_type },
    //     { block_idx,          kind: "section_skip", section, constraint_type },
    //     {                     kind: "cap_yardage", value, constraint_type },
    //     {                     kind: "cap_intensity", value, constraint_type },
    //   ]
    //
    // The renderer decides how to display each kind. Section-skip + caps are
    // workout-level (no set_idx); stroke + equip are per-set.
    //
    // Substitution policy per scope §3.5 table.
    function computeSubstitutionsForSwimmer(workout, constraints) {
      const out = [];
      if (!workout || !Array.isArray(workout.blocks) || !Array.isArray(constraints) || constraints.length === 0) {
        return out;
      }
      const STROKE_REGEX = {
        fly:    /\b(fly|butterfly)\b/i,
        breast: /\bbreast(?:stroke)?\b/i,
        back:   /\bback(?:stroke)?\b/i,
        free:   /\b(free(?:style)?|FREE)\b/i,
      };
      const STROKE_FROM_TYPE = {
        stroke_no_fly:    "fly",
        stroke_no_breast: "breast",
        stroke_no_back:   "back",
        stroke_no_free:   "free",
      };
      // v1 substitution table (stroke). stroke_no_free defaults to back per scope §6 open fork.
      const STROKE_SUB = {
        stroke_no_fly:    "free",
        stroke_no_breast: "free",
        stroke_no_back:   "free",
        stroke_no_free:   "back",
      };
      const EQ_TO_BANNED = {
        paddles:   "equip_no_paddles",
        fins:      "equip_no_fins",
        snorkel:   "equip_no_snorkel",
        kick:      "equip_no_kickboard",
        pull:      "equip_no_buoy",
      };
      const SECTION_BANNED = {
        warmup:   null,                // no section_no_warmup in v1 vocab
        drill:    "section_no_drill",
        main:     "section_no_main",
        cooldown: null,                // no section_no_cooldown in v1 vocab
      };

      // Workout-level caps surface once each.
      for (const c of constraints) {
        if (c.constraint_type === "cap_yardage") {
          out.push({ kind: "cap_yardage", value: c.value_num, constraint_type: "cap_yardage" });
        } else if (c.constraint_type === "cap_intensity") {
          out.push({ kind: "cap_intensity", value: c.value_str || "easy_only", constraint_type: "cap_intensity" });
        }
      }

      for (let bi = 0; bi < workout.blocks.length; bi++) {
        const block = workout.blocks[bi];
        if (!block) continue;

        // Section-level skip — check any constraint marks this section banned.
        const sectionBan = SECTION_BANNED[block.section];
        if (sectionBan) {
          const hit = constraints.find(c => c.constraint_type === sectionBan);
          if (hit) {
            out.push({ block_idx: bi, kind: "section_skip", section: block.section, constraint_type: sectionBan });
            continue;  // No need to also annotate per-set when the whole section is skipped.
          }
        }

        const sets = Array.isArray(block.sets) ? block.sets : [];
        for (let si = 0; si < sets.length; si++) {
          const s = sets[si];
          const blob = ((s.desc || "") + " " + (s.focus || ""));
          // Stroke subs — first matching constraint wins for this set.
          let strokeSubbed = false;
          for (const c of constraints) {
            const target = STROKE_FROM_TYPE[c.constraint_type];
            if (!target) continue;
            const re = STROKE_REGEX[target];
            if (re && re.test(blob)) {
              out.push({
                block_idx: bi, set_idx: si,
                kind: "stroke_sub",
                from: target,
                to:   STROKE_SUB[c.constraint_type],
                constraint_type: c.constraint_type,
              });
              strokeSubbed = true;
              break;
            }
          }
          // Equip drops — independent of stroke (a set can both swap stroke AND drop gear).
          if (s.eq) {
            const banKey = EQ_TO_BANNED[s.eq];
            if (banKey) {
              const hit = constraints.find(c => c.constraint_type === banKey);
              if (hit) {
                out.push({
                  block_idx: bi, set_idx: si,
                  kind: "equip_drop",
                  eq: s.eq,
                  constraint_type: banKey,
                });
              }
            }
          }
        }
      }
      return out;
    }

    // Tag-extract used by the constraint UIs to render substitutions
    // attached to a particular block/set. Returns array of subs for that
    // (block_idx, set_idx) — pre-filtered for the renderer.
    function pickSetSubs(subs, blockIdx, setIdx) {
      if (!Array.isArray(subs)) return [];
      return subs.filter(s => s.block_idx === blockIdx && s.set_idx === setIdx);
    }
    function pickBlockSubs(subs, blockIdx) {
      if (!Array.isArray(subs)) return [];
      return subs.filter(s => s.block_idx === blockIdx && s.set_idx == null);
    }
    function pickWorkoutSubs(subs) {
      if (!Array.isArray(subs)) return [];
      return subs.filter(s => s.block_idx == null);
    }

    function optionFitsAllLanes(option, lanesPaceSecs, minRest = 5, maxRestRatio = 1.5, paceBaseline = 120) {
      if (!option || !Array.isArray(option.sets) || option.sets.length === 0) return true;
      if (!Array.isArray(lanesPaceSecs) || lanesPaceSecs.length === 0) return true;
      const validLanes = lanesPaceSecs.filter(p => typeof p === "number" && p > 0);
      if (!validLanes.length) return true;
      for (const set of option.sets) {
        const intervalSecs = parseIntervalSecs(set.interval);
        if (intervalSecs === null) continue;   // no interval → unconstrained
        const dist = set.dist;
        if (!dist || dist <= 0) continue;       // defensive: skip malformed
        // baseSwimTime at baseline pace: dist/100 yards × baseline seconds per 100
        const baseSwimTime = (dist / 100) * paceBaseline;
        for (const paceL of validLanes) {
          const swimTimeL = (paceL / paceBaseline) * baseSwimTime;
          const restL = intervalSecs - swimTimeL;
          if (restL < minRest) return false;                          // slowest lane can't finish
          if (restL > maxRestRatio * swimTimeL) return false;         // fastest lane idle
        }
      }
      return true;
    }

    // ═══════════════════════════════════════════════════════════════
    //  TEMPLATE ENGINE (S1 — bank audit + 3 templates)
    //
    //  Generates bank-shaped option objects ({label, totalYards, sets[]})
    //  from structural templates. Complements the bank, doesn't replace
    //  it. Per TEMPLATE_ENGINE_SCOPE.md §2.
    //
    //  Output shape matches bank options EXACTLY:
    //    { label, totalYards, sets: [{ id?, reps, dist, desc, interval, focus, eq? }, ...] }
    //
    //  Intervals are authored at the canonical baseline pace (2:00/100,
    //  same as PACE_BASELINE_SECS in rescaleBlocksForPace). The existing
    //  rescaleBlocksForPace path adjusts them to the user's actual pace
    //  at render time — engine output integrates with the bank-fallback
    //  path with no special handling.
    //
    //  S1 = 3 templates (block_with_recovery, aerobic_volume,
    //  descend_ladder) chosen for combined ~24% bank coverage. S2 adds
    //  validator + anti-repeat + DB. S3 wires the per-section UI toggle
    //  and ⚡ badge.
    //
    //  Family selection priorities and definitions live in
    //  tools/bank_audit.json (run tools/bank_audit.py to refresh).
    // ═══════════════════════════════════════════════════════════════
    const TEMPLATE_BASELINE_PACE_SECS = 120;  // 2:00 / 100 yd — matches rescaleBlocksForPace

    // Canonical SCY distances per spec §1 V2. Engine output must pick from this set.
    const TEMPLATE_CANONICAL_DISTS = [25, 50, 75, 100, 150, 200, 300, 400, 500];

    // Per-effort rest offsets (added to swim-time at baseline pace).
    // Tuned for masters; rescaleBlocksForPace adjusts proportionally for
    // faster/slower swimmers.
    const TEMPLATE_REST_BY_EFFORT = {
      easy:      30,  // recovery rows
      aerobic:   15,  // steady aerobic — tight enough to feel
      moderate:  15,
      strong:    20,
      threshold: 15,  // sustainable hard; not full recovery
      racePace:  60,  // full recovery for race-pace work
      sprint:    60,  // full recovery between sprints
    };

    // Compute interval = swim_time(dist) + rest(effort), rounded to 5s.
    function templateComputeInterval(distYd, effortKey) {
      const restSecs = TEMPLATE_REST_BY_EFFORT[effortKey] ?? 15;
      const swimSecs = (distYd / 100) * TEMPLATE_BASELINE_PACE_SECS;
      const total = Math.max(30, Math.round((swimSecs + restSecs) / 5) * 5);
      return formatIntervalSecs(total);
    }

    // Pick a value from an array using a seeded-or-random index. The S2
    // anti-repeat layer will pass a seed; for S1 it's plain Math.random.
    function templatePick(choices, rand = Math.random) {
      if (!choices || choices.length === 0) return null;
      return choices[Math.floor(rand() * choices.length)];
    }

    // Given (distChoices, budget, [minReps, maxReps], preference), return
    // the {dist, reps} pair whose total comes closest to budget while
    // satisfying the rep-count constraint AND staying within ±5% (spec V1).
    // If no combination is within ±5%, returns the closest one anyway —
    // S2's validator will retry with a different template. preference can
    // be "longer" (bias toward longer dists when ties), "shorter" (bias
    // shorter), or "any" (uniform among candidates within tolerance).
    function templatePickRepShape(distChoices, budgetYd, minReps, maxReps, preference = "any", rand = Math.random) {
      const cands = [];
      for (const d of distChoices) {
        // Rep count that gets closest to budget, clamped to [min, max]
        const ideal = Math.round(budgetYd / d);
        const reps = Math.max(minReps, Math.min(maxReps, ideal));
        const total = reps * d;
        const pctOff = Math.abs(total - budgetYd) / budgetYd;
        cands.push({ dist: d, reps, total, pctOff });
      }
      // Step 1: If any candidate is in ±5% tolerance, apply preference
      // among those. This is the happy path — coach gets a sensible
      // choice with budget close to target.
      const within = cands.filter(c => c.pctOff <= 0.05);
      if (within.length) {
        if (preference === "longer") {
          within.sort((a, b) => b.dist - a.dist);
          return templatePick(within.slice(0, 2), rand);
        }
        if (preference === "shorter") {
          within.sort((a, b) => a.dist - b.dist);
          return templatePick(within.slice(0, 2), rand);
        }
        within.sort((a, b) => a.pctOff - b.pctOff);
        return templatePick(within.slice(0, Math.min(3, within.length)), rand);
      }
      // Step 2: No candidate in tolerance — preference is overridden by
      // closeness so we don't ship a 31%-off "shorter" pick when a
      // 6%-off "longer" one was available. S2's validator will catch the
      // residual and retry with a different template.
      cands.sort((a, b) => a.pctOff - b.pctOff);
      return cands[0];
    }

    // Default stroke for a workout type. Stroke-specific types use that
    // stroke; free-axis types default to freestyle.
    function templateDefaultStroke(typeKey) {
      if (["back", "breast", "fly", "free"].includes(typeKey)) return typeKey;
      if (typeKey === "im") return "IM";
      return "free";  // distance / sprint / endurance / mixed
    }

    // Stroke-name → desc-token (capitalization matches authored bank).
    function templateStrokeLabel(stroke) {
      const map = {
        free: "Freestyle", back: "Backstroke", breast: "Breaststroke",
        fly: "Butterfly", IM: "IM", choice: "Choice",
      };
      return map[stroke] || stroke;
    }

    // Distance bias by workout type — what rep distances make sense.
    // Picks from TEMPLATE_CANONICAL_DISTS subset appropriate to the type.
    function templateRepDistChoices(typeKey, section) {
      // Section-level constraint: drills favor shorter pieces, mains longer
      if (section === "drill") return [25, 50, 75, 100];
      // Type biases
      const typeBias = {
        distance:  [100, 150, 200, 300, 400],
        endurance: [100, 150, 200, 300],
        sprint:    [25, 50, 75],
        mixed:     [50, 75, 100, 150, 200],
        free:      [50, 75, 100, 150, 200],
        back:      [50, 75, 100, 150],
        breast:    [50, 75, 100, 150],
        fly:       [25, 50, 75, 100],
        im:        [100, 200],
      };
      return typeBias[typeKey] || [50, 100, 150, 200];
    }

    // ── Template definitions ───────────────────────────────────────
    // Each template defines slots + generate(). Generate returns a bank-
    // shaped option object. S1 ships 3 templates; S2 adds 7-10 more.
    const TEMPLATE_DEFINITIONS = {

      // ┌────────────────────────────────────────────────────────────┐
      // │ aerobic_volume — single row, N×D, aerobic effort           │
      // │ Coverage: 27 bank options (4.4%)                           │
      // │ Example: "12×100 — moderate aerobic (RPE 6/10)"            │
      // └────────────────────────────────────────────────────────────┘
      aerobic_volume: {
        id: "aerobic_volume",
        family: "single_row",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "im"],
        labelTemplate: (slots, totalYards) =>
          `Aerobic ${slots.stroke === "IM" ? "IM " : ""}${slots.repDist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Aerobic favors longer reps: coaches more often program 10×100
          // than 16×50 for the same yardage. Cap at 300yd (above that is
          // continuous_long_swim territory).
          const distChoices = templateRepDistChoices(typeKey, section).filter(d => d <= 300);
          const shape = templatePickRepShape(distChoices, budgetYd, 6, 12, "longer", rand);
          const effortPhrase = templatePick(
            ["moderate aerobic", "easy/moderate", "aerobic pace (RPE 6/10)", "steady aerobic"],
            rand,
          );
          const interval = templateComputeInterval(shape.dist, "aerobic");
          const totalYards = shape.total;
          const slots = { stroke, repDist: shape.dist, repCount: shape.reps, effortPhrase };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — ${effortPhrase}`,
              interval,
              focus: "Build aerobic volume at sustainable pace",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ descend_ladder — single row, N×D with descend 1→N cue     │
      // │ Coverage: 19 bank options (3.1%)                           │
      // │ Example: "Descend 100s — 4×100, descend 1→4"               │
      // └────────────────────────────────────────────────────────────┘
      descend_ladder: {
        id: "descend_ladder",
        family: "single_row",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Descend ${slots.stroke === "IM" ? "IM " : ""}${slots.repDist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const distChoices = templateRepDistChoices(typeKey, section);
          // Descend works best with 4-8 reps. No strong preference for
          // longer/shorter — coaches descend equally happily 4×200 or 8×50.
          const shape = templatePickRepShape(distChoices, budgetYd, 4, 8, "any", rand);
          const interval = templateComputeInterval(shape.dist, "strong");
          const totalYards = shape.total;
          const slots = { stroke, repDist: shape.dist, repCount: shape.reps };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — moderate to strong, descend 1→${shape.reps}`,
              interval,
              focus: "Each rep faster than the last",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ block_with_recovery — hard block + easy recovery row       │
      // │ Coverage: 107 bank options (17.4%) — largest single family │
      // │ Example: "8×100 race-pace + 1×100 easy recovery"           │
      // │                                                            │
      // │ Two-row composition. Allocates ~80% of budget to hard      │
      // │ block, ~20% to recovery row.                               │
      // └────────────────────────────────────────────────────────────┘
      block_with_recovery: {
        id: "block_with_recovery",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `${slots.effortAdj} ${slots.stroke === "IM" ? "IM " : ""}${slots.hardDist}s + Recovery (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Pick a hard effort appropriate for the type
          const effortChoices = typeKey === "sprint"
            ? [{ key: "racePace", phrase: "race pace, full recovery", adj: "Race-Pace" },
               { key: "sprint",   phrase: "strong, near race pace",   adj: "Strong" }]
            : [{ key: "threshold", phrase: "threshold pace (RPE 7-8/10)", adj: "Threshold" },
               { key: "racePace",  phrase: "race pace, focused effort",   adj: "Race-Pace" },
               { key: "strong",    phrase: "strong, sustained",           adj: "Strong" }];
          const effort = templatePick(effortChoices, rand);
          // Two-step budget allocation:
          //   1. Aim hard block at ~80% of budget (leaves room for recovery)
          //   2. Size recovery from the remainder, snapped to canonical
          // This keeps total within ±5% of budget even when the hard
          // block's discrete (dist, reps) options can't hit exactly 80%.
          const hardTarget = Math.round(budgetYd * 0.8);
          // Hard block: 4-10 reps. Sprint type prefers shorter (race-pace
          // intervals are typically 25/50/75); everything else "any".
          const hardDistChoices = templateRepDistChoices(typeKey, section);
          const hardPref = typeKey === "sprint" ? "shorter" : "any";
          const shape = templatePickRepShape(hardDistChoices, hardTarget, 4, 10, hardPref, rand);
          const hardDist = shape.dist, hardReps = shape.reps;
          const hardTotal = hardReps * hardDist;
          // Recovery: pick canonical dist closest to (budget - hardTotal).
          // Clamp to 50-300 to keep it a reasonable single-rep recovery.
          const recoveryTarget = Math.max(50, Math.min(300, budgetYd - hardTotal));
          const recoveryDist = TEMPLATE_CANONICAL_DISTS.reduce((best, d) =>
            Math.abs(d - recoveryTarget) < Math.abs(best - recoveryTarget) ? d : best, 100);
          const hardInterval = templateComputeInterval(hardDist, effort.key);
          const recoveryInterval = templateComputeInterval(recoveryDist, "easy");
          const totalYards = hardTotal + recoveryDist;
          const slots = { stroke, hardDist, hardReps, recoveryDist,
                          effortKey: effort.key, effortPhrase: effort.phrase, effortAdj: effort.adj };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets: [
              {
                reps: hardReps, dist: hardDist,
                desc: `${templateStrokeLabel(stroke)} — ${effort.phrase}`,
                interval: hardInterval,
                focus: `Sustain ${effort.adj.toLowerCase()} effort across all ${hardReps}`,
              },
              {
                reps: 1, dist: recoveryDist,
                desc: "Easy choice — active recovery",
                interval: recoveryInterval,
                focus: "Flush and reset",
              },
            ],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ main_plus_finisher — primary block + smaller closing block │
      // │ Coverage: 64 bank options (10.4%) — second-largest family  │
      // │ Example: "8×100 threshold + 4×50 sprint finisher"          │
      // │                                                            │
      // │ Two-row composition. Allocates ~70% to primary, ~30% to    │
      // │ finisher. Finisher uses shorter dist + faster effort cue.  │
      // └────────────────────────────────────────────────────────────┘
      main_plus_finisher: {
        id: "main_plus_finisher",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `${slots.primaryAdj} ${slots.stroke === "IM" ? "IM " : ""}${slots.primaryDist}s + ${slots.finisherDist}s Finisher (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const primaryTarget = Math.round(budgetYd * 0.7);
          const finisherTarget = budgetYd - primaryTarget;
          const primaryEffort = templatePick(
            [{ key: "threshold", phrase: "threshold pace (RPE 7-8/10)", adj: "Threshold" },
             { key: "strong",    phrase: "strong, sustained",            adj: "Strong" },
             { key: "racePace",  phrase: "race pace, focused",           adj: "Race-Pace" }],
            rand,
          );
          const primaryDistChoices = templateRepDistChoices(typeKey, section);
          const primaryShape = templatePickRepShape(primaryDistChoices, primaryTarget, 4, 10, "any", rand);
          // Finisher: shorter dist + faster effort. Sprint type gets "sprint" cue;
          // others step up one effort tier above primary.
          const finisherDistChoices = primaryDistChoices.filter(d => d <= primaryShape.dist);
          const finisherShape = templatePickRepShape(
            finisherDistChoices.length ? finisherDistChoices : primaryDistChoices,
            finisherTarget, 2, 6, "shorter", rand,
          );
          const finisherEffortKey = primaryEffort.key === "racePace" ? "sprint" : "racePace";
          const finisherPhrase = finisherEffortKey === "sprint" ? "ALL OUT — empty the tank" : "race pace — close fast";
          const primaryInterval = templateComputeInterval(primaryShape.dist, primaryEffort.key);
          const finisherInterval = templateComputeInterval(finisherShape.dist, finisherEffortKey);
          const totalYards = primaryShape.total + finisherShape.total;
          const slots = { stroke, primaryDist: primaryShape.dist, finisherDist: finisherShape.dist,
                          primaryAdj: primaryEffort.adj };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets: [
              { reps: primaryShape.reps, dist: primaryShape.dist,
                desc: `${templateStrokeLabel(stroke)} — ${primaryEffort.phrase}`,
                interval: primaryInterval,
                focus: `Sustain ${primaryEffort.adj.toLowerCase()} effort across all ${primaryShape.reps}` },
              { reps: finisherShape.reps, dist: finisherShape.dist,
                desc: `${templateStrokeLabel(stroke)} — ${finisherPhrase}`,
                interval: finisherInterval,
                focus: "Strong close after the main block" },
            ],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ stroke_rotation — distinct stroke per row, equal yardage   │
      // │ Coverage: 26 bank options (4.2%)                           │
      // │ IM types do full fly/back/breast/free; mixed picks 3.      │
      // └────────────────────────────────────────────────────────────┘
      stroke_rotation: {
        id: "stroke_rotation",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["im", "mixed"],
        labelTemplate: (slots, totalYards) =>
          `${slots.strokeCount}-stroke rotation ${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const allStrokes = ["fly", "back", "breast", "free"];
          const strokes = typeKey === "im"
            ? allStrokes
            : [...allStrokes].sort(() => rand() - 0.5).slice(0, 3);
          const distChoices = templateRepDistChoices(typeKey, section);
          const perStrokeBudget = Math.round(budgetYd / strokes.length);
          // Wider rep range (1-8) so per-stroke budget can be hit exactly
          // even when divisor produces awkward residuals.
          const shape = templatePickRepShape(distChoices, perStrokeBudget, 1, 8, "any", rand);
          const interval = templateComputeInterval(shape.dist, "strong");
          const sets = strokes.map(s => ({
            reps: shape.reps, dist: shape.dist,
            desc: `${templateStrokeLabel(s)} — moderate to strong, smooth`,
            interval,
            focus: `Build ${s === "free" ? "freestyle" : s} feel and pace`,
          }));
          const totalYards = sets.reduce((sum, x) => sum + x.reps * x.dist, 0);
          const slots = { strokeCount: strokes.length, dist: shape.dist };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets,
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ sandwich — easy → hard → easy bookend                      │
      // │ Coverage: 25 bank options (4.1%)                           │
      // │ 3-row composition. ~15% easy each side, ~70% hard middle.  │
      // └────────────────────────────────────────────────────────────┘
      sandwich: {
        id: "sandwich",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im"],
        labelTemplate: (slots, totalYards) =>
          `${slots.hardAdj} ${slots.stroke === "IM" ? "IM " : ""}${slots.hardDist}s Sandwich (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const easyTarget = Math.round(budgetYd * 0.15);
          const hardTarget = budgetYd - 2 * easyTarget;
          const easyDist = TEMPLATE_CANONICAL_DISTS.reduce((best, d) =>
            Math.abs(d - easyTarget) < Math.abs(best - easyTarget) && d <= 300 ? d : best, 100);
          const hardEffort = templatePick(
            [{ key: "threshold", phrase: "threshold pace (RPE 7-8/10)", adj: "Threshold" },
             { key: "racePace",  phrase: "race pace, focused",          adj: "Race-Pace" }],
            rand,
          );
          const hardDistChoices = templateRepDistChoices(typeKey, section);
          const hardShape = templatePickRepShape(hardDistChoices, hardTarget, 4, 10, "any", rand);
          const totalYards = easyDist + hardShape.total + easyDist;
          const slots = { stroke, hardDist: hardShape.dist, hardAdj: hardEffort.adj };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets: [
              { reps: 1, dist: easyDist,
                desc: `${templateStrokeLabel(stroke)} — easy warm-into`,
                interval: templateComputeInterval(easyDist, "easy"),
                focus: "Settle into stroke before the main block" },
              { reps: hardShape.reps, dist: hardShape.dist,
                desc: `${templateStrokeLabel(stroke)} — ${hardEffort.phrase}`,
                interval: templateComputeInterval(hardShape.dist, hardEffort.key),
                focus: `Sustain ${hardEffort.adj.toLowerCase()} across all ${hardShape.reps}` },
              { reps: 1, dist: easyDist,
                desc: "Easy choice — cool flush",
                interval: templateComputeInterval(easyDist, "easy"),
                focus: "Active recovery to close" },
            ],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ repeat_unit — N identical rounds                           │
      // │ Coverage: 24 bank options (3.9%)                           │
      // │ Example: "3 rounds × 4×100"                                │
      // └────────────────────────────────────────────────────────────┘
      repeat_unit: {
        id: "repeat_unit",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im"],
        labelTemplate: (slots, totalYards) =>
          `${slots.rounds} rounds × ${slots.repsPerRound}×${slots.dist} (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const distChoices = templateRepDistChoices(typeKey, section);
          // Try 2-4 rounds, find the (rounds, shape) combo closest to budget
          let best = null;
          for (const rounds of [2, 3, 4]) {
            const perRoundBudget = Math.round(budgetYd / rounds);
            const shape = templatePickRepShape(distChoices, perRoundBudget, 2, 6, "any", rand);
            const total = rounds * shape.total;
            const pctOff = Math.abs(total - budgetYd) / budgetYd;
            if (!best || pctOff < best.pctOff) {
              best = { rounds, shape, total, pctOff };
            }
          }
          const interval = templateComputeInterval(best.shape.dist, "strong");
          const sets = [];
          for (let i = 0; i < best.rounds; i++) {
            sets.push({
              reps: best.shape.reps, dist: best.shape.dist,
              desc: `${templateStrokeLabel(stroke)} — round ${i + 1} of ${best.rounds}, moderate to strong`,
              interval,
              focus: i === 0 ? "Set your pace on round 1" : (i === best.rounds - 1 ? "Hold pace through the final round" : "Match round 1's pace"),
            });
          }
          const slots = { stroke, dist: best.shape.dist, repsPerRound: best.shape.reps, rounds: best.rounds };
          return {
            label: this.labelTemplate(slots, best.total),
            totalYards: best.total,
            sets,
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ drill_progression — multi-row drill sequence              │
      // │ Coverage: 22 bank options (3.6%)                           │
      // │ Type-specific drill sets (IM order, free drills, etc.)    │
      // └────────────────────────────────────────────────────────────┘
      drill_progression: {
        id: "drill_progression",
        family: "multi_row_composition",
        applicableSections: ["main", "drill"],
        // 2026-05-22: extended applicableTypes to include the 4 workout-types
        // (distance, sprint, endurance, mixed) that previously had ZERO
        // applicable drill templates — measured at 100% drill fallback
        // before this change. drillsByType gets matching entries below.
        applicableTypes: ["im", "back", "breast", "fly", "free", "distance", "sprint", "endurance", "mixed"],
        labelTemplate: (slots, totalYards) =>
          `Drill Progression ${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          // Type-specific drill banks. Each entry is [stroke, drill-description].
          // First 5 keys are stroke-types (clean stroke focus); the next 4
          // are workout-types (distance/sprint/endurance map to free-heavy
          // drill blocks; mixed cycles all four strokes).
          const drillsByType = {
            im: [
              ["fly",    "Fly drill — 3-3-3 or single-arm"],
              ["back",   "Back drill — single-arm or catch-up"],
              ["breast", "Breast drill — 2 kick / 1 pull"],
              ["free",   "Free drill — catch-up or fingertip drag"],
            ],
            free: [
              ["free", "Catch-up drill — front-quadrant timing"],
              ["free", "Fingertip drag — high elbow recovery"],
              ["free", "Single-arm — full extension hold"],
            ],
            back: [
              ["back", "Single-arm back — focus on the catch"],
              ["back", "Catch-up back — clean hand entries"],
              ["back", "6-kick rotation drill"],
            ],
            breast: [
              ["breast", "2-kick / 1-pull — feel the glide"],
              ["breast", "Pull only with float kick"],
              ["breast", "Kick only with board — drive from hips"],
            ],
            fly: [
              ["fly", "3-3-3 drill — right / left / full"],
              ["fly", "Single-arm fly — control rhythm"],
              ["fly", "2-kick / 1-pull — find the rhythm"],
            ],
            distance: [
              ["free", "Catch-up — long stroke, deliberate timing"],
              ["free", "6-3-6 — kick balance into stroke rhythm"],
              ["free", "Fingertip drag — high-elbow recovery"],
              ["free", "Single-arm — full extension hold"],
            ],
            sprint: [
              ["free", "Fast-tempo build — short, quick turnover"],
              ["free", "Underwater dolphin off the wall — power"],
              ["free", "Catch acceleration — fast first 12 yd"],
            ],
            endurance: [
              ["free", "Distance per stroke — count strokes/length"],
              ["free", "Long pull — extend the glide"],
              ["free", "Bilateral breathing — symmetric balance"],
            ],
            mixed: [
              ["free",   "Free drill — catch-up"],
              ["back",   "Back drill — single-arm"],
              ["breast", "Breast drill — 2-kick / 1-pull"],
              ["fly",    "Fly drill — 3-3-3"],
            ],
          };
          const drills = drillsByType[typeKey] || drillsByType.free;
          // 25yd dist is standard for drills (3-3-3, catch-up, single-arm
          // typically authored as 25s). Include it even when the type's
          // bias excludes it. Joint search over (dist, totalReps) to hit
          // budget exactly — totalReps distributed across drills via
          // asymmetric counts (e.g., 3 drills of 25yd with budget 400 →
          // 16 total reps as 6+5+5). Asymmetric rep counts across drills
          // are standard coaching practice ("3 reps on the first one,
          // 2 on the next two").
          const drillDists = [25, 50, 75, 100];
          let best = null;
          for (const dist of drillDists) {
            // Total reps needed across all drills for exact budget match
            const idealTotalReps = budgetYd / dist;
            // Try rep counts that yield 1-6 reps per drill on average
            for (let totalReps = drills.length; totalReps <= drills.length * 6; totalReps++) {
              const total = totalReps * dist;
              const pctOff = Math.abs(total - budgetYd) / budgetYd;
              if (!best || pctOff < best.pctOff) {
                best = { dist, totalReps, total, pctOff };
              }
            }
          }
          // Distribute totalReps across drills as evenly as possible.
          // Remainder reps go to the first few drills (front-loaded).
          const baseReps = Math.floor(best.totalReps / drills.length);
          const remainderReps = best.totalReps - baseReps * drills.length;
          const repsPerDrill = drills.map((_, i) => baseReps + (i < remainderReps ? 1 : 0));
          const interval = templateComputeInterval(best.dist, "easy");
          const sets = drills.map(([s, drillDesc], i) => ({
            reps: repsPerDrill[i], dist: best.dist,
            desc: drillDesc,
            interval,
            focus: "Slow, focused on the cue — drill quality over speed",
          }));
          const totalYards = best.total;
          const slots = { dist: best.dist };
          return {
            label: this.labelTemplate(slots, totalYards),
            totalYards,
            sets,
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ build_within_rep — single row with within-rep escalation  │
      // │ Coverage: 19 bank options (3.1%)                           │
      // │ "Build", "negative split", or "locomotive" style.          │
      // └────────────────────────────────────────────────────────────┘
      build_within_rep: {
        id: "build_within_rep",
        family: "single_row",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im"],
        labelTemplate: (slots, totalYards) =>
          `Build ${slots.stroke === "IM" ? "IM " : ""}${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Build needs room WITHIN a rep to escalate — require dist ≥ 75
          const distChoices = templateRepDistChoices(typeKey, section).filter(d => d >= 75);
          const shape = templatePickRepShape(
            distChoices.length ? distChoices : [100, 150, 200],
            budgetYd, 4, 8, "any", rand,
          );
          const buildStyle = templatePick(
            ["build each rep — easy first quarter to fast last quarter",
             "negative split — second half faster than first",
             "locomotive — increase pace each segment of the rep"],
            rand,
          );
          const interval = templateComputeInterval(shape.dist, "strong");
          return {
            label: this.labelTemplate({ stroke, dist: shape.dist }, shape.total),
            totalYards: shape.total,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — ${buildStyle}`,
              interval,
              focus: "Controlled escalation within each rep — finish faster than you started",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ threshold_steady — single row at threshold pace            │
      // │ Coverage: 18 bank options (2.9%)                           │
      // │ Tighter intervals than aerobic_volume.                     │
      // └────────────────────────────────────────────────────────────┘
      threshold_steady: {
        id: "threshold_steady",
        family: "single_row",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "im"],
        labelTemplate: (slots, totalYards) =>
          `Threshold ${slots.stroke === "IM" ? "IM " : ""}${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const distChoices = templateRepDistChoices(typeKey, section);
          const shape = templatePickRepShape(distChoices, budgetYd, 4, 10, "any", rand);
          const interval = templateComputeInterval(shape.dist, "threshold");
          return {
            label: this.labelTemplate({ stroke, dist: shape.dist }, shape.total),
            totalYards: shape.total,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — threshold pace (RPE 7-8/10), sustainable`,
              interval,
              focus: "Hold threshold across all reps — controlled, not all out",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ race_day_simulation — race-pace events + recovery between │
      // │ Coverage: 17 bank options (2.8%)                           │
      // │ Standard race distances; 2-6 events with recovery between. │
      // └────────────────────────────────────────────────────────────┘
      race_day_simulation: {
        id: "race_day_simulation",
        family: "multi_row_composition",
        applicableSections: ["main"],
        applicableTypes: ["distance", "mixed", "sprint", "im", "free"],
        labelTemplate: (slots, totalYards) =>
          `Race-Day Simulation: ${slots.eventCount}×${slots.eventDist} (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          const raceDistances = typeKey === "sprint" ? [50, 100] :
                                 typeKey === "distance" ? [200, 400, 500] :
                                 [100, 200, 400];
          // Search all (eventDist, recoveryDist, count) — previously locked
          // recovery to ~50% of event, which jumped past ±5% for some
          // (event, budget) pairs. Letting recovery flex across canonical
          // dists (25 to event_dist) closes the budget gap. Recovery is
          // capped at event_dist so it stays a "recovery" semantically
          // (longer recoveries become weird "easy main + race-pace blip").
          let best = null;
          for (const eventDist of raceDistances) {
            const recoveryOptions = TEMPLATE_CANONICAL_DISTS.filter(d => d >= 25 && d <= eventDist);
            for (const recoveryDist of recoveryOptions) {
              for (let count = 2; count <= 6; count++) {
                const total = count * eventDist + (count - 1) * recoveryDist;
                const pctOff = Math.abs(total - budgetYd) / budgetYd;
                if (!best || pctOff < best.pctOff) {
                  best = { eventDist, recoveryDist, count, total, pctOff };
                }
              }
            }
          }
          const sets = [];
          for (let i = 0; i < best.count; i++) {
            sets.push({
              reps: 1, dist: best.eventDist,
              desc: `${templateStrokeLabel(stroke)} — race pace, Event ${i + 1}`,
              interval: templateComputeInterval(best.eventDist, "racePace"),
              focus: "Race-pace simulation — hit your goal split",
            });
            if (i < best.count - 1) {
              sets.push({
                reps: 1, dist: best.recoveryDist,
                desc: "Easy choice — between events",
                interval: templateComputeInterval(best.recoveryDist, "easy"),
                focus: "Recover before the next event",
              });
            }
          }
          return {
            label: this.labelTemplate({ eventCount: best.count, eventDist: best.eventDist }, best.total),
            totalYards: best.total,
            sets,
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ pyramid_up_down — all reps=1, peak in middle               │
      // │ Coverage: 13 bank options (2.1%) — spec marquee template   │
      // │ Walks the canonical-distance ladder; non-uniform steps OK. │
      // └────────────────────────────────────────────────────────────┘
      pyramid_up_down: {
        id: "pyramid_up_down",
        family: "single_rep_multi_row",
        applicableSections: ["main"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Pyramid Up-Down ${slots.base}-${slots.peak} (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Type biases give an ALLOWED range for base/peak; we search the
          // (base, peak, peak-reps) space to find the pyramid total closest
          // to budget. Pyramid totals jump in big discrete steps (canonical
          // dists have non-uniform gaps), so a narrow search misses ±5%
          // tolerance often. Peak-reps ∈ {1, 2} lets us "hold the peak" for
          // a finer-grained yardage knob without breaking the pyramid shape.
          const typeBiases = {
            sprint:    { baseMin: 25, baseMax: 50,  peakMin: 75,  peakMax: 100 },
            fly:       { baseMin: 25, baseMax: 50,  peakMin: 75,  peakMax: 100 },
            back:      { baseMin: 25, baseMax: 50,  peakMin: 100, peakMax: 200 },
            breast:    { baseMin: 25, baseMax: 50,  peakMin: 100, peakMax: 200 },
            distance:  { baseMin: 50, baseMax: 150, peakMin: 200, peakMax: 400 },
            endurance: { baseMin: 50, baseMax: 100, peakMin: 150, peakMax: 300 },
            im:        { baseMin: 50, baseMax: 100, peakMin: 150, peakMax: 300 },
            mixed:     { baseMin: 25, baseMax: 100, peakMin: 100, peakMax: 300 },
            free:      { baseMin: 25, baseMax: 75,  peakMin: 100, peakMax: 200 },
          };
          const bias = typeBiases[typeKey] || { baseMin: 50, baseMax: 100, peakMin: 100, peakMax: 200 };
          const inRange = (d, lo, hi) => d >= lo && d <= hi;
          const allowedBases = TEMPLATE_CANONICAL_DISTS.filter(d => inRange(d, bias.baseMin, bias.baseMax));
          const allowedPeaks = TEMPLATE_CANONICAL_DISTS.filter(d => inRange(d, bias.peakMin, bias.peakMax));
          let best = null;
          for (const base of allowedBases) {
            const baseIdx = TEMPLATE_CANONICAL_DISTS.indexOf(base);
            for (const peak of allowedPeaks) {
              const peakIdx = TEMPLATE_CANONICAL_DISTS.indexOf(peak);
              if (peakIdx < baseIdx + 2) continue;  // need ≥3 rungs per side
              for (const peakReps of [1, 2]) {
                const dists = [];
                for (let i = baseIdx; i <= peakIdx; i++) dists.push(TEMPLATE_CANONICAL_DISTS[i]);
                if (peakReps === 2) dists.push(TEMPLATE_CANONICAL_DISTS[peakIdx]);  // hold peak
                for (let i = peakIdx - 1; i >= baseIdx; i--) dists.push(TEMPLATE_CANONICAL_DISTS[i]);
                const total = dists.reduce((s, d) => s + d, 0);
                const pctOff = Math.abs(total - budgetYd) / budgetYd;
                if (!best || pctOff < best.pctOff) {
                  best = { dists, total, pctOff, base, peak, peakReps };
                }
              }
            }
          }
          // Fallback (shouldn't fire with valid bias config)
          if (!best) {
            best = { dists: [50, 100, 150, 100, 50], total: 450, pctOff: 1, base: 50, peak: 150, peakReps: 1 };
          }
          // Peak position depends on whether we held the peak for 2 rungs
          const ascLen = best.dists.length / 2 + (best.peakReps === 2 ? 0.5 : 0);
          const sets = best.dists.map((d, i) => {
            const atPeak = best.peakReps === 2
              ? (d === best.peak && (i === Math.floor(ascLen - 0.5) || i === Math.floor(ascLen + 0.5)))
              : d === best.peak;
            return {
              reps: 1, dist: d,
              desc: atPeak
                ? `${templateStrokeLabel(stroke)} — peak rung, push hard`
                : `${templateStrokeLabel(stroke)} — ${i < ascLen ? "build up" : "descend down"} the pyramid`,
              interval: templateComputeInterval(d, "strong"),
              focus: atPeak ? "Apex — hardest rung" : "Maintain form and pace control",
            };
          });
          return {
            label: this.labelTemplate({ base: best.base, peak: best.peak }, best.total),
            totalYards: best.total,
            sets,
          };
        },
      },

      // ═══════════════════════════════════════════════════════════════
      //  S3 — Warmup + cooldown templates
      //  Per spec §11 these were originally out-of-scope for v1 ("Engine-
      //  generated warmups and cooldowns"). Cap'n's S3 choice was to
      //  enable engine for all 4 sections, which required templates for
      //  warmup + cooldown. The five below are shaped on coaching norms
      //  (warmups: 300-800yd easy/build, often no-interval; cooldowns:
      //  200-400yd easy throughout).
      // ═══════════════════════════════════════════════════════════════

      // ┌────────────────────────────────────────────────────────────┐
      // │ aerobic_warmup — single row, easy aerobic                  │
      // │ Warmup. Typical: 8×50 or 6×100 easy.                       │
      // └────────────────────────────────────────────────────────────┘
      aerobic_warmup: {
        id: "aerobic_warmup",
        family: "single_row",
        applicableSections: ["warmup"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Aerobic warmup ${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Warmup dists stay short — 50/75/100 typical, sometimes 150
          const distChoices = [50, 75, 100, 150].filter(d => d * 4 <= budgetYd * 1.5);
          const shape = templatePickRepShape(
            distChoices.length ? distChoices : [50, 75, 100],
            budgetYd, 4, 10, "any", rand,
          );
          const effortPhrase = templatePick(
            ["easy, settle in", "easy/moderate, loosen up", "aerobic pace, find your stroke"],
            rand,
          );
          return {
            label: this.labelTemplate({ dist: shape.dist }, shape.total),
            totalYards: shape.total,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — ${effortPhrase}`,
              interval: NO_INTERVAL_CANONICAL,
              focus: "Prime the aerobic engine — long stroke",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ warmup_build — single row, build within rep                │
      // │ Warmup. 4×100 easy→fast over the rep.                      │
      // └────────────────────────────────────────────────────────────┘
      warmup_build: {
        id: "warmup_build",
        family: "single_row",
        applicableSections: ["warmup"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Build warmup ${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Build needs room within a rep — 75 or 100 minimum, 150 max for warmup
          const distChoices = [75, 100, 150];
          const shape = templatePickRepShape(distChoices, budgetYd, 3, 6, "any", rand);
          return {
            label: this.labelTemplate({ dist: shape.dist }, shape.total),
            totalYards: shape.total,
            sets: [{
              reps: shape.reps, dist: shape.dist,
              desc: `${templateStrokeLabel(stroke)} — build from easy to moderate across each rep`,
              interval: templateComputeInterval(shape.dist, "aerobic"),
              focus: "Progressive warm-up — finish each rep stronger than you started",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ multi_stroke_warmup — N strokes, each row a different one  │
      // │ Warmup. Loosens all four strokes before mains.             │
      // └────────────────────────────────────────────────────────────┘
      multi_stroke_warmup: {
        id: "multi_stroke_warmup",
        family: "multi_row_composition",
        applicableSections: ["warmup"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `${slots.strokeCount}-stroke warmup ${slots.dist}s (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          // IM types do full 4-stroke order; everyone else 3 strokes
          const allStrokes = ["fly", "back", "breast", "free"];
          const strokes = typeKey === "im"
            ? allStrokes
            : [...allStrokes].sort(() => rand() - 0.5).slice(0, 3);
          // Short pieces for warmup — 25/50/75. Joint search over (dist,
          // totalReps) with front-loaded asymmetric distribution so the
          // total can hit budget exactly even when budget/strokes.length
          // doesn't divide cleanly (e.g., 400yd / 3 strokes / canonical
          // dists). Same trick as drill_progression.
          const warmupDists = [25, 50, 75];
          let best = null;
          for (const dist of warmupDists) {
            for (let totalReps = strokes.length; totalReps <= strokes.length * 8; totalReps++) {
              const total = totalReps * dist;
              const pctOff = Math.abs(total - budgetYd) / budgetYd;
              if (!best || pctOff < best.pctOff) {
                best = { dist, totalReps, total, pctOff };
              }
            }
          }
          const baseReps = Math.floor(best.totalReps / strokes.length);
          const remainderReps = best.totalReps - baseReps * strokes.length;
          const repsPerStroke = strokes.map((_, i) => baseReps + (i < remainderReps ? 1 : 0));
          const interval = templateComputeInterval(best.dist, "easy");
          const sets = strokes.map((s, i) => ({
            reps: repsPerStroke[i], dist: best.dist,
            desc: `${templateStrokeLabel(s)} — easy, focus on stroke quality`,
            interval,
            focus: `Loosen ${s === "free" ? "freestyle" : s} mechanics`,
          }));
          const totalYards = best.total;
          const shape = { dist: best.dist };
          return {
            label: this.labelTemplate({ strokeCount: strokes.length, dist: shape.dist }, totalYards),
            totalYards,
            sets,
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ easy_swim_cooldown — single long easy rep                  │
      // │ Cooldown. 1×200/300/400 easy.                              │
      // └────────────────────────────────────────────────────────────┘
      easy_swim_cooldown: {
        id: "easy_swim_cooldown",
        family: "single_row",
        applicableSections: ["cooldown"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Easy ${slots.dist} cooldown (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          // Cooldown stroke: most types do freestyle; back/breast/fly/im swimmers
          // appreciate doing the off-stroke (often back) for active recovery.
          const cooldownStroke = ["back", "breast", "fly", "im"].includes(typeKey)
            ? (typeKey === "back" ? "free" : "back")  // backstrokers cool down with free; others cool down with back
            : (rand() < 0.7 ? "free" : "choice");
          // Pick canonical dist closest to budget (single rep)
          const cooldownDist = TEMPLATE_CANONICAL_DISTS.reduce((best, d) =>
            (d >= 100 && d <= 500 && Math.abs(d - budgetYd) < Math.abs(best - budgetYd)) ? d : best, 200);
          return {
            label: this.labelTemplate({ dist: cooldownDist }, cooldownDist),
            totalYards: cooldownDist,
            sets: [{
              reps: 1, dist: cooldownDist,
              desc: `${templateStrokeLabel(cooldownStroke)} — easy, fully recovered`,
              interval: NO_INTERVAL_CANONICAL,
              focus: "Active recovery — flush the system",
            }],
          };
        },
      },

      // ┌────────────────────────────────────────────────────────────┐
      // │ cooldown_descend — descending ladder, each piece easier    │
      // │ Cooldown. 200/150/100/50 type pattern.                     │
      // └────────────────────────────────────────────────────────────┘
      cooldown_descend: {
        id: "cooldown_descend",
        family: "single_rep_multi_row",
        applicableSections: ["cooldown"],
        applicableTypes: ["distance", "endurance", "mixed", "free", "back", "breast", "fly", "im", "sprint"],
        labelTemplate: (slots, totalYards) =>
          `Descending cooldown ${slots.peak}-${slots.base} (${totalYards} yd)`,
        generate(typeKey, section, budgetYd, rand = Math.random) {
          const stroke = templateDefaultStroke(typeKey);
          // Build descending ladders of varying lengths to fit budget
          // Patterns: [200, 150, 100, 50] = 500; [150, 100, 50] = 300;
          // [100, 75, 50, 25] = 250; [200, 150, 100] = 450
          const patterns = [
            [200, 150, 100, 50],
            [150, 100, 75, 50, 25],
            [100, 75, 50, 25],
            [200, 150, 100],
            [150, 100, 50],
            [100, 50, 25],
          ];
          let best = null;
          for (const p of patterns) {
            const total = p.reduce((s, d) => s + d, 0);
            const pctOff = Math.abs(total - budgetYd) / Math.max(budgetYd, 1);
            if (!best || pctOff < best.pctOff) {
              best = { dists: p, total, pctOff };
            }
          }
          const sets = best.dists.map((d, i) => ({
            reps: 1, dist: d,
            desc: `${templateStrokeLabel(stroke)} — ${i === 0 ? "easy" : "easier than the last"}`,
            interval: NO_INTERVAL_CANONICAL,
            focus: i === best.dists.length - 1 ? "Final flush" : "Bring the heart rate down",
          }));
          return {
            label: this.labelTemplate({ peak: best.dists[0], base: best.dists[best.dists.length - 1] }, best.total),
            totalYards: best.total,
            sets,
          };
        },
      },
    };

    // ── Validator (S2.5 — spec §5) ───────────────────────────────────
    // Returns array of { code, msg } error objects. Empty array = passes.
    // V5 (mix-distribution) and V7 (cross-rep descending intervals) are
    // deferred: V5 needs picker context (mix pills) that engine doesn't
    // see yet (S3); V7 doesn't apply to current templates whose descend
    // shape is a single-row cue, not cross-rep intervals.
    function templateValidate(option, context) {
      const errs = [];
      if (!option) { errs.push({ code: "V0", msg: "null output" }); return errs; }
      if (!option.label) errs.push({ code: "V0", msg: "missing label" });
      if (!Array.isArray(option.sets) || option.sets.length === 0) {
        errs.push({ code: "V0", msg: "missing/empty sets" });
        return errs;
      }
      // V1: yardage within ±5% of budget
      if (context?.budgetYd) {
        const pctOff = Math.abs(option.totalYards - context.budgetYd) / context.budgetYd;
        if (pctOff > 0.05) {
          errs.push({ code: "V1", msg: `yardage ${option.totalYards} vs budget ${context.budgetYd} (${(pctOff * 100).toFixed(1)}% off)` });
        }
      }
      // Per-row checks
      for (const [i, s] of option.sets.entries()) {
        // V2: canonical distance
        if (!TEMPLATE_CANONICAL_DISTS.includes(s.dist)) {
          errs.push({ code: "V2", msg: `set[${i}] dist=${s.dist} not canonical` });
        }
        // V3: interval present + format
        if (!s.interval) {
          errs.push({ code: "V3", msg: `set[${i}] missing interval` });
        } else if (s.interval !== NO_INTERVAL_CANONICAL && !/^On \d+:\d{2}$/.test(s.interval) && !/^On :\d{2}$/.test(s.interval)) {
          errs.push({ code: "V3", msg: `set[${i}] interval malformed: ${s.interval}` });
        } else {
          // V4: interval realism — interval ≥ swim time + 5s rest at baseline pace
          const secs = parseIntervalSecs(s.interval);
          if (secs !== null) {
            const swimTime = (s.dist / 100) * TEMPLATE_BASELINE_PACE_SECS;
            if (secs < swimTime + 5) {
              errs.push({ code: "V4", msg: `set[${i}] interval ${secs}s < swim time ${Math.round(swimTime)}s + 5s rest` });
            }
          }
        }
        // V6: equipment-cue match
        const descBlob = (s.desc || "") + " " + (s.focus || "");
        if (s.eq === "pull" && !/pull|paddle|buoy/i.test(descBlob)) {
          errs.push({ code: "V6", msg: `set[${i}] eq=pull but no pull/paddle/buoy cue` });
        }
        if (s.eq === "kick" && !/kick|board/i.test(descBlob)) {
          errs.push({ code: "V6", msg: `set[${i}] eq=kick but no kick/board cue` });
        }
        // V8: focus non-empty
        if (!s.focus || s.focus.trim() === "") {
          errs.push({ code: "V8", msg: `set[${i}] empty focus` });
        }
      }
      // v2.0 — V-Lane: every set's interval fits all lane paces (rest ≥ 5s
      // for slowest lane, rest ≤ 1.5× swimTime for fastest). Only fires
      // when context.lanesPaceSecs is non-empty (single-pace generate path
      // skips this check entirely). Same math as the bank picker's
      // optionFitsAllLanes — instead of returning bool we surface specific
      // failures so the retry path knows the rule was violated.
      if (Array.isArray(context?.lanesPaceSecs) && context.lanesPaceSecs.length > 0) {
        const lanes = context.lanesPaceSecs.filter(p => typeof p === "number" && p > 0);
        if (lanes.length && !optionFitsAllLanes(option, lanes, 5, 1.5, TEMPLATE_BASELINE_PACE_SECS)) {
          errs.push({ code: "V-Lane", msg: "engine output's intervals don't fit all lane paces" });
        }
      }
      // Phase 3 PSC slice 2 — V9: engine output must not violate any active
      // swimmer constraint. Same option-level check as the bank picker's
      // step-0 filter. Retry-3 logic in generateWithRetry handles rejection.
      // Skipped when activeConstraints is empty (no overhead in the default case).
      if (Array.isArray(context?.activeConstraints) && context.activeConstraints.length > 0) {
        if (optionViolatesPSC(option, context.activeConstraints)) {
          errs.push({ code: "V9", msg: "engine output violates an active per-swimmer constraint" });
        }
      }
      return errs;
    }

    // Engine entry point. Two surfaces:
    //   .generate(...)            — single-attempt template invocation.
    //                                 Returns bank-shaped option or null.
    //                                 No validation, no retry, no anti-repeat.
    //                                 Useful for tests and direct invocations.
    //   .generateWithRetry({...}) — validated invocation per spec §5.
    //                                 Hybrid retry-3 per Cap'n's S2.5 choice:
    //                                 attempt 1+2 = same template w/ new slots,
    //                                 attempt 3 = different applicable template
    //                                 (respecting anti-repeat). Returns
    //                                 { option, errors, attempts, templateUsed }
    //                                 or { option: null, ... } on full failure
    //                                 — caller falls back to bank.
    //   .validate(option, ctx)    — exposed validator.
    const TEMPLATE_ENGINE = {
      templates: TEMPLATE_DEFINITIONS,
      list() { return Object.keys(TEMPLATE_DEFINITIONS); },
      applicable(section, typeKey) {
        return Object.values(TEMPLATE_DEFINITIONS).filter(t =>
          t.applicableSections.includes(section) &&
          t.applicableTypes.includes(typeKey)
        );
      },
      generate(templateId, typeKey, section, budgetYd, rand) {
        const tpl = TEMPLATE_DEFINITIONS[templateId];
        if (!tpl) return null;
        if (!tpl.applicableSections.includes(section)) return null;
        if (!tpl.applicableTypes.includes(typeKey)) return null;
        return tpl.generate(typeKey, section, budgetYd, rand);
      },
      validate(option, context) {
        return templateValidate(option, context);
      },
      generateWithRetry({ templateId, typeKey, section, budgetYd, recent = [], lanesPaceSecs = null, activeConstraints = [], rand = Math.random }) {
        const attempts = [];
        const ctx = { budgetYd, typeKey, section, lanesPaceSecs, activeConstraints };
        const defaultStroke = templateDefaultStroke(typeKey);
        // Anti-repeat lookup set, keyed by `template_id:stroke`.
        // The picker (S3) passes the last 10 generated entries.
        const excluded = new Set((recent || []).map(r => `${r.template_id}:${r.stroke}`));
        // If the requested template+stroke is in the excluded set, we
        // skip directly to the "different template" leg of retry.
        const requestedExcluded = excluded.has(`${templateId}:${defaultStroke}`);
        const tryGen = (tid) => {
          const tpl = TEMPLATE_DEFINITIONS[tid];
          if (!tpl) return { option: null, errors: [{ code: "V0", msg: `unknown template ${tid}` }] };
          if (!tpl.applicableSections.includes(section)) return { option: null, errors: [{ code: "V0", msg: `${tid} not applicable to section ${section}` }] };
          if (!tpl.applicableTypes.includes(typeKey)) return { option: null, errors: [{ code: "V0", msg: `${tid} not applicable to type ${typeKey}` }] };
          const opt = tpl.generate(typeKey, section, budgetYd, rand);
          const errs = templateValidate(opt, ctx);
          return { option: opt, errors: errs };
        };
        // Attempts 1+2: original template, new slot rolls each time
        // (slot picks are randomized inside each template — calling again
        // produces a different shape). Skip if anti-repeat excluded it.
        if (!requestedExcluded) {
          for (let i = 0; i < 2; i++) {
            const r = tryGen(templateId);
            attempts.push({ templateId, errors: r.errors });
            if (r.errors.length === 0) {
              return { option: r.option, attempts, templateUsed: templateId };
            }
          }
        }
        // Attempt 3: pick a different applicable template (anti-repeat aware)
        const alternatives = Object.keys(TEMPLATE_DEFINITIONS).filter(id => {
          if (id === templateId) return false;
          const tpl = TEMPLATE_DEFINITIONS[id];
          if (!tpl.applicableSections.includes(section)) return false;
          if (!tpl.applicableTypes.includes(typeKey)) return false;
          if (excluded.has(`${id}:${defaultStroke}`)) return false;
          return true;
        });
        if (alternatives.length) {
          const altId = alternatives[Math.floor(rand() * alternatives.length)];
          const r = tryGen(altId);
          attempts.push({ templateId: altId, errors: r.errors });
          if (r.errors.length === 0) {
            return { option: r.option, attempts, templateUsed: altId };
          }
        }
        // All attempts exhausted — caller falls back to bank
        return { option: null, attempts, templateUsed: null };
      },
    };

    // Expose for browser-console smoke tests during S1/S2. Wired to UI in S3.
    if (typeof window !== "undefined") window.TEMPLATE_ENGINE = TEMPLATE_ENGINE;

    // ═══════════════════════════════════════════════════════════════
    //  UNIT CONVERSION — yd ↔ m
    //  Used by the bank-fallback path when a content gap forces us to
    //  pull options authored for a different pool unit. Distances get
    //  multiplied by 0.9144 (or 1/0.9144) and snapped to pool-realistic
    //  increments; intervals scale by the same ratio so per-unit pace
    //  stays equivalent. `label` and `desc` text are NOT rewritten —
    //  see the manual / code-quality-plan memory for why.
    // ═══════════════════════════════════════════════════════════════
    const YARDS_PER_METER = 1 / 0.9144;

    // Snap a distance to a pool-realistic increment:
    //   ≥100 → nearest 25 (matches typical 100/150/200/400/etc authoring)
    //   <100 → nearest 5  (captures 25/50/75 plus the occasional 15-yd
    //                       drill cue without forcing 0)
    function snapPoolDistance(value) {
      if (value <= 0) return 0;
      if (value >= 100) return Math.round(value / 25) * 25;
      return Math.max(5, Math.round(value / 5) * 5);
    }
    function convertYardsToMeters(yards)   { return snapPoolDistance(yards * 0.9144); }
    function convertMetersToYards(meters)  { return snapPoolDistance(meters * YARDS_PER_METER); }

    // Convert a whole option (warmup/drill/main/cooldown) across the
    // unit boundary. Each set's dist + interval get converted; totalYards
    // is recomputed from the post-conversion sets. The returned option is
    // a fresh copy; the input is untouched. fromUnit / toUnit ∈ {"yd","m"}.
    function convertOptionUnits(option, fromUnit, toUnit) {
      if (fromUnit === toUnit) return option;
      const ratio = toUnit === "m" ? 0.9144 : YARDS_PER_METER;
      const distConvert = toUnit === "m" ? convertYardsToMeters : convertMetersToYards;
      const newSets = option.sets.map(s => ({
        ...s,
        dist:     distConvert(s.dist),
        interval: scaleInterval(s.interval, ratio),
      }));
      const newTotal = newSets.reduce((sum, s) => sum + (s.reps || 1) * s.dist, 0);
      return { ...option, sets: newSets, totalYards: newTotal };
    }

    // Unit-aware bank selection. Replaces the older ternary fallback that
    // returned yard-authored options to a meters-mode user without converting.
    // Fallback chain (defense in depth — today all three banks have full
    // coverage for all 9 typeIds, so the cross-unit branch is dead code now):
    //   25m → SCM → LCM → SCY    (SCM authored for 25m; LCM authored for 50m
    //                              but same unit; SCY needs yd→m conversion)
    //   50m → LCM → SCY          (skip SCM — different pool size)
    //   25y → SCY                (no fallback — SCY is the universal source)
    //
    // `kind`   — "warmup" | "cooldown" | "drill" | "main"
    // `typeId` — workout type (ignored for flat warmup/cooldown banks)
    // `poolMode` — "25y" | "25m" | "50m"
    function getBankOptions(kind, typeId, poolMode, ugcOverlay = null) {
      const targetUnit = poolMode === "25y" ? "yd" : "m";
      const byType = (kind === "drill" || kind === "main");

      const TABLE = {
        warmup: {
          "25m": [["m", WARMUP_OPTIONS_SCM], ["m", WARMUP_OPTIONS_50M], ["yd", WARMUP_OPTIONS]],
          "50m": [["m", WARMUP_OPTIONS_50M], ["yd", WARMUP_OPTIONS]],
          "25y": [["yd", WARMUP_OPTIONS]],
        },
        cooldown: {
          "25m": [["m", COOLDOWN_OPTIONS_SCM], ["m", COOLDOWN_OPTIONS_50M], ["yd", COOLDOWN_OPTIONS]],
          "50m": [["m", COOLDOWN_OPTIONS_50M], ["yd", COOLDOWN_OPTIONS]],
          "25y": [["yd", COOLDOWN_OPTIONS]],
        },
        drill: {
          "25m": [["m", DRILL_OPTIONS_SCM], ["m", DRILL_OPTIONS_50M], ["yd", DRILL_OPTIONS]],
          "50m": [["m", DRILL_OPTIONS_50M], ["yd", DRILL_OPTIONS]],
          "25y": [["yd", DRILL_OPTIONS]],
        },
        kick: {
          "25m": [["m", KICK_OPTIONS_SCM], ["m", KICK_OPTIONS_50M], ["yd", KICK_OPTIONS]],
          "50m": [["m", KICK_OPTIONS_50M], ["yd", KICK_OPTIONS]],
          "25y": [["yd", KICK_OPTIONS]],
        },
        main: {
          "25m": [["m", MAIN_OPTIONS_SCM], ["m", MAIN_OPTIONS_50M], ["yd", MAIN_OPTIONS]],
          "50m": [["m", MAIN_OPTIONS_50M], ["yd", MAIN_OPTIONS]],
          "25y": [["yd", MAIN_OPTIONS]],
        },
      };
      const chain = TABLE[kind][poolMode];

      // Canonical chain — walk fallback list until a non-empty result.
      // Phase H Stage 2: banks are flat arrays with `types: []` + `strokes: []`
      // tags on each option (multi-tag membership). For drill/main, filter by
      // typeId across BOTH arrays so an option declaring `strokes: ["back"]` is
      // found when picker asks for typeId="back", and an option declaring
      // `types: ["distance", "sprint"]` is found in either pool. Fallback to
      // `mixed`-tagged options when the primary filter returns empty —
      // preserves the previous `data[typeId] || data.mixed` semantics.
      let canonical = [];
      for (const [unit, data] of chain) {
        let list;
        if (!byType) {
          list = data;
        } else {
          list = data.filter(o =>
            (o.types   && o.types.includes(typeId)) ||
            (o.strokes && o.strokes.includes(typeId))
          );
          if (list.length === 0 && typeId !== "mixed") {
            list = data.filter(o => o.types && o.types.includes("mixed"));
          }
        }
        if (list && list.length > 0) {
          canonical = unit === targetUnit ? list : list.map(o => convertOptionUnits(o, unit, targetUnit));
          break;
        }
      }

      // UGC overlay — exact-pool-mode only (no cross-unit fallback per
      // spec §9: per-pool-mode authoring; no auto-translate in v1).
      const overlayRows = getOverlayRowsForTuple(ugcOverlay, kind, typeId, poolMode);
      return overlayRows.length > 0 ? canonical.concat(overlayRows) : canonical;
    }

    // UGC overlay extractor for picker. Returns array of options
    // matching (kind, typeId, poolMode) exactly from the UGC overlay
    // object. ugcOverlay shape matches the 12 JS bank constants.
    // Spec: UGC_COACH_SETS_SCOPE.md §7.
    function getOverlayRowsForTuple(ugcOverlay, kind, typeId, poolMode) {
      if (!ugcOverlay) return [];
      const KEY_MAP = {
        warmup:   { "25y": "WARMUP_OPTIONS",   "25m": "WARMUP_OPTIONS_SCM",   "50m": "WARMUP_OPTIONS_50M"   },
        cooldown: { "25y": "COOLDOWN_OPTIONS", "25m": "COOLDOWN_OPTIONS_SCM", "50m": "COOLDOWN_OPTIONS_50M" },
        drill:    { "25y": "DRILL_OPTIONS",    "25m": "DRILL_OPTIONS_SCM",    "50m": "DRILL_OPTIONS_50M"    },
        main:     { "25y": "MAIN_OPTIONS",     "25m": "MAIN_OPTIONS_SCM",     "50m": "MAIN_OPTIONS_50M"     },
      };
      const key = KEY_MAP[kind] && KEY_MAP[kind][poolMode];
      if (!key) return [];
      const data = ugcOverlay[key];
      if (!data) return [];
      if (kind === "warmup" || kind === "cooldown") {
        return Array.isArray(data) ? data : [];
      }
      // drill/main — nested by type_id OR stroke_id sub-key. Phase H Stage 2:
      // dbGetUgcOverlay projects multi-tag options into EVERY matching bucket,
      // so the "all" branch must dedup by _option_id to avoid a multi-tag
      // option appearing once per bucket in catalog browse.
      if (typeof data !== "object") return [];
      if (typeId === "all") {
        const seen = new Set();
        const out = [];
        for (const sub of Object.keys(data)) {
          if (!Array.isArray(data[sub])) continue;
          for (const opt of data[sub]) {
            const id = opt && opt._option_id;
            if (id != null) {
              if (seen.has(id)) continue;
              seen.add(id);
            }
            out.push(opt);
          }
        }
        return out;
      }
      return Array.isArray(data[typeId]) ? data[typeId] : [];
    }

    // ─── Catalog data accessor (used by CatalogView) ────────────────────
    // Returns the AUTHORED options for a given (kind, typeId, poolMode)
    // tuple — no cross-unit fallback, no conversion, no .mixed fallback.
    // The catalog browse intentionally shows the bank as authored, so a
    // coach can see what's actually in each bucket per pool mode.
    //
    // typeId === "all" (only valid for kind=drill/main) returns ALL typed
    // entries flattened, with each option carrying its source `_typeId`.
    //
    // Phase III note: when editable bank lands, this is the right hook
    // to merge in DB overrides (or whatever the writeback mechanism becomes).
    function getCatalogList(kind, typeId, poolMode, ugcOverlay = null) {
      const BANKS = {
        warmup:   { "25y": WARMUP_OPTIONS,   "25m": WARMUP_OPTIONS_SCM,   "50m": WARMUP_OPTIONS_50M   },
        cooldown: { "25y": COOLDOWN_OPTIONS, "25m": COOLDOWN_OPTIONS_SCM, "50m": COOLDOWN_OPTIONS_50M },
        drill:    { "25y": DRILL_OPTIONS,    "25m": DRILL_OPTIONS_SCM,    "50m": DRILL_OPTIONS_50M    },
        kick:     { "25y": KICK_OPTIONS,     "25m": KICK_OPTIONS_SCM,     "50m": KICK_OPTIONS_50M     },
        main:     { "25y": MAIN_OPTIONS,     "25m": MAIN_OPTIONS_SCM,     "50m": MAIN_OPTIONS_50M     },
      };
      const bank = BANKS[kind] && BANKS[kind][poolMode];
      // kick is a non-typed (flat) bank like warmup/cooldown — its options
      // carry empty types[]/strokes[], so it shows everything regardless of typeId.
      const isFlat = (kind === "warmup" || kind === "cooldown" || kind === "kick");
      // Phase H Stage 2: banks are flat arrays with types[]/strokes[] tags.
      // - flat sections (warmup/cooldown): show everything, _typeId=null
      // - typed sections, typeId="all": show everything, _typeId=first tag
      //   (preserves catalog grouping by primary type)
      // - typed sections, specific typeId: filter by types/strokes inclusion
      let canonical;
      if (!bank) {
        canonical = [];
      } else if (isFlat) {
        canonical = bank.map((o, i) => ({ ...o, _typeId: null, _idx: i }));
      } else if (typeId === "all") {
        canonical = bank.map((o, i) => ({
          ...o,
          _typeId: (o.types && o.types[0]) || (o.strokes && o.strokes[0]) || null,
          _idx: i,
        }));
      } else {
        canonical = bank
          .filter(o =>
            (o.types   && o.types.includes(typeId)) ||
            (o.strokes && o.strokes.includes(typeId))
          )
          .map((o, i) => ({ ...o, _typeId: typeId, _idx: i }));
      }
      // UGC overlay (Phase B+) — append exact-pool-mode rows that match
      // the tuple. Already carry _ugc/_author_sub/_visibility metadata
      // from the server. We add _typeId/_idx to match the catalog shape.
      const overlayRows = getOverlayRowsForTuple(ugcOverlay, kind, typeId, poolMode);
      if (!overlayRows.length) return canonical;
      const overlayShaped = overlayRows.map((o, i) => ({
        ...o,
        _typeId: isFlat ? null : (o._typeId || typeId),
        _idx:    canonical.length + i,
      }));
      return canonical.concat(overlayShaped);
    }

    // Format a workout as plain text for clipboard sharing.
    function workoutToText(workout, meta, paceInput, unit = "yds") {
      if (!workout) return "";
      const lines = [];
      const estMin = calcEstimatedMin(workout.blocks);
      lines.push(`${meta.label} Swim Workout — ${workout.totalYards.toLocaleString()} ${unit} (~${estMin} min)`);
      if (paceInput) lines.push(`Pace target: ${paceInput}/100`);
      lines.push("");
      for (const block of workout.blocks) {
        // Section model B — dryland blocks (no swim sets) render as an
        // exercise list in copied text.
        if (block.kind === "dryland" || !Array.isArray(block.sets)) {
          lines.push(`${(block.name || "DRYLAND").toUpperCase()}  (dryland · ${block.placement === "post" ? "after pool" : "before pool"})`);
          for (const e of (block.exercises || [])) {
            lines.push(`  ${e.sets > 1 ? e.sets + " × " : ""}${e.reps} — ${e.name}${e.rest ? ` (rest ${e.rest})` : ""}`);
          }
          lines.push("");
          continue;
        }
        const rounds = block.rounds || 1;
        const roundNote = rounds > 1 ? `  ×${rounds} rounds, ${block.roundRestSecs ?? 30}s rest between` : "";
        lines.push(`${block.name.toUpperCase()}  (${block.totalYards.toLocaleString()} ${unit})${roundNote}`);
        const renderSets = () => {
          for (const set of block.sets) {
            const ivl = set.interval ? ` @ ${set.interval}` : "";
            lines.push(`  ${set.reps} × ${set.dist} — ${set.desc}${ivl}`);
          }
        };
        if (rounds > 1) {
          for (let r = 1; r <= rounds; r++) {
            lines.push(`  — Round ${r} —`);
            renderSets();
            if (r < rounds) lines.push(`  (${block.roundRestSecs ?? 30}s rest)`);
          }
        } else {
          renderSets();
        }
        lines.push("");
      }
      return lines.join("\n").trimEnd();
    }

    // Sum reps×interval across all sets; fall back to 35 yd/min for sets with no interval.
    function calcEstimatedMin(blocks) {
      const FALLBACK_YD_PER_MIN = 35;
      // S4 #17 — implicit transition between sets (read board, push off, pause
      // between reps of different sets). ~10s per boundary. Round-to-round
      // transitions are NOT included here because `roundRestSecs` already
      // accounts for the planned between-rounds rest.
      const TRANSITION_SECS_PER_BOUNDARY = 10;
      let totalSecs = 0;
      let totalBoundaries = 0;
      for (const block of blocks) {
        // Section model B — dryland blocks have no swim sets/intervals; skip
        // them in the swim time estimate (their reps×sets timing is informal).
        if (!block || !Array.isArray(block.sets)) continue;
        const rounds = block.rounds || 1;
        let blockSecs = 0;
        for (const set of block.sets) {
          const secs = parseIntervalSecs(set.interval);
          if (secs !== null) {
            blockSecs += set.reps * secs;
          } else {
            blockSecs += (set.reps * set.dist / FALLBACK_YD_PER_MIN) * 60;
          }
        }
        totalSecs += blockSecs * rounds;
        if (rounds > 1) totalSecs += (rounds - 1) * (block.roundRestSecs ?? 30);
        // Within-block boundaries: (sets.length - 1) per round.
        totalBoundaries += Math.max(0, block.sets.length - 1) * rounds;
      }
      // Block-to-block boundaries: (blocks.length - 1) — once between each adjacent pair.
      totalBoundaries += Math.max(0, blocks.length - 1);
      totalSecs += totalBoundaries * TRANSITION_SECS_PER_BOUNDARY;
      return Math.round(totalSecs / 60);
    }

    // S4 #18 — Memoized cache of all `dist` values appearing in any option's
    // sets, per pool mode. Used to gate longer-swaps: we only offer a longer
    // version if the bank actually contains sets at that distance, rather than
    // hardcoding a 400 cap that drifts between yd / m.
    const _distSetCache = new Map();
    function _distSetForMode(poolMode) {
      const cached = _distSetCache.get(poolMode);
      if (cached) return cached;
      const isMeters25 = poolMode === "25m";
      const isMeters50 = poolMode === "50m";
      const flat   = isMeters25 ? [WARMUP_OPTIONS_SCM, COOLDOWN_OPTIONS_SCM]
                   : isMeters50 ? [WARMUP_OPTIONS_50M, COOLDOWN_OPTIONS_50M]
                   :              [WARMUP_OPTIONS,     COOLDOWN_OPTIONS];
      const typed  = isMeters25 ? [DRILL_OPTIONS_SCM, MAIN_OPTIONS_SCM]
                   : isMeters50 ? [DRILL_OPTIONS_50M, MAIN_OPTIONS_50M]
                   :              [DRILL_OPTIONS,     MAIN_OPTIONS];
      // Phase H Stage 2: all four banks (flat + typed) are flat arrays now.
      const dists = new Set();
      for (const bank of [...flat, ...typed]) {
        for (const opt of bank) for (const s of opt.sets) dists.add(s.dist);
      }
      _distSetCache.set(poolMode, dists);
      return dists;
    }

    // S4 #8 — Riegel-ish exponent for distance↔interval scaling on swaps.
    // T2 = T1 × (D2/D1)^EXP. EXP=1.06 is the standard "Riegel" coefficient
    // for fatigue-aware time prediction in middle-distance running/swimming.
    // Real swim data sits in the 1.05–1.10 range across stroke + pool size.
    const RIEGEL_EXP = 1.06;
    function riegelRatio(oldDist, newDist) {
      return Math.pow(newDist / oldDist, RIEGEL_EXP);
    }

    // S4 #8 + #18 — getEquivalents now takes poolMode so it can:
    //   1. Apply a Riegel-ish (non-linear) interval scale instead of a flat ratio
    //      (linear under-corrected the interval for sprints and over-corrected
    //      for long aerobic doubling).
    //   2. Gate BOTH swaps on whether the bank actually contains sets at the
    //      target distance — catches half-yard / half-meter outputs (e.g. 75yd
    //      shorter would be 37.5, which no pool supports) and unauthored long
    //      distances. Replaces the old hardcoded 400 cap.
    //   3. Suppress both swaps when the description encodes within-set
    //      structure (e.g. "25 Fly Drill / 25 Back Drill / 25 Breast Drill"
    //      describes a 75-yd structure that doesn't survive a 2× / ½× rescale).
    //      Heuristic: desc contains `<digits><text>/<digits>` — a numeric
    //      breakdown separated by a slash.
    const STRUCTURED_DESC_RE = /\d+[a-z\s]+\/\s*\d+/i;
    function getEquivalents(set, poolMode = "25y") {
      const result = { shorter: null, longer: null };
      // Suppress swaps entirely for sets whose desc describes a within-set
      // numeric breakdown — the rescale would leave the displayed text wrong.
      if (set.desc && STRUCTURED_DESC_RE.test(set.desc)) return result;
      const dists = _distSetForMode(poolMode);
      if (set.dist >= 50) {
        const newDist = set.dist / 2;
        if (dists.has(newDist)) {
          result.shorter = {
            ...set,
            dist:     newDist,
            reps:     set.reps * 2,
            interval: scaleInterval(set.interval, riegelRatio(set.dist, newDist)),
          };
        }
      }
      if (set.reps >= 2 && set.reps % 2 === 0) {
        const newDist = set.dist * 2;
        if (dists.has(newDist)) {
          result.longer = {
            ...set,
            dist:     newDist,
            reps:     set.reps / 2,
            interval: scaleInterval(set.interval, riegelRatio(set.dist, newDist)),
          };
        }
      }
      return result;
    }

    // ═══════════════════════════════════════════════════════════════
    //  COMPONENTS
    // ═══════════════════════════════════════════════════════════════

    function EquipmentBadge({ name }) {
      const colors = {
        kickboard: { bg: "#dbeafe", fg: "#1e40af" },
        fins:      { bg: "#cffafe", fg: "#155e75" },
        paddles:   { bg: "#fef3c7", fg: "#92400e" },
        buoy:      { bg: "#fce7f3", fg: "#9d174d" },
      };
      const c = colors[name] || { bg: "var(--color-text)", fg: "var(--color-border)" };
      return (
        <span style={{ background: c.bg, color: c.fg, fontSize: 10, fontWeight: 700,
                       padding: "2px 7px", borderRadius: 999, marginRight: 4,
                       textTransform: "uppercase", letterSpacing: "0.04em" }}>
          {name}
        </span>
      );
    }

    function SetRow({ set, idx, blockIdx, setIdx, isSwapOpen, onToggleSwap, onApplySwap, equipment, isEditingInterval, editIntervalDraft, setEditIntervalDraft, editIntervalError, onStartEditInterval, onCommitInterval, onClearInterval, onCancelInterval, isEditingDesc, editDescDraft, setEditDescDraft, onStartEditDesc, onCommitDesc, onCancelDesc, unit = "yds", blockSection, blockZoneId, poolMode = "25y", isFavoriteSet, isDisfavorSet, onCycleSetStatus }) {
      const eqItems = equipmentForSet(set, equipment);
      const desc = enhanceDesc(set, equipment);
      const equiv = onToggleSwap ? getEquivalents(set, poolMode) : {};
      const hasSwap = !!(equiv.shorter || equiv.longer);
      // H: per-set pip only when the set's zone diverges from the block's
      // dominant zone (keeps the table visually quiet most of the time).
      const setZoneId = set.zone || inferSetZone(set, blockSection);
      const showSetPip = blockZoneId && setZoneId && setZoneId !== blockZoneId;
      const setZone = showSetPip ? (ZONES[setZoneId] || null) : null;
      return (
        <React.Fragment>
          <tr style={{ background: idx % 2 === 0 ? "#ffffff" : "#f8fafc" }}>
            <td style={{ padding: "10px 16px", fontSize: 13, fontWeight: 600, color: "var(--color-border)", whiteSpace: "nowrap" }}>
              <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
                {setZone && (
                  <span className="screen-only"
                    title={`This set is ${setZone.label} (block is ${(ZONES[blockZoneId] || {}).label || "n/a"})`}
                    style={{
                      width: 8, height: 8, borderRadius: "50%",
                      background: setZone.color,
                      display: "inline-block", flexShrink: 0,
                    }} />
                )}
                {set.reps > 1 ? `${set.reps} × ${set.dist}` : `${set.dist}`}
                {hasSwap && (
                  <button onClick={() => onToggleSwap(blockIdx, setIdx)}
                    style={{ fontSize: 11, padding: "2px 6px", borderRadius: 4, border: `1px solid ${isSwapOpen ? "var(--color-text-muted)" : "#cbd5e1"}`, background: isSwapOpen ? "var(--color-text)" : "transparent", cursor: "pointer", color: "var(--color-text-dim)", lineHeight: 1 }}>
                    ⇄
                  </button>
                )}
                {/* v1.6 — per-set fav/disfavor cycling button. Shows when
                    the set has a stable ID (otherwise the API call would
                    fail format validation). Click cycles: neutral → favorite
                    → disfavor → neutral. Mutex enforced server-side. */}
                {set.id && onCycleSetStatus && (() => {
                  const state = isFavoriteSet ? "favorite" : isDisfavorSet ? "disfavor" : "neutral";
                  const icon = state === "favorite" ? "★" : state === "disfavor" ? "👎" : "☆";
                  const color = state === "favorite" ? "var(--color-warn)" : state === "disfavor" ? "#ef4444" : "var(--color-text-dim)";
                  const titles = {
                    neutral:  "Neutral — click to mark this set as favorite (next click: disfavor)",
                    favorite: "Favorite (3× weight) — click to mark as disfavor",
                    disfavor: "Disfavor (0.25× weight) — click to clear",
                  };
                  return (
                    <button
                      className="screen-only"
                      onClick={() => onCycleSetStatus(set.id)}
                      title={titles[state]}
                      style={{
                        fontSize: 12, lineHeight: 1, cursor: "pointer",
                        padding: "2px 5px", borderRadius: 4,
                        background: state !== "neutral" ? "rgba(245,158,11,0.10)" : "transparent",
                        border: "none",
                        color,
                        opacity: state === "neutral" ? 0.35 : 1,
                        transition: "opacity 0.15s, color 0.15s, background 0.15s",
                      }}>
                      {icon}
                    </button>
                  );
                })()}
              </div>
            </td>
            <td style={{ padding: "10px 16px", fontSize: 13, fontWeight: 700, color: "var(--color-card)", whiteSpace: "nowrap" }}>
              {(set.reps * set.dist).toLocaleString()} {unit}
            </td>
            <td style={{ padding: "10px 16px", fontSize: 13, color: "var(--color-border)" }}>
              {isEditingDesc ? (
                <input
                  autoFocus
                  value={editDescDraft}
                  onChange={e => setEditDescDraft(e.target.value)}
                  onKeyDown={e => {
                    if (e.key === "Enter")  onCommitDesc(blockIdx, setIdx, editDescDraft);
                    if (e.key === "Escape") onCancelDesc();
                  }}
                  onBlur={() => onCommitDesc(blockIdx, setIdx, editDescDraft)}
                  style={{ width: "100%", fontSize: 13, padding: "2px 6px", borderRadius: 4, border: "1px solid #94a3b8", background: "#fff", color: "var(--color-card)", boxSizing: "border-box" }}
                />
              ) : (
                <span
                  onClick={() => onStartEditDesc && onStartEditDesc(blockIdx, setIdx, set.desc)}
                  title={onStartEditDesc ? "Click to edit" : undefined}
                  style={{ cursor: onStartEditDesc ? "text" : "default", borderBottom: onStartEditDesc ? "1px dashed #94a3b8" : "none", paddingBottom: onStartEditDesc ? 1 : 0 }}>
                  {desc}
                </span>
              )}
              {eqItems.length > 0 && (
                <div className="equipment-badges" style={{ marginTop: 6 }}>
                  {eqItems.map(e => <EquipmentBadge key={e} name={e} />)}
                </div>
              )}
            </td>
            <td style={{ padding: "10px 16px", fontSize: 12, color: "var(--color-text-dim)", fontFamily: "monospace" }}>
              {isEditingInterval ? (() => {
                const hasOn = /^On\s+/i.test(editIntervalDraft);
                const timePart = hasOn ? editIntervalDraft.replace(/^On\s+/i, "") : editIntervalDraft;
                const hasError = !!editIntervalError;
                return (
                  <div>
                    <span style={{ display: "inline-flex", alignItems: "center", gap: 4 }}>
                      {hasOn && <span style={{ color: "var(--color-text-muted)" }}>On</span>}
                      <input
                        autoFocus
                        value={timePart}
                        onChange={e => setEditIntervalDraft(hasOn ? "On " + e.target.value : e.target.value)}
                        onKeyDown={e => {
                          if (e.key === "Enter")  onCommitInterval(blockIdx, setIdx, editIntervalDraft);
                          if (e.key === "Escape") onCancelInterval();
                        }}
                        onBlur={() => onCommitInterval(blockIdx, setIdx, editIntervalDraft)}
                        style={{ width: 52, fontFamily: "monospace", fontSize: 12, padding: "2px 5px", borderRadius: 4,
                                 border: hasError ? "1px solid #dc2626" : "1px solid #94a3b8",
                                 background: "#fff", color: "var(--color-card)" }}
                      />
                      {/* S2 — ⊘ clears to no-interval. onMouseDown preventDefault keeps
                          focus on the input so onBlur doesn't fire and try to commit
                          the (possibly invalid) draft before the clear takes effect. */}
                      {onClearInterval && (
                        <button
                          type="button"
                          title="Clear to no interval"
                          onMouseDown={e => e.preventDefault()}
                          onClick={() => onClearInterval(blockIdx, setIdx)}
                          style={{ fontSize: 14, lineHeight: 1, padding: "0 4px", borderRadius: 4,
                                   border: "1px solid #cbd5e1", background: "#f8fafc", color: "var(--color-text-dim)",
                                   cursor: "pointer" }}
                        >
                          ⊘
                        </button>
                      )}
                    </span>
                    {hasError && (
                      <div style={{ fontSize: 11, color: "#dc2626", marginTop: 3, fontFamily: "inherit", fontStyle: "italic" }}>
                        {editIntervalError}
                      </div>
                    )}
                  </div>
                );
              })() : (
                <span
                  onClick={() => onStartEditInterval && onStartEditInterval(blockIdx, setIdx, set.interval)}
                  title={onStartEditInterval ? "Click to edit" : undefined}
                  style={{ cursor: onStartEditInterval ? "text" : "default", borderBottom: onStartEditInterval ? "1px dashed #94a3b8" : "none", paddingBottom: onStartEditInterval ? 1 : 0 }}>
                  {set.interval}
                </span>
              )}
            </td>
            <td style={{ padding: "10px 16px", fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>{set.focus}</td>
          </tr>
          {isSwapOpen && (
            <tr>
              <td colSpan={5} style={{ padding: "0 16px 12px 16px", background: "#f0f9ff", borderBottom: "2px solid #bae6fd" }}>
                <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", paddingTop: 8 }}>
                  <span style={{ fontSize: 11, fontWeight: 600, color: "var(--color-border-strong)", textTransform: "uppercase", letterSpacing: "0.05em" }}>Swap to:</span>
                  {equiv.shorter && (
                    <button onClick={() => onApplySwap(blockIdx, setIdx, equiv.shorter)}
                      style={{ fontSize: 12, padding: "5px 12px", borderRadius: 6, border: "1px solid #7dd3fc", background: "#e0f2fe", color: "#0369a1", cursor: "pointer", fontWeight: 600 }}>
                      ↙ Shorter — {equiv.shorter.reps} × {equiv.shorter.dist} {unit} &nbsp;<span style={{ fontWeight: 400, color: "#0284c7" }}>{equiv.shorter.interval}</span>
                    </button>
                  )}
                  {equiv.longer && (
                    <button onClick={() => onApplySwap(blockIdx, setIdx, equiv.longer)}
                      style={{ fontSize: 12, padding: "5px 12px", borderRadius: 6, border: "1px solid #86efac", background: "#f0fdf4", color: "#15803d", cursor: "pointer", fontWeight: 600 }}>
                      ↗ Longer — {equiv.longer.reps} × {equiv.longer.dist} {unit} &nbsp;<span style={{ fontWeight: 400, color: "#16a34a" }}>{equiv.longer.interval}</span>
                    </button>
                  )}
                  <button onClick={() => onToggleSwap(blockIdx, setIdx)}
                    style={{ fontSize: 11, padding: "4px 10px", borderRadius: 6, border: "1px solid #cbd5e1", background: "#f1f5f9", color: "var(--color-text-dim)", cursor: "pointer", marginLeft: 4 }}>
                    Cancel
                  </button>
                </div>
              </td>
            </tr>
          )}
        </React.Fragment>
      );
    }

    function EquipmentPicker({ equipment, onChange }) {
      // F: tri-state cycle on each card. Click cycles off → preferred → required → off.
      const cycleNext = (mode) =>
        mode === "off" ? "preferred" :
        mode === "preferred" ? "required" :
        "off";
      return (
        <div style={{ marginBottom: 20 }}>
          <p style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.15em", marginBottom: 8, marginTop: 0 }}>
            Equipment Available
            <span style={{ color: "var(--color-text-dim)", textTransform: "none", letterSpacing: 0, fontWeight: 400, marginLeft: 8 }}>
              (tap to cycle: off → preferred → required)
            </span>
          </p>
          <div className="equipment-grid">
            {EQUIPMENT_LIST.map(eq => {
              const mode = equipMode(equipment, eq.id);
              const isPreferred = mode === "preferred";
              const isRequired  = mode === "required";
              const isOn        = mode !== "off";
              // Styling tiers:
              //  off       — slate border, slate bg, no check
              //  preferred — soft slate-blue border, dim bg, soft check
              //  required  — bright blue border, deep blue bg, ★ badge
              const border =
                isRequired ? "var(--color-primary)" :
                isPreferred ? "var(--color-border-strong)" :
                "var(--color-border)";
              const bg =
                isRequired ? "#1e3a5f" :
                isPreferred ? "#1f293b" :
                "var(--color-card)";
              return (
                <button key={eq.id}
                  type="button"
                  onClick={() => onChange(eq.id, cycleNext(mode))}
                  title={`Currently ${mode}. Tap to set ${cycleNext(mode)}.`}
                  style={{
                    display: "flex", alignItems: "center", gap: 10,
                    padding: "12px 14px", borderRadius: 10,
                    border: `2px solid ${border}`,
                    background: bg,
                    cursor: "pointer", userSelect: "none",
                    transition: "all 0.15s",
                    textAlign: "left", width: "100%",
                  }}>
                  {/* Tri-state check indicator */}
                  <span style={{
                    width: 18, height: 18, borderRadius: 4,
                    border: `2px solid ${isRequired ? "var(--color-primary)" : isPreferred ? "var(--color-text-dim)" : "var(--color-border-strong)"}`,
                    background: isRequired ? "var(--color-primary)" : isPreferred ? "rgba(100,116,139,0.25)" : "transparent",
                    display: "inline-flex", alignItems: "center", justifyContent: "center",
                    flexShrink: 0,
                    color: isRequired ? "#fff" : isPreferred ? "var(--color-text-muted)" : "transparent",
                    fontSize: 13, fontWeight: 900, lineHeight: 1,
                  }}>
                    {isRequired ? "✓" : isPreferred ? "✓" : ""}
                  </span>
                  <span style={{ fontSize: 18, flexShrink: 0 }}>{eq.icon}</span>
                  <div style={{ flex: 1, minWidth: 0 }}>
                    <div style={{ fontSize: 13, fontWeight: 700, color: isOn ? "#fff" : "var(--color-text)", display: "flex", alignItems: "center", gap: 6, minWidth: 0 }}>
                      <span style={{ flex: 1, minWidth: 0, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                        {eq.label}
                      </span>
                      {isRequired && (
                        <span title="Required — every workout must include this"
                          style={{ fontSize: 9, fontWeight: 800, color: "var(--color-warn)", letterSpacing: "0.05em", flexShrink: 0 }}>★ REQ</span>
                      )}
                      {isPreferred && (
                        <span title="Preferred — generator boosts options containing this, but won't reject if absent"
                          style={{ fontSize: 9, fontWeight: 700, color: "var(--color-text-muted)", letterSpacing: "0.05em", flexShrink: 0 }}>PREF</span>
                      )}
                    </div>
                    <div style={{ fontSize: 10, color: isOn ? "#bfdbfe" : "var(--color-text-dim)", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                      {eq.description}
                    </div>
                  </div>
                </button>
              );
            })}
          </div>
        </div>
      );
    }

    function RoundRestRow({ restSecs, onChange }) {
      const [editing, setEditing] = React.useState(false);
      const [draft, setDraft]     = React.useState("");
      function commit() {
        const v = parseInt(draft, 10);
        if (!isNaN(v) && onChange) onChange(v);
        setEditing(false);
      }
      if (editing) {
        return (
          <tr>
            <td colSpan={5} style={{ padding: "4px 16px", background: "var(--color-bg)", borderTop: "1px dashed var(--color-border)", borderBottom: "1px dashed var(--color-border)" }}>
              <span style={{ fontSize: 11, color: "var(--color-text-dim)", marginRight: 8 }}>Rest between rounds:</span>
              <input type="number" autoFocus value={draft} min={0} max={300}
                onChange={e => setDraft(e.target.value)}
                onBlur={commit}
                onKeyDown={e => { if (e.key === "Enter") commit(); if (e.key === "Escape") setEditing(false); }}
                style={{ width: 52, fontSize: 12, fontFamily: "monospace", padding: "2px 6px", borderRadius: 4,
                         border: "1px solid var(--color-primary)", background: "var(--color-bg)", color: "var(--color-primary)", outline: "none" }} />
              <span style={{ fontSize: 11, color: "var(--color-text-dim)", marginLeft: 6 }}>sec</span>
            </td>
          </tr>
        );
      }
      return (
        <tr className="screen-only" onClick={onChange ? () => { setDraft(String(restSecs)); setEditing(true); } : undefined}
          style={{ cursor: onChange ? "pointer" : "default" }}>
          <td colSpan={5} style={{ padding: "7px 16px", background: "var(--color-bg)", borderTop: "1px dashed var(--color-border-strong)", borderBottom: "1px dashed var(--color-border-strong)", textAlign: "center" }}>
            <span style={{ fontSize: 12, color: "var(--color-text-muted)", fontWeight: 600, letterSpacing: "0.03em" }}>
              ⏸ {restSecs}s rest between rounds{onChange ? <span style={{ color: "var(--color-text-dim)", marginLeft: 8, fontSize: 11, fontWeight: 400 }}>tap to edit</span> : null}
            </span>
          </td>
        </tr>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  DRYLAND (Section model Part B) — non-swim blocks: exercises with
    //  sets×reps, no distance/interval/pace. Live in the same workout
    //  blocks[] array with kind:"dryland"; rendered by DrylandBlock (NOT
    //  WorkoutBlock) via a call-site dispatch. Excluded from yardage math
    //  (totalYards:0). Inserted post-generation, never engine-generated.
    // ═══════════════════════════════════════════════════════════════
    // Starter bank — curated dryland blocks a coach can drop in. `reps` is a
    // free string ("12", "30s", "10 each side"). `placement` is the default
    // (pre/post); the coach can override at insert time.
    const DRYLAND_OPTIONS = [
      { id: "dl_activation", name: "Pre-Pool Activation", placement: "pre", exercises: [
        { name: "Arm circles (fwd/back)", sets: 1, reps: "20 each way", rest: null },
        { name: "Leg swings (front/side)", sets: 1, reps: "10 each leg", rest: null },
        { name: "Band pull-aparts", sets: 2, reps: "15", rest: null },
        { name: "Scapular push-ups", sets: 2, reps: "12", rest: null },
        { name: "Bodyweight squats", sets: 1, reps: "15", rest: null },
      ]},
      { id: "dl_core", name: "Core Circuit", placement: "post", exercises: [
        { name: "Front plank", sets: 3, reps: "45s hold", rest: "20s" },
        { name: "Dead bug", sets: 3, reps: "10 each side", rest: "20s" },
        { name: "Hollow-body hold", sets: 3, reps: "30s hold", rest: "20s" },
        { name: "Russian twists", sets: 3, reps: "20", rest: "20s" },
      ]},
      { id: "dl_shoulder", name: "Shoulder Prehab", placement: "pre", exercises: [
        { name: "Band Y-T-W raises", sets: 2, reps: "10 each", rest: "20s" },
        { name: "Band external rotation", sets: 2, reps: "15 each arm", rest: "20s" },
        { name: "Scapular retraction holds", sets: 2, reps: "20s hold", rest: "20s" },
      ]},
      { id: "dl_strength", name: "Dryland Strength", placement: "post", exercises: [
        { name: "Push-ups", sets: 3, reps: "12", rest: "45s" },
        { name: "Walking lunges", sets: 3, reps: "10 each leg", rest: "45s" },
        { name: "Pull-ups (or rows)", sets: 3, reps: "8", rest: "60s" },
        { name: "Glute bridge", sets: 3, reps: "15", rest: "45s" },
      ]},
      { id: "dl_stretch", name: "Post-Pool Stretch", placement: "post", exercises: [
        { name: "Lat stretch (each side)", sets: 1, reps: "30s hold", rest: null },
        { name: "Pec / doorway stretch", sets: 1, reps: "30s hold", rest: null },
        { name: "Cross-body shoulder", sets: 1, reps: "30s each", rest: null },
        { name: "Hip-flexor lunge stretch", sets: 1, reps: "30s each", rest: null },
      ]},
      { id: "dl_lower", name: "Lower-Body Strength", placement: "post", exercises: [
        { name: "Goblet squat", sets: 3, reps: "10", rest: "60s" },
        { name: "Romanian deadlift (DB)", sets: 3, reps: "10", rest: "60s" },
        { name: "Step-ups (each leg)", sets: 3, reps: "10 each", rest: "45s" },
        { name: "Calf raises", sets: 3, reps: "15", rest: "30s" },
      ]},
      { id: "dl_power", name: "Power / Plyometrics", placement: "pre", exercises: [
        { name: "Box jumps (or squat jumps)", sets: 4, reps: "5", rest: "60s" },
        { name: "Broad jumps", sets: 3, reps: "5", rest: "60s" },
        { name: "Medicine-ball slams", sets: 3, reps: "8", rest: "45s" },
        { name: "Streamline jumps", sets: 3, reps: "6", rest: "45s" },
      ]},
      { id: "dl_mobility", name: "Mobility Flow", placement: "pre", exercises: [
        { name: "World's greatest stretch (each)", sets: 1, reps: "5 each side", rest: null },
        { name: "Cat-cow", sets: 1, reps: "10", rest: null },
        { name: "Thoracic rotations (each)", sets: 1, reps: "8 each", rest: null },
        { name: "Hip 90/90 switches", sets: 1, reps: "8 each", rest: null },
        { name: "Ankle rocks", sets: 1, reps: "10 each", rest: null },
      ]},
      { id: "dl_swimmer_prehab", name: "Swimmer Prehab (shoulders + core)", placement: "pre", exercises: [
        { name: "Band pull-aparts", sets: 2, reps: "15", rest: "20s" },
        { name: "Prone Y-T-W (light)", sets: 2, reps: "8 each", rest: "20s" },
        { name: "Serratus wall slides", sets: 2, reps: "12", rest: "20s" },
        { name: "Side plank (each side)", sets: 2, reps: "30s hold", rest: "20s" },
        { name: "Bird dog", sets: 2, reps: "8 each side", rest: "20s" },
      ]},
      { id: "dl_band", name: "Band Pull (swim-specific)", placement: "post", exercises: [
        { name: "Band freestyle pull (each arm)", sets: 3, reps: "15 each", rest: "30s" },
        { name: "Band straight-arm pulldown", sets: 3, reps: "15", rest: "30s" },
        { name: "Band catch + finish hold", sets: 3, reps: "20s hold", rest: "30s" },
      ]},
      { id: "dl_recovery", name: "Recovery / Foam Roll", placement: "post", exercises: [
        { name: "Foam roll lats (each)", sets: 1, reps: "45s each", rest: null },
        { name: "Foam roll quads / IT band", sets: 1, reps: "45s each", rest: null },
        { name: "Foam roll upper back", sets: 1, reps: "45s", rest: null },
        { name: "Child's pose + side reach", sets: 1, reps: "45s", rest: null },
      ]},
      { id: "dl_core_strong", name: "Core — Advanced", placement: "post", exercises: [
        { name: "Hanging knee raises", sets: 3, reps: "12", rest: "30s" },
        { name: "V-ups", sets: 3, reps: "12", rest: "30s" },
        { name: "Plank shoulder taps", sets: 3, reps: "20", rest: "30s" },
        { name: "Flutter kicks", sets: 3, reps: "30s", rest: "30s" },
        { name: "Superman holds", sets: 3, reps: "20s hold", rest: "30s" },
      ]},
    ];

    // Plain-language explainer per dryland exercise — what it is, how to do it,
    // and why it matters for swimmers. Keyed by the EXACT exercise `name` used
    // in DRYLAND_OPTIONS (the iOS DrylandGlossary mirrors these same keys).
    // Shown as a hover tooltip (+ ⓘ marker) on each exercise row.
    const DRYLAND_EXPLAINERS = {
      "Arm circles (fwd/back)": "Big, controlled circles of the arms forward then backward to warm up the shoulder joint and rotator cuff before swimming.",
      "Leg swings (front/side)": "Stand tall and swing one leg front-to-back, then side-to-side; loosens the hips and hamstrings for kicking.",
      "Band pull-aparts": "Hold a resistance band at shoulder height with straight arms and pull it apart, squeezing the shoulder blades together; strengthens the upper back.",
      "Scapular push-ups": "In a straight-arm push-up plank, pinch the shoulder blades together then push them apart — only the blades move. Builds scapular control.",
      "Bodyweight squats": "Feet shoulder-width, sit the hips back and down until the thighs are near parallel, then stand; warms up the legs and hips.",
      "Front plank": "Hold a straight line on forearms and toes, bracing the core — don't let the hips sag or pike. Builds the core stability behind good body position.",
      "Dead bug": "Lie on your back, arms up and knees bent; slowly extend opposite arm and leg while pressing the low back into the floor. Trains anti-extension core control.",
      "Hollow-body hold": "Lie on your back, press the low back down, and lift the shoulders and legs into a shallow banana shape — the streamline core position.",
      "Russian twists": "Sit leaning back with feet up and rotate the torso side to side, tapping the floor each side; works the obliques for stroke rotation.",
      "Band Y-T-W raises": "With a light band, raise the arms into a Y, then T, then W shape, squeezing the shoulder blades each time; rotator-cuff and posture work.",
      "Band external rotation": "Elbow tucked at your side and bent 90°, rotate the forearm outward against a band; strengthens the rotator cuff to protect the shoulder.",
      "Scapular retraction holds": "Pull the shoulder blades down and back and hold; reinforces good posture and a strong catch position.",
      "Push-ups": "Lower the chest to the floor and press back up in a rigid plank; builds the pressing strength used in the pull.",
      "Walking lunges": "Step forward into a lunge, dropping the back knee toward the floor, then step through; builds single-leg strength and balance.",
      "Pull-ups (or rows)": "Hang from a bar and pull the chin over it (or row a band/weight if no bar); the primary back and lat builder, mirroring the swim pull.",
      "Glute bridge": "Lie on your back with knees bent and drive the hips up, squeezing the glutes; strengthens the posterior chain for a stable body line.",
      "Lat stretch (each side)": "Reach one arm overhead and lean away to lengthen the lat down the side of the torso; opens the shoulders for streamline.",
      "Pec / doorway stretch": "Place a forearm on a doorframe and lean through to stretch the chest; counteracts the rounded posture swimming builds.",
      "Cross-body shoulder": "Pull one arm across the chest with the other to stretch the back of the shoulder; relieves shoulder tightness.",
      "Hip-flexor lunge stretch": "In a low lunge, tuck the hips and press forward to stretch the front of the back hip; loosens hip flexors tightened by kicking.",
      "Goblet squat": "Hold a weight at the chest and squat deep, keeping the chest tall; builds leg strength with good posture.",
      "Romanian deadlift (DB)": "Holding dumbbells, hinge at the hips with a flat back, lowering the weights down the legs, then stand; targets hamstrings and glutes.",
      "Step-ups (each leg)": "Step up onto a box with one leg, driving through the heel, then control back down; single-leg power for starts and turns.",
      "Calf raises": "Rise onto the balls of the feet and lower slowly; strengthens the calves and ankles for kicking and push-offs.",
      "Box jumps (or squat jumps)": "Explosively jump onto a box (or straight up); develops the leg power used off the blocks and walls.",
      "Broad jumps": "Jump forward as far as possible from a standing start, landing softly; builds the horizontal explosive power of the start.",
      "Medicine-ball slams": "Lift a medicine ball overhead and slam it down hard, hinging at the hips; full-body power and core drive.",
      "Streamline jumps": "Jump straight up holding a tight streamline with arms locked overhead; links explosive legs to the streamline position off walls.",
      "World's greatest stretch (each)": "From a lunge, drop the elbow inside the front foot, then rotate that same arm to the sky; a full-body mobility opener.",
      "Cat-cow": "On all fours, alternately round and arch the spine; warms up and mobilizes the whole back.",
      "Thoracic rotations (each)": "On all fours or side-lying, rotate the upper back to open the chest; improves the trunk rotation used in freestyle and backstroke.",
      "Hip 90/90 switches": "Sit with both knees bent at 90° to one side, then rotate them to the other side; opens the hips internally and externally.",
      "Ankle rocks": "In a half-kneel, rock the front knee forward over the toes to mobilize the ankle; improves push-off and streamline flexibility.",
      "Prone Y-T-W (light)": "Lie face-down and lift the arms into Y, T, and W shapes off the floor; strengthens the mid-back and rotator cuff.",
      "Serratus wall slides": "Press the forearms on a wall and slide them up while pushing into the wall; activates the serratus for healthy shoulder mechanics.",
      "Side plank (each side)": "Hold a straight line on one forearm and the side of the feet; builds the lateral core stability behind body roll.",
      "Bird dog": "On all fours, extend opposite arm and leg level with the body and hold; trains balanced core stability.",
      "Band freestyle pull (each arm)": "Anchor a band and mimic the freestyle pull stroke against resistance; strengthens the exact catch-and-pull pattern.",
      "Band straight-arm pulldown": "With straight arms, pull a high band down to the thighs; builds the lat engagement that starts the catch.",
      "Band catch + finish hold": "Hold the early-catch position against a band, then the finish position; grooves the strong points of the stroke.",
      "Foam roll lats (each)": "Lie on your side with a foam roller under the lat and roll slowly; releases tightness through the side of the back.",
      "Foam roll quads / IT band": "Roll the front and outside of the thigh on a foam roller to release the quads and IT band after kicking.",
      "Foam roll upper back": "Lie back on a roller under the upper back and roll along the spine; loosens the mid-back for better posture.",
      "Child's pose + side reach": "Kneel and sit back with the arms stretched forward, then walk the hands to each side; a gentle back and lat stretch to finish.",
      "Hanging knee raises": "Hang from a bar and raise the knees toward the chest with control; builds lower-ab strength without straining the back.",
      "V-ups": "Lie flat and simultaneously lift the legs and torso to meet over the hips in a V; strong full-core flexion.",
      "Plank shoulder taps": "In a plank, tap each hand to the opposite shoulder without letting the hips rock; anti-rotation core stability.",
      "Flutter kicks": "Lie on your back, low back pressed down, and make small fast scissoring kicks; mirrors and strengthens the freestyle kick.",
      "Superman holds": "Lie face-down and lift the arms, chest, and legs off the floor and hold; strengthens the lower back and posterior chain.",
    };

    // Make a fresh dryland block (deep-copies exercises so edits don't mutate
    // the bank). `totalYards:0` keeps it out of all yardage math.
    function makeDrylandBlock(option, placement) {
      return {
        kind: "dryland",
        section: "dryland",
        name: option.name,
        placement: placement || option.placement || "pre",
        exercises: (option.exercises || []).map(e => ({ ...e })),
        totalYards: 0,
      };
    }

    // Renderer for a dryland block. Exercise list (sets × reps, optional rest);
    // no pace clock, no swap/interval/favorite affordances. `onRemove` optional.
    // `onChange(updatedBlock)` makes the block editable (✎ toggle → inline
    // exercise editor with a local draft, committed on Done). Passed only at
    // the main workout display; read-only everywhere else.
    function DrylandBlock({ block, onRemove = null, onChange = null }) {
      const ex = Array.isArray(block.exercises) ? block.exercises : [];
      const [editing, setEditing] = useState(false);
      const [draft, setDraft]     = useState(null);
      const startEdit = () => { setDraft({ name: block.name || "", exercises: ex.map(e => ({ ...e })) }); setEditing(true); };
      const commit = () => {
        if (onChange) onChange({
          ...block,
          name: (draft.name || "").trim() || "Dryland",
          exercises: (draft.exercises || []).filter(e => (e.name || "").trim() || String(e.reps ?? "").trim()),
        });
        setEditing(false);
      };
      const updEx = (i, k, v) => setDraft(d => ({ ...d, exercises: d.exercises.map((e, j) => j === i ? { ...e, [k]: v } : e) }));
      const addEx = () => setDraft(d => ({ ...d, exercises: [...d.exercises, { name: "", sets: 1, reps: "10", rest: null }] }));
      const rmEx  = (i) => setDraft(d => ({ ...d, exercises: d.exercises.filter((_, j) => j !== i) }));
      const inp = { padding: "4px 6px", fontSize: 12, borderRadius: 4, border: "1px solid #d8c19a", background: "#fff", color: "#5b4326", fontFamily: "inherit" };
      const headerBtn = { background: "transparent", border: "none", color: "#a3702c", fontSize: 14, cursor: "pointer", lineHeight: 1, fontWeight: 700 };
      return (
        <div style={{ border: "1px solid #a3702c", borderRadius: 10, marginBottom: 14, background: "#fbf6ee", overflow: "hidden" }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between",
            background: "#f0e2cc", color: "#7a4a12", padding: "8px 14px", fontWeight: 700, fontSize: 14, gap: 8 }}>
            <span style={{ display: "flex", alignItems: "center", gap: 8, flex: 1, minWidth: 0 }}>🏋
              {editing
                ? <input value={draft.name} onChange={e => setDraft(d => ({ ...d, name: e.target.value }))} style={{ ...inp, fontWeight: 700, width: 170 }} />
                : <span>{block.name || "Dryland"}</span>}
              <span style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.06em", opacity: 0.7 }}>
                Dryland · {block.placement === "post" ? "after pool" : "before pool"}
              </span>
            </span>
            <span style={{ display: "flex", gap: 10, flexShrink: 0 }}>
              {onChange && (editing
                ? <button onClick={commit} style={headerBtn} title="Done editing">✓ Done</button>
                : <button onClick={startEdit} style={headerBtn} title="Edit exercises">✎</button>)}
              {onRemove && !editing && <button onClick={onRemove} title="Remove this dryland block" style={{ ...headerBtn, fontSize: 16 }}>✕</button>}
            </span>
          </div>
          <div style={{ padding: "8px 14px" }}>
            {editing ? (
              <>
                {draft.exercises.map((e, i) => (
                  <div key={i} style={{ display: "flex", gap: 6, alignItems: "center", padding: "4px 0", flexWrap: "wrap" }}>
                    <input type="number" min="1" value={e.sets} onChange={ev => updEx(i, "sets", Number(ev.target.value) || 1)} title="sets" style={{ ...inp, width: 44 }} />
                    <span style={{ color: "#9a7b4f" }}>×</span>
                    <input value={e.reps} onChange={ev => updEx(i, "reps", ev.target.value)} placeholder="reps / hold" title="reps or hold" style={{ ...inp, width: 92 }} />
                    <input value={e.name} onChange={ev => updEx(i, "name", ev.target.value)} placeholder="exercise" style={{ ...inp, flex: 1, minWidth: 120 }} />
                    <input value={e.rest || ""} onChange={ev => updEx(i, "rest", ev.target.value || null)} placeholder="rest" title="rest (optional)" style={{ ...inp, width: 64 }} />
                    <button onClick={() => rmEx(i)} title="Remove exercise" style={{ background: "transparent", border: "none", color: "#a3702c", cursor: "pointer", fontSize: 14 }}>✕</button>
                  </div>
                ))}
                <button onClick={addEx} style={{ marginTop: 6, padding: "4px 10px", border: "1px dashed #a3702c", borderRadius: 6, background: "transparent", color: "#a3702c", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>+ Add exercise</button>
              </>
            ) : (
              ex.length === 0 ? (
                <div style={{ color: "#9a7b4f", fontSize: 12, fontStyle: "italic" }}>No exercises.</div>
              ) : ex.map((e, i) => (
                <div key={i} style={{ display: "flex", alignItems: "baseline", gap: 10, padding: "4px 0", borderTop: i > 0 ? "1px solid #ecddc3" : "none" }}>
                  <span style={{ minWidth: 70, fontWeight: 700, color: "#7a4a12", fontSize: 13, fontFamily: "ui-monospace, monospace" }}>
                    {e.sets > 1 ? `${e.sets} × ${e.reps}` : `${e.reps}`}
                  </span>
                  <span style={{ flex: 1, fontSize: 13 }}>
                    <span title={DRYLAND_EXPLAINERS[e.name] || undefined}
                      style={{ color: "#5b4326", ...(DRYLAND_EXPLAINERS[e.name] ? { borderBottom: "1px dotted #c9a96a", cursor: "help" } : {}) }}>
                      {e.name}
                    </span>
                    {DRYLAND_EXPLAINERS[e.name] ? (
                      <span title={DRYLAND_EXPLAINERS[e.name]} style={{ color: "#a3702c", fontSize: 11, marginLeft: 4, cursor: "help" }}>ⓘ</span>
                    ) : null}
                  </span>
                  {e.rest && <span style={{ color: "#9a7b4f", fontSize: 11 }}>rest {e.rest}</span>}
                </div>
              ))
            )}
          </div>
        </div>
      );
    }

    function WorkoutBlock({ block, equipment, onRegenerate, regenError, blockIdx, openSwapKey, onToggleSwap, onApplySwap, editIntervalKey, editIntervalDraft, setEditIntervalDraft, editIntervalError, onStartEditInterval, onCommitInterval, onClearInterval, onCancelInterval, editDescKey, editDescDraft, setEditDescDraft, onStartEditDesc, onCommitDesc, onCancelDesc, isFavorited, onToggleFavorite, isDisfavorited, onToggleDisfavorite, isEngineDisfavorited, onToggleEngineDisfavor, isEngineFavorited, onToggleEngineFavorite, onRoundRestChange, unit = "yds", isPinned, onTogglePin, recentMainLabels, poolMode = "25y", sectionSource = "bank", onSectionSourceChange, favoriteSets, disfavorSets, onCycleSetStatus, isCoach = false, onSnapshot = null, ugcSetIdMeta = null }) {
      // UGC Phase C/D — derive whether this block was sourced from a UGC
      // overlay option, and which variant of the badge to show.
      // Look up the first matching set's metadata (all sets in an overlay
      // option share the same _is_own/_visibility so any hit answers it).
      let ugcMeta = null;
      if (ugcSetIdMeta && block && Array.isArray(block.sets)) {
        for (const s of block.sets) {
          if (s && s.id && ugcSetIdMeta.has(s.id)) {
            ugcMeta = ugcSetIdMeta.get(s.id);
            break;
          }
        }
      }
      const isUgcBlock = !!ugcMeta;
      const s = SECTION_STYLES[block.section];
      // Light header text (e.g. main set on dark slate) → use white-tinted button.
      // Dark header text → use dark-tinted button.
      const isDarkHeader = s.headerText === "#ffffff";
      const btnBg     = isDarkHeader ? "rgba(255,255,255,0.12)" : "rgba(15,23,42,0.06)";
      const btnHoverBg = isDarkHeader ? "rgba(255,255,255,0.20)" : "rgba(15,23,42,0.12)";
      const btnBorder = isDarkHeader ? "rgba(255,255,255,0.25)" : "rgba(15,23,42,0.15)";
      const [hovered, setHovered] = useState(false);
      // H: derive the block's effort-zone for the header pip; per-set pips
      // are shown only when a set diverges from this dominant zone.
      const blockZoneId = useMemo(() => inferBlockZone(block), [block]);
      const blockZone   = ZONES[blockZoneId] || ZONES.aerobic;
      return (
        <div className="workout-block" style={{ borderRadius: 12, border: isPinned ? `2px solid ${s.dot}` : `1px solid ${s.border}`, overflow: "hidden", marginBottom: 16 }}>
          <div className="workout-block-header" style={{ background: s.headerBg, color: s.headerText, padding: "12px 20px", display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
              <span style={{ width: 10, height: 10, borderRadius: "50%", background: s.dot, display: "inline-block" }} />
              <span style={{ fontWeight: 700, fontSize: 12, textTransform: "uppercase", letterSpacing: "0.1em" }}>{block.name}</span>
              {block.__engineMeta && (
                <span
                  title={`Generated by the SetForge engine (template: ${block.__engineMeta.template_id})`}
                  style={{
                    display: "inline-flex", alignItems: "center",
                    padding: "1px 7px", borderRadius: 999,
                    background: "rgba(245,158,11,0.18)",
                    border: "1px solid var(--color-warn)",
                    color: isDarkHeader ? "var(--color-warn)" : "#92400e",
                    fontSize: 10, fontWeight: 700, letterSpacing: "0.04em",
                  }}>
                  ⚡ ENGINE
                </span>
              )}
              {/* UGC Phase C/D — badge when block came from the UGC overlay.
                  Three variants based on ugcMeta (set in App-level walker):
                    📝 = your own (any visibility)
                    👥 = team-shared from another coach (Phase D)
                    🌐 = admin-approved public (Phase E — wired but quiet
                         until visibility='public' rows exist) */}
              {isUgcBlock && (() => {
                let icon, label, title;
                if (ugcMeta._is_own) {
                  icon = "📝"; label = "UGC"; title = "Your own UGC bank option (📝 My Sets)";
                } else if (ugcMeta._visibility === "team") {
                  icon = "👥"; label = "TEAM";  title = "Team-shared UGC from another coach on your team";
                } else if (ugcMeta._visibility === "public") {
                  icon = "🌐"; label = "PUBLIC"; title = "Public UGC option (admin-approved)";
                } else {
                  icon = "📝"; label = "UGC"; title = "UGC bank option";
                }
                return (
                  <span title={title}
                    style={{
                      display: "inline-flex", alignItems: "center",
                      padding: "1px 7px", borderRadius: 999,
                      background: "rgba(59,130,246,0.18)",
                      border: "1px solid var(--color-primary)",
                      color: isDarkHeader ? "var(--color-primary)" : "#1e40af",
                      fontSize: 10, fontWeight: 700, letterSpacing: "0.04em",
                    }}>
                    {icon} {label}
                  </span>
                );
              })()}
              <span className="screen-only"
                title={`Effort zone: ${blockZone.label}`}
                style={{
                  display: "inline-flex", alignItems: "center", gap: 4,
                  padding: "1px 7px", borderRadius: 999,
                  background: `${blockZone.color}22`,
                  border: `1px solid ${blockZone.color}`,
                  color: isDarkHeader ? "#fff" : blockZone.color,
                  fontSize: 10, fontWeight: 700, letterSpacing: "0.04em",
                  textTransform: "uppercase",
                }}>
                <span style={{ width: 6, height: 6, borderRadius: "50%", background: blockZone.color, display: "inline-block" }} />
                {blockZone.label}
              </span>
              {isPinned && <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: "0.06em", opacity: 0.8 }}>PINNED</span>}
              {(() => {
                if (!recentMainLabels) return null;
                const lbl = extractMainLabel(block);
                if (!lbl) return null;
                const days = recentMainLabels.get(lbl);
                if (days === undefined) return null;
                return (
                  <span className="screen-only"
                    title={`You completed this main set ${days === 0 ? "today" : days === 1 ? "yesterday" : days + " days ago"}`}
                    style={{
                      fontSize: 10, fontWeight: 700, letterSpacing: "0.02em",
                      color: "#fca5a5", background: "rgba(127,29,29,0.45)",
                      border: "1px solid #7f1d1d",
                      padding: "1px 6px", borderRadius: 999,
                    }}>
                    last seen {days === 0 ? "today" : `${days}d ago`}
                  </span>
                );
              })()}
            </div>
            <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
              {/* S3 — per-section Bank | Engine | Mix toggle. Triggers a
                  regen of this section under the chosen source. Disabled
                  when section is pinned (pin wins). */}
              {onSectionSourceChange && !isPinned && (
                <div className="screen-only" style={{ display: "inline-flex", gap: 2, borderRadius: 6, background: btnBg, padding: 2, border: `1px solid ${btnBorder}` }}>
                  {["bank", "engine", "mix"].map(opt => {
                    const active = sectionSource === opt;
                    return (
                      <button
                        key={opt}
                        type="button"
                        onClick={() => onSectionSourceChange(opt)}
                        title={`Set this section's source to ${opt === "bank" ? "the bank (curated sets)" : opt === "engine" ? "the engine (template-generated)" : "mix (bank prefix + engine suffix)"}`}
                        style={{
                          fontSize: 10, fontWeight: 700, letterSpacing: "0.04em",
                          padding: "2px 8px", borderRadius: 4,
                          background: active ? (isDarkHeader ? "rgba(255,255,255,0.22)" : "rgba(15,23,42,0.18)") : "transparent",
                          color: s.headerText, opacity: active ? 1 : 0.6,
                          border: "none", cursor: "pointer", textTransform: "uppercase",
                        }}>
                        {opt}
                      </button>
                    );
                  })}
                </div>
              )}
              <span style={{ fontWeight: 600, fontSize: 13 }}>{block.totalYards.toLocaleString()} {unit}</span>
              {/* v1.3 — Tri-state label-status toggle. Behavior branches on
                  whether the block is bank-sourced or engine-sourced:
                  • Bank: [★ / — / 👎] targets `block.label` in fav/disfav tables
                  • Engine: [★ disabled / — / 👎] targets `block.__engineMeta`
                    (template_id, stroke) in engine_disfavorites — engine
                    doesn't support favorites (per v1.3 scope choice), and
                    favoriting a dynamic engine label wouldn't survive the
                    next generate anyway.
                  Mutex is server-side for bank, single-tag (no opposite) for
                  engine. */}
              {block.section === "main" && (block.label || block.__engineMeta) && (onToggleFavorite || onToggleDisfavorite || onToggleEngineDisfavor || onToggleEngineFavorite) && (() => {
                const isEngineBlock = !!block.__engineMeta;
                const segments = isEngineBlock
                  ? [
                      // v1.13 — ★ now active for engine blocks too. Targets
                      // (template_id, stroke) in engine_favorites (settings
                      // .extra) with 3× pick weight. Universal precedence:
                      // favorite wins over disfavor on the same tuple.
                      { id: "favorite", icon: "★",  active: !!isEngineFavorited, disabled: false,
                        title: `Favorite this template — pick "${block.__engineMeta.template_id}" + ${block.__engineMeta.stroke} more often (3× weight)`,
                        activeColor: "var(--color-warn)" },
                      { id: "neutral",  icon: "—",  active: !isEngineDisfavorited && !isEngineFavorited,
                        title: "Neutral — default engine template weight",
                        activeColor: s.headerText },
                      { id: "disfavor", icon: "👎", active: !!isEngineDisfavorited,
                        title: `Disfavor this template — pick "${block.__engineMeta.template_id}" + ${block.__engineMeta.stroke} less often (0.25× weight)`,
                        activeColor: "#ef4444" },
                    ]
                  : [
                      { id: "favorite", icon: "★",  active: isFavorited, disabled: false,
                        title: "Favorite — pick this more often (3× weight)",
                        activeColor: "var(--color-warn)" },
                      { id: "neutral",  icon: "—",  active: !isFavorited && !isDisfavorited, disabled: false,
                        title: "Neutral — default pick weight",
                        activeColor: s.headerText },
                      { id: "disfavor", icon: "👎", active: isDisfavorited, disabled: false,
                        title: "Disfavor — pick this less often (0.25× weight)",
                        activeColor: "#ef4444" },
                    ];
                return (
                  <div className="screen-only" data-tour="step-favorite-toggle" style={{
                    display: "inline-flex", gap: 2, borderRadius: 6,
                    background: btnBg, padding: 2, border: `1px solid ${btnBorder}`,
                  }}>
                    {segments.map(opt => (
                      <button
                        key={opt.id}
                        type="button"
                        disabled={opt.disabled}
                        onClick={() => {
                          if (opt.disabled) return;
                          if (isEngineBlock) {
                            // v1.13 — Engine: ★ targets engine_favorites,
                            // 👎 targets engine_disfavorites, — clears
                            // whichever was active. Mutex handled in the
                            // App-level toggle handlers (favorite-add
                            // strips disfavor and vice versa).
                            if (opt.id === "favorite" && !isEngineFavorited && onToggleEngineFavorite) {
                              onToggleEngineFavorite(block.__engineMeta);
                            } else if (opt.id === "disfavor" && !isEngineDisfavorited && onToggleEngineDisfavor) {
                              onToggleEngineDisfavor(block.__engineMeta);
                            } else if (opt.id === "neutral") {
                              if (isEngineFavorited && onToggleEngineFavorite) onToggleEngineFavorite(block.__engineMeta);
                              else if (isEngineDisfavorited && onToggleEngineDisfavor) onToggleEngineDisfavor(block.__engineMeta);
                            }
                          } else {
                            // Bank: target block.label in favorites/disfavorites
                            if (opt.id === "favorite" && !isFavorited && onToggleFavorite) {
                              onToggleFavorite(block.label);
                            } else if (opt.id === "disfavor" && !isDisfavorited && onToggleDisfavorite) {
                              onToggleDisfavorite(block.label);
                            } else if (opt.id === "neutral") {
                              if (isFavorited && onToggleFavorite) onToggleFavorite(block.label);
                              else if (isDisfavorited && onToggleDisfavorite) onToggleDisfavorite(block.label);
                            }
                          }
                        }}
                        title={opt.title}
                        style={{
                          fontSize: 14, lineHeight: 1,
                          cursor: opt.disabled ? "not-allowed" : "pointer",
                          padding: "3px 7px", borderRadius: 4,
                          background: opt.active
                            ? (isDarkHeader ? "rgba(255,255,255,0.22)" : "rgba(15,23,42,0.18)")
                            : "transparent",
                          color: opt.active ? opt.activeColor : s.headerText,
                          opacity: opt.disabled ? 0.25 : (opt.active ? 1 : 0.55),
                          border: "none",
                          transition: "background 0.15s, opacity 0.15s",
                        }}>
                        {opt.icon}
                      </button>
                    ))}
                  </div>
                );
              })()}
              {/* UGC Phase C — coach-only 📥 snapshot. Pre-fills the
                  UGC form with this block's section/label/sets so the
                  coach can save it to their own bank. */}
              {isCoach && onSnapshot && block.sets && block.sets.length > 0 && (
                <button
                  type="button"
                  className="screen-only"
                  onClick={() => onSnapshot(block)}
                  title="Save this set to your bank (📝 My Sets)"
                  style={{
                    background: btnBg, border: `1px solid ${btnBorder}`,
                    color: s.headerText, fontSize: 14, lineHeight: 1,
                    cursor: "pointer", padding: "4px 7px", borderRadius: 6,
                  }}>
                  📥
                </button>
              )}
              {onTogglePin && (
                <button
                  type="button"
                  className="screen-only"
                  onClick={() => onTogglePin(block.section)}
                  title={isPinned ? "Unpin — regenerate this section next time" : "Pin — keep this section on next generate"}
                  style={{
                    background: isPinned ? "rgba(255,255,255,0.20)" : btnBg,
                    border: `1px solid ${isPinned ? "rgba(255,255,255,0.45)" : btnBorder}`,
                    color: s.headerText, fontSize: 14, lineHeight: 1,
                    cursor: "pointer", padding: "4px 7px", borderRadius: 6,
                    transition: "background 0.15s",
                  }}>
                  {isPinned ? "📌" : "📍"}
                </button>
              )}
              {onRegenerate && !isPinned && (
                <button
                  type="button"
                  className="screen-only"
                  onClick={() => onRegenerate(block.section)}
                  onMouseEnter={() => setHovered(true)}
                  onMouseLeave={() => setHovered(false)}
                  title={`Regenerate ${block.name} only`}
                  aria-label={`Regenerate ${block.name}`}
                  style={{
                    display: "inline-flex", alignItems: "center", gap: 4,
                    padding: "4px 9px", borderRadius: 6,
                    background: hovered ? btnHoverBg : btnBg,
                    color: s.headerText, border: `1px solid ${btnBorder}`,
                    fontSize: 12, fontWeight: 600, cursor: "pointer",
                    lineHeight: 1, transition: "background 0.15s",
                  }}>
                  <span style={{ fontSize: 13 }}>🔄</span>
                  <span>Regenerate</span>
                </button>
              )}
            </div>
          </div>
          {regenError && (
            <div className="screen-only" role="alert" style={{
              background: "#fef2f2", borderBottom: `1px solid ${s.border}`,
              color: "#991b1b", fontSize: 12, padding: "8px 20px",
              display: "flex", alignItems: "center", gap: 8,
            }}>
              <span>⚠️</span>
              <span>{regenError}</span>
            </div>
          )}
          <div style={{ background: s.bg, overflowX: "auto" }}>
            <table style={{ width: "100%", borderCollapse: "collapse" }}>
              <thead>
                <tr style={{ borderBottom: "1px solid #e2e8f0" }}>
                  {["Reps × Dist", "Total", "Description", "Interval", "Focus"].map(h => (
                    <th key={h} style={{ padding: "8px 16px", textAlign: "left", fontSize: 11, fontWeight: 600, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.05em" }}>{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {(() => {
                  const rounds = block.rounds || 1;
                  const rows = [];
                  // v1.1 — Mix mode renders sub-blocks with header dividers
                  // (per spec §7.3 "two labeled sub-blocks within one section
                  // header"). When block.subBlocks is present, each
                  // sub-block gets its own labeled divider row; sub-block
                  // sourced from engine carries __engineMeta and gets a ⚡
                  // badge on its divider.
                  const subBlocks = Array.isArray(block.subBlocks) && block.subBlocks.length
                    ? block.subBlocks
                    : [{ label: null, sets: block.sets, totalYards: block.totalYards }];
                  for (let r = 0; r < rounds; r++) {
                    if (rounds > 1) {
                      rows.push(
                        <tr key={`round-${r}`}>
                          <td colSpan={5} style={{ padding: "6px 16px", background: "var(--color-card)", borderTop: r > 0 ? "1px solid var(--color-border)" : undefined }}>
                            <span style={{ fontSize: 11, fontWeight: 700, color: "var(--color-primary)", textTransform: "uppercase", letterSpacing: "0.08em" }}>
                              Round {r + 1}
                            </span>
                          </td>
                        </tr>
                      );
                    }
                    subBlocks.forEach((sb, sbIdx) => {
                      // Render a sub-block divider row when there's >1 sub-block
                      if (subBlocks.length > 1 && sb.label) {
                        const isEngineSub = !!sb.__engineMeta;
                        rows.push(
                          <tr key={`r${r}-sb${sbIdx}`}>
                            <td colSpan={5} style={{ padding: "8px 16px", background: "#f1f5f9", borderTop: sbIdx > 0 ? "1px solid #cbd5e1" : undefined }}>
                              <span style={{ fontSize: 11, fontWeight: 700, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>
                                {sb.label} ({sb.totalYards} {unit})
                              </span>
                              {isEngineSub && (
                                <span title={`Generated by the SetForge engine (template: ${sb.__engineMeta.template_id})`}
                                  style={{
                                    marginLeft: 8, padding: "1px 6px", borderRadius: 999,
                                    background: "rgba(245,158,11,0.18)",
                                    border: "1px solid var(--color-warn)",
                                    color: "#92400e",
                                    fontSize: 10, fontWeight: 700, letterSpacing: "0.04em",
                                  }}>
                                  ⚡ ENGINE
                                </span>
                              )}
                            </td>
                          </tr>
                        );
                      }
                      let cumulativeSetIdx = 0;
                      // Setidx for the SetRow keys: count sets across prior sub-blocks
                      // so editing intervals/descs (which use a "blockIdx-setIdx" key)
                      // still target the right set in block.sets (the flat-concat list).
                      for (let priorSb = 0; priorSb < sbIdx; priorSb++) {
                        cumulativeSetIdx += subBlocks[priorSb].sets.length;
                      }
                      sb.sets.forEach((set, i) => {
                        const flatIdx = cumulativeSetIdx + i;
                        rows.push(
                          <SetRow key={`${r}-sb${sbIdx}-${i}`} set={set} idx={flatIdx} equipment={equipment}
                            blockIdx={blockIdx} setIdx={flatIdx}
                            isSwapOpen={openSwapKey === `${blockIdx}-${flatIdx}`}
                            onToggleSwap={onToggleSwap} onApplySwap={onApplySwap}
                            isEditingInterval={editIntervalKey === `${blockIdx}-${flatIdx}`}
                            editIntervalDraft={editIntervalDraft}
                            setEditIntervalDraft={setEditIntervalDraft}
                            editIntervalError={editIntervalKey === `${blockIdx}-${flatIdx}` ? editIntervalError : null}
                            onStartEditInterval={onStartEditInterval}
                            onCommitInterval={onCommitInterval}
                            onClearInterval={onClearInterval}
                            onCancelInterval={onCancelInterval}
                            poolMode={poolMode}
                            isEditingDesc={editDescKey === `${blockIdx}-${flatIdx}`}
                            editDescDraft={editDescDraft}
                            setEditDescDraft={setEditDescDraft}
                            onStartEditDesc={onStartEditDesc}
                            onCommitDesc={onCommitDesc}
                            onCancelDesc={onCancelDesc}
                            unit={unit}
                            blockSection={block.section}
                            blockZoneId={blockZoneId}
                            isFavoriteSet={!!(set.id && favoriteSets && favoriteSets.has(set.id))}
                            isDisfavorSet={!!(set.id && disfavorSets && disfavorSets.has(set.id))}
                            onCycleSetStatus={onCycleSetStatus}
                          />
                        );
                      });
                    });
                    // Rest row between rounds (not after last round)
                    if (rounds > 1 && r < rounds - 1) {
                      rows.push(
                        <RoundRestRow key={`rest-${r}`}
                          restSecs={block.roundRestSecs ?? 30}
                          onChange={onRoundRestChange ? (v) => onRoundRestChange(blockIdx, v) : null}
                        />
                      );
                    }
                  }
                  return rows;
                })()}
              </tbody>
            </table>
          </div>
        </div>
      );
    }

    // ─── Max Yardage Slider ──────────────────────────────────────────
    // Compute the minimum possible total yards for a given type
    function minYardsForType(typeId, poolMode = "25y") {
      const allWarmups   = getBankOptions("warmup",   typeId, poolMode);
      const allCooldowns = getBankOptions("cooldown", typeId, poolMode);
      const drillList    = getBankOptions("drill",    typeId, poolMode);
      const mainList     = getBankOptions("main",     typeId, poolMode);
      const minWarmup   = Math.min(...allWarmups.map(o => o.totalYards));
      const minDrill    = Math.min(...drillList.map(o => o.totalYards));
      const minMain     = Math.min(...mainList.map(o => o.totalYards));
      const minCooldown = Math.min(...allCooldowns.map(o => o.totalYards));
      return minWarmup + minDrill + minMain + minCooldown;
    }

    function YardageSlider({ value, onChange, selectedType, poolMode = "25y", paceInput = "2:00", onPaceChange, sliderMin: sliderMinProp, sliderMax: sliderMaxProp, onRangeChange }) {
      const [activePreset, setActivePreset] = React.useState(null);
      const [editingBound, setEditingBound] = React.useState(null); // 'min' | 'max' | null
      const [boundDraft,   setBoundDraft]   = React.useState("");
      const is25m    = poolMode === "25m";
      const is50m    = poolMode === "50m";
      const isMeters = is25m || is50m;
      const ABS_MIN     = isMeters ? 2000 : 1900;
      const ABS_MAX     = 6000;
      const SLIDER_STEP = is50m ? 100 : 50;     // SCM uses 50m step like SCY (more granular than LCM)
      const SLIDER_MIN  = Math.max(ABS_MIN, sliderMinProp ?? ABS_MIN);
      const SLIDER_MAX  = Math.min(ABS_MAX, sliderMaxProp ?? 5000);
      const unit = isMeters ? "m" : "yds";
      const pct = ((value - SLIDER_MIN) / (SLIDER_MAX - SLIDER_MIN) * 100).toFixed(1);
      // 5 evenly-spaced ticks between SLIDER_MIN and SLIDER_MAX
      const ticks = [0,1,2,3,4].map(i => Math.round((SLIDER_MIN + i * (SLIDER_MAX - SLIDER_MIN) / 4) / SLIDER_STEP) * SLIDER_STEP);
      const tickLabel = v => v >= 1000 ? `${(v / 1000).toFixed(1)}k` : String(v);

      function commitBound(which, raw) {
        const parsed = parseInt(raw, 10);
        if (!isNaN(parsed)) {
          const snap = Math.round(parsed / SLIDER_STEP) * SLIDER_STEP;
          let newMin = SLIDER_MIN, newMax = SLIDER_MAX;
          if (which === "min") newMin = Math.max(ABS_MIN, Math.min(snap, SLIDER_MAX - SLIDER_STEP));
          if (which === "max") newMax = Math.min(ABS_MAX, Math.max(snap, newMin + SLIDER_STEP));
          if (newMin !== SLIDER_MIN || newMax !== SLIDER_MAX) {
            onRangeChange && onRangeChange(newMin, newMax);
          }
        }
        setEditingBound(null);
        setBoundDraft("");
      }

      const typeMin = selectedType ? minYardsForType(selectedType, poolMode) : null;
      const tooLow  = typeMin !== null && value < typeMin;

      return (
        <div style={{ background: "var(--color-card)", border: `1px solid ${tooLow ? "#7f1d1d" : "var(--color-border)"}`, borderRadius: 12, padding: "18px 20px", marginBottom: 20 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 }}>
            <div>
              <span style={{ color: "var(--color-text-muted)", fontSize: 11, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.1em" }}>Max {isMeters ? "Distance" : "Yardage"}</span>
              <span style={{ color: "var(--color-text-dim)", fontSize: 11, marginLeft: 8 }}>(min locked at {SLIDER_MIN.toLocaleString()} {unit})</span>
            </div>
            <div style={{ display: "flex", alignItems: "baseline", gap: 4 }}>
              <span style={{ color: "#ffffff", fontSize: 26, fontWeight: 900 }}>{value.toLocaleString()}</span>
              <span style={{ color: "var(--color-text-muted)", fontSize: 12 }}>{unit}</span>
            </div>
          </div>

          {/* Pace input + time presets */}
          {(() => {
            function parsePaceSecs(p) {
              const parts = (p || "").trim().split(":");
              if (parts.length !== 2) return null;
              const m = parseInt(parts[0], 10), s = parseInt(parts[1], 10);
              if (isNaN(m) || isNaN(s) || m * 60 + s < 30) return null;
              return m * 60 + s;
            }
            function yardsForMins(mins) {
              const ps = parsePaceSecs(paceInput);
              if (!ps) return null;
              const y = Math.round(2400 * (mins / 60) * (120 / ps) / 50) * 50;
              return Math.min(5000, Math.max(isMeters ? 2000 : 1900, y));
            }
            const presets = [{ label: "~60 min", mins: 60 }, { label: "~90 min", mins: 90 }, { label: "~2 hr", mins: 120 }];
            return (
              <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 14, flexWrap: "wrap" }}>
                <span style={{ color: "var(--color-text-dim)", fontSize: 11 }}>Pace / 100 {unit}:</span>
                <input
                  type="text"
                  value={paceInput}
                  data-tour="step-pace-input"
                  onChange={e => { setActivePreset(null); onPaceChange && onPaceChange(e.target.value); }}
                  placeholder="M:SS"
                  style={{ width: 52, fontFamily: "monospace", fontSize: 13, padding: "4px 7px", borderRadius: 6,
                           border: "1px solid var(--color-border-strong)", background: "var(--color-bg)", color: "var(--color-text)", outline: "none" }}
                />
                {presets.map(({ label, mins }) => {
                  const y = yardsForMins(mins);
                  const active = activePreset === label;
                  return (
                    <button key={label} onClick={() => { if (y) { setActivePreset(label); onChange(y); } }} disabled={!y}
                      style={{ fontSize: 12, fontWeight: 600, padding: "4px 12px", borderRadius: 6,
                               border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                               background: active ? "#1d4ed8" : "var(--color-bg)",
                               color: active ? "#fff" : "var(--color-text-muted)", cursor: y ? "pointer" : "not-allowed" }}>
                      {label}
                    </button>
                  );
                })}
              </div>
            );
          })()}

          {/* Visual track + invisible native input on top */}
          <div style={{ position: "relative", marginBottom: 4 }}>
            <div style={{ height: 6, borderRadius: 3, background: "var(--color-border)", position: "relative" }}>
              <div style={{ position: "absolute", left: 0, top: 0, height: "100%", width: `${pct}%`,
                            background: tooLow ? "linear-gradient(90deg,#ef4444,#f87171)" : "linear-gradient(90deg,var(--color-primary),var(--color-primary))",
                            borderRadius: 3 }} />
            </div>
            <input type="range" min={SLIDER_MIN} max={SLIDER_MAX} step={SLIDER_STEP} value={value}
              onChange={e => { setActivePreset(null); onChange(Number(e.target.value)); }}
              style={{ position: "absolute", top: -4, left: 0, width: "100%", opacity: 0, height: 14, cursor: "pointer", margin: 0 }} />
          </div>

          {/* Tick labels — first and last are editable bounds */}
          <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
            {ticks.map((v, i) => {
              const isMin = i === 0, isMax = i === ticks.length - 1;
              const boundKey = isMin ? "min" : isMax ? "max" : null;
              if (boundKey && editingBound === boundKey) {
                return (
                  <input key={v} type="number" value={boundDraft} autoFocus
                    onChange={e => setBoundDraft(e.target.value)}
                    onBlur={() => commitBound(boundKey, boundDraft)}
                    onKeyDown={e => { if (e.key === "Enter") commitBound(boundKey, boundDraft); if (e.key === "Escape") { setEditingBound(null); setBoundDraft(""); } }}
                    style={{ width: 52, fontSize: 10, fontFamily: "monospace", padding: "1px 4px", borderRadius: 4,
                             border: "1px solid var(--color-primary)", background: "var(--color-bg)", color: "var(--color-primary)", outline: "none" }} />
                );
              }
              return (
                <span key={v}
                  onClick={() => boundKey ? (setEditingBound(boundKey), setBoundDraft(String(v))) : onChange(v)}
                  title={boundKey ? `Click to set ${boundKey} yardage` : undefined}
                  style={{ fontSize: 10, cursor: "pointer",
                            color: v === value ? "var(--color-primary)" : v < (typeMin||0) ? "#7f1d1d" : "var(--color-border-strong)",
                            fontWeight: v === value ? 700 : 400,
                            textDecoration: boundKey ? "underline dotted" : undefined }}>
                  {tickLabel(v)}{boundKey ? " ✎" : ""}
                </span>
              );
            })}
          </div>

          {/* Status row */}
          {tooLow ? (
            <div style={{ background: "#450a0a", border: "1px solid #7f1d1d", borderRadius: 8, padding: "10px 14px", display: "flex", alignItems: "center", gap: 8 }}>
              <span style={{ fontSize: 16 }}>⚠️</span>
              <div>
                <span style={{ color: "#fca5a5", fontSize: 12, fontWeight: 600 }}>
                  This workout type needs at least {typeMin.toLocaleString()} {unit}.
                </span>
                <span style={{ color: "var(--color-destructive)", fontSize: 12 }}> Raise the max or choose a different type.</span>
              </div>
            </div>
          ) : typeMin !== null ? (
            <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
              <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>Workout range:</span>
              <span style={{ fontSize: 11, color: "#34d399", background: "#064e3b", borderRadius: 6, padding: "2px 8px", fontWeight: 600 }}>
                {typeMin.toLocaleString()} – {value.toLocaleString()} {unit} ✓
              </span>
            </div>
          ) : (
            <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
              <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>Select a workout type to see its range.</span>
            </div>
          )}
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  SAVE-TO-HISTORY FORM — appears below a freshly generated workout
    // ═══════════════════════════════════════════════════════════════
    function SaveToHistoryForm({ dateDraft, setDateDraft, initialsDraft, setInitialsDraft, noteDraft, setNoteDraft, difficultyDraft, setDifficultyDraft, saveStatus, saveError, onSave }) {
      const isSaved  = saveStatus === "saved";
      const isSaving = saveStatus === "saving";
      return (
        <div className="screen-only" style={{ marginTop: 20, background: "var(--color-card)", borderRadius: 12, padding: 18, border: "1px solid var(--color-border)" }}>
          <div style={{ display: "flex", alignItems: "baseline", gap: 8, marginBottom: 12 }}>
            <span style={{ fontSize: 14, fontWeight: 700, color: "var(--color-text)" }}>📒 Log this workout</span>
            <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>(saves to your history)</span>
          </div>
          <div style={{ display: "grid", gridTemplateColumns: "150px 90px 1fr", gap: 12, alignItems: "start" }}>
            <div>
              <label style={{ display: "block", fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 4 }}>Date Performed</label>
              <input type="date" value={dateDraft} onChange={e => setDateDraft(e.target.value)} disabled={isSaving}
                style={{ width: "100%", padding: "8px 10px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, fontFamily: "inherit", colorScheme: "dark" }} />
            </div>
            <div>
              <label style={{ display: "block", fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 4 }}>Initials</label>
              <input type="text" value={initialsDraft}
                onChange={e => setInitialsDraft(normalizeInitials(e.target.value))}
                disabled={isSaving}
                placeholder="e.g. CD"
                maxLength={4}
                style={{ width: "100%", padding: "8px 10px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, fontFamily: "inherit", textTransform: "uppercase", letterSpacing: "0.05em" }} />
            </div>
            <div>
              <label style={{ display: "block", fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 4 }}>Notes (optional)</label>
              <textarea value={noteDraft} onChange={e => setNoteDraft(e.target.value)} disabled={isSaving}
                placeholder="How did it go? Anything to remember?"
                rows={2}
                style={{ width: "100%", padding: "8px 10px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, fontFamily: "inherit", resize: "vertical" }} />
            </div>
          </div>
          <div style={{ marginTop: 10, display: "flex", alignItems: "center", gap: 12 }}>
            <label style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>Difficulty</label>
            <StarRating value={difficultyDraft} onChange={setDifficultyDraft} />
            <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>{difficultyDraft ? `${difficultyDraft}/5` : "optional"}</span>
          </div>
          <div style={{ marginTop: 12, display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap" }}>
            <button onClick={onSave} disabled={isSaving}
              style={{
                padding: "9px 20px", borderRadius: 8, border: "none",
                background: isSaved ? "#16a34a" : "var(--color-primary)", color: "#fff",
                fontWeight: 700, fontSize: 13,
                cursor: isSaving ? "wait" : "pointer", opacity: isSaving ? 0.8 : 1,
              }}>
              {isSaving ? "Saving…" : isSaved ? "✓ Saved" : "💾 Save to History"}
            </button>
            {isSaved && (
              <span style={{ color: "#86efac", fontSize: 12 }}>
                Logged. (commit may take ~30–60s to appear in the repo)
              </span>
            )}
            {saveStatus === "error" && (
              <span style={{ color: "#fca5a5", fontSize: 12 }}>
                Save failed: {saveError}. Click Save again to retry.
              </span>
            )}
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  HISTORY VIEW — list, filter, expand, edit notes, delete, re-print
    // ═══════════════════════════════════════════════════════════════
    function HistoryView({ history, historyLoaded, onUpdateNotes, onUpdateCompleted, onUpdateDifficulty, onDelete, onLoadAndPrint, onRepeat, onRun, favorites, onToggleFavorite, recentMainLabels, goals = [], mySub }) {
      const [filterType,   setFilterType]   = useState("all");
      const [filterUser,   setFilterUser]   = useState("all"); // "all" | "__unattributed__" | sub string
      const [weekMode,     setWeekMode]     = useState("all"); // "all" | "4wk"
      const [typeWindow,   setTypeWindow]   = useState("4wk"); // "4wk" | "12wk" | "all" — windows the Type Breakdown stat
      const [filterStatus, setFilterStatus] = useState("all"); // "all" | "done" | "planned"
      const [editingId,    setEditingId]    = useState(null);
      const [editingNotes, setEditingNotes] = useState("");
      const [expandedId,   setExpandedId]   = useState(null);

      // Distinct subs present in history. Display label = most common initials for that sub.
      const userBuckets = useMemo(() => {
        const subMap = new Map(); // sub → Map<initials, count>
        let hasUnattributed = false;
        for (const e of history) {
          const sub      = (e.sub          || "").trim();
          const initials = (e.userInitials || "").trim();
          if (sub) {
            if (!subMap.has(sub)) subMap.set(sub, new Map());
            const counts = subMap.get(sub);
            if (initials) counts.set(initials, (counts.get(initials) || 0) + 1);
          } else {
            hasUnattributed = true;
          }
        }
        const users = [...subMap.entries()].map(([sub, counts]) => {
          const label = counts.size > 0
            ? [...counts.entries()].sort((a, b) => b[1] - a[1])[0][0]
            : "Unknown";
          return { id: sub, label };
        }).sort((a, b) => a.label.localeCompare(b.label));
        return { users, hasUnattributed };
      }, [history]);

      // If the selected user filter no longer exists in history, reset to "all".
      useEffect(() => {
        if (filterUser === "all") return;
        if (filterUser === "__unattributed__" && !userBuckets.hasUnattributed) setFilterUser("all");
        else if (filterUser !== "__unattributed__" && !userBuckets.users.find(u => u.id === filterUser)) setFilterUser("all");
      }, [filterUser, userBuckets]);

      const filtered = useMemo(
        () => history.filter(e => {
          if (filterType !== "all" && e.type !== filterType) return false;
          const sub = (e.sub || "").trim();
          if (filterUser !== "all") {
            if (filterUser === "__unattributed__" && sub) return false;
            if (filterUser !== "__unattributed__" && sub !== filterUser) return false;
          }
          if (filterStatus !== "all") {
            const isDone = e.completed !== false; // treat missing as done (legacy entries)
            if (filterStatus === "done"    && !isDone) return false;
            if (filterStatus === "planned" &&  isDone) return false;
          }
          return true;
        }),
        [history, filterType, filterUser, filterStatus]
      );

      // Show the user-filter row only when it would offer a meaningful choice:
      // at least 2 distinct buckets among (users, unattributed).
      const userBucketCount = userBuckets.users.length + (userBuckets.hasUnattributed ? 1 : 0);
      const showUserFilter = userBucketCount > 1;

      const stats = useMemo(() => {
        // Stats count only completed entries (planned workouts excluded)
        const done     = filtered.filter(e => e.completed !== false);
        const sessions = done.length;
        const yards    = done.reduce((s, e) => s + (e.totalYards || 0), 0);
        const avg      = sessions ? Math.round(yards / sessions) : 0;

        // Workouts per week (completed only)
        const now        = Date.now();
        const msPerWeek  = 7 * 24 * 60 * 60 * 1000;
        const fourWksAgo = now - 4 * msPerWeek;
        const allDates   = done.map(e => e.dateCompleted ? new Date(e.dateCompleted).getTime() : 0).filter(Boolean);
        const firstDate  = allDates.length > 0 ? Math.min(...allDates) : now;
        // All-time: sessions / weeks since first entry
        const wkAll = allDates.length > 0
          ? sessions / Math.max(1, (now - firstDate) / msPerWeek)
          : 0;
        // 4-week: sessions in last 28 days / actual elapsed weeks (capped at 4)
        const recentCount  = done.filter(e => {
          const d = e.dateCompleted ? new Date(e.dateCompleted).getTime() : 0;
          return d >= fourWksAgo;
        }).length;
        const weeksElapsed = Math.min(4, Math.max(1, (now - firstDate) / msPerWeek));
        const wkRecent     = recentCount / weeksElapsed;

        // Weekly yards sparkline — last 12 ISO weeks, done-only.
        // Bucket key = Monday of the entry's ISO week (UTC), formatted yyyy-mm-dd.
        const SPARK_WEEKS = 12;
        const mondayUTC = (ts) => {
          const d = new Date(ts);
          // JS getUTCDay: Sun=0, Mon=1, ... Sat=6 — shift so Mon=0..Sun=6
          const dow = (d.getUTCDay() + 6) % 7;
          const m = new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate() - dow));
          return m;
        };
        const fmtMonday = (d) => d.toISOString().slice(0, 10);
        const nowMonday = mondayUTC(now);
        const weeks = Array.from({ length: SPARK_WEEKS }).map((_, i) => {
          const m = new Date(nowMonday);
          m.setUTCDate(m.getUTCDate() - (SPARK_WEEKS - 1 - i) * 7);
          return { key: fmtMonday(m), monday: m, yards: 0 };
        });
        const weekIdx = new Map(weeks.map((w, i) => [w.key, i]));
        for (const e of done) {
          if (!e.dateCompleted) continue;
          // dateCompleted is yyyy-mm-dd; interpret as UTC midnight so week math is stable.
          const ts = new Date(`${e.dateCompleted}T00:00:00Z`).getTime();
          const k = fmtMonday(mondayUTC(ts));
          const i = weekIdx.get(k);
          if (i !== undefined) weeks[i].yards += (e.totalYards || 0);
        }
        const sparkMax = weeks.reduce((m, w) => Math.max(m, w.yards), 0);

        // Type distribution (completed only), windowed
        const winDays = typeWindow === "4wk" ? 28 : typeWindow === "12wk" ? 84 : null;
        const winCutoff = winDays === null ? -Infinity : (now - winDays * 24 * 60 * 60 * 1000);
        const windowed = done.filter(e => {
          if (winCutoff === -Infinity) return true;
          const d = e.dateCompleted ? new Date(e.dateCompleted).getTime() : 0;
          return d >= winCutoff;
        });
        const winSessions = windowed.length;
        const typeCounts = new Map();
        for (const e of windowed) typeCounts.set(e.type, (typeCounts.get(e.type) || 0) + 1);
        const typeBreakdown = [...typeCounts.entries()]
          .map(([type, count]) => ({ type, count, pct: winSessions ? Math.round(count / winSessions * 100) : 0 }))
          .sort((a, b) => b.count - a.count);

        // N4: Intensity distribution — yards per zone, derived from inferSetZone
        // for every set in every windowed workout. Two outputs:
        //   intensityTotals: { zoneId → yards } across the whole window
        //   weeklyIntensity: 12-week stacked-bar series (latest 12 ISO weeks)
        const intensityTotals = { easy: 0, aerobic: 0, threshold: 0, vo2: 0, anaerobic: 0 };
        const weeklyIntensity = weeks.map(w => ({
          key: w.key, monday: w.monday,
          zones: { easy: 0, aerobic: 0, threshold: 0, vo2: 0, anaerobic: 0 },
          total: 0,
        }));
        for (const e of done) {
          if (!Array.isArray(e.blocks)) continue;
          const ts = e.dateCompleted ? new Date(`${e.dateCompleted}T00:00:00Z`).getTime() : 0;
          const wIdx = weekIdx.get(fmtMonday(mondayUTC(ts)));
          const inWindow = ts >= winCutoff;
          for (const b of e.blocks) {
            for (const s of (b.sets || [])) {
              const z = s.zone || inferSetZone(s, b.section);
              const y = (s.reps || 1) * (s.dist || 0);
              if (inWindow) intensityTotals[z] = (intensityTotals[z] || 0) + y;
              if (wIdx !== undefined) {
                weeklyIntensity[wIdx].zones[z] += y;
                weeklyIntensity[wIdx].total   += y;
              }
            }
          }
        }
        const intensityYards = Object.values(intensityTotals).reduce((s, v) => s + v, 0);
        // 80/20 ratio: % of yards at or below aerobic vs everything threshold+.
        const easyAerobicYards = intensityTotals.easy + intensityTotals.aerobic;
        const hardYards        = intensityYards - easyAerobicYards;
        const ratio8020 = intensityYards > 0
          ? { easy: Math.round(easyAerobicYards / intensityYards * 100),
              hard: Math.round(hardYards / intensityYards * 100) }
          : null;
        const weeklyIntensityMax = weeklyIntensity.reduce((m, w) => Math.max(m, w.total), 0);

        return { sessions, yards, avg, wkAll, wkRecent, typeBreakdown, winSessions, weeks, sparkMax,
                 intensityTotals, intensityYards, ratio8020, weeklyIntensity, weeklyIntensityMax };
      }, [filtered, typeWindow]);

      // Goal progress vs current period (ISO week Mon..Sun, or calendar month).
      // Only counts the signed-in user's completed workouts.
      const goalProgress = useMemo(() => {
        if (!goals.length || !mySub) return [];
        const now = new Date();
        // ISO week boundaries (Mon..Sun) in local time.
        const dow = (now.getDay() + 6) % 7; // 0 = Monday
        const weekStart = new Date(now); weekStart.setHours(0,0,0,0); weekStart.setDate(now.getDate() - dow);
        const weekStartMs = weekStart.getTime();
        // Calendar month boundary in local time.
        const monthStart = new Date(now.getFullYear(), now.getMonth(), 1).getTime();

        let wkSessions = 0, wkYards = 0, moYards = 0;
        for (const e of history) {
          if (e.sub !== mySub) continue;
          if (e.completed === false) continue;
          if (!e.dateCompleted) continue;
          const ts = new Date(`${e.dateCompleted}T00:00:00`).getTime();
          if (ts >= weekStartMs) { wkSessions += 1; wkYards += (e.totalYards || 0); }
          if (ts >= monthStart)  { moYards   += (e.totalYards || 0); }
        }
        const valueByMetric = {
          workouts_per_week: wkSessions,
          yards_per_week:    wkYards,
          yards_per_month:   moYards,
        };
        return goals.map(g => ({
          metric: g.metric,
          target: g.target_value,
          current: valueByMetric[g.metric] || 0,
        }));
      }, [goals, history, mySub]);

      if (!historyLoaded) {
        return <div style={{ textAlign: "center", color: "var(--color-text-dim)", padding: 40 }}>Loading history…</div>;
      }
      if (history.length === 0) {
        return (
          <div style={{ textAlign: "center", color: "var(--color-text-dim)", padding: 50 }}>
            <div style={{ fontSize: 44, marginBottom: 12 }}>📜</div>
            <p style={{ fontSize: 16, color: "var(--color-text-muted)", margin: 0 }}>No workouts logged yet.</p>
            <p style={{ fontSize: 12, color: "var(--color-text-dim)", marginTop: 6 }}>Generate a workout, swim it, then click <b>Save to History</b> to log it here.</p>
          </div>
        );
      }

      return (
        <div className="screen-only">
          {/* Favorites panel */}
          {favorites && favorites.length > 0 && (
            <div style={{ marginBottom: 18, padding: "14px 16px", borderRadius: 10, background: "#1a1f2e", border: "1px solid #2d3748" }}>
              <div style={{ fontSize: 11, fontWeight: 700, color: "var(--color-warn)", textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 10 }}>
                ★ Favorites
              </div>
              <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
                {favorites.map(label => {
                  const seenDays = recentMainLabels ? recentMainLabels.get(label) : undefined;
                  return (
                    <div key={label} style={{ display: "flex", alignItems: "center", gap: 6, padding: "5px 10px", borderRadius: 8, background: "var(--color-bg)", border: "1px solid #f59e0b33" }}>
                      <span style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>{label}</span>
                      {seenDays !== undefined && (
                        <span title={`Last completed ${seenDays === 0 ? "today" : seenDays === 1 ? "yesterday" : seenDays + " days ago"}`}
                          style={{ fontSize: 10, color: "#fca5a5", background: "#7f1d1d33", border: "1px solid #7f1d1d", padding: "1px 6px", borderRadius: 999, fontWeight: 700, letterSpacing: "0.02em" }}>
                          last seen {seenDays === 0 ? "today" : `${seenDays}d ago`}
                        </span>
                      )}
                      <button
                        onClick={() => onToggleFavorite && onToggleFavorite(label)}
                        title="Remove from favorites"
                        style={{ background: "transparent", border: "none", color: "var(--color-warn)", fontSize: 16, cursor: "pointer", padding: "0 2px", lineHeight: 1 }}>
                        ★
                      </button>
                    </div>
                  );
                })}
              </div>
            </div>
          )}

          {/* Type filter chips */}
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: showUserFilter ? 8 : 14 }}>
            {[{ id: "all", label: "All", emoji: "📋" }, ...WORKOUT_TYPES].map(t => {
              const active = filterType === t.id;
              return (
                <button key={t.id} onClick={() => setFilterType(t.id)}
                  style={{
                    padding: "6px 12px", borderRadius: 999, border: "none",
                    background: active ? "var(--color-primary)" : "var(--color-card)",
                    color: active ? "#fff" : "var(--color-text-muted)",
                    fontSize: 12, fontWeight: 600, cursor: "pointer",
                  }}>
                  {t.emoji} {t.label}
                </button>
              );
            })}
          </div>

          {/* User filter chips — only shown when there's a meaningful choice. */}
          {showUserFilter && (
            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 14, alignItems: "center" }}>
              <span style={{ fontSize: 10, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 4 }}>Who:</span>
              {[
                { id: "all", label: "All" },
                ...userBuckets.users,
                ...(userBuckets.hasUnattributed ? [{ id: "__unattributed__", label: "Unattributed" }] : []),
              ].map(u => {
                const active = filterUser === u.id;
                return (
                  <button key={u.id} onClick={() => setFilterUser(u.id)}
                    style={{
                      padding: "6px 12px", borderRadius: 999, border: "none",
                      background: active ? "#0ea5e9" : "var(--color-card)",
                      color: active ? "#fff" : "var(--color-text-muted)",
                      fontSize: 12, fontWeight: 600, cursor: "pointer",
                      fontVariantNumeric: "tabular-nums",
                    }}>
                    {u.label}
                  </button>
                );
              })}
            </div>
          )}

          {/* Status filter chips */}
          <div style={{ display: "flex", gap: 8, marginBottom: 14, alignItems: "center" }}>
            <span style={{ fontSize: 10, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 4 }}>Status:</span>
            {[
              { id: "all",     label: "All" },
              { id: "done",    label: "✓ Done" },
              { id: "planned", label: "Planned" },
            ].map(s => {
              const active = filterStatus === s.id;
              return (
                <button key={s.id} onClick={() => setFilterStatus(s.id)}
                  style={{
                    padding: "6px 12px", borderRadius: 999, border: "none",
                    background: active ? (s.id === "done" ? "#16a34a" : s.id === "planned" ? "#7c3aed" : "var(--color-border-strong)") : "var(--color-card)",
                    color: active ? "#fff" : "var(--color-text-muted)",
                    fontSize: 12, fontWeight: 600, cursor: "pointer",
                  }}>
                  {s.label}
                </button>
              );
            })}
          </div>

          {/* Goal progress — only renders if user has set at least one goal */}
          {goalProgress.length > 0 && (
            <div className="card" style={{ borderRadius: 12, padding: 16, marginBottom: 16 }}>
              <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 10 }}>
                🎯 Goals · current period
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
                {goalProgress.map(gp => {
                  const meta = GOAL_METRICS.find(m => m.id === gp.metric);
                  if (!meta) return null;
                  const pct = Math.min(100, gp.target > 0 ? (gp.current / gp.target) * 100 : 0);
                  const hit = gp.current >= gp.target;
                  const barColor = hit ? "var(--color-positive)" : pct >= 75 ? "var(--color-primary)" : "#7c3aed";
                  return (
                    <div key={gp.metric}>
                      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", fontSize: 12, marginBottom: 4 }}>
                        <span style={{ color: "#cbd5e1", fontWeight: 600 }}>
                          {meta.label}
                          <span style={{ color: "var(--color-border-strong)", fontWeight: 400, marginLeft: 6 }}>· this {meta.period}</span>
                        </span>
                        <span style={{ color: hit ? "#4ade80" : "var(--color-text-muted)", fontVariantNumeric: "tabular-nums", fontWeight: 700 }}>
                          {gp.current.toLocaleString()} / {gp.target.toLocaleString()} {meta.unit}
                          {hit && <span style={{ marginLeft: 6 }}>✓</span>}
                        </span>
                      </div>
                      <div style={{ background: "var(--color-bg)", borderRadius: 4, height: 8, overflow: "hidden" }}>
                        <div style={{ width: `${pct}%`, height: "100%", background: barColor, transition: "width 0.4s" }} />
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          )}

          {/* Stats summary */}
          <div className="card" style={{ borderRadius: 12, padding: 16, marginBottom: 16 }}>
            {/* Key numbers row */}
            <div style={{ display: "flex", gap: 28, flexWrap: "wrap", marginBottom: stats.sessions > 0 ? 16 : 0 }}>
              {[
                ["Sessions",     stats.sessions.toLocaleString()],
                ["Total Yards",  stats.yards.toLocaleString()],
                ["Avg / Session", stats.avg.toLocaleString()],
              ].map(([k, v]) => (
                <div key={k}>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>{k}</div>
                  <div style={{ fontSize: 22, fontWeight: 900, color: "#fff" }}>{v}</div>
                </div>
              ))}
              {/* Workouts / week with toggle */}
              <div>
                <div style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 2 }}>
                  <span style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>Per Week</span>
                  <button
                    onClick={() => setWeekMode(m => m === "all" ? "4wk" : "all")}
                    style={{ fontSize: 9, padding: "1px 7px", borderRadius: 999, border: "1px solid #7c3aed", background: "#7c3aed33", color: "#c4b5fd", cursor: "pointer", fontWeight: 700 }}>
                    {weekMode === "all" ? "All time" : "4 wks"}
                  </button>
                </div>
                <div style={{ fontSize: 22, fontWeight: 900, color: "#fff" }}>
                  {(weekMode === "all" ? stats.wkAll : stats.wkRecent).toFixed(1)}
                </div>
              </div>
            </div>
            {/* Yards/week sparkline — last 12 ISO weeks */}
            {stats.sessions > 0 && (
              <div style={{ marginBottom: 16 }}>
                <div style={{ display: "flex", alignItems: "baseline", gap: 8, marginBottom: 6 }}>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>Yards / Week</div>
                  <div style={{ fontSize: 10, color: "var(--color-border-strong)" }}>last 12 weeks</div>
                  {stats.sparkMax > 0 && (
                    <div style={{ marginLeft: "auto", fontSize: 10, color: "var(--color-text-dim)", fontVariantNumeric: "tabular-nums" }}>
                      peak {stats.sparkMax.toLocaleString()}
                    </div>
                  )}
                </div>
                {stats.sparkMax > 0 ? (
                  <svg viewBox="0 0 240 48" preserveAspectRatio="none"
                    style={{ width: "100%", height: 48, display: "block" }}>
                    {stats.weeks.map((w, i) => {
                      const bw = 240 / stats.weeks.length;
                      const x  = i * bw;
                      const h  = (w.yards / stats.sparkMax) * 44;
                      const y  = 48 - h;
                      const isCurrent = i === stats.weeks.length - 1;
                      return (
                        <g key={w.key}>
                          <rect
                            x={x + 1} y={y}
                            width={Math.max(0, bw - 2)} height={h}
                            fill={isCurrent ? "#38bdf8" : "var(--color-primary)"}
                            opacity={w.yards > 0 ? 0.95 : 0}
                            rx={1}
                          >
                            <title>Week of {w.key}: {w.yards.toLocaleString()} yds</title>
                          </rect>
                          {/* baseline tick so empty weeks are still legible */}
                          {w.yards === 0 && (
                            <rect x={x + 1} y={47} width={Math.max(0, bw - 2)} height={1} fill="var(--color-border)">
                              <title>Week of {w.key}: 0 yds</title>
                            </rect>
                          )}
                        </g>
                      );
                    })}
                  </svg>
                ) : (
                  <div style={{ fontSize: 11, color: "var(--color-text-dim)", fontStyle: "italic", padding: "4px 0" }}>
                    No completed workouts in the last 12 weeks.
                  </div>
                )}
              </div>
            )}
            {/* N4: Intensity distribution — stacked horizontal bar + 80/20 ratio */}
            {stats.sessions > 0 && stats.intensityYards > 0 && (
              <div style={{ marginBottom: 16 }}>
                <div style={{ display: "flex", alignItems: "baseline", gap: 8, marginBottom: 6 }}>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>Intensity</div>
                  <div style={{ fontSize: 10, color: "var(--color-border-strong)" }}>
                    {typeWindow === "4wk" ? "4 wks" : typeWindow === "12wk" ? "12 wks" : "all time"}
                  </div>
                  {stats.ratio8020 && (
                    <div style={{ marginLeft: "auto", fontSize: 10, fontVariantNumeric: "tabular-nums" }}>
                      <span style={{ color: "var(--color-positive)", fontWeight: 700 }}>{stats.ratio8020.easy}%</span>
                      <span style={{ color: "var(--color-border-strong)" }}> easy/aerobic </span>
                      <span style={{ color: "var(--color-border-strong)" }}>·</span>
                      <span style={{ color: "#f97316", fontWeight: 700 }}> {stats.ratio8020.hard}%</span>
                      <span style={{ color: "var(--color-border-strong)" }}> threshold+</span>
                    </div>
                  )}
                </div>
                {/* Stacked horizontal bar */}
                <div style={{ display: "flex", height: 14, borderRadius: 4, overflow: "hidden", background: "var(--color-bg)", marginBottom: 6 }}>
                  {ZONE_ORDER.map(zId => {
                    const y   = stats.intensityTotals[zId] || 0;
                    const pct = stats.intensityYards > 0 ? (y / stats.intensityYards) * 100 : 0;
                    if (pct <= 0) return null;
                    return (
                      <div key={zId}
                        title={`${ZONES[zId].label}: ${y.toLocaleString()} yds · ${pct.toFixed(1)}%`}
                        style={{ width: `${pct}%`, background: ZONES[zId].color, transition: "width 0.4s" }} />
                    );
                  })}
                </div>
                {/* Per-zone legend */}
                <div style={{ display: "flex", flexWrap: "wrap", gap: "4px 14px", fontSize: 10 }}>
                  {ZONE_ORDER.map(zId => {
                    const y   = stats.intensityTotals[zId] || 0;
                    const pct = stats.intensityYards > 0 ? (y / stats.intensityYards) * 100 : 0;
                    return (
                      <span key={zId} style={{ display: "inline-flex", alignItems: "center", gap: 4, color: pct > 0 ? "#cbd5e1" : "var(--color-border-strong)" }}>
                        <span style={{ width: 8, height: 8, borderRadius: "50%", background: ZONES[zId].color, display: "inline-block", opacity: pct > 0 ? 1 : 0.4 }} />
                        <span>{ZONES[zId].label}</span>
                        <span style={{ color: "var(--color-text-dim)", fontVariantNumeric: "tabular-nums" }}>{pct.toFixed(0)}%</span>
                      </span>
                    );
                  })}
                </div>
                {/* Bars-over-time: stacked weekly bars, last 12 ISO weeks */}
                {stats.weeklyIntensityMax > 0 && (
                  <div style={{ marginTop: 10 }}>
                    <div style={{ fontSize: 10, color: "var(--color-border-strong)", marginBottom: 4 }}>last 12 weeks</div>
                    <svg viewBox="0 0 240 48" preserveAspectRatio="none"
                      style={{ width: "100%", height: 48, display: "block" }}>
                      {stats.weeklyIntensity.map((w, i) => {
                        const bw = 240 / stats.weeklyIntensity.length;
                        const x  = i * bw;
                        const colWidth = Math.max(0, bw - 2);
                        const totalHeight = (w.total / stats.weeklyIntensityMax) * 44;
                        let yCursor = 48 - totalHeight;
                        const segments = [];
                        // Stack from easy (bottom) to anaerobic (top).
                        for (const zId of ZONE_ORDER) {
                          const ratio = w.total > 0 ? w.zones[zId] / w.total : 0;
                          if (ratio <= 0) continue;
                          const segH = totalHeight * ratio;
                          segments.push(
                            <rect key={zId}
                              x={x + 1} y={yCursor}
                              width={colWidth} height={segH}
                              fill={ZONES[zId].color} opacity={0.95}>
                              <title>{`${w.key} · ${ZONES[zId].label}: ${w.zones[zId].toLocaleString()} yds`}</title>
                            </rect>
                          );
                          yCursor += segH;
                        }
                        return (
                          <g key={w.key}>
                            {segments}
                            {/* baseline tick for empty weeks */}
                            {w.total === 0 && (
                              <rect x={x + 1} y={47} width={colWidth} height={1} fill="var(--color-border)">
                                <title>{`${w.key}: no yards`}</title>
                              </rect>
                            )}
                          </g>
                        );
                      })}
                    </svg>
                  </div>
                )}
              </div>
            )}

            {/* Type distribution bars */}
            {stats.sessions > 0 && (
              <div>
                <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8, flexWrap: "wrap" }}>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>Type Breakdown</div>
                  <div style={{ display: "inline-flex", gap: 4, marginLeft: "auto" }}>
                    {[
                      { id: "4wk",  label: "4 wks"  },
                      { id: "12wk", label: "12 wks" },
                      { id: "all",  label: "All"    },
                    ].map(o => {
                      const active = typeWindow === o.id;
                      return (
                        <button key={o.id} onClick={() => setTypeWindow(o.id)}
                          style={{
                            fontSize: 9, padding: "1px 7px", borderRadius: 999,
                            border: `1px solid ${active ? "#7c3aed" : "var(--color-border)"}`,
                            background: active ? "#7c3aed33" : "transparent",
                            color: active ? "#c4b5fd" : "var(--color-text-dim)",
                            cursor: "pointer", fontWeight: 700,
                          }}>
                          {o.label}
                        </button>
                      );
                    })}
                  </div>
                </div>
                {stats.typeBreakdown.length > 0 ? (
                  <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
                    {stats.typeBreakdown.map(({ type, count, pct }) => {
                      const meta     = WORKOUT_TYPES.find(t => t.id === type);
                      const maxCount = stats.typeBreakdown[0].count;
                      return (
                        <div key={type} style={{ display: "flex", alignItems: "center", gap: 8 }}>
                          <span style={{ fontSize: 11, color: "#cbd5e1", width: 90, flexShrink: 0, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                            {meta ? `${meta.emoji} ${meta.label}` : type}
                          </span>
                          <div style={{ flex: 1, background: "var(--color-bg)", borderRadius: 4, height: 8, overflow: "hidden" }}>
                            <div style={{ width: `${(count / maxCount) * 100}%`, height: "100%", background: meta ? meta.badge : "var(--color-primary)", borderRadius: 4, transition: "width 0.4s" }} />
                          </div>
                          <span style={{ fontSize: 11, color: "var(--color-text-dim)", width: 44, textAlign: "right", flexShrink: 0, fontVariantNumeric: "tabular-nums" }}>{count} · {pct}%</span>
                        </div>
                      );
                    })}
                  </div>
                ) : (
                  <div style={{ fontSize: 11, color: "var(--color-text-dim)", fontStyle: "italic", padding: "4px 0" }}>
                    No completed workouts in this window.
                  </div>
                )}
              </div>
            )}
          </div>

          {/* Entries */}
          {filtered.map(entry => {
            const meta       = WORKOUT_TYPES.find(t => t.id === entry.type);
            const isExpanded = expandedId === entry.id;
            const isEditing  = editingId === entry.id;
            // Filter via equipMode so the literal string "off" doesn't slip through
            // the truthy check (regression introduced when equipment moved from
            // booleans to the tri-state "off"|"preferred"|"required" strings).
            const eqList     = entry.equipment
              ? Object.keys(entry.equipment).filter(k => equipMode(entry.equipment, k) !== "off")
              : [];
            return (
              <div key={entry.id} className="card" style={{ borderRadius: 12, padding: 16, marginBottom: 12 }}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 12 }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap" }}>
                    <span style={{ fontSize: 13, color: "#cbd5e1", fontVariantNumeric: "tabular-nums" }}>{entry.dateCompleted || "—"}</span>
                    <button
                      onClick={() => onUpdateCompleted(entry.id, !(entry.completed !== false))}
                      title={entry.completed !== false ? "Mark as planned" : "Mark as done"}
                      style={{
                        padding: "2px 9px", borderRadius: 999, border: "none", cursor: "pointer", fontSize: 11, fontWeight: 700,
                        background: entry.completed !== false ? "#16a34a22" : "#7c3aed22",
                        color:      entry.completed !== false ? "#86efac"   : "#c4b5fd",
                      }}>
                      {entry.completed !== false ? "✓ Done" : "Planned"}
                    </button>
                    {meta && (
                      <span style={{ background: meta.badge, color: meta.badgeText, borderRadius: 999, padding: "3px 10px", fontSize: 11, fontWeight: 700 }}>
                        {meta.emoji} {meta.label}
                      </span>
                    )}
                    {entry.userInitials && (
                      <span title="Logged by"
                        style={{ background: "#0ea5e9", color: "#fff", borderRadius: 999, padding: "3px 9px", fontSize: 10, fontWeight: 800, letterSpacing: "0.06em" }}>
                        {entry.userInitials}
                      </span>
                    )}
                    <span style={{ fontSize: 13, color: "#fff", fontWeight: 700 }}>{(entry.totalYards || 0).toLocaleString()} {(entry.poolMode === "50m" || entry.poolMode === "25m") ? "m" : "yds"}</span>
                    {entry.estimatedMin && <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>~{entry.estimatedMin} min</span>}
                    {entry.multi_lane && Array.isArray(entry.multi_lane.lanes) && entry.multi_lane.lanes.length > 0 && (
                      <span title={`Generated for ${entry.multi_lane.lanes.length} lanes: ${entry.multi_lane.lanes.map(l => l.pace).join(" / ")}`}
                        style={{ background: "rgba(59, 130, 246, 0.18)", color: "var(--color-primary)", border: "1px solid rgba(59, 130, 246, 0.45)", borderRadius: 999, padding: "2px 8px", fontSize: 10, fontWeight: 700, letterSpacing: "0.04em" }}>
                        🏊 {entry.multi_lane.lanes.length}-lane
                      </span>
                    )}
                    <span title={entry.difficulty != null ? `Self-rated difficulty: ${entry.difficulty}/5 — click to change` : "Click to rate difficulty (1–5)"}>
                      <StarRating
                        value={entry.difficulty ?? null}
                        size={14}
                        allowToggleClear={false}
                        showClear
                        onChange={n => onUpdateDifficulty(entry.id, n)}
                      />
                    </span>
                  </div>
                  <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                    <button onClick={() => setExpandedId(isExpanded ? null : entry.id)}
                      style={{ padding: "4px 10px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                      {isExpanded ? "Hide" : "View"}
                    </button>
                    <button onClick={() => { setEditingId(entry.id); setEditingNotes(entry.notes || ""); }}
                      style={{ padding: "4px 10px", background: "transparent", color: "#cbd5e1", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                      Notes
                    </button>
                    <button onClick={() => onRepeat(entry)}
                      title="Open this workout as a fresh, unsaved one to log again"
                      style={{ padding: "4px 10px", background: "transparent", color: "#86efac", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                      🔁 Repeat
                    </button>
                    {entry.blocks && (
                      <button onClick={() => onRun(entry)}
                        title="Step through this workout section by section"
                        style={{ padding: "4px 10px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                        ▶ Run
                      </button>
                    )}
                    <button onClick={() => onLoadAndPrint(entry)}
                      style={{ padding: "4px 10px", background: "transparent", color: "#cbd5e1", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                      🖨 Print
                    </button>
                    <button onClick={() => { if (window.confirm("Delete this workout from history?")) onDelete(entry.id); }}
                      style={{ padding: "4px 10px", background: "transparent", color: "#fca5a5", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 11, cursor: "pointer" }}>
                      Delete
                    </button>
                  </div>
                </div>

                {eqList.length > 0 && (
                  <div style={{ marginTop: 8, display: "flex", gap: 6, flexWrap: "wrap" }}>
                    {eqList.map(k => (
                      <span key={k} style={{ background: "var(--color-border)", color: "#cbd5e1", padding: "2px 8px", borderRadius: 999, fontSize: 9, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.04em" }}>{k}</span>
                    ))}
                  </div>
                )}

                {entry.focusNote && (
                  <div style={{ marginTop: 8, fontSize: 12, color: "var(--color-primary)", fontWeight: 600 }}>
                    🎯 {entry.focusNote}
                  </div>
                )}

                {isEditing ? (
                  <div style={{ marginTop: 12 }}>
                    <textarea value={editingNotes} onChange={e => setEditingNotes(e.target.value)} rows={2}
                      style={{ width: "100%", padding: 8, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, fontFamily: "inherit", resize: "vertical" }} />
                    <div style={{ display: "flex", gap: 8, marginTop: 8 }}>
                      <button onClick={() => { onUpdateNotes(entry.id, editingNotes); setEditingId(null); }}
                        style={{ padding: "5px 14px", background: "var(--color-primary)", color: "#fff", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 600, cursor: "pointer" }}>Save Notes</button>
                      <button onClick={() => setEditingId(null)}
                        style={{ padding: "5px 14px", background: "transparent", color: "var(--color-text-muted)", border: "1px solid var(--color-border)", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>Cancel</button>
                    </div>
                  </div>
                ) : entry.notes ? (
                  <div style={{ marginTop: 8, color: "#cbd5e1", fontSize: 13, fontStyle: "italic", lineHeight: 1.4 }}>
                    “{entry.notes}”
                  </div>
                ) : null}

                {isExpanded && entry.blocks && (
                  <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid var(--color-border)" }}>
                    {entry.blocks.map((b, i) => b && b.kind === "dryland"
                      ? <DrylandBlock key={i} block={b} />
                      : <WorkoutBlock key={i} block={b} equipment={entry.equipment || {}} recentMainLabels={recentMainLabels} poolMode={entry.poolMode || "25y"} />)}
                  </div>
                )}
              </div>
            );
          })}
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  PACE CLOCK
    //  Tap a set card to step through reps with a live countdown.
    // ═══════════════════════════════════════════════════════════════

    // Returns true when viewport is wider than it is tall (landscape).
    function useIsLandscape() {
      const [land, setLand] = useState(() => window.innerWidth > window.innerHeight);
      useEffect(() => {
        const update = () => setLand(window.innerWidth > window.innerHeight);
        window.addEventListener("resize", update);
        window.addEventListener("orientationchange", update);
        return () => {
          window.removeEventListener("resize", update);
          window.removeEventListener("orientationchange", update);
        };
      }, []);
      return land;
    }

    // Parse "On 2:00", "On :45", "On 1:10" → seconds. "No interval" → null.
    function parseIntervalSeconds(str) {
      if (!str || /no interval/i.test(str)) return null;
      const full = str.match(/(\d+):(\d{2})/);
      if (full) return parseInt(full[1], 10) * 60 + parseInt(full[2], 10);
      const sec  = str.match(/:(\d{2})/);
      if (sec)  return parseInt(sec[1], 10);
      return null;
    }

    function fmtTime(secs) {
      const m = Math.floor(secs / 60);
      const s = secs % 60;
      return `${m}:${String(s).padStart(2, "0")}`;
    }

    function PaceClockView({ set, sectionName, focusNote, onClose, onNext, onWorkoutFinish, onLap, isLastSet, restSecs, autoStart, elapsed, audioCues = false, lapButton = true, unit = "yds" }) {
      const totalSecs  = parseIntervalSeconds(set.interval);
      const hasTimer   = totalSecs !== null;
      const isLandscape = useIsLandscape();

      const [repIdx,    setRepIdx]   = useState(0);
      const [secsLeft,  setSecsLeft] = useState(totalSecs ?? 0);
      const [running,   setRunning]  = useState(() => !!(autoStart && totalSecs !== null));
      const [finished,  setFinished] = useState(false);
      const [restLeft,  setRestLeft] = useState(null); // null = not in rest phase
      // E: lap-split state. `lastSplit` shows transient feedback after each lap;
      // `setSplits` is the running list for this single set (parent merges across sets).
      const [lastSplit, setLastSplit] = useState(null);
      const [setSplits, setSetSplits] = useState([]);

      // Tick down every second while running
      useEffect(() => {
        if (!running || !hasTimer) return;
        const id = setInterval(() => setSecsLeft(s => Math.max(0, s - 1)), 1000);
        return () => clearInterval(id);
      }, [running, hasTimer]);

      // When countdown hits zero, record an on-time split and advance rep or finish.
      useEffect(() => {
        if (!running || !hasTimer || secsLeft !== 0) return;
        // E: auto-advance = swimmer made the interval exactly (delta 0).
        recordSplit(totalSecs);
        if (repIdx + 1 >= set.reps) {
          setRunning(false);
          setFinished(true);
        } else {
          setRepIdx(r => r + 1);
          setSecsLeft(totalSecs);
        }
      }, [secsLeft, running, hasTimer]);

      // E: helper — record a split for the current rep and bubble up to parent.
      function recordSplit(actualSecs) {
        if (!hasTimer) return;
        const split = {
          rep:           repIdx,
          actual_secs:   Math.max(0, Math.round(actualSecs)),
          expected_secs: totalSecs,
          delta:         Math.round(actualSecs) - totalSecs,
        };
        setSetSplits(prev => [...prev, split]);
        setLastSplit(split);
        if (onLap) onLap(split);
      }

      function handleLap() {
        if (!hasTimer || !running) return;
        const actualSecs = totalSecs - secsLeft;
        if (actualSecs <= 0) return; // ignore taps before the rep has run
        recordSplit(actualSecs);
        if (repIdx + 1 >= set.reps) {
          setRunning(false);
          setFinished(true);
        } else {
          setRepIdx(r => r + 1);
          setSecsLeft(totalSecs);
        }
      }

      // When a set finishes, kick off rest phase (or 1.5s flash for 0s)
      useEffect(() => {
        if (!finished || isLastSet || restSecs === null) return;
        if (restSecs === 0) {
          const id = setTimeout(() => { if (onNext) onNext(); }, 1500);
          return () => clearTimeout(id);
        }
        setRestLeft(restSecs);
      }, [finished, isLastSet, restSecs, onNext]);

      // Tick rest countdown; call onNext when it reaches zero.
      // W1: fire audio cues on the 3/2/1 beeps and the 0 "go" tone.
      useEffect(() => {
        if (restLeft === null) return;
        if (audioCues) {
          if (restLeft === 0)                 playRestCue("go");
          else if (restLeft <= 3)             playRestCue("beep");
        }
        if (restLeft === 0) { if (onNext) onNext(); return; }
        const id = setTimeout(() => setRestLeft(r => r - 1), 1000);
        return () => clearTimeout(id);
      }, [restLeft, audioCues]);

      const handleReset = () => {
        setRunning(false);
        setFinished(false);
        setRepIdx(0);
        setSecsLeft(totalSecs ?? 0);
        // E: dump in-progress splits — resetting means "swim this set over."
        setSetSplits([]);
        setLastSplit(null);
      };

      const handleSkip = () => {
        if (repIdx + 1 >= set.reps) {
          setRunning(false);
          setFinished(true);
        } else {
          setRepIdx(r => r + 1);
          setSecsLeft(totalSecs ?? 0);
        }
      };

      const isAtStart = repIdx === 0 && secsLeft === (totalSecs ?? 0) && !running;

      // E: split feedback — running avg across this set plus last-rep delta.
      const avgDelta = setSplits.length > 0
        ? Math.round(setSplits.reduce((s, sp) => s + sp.delta, 0) / setSplits.length)
        : null;
      const fmtSigned = (n) => {
        if (n == null) return "—";
        const abs = Math.abs(n);
        const m = Math.floor(abs / 60);
        const s = abs % 60;
        const t = m > 0 ? `${m}:${String(s).padStart(2, "0")}` : `0:${String(s).padStart(2, "0")}`;
        return (n > 0 ? "+" : n < 0 ? "−" : "±") + t;
      };
      const splitFeedback = lapButton && hasTimer && (setSplits.length > 0 || lastSplit) && (
        <div className="screen-only" style={{
          display: "flex", alignItems: "center", justifyContent: "center", gap: 14,
          padding: "6px 12px", borderRadius: 999,
          background: "rgba(15,23,42,0.6)", border: "1px solid var(--color-card)",
          fontVariantNumeric: "tabular-nums",
          fontSize: isLandscape ? 13 : 14,
        }}>
          {lastSplit && (
            <span style={{
              color: lastSplit.delta < 0 ? "#86efac" : lastSplit.delta > 0 ? "#fca5a5" : "#cbd5e1",
              fontWeight: 800,
            }}>
              last {fmtSigned(lastSplit.delta)} <span style={{ color: "var(--color-border-strong)", fontWeight: 400 }}>· {fmtTime(lastSplit.actual_secs)}</span>
            </span>
          )}
          {avgDelta !== null && setSplits.length > 1 && (
            <span style={{ color: "var(--color-text-muted)" }}>
              avg ∆ <span style={{ color: avgDelta < 0 ? "#86efac" : avgDelta > 0 ? "#fca5a5" : "#cbd5e1", fontWeight: 700 }}>{fmtSigned(avgDelta)}</span>
              <span style={{ color: "var(--color-border-strong)" }}> · {setSplits.length}/{set.reps}</span>
            </span>
          )}
        </div>
      );

      // Rep dots shared between portrait and landscape
      const repDots = set.reps <= 20 && (
        <div style={{ display: "flex", gap: isLandscape ? 16 : 12, flexWrap: "wrap", justifyContent: "center" }}>
          {Array.from({ length: set.reps }).map((_, i) => (
            <div key={i} style={{
              width: isLandscape ? 28 : 18, height: isLandscape ? 28 : 18,
              borderRadius: "50%",
              background: i < repIdx ? "var(--color-positive)" : i === repIdx ? "var(--color-primary)" : "var(--color-card)",
              border: `2px solid ${i < repIdx ? "var(--color-positive)" : i === repIdx ? "var(--color-primary)" : "var(--color-border)"}`,
              transition: "background 0.3s, border-color 0.3s",
            }} />
          ))}
        </div>
      );

      // Controls shared between portrait and landscape
      const btnBase = {
        borderRadius: 16, fontWeight: 700, cursor: "pointer",
        padding: isLandscape ? "14px 0" : "22px 0",
        minHeight: isLandscape ? 60 : 70,
        fontSize: isLandscape ? 18 : 22,
      };
      const controls = (
        <div style={{ display: "flex", gap: isLandscape ? 10 : 14, width: "100%" }}>
          {!finished && (
            <>
              {hasTimer && running ? (
                lapButton ? (
                  // E: when timer is running AND lap button is on, the primary
                  // action is "✓ Lap" — coach taps each rep to record splits.
                  <>
                    <button onClick={handleLap} style={{
                      ...btnBase, flex: 2, border: "none", background: "#16a34a",
                      color: "#fff", fontSize: isLandscape ? 20 : 26, fontWeight: 800,
                    }}>
                      ✓ Lap
                    </button>
                    <button onClick={() => setRunning(false)} style={{
                      ...btnBase, flex: 1, border: "1px solid var(--color-border)",
                      background: "var(--color-bg)", color: "#cbd5e1",
                    }}>
                      ⏸
                    </button>
                  </>
                ) : (
                  // Run-screen v1: lap button OFF — primary action is Pause
                  // (no manual lap input; timer auto-advances on hit).
                  <button onClick={() => setRunning(false)} style={{
                    ...btnBase, flex: 2, border: "none",
                    background: "var(--color-primary)",
                    color: "#fff", fontSize: isLandscape ? 20 : 26, fontWeight: 800,
                  }}>
                    ⏸ Pause
                  </button>
                )
              ) : hasTimer ? (
                <button onClick={() => setRunning(r => !r)} style={{
                  ...btnBase, flex: 2, border: "none",
                  background: "var(--color-primary)",
                  color: "#fff", fontSize: isLandscape ? 20 : 26, fontWeight: 800,
                }}>
                  {isAtStart ? "▶ Start" : "▶ Resume"}
                </button>
              ) : (
                <button onClick={handleSkip} style={{
                  ...btnBase, flex: 2, border: "none", background: "var(--color-primary)",
                  color: "#fff", fontSize: isLandscape ? 20 : 26, fontWeight: 800,
                }}>
                  {repIdx + 1 >= set.reps ? "Done ✓" : "Next Rep →"}
                </button>
              )}
              {hasTimer && (
                <button onClick={handleSkip} style={{
                  ...btnBase, flex: 1, border: "1px solid var(--color-border)",
                  background: "var(--color-bg)", color: "var(--color-text-dim)",
                }}>
                  Skip →
                </button>
              )}
            </>
          )}
          {!finished ? (
            <button onClick={handleReset} style={{
              ...btnBase, border: "1px solid var(--color-card)", background: "transparent",
              color: "var(--color-border-strong)", width: isLandscape ? undefined : "100%",
            }}>
              ↺ Reset
            </button>
          ) : isLastSet ? (
            <button onClick={onWorkoutFinish || onClose} style={{
              ...btnBase, flex: 1, border: "1px solid #16a34a",
              background: "#16a34a22", color: "#86efac",
            }}>
              Finish ✓
            </button>
          ) : restSecs === null ? (
            <button onClick={onNext || onClose} style={{
              ...btnBase, flex: 1, border: "1px solid var(--color-card)",
              background: "transparent", color: "var(--color-border-strong)",
            }}>
              Next Set →
            </button>
          ) : restLeft !== null && restLeft > 0 ? (
            <button onClick={() => setRestLeft(0)} style={{
              ...btnBase, flex: 1, border: "1px solid var(--color-warn)",
              background: "transparent", color: "var(--color-warn)",
            }}>
              Skip Rest →
            </button>
          ) : null}
        </div>
      );

      if (isLandscape) {
        // ── LANDSCAPE: two-column layout ──────────────────────────────
        // Run-screen v1 rebuild for poolside glance-readability:
        //  - Info hierarchy swapped: live state (REP X OF Y, clock) dominates
        //    over static set info on the left.
        //  - Clock grown 80→128. State label ("INTERVAL"/"REST"/"GO") sits
        //    above the number so glance disambiguates without reading color.
        //  - repDots moved to the right column under the clock (dynamic state
        //    with dynamic state). splitFeedback stays on the left.
        //  - Column ratio flipped 1.1:1 → 1:1.2 so the clock side gets the
        //    extra real estate.
        //  - Right column wrapped in a min-height container so the swap
        //    between running / REST / Set Complete doesn't shift layout.

        // State label + color drive the glance disambiguation.
        let phaseLabel = null;
        let phaseColor = "var(--color-text-dim)";
        if (finished && restLeft !== null && restLeft > 0) {
          if (restLeft <= 3) { phaseLabel = "GO IN"; phaseColor = "#4ade80"; }
          else               { phaseLabel = "REST";  phaseColor = "var(--color-warn)"; }
        } else if (finished) {
          phaseLabel = null; // celebration screen has its own headline
        } else if (hasTimer) {
          phaseLabel = "INTERVAL"; phaseColor = running ? "var(--color-text)" : "var(--color-border-strong)";
        } else {
          phaseLabel = "FREE";     phaseColor = "var(--color-border-strong)";
        }

        return (
          <div style={{
            position: "fixed", inset: 0, zIndex: 10000,
            background: "#020617",
            display: "flex", flexDirection: "column",
            fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
          }}>
            {/* Top bar */}
            <div style={{
              display: "flex", justifyContent: "space-between", alignItems: "center",
              padding: "8px 16px", borderBottom: "1px solid var(--color-card)", flexShrink: 0,
            }}>
              <button onClick={onClose} style={{
                background: "transparent", border: "none", color: "var(--color-text-muted)",
                fontSize: 28, lineHeight: 1, cursor: "pointer",
                padding: "4px 8px", borderRadius: 8, minWidth: 44, minHeight: 44,
              }}>✕</button>
              <div style={{ textAlign: "center", minWidth: 0, flex: 1, padding: "0 8px" }}>
                <div style={{ fontSize: 15, color: "var(--color-border-strong)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em" }}>
                  {sectionName || "Pace Clock"}
                </div>
                {focusNote && (
                  <div style={{ fontSize: 12, color: "var(--color-primary)", fontStyle: "italic", marginTop: 2, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                    🎯 {focusNote}
                  </div>
                )}
              </div>
              <div style={{ minWidth: 44, textAlign: "right" }}>
                {elapsed !== undefined && (
                  <div style={{ fontSize: 36, color: "var(--color-warn)", fontWeight: 700, fontFamily: "monospace", letterSpacing: "0.04em" }}>
                    {Math.floor(elapsed / 60)}:{String(elapsed % 60).padStart(2, "0")}
                  </div>
                )}
              </div>
            </div>

            {/* Two-column content — right column gets more room */}
            <div style={{ flex: 1, display: "flex", overflow: "hidden" }}>

              {/* Left: set info (now visually secondary to the clock) */}
              <div style={{
                flex: 1, display: "flex", flexDirection: "column",
                justifyContent: "center", padding: "12px 20px",
                borderRight: "1px solid var(--color-card)",
              }}>
                <div style={{ fontSize: 28, fontWeight: 800, color: "var(--color-text)", marginBottom: 8 }}>
                  {set.reps} × {set.dist} {unit}
                </div>
                <div style={{ fontSize: 24, color: "#cbd5e1", lineHeight: 1.35, marginBottom: 6 }}>
                  {set.desc}
                </div>
                <div style={{ fontSize: 22, color: "var(--color-border-strong)", fontFamily: "monospace" }}>
                  {set.interval}
                </div>
                {splitFeedback && <div style={{ marginTop: 14 }}>{splitFeedback}</div>}
              </div>

              {/* Right: clock — bigger flex, bigger number, label above */}
              <div style={{
                flex: 1.2, display: "flex", flexDirection: "column",
                alignItems: "center", justifyContent: "center", padding: "12px 16px",
              }}>
                {/* Min-height container kills layout shift between states */}
                <div style={{
                  minHeight: 220, width: "100%",
                  display: "flex", flexDirection: "column",
                  alignItems: "center", justifyContent: "center",
                }}>
                  {finished ? (
                    restLeft !== null && restLeft > 0 ? (
                      <div style={{ textAlign: "center" }}>
                        <div style={{ fontSize: 16, fontWeight: 800, color: phaseColor, letterSpacing: "0.14em", marginBottom: 8 }}>
                          {phaseLabel}
                        </div>
                        <div style={{ fontSize: 128, fontWeight: 900, color: phaseColor, fontFamily: "monospace", letterSpacing: "-0.03em", lineHeight: 1 }}>
                          {restLeft}
                        </div>
                        <div style={{ fontSize: 13, color: "var(--color-text-dim)", marginTop: 8 }}>Next set starting soon…</div>
                      </div>
                    ) : (
                      <div style={{ textAlign: "center" }}>
                        <div style={{ fontSize: 56, marginBottom: 10 }}>✅</div>
                        <div style={{ fontSize: 32, fontWeight: 800, color: "#4ade80" }}>Set Complete!</div>
                        <div style={{ fontSize: 18, color: "var(--color-text-dim)", marginTop: 8 }}>
                          {set.reps} rep{set.reps !== 1 ? "s" : ""} done
                        </div>
                      </div>
                    )
                  ) : (
                    <>
                      <div style={{ fontSize: 32, fontWeight: 700, color: "var(--color-text-dim)", marginBottom: 8, letterSpacing: "0.06em" }}>
                        REP <span style={{ color: "var(--color-text)" }}>{repIdx + 1}</span>
                        {" "}<span style={{ color: "var(--color-border)" }}>OF</span>{" "}
                        {set.reps}
                      </div>
                      {phaseLabel && (
                        <div style={{ fontSize: 13, fontWeight: 800, color: phaseColor, letterSpacing: "0.14em", marginBottom: 4 }}>
                          {phaseLabel}
                        </div>
                      )}
                      {hasTimer ? (
                        <div style={{
                          fontSize: 128, fontWeight: 900,
                          color: running ? "#ffffff" : "var(--color-border)",
                          fontFamily: "monospace", letterSpacing: "-0.03em", lineHeight: 1,
                          transition: "color 0.25s",
                        }}>
                          {fmtTime(secsLeft)}
                        </div>
                      ) : (
                        <div style={{ fontSize: 22, color: "var(--color-border-strong)", fontStyle: "italic" }}>
                          Swim easy
                        </div>
                      )}
                      {repDots && <div style={{ marginTop: 14 }}>{repDots}</div>}
                    </>
                  )}
                </div>
              </div>
            </div>

            {/* Controls */}
            <div style={{ padding: "10px 16px 14px", borderTop: "1px solid var(--color-card)", flexShrink: 0 }}>
              {controls}
            </div>
          </div>
        );
      }

      // ── PORTRAIT: original vertical layout ────────────────────────
      return (
        <div style={{
          position: "fixed", inset: 0, zIndex: 10000,
          background: "#020617",
          display: "flex", flexDirection: "column", alignItems: "center",
          fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
        }}>
          {/* Top bar */}
          <div style={{
            width: "100%", display: "flex", justifyContent: "space-between",
            alignItems: "center", padding: "16px 20px",
            borderBottom: "1px solid var(--color-card)",
          }}>
            <button onClick={onClose} style={{
              background: "transparent", border: "none", color: "var(--color-text-muted)",
              fontSize: 36, lineHeight: 1, cursor: "pointer",
              padding: "4px 8px", borderRadius: 8, minWidth: 52, minHeight: 52,
            }}>✕</button>
            <div style={{ textAlign: "center", minWidth: 0, flex: 1, padding: "0 8px" }}>
              <div style={{ fontSize: 18, color: "var(--color-border-strong)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em" }}>
                {sectionName || "Pace Clock"}
              </div>
              {focusNote && (
                <div style={{ fontSize: 13, color: "var(--color-primary)", fontStyle: "italic", marginTop: 3, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                  🎯 {focusNote}
                </div>
              )}
            </div>
            <div style={{ minWidth: 52, textAlign: "right" }}>
              {elapsed !== undefined && (
                <div style={{ fontSize: 36, color: "var(--color-warn)", fontWeight: 700, fontFamily: "monospace", letterSpacing: "0.04em" }}>
                  {Math.floor(elapsed / 60)}:{String(elapsed % 60).padStart(2, "0")}
                </div>
              )}
            </div>
          </div>

          {/* Set description + interval label */}
          <div style={{ width: "100%", padding: "20px 24px 0", textAlign: "center" }}>
            <div style={{ fontSize: 26, fontWeight: 800, color: "var(--color-text)", lineHeight: 1.3, marginBottom: 8 }}>
              {set.reps} × {set.dist} {unit}
            </div>
            <div style={{ fontSize: 30, color: "#cbd5e1", lineHeight: 1.4, marginBottom: 8 }}>
              {set.desc}
            </div>
            <div style={{ fontSize: 20, color: "var(--color-border-strong)", fontFamily: "monospace" }}>
              {set.interval}
            </div>
          </div>

          {/* Clock area */}
          <div style={{
            flex: 1, display: "flex", flexDirection: "column",
            alignItems: "center", justifyContent: "center",
            width: "100%", padding: "0 24px",
          }}>
            {finished ? (
              restLeft !== null ? (
                <div style={{ textAlign: "center" }}>
                  <div style={{ fontSize: 18, fontWeight: 700, color: "var(--color-warn)", letterSpacing: "0.12em", marginBottom: 8 }}>REST</div>
                  <div style={{ fontSize: 110, fontWeight: 900, color: "var(--color-warn)", fontFamily: "monospace", letterSpacing: "-0.03em", lineHeight: 1, marginBottom: 20 }}>
                    {restLeft}
                  </div>
                  <div style={{ fontSize: 16, color: "var(--color-text-dim)" }}>Next set starting soon…</div>
                </div>
              ) : (
                <div style={{ textAlign: "center" }}>
                  <div style={{ fontSize: 72, marginBottom: 16 }}>✅</div>
                  <div style={{ fontSize: 38, fontWeight: 800, color: "#4ade80" }}>Set Complete!</div>
                  <div style={{ fontSize: 24, color: "var(--color-text-dim)", marginTop: 12 }}>
                    {set.reps} rep{set.reps !== 1 ? "s" : ""} done
                  </div>
                </div>
              )
            ) : (
              <>
                <div style={{ fontSize: 28, fontWeight: 700, color: "var(--color-text-dim)", marginBottom: 16, letterSpacing: "0.06em" }}>
                  REP <span style={{ color: "var(--color-text)" }}>{repIdx + 1}</span>
                  {" "}<span style={{ color: "var(--color-border)" }}>OF</span>{" "}
                  {set.reps}
                </div>
                {hasTimer ? (
                  <div style={{
                    fontSize: 110, fontWeight: 900,
                    color: running ? "#ffffff" : "var(--color-border)",
                    fontFamily: "monospace", letterSpacing: "-0.03em", lineHeight: 1,
                    marginBottom: 28, transition: "color 0.25s",
                  }}>
                    {fmtTime(secsLeft)}
                  </div>
                ) : (
                  <div style={{ fontSize: 26, color: "var(--color-border-strong)", marginBottom: 28, fontStyle: "italic" }}>
                    No interval — swim easy
                  </div>
                )}
                <div style={{ marginBottom: 8 }}>{repDots}</div>
                {splitFeedback && <div style={{ marginTop: 12 }}>{splitFeedback}</div>}
              </>
            )}
          </div>

          {/* Controls */}
          <div style={{ width: "100%", padding: "0 20px 40px", display: "flex", flexDirection: "column", gap: 14 }}>
            {controls}
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  REST PICKER MODAL — shown when tapping "Run Workout"
    // ═══════════════════════════════════════════════════════════════

    const REST_OPTIONS = [
      { label: "Manual", value: null },
      { label: "0s",     value: 0 },
      { label: "30s",    value: 30 },
      { label: "45s",    value: 45 },
      { label: "60s",    value: 60 },
    ];

    // ═══════════════════════════════════════════════════════════════
    //  PROFILE / ACCOUNT MODAL — identity, stats, active sessions
    // ═══════════════════════════════════════════════════════════════
    // Goal metric metadata — shared by ProfileModal (set/edit) and the
    // generator stats panel (progress bars).
    const GOAL_METRICS = [
      { id: "workouts_per_week", label: "Workouts / week", unit: "workouts", period: "week",  defaultTarget: 3 },
      { id: "yards_per_week",    label: "Yards / week",    unit: "yds",      period: "week",  defaultTarget: 8000 },
      { id: "yards_per_month",   label: "Yards / month",   unit: "yds",      period: "month", defaultTarget: 32000 },
    ];

    // Single editable row used in the Profile modal's Identity panel.
    // Click "Edit" → input + Save/Cancel. onSave throws on validation error.
    // JoinGroupSection (R-F) — swimmer-side code redemption in Profile modal.
    // Open to all authenticated users; surfaces specific errors (DOB required,
    // #33 conflict, already-in-group, expired, etc.). On success, parent
    // refreshes me + history.
    function JoinGroupSection({ me, setMe, onJoined }) {
      const [code,     setCode]    = React.useState("");
      const [preview,  setPreview] = React.useState(null);                       // {group_name, intended_role, expires_at, is_expired, is_redeemed}
      const [result,   setResult]  = React.useState(null);
      const [err,      setErr]     = React.useState(null);
      const [needsDob, setNeedsDob] = React.useState(false);
      const [dobDraft, setDobDraft] = React.useState("");
      const [busy,     setBusy]    = React.useState(false);

      const normalizedCode = code.trim().toLowerCase();

      const doPreview = async () => {
        if (!normalizedCode) { setErr("Enter a code first"); return; }
        setErr(null); setResult(null);
        try {
          const res = await fetch(`/api/join-tokens/${encodeURIComponent(normalizedCode)}/preview`, { cache: "no-store" });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          const p = await res.json();
          if (p.is_redeemed) throw new Error("That code has already been redeemed.");
          if (p.is_expired)  throw new Error("That code has expired.");
          setPreview(p);
        } catch (e) { setErr(e.message); setPreview(null); }
      };

      const submitDob = async () => {
        if (!dobDraft) { setErr("Enter your date of birth"); return; }
        setBusy(true); setErr(null);
        try {
          const res = await fetch("/api/me/dob", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ dob: dobDraft }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || j.reason || `HTTP ${res.status}`);
          }
          setMe(prev => prev ? { ...prev, dob: dobDraft } : prev);
          setNeedsDob(false);
          setDobDraft("");
          // Immediately retry the redeem now that DOB is set.
          await doRedeem();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const doRedeem = async () => {
        setBusy(true); setErr(null);
        try {
          const res = await fetch(`/api/join-tokens/${encodeURIComponent(normalizedCode)}/redeem`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    "{}",
          });
          const j = await res.json();
          if (!res.ok) {
            if (j.reason === "dob_required" || j.error === "dob_required") {
              setNeedsDob(true);
              return;
            }
            if (j.reason === "primary_in_other_group_same_coach") {
              throw new Error(`You're already a primary in another of this coach's groups: ${j.conflict_group_name}. Leave that group first, or ask the coach to issue a 'secondary' code instead.`);
            }
            if (j.reason === "already_in_group")  throw new Error("You're already a member of this group.");
            if (j.reason === "already_redeemed")  throw new Error("That code has already been redeemed.");
            if (j.reason === "expired")           throw new Error("That code has expired.");
            if (j.reason === "group_archived")    throw new Error("That group has been archived.");
            if (j.reason === "invalid_token")     throw new Error("That code isn't valid.");
            throw new Error(j.error || j.reason || `HTTP ${res.status}`);
          }
          setResult(j);
          setCode("");
          setPreview(null);
          if (onJoined) onJoined(j);
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      return (
        <div style={{ marginTop: 18, padding: 14, background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 8 }}>
          <h4 style={{ color: "var(--color-text)", margin: 0, marginBottom: 6, fontSize: 14 }}>Join a group</h4>
          <p style={{ color: "var(--color-text-muted)", fontSize: 12, margin: "0 0 10px", lineHeight: 1.5 }}>
            Got a join code from a coach? Paste it here. Once you join, workouts the coach generates for the group will show up in your "📥 Assigned to me" view.
          </p>

          {result ? (
            <div style={{ padding: 10, background: "#22c55e22", border: "1px solid #22c55e", borderRadius: 6 }}>
              <div style={{ color: "var(--color-positive)", fontWeight: 700, fontSize: 13 }}>✓ Joined {result.group_name}</div>
              <div style={{ color: "var(--color-text-muted)", fontSize: 11, marginTop: 4 }}>Role: <strong>{result.role}</strong>.</div>
              <button onClick={() => setResult(null)}
                style={{ marginTop: 8, padding: "4px 10px", background: "transparent", color: "var(--color-text-muted)", border: "1px solid var(--color-border)", borderRadius: 4, fontSize: 11, cursor: "pointer" }}>
                Dismiss
              </button>
            </div>
          ) : needsDob ? (
            <div style={{ padding: 10, background: "#f59e0b11", border: "1px solid var(--color-warn)", borderRadius: 6 }}>
              <div style={{ color: "var(--color-warn)", fontWeight: 700, fontSize: 13, marginBottom: 6 }}>Date of birth required</div>
              <div style={{ color: "var(--color-text-muted)", fontSize: 11, marginBottom: 8, lineHeight: 1.5 }}>
                Joining a group triggers minor-protection rules that depend on your age. Please add your DOB to continue.
              </div>
              <div style={{ display: "flex", gap: 6 }}>
                <input type="date" value={dobDraft} onChange={e => setDobDraft(e.target.value)}
                  min={DOB_MIN} max={DOB_MAX_TODAY()}
                  style={{ flex: 1, padding: "6px 10px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
                <button onClick={submitDob} disabled={busy || !dobDraft}
                  style={{ padding: "6px 14px", background: dobDraft ? "var(--color-positive)" : "var(--color-border-strong)", color: dobDraft ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: dobDraft ? "pointer" : "not-allowed" }}>
                  Save & retry
                </button>
              </div>
            </div>
          ) : (
            <>
              <div style={{ display: "flex", gap: 6, marginBottom: 6 }}>
                <input value={code} onChange={e => { setCode(e.target.value); setPreview(null); setErr(null); }}
                  placeholder="Paste 10-character code"
                  onKeyDown={e => { if (e.key === "Enter") { preview ? doRedeem() : doPreview(); } }}
                  style={{ flex: 1, padding: "6px 10px", fontSize: 13, fontFamily: "monospace", letterSpacing: "0.08em", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, textTransform: "lowercase" }} />
                {!preview && (
                  <button onClick={doPreview} disabled={!normalizedCode || busy}
                    style={{ padding: "6px 14px", background: normalizedCode ? "var(--color-primary)" : "var(--color-border-strong)", color: normalizedCode ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: normalizedCode ? "pointer" : "not-allowed" }}>
                    Look up
                  </button>
                )}
              </div>
              {preview && (
                <div style={{ padding: 10, background: "var(--color-card)", border: "1px solid var(--color-primary)", borderRadius: 6, marginBottom: 6 }}>
                  <div style={{ fontSize: 12, color: "#cbd5e1", marginBottom: 6 }}>
                    Joining <strong style={{ color: "var(--color-text)" }}>{preview.group_name}</strong> as <strong style={{ color: "var(--color-primary)" }}>{preview.intended_role}</strong>.
                  </div>
                  <div style={{ display: "flex", gap: 6 }}>
                    <button onClick={doRedeem} disabled={busy}
                      style={{ padding: "6px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                      {busy ? "Joining…" : "Join group"}
                    </button>
                    <button onClick={() => { setPreview(null); setCode(""); }}
                      style={{ padding: "6px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 5, fontSize: 12, cursor: "pointer" }}>
                      Cancel
                    </button>
                  </div>
                </div>
              )}
              {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 6 }}>{err}</div>}
            </>
          )}
        </div>
      );
    }

    // Profile gender row — matches EditableProfileField row layout but uses a
    // select. Saves through PATCH /api/me (same endpoint, gender is in the
    // allowed-fields list per migration 014 / db.js dbUpdateMe).
    function ProfileGenderRow({ me, setMe, onProfileChange }) {
      const [editing, setEditing] = React.useState(false);
      const [draft,   setDraft]   = React.useState("");
      const [saving,  setSaving]  = React.useState(false);
      const [error,   setError]   = React.useState(null);
      const startEdit = () => { setDraft(me?.gender || ""); setError(null); setEditing(true); };
      const commit = async () => {
        setSaving(true); setError(null);
        try {
          const res = await fetch("/api/me", {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ gender: draft || null }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setMe(prev => prev ? { ...prev, gender: draft || null } : prev);
          if (onProfileChange) onProfileChange();
          setEditing(false);
        } catch (err) { setError(err.message || String(err)); }
        finally { setSaving(false); }
      };
      return (
        <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "4px 0" }}>
          <span style={{ color: "var(--color-text-dim)", width: 100, flexShrink: 0 }}>Gender:</span>
          {editing ? (
            <>
              <select value={draft} onChange={e => setDraft(e.target.value)} autoFocus
                style={{ flex: 1, minWidth: 0, padding: "5px 9px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-primary)", borderRadius: 5, outline: "none" }}>
                {GENDER_OPTIONS.map(o => <option key={o.v} value={o.v}>{o.label}</option>)}
              </select>
              <button onClick={commit} disabled={saving}
                style={{ padding: "5px 11px", borderRadius: 5, border: "none", background: saving ? "#1e3a5f" : "var(--color-primary)", color: "#fff", fontSize: 12, fontWeight: 700, cursor: saving ? "default" : "pointer" }}>
                {saving ? "…" : "Save"}
              </button>
              <button onClick={() => setEditing(false)} disabled={saving}
                style={{ padding: "5px 9px", borderRadius: 5, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-dim)", fontSize: 11, cursor: "pointer" }}>
                Cancel
              </button>
              {error && <span style={{ color: "#fca5a5", fontSize: 11 }}>{error}</span>}
            </>
          ) : (
            <>
              <span style={{ flex: 1, color: "var(--color-text)", fontWeight: 600 }}>
                {me?.gender ? genderLabel(me.gender) : <span style={{ color: "var(--color-border-strong)", fontStyle: "italic", fontWeight: 400 }}>not set</span>}
              </span>
              <button onClick={startEdit}
                style={{ padding: "3px 9px", borderRadius: 5, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>
                Edit
              </button>
            </>
          )}
        </div>
      );
    }

    function EditableProfileField({ label, value, placeholder, maxLength, type = "text", transform, badge, onSave }) {
      const [editing, setEditing] = React.useState(false);
      const [draft, setDraft]     = React.useState("");
      const [saving, setSaving]   = React.useState(false);
      const [error, setError]     = React.useState(null);
      const startEdit = () => {
        setDraft(value || "");
        setError(null);
        setEditing(true);
      };
      const commit = async () => {
        const v = transform ? transform(draft) : draft;
        setSaving(true); setError(null);
        try {
          await onSave(v.trim());
          setEditing(false);
        } catch (err) {
          setError(err.message || String(err));
        } finally {
          setSaving(false);
        }
      };
      return (
        <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "4px 0" }}>
          <span style={{ color: "var(--color-text-dim)", width: 100, flexShrink: 0 }}>{label}:</span>
          {editing ? (
            <>
              <input
                type={type}
                value={draft}
                autoFocus
                maxLength={maxLength}
                placeholder={placeholder}
                onChange={e => setDraft(e.target.value)}
                onKeyDown={e => { if (e.key === "Enter") commit(); if (e.key === "Escape") setEditing(false); }}
                style={{
                  flex: 1, minWidth: 0,
                  padding: "5px 9px", fontSize: 13,
                  background: "var(--color-bg)", color: "var(--color-text)",
                  border: "1px solid var(--color-primary)", borderRadius: 5, outline: "none",
                }} />
              <button onClick={commit} disabled={saving}
                style={{ padding: "5px 11px", borderRadius: 5, border: "none",
                  background: saving ? "#1e3a5f" : "var(--color-primary)", color: "#fff",
                  fontSize: 12, fontWeight: 700, cursor: saving ? "default" : "pointer" }}>
                {saving ? "…" : "Save"}
              </button>
              <button onClick={() => setEditing(false)} disabled={saving}
                className="btn btn-sm btn-outlined btn-neutral">
                Cancel
              </button>
            </>
          ) : (
            <>
              <span style={{ flex: 1, color: "var(--color-text)", fontWeight: 600, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                {value || <span style={{ color: "var(--color-border-strong)", fontStyle: "italic", fontWeight: 400 }}>{placeholder || "—"}</span>}
                {badge}
              </span>
              <button onClick={startEdit}
                className="btn btn-sm btn-outlined btn-primary">
                Edit
              </button>
            </>
          )}
          {error && (
            <span style={{ color: "#fca5a5", fontSize: 11, marginLeft: 8 }}>{error}</span>
          )}
        </div>
      );
    }

    function GoalRow({ metric, value, onSave, onDelete }) {
      const [editing, setEditing] = React.useState(false);
      const [draft, setDraft]     = React.useState("");
      const startEdit = () => {
        setDraft(String(value ?? metric.defaultTarget));
        setEditing(true);
      };
      const commit = () => {
        const v = parseInt(draft, 10);
        if (v > 0) onSave(v);
        setEditing(false);
      };
      const isSet = value != null;
      return (
        <div className="card" style={{
          display: "flex", alignItems: "center", gap: 10,
          borderRadius: 8, padding: "8px 12px",
        }}>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>{metric.label}</div>
            {!editing && isSet && (
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 2 }}>
                Target: <span style={{ color: "var(--color-primary)", fontWeight: 700 }}>{value.toLocaleString()}</span> {metric.unit}
              </div>
            )}
          </div>
          {editing ? (
            <>
              <input type="number" autoFocus value={draft} min={1}
                onChange={e => setDraft(e.target.value)}
                onKeyDown={e => { if (e.key === "Enter") commit(); if (e.key === "Escape") setEditing(false); }}
                style={{
                  width: 80, padding: "4px 8px", fontSize: 13,
                  background: "var(--color-bg)", color: "var(--color-text)",
                  border: "1px solid var(--color-primary)", borderRadius: 6, outline: "none",
                  fontFamily: "monospace", textAlign: "right",
                }} />
              <button onClick={commit}
                style={{ padding: "4px 10px", borderRadius: 6, border: "none",
                  background: "var(--color-primary)", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                Save
              </button>
              <button onClick={() => setEditing(false)}
                className="btn btn-sm btn-outlined btn-neutral">
                Cancel
              </button>
            </>
          ) : isSet ? (
            <>
              <button onClick={startEdit}
                style={{ padding: "4px 10px", borderRadius: 6,
                  border: "1px solid var(--color-border)", background: "transparent",
                  color: "#cbd5e1", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                Edit
              </button>
              <button onClick={onDelete}
                title="Remove this goal"
                style={{ padding: "4px 8px", borderRadius: 6,
                  border: "1px solid var(--color-border)", background: "transparent",
                  color: "#fca5a5", fontSize: 11, cursor: "pointer" }}>
                ✕
              </button>
            </>
          ) : (
            <button onClick={startEdit}
              style={{ padding: "4px 12px", borderRadius: 6, border: "1px dashed var(--color-border-strong)",
                background: "transparent", color: "var(--color-primary)",
                fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
              + Set a goal
            </button>
          )}
        </div>
      );
    }

    // N5: PhaseRow lives in the Profile modal Goals section. Five buttons,
    // single-select; clicking the active one clears the phase.
    function PhaseRow({ value, onChange }) {
      const selected = value && PHASES[value] ? PHASES[value] : null;
      return (
        <div className="card" style={{
          display: "flex", flexDirection: "column", gap: 8,
          borderRadius: 8, padding: "8px 12px",
        }}>
          <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>📅 Training phase</div>
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 2 }}>
                {selected
                  ? <><span style={{ color: selected.color, fontWeight: 700 }}>{selected.emoji} {selected.label}</span> · {selected.description}</>
                  : <span style={{ fontStyle: "italic" }}>No phase set — generator runs without phase bias.</span>}
              </div>
            </div>
          </div>
          <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
            {PHASE_ORDER.map(pid => {
              const p = PHASES[pid];
              const active = value === pid;
              return (
                <button key={pid}
                  onClick={() => onChange(active ? null : pid)}
                  title={`${p.label} — ${p.description}`}
                  className="pill"
                  style={{
                    border: `1px solid ${active ? p.color : "var(--color-border)"}`,
                    background: active ? `${p.color}22` : "transparent",
                    color: active ? p.color : "var(--color-text-muted)",
                    fontSize: 12, cursor: "pointer",
                    display: "inline-flex", alignItems: "center", gap: 4,
                  }}>
                  <span>{p.emoji}</span>
                  <span>{p.label}</span>
                </button>
              );
            })}
          </div>
        </div>
      );
    }

    // N7: BenchmarksSection lives in the Profile modal Goals area. Logs
    // standard test-set results (T-30, 500 TT, Broken 500) and auto-fills
    // the generator paceInput from the latest t30/tt500 (broken500 is
    // sprint-focused so does NOT auto-fill aerobic pace).
    function BenchmarksSection({ onPaceUpdate }) {
      const [list,        setList]        = React.useState(null);
      const [kind,        setKind]        = React.useState("t30");
      const [poolMode,    setPoolModeBM]  = React.useState("25y");
      // T-30 field: total distance in yards/meters
      const [t30Distance, setT30Distance] = React.useState("");
      // TT500 field: total time as M:SS string
      const [tt500Time,   setTt500Time]   = React.useState("");
      // Broken500 field: total time as M:SS string
      const [broken500Time, setBroken500Time] = React.useState("");
      const [notes,       setNotes]       = React.useState("");
      const [busy,        setBusy]        = React.useState(false);
      const [err,         setErr]         = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch("/api/benchmarks", { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setList(await res.json());
        } catch (e) { setList([]); setErr(e.message); }
      }, []);
      React.useEffect(() => { load(); }, [load]);

      const parseMSS = (s) => {
        const m = String(s || "").trim().match(/^(\d{1,2}):(\d{2})$/);
        if (!m) return null;
        const secs = parseInt(m[1], 10) * 60 + parseInt(m[2], 10);
        if (secs < 30 || secs > 1800) return null;
        return secs;
      };
      const fmtSecs = (s) => {
        if (s == null) return "—";
        const m = Math.floor(s / 60);
        const ss = String(s % 60).padStart(2, "0");
        return `${m}:${ss}`;
      };
      const fmtPace100 = (s) => fmtSecs(s) + "/100";

      const KIND_META = {
        t30:       { emoji: "🕒", label: "T-30",       desc: "30-min continuous swim — record total distance" },
        tt500:     { emoji: "🏁", label: "500 TT",     desc: "500 time trial — record total time" },
        broken500: { emoji: "💥", label: "Broken 500", desc: "10×50 on a generous interval — record total time" },
      };

      const submit = async () => {
        setErr(null);
        const body = { kind, pool_mode: poolMode, notes: notes.trim() || null };
        if (kind === "t30") {
          const yds = parseInt(t30Distance, 10);
          if (!Number.isFinite(yds) || yds < 100 || yds > 5000) { setErr("Enter total distance between 100 and 5000"); return; }
          body.total_yards = yds;
        } else if (kind === "tt500") {
          const secs = parseMSS(tt500Time);
          if (!secs) { setErr("Enter time as M:SS (3:00 to 30:00)"); return; }
          body.total_secs = secs;
        } else {
          const secs = parseMSS(broken500Time);
          if (!secs) { setErr("Enter time as M:SS (3:00 to 30:00)"); return; }
          body.total_secs = secs;
        }
        setBusy(true);
        try {
          const res = await fetch("/api/benchmarks", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify(body),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          // Auto-fill paceInput from t30/tt500 (not broken500).
          if ((kind === "t30" || kind === "tt500") && j.pace_100_secs && onPaceUpdate) {
            onPaceUpdate(fmtSecs(j.pace_100_secs));
          }
          // Reset inputs.
          setT30Distance(""); setTt500Time(""); setBroken500Time(""); setNotes("");
          await load();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const remove = async (id) => {
        if (!window.confirm("Delete this benchmark entry?")) return;
        try {
          const res = await fetch(`/api/benchmarks/${id}`, { method: "DELETE", headers: { ...csrfHeaders() } });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      return (
        <div className="card" style={{
          display: "flex", flexDirection: "column", gap: 8,
          borderRadius: 8, padding: "8px 12px",
        }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
            <div style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>🎯 Benchmarks</div>
            <span style={{ fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic" }}>{list ? `${list.length} recorded` : "…"}</span>
          </div>
          <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: -4 }}>
            Standard test-set results. T-30 / 500 TT auto-fill your generator pace.
          </div>

          {/* Kind picker */}
          <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
            {["t30","tt500","broken500"].map(k => {
              const m = KIND_META[k];
              const active = kind === k;
              return (
                <button key={k} onClick={() => setKind(k)} title={m.desc}
                  style={{
                    padding: "4px 9px", borderRadius: 999,
                    border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`,
                    background: active ? "rgba(245,158,11,0.18)" : "transparent",
                    color: active ? "var(--color-warn)" : "var(--color-text-muted)",
                    fontSize: 11, fontWeight: 700, cursor: "pointer",
                  }}>
                  {m.emoji} {m.label}
                </button>
              );
            })}
          </div>

          {/* Kind-specific input */}
          <div style={{ display: "flex", gap: 6, flexWrap: "wrap", alignItems: "center" }}>
            {kind === "t30" && (
              <>
                <input type="number" min="100" max="5000" step="25"
                  value={t30Distance} onChange={(e) => setT30Distance(e.target.value)}
                  placeholder="Total yards (e.g. 1850)"
                  style={{ flex: 2, minWidth: 140, padding: "6px 8px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 12 }} />
                {Number.isFinite(parseInt(t30Distance, 10)) && parseInt(t30Distance, 10) > 0 && (
                  <span style={{ fontSize: 11, color: "var(--color-text-dim)", fontFamily: "ui-monospace, monospace" }}>
                    → {fmtSecs(Math.round(1800 * 100 / Math.max(1, parseInt(t30Distance, 10))))}/100
                  </span>
                )}
              </>
            )}
            {(kind === "tt500" || kind === "broken500") && (
              <>
                <input type="text" value={kind === "tt500" ? tt500Time : broken500Time}
                  onChange={(e) => kind === "tt500" ? setTt500Time(e.target.value) : setBroken500Time(e.target.value)}
                  placeholder="M:SS (e.g. 6:30)"
                  style={{ flex: 2, minWidth: 140, padding: "6px 8px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 12, fontFamily: "ui-monospace, monospace" }} />
                {parseMSS(kind === "tt500" ? tt500Time : broken500Time) && (
                  <span style={{ fontSize: 11, color: "var(--color-text-dim)", fontFamily: "ui-monospace, monospace" }}>
                    → {fmtSecs(Math.round(parseMSS(kind === "tt500" ? tt500Time : broken500Time) * 100 / 500))}/100
                  </span>
                )}
              </>
            )}
            <select value={poolMode} onChange={(e) => setPoolModeBM(e.target.value)}
              style={{ padding: "6px 8px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 12 }}>
              <option value="25y">25y</option>
              <option value="25m">25m</option>
              <option value="50m">50m</option>
            </select>
            <button onClick={submit} disabled={busy}
              className="btn btn-md btn-filled btn-primary">
              {busy ? "…" : "Record"}
            </button>
          </div>
          <input type="text" value={notes} onChange={(e) => setNotes(e.target.value)}
            placeholder="Optional notes"
            maxLength={500}
            style={{ padding: "5px 8px", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 11 }} />
          {err && <div style={{ color: "#fca5a5", fontSize: 11 }}>{err}</div>}

          {/* List */}
          {list && list.length > 0 && (
            <div style={{ marginTop: 4 }}>
              {list.slice(0, 6).map(b => {
                const m = KIND_META[b.kind];
                return (
                  <div key={b.id} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 6, padding: "5px 8px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 5, marginBottom: 3, fontSize: 11 }}>
                    <div style={{ display: "flex", alignItems: "center", gap: 6, flex: 1, minWidth: 0 }}>
                      <span style={{ color: "var(--color-text-muted)" }}>{m ? m.emoji : "•"}</span>
                      <span style={{ color: "var(--color-text)", fontWeight: 600 }}>{m ? m.label : b.kind}</span>
                      <span style={{ color: "var(--color-text-dim)" }}>·</span>
                      <span style={{ color: "var(--color-warn)", fontFamily: "ui-monospace, monospace" }}>{fmtPace100(b.pace_100_secs)}</span>
                      <span style={{ color: "var(--color-text-dim)" }}>·</span>
                      <span style={{ color: "var(--color-text-muted)", fontSize: 10 }}>{b.recorded_at ? new Date(b.recorded_at).toLocaleDateString() : ""}</span>
                      <span style={{ color: "var(--color-border-strong)", fontSize: 10 }}>({b.pool_mode})</span>
                    </div>
                    <button onClick={() => remove(b.id)}
                      title="Delete this entry"
                      style={{ padding: "2px 7px", background: "transparent", border: "1px solid var(--color-border-strong)", color: "var(--color-text-muted)", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>×</button>
                  </div>
                );
              })}
              {list.length > 6 && (
                <div style={{ fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic", marginTop: 3 }}>
                  Showing 6 most recent of {list.length} total.
                </div>
              )}
            </div>
          )}
        </div>
      );
    }

    // J: LevelRow lives in the Profile modal Goals section. Three buttons,
    // single-select; clicking the active one clears the level. Picking a
    // level immediately overwrites paceInput with the level's preset pace
    // (handled in the caller via onChange's second arg).
    function LevelRow({ value, onChange }) {
      const selected = value && LEVEL_PRESETS[value] ? LEVEL_PRESETS[value] : null;
      return (
        <div className="card" style={{
          display: "flex", flexDirection: "column", gap: 8,
          borderRadius: 8, padding: "8px 12px",
        }}>
          <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>🎯 Swimmer level</div>
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 2 }}>
                {selected
                  ? <><span style={{ color: "var(--color-warn)", fontWeight: 700 }}>{selected.emoji} {selected.label}</span> · {selected.description} · presets pace to {selected.pace}/100</>
                  : <span style={{ fontStyle: "italic" }}>No level set — pick one to auto-fill your pace.</span>}
              </div>
            </div>
          </div>
          <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
            {LEVEL_ORDER.map(lid => {
              const l = LEVEL_PRESETS[lid];
              const active = value === lid;
              return (
                <button key={lid}
                  onClick={() => onChange(active ? null : lid, active ? null : l.pace)}
                  title={`${l.label} — pace ${l.pace}/100 · ${l.description}`}
                  className="pill"
                  style={{
                    border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`,
                    background: active ? "rgba(245,158,11,0.18)" : "transparent",
                    color: active ? "var(--color-warn)" : "var(--color-text-muted)",
                    fontSize: 12, cursor: "pointer",
                    display: "inline-flex", alignItems: "center", gap: 4,
                  }}>
                  <span>{l.emoji}</span>
                  <span>{l.label}</span>
                  <span style={{ fontFamily: "ui-monospace, SFMono-Regular, monospace", fontWeight: 500, marginLeft: 2 }}>· {l.pace}</span>
                </button>
              );
            })}
          </div>
        </div>
      );
    }

    function NextEventRow({ value, onSave, onClear }) {
      const [editing, setEditing] = React.useState(false);
      const [draftName, setDraftName] = React.useState("");
      const [draftDate, setDraftDate] = React.useState("");
      const startEdit = () => {
        setDraftName(value?.name || "");
        setDraftDate(value?.date || "");
        setEditing(true);
      };
      const commit = async () => {
        const ok = await onSave(draftName, draftDate);
        if (ok) setEditing(false);
      };
      const isSet = value && value.name && value.date;
      // Display "N days" relative to today for the read-only view.
      const daysLabel = (() => {
        if (!isSet) return null;
        const today = new Date(); today.setHours(0,0,0,0);
        const target = new Date(`${value.date}T00:00:00`);
        if (isNaN(target)) return null;
        const d = Math.round((target - today) / 86400000);
        if (d < 0) return `${Math.abs(d)}d ago`;
        if (d === 0) return "today!";
        if (d === 1) return "tomorrow";
        return `${d} days`;
      })();
      return (
        <div className="card" style={{
          display: "flex", alignItems: "center", gap: 10,
          borderRadius: 8, padding: "8px 12px",
        }}>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ fontSize: 13, color: "var(--color-text)", fontWeight: 600 }}>🏁 Next event</div>
            {!editing && isSet && (
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                <span style={{ color: "var(--color-primary)", fontWeight: 700 }}>{value.name}</span>
                <span> · {value.date}</span>
                {daysLabel && <span style={{ color: "var(--color-border-strong)" }}> · {daysLabel}</span>}
              </div>
            )}
          </div>
          {editing ? (
            <>
              <input type="text" autoFocus value={draftName}
                placeholder="Event name"
                maxLength={80}
                onChange={e => setDraftName(e.target.value)}
                onKeyDown={e => { if (e.key === "Enter") commit(); if (e.key === "Escape") setEditing(false); }}
                style={{
                  width: 140, padding: "4px 8px", fontSize: 13,
                  background: "var(--color-bg)", color: "var(--color-text)",
                  border: "1px solid var(--color-primary)", borderRadius: 6, outline: "none",
                }} />
              <input type="date" value={draftDate}
                onChange={e => setDraftDate(e.target.value)}
                onKeyDown={e => { if (e.key === "Enter") commit(); if (e.key === "Escape") setEditing(false); }}
                style={{
                  padding: "4px 6px", fontSize: 13,
                  background: "var(--color-bg)", color: "var(--color-text)",
                  border: "1px solid var(--color-primary)", borderRadius: 6, outline: "none",
                  colorScheme: "dark",
                }} />
              <button onClick={commit}
                style={{ padding: "4px 10px", borderRadius: 6, border: "none",
                  background: "var(--color-primary)", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                Save
              </button>
              <button onClick={() => setEditing(false)}
                className="btn btn-sm btn-outlined btn-neutral">
                Cancel
              </button>
            </>
          ) : isSet ? (
            <>
              <button onClick={startEdit}
                className="btn btn-sm btn-outlined btn-neutral">
                Edit
              </button>
              <button onClick={onClear}
                title="Remove this event"
                className="btn btn-sm btn-outlined btn-destructive">
                ✕
              </button>
            </>
          ) : (
            <button onClick={startEdit}
              style={{ padding: "4px 12px", borderRadius: 6, border: "1px dashed var(--color-border-strong)",
                background: "transparent", color: "var(--color-primary)",
                fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
              + Set an event
            </button>
          )}
        </div>
      );
    }

    // Session 5: user → admin feedback modal. Auto-captures page + user agent.
    // Cat Phase II (2026-05-15): accepts initialCategory/initialSubject/initialBody
    // so the catalog's per-option flag button can prefill an option-identity report.
    function FeedbackModal({ onClose, initialCategory = "idea", initialSubject = "", initialBody = "" }) {
      const [category, setCategory] = React.useState(initialCategory);
      const [subject, setSubject]   = React.useState(initialSubject);
      const [body, setBody]         = React.useState(initialBody);
      const [state, setState]       = React.useState("idle"); // idle | sending | sent | error
      const [error, setError]       = React.useState(null);
      const canSubmit = subject.trim().length > 0 && body.trim().length > 0 && state !== "sending";

      const submit = async () => {
        setState("sending"); setError(null);
        try {
          const res = await fetch("/api/feedback", {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({
              category,
              subject: subject.trim(),
              body:    body.trim(),
              page:    typeof window !== "undefined" ? window.location.pathname : null,
              user_agent: typeof navigator !== "undefined" ? navigator.userAgent : null,
            }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setState("sent");
          setTimeout(onClose, 1100);
        } catch (err) {
          setState("error");
          setError(err.message || String(err));
        }
      };

      const cats = [
        { id: "idea",         label: "💡 Idea" },
        { id: "bug",          label: "🐛 Bug" },
        { id: "praise",       label: "✨ Praise" },
        { id: "catalog-flag", label: "🚩 Catalog flag" },
        { id: "other",        label: "Other" },
      ];

      return (
        <div onClick={onClose} className="modal-overlay" style={{ padding: 16, fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" }}>
          <div onClick={e => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            maxWidth: 480, width: "100%", maxHeight: "85vh", overflowY: "auto",
            color: "#cbd5e1",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "16px 20px", borderBottom: "1px solid var(--color-border)" }}>
              <div style={{ fontSize: 16, fontWeight: 700, color: "var(--color-text)" }}>💬 Send feedback</div>
              <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: "none", color: "var(--color-text-muted)", fontSize: 20, cursor: "pointer", padding: 4 }}>✕</button>
            </div>

            {state === "sent" ? (
              <div style={{ padding: "40px 24px", textAlign: "center" }}>
                <div style={{ fontSize: 48, marginBottom: 8 }}>✅</div>
                <div style={{ fontSize: 16, fontWeight: 700, color: "#4ade80" }}>Thanks — feedback sent.</div>
              </div>
            ) : (
              <div style={{ padding: "16px 20px 20px" }}>
                <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 8 }}>Category</div>
                <div style={{ display: "flex", gap: 6, marginBottom: 14, flexWrap: "wrap" }}>
                  {cats.map(c => {
                    const active = category === c.id;
                    return (
                      <button key={c.id} onClick={() => setCategory(c.id)}
                        className="pill"
                        style={{
                          border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                          background: active ? "#1e3a5f" : "transparent",
                          color: active ? "var(--color-primary)" : "var(--color-text-muted)",
                          fontSize: 12, cursor: "pointer",
                        }}>
                        {c.label}
                      </button>
                    );
                  })}
                </div>

                <label style={{ display: "block", fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 4 }}>Subject</label>
                <input value={subject} maxLength={255}
                  onChange={e => setSubject(e.target.value)}
                  placeholder="One-line summary"
                  style={{
                    width: "100%", boxSizing: "border-box",
                    padding: "8px 10px", marginBottom: 14, fontSize: 13,
                    background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6,
                    color: "var(--color-text)", outline: "none",
                  }} />

                <label style={{ display: "block", fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 4 }}>Details</label>
                <textarea value={body} rows={6} maxLength={10000}
                  onChange={e => setBody(e.target.value)}
                  placeholder="What happened? What did you expect? Steps to reproduce if it's a bug."
                  style={{
                    width: "100%", boxSizing: "border-box",
                    padding: "8px 10px", marginBottom: 8, fontSize: 13,
                    background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6,
                    color: "var(--color-text)", outline: "none", fontFamily: "inherit", resize: "vertical",
                  }} />

                <div style={{ fontSize: 10, color: "var(--color-border-strong)", marginBottom: 14 }}>
                  Page and browser info are attached automatically.
                </div>

                {error && (
                  <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>{error}</div>
                )}

                <div style={{ display: "flex", gap: 10, justifyContent: "flex-end" }}>
                  <button onClick={onClose}
                    style={{
                      padding: "8px 14px", borderRadius: 8, border: "1px solid var(--color-border)",
                      background: "transparent", color: "var(--color-text-dim)",
                      fontSize: 13, fontWeight: 600, cursor: "pointer",
                    }}>
                    Cancel
                  </button>
                  <button onClick={submit} disabled={!canSubmit}
                    style={{
                      padding: "8px 18px", borderRadius: 8, border: "none",
                      background: canSubmit ? "var(--color-primary)" : "#1e3a5f",
                      color: "#fff", fontSize: 13, fontWeight: 700,
                      cursor: canSubmit ? "pointer" : "default",
                    }}>
                    {state === "sending" ? "Sending…" : "Send"}
                  </button>
                </div>
              </div>
            )}
          </div>
        </div>
      );
    }

    // W2 — Natural-language intent parser modal. Coach types a one-liner;
    // parseIntent extracts known tokens; user previews chips and clicks
    // Apply to push values into the generator form. Apply behavior is
    // owned by App via the onApply callback so the modal stays a pure UI.
    function IntentParserModal({ onClose, onApply }) {
      const [text, setText]   = React.useState("");
      const [parsed, setParsed] = React.useState(null); // { tokens, unparsed } | null

      const inputRef = React.useRef(null);
      React.useEffect(() => { if (inputRef.current) inputRef.current.focus(); }, []);

      const handleParse = () => {
        const result = parseIntent(text);
        setParsed(result);
      };

      const handleApply = () => {
        if (!parsed || !parsed.tokens.length) return;
        onApply(parsed);
        onClose();
      };

      const examples = [
        "easy 2k aerobic",
        "threshold 25 min @ 2:00",
        "im 2400 yards with paddles",
        "sprint 1500 m",
        "back 60 min",
        "recovery, just kicking",
      ];

      // Pretty-print a single token for the chip preview.
      const labelFor = (tok) => {
        switch (tok.kind) {
          case "type":      return `Type · ${tok.value}`;
          case "distance":  return `Distance · ${tok.value} ${tok.unit === "y" ? "yd" : "m"}`;
          case "duration":  return `Duration · ${tok.value} min`;
          case "pace":      return `Pace · ${Math.floor(tok.value/60)}:${String(tok.value%60).padStart(2,"0")}/100`;
          case "pace_ref":  return `Pace · ${tok.value}`;
          case "zone":      return `Zone · ${tok.value}`;
          case "stroke":    return `Stroke · ${tok.value}`;
          case "recovery":  return `Recovery mode`;
          case "equipment": return `Equipment · ${tok.value}`;
          default:          return `${tok.kind} · ${tok.value}`;
        }
      };

      const colorFor = (tok) => {
        switch (tok.kind) {
          case "type":      return { bg: "#1e3a8a", border: "var(--color-primary)", color: "#dbeafe" };
          case "distance":  return { bg: "#0c4a6e", border: "#0ea5e9", color: "#bae6fd" };
          case "duration":  return { bg: "#0c4a6e", border: "#0ea5e9", color: "#bae6fd" };
          case "pace":      return { bg: "#581c87", border: "#a855f7", color: "#e9d5ff" };
          case "pace_ref":  return { bg: "#581c87", border: "#a855f7", color: "#e9d5ff" };
          case "zone":      return { bg: "#365314", border: "#65a30d", color: "#d9f99d" };
          case "stroke":    return { bg: "var(--color-card)", border: "var(--color-border-strong)", color: "#cbd5e1" };
          case "recovery":  return { bg: "#064e3b", border: "#10b981", color: "#a7f3d0" };
          case "equipment": return { bg: "#7c2d12", border: "#ea580c", color: "#fed7aa" };
          default:          return { bg: "var(--color-border)", border: "var(--color-text-dim)", color: "#cbd5e1" };
        }
      };

      const canApply = parsed && parsed.tokens.length > 0;

      return (
        <div onClick={onClose}
          style={{
            position: "fixed", inset: 0, zIndex: 9000,
            background: "rgba(2,6,23,0.7)",
            display: "flex", alignItems: "flex-start", justifyContent: "center",
            padding: "10vh 16px",
          }}>
          <div onClick={e => e.stopPropagation()}
            style={{
              width: "100%", maxWidth: 520,
              background: "var(--color-bg)", border: "1px solid var(--color-card)",
              borderRadius: 14, padding: "22px 22px 18px",
              boxShadow: "0 24px 60px rgba(0,0,0,0.5)",
            }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 14 }}>
              <h3 style={{ color: "var(--color-text)", fontSize: 17, margin: 0, fontWeight: 700 }}>Quick Input</h3>
              <button onClick={onClose} aria-label="Close"
                style={{ background: "transparent", border: "none", color: "var(--color-text-dim)", fontSize: 22, cursor: "pointer", padding: 0, lineHeight: 1 }}>×</button>
            </div>
            <p style={{ color: "var(--color-text-muted)", fontSize: 12, margin: "0 0 10px", lineHeight: 1.4 }}>
              Type a coach-style one-liner. We'll extract what we recognize and let you confirm before applying.
            </p>

            <div style={{ display: "flex", gap: 8, marginBottom: 10 }}>
              <input
                ref={inputRef}
                type="text"
                value={text}
                onChange={e => { setText(e.target.value); setParsed(null); }}
                onKeyDown={e => { if (e.key === "Enter") handleParse(); if (e.key === "Escape") onClose(); }}
                placeholder='e.g. "easy 2k aerobic"'
                style={{
                  flex: 1, padding: "9px 12px", borderRadius: 8,
                  border: "1px solid var(--color-border)", background: "var(--color-card)",
                  color: "var(--color-text)", fontSize: 14, outline: "none",
                }}
              />
              <button onClick={handleParse}
                style={{
                  padding: "9px 16px", borderRadius: 8, border: "1px solid var(--color-primary)",
                  background: "#1d4ed8", color: "#fff", fontSize: 13, fontWeight: 700, cursor: "pointer",
                }}>Parse</button>
            </div>

            {/* Examples row — clickable to fill the input */}
            <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 14 }}>
              <span style={{ color: "var(--color-border-strong)", fontSize: 11, alignSelf: "center" }}>Try:</span>
              {examples.map(ex => (
                <button key={ex} onClick={() => { setText(ex); setParsed(null); }}
                  style={{
                    padding: "3px 9px", borderRadius: 12, border: "1px solid var(--color-border)",
                    background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer",
                    fontStyle: "italic",
                  }}>"{ex}"</button>
              ))}
            </div>

            {/* Parsed-tokens preview */}
            {parsed && (
              <div style={{ borderTop: "1px solid var(--color-card)", paddingTop: 12, marginBottom: 14 }}>
                <div style={{ color: "var(--color-text-dim)", fontSize: 10, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>
                  Parsed ({parsed.tokens.length})
                </div>
                {parsed.tokens.length === 0 ? (
                  <div style={{ color: "var(--color-text-muted)", fontSize: 13, fontStyle: "italic" }}>
                    Nothing recognized. Try one of the examples above, or check supported vocabulary in the manual.
                  </div>
                ) : (
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                    {parsed.tokens.map((tok, i) => {
                      const c = colorFor(tok);
                      return (
                        <span key={i} title={`Matched: "${tok.raw}"`}
                          style={{
                            padding: "4px 10px", borderRadius: 999,
                            background: c.bg, border: `1px solid ${c.border}`,
                            color: c.color, fontSize: 12, fontWeight: 600,
                          }}>
                          {labelFor(tok)}
                        </span>
                      );
                    })}
                  </div>
                )}
                {parsed.unparsed && (
                  <div style={{ marginTop: 8, color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>
                    Not recognized: "{parsed.unparsed}"
                  </div>
                )}
              </div>
            )}

            <div style={{ display: "flex", gap: 8, justifyContent: "flex-end" }}>
              <button onClick={onClose}
                style={{
                  padding: "8px 14px", borderRadius: 8, border: "1px solid var(--color-border)",
                  background: "transparent", color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600, cursor: "pointer",
                }}>Cancel</button>
              <button onClick={handleApply} disabled={!canApply}
                style={{
                  padding: "8px 18px", borderRadius: 8, border: "none",
                  background: canApply ? "#16a34a" : "var(--color-border)",
                  color: canApply ? "#fff" : "var(--color-text-dim)",
                  fontSize: 13, fontWeight: 700, cursor: canApply ? "pointer" : "not-allowed",
                }}>Apply</button>
            </div>
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  N6 — Multi-pace export
    //  Modal for picking pace source + render mode; print overlay that
    //  renders the same workout at N paces. Coach-only, group-only.
    // ═══════════════════════════════════════════════════════════════

    // Parse "M:SS" → seconds; null on bad input.
    function parsePaceMSS(str) {
      if (!str) return null;
      const m = String(str).trim().match(/^(\d{1,2}):(\d{2})$/);
      if (!m) return null;
      const secs = parseInt(m[1], 10) * 60 + parseInt(m[2], 10);
      if (secs < 30 || secs > 300) return null;
      return secs;
    }

    // v2.0 — Multi-lane multi-pace generate control. Sits below YardageSlider
    // when the viewer is a coach. Toggle ON expands to N lane rows; user
    // edits paces; the picker filters options that fit ALL lanes.
    // v3 impersonation — dedicated quick-start modal. Top-nav button opens
    // this; coach/support types a name/email fragment, picks a target, hits
    // Confirm. Fetches /api/admin/users (admin-gated server-side) so the
    // search list is the same source-of-truth as AdminUsers. Read-only safe.
    function ImpersonationStartModal({ me, onClose, onStartImpersonation }) {
      const [users,  setUsers]  = React.useState(null);
      const [query,  setQuery]  = React.useState("");
      const [picked, setPicked] = React.useState(null);
      const [err,    setErr]    = React.useState(null);
      const [busy,   setBusy]   = React.useState(false);
      React.useEffect(() => {
        fetch("/api/admin/users", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(setUsers)
          .catch(() => setUsers([]));
      }, []);
      const filtered = React.useMemo(() => {
        if (!users) return [];
        const q = query.trim().toLowerCase();
        if (!q) return users.slice(0, 20);
        return users.filter(u => {
          return (u.email || "").toLowerCase().includes(q)
            || (u.display_name || "").toLowerCase().includes(q)
            || (u.initials || "").toLowerCase().includes(q)
            || u.sub.toLowerCase().includes(q);
        }).slice(0, 20);
      }, [users, query]);
      const confirm = async () => {
        if (!picked) return;
        setBusy(true); setErr(null);
        try {
          await onStartImpersonation(picked.sub, picked.display_name || picked.initials || picked.email, picked.email);
          onClose();
        } catch (e) {
          setErr(e.message); setBusy(false);
        }
      };
      return (
        <div onClick={onClose} className="modal-overlay" style={{ padding: 20 }}>
          <div onClick={(e) => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            padding: 22, maxWidth: 560, width: "100%", maxHeight: "80vh", display: "flex", flexDirection: "column",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 }}>
              <div style={{ color: "#fff", fontSize: 16, fontWeight: 700 }}>🛂 Start impersonation</div>
              <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: "none", color: "var(--color-text-dim)", fontSize: 18, cursor: "pointer", padding: 0 }}>×</button>
            </div>
            <div style={{ fontSize: 12, color: "var(--color-text-dim)", marginBottom: 10 }}>
              30-min read-only session. Audit-logged. The user will NOT be notified.
            </div>
            <input type="text" value={query} onChange={e => { setQuery(e.target.value); setPicked(null); }}
              autoFocus placeholder="Search by email, name, initials, or sub…"
              style={{ width: "100%", padding: "8px 12px", borderRadius: 6, border: "1px solid var(--color-border-strong)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 13, marginBottom: 12 }} />
            {users === null ? (
              <div style={{ color: "var(--color-text-dim)", fontSize: 12 }}>Loading users…</div>
            ) : (
              <div style={{ overflowY: "auto", flex: 1, border: "1px solid var(--color-border)", borderRadius: 6 }}>
                {filtered.length === 0 ? (
                  <div style={{ padding: 16, color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>No matches.</div>
                ) : filtered.map(u => {
                  const isSelf = u.sub === me?.sub;
                  const isPicked = picked && picked.sub === u.sub;
                  return (
                    <div key={u.sub} onClick={() => !isSelf && setPicked(u)}
                      style={{
                        padding: "8px 12px", borderBottom: "1px solid var(--color-card)",
                        cursor: isSelf ? "not-allowed" : "pointer",
                        background: isPicked ? "rgba(220,38,38,0.18)" : "transparent",
                        opacity: isSelf ? 0.4 : 1,
                      }}>
                      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 8 }}>
                        <span style={{ color: "#fff", fontSize: 13, fontWeight: 600 }}>{u.display_name || u.initials || "—"}</span>
                        <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontFamily: "monospace" }}>{u.email || u.sub.slice(0, 16) + "…"}</span>
                      </div>
                      {isSelf && <div style={{ fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic" }}>cannot impersonate yourself</div>}
                    </div>
                  );
                })}
              </div>
            )}
            {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 8 }}>{err}</div>}
            <div style={{ display: "flex", justifyContent: "flex-end", gap: 8, marginTop: 14 }}>
              <button onClick={onClose} disabled={busy}
                style={{ padding: "8px 14px", borderRadius: 6, background: "var(--color-border)", border: "none", color: "var(--color-text)", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Cancel</button>
              <button onClick={confirm} disabled={!picked || busy}
                style={{ padding: "8px 14px", borderRadius: 6, background: picked ? "#dc2626" : "var(--color-border)", border: "none", color: "#fff", fontSize: 13, fontWeight: 700, cursor: picked ? "pointer" : "not-allowed" }}>
                {busy ? "Starting…" : "🛂 Impersonate"}
              </button>
            </div>
          </div>
        </div>
      );
    }

    // v3 impersonation banner. Persistent sticky red bar at the top of the
    // app while an impersonation session is active. Shows target identity +
    // a countdown to expiry + Exit button. Ticks every 5s. Auto-fires the
    // exit when the timer hits zero so the UI doesn't get stuck.
    function ImpersonationBanner({ state, onExit }) {
      const [now, setNow] = React.useState(() => Date.now());
      React.useEffect(() => {
        const id = setInterval(() => setNow(Date.now()), 5000);
        return () => clearInterval(id);
      }, []);
      const expiresMs = state.expires_at ? new Date(state.expires_at).getTime() : 0;
      const remainingSec = Math.max(0, Math.floor((expiresMs - now) / 1000));
      const mm = Math.floor(remainingSec / 60);
      const ss = remainingSec % 60;
      // Auto-exit on expiry — fire once when the timer crosses zero.
      const firedRef = React.useRef(false);
      React.useEffect(() => {
        if (remainingSec === 0 && !firedRef.current) {
          firedRef.current = true;
          onExit && onExit();
        }
      }, [remainingSec, onExit]);
      return (
        <div className="screen-only" style={{
          position: "sticky", top: 0, zIndex: 9100,
          background: "#dc2626", color: "#fff",
          padding: "8px 16px", fontSize: 12, fontWeight: 700,
          display: "flex", justifyContent: "center", alignItems: "center", gap: 12, flexWrap: "wrap",
          boxShadow: "0 2px 8px rgba(0,0,0,0.4)",
        }}>
          <span>🛂 Acting as <strong>{state.target_name || state.target_sub.slice(0, 8)}</strong>{state.target_email ? <span style={{ opacity: 0.8, fontWeight: 400 }}> ({state.target_email})</span> : null} — read-only</span>
          <span style={{ fontFamily: "monospace", background: "rgba(0,0,0,0.35)", padding: "2px 8px", borderRadius: 4 }}>
            {mm}:{String(ss).padStart(2, "0")} left
          </span>
          <button
            type="button"
            onClick={onExit}
            style={{
              background: "rgba(0,0,0,0.45)", color: "#fff",
              border: "1px solid rgba(255,255,255,0.6)", borderRadius: 4,
              padding: "3px 12px", fontSize: 11, fontWeight: 700,
              cursor: "pointer",
            }}>
            Exit
          </button>
        </div>
      );
    }

    function MultiLaneControl({ multiLaneMode, setMultiLaneMode, manualLanesPace, setManualLanesPace, lanePlansForTarget, generateForPlanId }) {
      const currentPlan = (lanePlansForTarget || []).find(p => p.id === generateForPlanId) || null;
      const planLanes = currentPlan && currentPlan.plan_data && Array.isArray(currentPlan.plan_data.lanes)
        ? currentPlan.plan_data.lanes
        : null;
      const usePlanPrefill = () => {
        if (!planLanes || !planLanes.length) return;
        setManualLanesPace(planLanes.map((l, i) => ({
          lane_label: l.lane_label || `Lane ${i + 1}`,
          pace: l.pace || "",
        })));
      };
      const addRow = () => setManualLanesPace(prev => [
        ...prev,
        { lane_label: `Lane ${prev.length + 1}`, pace: "" },
      ]);
      const removeRow = (idx) => setManualLanesPace(prev =>
        prev.length > 1 ? prev.filter((_, i) => i !== idx) : prev
      );
      const updateRow = (idx, field, val) => setManualLanesPace(prev =>
        prev.map((r, i) => i === idx ? { ...r, [field]: val } : r)
      );
      return (
        <div style={{ marginTop: -4, marginBottom: 14, padding: 10, border: "1px solid var(--color-border)", borderRadius: 8, background: "var(--color-card)" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap" }}>
            <span style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.08em" }}>
              Multi-lane
            </span>
            <button
              type="button"
              onClick={() => setMultiLaneMode(v => !v)}
              title="When ON, the picker only chooses options whose intervals fit every lane's pace, and the result opens in the multi-pace print view."
              style={{
                fontSize: 11, fontWeight: 700, padding: "3px 10px", borderRadius: 6,
                border: "1px solid var(--color-border-strong)",
                background: multiLaneMode ? "var(--color-primary)" : "transparent",
                color: multiLaneMode ? "#fff" : "var(--color-text-dim)",
                cursor: "pointer",
              }}>
              {multiLaneMode ? "ON" : "OFF"}
            </button>
            {multiLaneMode && planLanes && planLanes.length > 0 && (
              <button
                type="button"
                onClick={usePlanPrefill}
                title={`Prefill from lane plan "${currentPlan?.name || "(default)"}"`}
                style={{
                  fontSize: 11, fontWeight: 600, padding: "3px 10px", borderRadius: 6,
                  border: "1px solid var(--color-border-strong)",
                  background: "transparent",
                  color: "var(--color-text-dim)",
                  cursor: "pointer",
                }}>
                Use lane plan
              </button>
            )}
          </div>
          {multiLaneMode && (
            <div style={{ marginTop: 10, display: "grid", gap: 6 }}>
              {manualLanesPace.map((row, i) => {
                const isValid = row.pace === "" || parsePaceMSS(row.pace) !== null;
                return (
                  <div key={i} style={{ display: "flex", gap: 6, alignItems: "center" }}>
                    <input
                      type="text"
                      value={row.lane_label}
                      onChange={e => updateRow(i, "lane_label", e.target.value)}
                      placeholder={`Lane ${i + 1}`}
                      style={{
                        fontSize: 12, padding: "4px 8px", borderRadius: 4,
                        border: "1px solid var(--color-border-strong)", background: "var(--color-bg)",
                        color: "var(--color-text)", width: 100,
                      }} />
                    <input
                      type="text"
                      value={row.pace}
                      onChange={e => updateRow(i, "pace", e.target.value)}
                      placeholder="M:SS"
                      style={{
                        fontSize: 12, fontFamily: "monospace", padding: "4px 8px", borderRadius: 4,
                        border: `1px solid ${isValid ? "var(--color-border-strong)" : "#ef4444"}`,
                        background: "var(--color-bg)", color: "var(--color-text)", width: 60,
                      }} />
                    <span style={{ fontSize: 10, color: "var(--color-text-dim)" }}>/ 100</span>
                    {manualLanesPace.length > 1 && (
                      <button
                        type="button"
                        onClick={() => removeRow(i)}
                        title="Remove lane"
                        style={{
                          fontSize: 11, padding: "2px 7px", borderRadius: 4,
                          border: "1px solid var(--color-border-strong)", background: "transparent",
                          color: "var(--color-text-dim)", cursor: "pointer",
                        }}>
                        ×
                      </button>
                    )}
                  </div>
                );
              })}
              <button
                type="button"
                onClick={addRow}
                style={{
                  fontSize: 11, padding: "4px 10px", borderRadius: 4,
                  border: "1px dashed var(--color-border-strong)", background: "transparent",
                  color: "var(--color-text-dim)", cursor: "pointer", justifySelf: "start",
                }}>
                + Add lane
              </button>
            </div>
          )}
        </div>
      );
    }

    // Take a blocks array, rescale every set's interval to a target pace.
    // No-op for unparseable paces or "No interval" sets (scaleInterval
    // already returns the input untouched in that case).
    function rescaleBlocksForPace(blocks, paceStr) {
      const secs = parsePaceMSS(paceStr);
      if (!secs) return blocks;
      const ratio = secs / 120; // PACE_BASELINE_SECS
      return blocks.map(b => Array.isArray(b.sets) ? ({
        ...b,
        sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, ratio) })),
      }) : b);   // Section model B — dryland blocks have no intervals; pass through.
    }

    function MultiPaceModal({ workout, generateForTarget, lanePlansForTarget, onClose, onPreview }) {
      // Default: plan source if a default plan exists, else manual.
      const defaultPlan = (lanePlansForTarget || []).find(p => p.is_default) || (lanePlansForTarget || [])[0] || null;
      const [source, setSource] = React.useState(defaultPlan ? "plan" : "manual");
      const [planId, setPlanId] = React.useState(defaultPlan ? defaultPlan.id : null);
      const [manual, setManual] = React.useState([
        { lane_label: "Lane 1", pace: "1:30" },
        { lane_label: "Lane 2", pace: "1:40" },
        { lane_label: "Lane 3", pace: "1:50" },
      ]);
      const [mode, setMode] = React.useState("per_lane"); // per_lane | matrix
      const [err,  setErr]  = React.useState(null);

      const currentPlan = source === "plan"
        ? (lanePlansForTarget || []).find(p => p.id === planId) || null
        : null;

      // Derive lanes preview (read-only) from the chosen source.
      const planLanes = currentPlan && currentPlan.plan_data && Array.isArray(currentPlan.plan_data.lanes)
        ? currentPlan.plan_data.lanes.map((l, i) => ({
            lane_label: l.lane_label || `Lane ${i + 1}`,
            pace: l.pace || "",
            // Phase 3 PSC slice 4 — preserve members so the per-lane print
            // view can render per-swimmer substitution annotations.
            members: Array.isArray(l.members) ? l.members : [],
          }))
        : [];

      const previewLanes = source === "plan" ? planLanes : manual;

      const addManualRow  = () => setManual(prev => [...prev, { lane_label: `Lane ${prev.length + 1}`, pace: "" }]);
      const removeManual  = (idx) => setManual(prev => prev.filter((_, i) => i !== idx));
      const updateManual  = (idx, field, val) => setManual(prev => prev.map((r, i) => i === idx ? { ...r, [field]: val } : r));

      const submit = () => {
        setErr(null);
        const lanes = previewLanes.filter(l => l && l.pace);
        if (!lanes.length) { setErr("Need at least one lane with a pace."); return; }
        for (const l of lanes) {
          if (parsePaceMSS(l.pace) === null) {
            setErr(`"${l.pace}" isn't a valid pace — use M:SS between 0:30 and 5:00.`);
            return;
          }
        }
        onPreview({ lanes, mode });
      };

      const headerLabel = generateForTarget?.name || "Group";

      return (
        <div onClick={onClose} className="modal-overlay" style={{ padding: 20 }}>
          <div onClick={(e) => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            padding: 22, maxWidth: 640, width: "100%", maxHeight: "90vh", overflowY: "auto",
          }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 14 }}>
              <div>
                <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em" }}>Multi-pace print</div>
                <div style={{ color: "var(--color-text)", fontSize: 18, fontWeight: 700, marginTop: 2 }}>{headerLabel}</div>
              </div>
              <button onClick={onClose} style={{
                background: "transparent", border: "none", color: "var(--color-text-muted)",
                fontSize: 22, cursor: "pointer", lineHeight: 1,
              }}>×</button>
            </div>

            {/* Source toggle */}
            <div style={{ display: "flex", gap: 8, marginBottom: 14 }}>
              {[
                { id: "plan",   label: "Use lane plan", disabled: !lanePlansForTarget || lanePlansForTarget.length === 0 },
                { id: "manual", label: "Manual entry",  disabled: false },
              ].map(opt => {
                const active = source === opt.id;
                return (
                  <button key={opt.id}
                    disabled={opt.disabled}
                    onClick={() => { setSource(opt.id); setErr(null); }}
                    style={{
                      flex: 1, padding: "9px 12px", borderRadius: 8,
                      border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`,
                      background: active ? "rgba(245,158,11,0.18)" : "transparent",
                      color: opt.disabled ? "var(--color-border-strong)" : (active ? "var(--color-warn)" : "var(--color-text-muted)"),
                      fontSize: 13, fontWeight: 700,
                      cursor: opt.disabled ? "not-allowed" : "pointer",
                    }}>
                    {opt.label}
                    {opt.disabled && <span style={{ marginLeft: 6, fontSize: 10, color: "var(--color-border-strong)" }}>(no plans)</span>}
                  </button>
                );
              })}
            </div>

            {/* Plan picker */}
            {source === "plan" && lanePlansForTarget && lanePlansForTarget.length > 0 && (
              <div style={{ marginBottom: 14 }}>
                <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 5 }}>Plan</label>
                <select value={planId || ""} onChange={(e) => setPlanId(Number(e.target.value) || null)}
                  style={{ width: "100%", padding: "8px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 13 }}>
                  {lanePlansForTarget.map(p => (
                    <option key={p.id} value={p.id}>{p.name}{p.is_default ? " (default)" : ""}</option>
                  ))}
                </select>
              </div>
            )}

            {/* Lane preview / manual entry */}
            <div style={{ marginBottom: 14 }}>
              <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 6 }}>
                Lanes ({previewLanes.length})
              </label>
              {source === "plan" && (
                <div style={{ border: "1px solid var(--color-border)", borderRadius: 6, background: "var(--color-card)" }}>
                  {previewLanes.length === 0 ? (
                    <div style={{ padding: 10, color: "var(--color-text-muted)", fontSize: 12, fontStyle: "italic" }}>
                      Selected plan has no lanes — pick another plan or switch to Manual entry.
                    </div>
                  ) : (
                    previewLanes.map((l, i) => (
                      <div key={i} style={{ display: "flex", justifyContent: "space-between", padding: "7px 10px", borderTop: i === 0 ? "none" : "1px solid var(--color-border)", fontSize: 13, color: "var(--color-text)" }}>
                        <span>{l.lane_label}</span>
                        <span style={{ fontFamily: "ui-monospace, SFMono-Regular, monospace", color: "var(--color-warn)" }}>{l.pace || "—"}</span>
                      </div>
                    ))
                  )}
                </div>
              )}
              {source === "manual" && (
                <div>
                  {manual.map((row, i) => (
                    <div key={i} style={{ display: "flex", gap: 6, marginBottom: 6 }}>
                      <input value={row.lane_label} onChange={(e) => updateManual(i, "lane_label", e.target.value)}
                        placeholder={`Lane ${i + 1}`}
                        style={{ flex: 2, padding: "7px 9px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 13 }} />
                      <input value={row.pace} onChange={(e) => updateManual(i, "pace", e.target.value)}
                        placeholder="1:30"
                        style={{ flex: 1, padding: "7px 9px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 13, fontFamily: "ui-monospace, SFMono-Regular, monospace" }} />
                      <button onClick={() => removeManual(i)} disabled={manual.length <= 1}
                        title="Remove lane"
                        style={{ padding: "0 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "transparent", color: manual.length <= 1 ? "var(--color-border-strong)" : "var(--color-text-muted)", cursor: manual.length <= 1 ? "not-allowed" : "pointer", fontSize: 14 }}>×</button>
                    </div>
                  ))}
                  <button onClick={addManualRow}
                    style={{ padding: "6px 12px", borderRadius: 6, border: "1px dashed var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 12, cursor: "pointer", marginTop: 2 }}>
                    + Add lane
                  </button>
                </div>
              )}
            </div>

            {/* Render mode */}
            <div style={{ marginBottom: 14 }}>
              <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 6 }}>Render</label>
              <div style={{ display: "flex", gap: 8 }}>
                {[
                  { id: "per_lane", label: "Per-lane pages", desc: "One page per lane, each full workout at that lane's pace" },
                  { id: "matrix",   label: "Matrix view",    desc: "Single page, N pace columns alongside the workout" },
                ].map(m => {
                  const active = mode === m.id;
                  return (
                    <button key={m.id} onClick={() => setMode(m.id)} title={m.desc}
                      style={{
                        flex: 1, padding: "9px 12px", borderRadius: 8,
                        border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`,
                        background: active ? "rgba(245,158,11,0.18)" : "transparent",
                        color: active ? "var(--color-warn)" : "var(--color-text-muted)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer",
                      }}>{m.label}</button>
                  );
                })}
              </div>
            </div>

            {err && (
              <div style={{ padding: "8px 10px", borderRadius: 6, background: "rgba(239,68,68,0.12)", border: "1px solid #ef4444", color: "#fca5a5", fontSize: 12, marginBottom: 12 }}>
                {err}
              </div>
            )}

            <div style={{ display: "flex", justifyContent: "flex-end", gap: 8 }}>
              <button onClick={onClose}
                style={{ padding: "9px 14px", borderRadius: 7, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>
                Cancel
              </button>
              <button onClick={submit}
                style={{ padding: "9px 14px", borderRadius: 7, border: "1px solid var(--color-warn)", background: "rgba(245,158,11,0.18)", color: "var(--color-warn)", fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                Preview & Print ▸
              </button>
            </div>
          </div>
        </div>
      );
    }

    // Print overlay. Renders the workout at every lane's pace. Mounts as
    // a fullscreen layer that hides the rest of the app via body class.
    function MultiPacePrintView({ workout, lanes, mode, onClose, groupActiveConstraints = {} }) {
      // Toggle body class so screen view hides the rest of the app under
      // this overlay AND so we can scope print CSS to the multi-pace path.
      React.useEffect(() => {
        document.body.classList.add("multipace-active");
        // Auto-trigger print dialog after layout settles.
        const t = setTimeout(() => { try { window.print(); } catch (_) {} }, 250);
        return () => {
          document.body.classList.remove("multipace-active");
          clearTimeout(t);
        };
      }, []);

      const headerTitleFor = (lane) =>
        `${lane.lane_label || "Lane"}${lane.pace ? " · " + lane.pace : ""}`;

      if (mode === "matrix") {
        // Matrix: one row per set across all blocks, N columns for the
        // per-lane interval. Block headers separate the sections.
        return (
          <div className="multipace-overlay" style={{ position: "fixed", inset: 0, zIndex: 10000, background: "#fff", color: "#000", padding: "0.5in", overflow: "auto" }}>
            <button className="screen-only" onClick={onClose}
              style={{ position: "absolute", top: 12, right: 14, padding: "6px 12px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-bg)", color: "var(--color-text)", fontSize: 12, fontWeight: 700, cursor: "pointer", zIndex: 10001 }}>
              ✕ Close print view
            </button>
            <div className="multipace-page">
              <div className="multipace-print-header" style={{ borderBottom: "2pt solid #000", paddingBottom: 8, marginBottom: 12 }}>
                <div style={{ fontSize: "20pt", fontWeight: 700, margin: 0 }}>Multi-pace matrix</div>
                <div style={{ fontSize: "11pt", fontStyle: "italic", marginTop: 4 }}>
                  Same workout, {lanes.length} lane{lanes.length === 1 ? "" : "s"}. Intervals scaled from a 2:00/100 baseline.
                </div>
              </div>
              <table style={{ width: "100%", borderCollapse: "collapse", fontFamily: "Georgia, 'Times New Roman', serif", fontSize: "10pt" }}>
                <thead>
                  <tr>
                    <th style={{ textAlign: "left", borderBottom: "1.5pt solid #000", padding: "6pt 4pt", width: "44%" }}>Set</th>
                    {lanes.map((l, i) => (
                      <th key={i} style={{ textAlign: "left", borderBottom: "1.5pt solid #000", padding: "6pt 4pt" }}>{l.lane_label}<br /><span style={{ fontWeight: 400, fontStyle: "italic" }}>{l.pace}</span></th>
                    ))}
                  </tr>
                </thead>
                <tbody>
                  {workout.blocks.map((block, bi) => (
                    <React.Fragment key={bi}>
                      <tr>
                        <td colSpan={1 + lanes.length} style={{ padding: "8pt 0 3pt", borderBottom: "0.5pt solid #000", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                          {block.name || block.label || block.section}
                          {block.label && block.name && block.label !== block.name ? <span style={{ fontWeight: 400, fontStyle: "italic", textTransform: "none", letterSpacing: 0, marginLeft: 6 }}>— {block.label}</span> : null}
                        </td>
                      </tr>
                      {block.kind === "dryland"
                        ? (block.exercises || []).map((ex, ei) => (
                            <tr key={"dl" + ei}><td colSpan={1 + lanes.length} style={{ padding: "3pt 4pt", borderBottom: "0.25pt solid #ccc" }}>
                              <b>{ex.sets > 1 ? `${ex.sets} × ${ex.reps}` : ex.reps}</b> — {ex.name}{ex.rest ? ` (rest ${ex.rest})` : ""}
                            </td></tr>
                          ))
                        : (block.sets || []).map((s, si) => {
                        const setKey = bi + "-" + si;
                        return (
                          <tr key={setKey} className="multipace-matrix-row">
                            <td style={{ padding: "4pt 4pt", borderBottom: "0.25pt solid #000", verticalAlign: "top" }}>
                              <b>{s.reps}×{s.dist}</b>{s.eq ? <i style={{ marginLeft: 4 }}>[{s.eq}]</i> : null}
                              <div style={{ fontSize: "9pt", marginTop: 2 }}>{s.desc || ""}</div>
                            </td>
                            {lanes.map((l, li) => {
                              const secs = parsePaceMSS(l.pace);
                              const ratio = secs ? secs / 120 : 1;
                              const scaledInt = scaleInterval(s.interval, ratio);
                              return (
                                <td key={li} style={{ padding: "4pt 4pt", borderBottom: "0.25pt solid #000", verticalAlign: "top", fontFamily: "ui-monospace, 'SFMono-Regular', monospace", fontSize: "9.5pt" }}>
                                  {scaledInt || "—"}
                                </td>
                              );
                            })}
                          </tr>
                        );
                      })}
                    </React.Fragment>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
        );
      }

      // Per-lane mode: one page per lane.
      return (
        <div className="multipace-overlay" style={{ position: "fixed", inset: 0, zIndex: 10000, background: "#fff", color: "#000", overflow: "auto" }}>
          <button className="screen-only" onClick={onClose}
            style={{ position: "fixed", top: 12, right: 14, padding: "6px 12px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-bg)", color: "var(--color-text)", fontSize: 12, fontWeight: 700, cursor: "pointer", zIndex: 10001 }}>
            ✕ Close print view
          </button>
          {lanes.map((lane, li) => {
            const rescaled = rescaleBlocksForPace(workout.blocks, lane.pace);

            // Phase 3 PSC slice 4 — per-swimmer-on-lane substitution annotations.
            // Per scope §3.7: lane card shows per-swimmer sub when ANY member of
            // this lane has an active constraint. Workout content is unchanged
            // at the workout level; the annotations are visual only.
            //
            // Build a map of (blockIdx, setIdx) → array of "<name>: <sub>" strings.
            // Also collect workout-level annotations (caps + section skips) for
            // the header.
            const laneMembers = Array.isArray(lane.members) ? lane.members : [];
            const perSetAnnotations = {}; // key: `${bi}-${si}` → ["Linda: sub free for fly", …]
            const perBlockAnnotations = {}; // key: bi → [section-skip notes]
            const workoutLevelNotes = [];
            for (const m of laneMembers) {
              const key = m.swimmer_sub || m.managed_id;
              if (!key) continue;
              const memberConstraints = groupActiveConstraints[key] || [];
              if (memberConstraints.length === 0) continue;
              const subs = computeSubstitutionsForSwimmer({ blocks: rescaled }, memberConstraints);
              const memberName = (m.display_name || m.label || (key.startsWith("ms_") ? key : key.slice(-8))).slice(0, 24);
              for (const s of subs) {
                let str = "";
                if (s.kind === "stroke_sub") {
                  str = `${memberName}: ${s.from} → ${s.to}`;
                } else if (s.kind === "equip_drop") {
                  str = `${memberName}: drop ${s.eq}`;
                } else if (s.kind === "section_skip") {
                  const k = s.block_idx;
                  (perBlockAnnotations[k] = perBlockAnnotations[k] || []).push(`${memberName}: skip section`);
                  continue;
                } else if (s.kind === "cap_yardage") {
                  workoutLevelNotes.push(`${memberName}: cap ${s.value} yd (trim from end)`);
                  continue;
                } else if (s.kind === "cap_intensity") {
                  workoutLevelNotes.push(`${memberName}: ${s.value}`);
                  continue;
                }
                if (s.block_idx != null && s.set_idx != null) {
                  const k = `${s.block_idx}-${s.set_idx}`;
                  (perSetAnnotations[k] = perSetAnnotations[k] || []).push(str);
                }
              }
            }

            return (
              <div key={li} className="multipace-page" style={{ padding: "0.5in", minHeight: "10in", pageBreakAfter: li === lanes.length - 1 ? "auto" : "always" }}>
                <div className="multipace-print-header" style={{ borderBottom: "2pt solid #000", paddingBottom: 8, marginBottom: 12 }}>
                  <div style={{ fontSize: "22pt", fontWeight: 700, margin: 0 }}>{headerTitleFor(lane)}</div>
                  <div style={{ fontSize: "11pt", fontStyle: "italic", marginTop: 4 }}>
                    {workout.typeLabel || workout.type || "Workout"} · {workout.totalYards} {(workout.poolMode === "25m" || workout.poolMode === "50m") ? "m" : "yds"}
                    {workout.focusNote ? <span> · {workout.focusNote}</span> : null}
                  </div>
                  {workoutLevelNotes.length > 0 && (
                    <div style={{ fontSize: "10pt", marginTop: 6, padding: "4pt 6pt", border: "0.5pt solid #000", background: "#f7f7f7" }}>
                      <strong>⚠ Constraints:</strong> {workoutLevelNotes.join(" · ")}
                    </div>
                  )}
                </div>
                {rescaled.map((block, bi) => {
                  const blockNotes = perBlockAnnotations[bi] || [];
                  return (
                    <div key={bi} className="multipace-block" style={{ border: "1pt solid #000", padding: "8pt 10pt", marginBottom: 10, pageBreakInside: "avoid", breakInside: "avoid" }}>
                      <div style={{ fontWeight: 700, fontSize: "12pt", textTransform: "uppercase", letterSpacing: "0.05em", marginBottom: 6 }}>
                        {block.name || block.label || block.section}
                        {block.label && block.name && block.label !== block.name ? <span style={{ fontWeight: 400, fontStyle: "italic", textTransform: "none", letterSpacing: 0, marginLeft: 6 }}>— {block.label}</span> : null}
                        <span style={{ float: "right", fontWeight: 400, fontStyle: "italic" }}>{block.totalYards}</span>
                      </div>
                      {blockNotes.length > 0 && (
                        <div style={{ fontSize: "9.5pt", fontStyle: "italic", marginBottom: 6, color: "#444" }}>
                          ⚠ {blockNotes.join(" · ")}
                        </div>
                      )}
                      {block.kind === "dryland"
                        ? (block.exercises || []).map((ex, ei) => (
                            <div key={"dl" + ei} style={{ marginBottom: 3, fontSize: "11pt" }}>
                              <b>{ex.sets > 1 ? `${ex.sets} × ${ex.reps}` : ex.reps}</b> — {ex.name}{ex.rest ? ` (rest ${ex.rest})` : ""}
                            </div>
                          ))
                        : (block.sets || []).map((s, si) => {
                        const setNotes = perSetAnnotations[`${bi}-${si}`] || [];
                        return (
                          <div key={si} style={{ marginBottom: 3, fontSize: "11pt" }}>
                            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline" }}>
                              <span><b>{s.reps}×{s.dist}</b>{s.eq ? <i style={{ marginLeft: 4 }}>[{s.eq}]</i> : null} {s.desc || ""}</span>
                              <span style={{ fontFamily: "ui-monospace, 'SFMono-Regular', monospace", marginLeft: 12, whiteSpace: "nowrap" }}>{s.interval || ""}</span>
                            </div>
                            {setNotes.length > 0 && (
                              <div style={{ fontSize: "9.5pt", fontStyle: "italic", color: "#444", paddingLeft: "1em", marginTop: 1 }}>
                                ⚠ {setNotes.join(" · ")}
                              </div>
                            )}
                          </div>
                        );
                      })}
                      {block.rounds && block.rounds > 1 ? (
                        <div style={{ fontStyle: "italic", marginTop: 4, fontSize: "10pt" }}>Rounds: {block.rounds}{block.roundRestSecs ? ` · rest ${block.roundRestSecs}s between rounds` : ""}</div>
                      ) : null}
                    </div>
                  );
                })}
              </div>
            );
          })}
        </div>
      );
    }

    // Phase 3 PSC slice 4 — read-only Active Constraints subsection.
    // Spec: PER_SWIMMER_CONSTRAINTS_SCOPE.md §3.9. Read-only by decision #7
    // (coach is write authority). Lives inside ProfileModal so swimmers see
    // their constraints in the same place they see favorites/disfavorites.
    function ProfileActiveConstraintsSection({ constraints }) {
      // Burst mitigation — accept constraints as a prop from ProfileModal
      // (sourced from App-level myConstraints, populated by bootstrap).
      // Dropped the standalone /api/me/constraints fetch.
      const rows = Array.isArray(constraints) ? constraints : [];
      const err = null;

      // Hide the entire section when the swimmer has no constraints —
      // ProfileModal is busy enough; empty cards add noise. Coach + admin
      // can still see "no constraints" on the per-swimmer panel.
      if (rows.length === 0) return null;

      return (
        <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
          <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-warn)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 12 }}>
            ⚠ Active constraints ({rows.length})
          </div>
          <div style={{ display: "grid", gap: 6, fontSize: 12 }}>
            {rows.map(r => (
              <div key={r.id} style={{ background: "var(--color-card)", border: "1px solid var(--color-warn)", borderRadius: 6, padding: "8px 10px" }}>
                <div style={{ color: "var(--color-text)", fontWeight: 600 }}>
                  <span style={{ marginRight: 6 }}>⚠</span>
                  {formatPscRow(r)}
                </div>
                <div style={{ color: "var(--color-text-muted)", fontSize: 10, marginTop: 4 }}>
                  Set by coach <code style={{ fontFamily: "monospace" }}>{(r.set_by_coach_sub || "").slice(-8) || "—"}</code>
                </div>
              </div>
            ))}
          </div>
          <div style={{ marginTop: 10, fontSize: 11, color: "var(--color-text-muted)", lineHeight: 1.5 }}>
            Constraints are set by your coach. To change them, talk with your coach. Your generated workouts already honor these — you don't need to do anything.
          </div>
          {err && <div style={{ color: "#fca5a5", fontSize: 11, marginTop: 6 }}>Error: {err}</div>}
        </div>
      );
    }

    function ProfileModal({ onClose, onProfileChange, onPaceUpdate, authMode, onSendFeedback, onStartTour, appEffectiveMe, appViewAsRole, appSetViewAsRole, appViewAsParent, appSetViewAsParent, appMe, appGoals, appFavorites, appDisfavorites, appFavoriteSets, appDisfavorSets, appMyConstraints, appSessions, appTeamDefaults, appBillingStatus, appLevel, appNextEvent, appPhase, appDisfavorMode, appEngineDisfavorites, appEngineFavorites }) {
      // Burst-mitigation B — seed ALL local state from App-level props.
      // /api/me/bootstrap returns everything ProfileModal needs (sessions +
      // team-defaults + billing-status added in B). loadAll's Promise.all is
      // now empty; only billing-history + coach-impact remain as on-demand
      // fetches (sequenced post-mount, not part of the open burst).
      const [me, setMe]             = React.useState(() => appMe || null);
      const [sessions, setSessions] = React.useState(() => Array.isArray(appSessions) ? appSessions : []);
      const [goals, setGoals]       = React.useState(() => Array.isArray(appGoals) ? appGoals : []);
      const [nextEvent, setNextEventLocal] = React.useState(() => appNextEvent || null);
      const [phase, setPhaseLocal]  = React.useState(() => appPhase || null);
      const [level, setLevelLocal]  = React.useState(() => appLevel || null);
      const [loading, setLoading]   = React.useState(true);
      const [revoking, setRevoking] = React.useState(false);
      const [revokeMsg, setRevokeMsg] = React.useState(null);
      // v1.4 — disfavorites audit panel
      const [bankDisfavorites, setBankDisfavorites] = React.useState(() =>
        Array.isArray(appDisfavorites) ? appDisfavorites : []);
      const [engineDisfavoritesLocal, setEngineDisfavoritesLocal] = React.useState(() =>
        Array.isArray(appEngineDisfavorites) ? appEngineDisfavorites : []);
      // v1.10 — set-level disfavorites for the audit panel — seeded from
      // App-level disfavorSets (Set), which arrived via bootstrap.
      const [setDisfavoritesLocal, setSetDisfavoritesLocal] = React.useState(() =>
        appDisfavorSets instanceof Set ? Array.from(appDisfavorSets) : []);
      // v1.12 — favorites audit (parallel of disfavor panel)
      const [bankFavoritesLocal, setBankFavoritesLocal] = React.useState(() =>
        Array.isArray(appFavorites) ? appFavorites : []);
      const [setFavoritesLocal, setSetFavoritesLocal] = React.useState(() =>
        appFavoriteSets instanceof Set ? Array.from(appFavoriteSets) : []);
      // v1.13 — engine favorites in the audit panel (mirror of v1.4 engine disfavorites)
      const [engineFavoritesLocal, setEngineFavoritesLocal] = React.useState(() =>
        Array.isArray(appEngineFavorites) ? appEngineFavorites : []);
      // v3.0 — coach curation impact (last 30 days, aggregate counts)
      const [impactData, setImpactData] = React.useState(null);
      // Billing v1 — subscription status + history. Status reads users.tier
      // (always safe). History reads billing_history (empty until first
      // invoice arrives via Stripe webhook).
      const [billingStatus,  setBillingStatus]  = React.useState(() => appBillingStatus || null);
      const [billingHistory, setBillingHistory] = React.useState([]);
      const [billingBusy,    setBillingBusy]    = React.useState(false);
      const [billingMsg,     setBillingMsg]     = React.useState(null);
      // v1.8 — disfavor mode: "downweight" (default 0.25×) or "exclude" (weight 0)
      const [disfavorModeLocal, setDisfavorModeLocal] = React.useState(() =>
        (appDisfavorMode === "downweight" || appDisfavorMode === "exclude") ? appDisfavorMode : "downweight");
      // Team Curation v1 slice 5 — defaults inherited from any team the user is in.
      // Array of { team_id, team_name, default_pace_base, default_disfavor_mode, default_equipment_modes }.
      const [teamDefaults, setTeamDefaults] = React.useState(() =>
        Array.isArray(appTeamDefaults) ? appTeamDefaults : []);
      // Profile cleanup 2026-05-28 — ProfileModal had grown to ~14 sections in
      // one scroll; split into 3 tabs (Account / Curation / Subscription).
      // Default tab is Account; persists across re-opens via localStorage so
      // a user who lives in the Curation tab stays there.
      const [tab, setTab] = React.useState(() => {
        try { return localStorage.getItem("profileTab") || "account"; }
        catch (_) { return "account"; }
      });
      React.useEffect(() => {
        try { localStorage.setItem("profileTab", tab); } catch (_) {}
      }, [tab]);

      const loadAll = React.useCallback(async () => {
        // Burst mitigation B — every section ProfileModal needs that's
        // available pre-open arrives via /api/me/bootstrap (which already
        // ran before the user opened the modal). Local state is seeded from
        // the App props in the useState initializers above. loadAll's
        // Promise.all is now empty. Only two on-demand fetches remain,
        // sequenced one tick apart so they don't form a burst:
        //   - billing-history: subscription invoices (rarely non-empty;
        //     loaded only because the Subscription panel shows them inline).
        //   - coach-impact: 30-day curation reach + effectiveness, coach-only.
        // Both are best-effort; failures don't block the modal.
        setLoading(true);
        try {
          if (appEffectiveMe ? appEffectiveMe.is_coach : appMe?.is_coach) {
            fetch("/api/coach/curation-impact", { cache: "no-store" })
              .then(r => r.ok ? r.json() : null)
              .then(d => { if (d) setImpactData(d); })
              .catch(() => {});
          }
          fetch("/api/billing/history?limit=5", { cache: "no-store" })
            .then(r => r.ok ? r.json() : [])
            .then(d => { if (Array.isArray(d)) setBillingHistory(d); })
            .catch(() => {});
        } finally {
          setLoading(false);
        }
      }, []);

      // v1.4 — remove a bank disfavorite (DELETE /api/disfavorites/:label)
      const handleRemoveBankDisfavorite = async (label) => {
        const prev = bankDisfavorites;
        setBankDisfavorites(prev.filter(l => l !== label));
        try {
          await fetch(`/api/disfavorites/${encodeURIComponent(label)}`, {
            method: "DELETE",
            headers: { ...csrfHeaders() },
          });
          // Tell the parent app to refresh its local mirror via window event
          // (ProfileModal is self-contained; the App's disfavorites array
          // re-fetches on profile-change events).
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setBankDisfavorites(prev);
        }
      };

      // v1.8 — switch disfavor mode (downweight ↔ exclude). POSTs to
      // settings.extra and notifies the parent App via onProfileChange so
      // its own disfavorMode state refreshes (which the picker reads).
      const handleSetDisfavorMode = async (mode) => {
        if (mode !== "downweight" && mode !== "exclude") return;
        const prev = disfavorModeLocal;
        setDisfavorModeLocal(mode);
        try {
          await fetch("/api/settings/extra", {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ disfavor_mode: mode }),
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setDisfavorModeLocal(prev);
        }
      };

      // v1.10 — remove a set-level disfavorite (DELETE /api/disfavor-sets/:setId)
      const handleRemoveSetDisfavorite = async (setId) => {
        const prev = setDisfavoritesLocal;
        setSetDisfavoritesLocal(prev.filter(s => s !== setId));
        try {
          await fetch(`/api/disfavor-sets/${encodeURIComponent(setId)}`, {
            method: "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setSetDisfavoritesLocal(prev);
        }
      };

      // v1.10 — context lookup: find an option that contains the given
      // set_id, returning { label, desc } for the audit panel display.
      // Scans MAIN/DRILL/WARMUP/COOLDOWN_OPTIONS (global script constants).
      // Returns null if no match (orphaned set_id — bank evolution).
      const lookupSetContext = (setId) => {
        if (!setId) return null;
        // Phase H Stage 2: all bank constants are flat arrays.
        const banks = [
          typeof MAIN_OPTIONS     !== "undefined" ? MAIN_OPTIONS     : null,
          typeof DRILL_OPTIONS    !== "undefined" ? DRILL_OPTIONS    : null,
          typeof WARMUP_OPTIONS   !== "undefined" ? WARMUP_OPTIONS   : null,
          typeof COOLDOWN_OPTIONS !== "undefined" ? COOLDOWN_OPTIONS : null,
        ];
        for (const arr of banks) {
          if (!Array.isArray(arr)) continue;
          for (const opt of arr) {
            if (!opt || !Array.isArray(opt.sets)) continue;
            for (const s of opt.sets) {
              if (s && s.id === setId) {
                return { label: opt.label, desc: s.desc, dist: s.dist, reps: s.reps };
              }
            }
          }
        }
        return null;
      };

      // v1.4 — remove an engine disfavorite (rewrite engine_disfavorites array)
      const handleRemoveEngineDisfavorite = async (template_id, stroke) => {
        const prev = engineDisfavoritesLocal;
        const next = prev.filter(e => !(e.template_id === template_id && e.stroke === stroke));
        setEngineDisfavoritesLocal(next);
        try {
          await fetch("/api/settings/extra", {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ engine_disfavorites: next }),
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setEngineDisfavoritesLocal(prev);
        }
      };

      // v1.13 — remove an engine favorite (rewrite engine_favorites array)
      const handleRemoveEngineFavorite = async (template_id, stroke) => {
        const prev = engineFavoritesLocal;
        const next = prev.filter(e => !(e.template_id === template_id && e.stroke === stroke));
        setEngineFavoritesLocal(next);
        try {
          await fetch("/api/settings/extra", {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ engine_favorites: next }),
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setEngineFavoritesLocal(prev);
        }
      };

      // v1.12 — remove a bank favorite (DELETE /api/favorites/:label)
      const handleRemoveBankFavorite = async (label) => {
        const prev = bankFavoritesLocal;
        setBankFavoritesLocal(prev.filter(l => l !== label));
        try {
          await fetch(`/api/favorites/${encodeURIComponent(label)}`, {
            method: "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setBankFavoritesLocal(prev);
        }
      };

      // v1.12 — remove a set-level favorite (DELETE /api/favorite-sets/:id)
      const handleRemoveSetFavorite = async (setId) => {
        const prev = setFavoritesLocal;
        setSetFavoritesLocal(prev.filter(s => s !== setId));
        try {
          await fetch(`/api/favorite-sets/${encodeURIComponent(setId)}`, {
            method: "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (onProfileChange) onProfileChange();
        } catch (_) {
          setSetFavoritesLocal(prev);
        }
      };

      React.useEffect(() => { loadAll(); }, [loadAll]);

      const handleSignoutOthers = async () => {
        setRevoking(true); setRevokeMsg(null);
        try {
          const res = await fetch("/api/auth/signout-all-others", {
            method: "POST",
            headers: { ...csrfHeaders() },
          });
          const data = await res.json().catch(() => ({}));
          if (res.ok) {
            setRevokeMsg(`Signed out ${data.revoked} other session(s).`);
            await loadAll();
          } else {
            setRevokeMsg(`Error: ${data.error || res.status}`);
          }
        } catch (err) {
          setRevokeMsg(`Error: ${err.message}`);
        } finally {
          setRevoking(false);
        }
      };

      const handleSaveGoal = async (metric, targetValue) => {
        const v = parseInt(targetValue, 10);
        if (!v || v <= 0) return;
        const res = await fetch("/api/goals", {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ metric, target_value: v }),
        });
        if (res.ok) {
          setGoals(prev => {
            const without = prev.filter(g => g.metric !== metric);
            return [...without, { metric, target_value: v }];
          });
        }
      };

      const handleDeleteGoal = async (metric) => {
        const res = await fetch(`/api/goals/${encodeURIComponent(metric)}`, {
          method: "DELETE",
          headers: { ...csrfHeaders() },
        });
        if (res.ok) {
          setGoals(prev => prev.filter(g => g.metric !== metric));
        }
      };

      const handleSaveNextEvent = async (name, date) => {
        const trimmed = (name || "").trim();
        if (!trimmed || !/^\d{4}-\d{2}-\d{2}$/.test(date)) return false;
        const res = await fetch("/api/settings/extra", {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ next_event: { name: trimmed, date } }),
        });
        if (res.ok) {
          setNextEventLocal({ name: trimmed, date });
          if (onProfileChange) onProfileChange();
          return true;
        }
        return false;
      };

      const handleClearNextEvent = async () => {
        const res = await fetch("/api/settings/extra", {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ next_event: null }),
        });
        if (res.ok) {
          setNextEventLocal(null);
          if (onProfileChange) onProfileChange();
        }
      };

      // N5: set or clear the training phase. Null = cleared (no phase bias).
      const handleSetPhase = async (phaseId) => {
        const value = phaseId && PHASES[phaseId] ? phaseId : null;
        const res = await fetch("/api/settings/extra", {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ phase: value }),
        });
        if (res.ok) {
          setPhaseLocal(value);
          if (onProfileChange) onProfileChange();
        }
      };

      // J: set or clear the swimmer level. Picking a level also overwrites
      // paceInput with that level's preset pace (handled via onPaceUpdate
      // callback up to App). LevelRow's onChange passes (id, pace) — when
      // clearing (id===null), pace is null too and we don't touch paceInput.
      const handleSetLevel = async (levelId, presetPace) => {
        const value = levelId && LEVEL_PRESETS[levelId] ? levelId : null;
        const res = await fetch("/api/settings/extra", {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ level: value }),
        });
        if (res.ok) {
          setLevelLocal(value);
          if (value && presetPace && onPaceUpdate) onPaceUpdate(presetPace);
          if (onProfileChange) onProfileChange();
        }
      };

      const handleRevokeOne = async (prefix) => {
        setRevokeMsg(null);
        try {
          const res = await fetch(`/api/auth/sessions/${encodeURIComponent(prefix)}`, {
            method: "DELETE",
            headers: { ...csrfHeaders() },
          });
          const data = await res.json().catch(() => ({}));
          if (res.ok) {
            setRevokeMsg(`Revoked session ${prefix}…`);
            await loadAll();
          } else {
            setRevokeMsg(`Error: ${data.error || res.status}`);
          }
        } catch (err) {
          setRevokeMsg(`Error: ${err.message}`);
        }
      };

      const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" }) : "—";
      const fmtTime = (d) => d ? new Date(d).toLocaleString("en-US", { dateStyle: "short", timeStyle: "short" }) : "—";
      const poolLabel = (m) => ({ "25y": "SCY (25y)", "25m": "SCM (25m)", "50m": "LCM (50m)", "yds": "SCY (legacy)" })[m] || m;
      const poolUnit  = (m) => (m === "50m" || m === "25m") ? "m" : "yds";

      return (
        <div className="modal-overlay" style={{ padding: 16 }} onClick={onClose}>
          <div onClick={e => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            maxWidth: 560, width: "100%", maxHeight: "85vh", overflowY: "auto",
            color: "#cbd5e1",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "16px 20px", borderBottom: "1px solid var(--color-border)" }}>
              <div style={{ fontSize: 16, fontWeight: 700, color: "var(--color-text)" }}>👤 Profile</div>
              <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: "none", color: "var(--color-text-muted)", fontSize: 20, cursor: "pointer", padding: 4 }}>✕</button>
            </div>

            {/* Tab bar — Account / Curation / Subscription. Render only when
                me is loaded so the loading/error states stay clean. */}
            {!loading && me && (() => {
              const tabBtn = (id, label) => (
                <button onClick={() => setTab(id)} key={id}
                  style={{
                    flex: 1, padding: "10px 0", border: "none",
                    background: tab === id ? "var(--color-card)" : "transparent",
                    color: tab === id ? "var(--color-primary)" : "var(--color-text-muted)",
                    borderBottom: tab === id ? "2px solid var(--color-primary)" : "2px solid transparent",
                    fontSize: 13, fontWeight: 600, cursor: "pointer",
                    transition: "color 120ms, border-color 120ms",
                  }}>
                  {label}
                </button>
              );
              return (
                <div style={{ display: "flex", borderBottom: "1px solid var(--color-border)" }}>
                  {tabBtn("account",      "Account")}
                  {tabBtn("curation",     "Curation")}
                  {tabBtn("subscription", "Subscription")}
                </div>
              );
            })()}

            {loading ? (
              <div style={{ padding: 40, textAlign: "center", color: "var(--color-text-dim)" }}>Loading…</div>
            ) : !me ? (
              <div style={{ padding: 40, textAlign: "center", color: "#f87171" }}>Could not load profile.</div>
            ) : (
              <>
                {tab === "account" && (
                <div style={{ padding: "18px 20px" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Identity</div>
                  <div style={{ display: "grid", gap: 6, fontSize: 13 }}>
                    <EditableProfileField
                      label="Name"
                      value={me.display_name}
                      placeholder="Your name (used to personalize the app)"
                      maxLength={120}
                      onSave={async (v) => {
                        const res = await fetch("/api/me", {
                          method: "PATCH",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body: JSON.stringify({ display_name: v }),
                        });
                        if (!res.ok) {
                          const body = await res.json().catch(() => ({}));
                          throw new Error(body.error || `HTTP ${res.status}`);
                        }
                        setMe(prev => prev ? { ...prev, display_name: v || null } : prev);
                        if (onProfileChange) onProfileChange();
                      }} />
                    <EditableProfileField
                      label="Initials"
                      value={me.initials}
                      placeholder="Up to 8 characters"
                      maxLength={8}
                      transform={(v) => v.toUpperCase()}
                      onSave={async (v) => {
                        const res = await fetch("/api/me", {
                          method: "PATCH",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body: JSON.stringify({ initials: v }),
                        });
                        if (!res.ok) {
                          const body = await res.json().catch(() => ({}));
                          throw new Error(body.error || `HTTP ${res.status}`);
                        }
                        setMe(prev => prev ? { ...prev, initials: v || null } : prev);
                        if (onProfileChange) onProfileChange();
                      }} />
                    <EditableProfileField
                      label="Email"
                      type="email"
                      value={me.email}
                      placeholder="you@example.com"
                      badge={me.email_verified ? <span style={{ color: "#10b981", fontSize: 11, marginLeft: 4 }}>✓ verified</span> : null}
                      onSave={async (v) => {
                        const res = await fetch("/api/me", {
                          method: "PATCH",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body: JSON.stringify({ email: v }),
                        });
                        if (!res.ok) {
                          const body = await res.json().catch(() => ({}));
                          throw new Error(body.error || `HTTP ${res.status}`);
                        }
                        setMe(prev => prev ? { ...prev, email: v || null, email_verified: (v === prev.email) ? prev.email_verified : false } : prev);
                        if (onProfileChange) onProfileChange();
                      }} />
                    {/* Gender — custom row (EditableProfileField is input-only,
                        gender needs a select). Saves through the same PATCH /api/me. */}
                    <ProfileGenderRow me={me} setMe={setMe} onProfileChange={onProfileChange} />
                    {/* Class year (graduation year). Grade is derived server-side
                        from class_year, shown as a read-only badge. */}
                    <EditableProfileField
                      label="Class year"
                      value={me.class_year ? String(me.class_year) : ""}
                      placeholder="Graduation year, e.g. 2027"
                      maxLength={4}
                      transform={(v) => v.replace(/\D/g, "").slice(0, 4)}
                      badge={me.grade != null ? <span style={{ color: "var(--color-text-dim)", fontSize: 11, marginLeft: 4 }}>{me.grade >= 1 && me.grade <= 12 ? `Grade ${me.grade}` : me.grade > 12 ? "Graduated" : "Pre-HS"}</span> : null}
                      onSave={async (v) => {
                        const res = await fetch("/api/me", {
                          method: "PATCH",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body: JSON.stringify({ class_year: v === "" ? null : Number(v) }),
                        });
                        if (!res.ok) {
                          const body = await res.json().catch(() => ({}));
                          throw new Error(body.error || `HTTP ${res.status}`);
                        }
                        setMe(prev => prev ? { ...prev, class_year: v === "" ? null : Number(v) } : prev);
                        if (onProfileChange) onProfileChange();
                      }} />
                    <EditableProfileField
                      label="USA-S ID"
                      value={me.usa_swimming_id}
                      placeholder="USA Swimming member ID (optional)"
                      maxLength={255}
                      onSave={async (v) => {
                        const res = await fetch("/api/me", {
                          method: "PATCH",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body: JSON.stringify({ usa_swimming_id: v === "" ? null : v }),
                        });
                        if (!res.ok) {
                          const body = await res.json().catch(() => ({}));
                          throw new Error(body.error || `HTTP ${res.status}`);
                        }
                        setMe(prev => prev ? { ...prev, usa_swimming_id: v || null } : prev);
                        if (onProfileChange) onProfileChange();
                      }} />
                    {me?.sub && (
                      <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid var(--color-card)" }}>
                        <AddressManager swimmerRef={me.sub} showConsent={true} />
                      </div>
                    )}
                    <div style={{ display: "grid", gridTemplateColumns: "auto 1fr", gap: "6px 16px", marginTop: 4 }}>
                      <span style={{ color: "var(--color-text-dim)" }}>Member since:</span>
                      <span style={{ color: "var(--color-text)" }}>{fmtDate(me.created_at)}</span>
                      <span style={{ color: "var(--color-text-dim)" }}>Last login:</span>
                      <span style={{ color: "var(--color-text)" }}>{fmtTime(me.last_login_at)}</span>
                      {appEffectiveMe?.is_admin && (<>
                        <span style={{ color: "var(--color-text-dim)" }}>Role:</span>
                        <span style={{ color: "var(--color-primary)", fontWeight: 700 }}>Admin</span>
                      </>)}
                    </div>
                    {/* R-F: join-group code redemption (visible to all
                        authenticated users; not coach-gated). */}
                    <JoinGroupSection me={me} setMe={setMe} onJoined={() => { if (onProfileChange) onProfileChange(); }} />
                    {/* R-I: claim a managed profile a coach created for you */}
                    <ClaimManagedSection me={me} setMe={setMe} onClaimed={() => { if (onProfileChange) onProfileChange(); }} />
                  </div>
                  {/* Setforge rebrand 2026-05-20 — Send feedback + Sign out
                      folded down from the top nav (REBRAND_SCOPE §8.1, §8.4). */}
                  <div style={{ display: "flex", gap: 8, marginTop: 16, paddingTop: 16, borderTop: "1px solid var(--color-card)" }}>
                    {onSendFeedback && (
                      <button onClick={() => { onSendFeedback(); onClose(); }}
                        title="Send feedback or report a bug"
                        style={{ flex: 1, padding: "9px 12px", borderRadius: 8, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600, cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 6 }}>
                        💬 Send feedback
                      </button>
                    )}
                    {authMode === "apple" && (
                      <a href="/api/auth/signout" title="Sign out"
                        style={{ flex: 1, padding: "9px 12px", borderRadius: 8, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600, cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 6, textDecoration: "none" }}>
                        🚪 Sign out
                      </a>
                    )}
                  </div>
                  {onStartTour && (
                    <button onClick={() => { onClose(); onStartTour(); }}
                      title="Replay the quick walkthrough of the workout generator"
                      style={{ width: "100%", marginTop: 8, padding: "9px 12px", borderRadius: 8, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600, cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 6 }}>
                      👋 Take the tour
                    </button>
                  )}
                </div>

                )}

                {tab === "account" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Workouts</div>
                  <div style={{ display: "flex", alignItems: "baseline", gap: 8, marginBottom: 12 }}>
                    <span style={{ fontSize: 28, fontWeight: 900, color: "var(--color-text)" }}>{me.workout_count.toLocaleString()}</span>
                    <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>total saved</span>
                  </div>
                  {me.stats_by_pool.length > 0 && (
                    <div style={{ fontSize: 12, display: "grid", gap: 4 }}>
                      {me.stats_by_pool.map(s => (
                        <div key={s.pool_mode} style={{ display: "flex", justifyContent: "space-between" }}>
                          <span style={{ color: "var(--color-text-muted)" }}>{poolLabel(s.pool_mode)}</span>
                          <span style={{ color: "var(--color-text)" }}>{s.count.toLocaleString()} workouts · {s.total.toLocaleString()} {poolUnit(s.pool_mode)}</span>
                        </div>
                      ))}
                    </div>
                  )}
                </div>
                )}

                {tab === "account" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
                    🎯 Goals
                  </div>
                  <div style={{ display: "grid", gap: 8 }}>
                    {GOAL_METRICS.map(m => {
                      const existing = goals.find(g => g.metric === m.id);
                      return (
                        <GoalRow key={m.id} metric={m}
                          value={existing ? existing.target_value : null}
                          onSave={(v) => handleSaveGoal(m.id, v)}
                          onDelete={() => handleDeleteGoal(m.id)} />
                      );
                    })}
                    <NextEventRow value={nextEvent}
                      onSave={handleSaveNextEvent}
                      onClear={handleClearNextEvent} />
                    <PhaseRow value={phase} onChange={handleSetPhase} />
                    <LevelRow value={level} onChange={handleSetLevel} />
                    <BenchmarksSection onPaceUpdate={onPaceUpdate} />
                  </div>
                </div>
                )}

                {/* Phase 3 PSC slice 4 — read-only Active Constraints section.
                    Closes scope decision #8 (transparency): swimmer sees their
                    own constraints with set-by + expiry. Read-only — coach is
                    the write authority. */}
                {tab === "curation" && (
                  <ProfileActiveConstraintsSection constraints={appMyConstraints} />
                )}

                {/* v1.12 — Favorites audit panel. Mirrors the v1.4/v1.10
                    Disfavorites panel for favorites. Bank labels + set IDs
                    favorited by this user, with one-click remove. */}
                {tab === "curation" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 12 }}>
                    Favorites ({bankFavoritesLocal.length + setFavoritesLocal.length + engineFavoritesLocal.length})
                  </div>
                  {bankFavoritesLocal.length === 0 && setFavoritesLocal.length === 0 && engineFavoritesLocal.length === 0 ? (
                    <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>
                      No favorites yet. Click ★ on any main-set in a workout, or use the ☆ button on a specific set, to boost pick weight.
                    </div>
                  ) : (
                    <div style={{ display: "grid", gap: 12 }}>
                      {bankFavoritesLocal.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Bank labels ({bankFavoritesLocal.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {bankFavoritesLocal.map(label => (
                              <div key={label} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px" }}>
                                <span style={{ color: "var(--color-text)" }}>★ {label}</span>
                                <button onClick={() => handleRemoveBankFavorite(label)}
                                  title="Remove from favorites"
                                  style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer" }}>
                                  Remove
                                </button>
                              </div>
                            ))}
                          </div>
                        </div>
                      )}
                      {engineFavoritesLocal.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Engine templates ({engineFavoritesLocal.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {engineFavoritesLocal.map((e, i) => (
                              <div key={`${e.template_id}:${e.stroke}:${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px" }}>
                                <span style={{ color: "var(--color-text)" }}>
                                  ★ <code style={{ fontSize: 11, color: "var(--color-warn)" }}>{e.template_id}</code>
                                  <span style={{ color: "var(--color-text-dim)", marginLeft: 6 }}>+ {e.stroke}</span>
                                </span>
                                <button onClick={() => handleRemoveEngineFavorite(e.template_id, e.stroke)}
                                  title="Remove from engine favorites"
                                  style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer" }}>
                                  Remove
                                </button>
                              </div>
                            ))}
                          </div>
                        </div>
                      )}
                      {setFavoritesLocal.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Sets ({setFavoritesLocal.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {setFavoritesLocal.map((setId, i) => {
                              const ctx = lookupSetContext(setId);
                              return (
                                <div key={`${setId}:${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px", gap: 10 }}>
                                  <span style={{ color: "var(--color-text)", overflow: "hidden", textOverflow: "ellipsis", minWidth: 0, flex: 1 }}>
                                    {ctx ? (
                                      <>
                                        <span>{ctx.reps > 1 ? `${ctx.reps}×${ctx.dist}` : `${ctx.dist}`}</span>
                                        <span style={{ color: "var(--color-text-dim)", margin: "0 6px" }}>·</span>
                                        <span style={{ color: "var(--color-text-dim)" }}>{(ctx.desc || "").slice(0, 80)}</span>
                                        <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 2 }}>
                                          from <em>{ctx.label}</em> · <code>{setId}</code>
                                        </div>
                                      </>
                                    ) : (
                                      <>
                                        <code style={{ fontSize: 11, color: "var(--color-warn)" }}>{setId}</code>
                                        <span style={{ color: "var(--color-text-dim)", marginLeft: 6, fontStyle: "italic", fontSize: 10 }}>(no longer in the bank)</span>
                                      </>
                                    )}
                                  </span>
                                  <button onClick={() => handleRemoveSetFavorite(setId)}
                                    title="Remove from set favorites"
                                    style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer", flexShrink: 0 }}>
                                    Remove
                                  </button>
                                </div>
                              );
                            })}
                          </div>
                        </div>
                      )}
                    </div>
                  )}
                </div>
                )}

                {/* Billing v1 — Subscription section. Always visible to
                    authenticated users (swimmers see "Free", coaches see
                    Subscribe / Manage). Reads /api/billing/status which is
                    safe regardless of BILLING_ACTIVE. */}
                {tab === "subscription" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
                    Subscription
                  </div>
                  {!billingStatus ? (
                    <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>Loading…</div>
                  ) : (
                    <div>
                      <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 12, flexWrap: "wrap" }}>
                        <span style={{ fontSize: 13, color: "var(--color-text)" }}>Current tier:</span>
                        <span style={{
                          padding: "3px 10px", borderRadius: 4, fontSize: 12, fontWeight: 700,
                          background: billingStatus.tier === "free" ? "var(--color-border)" : "#0d9488",
                          color: billingStatus.tier === "free" ? "#cbd5e1" : "#fff",
                          textTransform: "capitalize",
                        }}>
                          {billingStatus.tier}
                        </span>
                        {billingStatus.tier_source === "admin_grant" && (
                          <span title="Comped by operator (no payment on file)" style={{ padding: "3px 8px", borderRadius: 4, fontSize: 11, fontWeight: 700, background: "#7c3aed", color: "#fff" }}>
                            Comped
                          </span>
                        )}
                        {billingStatus.tier_granted_at && (
                          <span style={{ fontSize: 11, color: "var(--color-text-muted)" }}>
                            since {new Date(billingStatus.tier_granted_at).toLocaleDateString()}
                          </span>
                        )}
                      </div>

                      {billingStatus.tier === "free" && (
                        <div>
                          <div style={{ fontSize: 12, color: "var(--color-text-dim)", marginBottom: 10, lineHeight: 1.5 }}>
                            Subscribe to Coach for <strong>$10/month</strong> with a 14-day free trial.
                            Unlocks managed swimmers, teams, group assignments, multi-lane generate,
                            coach reports, UGC authoring, and team curation.
                            Cancel anytime before day 14 for no charge.
                          </div>
                          <button
                            onClick={async () => {
                              if (billingBusy) return;
                              setBillingBusy(true); setBillingMsg(null);
                              try {
                                const r = await fetch("/api/billing/checkout", {
                                  method: "POST",
                                  headers: { "Content-Type": "application/json", ...csrfHeaders() },
                                });
                                const d = await r.json().catch(() => ({}));
                                if (!r.ok) {
                                  if (d.error === "billing_not_configured") {
                                    setBillingMsg("Billing isn't live in this environment yet.");
                                  } else if (d.error === "no_price_id") {
                                    setBillingMsg("Stripe price ID not configured. Contact the operator.");
                                  } else {
                                    setBillingMsg(`Couldn't start checkout: ${d.error || r.status}`);
                                  }
                                  setBillingBusy(false);
                                  return;
                                }
                                // Redirect to Stripe-hosted checkout. setBillingBusy stays true
                                // so the button doesn't allow a second click during the redirect.
                                window.location.href = d.url;
                              } catch (e) {
                                setBillingMsg(`Network error: ${e.message}`);
                                setBillingBusy(false);
                              }
                            }}
                            disabled={billingBusy}
                            style={{
                              padding: "8px 16px", borderRadius: 6, border: "none",
                              background: billingBusy ? "var(--color-border)" : "var(--color-primary)",
                              color: "var(--color-bg)", fontSize: 13, fontWeight: 700,
                              cursor: billingBusy ? "wait" : "pointer",
                            }}>
                            {billingBusy ? "Starting…" : "Start free trial"}
                          </button>
                        </div>
                      )}

                      {billingStatus.tier !== "free" && billingStatus.tier_source !== "admin_grant" && billingStatus.has_stripe_customer && (
                        <button
                          onClick={async () => {
                            if (billingBusy) return;
                            setBillingBusy(true); setBillingMsg(null);
                            try {
                              const r = await fetch("/api/billing/portal", {
                                method: "POST",
                                headers: { "Content-Type": "application/json", ...csrfHeaders() },
                              });
                              const d = await r.json().catch(() => ({}));
                              if (!r.ok) {
                                setBillingMsg(`Couldn't open portal: ${d.error || r.status}`);
                                setBillingBusy(false);
                                return;
                              }
                              window.location.href = d.url;
                            } catch (e) {
                              setBillingMsg(`Network error: ${e.message}`);
                              setBillingBusy(false);
                            }
                          }}
                          disabled={billingBusy}
                          style={{
                            padding: "8px 16px", borderRadius: 6, border: "1px solid var(--color-border)",
                            background: "transparent", color: "var(--color-text)", fontSize: 13, fontWeight: 600,
                            cursor: billingBusy ? "wait" : "pointer",
                          }}>
                          {billingBusy ? "Opening…" : "Manage subscription"}
                        </button>
                      )}

                      {billingStatus.tier !== "free" && billingStatus.tier_source === "admin_grant" && (
                        <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic", lineHeight: 1.5 }}>
                          You're on a comped {billingStatus.tier} tier (no payment method on file).
                          Email hello@competitionaquatics.com with any questions.
                        </div>
                      )}

                      {billingHistory.length > 0 && (
                        <div style={{ marginTop: 16, paddingTop: 12, borderTop: "1px solid var(--color-card)" }}>
                          <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 6 }}>
                            Recent invoices
                          </div>
                          {billingHistory.map(inv => (
                            <div key={inv.id} style={{ display: "flex", justifyContent: "space-between", fontSize: 12, color: "var(--color-text-dim)", padding: "3px 0" }}>
                              <span>
                                {inv.occurred_at ? new Date(inv.occurred_at).toLocaleDateString() : "—"}
                                {" · "}
                                <span style={{ color: inv.status === "paid" ? "var(--color-positive)" : inv.status === "refunded" ? "var(--color-warn)" : "var(--color-text-muted)" }}>{inv.status}</span>
                              </span>
                              <span style={{ fontVariantNumeric: "tabular-nums" }}>
                                {inv.amount_cents < 0 ? "−" : ""}${Math.abs(inv.amount_cents / 100).toFixed(2)}
                              </span>
                            </div>
                          ))}
                        </div>
                      )}

                      {billingMsg && (
                        <div style={{ fontSize: 12, color: "var(--color-warn)", marginTop: 10 }}>
                          {billingMsg}
                        </div>
                      )}
                    </div>
                  )}
                </div>
                )}

                {/* v3.0 — Coach curation impact panel. Coach-only. Shows
                    how the coach's own curation (labels + sets + engine
                    tuples) is landing across their group's last-30d
                    workouts. Aggregate counts only, no swimmer names. */}
                {tab === "curation" && appEffectiveMe?.is_coach && (
                  <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                    <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 6 }}>
                      Curation impact (last 30 days)
                    </div>
                    {!impactData ? (
                      <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>Loading…</div>
                    ) : impactData.groupCount === 0 ? (
                      <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>
                        You don't coach any active groups yet. Curation propagation runs through groups — add one to start seeing impact.
                      </div>
                    ) : (
                      <div>
                        <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 12 }}>
                          {impactData.groupCount} {impactData.groupCount === 1 ? "group" : "groups"} · {impactData.swimmerCount} {impactData.swimmerCount === 1 ? "swimmer" : "swimmers"}
                          {impactData.swimmersWithActivity !== impactData.swimmerCount && (
                            <span> ({impactData.swimmersWithActivity} active in window)</span>
                          )}
                          {" · "}
                          {impactData.workoutCount} {impactData.workoutCount === 1 ? "workout" : "workouts"}
                        </div>
                        {impactData.labels.length === 0 && impactData.sets.length === 0 && impactData.engine.length === 0 ? (
                          <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>
                            You haven't curated anything yet. Click ★ or 👎 on a set to start influencing your group's picks.
                          </div>
                        ) : (
                          <div style={{ display: "grid", gap: 12 }}>
                            {impactData.labels.length > 0 && (
                              <div>
                                <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                                  Bank labels ({impactData.labels.length})
                                </div>
                                <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                                  {impactData.labels.map((row, i) => (
                                    <div key={`l${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px", gap: 10 }}>
                                      <span style={{ color: "var(--color-text)" }}>
                                        {row.kind === "favorite" ? "★" : "👎"} {row.label}
                                      </span>
                                      <span style={{ fontSize: 11, color: "var(--color-text-muted)", whiteSpace: "nowrap" }}>
                                        reach <strong style={{ color: "var(--color-text)" }}>{row.reach}</strong> · <strong style={{ color: "var(--color-text)" }}>{row.effectiveness}</strong> {row.effectiveness === 1 ? "wo" : "wos"}
                                      </span>
                                    </div>
                                  ))}
                                </div>
                              </div>
                            )}
                            {impactData.engine.length > 0 && (
                              <div>
                                <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                                  Engine templates ({impactData.engine.length})
                                </div>
                                <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                                  {impactData.engine.map((row, i) => (
                                    <div key={`e${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px", gap: 10 }}>
                                      <span style={{ color: "var(--color-text)" }}>
                                        {row.kind === "favorite" ? "★" : "👎"} <code style={{ fontSize: 11, color: "var(--color-warn)" }}>{row.template_id}</code>
                                        <span style={{ color: "var(--color-text-dim)", marginLeft: 6 }}>+ {row.stroke}</span>
                                      </span>
                                      <span style={{ fontSize: 11, color: "var(--color-text-muted)", whiteSpace: "nowrap" }}>
                                        reach <strong style={{ color: "var(--color-text)" }}>{row.reach}</strong> · <strong style={{ color: "var(--color-text)" }}>{row.effectiveness}</strong> {row.effectiveness === 1 ? "wo" : "wos"}
                                      </span>
                                    </div>
                                  ))}
                                </div>
                              </div>
                            )}
                            {impactData.sets.length > 0 && (
                              <div>
                                <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                                  Sets ({impactData.sets.length})
                                </div>
                                <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                                  {impactData.sets.map((row, i) => {
                                    const ctx = lookupSetContext(row.set_id);
                                    return (
                                      <div key={`s${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px", gap: 10 }}>
                                        <span style={{ color: "var(--color-text)", overflow: "hidden", textOverflow: "ellipsis", minWidth: 0, flex: 1 }}>
                                          {row.kind === "favorite" ? "★" : "👎"}
                                          {ctx ? (
                                            <>
                                              <span style={{ marginLeft: 4 }}>{ctx.reps > 1 ? `${ctx.reps}×${ctx.dist}` : `${ctx.dist}`}</span>
                                              <span style={{ color: "var(--color-text-dim)", margin: "0 6px" }}>·</span>
                                              <span style={{ color: "var(--color-text-dim)" }}>{(ctx.desc || "").slice(0, 60)}</span>
                                            </>
                                          ) : (
                                            <code style={{ fontSize: 11, color: "var(--color-warn)", marginLeft: 4 }}>{row.set_id}</code>
                                          )}
                                        </span>
                                        <span style={{ fontSize: 11, color: "var(--color-text-muted)", whiteSpace: "nowrap", flexShrink: 0 }}>
                                          reach <strong style={{ color: "var(--color-text)" }}>{row.reach}</strong> · <strong style={{ color: "var(--color-text)" }}>{row.effectiveness}</strong> {row.effectiveness === 1 ? "wo" : "wos"}
                                        </span>
                                      </div>
                                    );
                                  })}
                                </div>
                              </div>
                            )}
                          </div>
                        )}
                      </div>
                    )}
                  </div>
                )}

                {/* v1.4 — Disfavorites audit panel. Shows bank + engine
                    disfavorites with one-click remove. Empty state explains
                    how to add disfavorites (click 👎 in a workout block). */}
                {tab === "curation" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 12 }}>
                    Disfavorites ({bankDisfavorites.length + engineDisfavoritesLocal.length + setDisfavoritesLocal.length})
                  </div>
                  {/* Team Curation v1 slice 5: inheritance disclosure. Per
                      TEAM_CURATION_SCOPE.md §3.6 — show team defaults explicitly
                      so the user knows what's inherited and where their own
                      settings diverge. Multi-team coach gets one line per team
                      (v1: no conflict-resolution UI; defaults union silently
                      and v1.1 surfaces attribution per the spec fork). */}
                  {teamDefaults.length > 0 && (
                    <div style={{ marginBottom: 14, padding: "10px 12px", background: "rgba(59,130,246,0.08)", border: "1px solid var(--color-primary)", borderRadius: 6 }}>
                      <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-primary)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 6 }}>
                        👥 Team defaults you inherit
                      </div>
                      {teamDefaults.map(td => {
                        const bits = [];
                        if (td.default_pace_base)     bits.push(`pace ${td.default_pace_base}`);
                        if (td.default_disfavor_mode) bits.push(`disfavor mode ${td.default_disfavor_mode}`);
                        if (td.default_equipment_modes) bits.push("equipment defaults");
                        return (
                          <div key={td.team_id} style={{ fontSize: 12, color: "var(--color-text)", lineHeight: 1.5 }}>
                            <strong>{td.team_name}</strong>: {bits.join(" · ")}
                            {td.default_disfavor_mode && td.default_disfavor_mode !== disfavorModeLocal && (
                              <span style={{ color: "var(--color-warn)", fontSize: 11, marginLeft: 6 }}>
                                (you've overridden to {disfavorModeLocal})
                              </span>
                            )}
                          </div>
                        );
                      })}
                      <div style={{ fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic", marginTop: 6 }}>
                        Defaults apply to brand-new swimmers joining the team. Your own settings below override them; clear an override to fall back to the team default.
                      </div>
                    </div>
                  )}
                  {/* v1.8 — mode toggle: applies to all disfavor types */}
                  <div style={{ marginBottom: 14, display: "flex", gap: 10, alignItems: "center", flexWrap: "wrap" }}>
                    <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>Mode:</span>
                    <div style={{ display: "inline-flex", borderRadius: 6, border: "1px solid var(--color-border)", overflow: "hidden" }}>
                      {[
                        { id: "downweight", label: "Downweight (0.25×)", title: "Disfavored items get 0.25× pick weight — still possible but rare" },
                        { id: "exclude",    label: "Hard-exclude",        title: "Disfavored items get 0 weight — never picked unless the pool would empty (silent fallback)" },
                      ].map(opt => {
                        const active = disfavorModeLocal === opt.id;
                        return (
                          <button
                            key={opt.id}
                            type="button"
                            onClick={() => handleSetDisfavorMode(opt.id)}
                            title={opt.title}
                            style={{
                              fontSize: 11, fontWeight: 600, padding: "5px 10px",
                              background: active ? "var(--color-primary)" : "var(--color-card)",
                              color: active ? "#fff" : "var(--color-text-dim)",
                              border: "none", cursor: "pointer",
                              transition: "background 0.15s, color 0.15s",
                            }}>
                            {opt.label}
                          </button>
                        );
                      })}
                    </div>
                  </div>
                  {bankDisfavorites.length === 0 && engineDisfavoritesLocal.length === 0 && setDisfavoritesLocal.length === 0 ? (
                    <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>
                      No disfavorites yet. Click 👎 on any main-set in a workout to reduce its pick weight.
                    </div>
                  ) : (
                    <div style={{ display: "grid", gap: 12 }}>
                      {bankDisfavorites.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Bank labels ({bankDisfavorites.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {bankDisfavorites.map(label => (
                              <div key={label} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px" }}>
                                <span style={{ color: "var(--color-text)" }}>{label}</span>
                                <button onClick={() => handleRemoveBankDisfavorite(label)}
                                  title="Remove from disfavorites"
                                  style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer" }}>
                                  Remove
                                </button>
                              </div>
                            ))}
                          </div>
                        </div>
                      )}
                      {engineDisfavoritesLocal.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Engine templates ({engineDisfavoritesLocal.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {engineDisfavoritesLocal.map((e, i) => (
                              <div key={`${e.template_id}:${e.stroke}:${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px" }}>
                                <span style={{ color: "var(--color-text)" }}>
                                  <code style={{ fontSize: 11, color: "var(--color-warn)" }}>{e.template_id}</code>
                                  <span style={{ color: "var(--color-text-dim)", marginLeft: 6 }}>+ {e.stroke}</span>
                                </span>
                                <button onClick={() => handleRemoveEngineDisfavorite(e.template_id, e.stroke)}
                                  title="Remove from engine disfavorites"
                                  style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer" }}>
                                  Remove
                                </button>
                              </div>
                            ))}
                          </div>
                        </div>
                      )}
                      {/* v1.10 — set-level disfavorites with context lookup */}
                      {setDisfavoritesLocal.length > 0 && (
                        <div>
                          <div style={{ fontSize: 10, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 6 }}>
                            Sets ({setDisfavoritesLocal.length})
                          </div>
                          <div style={{ display: "grid", gap: 4, fontSize: 12 }}>
                            {setDisfavoritesLocal.map((setId, i) => {
                              const ctx = lookupSetContext(setId);
                              return (
                                <div key={`${setId}:${i}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 6, padding: "6px 10px", gap: 10 }}>
                                  <span style={{ color: "var(--color-text)", overflow: "hidden", textOverflow: "ellipsis", minWidth: 0, flex: 1 }}>
                                    {ctx ? (
                                      <>
                                        <span>{ctx.reps > 1 ? `${ctx.reps}×${ctx.dist}` : `${ctx.dist}`}</span>
                                        <span style={{ color: "var(--color-text-dim)", margin: "0 6px" }}>·</span>
                                        <span style={{ color: "var(--color-text-dim)" }}>{(ctx.desc || "").slice(0, 80)}</span>
                                        <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 2 }}>
                                          from <em>{ctx.label}</em> · <code>{setId}</code>
                                        </div>
                                      </>
                                    ) : (
                                      <>
                                        <code style={{ fontSize: 11, color: "var(--color-warn)" }}>{setId}</code>
                                        <span style={{ color: "var(--color-text-dim)", marginLeft: 6, fontStyle: "italic", fontSize: 10 }}>(no longer in the bank)</span>
                                      </>
                                    )}
                                  </span>
                                  <button onClick={() => handleRemoveSetDisfavorite(setId)}
                                    title="Remove from set disfavorites"
                                    style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 4, color: "var(--color-text-dim)", fontSize: 11, padding: "2px 8px", cursor: "pointer", flexShrink: 0 }}>
                                    Remove
                                  </button>
                                </div>
                              );
                            })}
                          </div>
                        </div>
                      )}
                    </div>
                  )}
                </div>
                )}

                {/* Community — link to the SetForge Discord. Sibling of
                    Favorites/Disfavorites per DISCORD_SCOPE §7. Adult
                    feedback is forwarded to a private Discord channel
                    for triage; minors' feedback stays in-app. The DOB
                    check happens server-side; UI surface is just the
                    invite link. */}
                {tab === "account" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
                    Community
                  </div>
                  <div style={{ fontSize: 13, color: "var(--color-text-muted)", marginBottom: 10, lineHeight: 1.5 }}>
                    Discord server for coaches and adult swimmers (13+). Feature requests, bug reports, coach-to-coach discussion.
                  </div>
                  <a href="https://discord.gg/N8BMxNbhf7" target="_blank" rel="noopener"
                    style={{ display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 14px", borderRadius: 6, border: "1px solid var(--color-primary)", color: "var(--color-primary)", textDecoration: "none", fontSize: 13, fontWeight: 600 }}>
                    💬 Join the SetForge Discord
                  </a>
                </div>
                )}

                {/* Self-serve data export (Phase 4 continuity). Authenticated
                    GET /api/me/export → JSON download via blob (cookie auth). */}
                {tab === "account" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
                    Your data
                  </div>
                  <div style={{ fontSize: 13, color: "var(--color-text-muted)", marginBottom: 10, lineHeight: 1.5 }}>
                    Download a full copy of your account as JSON — your profile, workouts, schedule, curation, and the swimmers and teams you coach.
                  </div>
                  <button
                    onClick={async () => {
                      try {
                        const res = await fetch("/api/me/export", { credentials: "include" });
                        if (!res.ok) throw new Error("export failed");
                        const blob = await res.blob();
                        const url = URL.createObjectURL(blob);
                        const a = document.createElement("a");
                        a.href = url;
                        a.download = `setforge-export-${new Date().toISOString().slice(0, 10)}.json`;
                        document.body.appendChild(a); a.click(); a.remove();
                        URL.revokeObjectURL(url);
                      } catch (_) { alert("Couldn't generate the export. Please try again."); }
                    }}
                    style={{ display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 14px", borderRadius: 6, border: "1px solid var(--color-primary)", background: "transparent", color: "var(--color-primary)", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>
                    ⬇ Export my data (JSON)
                  </button>
                </div>
                )}

                {/* View-as switcher — admin-only QA tool. Gated on REAL
                    admin (me.is_admin), not effectiveMe, so an admin who
                    flipped into "view as solo" can still see + exit the
                    switcher. Real `me` (from this modal's own /api/me
                    fetch) is the authoritative role check. */}
                {tab === "account" && me?.is_admin && (
                  <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                    <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 8 }}>
                      View as <span style={{ color: "var(--color-warn)", marginLeft: 6 }}>(admin QA)</span>
                    </div>
                    <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginBottom: 10 }}>
                      Override your role flags to preview what other users see. Doesn't change server-side permissions — coach/admin API endpoints still respond, the UI just hides those features.
                    </div>
                    <div style={{ display: "inline-flex", borderRadius: 6, border: "1px solid var(--color-border-strong)", overflow: "hidden" }}>
                      {[
                        { id: "self",  label: "Self",  title: "Your real role flags (admin + whatever else)" },
                        { id: "solo",  label: "Solo",  title: "Hide all coach + admin UI" },
                        { id: "coach", label: "Coach", title: "Show coach UI, hide admin UI" },
                      ].map(opt => {
                        const active = appViewAsRole === opt.id;
                        return (
                          <button
                            key={opt.id}
                            type="button"
                            onClick={() => appSetViewAsRole && appSetViewAsRole(opt.id)}
                            title={opt.title}
                            style={{
                              fontSize: 11, fontWeight: 700, padding: "5px 12px",
                              background: active ? "var(--color-primary)" : "var(--color-card)",
                              color: active ? "#fff" : "var(--color-text-dim)",
                              border: "none", cursor: "pointer",
                              transition: "background 0.15s, color 0.15s",
                            }}>
                            {opt.label}
                          </button>
                        );
                      })}
                    </div>
                    {/* Orthogonal "+ parent" toggle. Composes with the role
                        picker so admins can preview coach+parent or
                        solo+parent UIs (the 👪 nav + ParentDashboard
                        surface when flipped on). */}
                    <label
                      title="Also pretend you have at least one linked guardian row (shows 👪 nav + Parent view). Composes with the role picker."
                      style={{
                        display: "inline-flex", alignItems: "center", gap: 6,
                        marginLeft: 10, fontSize: 11, fontWeight: 600,
                        color: appViewAsParent ? "var(--color-primary)" : "var(--color-text-dim)",
                        cursor: "pointer", userSelect: "none",
                      }}>
                      <input
                        type="checkbox"
                        checked={!!appViewAsParent}
                        onChange={(e) => appSetViewAsParent && appSetViewAsParent(e.target.checked)}
                        style={{ margin: 0, cursor: "pointer" }}
                      />
                      + parent
                    </label>
                  </div>
                )}

                {tab === "account" && (
                <div style={{ padding: "18px 20px", borderTop: "1px solid var(--color-card)" }}>
                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 }}>
                    <div style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em" }}>Active sessions ({sessions.length})</div>
                    {sessions.length > 1 && (
                      <button onClick={handleSignoutOthers} disabled={revoking} style={{ background: "var(--color-border)", border: "none", borderRadius: 6, padding: "4px 10px", fontSize: 11, color: "var(--color-text)", cursor: revoking ? "wait" : "pointer", fontWeight: 600 }}>
                        {revoking ? "Revoking…" : "Sign out everywhere else"}
                      </button>
                    )}
                  </div>
                  {revokeMsg && (
                    <div style={{ fontSize: 12, color: "#10b981", marginBottom: 12 }}>{revokeMsg}</div>
                  )}
                  <div style={{ display: "grid", gap: 8, fontSize: 12 }}>
                    {sessions.map(s => (
                      <div key={s.id_prefix} style={{ background: s.is_current ? "#1e3a5f" : "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 8, padding: 10 }}>
                        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 8 }}>
                          <span style={{ color: "var(--color-text)", fontWeight: 600, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                            {s.user_agent ? s.user_agent.slice(0, 70) : "Unknown browser"}
                            {s.is_current && <span style={{ marginLeft: 8, fontSize: 10, color: "#10b981", fontWeight: 700 }}>● this device</span>}
                          </span>
                          <span style={{ color: "var(--color-text-dim)", fontSize: 10, flexShrink: 0 }}>{s.id_prefix}</span>
                        </div>
                        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 4 }}>
                          <span style={{ color: "var(--color-text-dim)", fontSize: 11 }}>{s.ip || "—"} · last seen {fmtTime(s.last_seen_at)}</span>
                          {!s.is_current && (
                            <button onClick={() => handleRevokeOne(s.id_prefix)} style={{ background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 6, padding: "2px 8px", fontSize: 10, color: "#cbd5e1", cursor: "pointer", fontWeight: 600 }}>
                              Revoke
                            </button>
                          )}
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
                )}
              </>
            )}
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  ADMIN VIEW — users, invites, audit log
    // ═══════════════════════════════════════════════════════════════
    // ─── Catalog view (Phase I read-only browse) ────────────────────────
    // Coach-gated. Walks the AUTHORED workout-set bank for the selected
    // pool mode / section / type and lists labels + totalYards + set count.
    // Click a row to expand the full set list. Search by label, filter by
    // equipment / zone. Print stylesheet expands everything for paper.
    const CATALOG_SECTIONS = ["warmup", "drill", "kick", "main", "cooldown"];
    const CATALOG_TYPES    = ["all", "im", "distance", "sprint", "endurance", "technique", "mixed", "back", "breast", "fly"];
    // Synthetic "all equipment on" state — passed to equipmentForSet so the
    // catalog renders every applicable badge regardless of the user's actual
    // equipment selections.
    const CATALOG_ALL_EQUIP = { kickboard: "preferred", fins: "preferred", paddles: "preferred", pullBuoy: "preferred", snorkel: "preferred" };

    // Does this option's sets satisfy the given equipment requirement key?
    // (Reuses EQUIP_REQUIREMENTS — the same mapping the generator uses.)
    function catalogOptionUsesEquip(opt, equipKey) {
      const tags = EQUIP_REQUIREMENTS[equipKey] || [];
      return (opt.sets || []).some(s => s.eq && tags.includes(s.eq));
    }

    // Teams view (relationships scope, Stage 1 / R-A).
    // Coach-gated container for the team layer of the org hierarchy. v1
    // surfaces only the owner role; team-admin tier is in the data model
    // but not exposed in UI until R-J. See RELATIONSHIPS_SCOPE.md.
    const TEAM_TYPE_LABELS = {
      high_school: "High School",
      summer:      "Summer",
      club:        "Club",
      masters:     "Masters",
    };
    const TEAM_TYPE_DESCRIPTIONS = {
      high_school: "FERPA-shape: minor protections, restricted disclosure, parental consent for under-18.",
      summer:      "Mixed-age seasonal program. Minor protections apply if under-18 members present.",
      club:        "Year-round mixed-age (USA Swimming pattern). Under-13 protections apply per member.",
      masters:     "Adults only (18+). Minimal compliance burden.",
    };

    // Parent dashboard (PARENT_PORTAL_MVP_SCOPE.md §3 — 2026-05-27).
    // Top-level view for users with at least one active guardian row.
    // Shown when they toggle 👪 in nav, regardless of whether they're
    // also a coach or swimmer themselves. The dashboard surfaces last
    // week's recap + this-week schedule for each linked swimmer,
    // plus a pause-digest toggle. Read-only — no write actions from
    // here per spec.
    function ParentDashboard({ me }) {
      const [swimmers,    setSwimmers]    = React.useState(null);   // null = loading
      const [thisWeek,    setThisWeek]    = React.useState(null);   // digest of week just ended
      const [selectedId,  setSelectedId]  = React.useState(null);   // managed_id or swimmer_sub
      const [paused,      setPaused]      = React.useState(!!me?.digest_paused);
      const [savingPause, setSavingPause] = React.useState(false);
      const [msg,         setMsg]         = React.useState(null);

      React.useEffect(() => {
        let cancelled = false;
        (async () => {
          try {
            const [swRes, dgRes] = await Promise.all([
              fetch("/api/parent/swimmers", { cache: "no-store" }),
              fetch("/api/parent/digest",   { cache: "no-store" }),
            ]);
            if (!swRes.ok) throw new Error(`HTTP ${swRes.status} (swimmers)`);
            const sw = await swRes.json();
            if (cancelled) return;
            setSwimmers(Array.isArray(sw) ? sw : []);
            if (sw.length > 0) {
              setSelectedId(sw[0].managed_id || sw[0].swimmer_sub);
            }
            if (dgRes.ok) {
              const d = await dgRes.json();
              setThisWeek(d);
            }
          } catch (e) {
            if (!cancelled) { setSwimmers([]); setMsg(e.message); }
          }
        })();
        return () => { cancelled = true; };
      }, []);

      const togglePause = async () => {
        if (savingPause) return;
        setSavingPause(true); setMsg(null);
        const next = !paused;
        try {
          const res = await fetch("/api/parent/settings", {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ digest_paused: next }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setPaused(next);
        } catch (e) {
          setMsg(`Couldn't update digest preference: ${e.message}`);
        } finally {
          setSavingPause(false);
        }
      };

      if (swimmers === null) {
        return <div style={{ color: "var(--color-text-dim)", fontSize: 13, padding: 24 }}>Loading…</div>;
      }
      if (swimmers.length === 0) {
        return (
          <div>
            <h2 style={{ color: "var(--color-text)", marginTop: 0 }}>👪 Parent view</h2>
            <div className="card" style={{ padding: 20, borderRadius: 10, color: "var(--color-text-dim)", fontSize: 14, lineHeight: 1.6 }}>
              <p style={{ marginTop: 0 }}>
                No swimmers linked yet. Once a coach finishes the invite
                process, your swimmer will show up here with their weekly
                recap and what's coming up.
              </p>
              <p style={{ marginBottom: 0, color: "var(--color-text-muted)", fontSize: 13 }}>
                If you weren't expecting to see this view, you can ignore it
                — nothing changes for your account.
              </p>
            </div>
          </div>
        );
      }

      const blocks = (thisWeek && Array.isArray(thisWeek.swimmers)) ? thisWeek.swimmers : [];
      const selected = swimmers.find(s => (s.managed_id || s.swimmer_sub) === selectedId) || swimmers[0];
      const block = blocks.find(b => {
        const bid = b.swimmer?.managed_id || b.swimmer?.swimmer_sub;
        return bid === (selected.managed_id || selected.swimmer_sub);
      });

      return (
        <div>
          <h2 style={{ color: "var(--color-text)", marginTop: 0 }}>
            👪 Parent view
            <span style={{ marginLeft: 10, fontSize: 12, fontWeight: 500, color: "var(--color-text-muted)" }}>
              ({swimmers.length} {swimmers.length === 1 ? "swimmer" : "swimmers"})
            </span>
          </h2>

          {/* Swimmer selector */}
          {swimmers.length > 1 && (
            <div style={{ display: "flex", gap: 6, marginBottom: 14, flexWrap: "wrap" }}>
              {swimmers.map(s => {
                const sid = s.managed_id || s.swimmer_sub;
                const active = sid === selectedId;
                return (
                  <button key={sid}
                    onClick={() => setSelectedId(sid)}
                    style={{
                      padding: "6px 12px", borderRadius: 999,
                      border: "1px solid " + (active ? "var(--color-primary)" : "var(--color-border)"),
                      background: active ? "var(--color-primary)" : "var(--color-card)",
                      color: active ? "var(--color-bg)" : "var(--color-text)",
                      fontSize: 12, fontWeight: 600, cursor: "pointer",
                    }}>
                    {s.display_name || s.initials || "Swimmer"}
                    {s.age != null && <span style={{ marginLeft: 6, opacity: 0.75 }}>· {s.age}y</span>}
                  </button>
                );
              })}
            </div>
          )}

          {/* This week's recap card for the selected swimmer */}
          <div className="card" style={{ padding: 16, borderRadius: 10, marginBottom: 14 }}>
            <h3 style={{ color: "var(--color-text)", marginTop: 0, marginBottom: 4, fontSize: 15 }}>
              {selected.display_name || "Swimmer"}
            </h3>
            <div style={{ fontSize: 12, color: "var(--color-text-muted)", marginBottom: 12 }}>
              Last week (Mon–Sun){thisWeek?.weekStart ? ` · week of ${thisWeek.weekStart}` : ""}
            </div>
            {block ? (
              <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(140px, 1fr))", gap: 12 }}>
                <Stat label="Workouts done"  value={`${block.done || 0} / ${block.assigned || 0}`} />
                <Stat label="Yardage"        value={(block.yardage || 0).toLocaleString()} />
                {block.attendance && block.attendance.total > 0 ? (
                  <Stat label="Attendance" value={`${block.attendance.done} / ${block.attendance.total}`} />
                ) : null}
                <Stat label="Next week" value={`${block.upcoming || 0} scheduled`} />
              </div>
            ) : (
              <div style={{ color: "var(--color-text-dim)", fontSize: 13, fontStyle: "italic" }}>
                No data yet for last week. Once the coach assigns workouts (or
                this swimmer logs their own), recaps will appear here.
              </div>
            )}
          </div>

          {/* Home address (Locations P2) — guardian manages + consent toggle.
              Household/siblings (P3) shown below it. */}
          <div className="card" style={{ padding: 14, borderRadius: 10, marginBottom: 14 }}>
            <AddressManager key={selected.managed_id || selected.swimmer_sub} swimmerRef={selected.managed_id || selected.swimmer_sub} showConsent={true} />
            <HouseholdSiblings key={"hh-" + (selected.managed_id || selected.swimmer_sub)} swimmerRef={selected.managed_id || selected.swimmer_sub} />
          </div>

          {/* Pause digest toggle */}
          <div className="card" style={{ padding: 14, borderRadius: 10, marginBottom: 14, display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12, flexWrap: "wrap" }}>
            <div>
              <div style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 600, marginBottom: 2 }}>
                Weekly Sunday digest
              </div>
              <div style={{ color: "var(--color-text-muted)", fontSize: 12 }}>
                {paused
                  ? "Paused. You can still check this page anytime."
                  : "Sent Sunday evening (US-Eastern). Pause anytime."}
              </div>
            </div>
            <button onClick={togglePause}
              disabled={savingPause}
              style={{
                padding: "6px 12px", borderRadius: 8,
                border: "1px solid " + (paused ? "var(--color-primary)" : "var(--color-border)"),
                background: paused ? "var(--color-primary)" : "var(--color-card)",
                color: paused ? "var(--color-bg)" : "var(--color-text)",
                fontSize: 12, fontWeight: 700,
                cursor: savingPause ? "wait" : "pointer",
                opacity: savingPause ? 0.6 : 1,
              }}>
              {paused ? "▶ Resume" : "⏸ Pause"}
            </button>
          </div>

          {msg && <div style={{ color: "var(--color-warn)", fontSize: 12, marginTop: 10 }}>{msg}</div>}
        </div>
      );
    }

    // Team roster v1 (2026-05-27). 🏊 Roster tab in TeamsView showing every
    // swimmer in any active group under the team, grouped by group section.
    // Read-only for all team coaches. Backed by /api/teams/:id/roster.
    function TeamRosterTab({ teamId }) {
      const [data, setData] = React.useState(null);                            // null = loading
      const [msg,  setMsg]  = React.useState(null);
      React.useEffect(() => {
        let cancelled = false;
        (async () => {
          try {
            const res = await fetch(`/api/teams/${teamId}/roster`, { cache: "no-store" });
            if (!res.ok) throw new Error(`HTTP ${res.status}`);
            const d = await res.json();
            if (!cancelled) setData(Array.isArray(d) ? d : []);
          } catch (e) {
            if (!cancelled) { setMsg(`Error loading roster: ${e.message}`); setData([]); }
          }
        })();
        return () => { cancelled = true; };
      }, [teamId]);

      if (data === null) {
        return <div style={{ color: "var(--color-text-dim)", fontSize: 12, padding: 12 }}>Loading roster…</div>;
      }
      const totalMembers = data.reduce((s, g) => s + g.member_count, 0);

      return (
        <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 }}>
            <h3 style={{ color: "var(--color-text)", marginTop: 0, marginBottom: 0, fontSize: 15 }}>
              🏊 Team Roster
              <span style={{ marginLeft: 8, fontSize: 12, fontWeight: 600, color: "var(--color-text-muted)" }}>
                ({totalMembers} {totalMembers === 1 ? "swimmer" : "swimmers"} across {data.length} {data.length === 1 ? "group" : "groups"})
              </span>
            </h3>
          </div>
          {data.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic", padding: 12 }}>
              No active groups in this team yet. Create one from the Groups tab.
            </div>
          ) : (
            data.map(section => (
              <div key={section.group_id} style={{ marginBottom: 18 }}>
                <div style={{ display: "flex", alignItems: "baseline", gap: 8, padding: "6px 0", borderBottom: "2px solid var(--color-primary)", marginBottom: 8 }}>
                  <span style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 700 }}>{section.group_name}</span>
                  {section.pool_mode && <span style={{ fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#3b82f622", border: "1px solid var(--color-primary)", color: "var(--color-primary)", fontWeight: 700 }}>{section.pool_mode}</span>}
                  <span style={{ color: "var(--color-text-muted)", fontSize: 11, marginLeft: "auto" }}>
                    {section.member_count} {section.member_count === 1 ? "swimmer" : "swimmers"}
                  </span>
                </div>
                {section.member_count === 0 ? (
                  <div style={{ color: "var(--color-text-dim)", fontSize: 11, fontStyle: "italic", padding: "4px 8px" }}>
                    No swimmers in this group yet.
                  </div>
                ) : (
                  <div style={{ display: "grid", gridTemplateColumns: "1fr auto auto", gap: 8, fontSize: 12 }}>
                    {section.members.map(m => (
                      <React.Fragment key={m.id}>
                        <div style={{ color: "var(--color-text)", padding: "4px 8px", borderBottom: "1px solid var(--color-card)" }}>
                          {m.display_name || m.initials || "(no name)"}
                          {m.is_minor === true && <span title="Minor" style={{ marginLeft: 6, fontSize: 10, padding: "1px 5px", borderRadius: 3, background: "rgba(245,158,11,0.15)", color: "var(--color-warn)", fontWeight: 700 }}>minor</span>}
                          {m.is_coppa_protected === true && <span title="COPPA-protected (under 13)" style={{ marginLeft: 4, fontSize: 10, padding: "1px 5px", borderRadius: 3, background: "rgba(239,68,68,0.15)", color: "var(--color-destructive)", fontWeight: 700 }}>U13</span>}
                        </div>
                        <div style={{ color: "var(--color-text-muted)", padding: "4px 8px", borderBottom: "1px solid var(--color-card)", fontVariantNumeric: "tabular-nums" }}>
                          {m.age != null ? `${m.age}y` : "—"}
                        </div>
                        <div style={{ color: "var(--color-text-muted)", padding: "4px 8px", borderBottom: "1px solid var(--color-card)" }}>
                          {m.gender || "—"}
                        </div>
                      </React.Fragment>
                    ))}
                  </div>
                )}
              </div>
            ))
          )}
          {msg && <div style={{ color: "var(--color-warn)", fontSize: 12, marginTop: 10 }}>{msg}</div>}
        </div>
      );
    }

    // Team Curation v1 (TEAM_CURATION_SCOPE.md slice 4 — 2026-05-27).
    // Settings tab inside TeamsView. Owner + admin can write team-level
    // favorites/disfavorites + 3 defaults (pace_base, disfavor_mode,
    // equipment_modes). Regular coaches see read-only with "set by … on …"
    // attribution. Apply-to-roster button (v1: pace_base only) bulk-pushes
    // the default to every active swimmer in any group under the team.
    function TeamSettingsTab({ teamId, viewerRole, archived }) {
      const canWrite = viewerRole === "owner" || viewerRole === "admin";
      // fmtDate is defined inside several other components; keep this one
      // self-contained so we don't pull from a closure that won't exist.
      const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" }) : "—";
      const [curation, setCuration] = React.useState(null);                   // { favorites, disfavorites }
      const [settings, setSettings] = React.useState(null);                   // { default_pace_base, default_disfavor_mode, default_equipment_modes }
      const [msg, setMsg]           = React.useState(null);
      const [newFav, setNewFav]     = React.useState("");
      const [newDisfav, setNewDisfav] = React.useState("");
      const [paceDraft, setPaceDraft] = React.useState("");                   // pending edit
      const [busy, setBusy]         = React.useState(false);
      // Ownership transfer (owner-only section).
      const [coaches, setCoaches]                 = React.useState([]);
      const [pendingTransfer, setPendingTransfer] = React.useState(null);
      const [transferTarget, setTransferTarget]   = React.useState("");
      const [transferBusy, setTransferBusy]       = React.useState(false);

      const load = React.useCallback(async () => {
        try {
          const [cRes, sRes, coRes, ptRes] = await Promise.all([
            fetch(`/api/teams/${teamId}/curation`, { cache: "no-store" }),
            fetch(`/api/teams/${teamId}/settings`, { cache: "no-store" }),
            fetch(`/api/teams/${teamId}/coaches`, { cache: "no-store" }),
            fetch(`/api/teams/${teamId}/pending-transfer`, { cache: "no-store" }),
          ]);
          if (!cRes.ok) throw new Error(`curation HTTP ${cRes.status}`);
          if (!sRes.ok) throw new Error(`settings HTTP ${sRes.status}`);
          const c = await cRes.json();
          const s = await sRes.json();
          setCuration(c);
          setSettings(s);
          setPaceDraft(s.default_pace_base || "");
          if (coRes.ok) { try { setCoaches(await coRes.json()); } catch (_) {} }
          if (ptRes.ok) { try { const pt = await ptRes.json(); setPendingTransfer(pt && pt.id ? pt : null); } catch (_) {} }
        } catch (e) {
          setMsg(`Error loading: ${e.message}`);
          setCuration({ favorites: [], disfavorites: [] });
          setSettings({ default_pace_base: null, default_disfavor_mode: null, default_equipment_modes: null });
        }
      }, [teamId]);
      React.useEffect(() => { load(); }, [load]);

      const addCuration = async (kind, label) => {
        const cleaned = (label || "").trim();
        if (!cleaned) return;
        setBusy(true); setMsg(null);
        try {
          const url = kind === "fav" ? `/api/teams/${teamId}/favorites` : `/api/teams/${teamId}/disfavorites`;
          const res = await fetch(url, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ label: cleaned }),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) {
            // Surface mutex conflicts nicely (in_favorites / in_disfavorites)
            if (j.error === "in_favorites")     throw new Error(`"${cleaned}" is already a team favorite. Remove it there first.`);
            if (j.error === "in_disfavorites")  throw new Error(`"${cleaned}" is already a team disfavorite. Remove it there first.`);
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          if (kind === "fav") setNewFav(""); else setNewDisfav("");
          await load();
        } catch (e) { setMsg(`Add failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const removeCuration = async (kind, label) => {
        if (!window.confirm(`Remove "${label}" from team ${kind === "fav" ? "favorites" : "disfavorites"}?`)) return;
        setBusy(true); setMsg(null);
        try {
          const url = kind === "fav"
            ? `/api/teams/${teamId}/favorites/${encodeURIComponent(label)}`
            : `/api/teams/${teamId}/disfavorites/${encodeURIComponent(label)}`;
          const res = await fetch(url, { method: "DELETE", headers: csrfHeaders() });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(`Remove failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const savePace = async () => {
        const value = paceDraft.trim() || null;
        if (value && !/^\d{1,2}:\d{2}$/.test(value)) { setMsg("Pace must look like 2:00."); return; }
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/settings`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ pace_base: value }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(`Save failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const saveMode = async (value) => {
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/settings`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ disfavor_mode: value }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(`Save failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const applyPaceToRoster = async () => {
        if (!settings?.default_pace_base) { setMsg("Set the team pace baseline before pushing it to the roster."); return; }
        const ok = window.confirm(
          `Push pace baseline "${settings.default_pace_base}" to every active swimmer in any group under this team?\n\n` +
          "This overwrites their existing pace. One-time action."
        );
        if (!ok) return;
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/settings/apply-to-roster`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ field: "pace_base" }),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setMsg(`✓ Pushed pace ${j.value} to ${j.count} swimmer${j.count === 1 ? "" : "s"}.`);
        } catch (e) { setMsg(`Apply failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const proposeTransfer = async () => {
        if (!transferTarget) return;
        if (!window.confirm("Propose this admin as the new team owner? They get 30 days to accept (or it auto-applies); you can cancel anytime before then. You'll become an admin and your team-shared + public sets move to them.")) return;
        setTransferBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/transfer-ownership`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ to_sub: transferTarget }),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setTransferTarget("");
          await load();
        } catch (e) { setMsg(`Transfer failed: ${e.message}`); }
        finally { setTransferBusy(false); }
      };

      const cancelTransfer = async () => {
        if (!window.confirm("Cancel the pending ownership transfer?")) return;
        setTransferBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/transfer-ownership/cancel`, { method: "POST", headers: csrfHeaders() });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          await load();
        } catch (e) { setMsg(`Cancel failed: ${e.message}`); }
        finally { setTransferBusy(false); }
      };

      if (curation === null || settings === null) {
        return <div style={{ color: "var(--color-text-dim)", fontSize: 12, padding: 12 }}>Loading team settings…</div>;
      }

      const renderCurationList = (kind, items, accent) => (
        <div style={{ marginBottom: 12 }}>
          {items.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic", padding: "6px 0" }}>
              No team {kind === "fav" ? "favorites" : "disfavorites"} set.
            </div>
          ) : (
            items.map(it => (
              <div key={it.label} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "6px 8px", borderBottom: "1px solid var(--color-border)", gap: 8 }}>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 600, overflow: "hidden", textOverflow: "ellipsis" }}>{it.label}</div>
                  <div style={{ color: "var(--color-text-dim)", fontSize: 10, marginTop: 2 }}>
                    set by <span style={{ fontFamily: "monospace" }}>{(it.set_by_coach_sub || "").slice(-8)}</span> · {fmtDate(it.created_at)}
                  </div>
                </div>
                {canWrite && !archived && (
                  <button onClick={() => removeCuration(kind, it.label)} disabled={busy}
                    title="Remove from team list"
                    style={{ padding: "3px 8px", background: "transparent", border: "1px solid var(--color-destructive)", borderRadius: 5, color: "var(--color-destructive)", fontSize: 11, cursor: busy ? "not-allowed" : "pointer" }}>
                    ×
                  </button>
                )}
              </div>
            ))
          )}
          {canWrite && !archived && (
            <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
              <input value={kind === "fav" ? newFav : newDisfav}
                onChange={e => kind === "fav" ? setNewFav(e.target.value) : setNewDisfav(e.target.value)}
                onKeyDown={e => { if (e.key === "Enter") addCuration(kind, kind === "fav" ? newFav : newDisfav); }}
                placeholder={`Add team ${kind === "fav" ? "favorite" : "disfavorite"} (e.g. "4 × 200 IM")`}
                style={{ flex: 1, padding: "6px 9px", fontSize: 12, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
              <button onClick={() => addCuration(kind, kind === "fav" ? newFav : newDisfav)} disabled={busy}
                style={{ padding: "6px 12px", background: accent, color: "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: busy ? "not-allowed" : "pointer" }}>
                + Add
              </button>
            </div>
          )}
        </div>
      );

      return (
        <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
          <h3 style={{ color: "var(--color-text)", marginTop: 0, fontSize: 15 }}>
            ⚙ Team Settings
            {!canWrite && (
              <span style={{ marginLeft: 8, fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)" }}>(read-only — owner + admin can edit)</span>
            )}
          </h3>

          <div style={{ marginBottom: 18 }}>
            <div style={{ color: "var(--color-positive)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 8 }}>
              ⭐ Team Favorites ({curation.favorites.length})
            </div>
            {renderCurationList("fav", curation.favorites, "var(--color-positive)")}
          </div>

          <div style={{ marginBottom: 18 }}>
            <div style={{ color: "var(--color-destructive)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 8 }}>
              👎 Team Disfavorites ({curation.disfavorites.length})
            </div>
            {renderCurationList("disfav", curation.disfavorites, "var(--color-destructive)")}
          </div>

          <TeamFacilitiesSection teamId={teamId} canWrite={canWrite && !archived} />

          {viewerRole === "owner" && (
            <div style={{ borderTop: "1px solid var(--color-border)", paddingTop: 14, marginTop: 6, marginBottom: 18 }}>
              <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
                Ownership
              </div>
              {pendingTransfer ? (
                <div style={{ background: "#f59e0b18", border: "1px solid var(--color-warn)", borderRadius: 8, padding: "10px 12px" }}>
                  <div style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 600 }}>⏳ Transfer in progress</div>
                  <div style={{ color: "var(--color-text-muted)", fontSize: 12, marginTop: 4 }}>
                    You proposed <span style={{ fontFamily: "monospace" }}>{(pendingTransfer.to_sub || "").slice(-8)}</span> as the new owner. Auto-completes {fmtDate(pendingTransfer.auto_complete_at)}.
                  </div>
                  {!archived && (
                    <button onClick={cancelTransfer} disabled={transferBusy}
                      style={{ marginTop: 8, padding: "5px 12px", background: "transparent", border: "1px solid var(--color-destructive)", borderRadius: 5, color: "var(--color-destructive)", fontSize: 12, fontWeight: 700, cursor: transferBusy ? "not-allowed" : "pointer" }}>
                      {transferBusy ? "Working…" : "Cancel transfer"}
                    </button>
                  )}
                </div>
              ) : (() => {
                const admins = (coaches || []).filter(c => c.role === "admin");
                return (
                  <div>
                    <p style={{ color: "var(--color-text-dim)", fontSize: 11, margin: "0 0 8px", lineHeight: 1.5 }}>
                      Hand the team off to an existing admin. They get a 30-day window to accept (or it auto-applies); you become an admin and your team-shared + public sets move to them.
                    </p>
                    {admins.length === 0 ? (
                      <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>
                        No admins to transfer to — promote a coach to admin first (Members &amp; Coaches tab).
                      </div>
                    ) : !archived ? (
                      <div style={{ display: "flex", gap: 6 }}>
                        <select value={transferTarget} onChange={e => setTransferTarget(e.target.value)}
                          style={{ flex: 1, padding: "6px 9px", fontSize: 12, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                          <option value="">— pick an admin —</option>
                          {admins.map(a => <option key={a.coach_sub} value={a.coach_sub}>{a.display_name || a.coach_sub.slice(-8)}</option>)}
                        </select>
                        <button onClick={proposeTransfer} disabled={!transferTarget || transferBusy}
                          style={{ padding: "6px 12px", background: (!transferTarget || transferBusy) ? "var(--color-border)" : "var(--color-warn)", color: (!transferTarget || transferBusy) ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: (!transferTarget || transferBusy) ? "not-allowed" : "pointer" }}>
                          Transfer →
                        </button>
                      </div>
                    ) : null}
                  </div>
                );
              })()}
            </div>
          )}

          <div style={{ borderTop: "1px solid var(--color-border)", paddingTop: 14, marginTop: 6 }}>
            <div style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>
              Team Defaults
            </div>
            <p style={{ color: "var(--color-text-dim)", fontSize: 11, margin: "0 0 12px", lineHeight: 1.5, fontStyle: "italic" }}>
              Defaults apply when a brand-new swimmer joins a group on this team. Use "Push to roster" to apply to your current swimmers (one-time).
            </p>

            <div style={{ display: "grid", gridTemplateColumns: "120px 1fr auto", gap: 10, alignItems: "center", marginBottom: 10 }}>
              <label style={{ color: "var(--color-text-muted)", fontSize: 12, fontWeight: 600 }}>Pace baseline</label>
              {canWrite && !archived ? (
                <input value={paceDraft}
                  onChange={e => setPaceDraft(e.target.value)}
                  placeholder="e.g. 2:00"
                  style={{ padding: "6px 9px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
              ) : (
                <span style={{ color: "var(--color-text)", fontSize: 13, fontVariantNumeric: "tabular-nums" }}>{settings.default_pace_base || "—"}</span>
              )}
              {canWrite && !archived && (
                <div style={{ display: "flex", gap: 6 }}>
                  <button onClick={savePace} disabled={busy || paceDraft === (settings.default_pace_base || "")}
                    style={{ padding: "6px 12px", background: (busy || paceDraft === (settings.default_pace_base || "")) ? "var(--color-border)" : "var(--color-primary)", color: (busy || paceDraft === (settings.default_pace_base || "")) ? "#cbd5e1" : "#fff", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: (busy || paceDraft === (settings.default_pace_base || "")) ? "not-allowed" : "pointer" }}>
                    Save
                  </button>
                  <button onClick={applyPaceToRoster} disabled={busy || !settings.default_pace_base}
                    title={settings.default_pace_base ? "Push to every active swimmer in every group under this team (one-time, overwrites)" : "Set + save a pace first"}
                    style={{ padding: "6px 12px", background: "transparent", border: "1px solid var(--color-warn)", borderRadius: 5, color: "var(--color-warn)", fontSize: 12, fontWeight: 700, cursor: (busy || !settings.default_pace_base) ? "not-allowed" : "pointer" }}>
                    Push to roster
                  </button>
                </div>
              )}
            </div>

            <div style={{ display: "grid", gridTemplateColumns: "120px 1fr auto", gap: 10, alignItems: "center", marginBottom: 4 }}>
              <label style={{ color: "var(--color-text-muted)", fontSize: 12, fontWeight: 600 }}>Disfavor mode</label>
              {canWrite && !archived ? (
                <div style={{ display: "flex", gap: 4 }}>
                  {[
                    { id: "downweight", label: "Downweight (0.25×)" },
                    { id: "exclude",    label: "Exclude (hard)"   },
                  ].map(opt => {
                    const active = settings.default_disfavor_mode === opt.id;
                    return (
                      <button key={opt.id} onClick={() => saveMode(opt.id)} disabled={busy}
                        className="pill"
                        style={{
                          border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                          background: active ? "#3b82f622" : "transparent",
                          color: active ? "var(--color-primary)" : "var(--color-text-muted)",
                          fontSize: 11, cursor: busy ? "not-allowed" : "pointer",
                        }}>
                        {opt.label}
                      </button>
                    );
                  })}
                  {settings.default_disfavor_mode && (
                    <button onClick={() => saveMode(null)} disabled={busy}
                      title="Clear default"
                      style={{ padding: "3px 8px", background: "transparent", border: "1px solid var(--color-border-strong)", borderRadius: 5, color: "var(--color-text-muted)", fontSize: 11, cursor: busy ? "not-allowed" : "pointer" }}>
                      Clear
                    </button>
                  )}
                </div>
              ) : (
                <span style={{ color: "var(--color-text)", fontSize: 13 }}>{settings.default_disfavor_mode || "—"}</span>
              )}
              <div />
            </div>

            <div style={{ color: "var(--color-text-dim)", fontSize: 11, marginTop: 10, fontStyle: "italic" }}>
              Equipment defaults + apply-to-roster for non-pace fields land in v1.1.
            </div>
          </div>

          {msg && <div style={{ color: msg.startsWith("✓") ? "var(--color-positive)" : "var(--color-warn)", fontSize: 12, marginTop: 12, padding: "8px 10px", background: msg.startsWith("✓") ? "rgba(34,197,94,0.1)" : "rgba(245,158,11,0.12)", borderRadius: 5 }}>{msg}</div>}
        </div>
      );
    }

    function TeamsView() {
      const [teams,       setTeams]       = React.useState(null);                 // null = loading
      const [selectedId,  setSelectedId]  = React.useState(null);
      const [detail,      setDetail]      = React.useState(null);                 // null = unloaded/loading
      const [creating,    setCreating]    = React.useState(false);
      const [createName,  setCreateName]  = React.useState("");
      const [createType,  setCreateType]  = React.useState("club");
      const [editingName, setEditingName] = React.useState(false);
      const [renameVal,   setRenameVal]   = React.useState("");
      const [addCoachSub, setAddCoachSub] = React.useState("");
      const [addCoachRole, setAddCoachRole] = React.useState("coach");           // R-J: role on add (coach / admin)
      const [coachPool,   setCoachPool]   = React.useState(null);                // null = unloaded
      // R-J: when remove fails with primary_on_groups, hold the conflict
      // info so the UI can prompt for per-group primary transfer.
      const [removeConflict, setRemoveConflict] = React.useState(null);          // { coachSub, groups: [...] }
      const [transferPlan,   setTransferPlan]   = React.useState({});            // { group_id: new_primary_sub }
      // Setforge rebrand 2026-05-20 — team detail tabs (REBRAND_SCOPE §8.2).
      // Members & Coaches / Groups / Events. Default = members.
      const [activeTab, setActiveTab] = React.useState("members");
      // R-C additions: groups in this team, team events, and managed-roster
      // (filtered to team) for the member-add picker inside each group.
      const [groups,        setGroups]        = React.useState(null);             // null = unloaded
      const [events,        setEvents]        = React.useState(null);
      const [managedInTeam, setManagedInTeam] = React.useState(null);
      const [creatingGroup, setCreatingGroup] = React.useState(false);
      const [newGroupName,  setNewGroupName]  = React.useState("");
      const [newGroupPool,  setNewGroupPool]  = React.useState("");
      const [creatingEvent, setCreatingEvent] = React.useState(false);
      const [newEventName,  setNewEventName]  = React.useState("");
      const [newEventDate,  setNewEventDate]  = React.useState("");
      const [editingEventId, setEditingEventId] = React.useState(null);
      const [editEventName,  setEditEventName]  = React.useState("");
      const [editEventDate,  setEditEventDate]  = React.useState("");
      // Meet-anchored taper (MEET_ANCHORED_TAPER_SCOPE §3.6): inline picker
      // appears when coach clicks 🎯 on an event row. anchoringEventId = ev.id
      // while the picker is open. Groups list fetched on open.
      const [anchoringEventId, setAnchoringEventId] = React.useState(null);
      const [anchorGroupsList, setAnchorGroupsList] = React.useState(null); // null while loading
      const [anchorTargetGroupId, setAnchorTargetGroupId] = React.useState("");
      const [anchorBusy, setAnchorBusy] = React.useState(false);
      const [msg,         setMsg]         = React.useState(null);
      // Ownership transfers proposed TO this coach (accept/decline banner).
      const [incomingTransfers, setIncomingTransfers] = React.useState([]);
      const [incomingBusy, setIncomingBusy] = React.useState(null);   // teamId being acted on
      const loadIncomingTransfers = React.useCallback(async () => {
        try {
          const res = await fetch("/api/me/incoming-transfers", { cache: "no-store" });
          if (res.ok) setIncomingTransfers(await res.json());
        } catch (_) {}
      }, []);
      React.useEffect(() => { loadIncomingTransfers(); }, [loadIncomingTransfers]);
      const respondTransfer = async (teamId, action) => {
        if (!window.confirm(action === "accept" ? "Accept ownership of this team? Roles swap immediately and the previous owner's team-shared + public sets move to you." : "Decline this ownership transfer?")) return;
        setIncomingBusy(teamId); setMsg(null);
        try {
          const res = await fetch(`/api/teams/${teamId}/transfer-ownership/${action}`, { method: "POST", headers: csrfHeaders() });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          await loadIncomingTransfers();
          await loadList();
          if (detail && String(detail.id) === String(teamId)) await loadDetail(teamId);
        } catch (e) { setMsg(`Transfer ${action} failed: ${e.message}`); }
        finally { setIncomingBusy(null); }
      };
      // Banner shown atop both list + detail views.
      const incomingBanner = (incomingTransfers && incomingTransfers.length > 0) ? (
        <div style={{ marginBottom: 16 }}>
          {incomingTransfers.map(t => (
            <div key={t.id} style={{ background: "#3b82f618", border: "1px solid var(--color-primary)", borderRadius: 10, padding: "12px 14px", marginBottom: 8 }}>
              <div style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 700 }}>🎯 You've been proposed as the new owner of {t.team_name}.</div>
              <div style={{ color: "var(--color-text-muted)", fontSize: 12, margin: "4px 0 10px" }}>
                Auto-applies {t.auto_complete_at ? new Date(t.auto_complete_at).toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" }) : "in 30 days"} if you do nothing.
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <button onClick={() => respondTransfer(t.team_id, "accept")} disabled={incomingBusy === t.team_id}
                  style={{ padding: "6px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: incomingBusy === t.team_id ? "not-allowed" : "pointer" }}>
                  {incomingBusy === t.team_id ? "Working…" : "Accept now"}
                </button>
                <button onClick={() => respondTransfer(t.team_id, "decline")} disabled={incomingBusy === t.team_id}
                  style={{ padding: "6px 14px", background: "transparent", border: "1px solid var(--color-destructive)", color: "var(--color-destructive)", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: incomingBusy === t.team_id ? "not-allowed" : "pointer" }}>
                  Decline
                </button>
              </div>
            </div>
          ))}
        </div>
      ) : null;

      const loadGroups = React.useCallback(async (teamId) => {
        if (!teamId) { setGroups(null); return; }
        try {
          const res = await fetch(`/api/teams/${teamId}/groups`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setGroups(await res.json());
        } catch (err) { setGroups([]); }
      }, []);
      const loadEvents = React.useCallback(async (teamId) => {
        if (!teamId) { setEvents(null); return; }
        try {
          const res = await fetch(`/api/teams/${teamId}/events`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setEvents(await res.json());
        } catch (err) { setEvents([]); }
      }, []);
      const loadManagedInTeam = React.useCallback(async (teamId) => {
        if (!teamId) { setManagedInTeam(null); return; }
        try {
          const res = await fetch("/api/managed-swimmers", { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const all = await res.json();
          setManagedInTeam((Array.isArray(all) ? all : []).filter(s => s.team_id === teamId && !s.archived));
        } catch (err) { setManagedInTeam([]); }
      }, []);

      const loadList = React.useCallback(async () => {
        try {
          const res = await fetch("/api/teams", { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setTeams(await res.json());
        } catch (err) {
          setTeams([]);
          setMsg(`Error loading teams: ${err.message}`);
        }
      }, []);

      const loadDetail = React.useCallback(async (id) => {
        if (!id) { setDetail(null); return; }
        setDetail(null);
        try {
          const res = await fetch(`/api/teams/${id}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const d = await res.json();
          setDetail(d);
          setRenameVal(d.name);
        } catch (err) {
          setMsg(`Error loading team: ${err.message}`);
        }
      }, []);

      // Composite team-detail loader — one GET seeds detail + groups + events
      // (was 3 parallel fetches). Cuts the team-select burst that, with the
      // managed-roster fetch, tripped 429s. Per-section loaders above stay for
      // post-mutation refresh (onChanged → loadGroups, create → loadEvents).
      const loadTeamDetail = React.useCallback(async (id) => {
        if (!id) { setDetail(null); setGroups(null); setEvents(null); return; }
        setDetail(null); setGroups(null); setEvents(null);
        try {
          const res = await fetch(`/api/teams/${id}/detail`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const d = await res.json();
          setDetail(d.team);
          setRenameVal(d.team.name);
          setGroups(Array.isArray(d.groups) ? d.groups : []);
          setEvents(Array.isArray(d.events) ? d.events : []);
        } catch (err) {
          setMsg(`Error loading team: ${err.message}`);
          setGroups([]); setEvents([]);
        }
      }, []);

      React.useEffect(() => { loadList(); }, [loadList]);
      React.useEffect(() => {
        loadTeamDetail(selectedId);
        loadManagedInTeam(selectedId);
      }, [selectedId, loadTeamDetail, loadManagedInTeam]);

      // Load the coach pool on first detail-open. Reused across visits to the
      // same team session; if a coach is added/removed they'll appear/disappear
      // on the next page-load (acceptable freshness for a picker).
      React.useEffect(() => {
        if (!selectedId || coachPool !== null) return;
        fetch("/api/coaches", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(setCoachPool)
          .catch(() => setCoachPool([]));
      }, [selectedId, coachPool]);

      const handleCreate = async () => {
        if (!createName.trim()) { setMsg("Name required"); return; }
        try {
          const res = await fetch("/api/teams", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ name: createName.trim(), team_type: createType }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setMsg(null);
          setCreateName(""); setCreateType("club"); setCreating(false);
          await loadList();
          setSelectedId(j.id);
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        }
      };

      const handleRename = async () => {
        if (!renameVal.trim() || renameVal === detail?.name) { setEditingName(false); return; }
        try {
          const res = await fetch(`/api/teams/${detail.id}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ name: renameVal.trim() }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setEditingName(false);
          await loadList();
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        }
      };

      const handleToggleArchive = async () => {
        if (!detail) return;
        const next = !detail.archived;
        const verb = next ? "archive" : "unarchive";
        if (!window.confirm(`${verb.charAt(0).toUpperCase() + verb.slice(1)} team "${detail.name}"?`)) return;
        try {
          const res = await fetch(`/api/teams/${detail.id}/archive`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ archived: next }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await loadList();
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        }
      };

      const handleAddCoach = async () => {
        if (!addCoachSub.trim()) { setMsg("coach_sub required"); return; }
        try {
          const res = await fetch(`/api/teams/${detail.id}/coaches`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ coach_sub: addCoachSub.trim(), role: addCoachRole }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setAddCoachSub("");
          setAddCoachRole("coach");
          setMsg(null);
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error adding coach: ${err.message}`);
        }
      };

      // R-J: promote/demote between admin and coach.
      const handleRoleChange = async (sub, newRole) => {
        try {
          const res = await fetch(`/api/teams/${detail.id}/coaches/${encodeURIComponent(sub)}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ role: newRole }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error changing role: ${err.message}`);
        }
      };

      const handleRemoveCoach = async (sub) => {
        if (!window.confirm("Remove this coach from the team?")) return;
        try {
          const res = await fetch(`/api/teams/${detail.id}/coaches/${encodeURIComponent(sub)}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            // R-J: surface primary_on_groups as a transfer-modal prompt.
            if (j.reason === "primary_on_groups") {
              setRemoveConflict({ coachSub: sub, groups: j.groups || [] });
              setTransferPlan({});
              return;
            }
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        }
      };

      // R-J: execute the per-group primary transfers then retry the remove.
      const executeRemoveWithTransfers = async () => {
        const conflict = removeConflict;
        if (!conflict) return;
        // Validate every conflicting group has a transfer destination set.
        const missing = conflict.groups.filter(g => !transferPlan[g.id]);
        if (missing.length > 0) {
          setMsg(`Pick a new primary for: ${missing.map(g => g.name).join(", ")}`);
          return;
        }
        try {
          for (const g of conflict.groups) {
            const res = await fetch(`/api/groups/${g.id}/primary-coach`, {
              method:  "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({ new_primary_sub: transferPlan[g.id] }),
            });
            if (!res.ok) {
              const j = await res.json().catch(() => ({}));
              throw new Error(`Transfer for "${g.name}" failed: ${j.error || `HTTP ${res.status}`}`);
            }
          }
          // Retry the original removal.
          const res = await fetch(`/api/teams/${detail.id}/coaches/${encodeURIComponent(conflict.coachSub)}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setRemoveConflict(null);
          setTransferPlan({});
          setMsg(null);
          await loadDetail(detail.id);
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        }
      };

      // R-C: create group within this team
      const handleCreateGroup = async () => {
        if (!newGroupName.trim()) { setMsg("Group name required"); return; }
        try {
          const res = await fetch(`/api/teams/${detail.id}/groups`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ name: newGroupName.trim(), pool_mode_default: newGroupPool || null }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setNewGroupName(""); setNewGroupPool(""); setCreatingGroup(false); setMsg(null);
          await loadGroups(detail.id);
        } catch (err) { setMsg(`Error creating group: ${err.message}`); }
      };

      // R-C: create team event
      const handleCreateEvent = async () => {
        if (!newEventName.trim()) { setMsg("Event name required"); return; }
        if (!newEventDate)        { setMsg("Event date required"); return; }
        try {
          const res = await fetch(`/api/teams/${detail.id}/events`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ name: newEventName.trim(), date: newEventDate }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setNewEventName(""); setNewEventDate(""); setCreatingEvent(false); setMsg(null);
          await loadEvents(detail.id);
        } catch (err) { setMsg(`Error creating event: ${err.message}`); }
      };

      // R-C polish: edit team event (inline)
      const startEditEvent = (ev) => {
        setEditingEventId(ev.id);
        setEditEventName(ev.name);
        setEditEventDate(ev.date);
        setMsg(null);
      };
      const cancelEditEvent = () => {
        setEditingEventId(null);
        setEditEventName("");
        setEditEventDate("");
      };
      const handleSaveEditEvent = async () => {
        if (!editEventName.trim()) { setMsg("Event name required"); return; }
        if (!editEventDate)        { setMsg("Event date required"); return; }
        try {
          const res = await fetch(`/api/events/${editingEventId}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ name: editEventName.trim(), date: editEventDate }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          cancelEditEvent();
          setMsg(null);
          await loadEvents(detail.id);
        } catch (err) { setMsg(`Error saving event: ${err.message}`); }
      };

      // R-C: delete team event
      const handleDeleteEvent = async (eventId) => {
        if (!window.confirm("Delete this event?")) return;
        try {
          const res = await fetch(`/api/events/${eventId}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await loadEvents(detail.id);
        } catch (err) { setMsg(`Error: ${err.message}`); }
      };

      // Open the inline anchor-picker for an event. Fetches the team's
      // groups so the coach can pick which group inherits this event as
      // its training-phase anchor. Per MEET_ANCHORED_TAPER_SCOPE §3.6.
      const openAnchorPicker = async (eventId) => {
        setAnchoringEventId(eventId);
        setAnchorGroupsList(null);
        setAnchorTargetGroupId("");
        try {
          const res = await fetch(`/api/teams/${detail.id}/groups`);
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const groups = await res.json();
          const live = (Array.isArray(groups) ? groups : []).filter(g => !g.archived);
          setAnchorGroupsList(live);
          if (live.length === 1) setAnchorTargetGroupId(String(live[0].id));
        } catch (err) {
          setMsg(`Error loading groups: ${err.message}`);
          setAnchoringEventId(null);
        }
      };

      const cancelAnchorPicker = () => {
        setAnchoringEventId(null);
        setAnchorGroupsList(null);
        setAnchorTargetGroupId("");
      };

      const handleSetAnchor = async () => {
        if (!anchoringEventId || !anchorTargetGroupId) return;
        setAnchorBusy(true);
        try {
          // event_id is a VARCHAR string (ev_xxx) on the server — pass through
          // unchanged. The initial implementation Number()-converted, which
          // turned the string into NaN and the server rejected the body.
          const res = await fetch(`/api/groups/${anchorTargetGroupId}/anchor`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ event_id: anchoringEventId }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          const data = await res.json();
          const groupName = anchorGroupsList?.find(g => String(g.id) === String(anchorTargetGroupId))?.name || "group";
          setMsg(`🎯 Anchor set for ${groupName} (${data.anchor?.weeks_out ?? "?"} wks out, suggested phase: ${data.anchor?.suggested_phase || "—"})`);
          cancelAnchorPicker();
        } catch (err) {
          setMsg(`Error: ${err.message}`);
        } finally {
          setAnchorBusy(false);
        }
      };

      // ── Render ──────────────────────────────────────────────────────
      const isOwner = detail?.viewer_role === "owner";

      // Detail panel
      if (selectedId && detail) {
        return (
          <div>
            {incomingBanner}
            <button onClick={() => { setSelectedId(null); setDetail(null); setEditingName(false); setMsg(null); }}
              style={{ marginBottom: 12, padding: "5px 11px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text-muted)", fontSize: 12, fontWeight: 600, cursor: "pointer" }}>
              ← All teams
            </button>
            <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12, flexWrap: "wrap", marginBottom: 10 }}>
                {editingName ? (
                  <div style={{ display: "flex", gap: 6, alignItems: "center", flex: 1 }}>
                    <input value={renameVal} onChange={e => setRenameVal(e.target.value)} autoFocus
                      onKeyDown={e => { if (e.key === "Enter") handleRename(); if (e.key === "Escape") { setEditingName(false); setRenameVal(detail.name); } }}
                      style={{ flex: 1, padding: "6px 10px", fontSize: 16, fontWeight: 700, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6 }} />
                    <button onClick={handleRename} style={{ padding: "5px 11px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>Save</button>
                    <button onClick={() => { setEditingName(false); setRenameVal(detail.name); }} style={{ padding: "5px 11px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>Cancel</button>
                  </div>
                ) : (
                  <h2 style={{ color: "var(--color-text)", margin: 0, fontSize: 20 }}>
                    {detail.name}
                    {detail.archived && <span style={{ marginLeft: 10, fontSize: 11, padding: "2px 8px", borderRadius: 999, background: "#64748b22", border: "1px solid #64748b", color: "var(--color-text-muted)" }}>archived</span>}
                    {isOwner && (
                      <button onClick={() => setEditingName(true)} title="Rename"
                        style={{ marginLeft: 10, padding: "3px 8px", background: "transparent", border: "1px solid var(--color-border)", borderRadius: 6, color: "var(--color-text-dim)", fontSize: 11, cursor: "pointer" }}>✎</button>
                    )}
                  </h2>
                )}
                <span style={{ fontSize: 11, padding: "3px 10px", borderRadius: 999, background: "#3b82f622", border: "1px solid var(--color-primary)", color: "var(--color-primary)", fontWeight: 700 }}>
                  {TEAM_TYPE_LABELS[detail.team_type] || detail.team_type}
                </span>
              </div>
              <div style={{ fontSize: 12, color: "var(--color-text-muted)", lineHeight: 1.5 }}>
                {TEAM_TYPE_DESCRIPTIONS[detail.team_type]}
              </div>
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 8 }}>
                Your role in this team: <span style={{ color: "#cbd5e1", fontWeight: 700 }}>{detail.viewer_role}</span>
                <span style={{ marginLeft: 10 }}>· Team ID: <code style={{ background: "var(--color-bg)", padding: "1px 5px", borderRadius: 3 }}>{detail.id}</code></span>
              </div>
              {isOwner && (
                <div style={{ marginTop: 12 }}>
                  <button onClick={handleToggleArchive}
                    style={{ padding: "5px 11px", background: detail.archived ? "var(--color-positive)" : "var(--color-text-dim)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    {detail.archived ? "Unarchive team" : "Archive team"}
                  </button>
                </div>
              )}
            </div>

            {/* Setforge rebrand 2026-05-20 — team detail tab nav.
                3 tabs: Members & Coaches / Groups / Events. */}
            <div style={{ display: "flex", gap: 4, borderBottom: "1px solid var(--color-border)", marginBottom: 16 }}>
              {[
                { id: "members",  label: "Members & Coaches", count: (detail.coaches || []).length },
                { id: "groups",   label: "Groups",            count: (groups || []).length },
                { id: "roster",   label: "🏊 Roster",         count: 0 },
                { id: "events",   label: "Events",            count: (events || []).length },
                { id: "settings", label: "⚙ Settings",        count: 0 },
              ].map(t => {
                const active = activeTab === t.id;
                return (
                  <button key={t.id} onClick={() => setActiveTab(t.id)}
                    style={{
                      padding: "9px 14px", border: "none", borderBottom: active ? "2px solid var(--color-primary)" : "2px solid transparent",
                      background: "transparent", color: active ? "var(--color-text)" : "var(--color-text-muted)",
                      fontSize: 13, fontWeight: active ? 700 : 600, cursor: "pointer",
                      marginBottom: -1,
                    }}>
                    {t.label}
                    {t.count > 0 && (
                      <span style={{ marginLeft: 6, fontSize: 11, color: active ? "var(--color-text-muted)" : "var(--color-text-dim)", fontWeight: 500 }}>
                        ({t.count})
                      </span>
                    )}
                  </button>
                );
              })}
            </div>

            {activeTab === "members" && (
            <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
              <h3 style={{ color: "var(--color-text)", marginTop: 0, fontSize: 15 }}>Coaches ({detail.coaches?.length || 0})</h3>
              {(detail.coaches || []).map(c => (
                <div key={c.coach_sub} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "8px 0", borderBottom: "1px solid var(--color-border)" }}>
                  <div>
                    <span style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 600 }}>
                      {c.display_name || c.initials || "(no name)"}
                    </span>
                    {(() => {
                      // R-J: role pill colored by tier. Owner = purple,
                      // Admin = blue, Coach = slate.
                      const colors = c.role === "owner" ? { bg: "#3b82f622", border: "var(--color-primary)", color: "var(--color-primary)" }
                                  : c.role === "admin"  ? { bg: "#3b82f622", border: "var(--color-primary)", color: "var(--color-primary)" }
                                  :                        { bg: "#33415522", border: "var(--color-border-strong)", color: "var(--color-text-muted)" };
                      return (
                        <span style={{ marginLeft: 8, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: colors.bg, border: `1px solid ${colors.border}`, color: colors.color, fontWeight: 700 }}>{c.role}</span>
                      );
                    })()}
                    <div style={{ fontSize: 10, color: "var(--color-text-dim)", fontFamily: "monospace", marginTop: 2 }}>{c.coach_sub}</div>
                  </div>
                  {isOwner && c.role !== "owner" && (
                    <div style={{ display: "flex", gap: 4 }}>
                      {/* R-J: promote/demote between admin and coach */}
                      {c.role === "coach" && (
                        <button onClick={() => handleRoleChange(c.coach_sub, "admin")}
                          title="Promote to admin (manage team coaches + archives)"
                          style={{ padding: "3px 8px", background: "transparent", border: "1px solid var(--color-primary)", borderRadius: 6, color: "var(--color-primary)", fontSize: 11, cursor: "pointer" }}>
                          ↑ Admin
                        </button>
                      )}
                      {c.role === "admin" && (
                        <button onClick={() => handleRoleChange(c.coach_sub, "coach")}
                          title="Demote to coach (no team-management rights)"
                          style={{ padding: "3px 8px", background: "transparent", border: "1px solid #64748b", borderRadius: 6, color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>
                          ↓ Coach
                        </button>
                      )}
                      <button onClick={() => handleRemoveCoach(c.coach_sub)}
                        style={{ padding: "3px 8px", background: "transparent", border: "1px solid #ef4444", borderRadius: 6, color: "var(--color-destructive)", fontSize: 11, cursor: "pointer" }}>
                        Remove
                      </button>
                    </div>
                  )}
                </div>
              ))}
              {isOwner && !detail.archived && (() => {
                // Filter out coaches already active on this team so the picker
                // only shows valid candidates.
                const activeSubs = new Set((detail.coaches || []).map(c => c.coach_sub));
                const candidates = (coachPool || []).filter(c => !activeSubs.has(c.sub));
                return (
                  <div style={{ marginTop: 12, padding: "10px 0 0", borderTop: "1px solid var(--color-border)" }}>
                    <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginBottom: 6 }}>Add a coach:</div>
                    {coachPool === null ? (
                      <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>Loading coach list…</div>
                    ) : candidates.length === 0 ? (
                      <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>
                        No other coaches available to add. {(coachPool.length === 0) ? "Grant the is_coach flag to a user via Admin → Users first." : "All eligible coaches are already on this team."}
                      </div>
                    ) : (
                      <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                        <select value={addCoachSub} onChange={e => setAddCoachSub(e.target.value)}
                          style={{ flex: 2, minWidth: 180, padding: "6px 10px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, cursor: "pointer" }}>
                          <option value="">— pick a coach —</option>
                          {candidates.map(c => (
                            <option key={c.sub} value={c.sub}>
                              {c.display_name || "(no name)"}
                              {c.initials ? ` (${c.initials})` : ""}
                              {` · …${c.sub.slice(-6)}`}
                            </option>
                          ))}
                        </select>
                        {/* R-J: role on add — coach (default) or admin */}
                        <select value={addCoachRole} onChange={e => setAddCoachRole(e.target.value)}
                          style={{ padding: "6px 10px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, cursor: "pointer" }}>
                          <option value="coach">Coach</option>
                          <option value="admin">Admin</option>
                        </select>
                        <button onClick={handleAddCoach} disabled={!addCoachSub}
                          style={{ padding: "6px 14px", background: addCoachSub ? "var(--color-primary)" : "var(--color-border-strong)", color: addCoachSub ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: addCoachSub ? "pointer" : "not-allowed" }}>
                          Add
                        </button>
                      </div>
                    )}
                    <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 4 }}>
                      Only users with the <code>is_coach</code> flag (or admin) appear. <b>Admin</b> can manage team coaches + archives; <b>Coach</b> can coach groups but not manage the team.
                    </div>
                  </div>
                );
              })()}
            </div>
            )}

            {activeTab === "groups" && (
            <div style={{ background: "var(--color-card)", border: "1px solid var(--color-warn)", borderRadius: 10, padding: 16, marginBottom: 16 }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }}>
                <h3 style={{ color: "var(--color-warn)", marginTop: 0, marginBottom: 0, fontSize: 15 }}>🏊‍♀️ Groups ({(groups || []).length})</h3>
                {(detail.viewer_role === "owner" || detail.viewer_role === "admin") && !detail.archived && (
                  <button onClick={() => setCreatingGroup(c => !c)}
                    style={{ padding: "5px 11px", background: creatingGroup ? "var(--color-border)" : "var(--color-warn)", color: creatingGroup ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    {creatingGroup ? "Cancel" : "+ New group"}
                  </button>
                )}
              </div>
              {creatingGroup && (
                <div style={{ background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 6, padding: 10, marginBottom: 10 }}>
                  <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 3 }}>Group name</label>
                  <input value={newGroupName} onChange={e => setNewGroupName(e.target.value)}
                    placeholder="e.g. Tuesday Sprint"
                    style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, marginBottom: 8 }} />
                  <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 3 }}>Default pool mode (optional)</label>
                  <select value={newGroupPool} onChange={e => setNewGroupPool(e.target.value)}
                    style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, marginBottom: 10 }}>
                    <option value="">— inherit / unspecified —</option>
                    <option value="25y">25 yd (SCY)</option>
                    <option value="25m">25 m (SCM)</option>
                    <option value="50m">50 m (LCM)</option>
                  </select>
                  <button onClick={handleCreateGroup}
                    style={{ padding: "6px 14px", background: "var(--color-warn)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    Create group
                  </button>
                </div>
              )}
              {groups === null ? (
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>Loading…</div>
              ) : groups.length === 0 ? (
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>No groups yet.</div>
              ) : (
                <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                  {groups.map(g => (
                    <GroupRow key={g.id} group={g}
                      coachPool={coachPool || []}
                      managedInTeam={managedInTeam || []}
                      onChanged={() => loadGroups(detail.id)} />
                  ))}
                </div>
              )}
            </div>
            )}

            {activeTab === "roster" && (
              <TeamRosterTab teamId={detail.id} />
            )}

            {activeTab === "events" && (
            <div style={{ background: "var(--color-card)", border: "1px solid var(--color-warn)", borderRadius: 10, padding: 16, marginBottom: 16 }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }}>
                <h3 style={{ color: "var(--color-warn)", marginTop: 0, marginBottom: 0, fontSize: 15 }}>📅 Events ({(events || []).length})</h3>
                {detail.viewer_role && !detail.archived && (
                  <button onClick={() => setCreatingEvent(c => !c)}
                    style={{ padding: "5px 11px", background: creatingEvent ? "var(--color-border)" : "var(--color-warn)", color: creatingEvent ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    {creatingEvent ? "Cancel" : "+ New event"}
                  </button>
                )}
              </div>
              {creatingEvent && (
                <div style={{ background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 6, padding: 10, marginBottom: 10 }}>
                  <div style={{ display: "grid", gridTemplateColumns: "2fr 1fr", gap: 8, marginBottom: 8 }}>
                    <div>
                      <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 3 }}>Event name</label>
                      <input value={newEventName} onChange={e => setNewEventName(e.target.value)}
                        placeholder="e.g. Cincinnati Invite"
                        style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
                    </div>
                    <div>
                      <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 3 }}>Date</label>
                      <input type="date" value={newEventDate} onChange={e => setNewEventDate(e.target.value)}
                        style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
                    </div>
                  </div>
                  <button onClick={handleCreateEvent}
                    style={{ padding: "6px 14px", background: "var(--color-warn)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    Create event
                  </button>
                </div>
              )}
              {events === null ? (
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>Loading…</div>
              ) : events.length === 0 ? (
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>No events yet.</div>
              ) : (
                <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
                  {events.map(ev => {
                    const isPast    = new Date(ev.date) < new Date(new Date().toDateString());
                    const isEditing = editingEventId === ev.id;
                    if (isEditing) {
                      return (
                        <div key={ev.id} style={{ padding: "8px 10px", background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 6 }}>
                          <div style={{ display: "grid", gridTemplateColumns: "2fr 1fr", gap: 8, marginBottom: 8 }}>
                            <input value={editEventName} onChange={e => setEditEventName(e.target.value)} placeholder="Event name"
                              onKeyDown={e => { if (e.key === "Enter") handleSaveEditEvent(); if (e.key === "Escape") cancelEditEvent(); }}
                              autoFocus
                              style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
                            <input type="date" value={editEventDate} onChange={e => setEditEventDate(e.target.value)}
                              style={{ width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }} />
                          </div>
                          <div style={{ display: "flex", gap: 6 }}>
                            <button onClick={handleSaveEditEvent}
                              style={{ padding: "5px 11px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>Save</button>
                            <button onClick={cancelEditEvent}
                              style={{ padding: "5px 11px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 5, fontSize: 12, cursor: "pointer" }}>Cancel</button>
                          </div>
                        </div>
                      );
                    }
                    const isAnchoring = anchoringEventId === ev.id;
                    return (
                      <div key={ev.id}>
                        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "8px 10px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 6, opacity: isPast ? 0.6 : 1 }}>
                          <div>
                            <span style={{ color: "var(--color-text)", fontWeight: 700, fontSize: 13 }}>{ev.name}</span>
                            <span style={{ marginLeft: 10, color: "var(--color-text-muted)", fontSize: 12 }}>{ev.date}</span>
                            {isPast && <span style={{ marginLeft: 8, fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic" }}>(past)</span>}
                          </div>
                          {detail.viewer_role && (
                            <div style={{ display: "flex", gap: 6 }}>
                              {!isPast && (
                                <button onClick={() => openAnchorPicker(ev.id)}
                                  title="Set as training-phase anchor for a group"
                                  style={{ padding: "3px 9px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                                  🎯 Anchor
                                </button>
                              )}
                              <button onClick={() => startEditEvent(ev)}
                                style={{ padding: "3px 9px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-primary)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                                Edit
                              </button>
                              <button onClick={() => handleDeleteEvent(ev.id)}
                                style={{ padding: "3px 9px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                                Delete
                              </button>
                            </div>
                          )}
                        </div>
                        {isAnchoring && (
                          <div style={{ marginTop: 6, padding: "10px 12px", background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 6 }}>
                            <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 8 }}>
                              Set <strong style={{ color: "var(--color-text)" }}>{ev.name}</strong> as anchor for which group? Group's Training Phase will auto-suggest base/build/peak/taper from weeks out. Replacing an existing anchor clears the prior one.
                            </div>
                            {anchorGroupsList === null ? (
                              <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>Loading groups…</div>
                            ) : anchorGroupsList.length === 0 ? (
                              <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>This team has no active groups. Create a group first.</div>
                            ) : (
                              <>
                                <select value={anchorTargetGroupId} onChange={e => setAnchorTargetGroupId(e.target.value)}
                                  style={{ padding: "5px 9px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, marginRight: 8, minWidth: 200 }}>
                                  <option value="">— pick a group —</option>
                                  {anchorGroupsList.map(g => (
                                    <option key={g.id} value={g.id}>{g.name}</option>
                                  ))}
                                </select>
                                <button onClick={handleSetAnchor} disabled={!anchorTargetGroupId || anchorBusy}
                                  style={{ padding: "5px 12px", background: (!anchorTargetGroupId || anchorBusy) ? "var(--color-border)" : "var(--color-warn)", color: (!anchorTargetGroupId || anchorBusy) ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: (!anchorTargetGroupId || anchorBusy) ? "not-allowed" : "pointer", marginRight: 6 }}>
                                  {anchorBusy ? "Setting…" : "Set anchor"}
                                </button>
                                <button onClick={cancelAnchorPicker} disabled={anchorBusy}
                                  style={{ padding: "5px 11px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 5, fontSize: 12, cursor: anchorBusy ? "not-allowed" : "pointer" }}>
                                  Cancel
                                </button>
                              </>
                            )}
                          </div>
                        )}
                      </div>
                    );
                  })}
                </div>
              )}
            </div>
            )}

            {activeTab === "settings" && (
              <TeamSettingsTab
                teamId={detail.id}
                viewerRole={detail.viewer_role}
                archived={!!detail.archived}
              />
            )}

            {msg && <div style={{ color: "#fca5a5", fontSize: 12, padding: "8px 0" }}>{msg}</div>}
          </div>
        );
      }

      // List view
      return (
        <div>
          {incomingBanner}
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
            <h2 style={{ color: "var(--color-text)", margin: 0 }}>👥 Teams</h2>
            <button onClick={() => setCreating(c => !c)}
              style={{ padding: "6px 14px", background: creating ? "var(--color-border)" : "var(--color-positive)", color: creating ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
              {creating ? "Cancel" : "+ New team"}
            </button>
          </div>

          {creating && (
            <div style={{ background: "var(--color-card)", border: "1px solid #22c55e", borderRadius: 10, padding: 14, marginBottom: 16 }}>
              <h3 style={{ color: "var(--color-text)", marginTop: 0, fontSize: 14 }}>Create team</h3>
              <div style={{ marginBottom: 10 }}>
                <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Name</label>
                <input value={createName} onChange={e => setCreateName(e.target.value)} placeholder="e.g. Cincinnati Masters"
                  style={{ width: "100%", padding: "6px 10px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6 }} />
              </div>
              <div style={{ marginBottom: 12 }}>
                <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Type</label>
                <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                  {Object.keys(TEAM_TYPE_LABELS).map(t => (
                    <button key={t} onClick={() => setCreateType(t)}
                      className="pill"
                      style={{ border: `1px solid ${createType === t ? "var(--color-primary)" : "var(--color-border)"}`, background: createType === t ? "#3b82f622" : "transparent", color: createType === t ? "var(--color-primary)" : "var(--color-text-muted)", fontSize: 12, cursor: "pointer" }}>
                      {TEAM_TYPE_LABELS[t]}
                    </button>
                  ))}
                </div>
                <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 6, lineHeight: 1.5 }}>
                  {TEAM_TYPE_DESCRIPTIONS[createType]}
                </div>
              </div>
              <button onClick={handleCreate}
                style={{ padding: "7px 16px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                Create
              </button>
            </div>
          )}

          {msg && <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>{msg}</div>}

          {/* R-J: per-group primary-transfer modal. Shows when removing a
              coach who's still primary on at least one group. Each group
              needs a destination; once all are picked, the modal runs the
              transfers in sequence then retries the remove. */}
          {removeConflict && detail && (() => {
            // Eligible new primaries: active team coaches excluding the
            // coach being removed.
            const eligible = (detail.coaches || []).filter(c =>
              c.coach_sub !== removeConflict.coachSub
            );
            return (
              <div onClick={() => { setRemoveConflict(null); setTransferPlan({}); }}
                className="modal-overlay" style={{ padding: 20 }}>
                <div onClick={(e) => e.stopPropagation()}
                  style={{ background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12, padding: 22, maxWidth: 560, width: "100%", maxHeight: "90vh", overflowY: "auto" }}>
                  <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>Coach removal blocked</div>
                  <div style={{ color: "var(--color-text)", fontSize: 16, fontWeight: 700, marginBottom: 10 }}>Transfer primary first</div>
                  <p style={{ color: "#cbd5e1", fontSize: 12, marginTop: 0, marginBottom: 12, lineHeight: 1.5 }}>
                    This coach is still primary on {removeConflict.groups.length} {removeConflict.groups.length === 1 ? "group" : "groups"}. Pick a new primary for each before removing them from the team.
                  </p>
                  {removeConflict.groups.map(g => (
                    <div key={g.id} className="card" style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8, padding: 8, borderRadius: 6 }}>
                      <span style={{ flex: 1, color: "var(--color-text)", fontSize: 13, fontWeight: 600 }}>{g.name}</span>
                      <select value={transferPlan[g.id] || ""}
                        onChange={(e) => setTransferPlan(prev => ({ ...prev, [g.id]: e.target.value }))}
                        style={{ flex: 1, padding: "5px 8px", fontSize: 12, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, cursor: "pointer" }}>
                        <option value="">— new primary —</option>
                        {eligible.map(c => (
                          <option key={c.coach_sub} value={c.coach_sub}>
                            {c.display_name || c.initials || c.coach_sub.slice(-6)} ({c.role})
                          </option>
                        ))}
                      </select>
                    </div>
                  ))}
                  {eligible.length === 0 && (
                    <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10, padding: 8, background: "rgba(239,68,68,0.12)", border: "1px solid #ef4444", borderRadius: 5 }}>
                      No other team coaches available to take over. Add another coach to this team first, then retry.
                    </div>
                  )}
                  <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
                    <button onClick={() => { setRemoveConflict(null); setTransferPlan({}); }}
                      className="btn btn-lg btn-outlined btn-neutral">
                      Cancel
                    </button>
                    <button onClick={executeRemoveWithTransfers}
                      disabled={eligible.length === 0}
                      style={{ padding: "8px 14px", borderRadius: 6, border: "none", background: eligible.length === 0 ? "var(--color-border)" : "var(--color-destructive)", color: eligible.length === 0 ? "var(--color-text-dim)" : "#fff", fontSize: 13, fontWeight: 700, cursor: eligible.length === 0 ? "not-allowed" : "pointer" }}>
                      Transfer & remove ▸
                    </button>
                  </div>
                </div>
              </div>
            );
          })()}

          {teams === null ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13 }}>Loading…</div>
          ) : teams.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13, fontStyle: "italic", padding: 20, textAlign: "center", border: "1px dashed var(--color-border)", borderRadius: 10 }}>
              No teams yet. Create one to get started, or join an existing team via an invite code (coming in a later session).
            </div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              {teams.map(t => (
                <button key={t.id} onClick={() => setSelectedId(t.id)}
                  style={{ display: "block", textAlign: "left", width: "100%", padding: "12px 14px", background: t.archived ? "#1e293b88" : "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 10, cursor: "pointer", color: "var(--color-text)" }}>
                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8 }}>
                    <span style={{ fontSize: 14, fontWeight: 700 }}>
                      {t.name}
                      {t.archived && <span style={{ marginLeft: 8, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#64748b22", border: "1px solid #64748b", color: "var(--color-text-muted)" }}>archived</span>}
                    </span>
                    <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                      <span style={{ fontSize: 10, padding: "2px 7px", borderRadius: 999, background: "#3b82f622", border: "1px solid var(--color-primary)", color: "var(--color-primary)", fontWeight: 700 }}>{TEAM_TYPE_LABELS[t.team_type] || t.team_type}</span>
                      <span style={{ fontSize: 10, padding: "2px 7px", borderRadius: 999, background: t.role === "owner" ? "#3b82f622" : "#33415522", border: `1px solid ${t.role === "owner" ? "var(--color-primary)" : "var(--color-border-strong)"}`, color: t.role === "owner" ? "var(--color-primary)" : "var(--color-text-muted)", fontWeight: 700 }}>{t.role}</span>
                    </div>
                  </div>
                </button>
              ))}
            </div>
          )}
        </div>
      );
    }

    // AssignedToMeView (Stage 4 / R-G) — swimmer-side list of workouts
    // assigned to them by coaches. Filterable by completion state. Click a
    // row to expand the workout in place; mark complete / partial / missed
    // inline OR run via Run-mode (which then writes back to the assignment).
    const COMPLETION_LABELS = {
      not_started: { label: "Not started", color: "var(--color-text-dim)" },
      partial:     { label: "Partial",     color: "var(--color-warn)" },
      complete:    { label: "Complete",    color: "var(--color-positive)" },
      missed:      { label: "Missed",      color: "var(--color-destructive)" },
    };

    // ClaimTokensPanel (Stage 5 / R-I) — coach-side managed-swimmer claim
    // token issuer. Per scope §10 + decision #16: confirmation dialog before
    // issuing because claim is destructive (managed row dies after redeem).
    // Per #28: blocked for under-13 (COPPA) and for parent_managed_flag.
    function ClaimTokensPanel({ managedId, isMinor, isCoppa, parentManaged, seedTokens = null }) {
      const [tokens,        setTokens]        = React.useState(seedTokens ?? null);
      const [latestIssued,  setLatestIssued]  = React.useState(null);
      const [copied,        setCopied]        = React.useState(false);
      const [err,           setErr]           = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/managed-swimmers/${managedId}/claim-tokens`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setTokens(await res.json());
        } catch (e) { setErr(e.message); setTokens([]); }
      }, [managedId]);
      // Seeded from the composite detail fetch — skip the initial load; still
      // refetch after issue/revoke. Ref guards only the first effect run.
      const skipInitial = React.useRef(seedTokens != null);
      React.useEffect(() => { if (skipInitial.current) { skipInitial.current = false; return; } load(); }, [load]);

      const blocked = isCoppa || parentManaged;
      const blockedReason = isCoppa     ? "Under-13 (COPPA-protected) — claim is blocked."
                         : parentManaged ? "Profile flagged as parent-managed — claim is blocked. Uncheck the flag on the swimmer detail to allow claim."
                         : null;

      const issue = async () => {
        if (!window.confirm(
          "Issue a claim link?\n\n" +
          "Once redeemed by the swimmer:\n" +
          "  • Their full-account profile inherits this managed profile's name / DOB / gender / paces (silent overwrite per scope decision #24)\n" +
          "  • All assignments + coach notes + group memberships are repointed to their account\n" +
          "  • This managed profile is DELETED\n\n" +
          "Code is valid for 30 days. You can revoke before redemption."
        )) return;
        setErr(null);
        try {
          const res = await fetch(`/api/managed-swimmers/${managedId}/claim-tokens`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    "{}",
          });
          const j = await res.json();
          if (!res.ok) {
            if (j.reason === "claim_blocked_under_13")   throw new Error("Claim blocked: swimmer is under 13.");
            if (j.reason === "claim_blocked_parent_managed") throw new Error("Claim blocked: profile is parent-managed. Uncheck the flag to enable.");
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setLatestIssued({ token: j.token, expires_at: j.expires_at });
          setCopied(false);
          await load();
        } catch (e) { setErr(e.message); }
      };

      const revoke = async (token) => {
        if (!window.confirm("Revoke this claim code? It will become unusable.")) return;
        try {
          const res = await fetch(`/api/claim-tokens/${token}`, { method: "DELETE", headers: { ...csrfHeaders() } });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          if (latestIssued?.token === token) setLatestIssued(null);
          await load();
        } catch (e) { setErr(e.message); }
      };

      const copyToClipboard = (token) => {
        try { navigator.clipboard.writeText(token); setCopied(true); setTimeout(() => setCopied(false), 2000); } catch (_) {}
      };

      return (
        <div style={{ marginTop: 14, padding: 14, background: "var(--color-card)", border: "1px solid var(--color-border-strong)", borderRadius: 8 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
            <h4 style={{ color: "var(--color-text)", margin: 0, fontSize: 14 }}>Claim link (managed → full-account)</h4>
            {!latestIssued && !blocked && (
              <button onClick={issue}
                style={{ padding: "5px 11px", background: "var(--color-warn)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                Send claim link
              </button>
            )}
          </div>
          {blocked && (
            <div style={{ padding: 8, background: "#dc262622", border: "1px solid #dc2626", borderRadius: 5, color: "#fca5a5", fontSize: 11, marginBottom: 8 }}>
              {blockedReason}
            </div>
          )}
          {err && <div style={{ color: "#fca5a5", fontSize: 11, marginBottom: 6 }}>{err}</div>}

          {latestIssued && (
            <div style={{ padding: 10, background: "var(--color-bg)", border: "1px solid #22c55e", borderRadius: 5, marginBottom: 6 }}>
              <div style={{ fontSize: 11, color: "var(--color-positive)", marginBottom: 6 }}>
                ✓ Claim code created. Send to the swimmer out-of-band:
              </div>
              <div style={{ display: "flex", gap: 6, alignItems: "center", marginBottom: 6 }}>
                <code style={{ flex: 1, fontFamily: "monospace", fontSize: 16, fontWeight: 700, padding: "6px 10px", background: "var(--color-card)", color: "var(--color-warn)", border: "1px solid var(--color-border-strong)", borderRadius: 4, letterSpacing: "0.1em", textAlign: "center" }}>
                  {latestIssued.token}
                </code>
                <button onClick={() => copyToClipboard(latestIssued.token)}
                  style={{ padding: "5px 11px", background: copied ? "var(--color-positive)" : "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 4, fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                  {copied ? "Copied!" : "Copy"}
                </button>
              </div>
              <div style={{ fontSize: 10, color: "var(--color-text-dim)" }}>
                Expires {latestIssued.expires_at ? new Date(latestIssued.expires_at).toLocaleDateString() : "in 30 days"}
              </div>
              <button onClick={() => setLatestIssued(null)}
                style={{ marginTop: 6, padding: "3px 8px", background: "transparent", color: "var(--color-text-muted)", border: "none", fontSize: 10, cursor: "pointer", textDecoration: "underline" }}>
                Dismiss
              </button>
            </div>
          )}

          {tokens && tokens.length > 0 && (
            <div>
              <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginBottom: 4 }}>Outstanding codes:</div>
              {tokens.map(t => (
                <div key={t.token} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "4px 8px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 4, marginBottom: 3, fontSize: 11 }}>
                  <span>
                    <code style={{ fontFamily: "monospace", color: "var(--color-warn)", fontSize: 11 }}>{t.token}</code>
                    <span style={{ marginLeft: 8, color: "var(--color-text-dim)", fontSize: 10 }}>expires {new Date(t.expires_at).toLocaleDateString()}</span>
                  </span>
                  <button onClick={() => revoke(t.token)}
                    style={{ padding: "2px 7px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>Revoke</button>
                </div>
              ))}
            </div>
          )}
        </div>
      );
    }

    // ClaimManagedSection (Stage 5 / R-I) — swimmer-side claim redemption in
    // Profile modal. Paste code → preview ("you're about to claim Sarah's
    // managed profile from Coach Foo") → confirm → redeem. Post-claim diff
    // screen per decision #30: "we updated these fields on your account."
    function ClaimManagedSection({ me, setMe, onClaimed }) {
      const [code,     setCode]    = React.useState("");
      const [preview,  setPreview] = React.useState(null);
      const [result,   setResult]  = React.useState(null);
      const [err,      setErr]     = React.useState(null);
      const [busy,     setBusy]    = React.useState(false);
      const normalized = code.trim().toLowerCase();

      const doPreview = async () => {
        if (!normalized) { setErr("Enter a code first"); return; }
        setErr(null); setResult(null);
        try {
          const res = await fetch(`/api/claim-tokens/${encodeURIComponent(normalized)}/preview`, { cache: "no-store" });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          const p = await res.json();
          if (p.is_redeemed) throw new Error("That claim code has already been used.");
          if (p.is_expired)  throw new Error("That claim code has expired.");
          setPreview(p);
        } catch (e) { setErr(e.message); setPreview(null); }
      };

      const doRedeem = async () => {
        setBusy(true); setErr(null);
        try {
          const res = await fetch(`/api/claim-tokens/${encodeURIComponent(normalized)}/redeem`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    "{}",
          });
          const j = await res.json();
          if (!res.ok) {
            if (j.reason === "group_conflict") {
              throw new Error(`Group conflict: the managed profile is primary in "${j.conflict_managed_group_name}" but you're already primary in "${j.conflict_swimmer_group_name}" with the same coach. Resolve manually (leave one) then retry.`);
            }
            if (j.reason === "already_redeemed") throw new Error("That code has already been used.");
            if (j.reason === "expired")          throw new Error("That code has expired.");
            if (j.reason === "invalid_token")    throw new Error("That code isn't valid.");
            throw new Error(j.error || j.reason || `HTTP ${res.status}`);
          }
          // Update me with the new identity fields the migration applied.
          // Recompute is_minor properly (month/day-aware) matching the DOB-save
          // path. Year-only subtraction misclassifies pre-birthday minors.
          if (j.identity_diff && j.identity_diff.length > 0) {
            setMe(prev => {
              if (!prev) return prev;
              const next = { ...prev };
              for (const d of j.identity_diff) next[d.field] = d.to;
              if ("dob" in next && next.dob) {
                const today = new Date();
                const dob   = new Date(next.dob);
                let age = today.getFullYear() - dob.getFullYear();
                const monthDiff = today.getMonth() - dob.getMonth();
                if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < dob.getDate())) age--;
                next.is_minor = age < 18;
              }
              return next;
            });
          }
          setResult(j);
          setCode("");
          setPreview(null);
          if (onClaimed) onClaimed(j);
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      return (
        <div style={{ marginTop: 14, padding: 14, background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 8 }}>
          <h4 style={{ color: "var(--color-text)", margin: 0, marginBottom: 6, fontSize: 14 }}>Claim a managed profile</h4>
          <p style={{ color: "var(--color-text-muted)", fontSize: 12, margin: "0 0 10px", lineHeight: 1.5 }}>
            Got a claim code from a coach? Paste it here to inherit a managed profile they were maintaining for you — your account picks up the name/DOB/gender/paces they had on file, plus all assignments, notes, and group memberships.
          </p>

          {result ? (
            <div style={{ padding: 12, background: "#22c55e22", border: "1px solid #22c55e", borderRadius: 6 }}>
              <div style={{ color: "var(--color-positive)", fontWeight: 700, fontSize: 14, marginBottom: 8 }}>✓ Claim complete</div>
              <div style={{ color: "#cbd5e1", fontSize: 12, marginBottom: 10 }}>
                Brought over: <strong>{result.repointed?.workout_assignments || 0}</strong> assignments
                {(result.repointed?.coach_swimmer_notes || 0) > 0 && <>, <strong>{result.repointed.coach_swimmer_notes}</strong> coach notes</>}
                {(result.repointed?.group_memberships || 0) > 0 && <>, <strong>{result.repointed.group_memberships}</strong> group membership{result.repointed.group_memberships === 1 ? "" : "s"}</>}.
              </div>
              {result.identity_diff && result.identity_diff.length > 0 ? (
                <div style={{ padding: 10, background: "var(--color-card)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                  <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em", marginBottom: 6 }}>
                    Updated on your account:
                  </div>
                  <table style={{ width: "100%", fontSize: 12, borderCollapse: "collapse" }}>
                    <thead><tr style={{ color: "var(--color-text-dim)", fontSize: 10 }}>
                      <th style={{ textAlign: "left", padding: 2 }}>Field</th>
                      <th style={{ textAlign: "left", padding: 2 }}>Was</th>
                      <th style={{ textAlign: "left", padding: 2 }}>Now</th>
                    </tr></thead>
                    <tbody>
                      {result.identity_diff.map(d => (
                        <tr key={d.field} style={{ borderTop: "1px solid var(--color-border)" }}>
                          <td style={{ padding: 4, color: "#cbd5e1", fontFamily: "monospace" }}>{d.field}</td>
                          <td style={{ padding: 4, color: "var(--color-text-dim)" }}>{d.from || "(empty)"}</td>
                          <td style={{ padding: 4, color: "var(--color-positive)", fontWeight: 700 }}>{d.to}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              ) : (
                <div style={{ fontSize: 11, color: "var(--color-text-muted)" }}>No identity fields changed (your account already had matching values).</div>
              )}
              <button onClick={() => setResult(null)}
                style={{ marginTop: 8, padding: "5px 12px", background: "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                Done
              </button>
            </div>
          ) : (
            <>
              <div style={{ display: "flex", gap: 6, marginBottom: 6 }}>
                <input value={code} onChange={e => { setCode(e.target.value); setPreview(null); setErr(null); }}
                  placeholder="Paste 10-character claim code"
                  onKeyDown={e => { if (e.key === "Enter") { preview ? doRedeem() : doPreview(); } }}
                  style={{ flex: 1, padding: "6px 10px", fontSize: 13, fontFamily: "monospace", letterSpacing: "0.08em", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5, textTransform: "lowercase" }} />
                {!preview && (
                  <button onClick={doPreview} disabled={!normalized || busy}
                    style={{ padding: "6px 14px", background: normalized ? "var(--color-warn)" : "var(--color-border-strong)", color: normalized ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: normalized ? "pointer" : "not-allowed" }}>
                    Look up
                  </button>
                )}
              </div>
              {preview && (
                <div style={{ padding: 10, background: "var(--color-card)", border: "1px solid var(--color-warn)", borderRadius: 6, marginBottom: 6 }}>
                  <div style={{ fontSize: 12, color: "#cbd5e1", marginBottom: 6 }}>
                    Claiming <strong style={{ color: "var(--color-text)" }}>{preview.managed_name}</strong> from coach <strong style={{ color: "var(--color-warn)" }}>{preview.coach_name || "(coach)"}</strong>.
                    <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginTop: 4, lineHeight: 1.5 }}>
                      Your account's name / DOB / gender / paces will be silently updated from the managed profile (per scope #24). All assignments + coach notes + group memberships will be repointed to your account. The managed profile will be deleted.
                    </div>
                  </div>
                  <div style={{ display: "flex", gap: 6 }}>
                    <button onClick={doRedeem} disabled={busy}
                      style={{ padding: "6px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                      {busy ? "Claiming…" : "Confirm claim"}
                    </button>
                    <button onClick={() => { setPreview(null); setCode(""); }}
                      style={{ padding: "6px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 5, fontSize: 12, cursor: "pointer" }}>
                      Cancel
                    </button>
                  </div>
                </div>
              )}
              {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 6 }}>{err}</div>}
            </>
          )}
        </div>
      );
    }

    // CoachNotesPanel (Stage 5 / R-H) — coach-side private notes journal
    // attached to a managed swimmer (or full-account swimmer after claim).
    // Visibility per-note: private / group_coaches / team_coaches. Default
    // visibility derived from the swimmer's team_type (high_school →
    // team_coaches; everyone else → private).
    function CoachNotesPanel({ targetManagedId, targetSwimmerSub, teamId, mySub, seedNotes = null }) {
      const [notes,       setNotes]        = React.useState(seedNotes ?? null);
      const [team,        setTeam]         = React.useState(null);   // for team_type lookup
      const [draftBody,   setDraftBody]    = React.useState("");
      const [draftVis,    setDraftVis]     = React.useState("private");
      const [busy,        setBusy]         = React.useState(false);
      const [err,         setErr]          = React.useState(null);
      const [editingId,   setEditingId]    = React.useState(null);
      const [editingBody, setEditingBody]  = React.useState("");
      const [editingVis,  setEditingVis]   = React.useState("private");

      const targetQuery = targetManagedId
        ? `managed_id=${encodeURIComponent(targetManagedId)}`
        : `swimmer_sub=${encodeURIComponent(targetSwimmerSub)}`;

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/coach-notes?${targetQuery}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setNotes(await res.json());
        } catch (e) { setNotes([]); setErr(e.message); }
      }, [targetQuery]);
      // Seeded from the composite detail fetch — skip the initial load; still
      // refetch after add/edit/delete. Ref guards only the first effect run.
      const skipInitial = React.useRef(seedNotes != null);
      React.useEffect(() => { if (skipInitial.current) { skipInitial.current = false; return; } load(); }, [load]);

      // Fetch the team (once) so we can apply the per-team-type default
      // visibility on the create form.
      React.useEffect(() => {
        if (!teamId) { setTeam(null); return; }
        fetch(`/api/teams/${teamId}`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(t => {
            setTeam(t);
            const def = (t && t.team_type === "high_school") ? "team_coaches" : "private";
            setDraftVis(def);
          })
          .catch(() => {});
      }, [teamId]);

      // Compute which visibility values are actually selectable. team_coaches
      // requires the swimmer to have a team; group_coaches requires a primary
      // group with this coach. We can't pre-check the group here (server
      // resolves at create-time), but the team check is cheap.
      const visOptions = [
        { id: "private",       label: "Private",        desc: "Only you can see this" },
        { id: "group_coaches", label: "Group coaches",  desc: "Other coaches on this swimmer's group" },
        { id: "team_coaches",  label: "Team coaches",   desc: "All coaches on this swimmer's team", disabled: !teamId },
      ];

      const createNote = async () => {
        if (!draftBody.trim()) { setErr("Note can't be empty"); return; }
        setBusy(true); setErr(null);
        try {
          const body = {
            visibility: draftVis,
            body:       draftBody.trim(),
          };
          if (targetManagedId)  body.managed_id  = targetManagedId;
          if (targetSwimmerSub) body.swimmer_sub = targetSwimmerSub;
          const res = await fetch("/api/coach-notes", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify(body),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) {
            if (j.error === "group_coaches_requires_group") throw new Error("This swimmer isn't in a group you coach — can't share with group coaches.");
            if (j.error === "team_coaches_requires_team")   throw new Error("This swimmer isn't on a team — can't share with team coaches.");
            if (j.error === "body_too_long")                throw new Error("Note too long (max 5000 chars).");
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setDraftBody("");
          // Keep draftVis as the user's most-recent preference for the next note.
          await load();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const startEdit = (n) => {
        setEditingId(n.id);
        setEditingBody(n.body);
        setEditingVis(n.visibility);
      };
      const cancelEdit = () => { setEditingId(null); setEditingBody(""); };

      const saveEdit = async () => {
        if (!editingBody.trim()) { setErr("Note can't be empty"); return; }
        setBusy(true); setErr(null);
        try {
          const res = await fetch(`/api/coach-notes/${editingId}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ body: editingBody.trim(), visibility: editingVis }),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          cancelEdit();
          await load();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const deleteNote = async (id) => {
        if (!window.confirm("Delete this note? You can't recover it.")) return;
        setBusy(true); setErr(null);
        try {
          const res = await fetch(`/api/coach-notes/${id}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const visPillStyle = (vis) => {
        const map = {
          private:       { bg: "rgba(100,116,139,0.18)", border: "var(--color-text-dim)", color: "var(--color-text-muted)" },
          group_coaches: { bg: "rgba(245,158,11,0.18)",  border: "var(--color-warn)", color: "var(--color-warn)" },
          team_coaches:  { bg: "rgba(59,130,246,0.18)",  border: "var(--color-primary)", color: "var(--color-primary)" },
        };
        const s = map[vis] || map.private;
        return { padding: "2px 8px", borderRadius: 999, border: `1px solid ${s.border}`, background: s.bg, color: s.color, fontSize: 10, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.04em" };
      };
      const visLabel = { private: "Private", group_coaches: "Group", team_coaches: "Team" };

      return (
        <div style={{ marginTop: 14, padding: 14, background: "var(--color-card)", border: "1px solid var(--color-border-strong)", borderRadius: 8 }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
            <h4 style={{ color: "var(--color-text)", margin: 0, fontSize: 14 }}>Coach notes ({notes ? notes.length : "…"})</h4>
            {team && team.team_type === "high_school" && (
              <span style={{ fontSize: 10, color: "var(--color-primary)", fontStyle: "italic" }}>High-school team — notes default to team-shared</span>
            )}
          </div>

          {/* Create form */}
          <div style={{ marginBottom: 10, padding: 8, background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 6 }}>
            <textarea value={draftBody} onChange={(e) => setDraftBody(e.target.value)}
              placeholder="Add a note about this swimmer…"
              rows={2} maxLength={5000}
              style={{ width: "100%", padding: "6px 8px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 12, resize: "vertical", fontFamily: "inherit", boxSizing: "border-box" }} />
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 6, gap: 6, flexWrap: "wrap" }}>
              <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
                {visOptions.map(o => {
                  const active = draftVis === o.id;
                  return (
                    <button key={o.id} onClick={() => !o.disabled && setDraftVis(o.id)}
                      disabled={o.disabled}
                      title={o.disabled ? "Unavailable — swimmer has no team" : o.desc}
                      style={{
                        padding: "3px 9px", borderRadius: 999,
                        border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                        background: active ? "rgba(59,130,246,0.18)" : "transparent",
                        color: o.disabled ? "var(--color-border-strong)" : (active ? "var(--color-primary)" : "var(--color-text-muted)"),
                        fontSize: 11, fontWeight: 700,
                        cursor: o.disabled ? "not-allowed" : "pointer",
                      }}>{o.label}</button>
                  );
                })}
              </div>
              <button onClick={createNote} disabled={busy || !draftBody.trim()}
                className="btn btn-sm btn-filled btn-primary">
                {busy ? "…" : "Add note"}
              </button>
            </div>
            {err && <div style={{ color: "#fca5a5", fontSize: 11, marginTop: 5 }}>{err}</div>}
          </div>

          {/* List */}
          {notes === null && <div style={{ color: "var(--color-text-muted)", fontSize: 11, fontStyle: "italic" }}>Loading notes…</div>}
          {notes !== null && notes.length === 0 && <div style={{ color: "var(--color-text-muted)", fontSize: 11, fontStyle: "italic" }}>No notes yet.</div>}
          {notes && notes.length > 0 && notes.map(n => {
            const mine = n.author_coach_sub === mySub;
            const editing = editingId === n.id;
            return (
              <div key={n.id} style={{ padding: 10, background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 6, marginBottom: 6 }}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 5, gap: 6, flexWrap: "wrap" }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 11, color: "var(--color-text-muted)" }}>
                    <span style={{ color: mine ? "var(--color-warn)" : "#cbd5e1", fontWeight: 700 }}>{n.author_name || (mine ? "You" : "(coach)")}{mine ? " (you)" : ""}</span>
                    <span>·</span>
                    <span>{n.created_at ? new Date(n.created_at).toLocaleString() : ""}</span>
                    {n.updated_at && n.updated_at !== n.created_at && <span style={{ fontStyle: "italic" }}>(edited)</span>}
                  </div>
                  <span style={visPillStyle(n.visibility)}>{visLabel[n.visibility] || n.visibility}</span>
                </div>
                {editing ? (
                  <>
                    <textarea value={editingBody} onChange={(e) => setEditingBody(e.target.value)}
                      rows={3} maxLength={5000}
                      style={{ width: "100%", padding: "6px 8px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 12, resize: "vertical", fontFamily: "inherit", boxSizing: "border-box" }} />
                    <div style={{ display: "flex", gap: 4, marginTop: 6, flexWrap: "wrap" }}>
                      {visOptions.map(o => {
                        const active = editingVis === o.id;
                        return (
                          <button key={o.id} onClick={() => !o.disabled && setEditingVis(o.id)}
                            disabled={o.disabled}
                            style={{
                              padding: "3px 9px", borderRadius: 999,
                              border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                              background: active ? "rgba(59,130,246,0.18)" : "transparent",
                              color: o.disabled ? "var(--color-border-strong)" : (active ? "var(--color-primary)" : "var(--color-text-muted)"),
                              fontSize: 11, fontWeight: 700,
                              cursor: o.disabled ? "not-allowed" : "pointer",
                            }}>{o.label}</button>
                        );
                      })}
                    </div>
                    <div style={{ marginTop: 6, display: "flex", gap: 5 }}>
                      <button onClick={saveEdit} disabled={busy}
                        style={{ padding: "4px 11px", borderRadius: 5, border: "none", background: "var(--color-positive)", color: "var(--color-bg)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>Save</button>
                      <button onClick={cancelEdit}
                        style={{ padding: "4px 11px", borderRadius: 5, border: "1px solid var(--color-border-strong)", background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>Cancel</button>
                    </div>
                  </>
                ) : (
                  <>
                    <div style={{ color: "var(--color-text)", fontSize: 12, whiteSpace: "pre-wrap", lineHeight: 1.45 }}>{n.body}</div>
                    {mine && (
                      <div style={{ marginTop: 5, display: "flex", gap: 5 }}>
                        <button onClick={() => startEdit(n)}
                          style={{ padding: "2px 8px", borderRadius: 5, border: "1px solid var(--color-border-strong)", background: "transparent", color: "var(--color-text-muted)", fontSize: 10, cursor: "pointer" }}>Edit</button>
                        <button onClick={() => deleteNote(n.id)}
                          style={{ padding: "2px 8px", borderRadius: 5, border: "1px solid #ef4444", background: "transparent", color: "var(--color-destructive)", fontSize: 10, cursor: "pointer" }}>Delete</button>
                      </div>
                    )}
                  </>
                )}
              </div>
            );
          })}
        </div>
      );
    }

    // IntentForm (I Phase 2a) — capture generator params for a scheduled
    // intent-mode row. Opens with optional pre-fill from an existing intent
    // row (edit mode). On submit POSTs to /api/scheduled-workouts with
    // intent_params, or PATCHes if editingId is set.
    const MIX_OPTIONS_INTENT = [
      { id: "balanced",     label: "Balanced" },
      { id: "warmup_heavy", label: "Warmup-heavy" },
      { id: "drill_heavy",  label: "Drill-heavy" },
      { id: "long_main",    label: "Long main" },
    ];

    function IntentForm({ initialDate, initialIntent, editingId, initialFacilityId = null, onClose, onSaved }) {
      const safeInitial = initialIntent || {};
      const [date,       setDate]       = React.useState(initialDate || "");
      const [type,       setType]       = React.useState(safeInitial.type || (WORKOUT_TYPES[0] && WORKOUT_TYPES[0].id) || "");
      const [maxYards,   setMaxYards]   = React.useState(safeInitial.maxYards || 2400);
      const [mix,        setMix]        = React.useState(safeInitial.mix || "balanced");
      const [recovery,   setRecovery]   = React.useState(!!safeInitial.recovery);
      // Section model A2 — include/skip per intent. main always on.
      const _initInc = Array.isArray(safeInitial.includedSections) ? safeInitial.includedSections : ["warmup","drill","main","cooldown"];
      const [skipWarmup,   setSkipWarmup]   = React.useState(!_initInc.includes("warmup"));
      const [skipDrill,    setSkipDrill]    = React.useState(!_initInc.includes("drill"));
      const [skipCooldown, setSkipCooldown] = React.useState(!_initInc.includes("cooldown"));
      // Kick is the opt-IN 5th section (default off) — seed from the intent's
      // saved includedSections so re-editing an intent preserves it.
      const [addKick,      setAddKick]      = React.useState(_initInc.includes("kick"));
      // Section model — optional dryland block to attach to this intent. "" = none.
      const [drylandPreset, setDrylandPreset] = React.useState(safeInitial.dryland?.preset || "");
      const [drylandPlace,  setDrylandPlace]  = React.useState(safeInitial.dryland?.placement || "pre");
      const [notes,      setNotes]      = React.useState(safeInitial.notes || "");
      // I Phase 2b — coach group fanout. groupId="" means "Yourself" (self-only,
      // no fanout). Selecting a group reveals an optional lane-plan picker.
      const [groupId,    setGroupId]    = React.useState(safeInitial.group_id || "");
      const [lanePlanId, setLanePlanId] = React.useState(safeInitial.lane_plan_id || "");
      const [coachTargets, setCoachTargets] = React.useState([]); // [] = none / not-coach
      const [lanePlans, setLanePlans]   = React.useState([]);     // [] when no group or no plans
      // P4 — facility lives on scheduled_workouts (NOT intent_params). It's a
      // team-level resource, so the picker only appears once a group (→team)
      // is chosen. Selecting a facility defaults pool_mode to its course at
      // generation time. "" = none / inherit.
      const [facilityId, setFacilityId] = React.useState(initialFacilityId != null ? String(initialFacilityId) : "");
      const [facilities, setFacilities] = React.useState([]);     // [] when no group / team has none
      const [busy,       setBusy]       = React.useState(false);
      const [err,        setErr]        = React.useState(null);

      // Fetch coach targets once. 403 means user isn't a coach — leave list empty.
      React.useEffect(() => {
        fetch("/api/picker/coach-targets", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(list => setCoachTargets(Array.isArray(list) ? list : []))
          .catch(() => setCoachTargets([]));
      }, []);

      // Fetch lane plans whenever the selected group changes.
      React.useEffect(() => {
        if (!groupId) { setLanePlans([]); return; }
        fetch(`/api/groups/${groupId}/lane-plans`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(list => setLanePlans(Array.isArray(list) ? list.filter(p => !p.archived) : []))
          .catch(() => setLanePlans([]));
      }, [groupId]);

      // Clear lane_plan_id if the user switches groups (stale plan id).
      React.useEffect(() => {
        if (!groupId) setLanePlanId("");
        else if (lanePlanId && !lanePlans.some(p => p.id === lanePlanId)) setLanePlanId("");
      }, [groupId, lanePlans]);

      // P4 — load the selected group's team facilities. Facilities are
      // team-level; the group carries team_id via coach-targets.
      React.useEffect(() => {
        if (!groupId) { setFacilities([]); return; }
        const g = coachTargets.find(t => t.id === groupId);
        if (!g || !g.team_id) { setFacilities([]); return; }
        fetch(`/api/teams/${g.team_id}/facilities`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(list => setFacilities(Array.isArray(list) ? list : []))
          .catch(() => setFacilities([]));
      }, [groupId, coachTargets]);

      // Clear a stale facility when switching groups (different team). Guard on
      // facilities.length > 0 so an edit-mode prefill isn't wiped during the
      // async window where the list hasn't loaded yet.
      React.useEffect(() => {
        if (!groupId) { setFacilityId(""); return; }
        if (facilityId && facilities.length > 0 && !facilities.some(f => String(f.id) === String(facilityId))) setFacilityId("");
      }, [groupId, facilities]);

      const submit = async () => {
        setErr(null);
        if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) { setErr("Pick a valid date"); return; }
        if (!type) { setErr("Pick a workout type"); return; }
        const intent_params = {
          type,
          maxYards: Number(maxYards),
          mix,
          recovery,
          // Equipment + phase come from the user's current state at
          // generation time — intent only captures the high-level shape.
        };
        // I Phase 2b — group fanout context. Only include when set so old
        // self-only flows produce identical JSON.
        if (groupId) {
          intent_params.group_id = groupId;
          if (lanePlanId) intent_params.lane_plan_id = lanePlanId;
        }
        // Section model A2 — only attach when something is skipped, so old
        // all-4 intents produce identical JSON.
        if (skipWarmup || skipDrill || addKick || skipCooldown) {
          const inc = [];
          if (!skipWarmup) inc.push("warmup");
          if (!skipDrill)  inc.push("drill");
          if (addKick)     inc.push("kick");
          inc.push("main");
          if (!skipCooldown) inc.push("cooldown");
          intent_params.includedSections = inc;
        }
        // Section model — attach a dryland block to the intent (resolved from
        // the preset bank at generation time). Only when one is chosen.
        if (drylandPreset) {
          intent_params.dryland = { preset: drylandPreset, placement: drylandPlace };
        }
        setBusy(true);
        try {
          // P4 — facility_id is a top-level column (not in intent_params).
          // null when self-only or no facility chosen.
          const facility_id = (groupId && facilityId) ? Number(facilityId) : null;
          if (editingId) {
            const res = await fetch(`/api/scheduled-workouts/${editingId}`, {
              method:  "PATCH",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({ scheduled_date: date, intent_params, notes: notes.trim() || null, facility_id }),
            });
            const j = await res.json();
            if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          } else {
            const res = await fetch("/api/scheduled-workouts", {
              method:  "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({ scheduled_date: date, intent_params, notes: notes.trim() || null, facility_id }),
            });
            const j = await res.json();
            if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          }
          if (onSaved) onSaved();
          if (onClose) onClose();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      return (
        <div onClick={onClose} className="modal-overlay" style={{ padding: 20 }}>
          <div onClick={(e) => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            padding: 22, maxWidth: 520, width: "100%", maxHeight: "90vh", overflowY: "auto",
          }}>
            <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>
              {editingId ? "Edit intent" : "Plan intent"}
            </div>
            <div style={{ color: "var(--color-text)", fontSize: 18, fontWeight: 700, marginBottom: 14 }}>
              Schedule a workout intent for later
            </div>

            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Date</label>
            <input type="date" value={date} onChange={(e) => setDate(e.target.value)}
              style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, marginBottom: 12 }} />

            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Workout type</label>
            <select value={type} onChange={(e) => setType(e.target.value)}
              style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, marginBottom: 12 }}>
              {WORKOUT_TYPES.map(t => <option key={t.id} value={t.id}>{t.emoji} {t.label}</option>)}
            </select>

            {/* I Phase 2b — For whom (self vs coach group). Hidden when user
                isn't a coach on any group. */}
            {coachTargets.length > 0 && (
              <>
                <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>For whom</label>
                <select value={groupId} onChange={(e) => setGroupId(e.target.value)}
                  style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, marginBottom: lanePlans.length > 0 ? 8 : 12 }}>
                  <option value="">👤 Yourself only</option>
                  {coachTargets.map(g => (
                    <option key={g.id} value={g.id}>
                      👥 {g.name} ({g.member_count} swimmer{g.member_count === 1 ? "" : "s"})
                    </option>
                  ))}
                </select>
                {groupId && lanePlans.length > 0 && (
                  <>
                    <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Lane plan (optional)</label>
                    <select value={lanePlanId} onChange={(e) => setLanePlanId(e.target.value)}
                      style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, marginBottom: 12 }}>
                      <option value="">No plan — all members at their stored pace</option>
                      {lanePlans.map(p => (
                        <option key={p.id} value={p.id}>{p.name}</option>
                      ))}
                    </select>
                  </>
                )}
                {groupId && lanePlans.length === 0 && (
                  <div style={{ color: "var(--color-text-dim)", fontSize: 11, fontStyle: "italic", marginBottom: 12 }}>
                    No lane plans for this group — fanout uses each member's stored pace.
                  </div>
                )}
                {groupId && facilities.length > 0 && (
                  <>
                    <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Facility (optional)</label>
                    <select value={facilityId} onChange={(e) => setFacilityId(e.target.value)}
                      style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13, marginBottom: facilityId ? 4 : 12 }}>
                      <option value="">No facility</option>
                      {facilities.map(f => (
                        <option key={f.id} value={f.id}>
                          🏊 {f.name}{f.course ? ` (${poolModeLabel(f.course)})` : ""}{f.is_primary ? " · primary" : ""}
                        </option>
                      ))}
                    </select>
                    {facilityId && (() => {
                      const f = facilities.find(x => String(x.id) === String(facilityId));
                      return f && f.course
                        ? <div style={{ color: "var(--color-text-dim)", fontSize: 11, fontStyle: "italic", marginBottom: 12 }}>
                            Generates in {poolModeLabel(f.course)} by default (this facility's course).
                          </div>
                        : <div style={{ marginBottom: 12 }} />;
                    })()}
                  </>
                )}
              </>
            )}

            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Max yardage</label>
            <div style={{ display: "flex", gap: 6, alignItems: "center", marginBottom: 12 }}>
              <input type="range" min="1200" max="5000" step="100" value={maxYards}
                onChange={(e) => setMaxYards(Number(e.target.value))}
                style={{ flex: 1 }} />
              <span style={{ minWidth: 60, fontFamily: "ui-monospace, monospace", color: "var(--color-text)", fontSize: 13, textAlign: "right" }}>{maxYards}</span>
            </div>

            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Mix</label>
            <div style={{ display: "flex", gap: 4, flexWrap: "wrap", marginBottom: 12 }}>
              {MIX_OPTIONS_INTENT.map(m => {
                const active = mix === m.id;
                return (
                  <button key={m.id} onClick={() => setMix(m.id)}
                    className="pill"
                    style={{ border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`, background: active ? "rgba(245,158,11,0.18)" : "transparent", color: active ? "var(--color-warn)" : "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>
                    {m.label}
                  </button>
                );
              })}
            </div>

            <div style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 12 }}>
              <input type="checkbox" id="intent-recovery" checked={recovery} onChange={(e) => setRecovery(e.target.checked)} />
              <label htmlFor="intent-recovery" style={{ color: "#cbd5e1", fontSize: 13, cursor: "pointer" }}>
                Recovery day (easy mains, intervals +10%)
              </label>
            </div>

            {/* Section model A2 — include/skip sections for this intent. Main always on. */}
            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Sections</label>
            <div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginBottom: 12 }}>
              {[
                { label: "Warmup",   skip: skipWarmup,   toggle: () => setSkipWarmup(v => !v) },
                { label: "Drill",    skip: skipDrill,    toggle: () => setSkipDrill(v => !v) },
                { label: "Kick",     skip: !addKick,     toggle: () => setAddKick(v => !v) },
                { label: "Main",     locked: true },
                { label: "Cooldown", skip: skipCooldown, toggle: () => setSkipCooldown(v => !v) },
              ].map(s => {
                const included = s.locked || !s.skip;
                return (
                  <button key={s.label} type="button" onClick={s.locked ? undefined : s.toggle}
                    className="pill"
                    style={{
                      border: `1px solid ${included ? "var(--color-primary)" : "var(--color-border)"}`,
                      background: included ? "rgba(59,130,246,0.12)" : "transparent",
                      color: included ? "var(--color-primary)" : "var(--color-text-dim)",
                      textDecoration: included ? "none" : "line-through",
                      fontSize: 11, cursor: s.locked ? "default" : "pointer", opacity: s.locked ? 0.85 : 1,
                    }}>
                    {included ? "✓ " : ""}{s.label}
                  </button>
                );
              })}
            </div>

            {/* Section model — optional dryland block on this intent. */}
            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Dryland (optional)</label>
            <div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 12 }}>
              <select value={drylandPreset} onChange={(e) => setDrylandPreset(e.target.value)}
                style={{ flex: 1, minWidth: 160, padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13 }}>
                <option value="">No dryland</option>
                {DRYLAND_OPTIONS.map(o => <option key={o.id} value={o.id}>🏋 {o.name}</option>)}
              </select>
              {drylandPreset && (
                <select value={drylandPlace} onChange={(e) => setDrylandPlace(e.target.value)}
                  style={{ padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 13 }}>
                  <option value="pre">Before pool</option>
                  <option value="post">After pool</option>
                </select>
              )}
            </div>

            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Notes (optional)</label>
            <textarea value={notes} onChange={(e) => setNotes(e.target.value)} rows={2} maxLength={500}
              placeholder="e.g. focus on stroke count, or post-race recovery"
              style={{ width: "100%", padding: "7px 9px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 12, resize: "vertical", boxSizing: "border-box", fontFamily: "inherit", marginBottom: 4 }} />

            {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 6 }}>{err}</div>}

            <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
              <button onClick={onClose}
                className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
              <button onClick={submit} disabled={busy}
                className="btn btn-lg btn-filled btn-warn">
                {busy ? "…" : (editingId ? "Save intent" : "Plan intent ▸")}
              </button>
            </div>
          </div>
        </div>
      );
    }

    // IntentPreviewOverlay (I Phase 2a) — fullscreen preview when the user
    // clicks ▶ Generate on an intent row. Shows the freshly-generated
    // workout + Run / Regenerate / Cancel actions. On Run: PATCHes the
    // schedule row's payload (intent → payload conversion) and hands off
    // to the parent to load into Run mode.
    function IntentPreviewOverlay({ scheduledRow, generatedWorkout, onRegenerate, onRun, onSave, onClose }) {
      const [busy, setBusy] = React.useState(false);
      const [err,  setErr]  = React.useState(null);
      const w = generatedWorkout;

      // Commit the generated workout to the schedule row. Returns true if
      // committed successfully. Shared between Save (no Run mode handoff)
      // and Run (commit + enter Run mode).
      const commit = async () => {
        setBusy(true); setErr(null);
        try {
          // Enrich payload with form context so ✎ Edit on the row later can
          // restore generator state (intent_params + defaults fill in fields
          // not carried in the raw workout output).
          const ip = scheduledRow.intent_params || {};
          const enrichedPayload = {
            ...w,
            maxYardsCap: w.maxYardsCap || Number(ip.maxYards) || 2400,
            poolMode:    w.poolMode    || "25y",
            equipment:   w.equipment   || ip.equipment || { kickboard: "off", fins: "off", paddles: "off", pullBuoy: "off", snorkel: "off" },
          };

          // I Phase 2b — branch by intent mode:
          //   * Group intent (has group_id): route through /api/log-workout
          //     which creates the coach's history entry, fans out assignments,
          //     and (via the Phase 2b dbLinkCompletedToSchedule enhancement)
          //     flips the schedule row intent→payload atomically.
          //   * Self intent: simple PATCH — no history entry until coach
          //     actually swims it.
          if (ip.group_id) {
            const entry = {
              ...enrichedPayload,
              id:            makeEntryId(),
              savedAt:       new Date().toISOString(),
              dateCompleted: scheduledRow.scheduled_date,
              type:          enrichedPayload.typeId,
              typeLabel:     w.assignment_target?.group_name ? `${w.assignment_target.group_name} workout` : (enrichedPayload.typeId || "workout"),
              totalYards:    w.totalYards,
              estimatedMin:  w.estimatedMin,
              maxYardsCap:   enrichedPayload.maxYardsCap,
              poolMode:      enrichedPayload.poolMode,
              equipment:     enrichedPayload.equipment,
              blocks:        w.blocks,
              notes:         (scheduledRow.notes || "").slice(0, 1000),
              focusNote:     enrichedPayload.focusNote || undefined,
              completed:     scheduledRow.scheduled_date <= new Date().toISOString().slice(0, 10),
              scheduled_id:  scheduledRow.id,
              assign_to: {
                group_id: ip.group_id,
                ...(ip.lane_plan_id ? { lane_plan_id: ip.lane_plan_id } : {}),
              },
              assignment_target: w.assignment_target || undefined,
            };
            const res = await fetch("/api/log-workout", {
              method:  "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify(entry),
            });
            const j = await res.json().catch(() => ({}));
            if (!res.ok || !j.ok) {
              throw new Error(j.error || `HTTP ${res.status}`);
            }
          } else {
            // Self-only — Phase 2a flow.
            const res = await fetch(`/api/scheduled-workouts/${scheduledRow.id}`, {
              method:  "PATCH",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({ payload: enrichedPayload }),
            });
            if (!res.ok) {
              const j = await res.json().catch(() => ({}));
              throw new Error(j.error || `HTTP ${res.status}`);
            }
          }
          return true;
        } catch (e) {
          setErr(e.message);
          setBusy(false);
          return false;
        }
      };

      const handleRun = async () => {
        const ok = await commit();
        if (ok) onRun(w);  // parent loads into Run mode + tags runScheduledId
      };
      const handleSave = async () => {
        const ok = await commit();
        if (ok && onSave) onSave(w);  // parent refreshes week view + closes
      };

      return (
        <div onClick={onClose} style={{
          position: "fixed", inset: 0, zIndex: 10000, background: "rgba(0,0,0,0.90)",
          display: "flex", alignItems: "center", justifyContent: "center", padding: 20,
        }}>
          <div onClick={(e) => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            padding: 22, maxWidth: 720, width: "100%", maxHeight: "90vh", overflowY: "auto",
          }}>
            <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>Preview generated workout</div>
            <div style={{ color: "var(--color-text)", fontSize: 18, fontWeight: 700, marginBottom: 4 }}>{w?.typeLabel || w?.type || "Workout"} · {w?.totalYards || 0}y</div>
            <div style={{ color: "var(--color-text-muted)", fontSize: 12, marginBottom: 14 }}>
              Generated from intent for {scheduledRow.scheduled_date}. Regenerate for a different shuffle, or run as-is.
            </div>

            {w && w.blocks && w.blocks.map((b, i) => (
              <div key={i} className="card" style={{ padding: 10, borderRadius: 6, marginBottom: 6 }}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 5 }}>
                  <span style={{ color: "var(--color-text)", fontSize: 13, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                    {b.name || b.section}{b.label && ` — ${b.label}`}
                  </span>
                  <span style={{ color: "var(--color-text-dim)", fontSize: 11 }}>{b.totalYards}y</span>
                </div>
                {b.sets && b.sets.map((s, si) => (
                  <div key={si} style={{ fontSize: 12, color: "#cbd5e1", display: "flex", justifyContent: "space-between", marginBottom: 2 }}>
                    <span><strong>{s.reps}×{s.dist}</strong>{s.eq ? <em style={{ marginLeft: 4 }}>[{s.eq}]</em> : null} {s.desc || ""}</span>
                    <span style={{ fontFamily: "ui-monospace, monospace", marginLeft: 8, color: "var(--color-text-muted)" }}>{s.interval || ""}</span>
                  </div>
                ))}
              </div>
            ))}

            {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 6 }}>{err}</div>}

            <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14, flexWrap: "wrap" }}>
              <button onClick={onClose} disabled={busy}
                className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
              <button onClick={() => onRegenerate(w)} disabled={busy}
                style={{ padding: "8px 14px", borderRadius: 6, border: "1px solid var(--color-primary)", background: "transparent", color: "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: "pointer" }}>↻ Regenerate</button>
              <button onClick={handleSave} disabled={busy}
                title={scheduledRow.intent_params?.group_id ? "Commit this workout and assign to the group, without entering Run mode" : "Commit this workout to the schedule, without entering Run mode"}
                style={{ padding: "8px 14px", borderRadius: 6, border: "1px solid var(--color-warn)", background: "transparent", color: "var(--color-warn)", fontSize: 13, fontWeight: 700, cursor: busy ? "not-allowed" : "pointer" }}>
                {busy ? "…" : "💾 Save"}
              </button>
              <button onClick={handleRun} disabled={busy}
                title="Commit this workout AND open Run mode for the swim"
                className="btn btn-lg btn-filled btn-positive">
                {busy ? "…" : "▶ Run this"}
              </button>
            </div>
          </div>
        </div>
      );
    }

    // WeekView (I — Week-view planning, Phase 1) — 7-day ISO-week grid
    // (Monday → Sunday). Lists each user's own scheduled-workout rows per
    // day. Actions: ▶ Run (loads into Run mode, link via runScheduledId),
    // ✎ Edit (loads back into generator), 🗐 Copy to (date picker), 🗑 Delete.
    // Completed rows show a green ✓ + workout link.
    // ═══════════════════════════════════════════════════════════════
    //  REPORT PRINT VIEW — Reporting v1 Phase E
    //  Mirror of MultiPacePrintView pattern: overlay, body class for
    //  CSS scoping, auto-print on mount, Close button to exit.
    //  Per-report layouts (R1-R4); R5/R6 share-out has no use case so
    //  export buttons hidden for those tabs.
    // ═══════════════════════════════════════════════════════════════
    function ReportPrintView({ tab, data, rangeLabel, onClose }) {
      React.useEffect(() => {
        document.body.classList.add("reports-print-active");
        const t = setTimeout(() => { try { window.print(); } catch (_) {} }, 250);
        return () => {
          document.body.classList.remove("reports-print-active");
          clearTimeout(t);
        };
      }, []);

      const pageStyle = { position: "fixed", inset: 0, zIndex: 10000, background: "#fff", color: "#000", padding: "0.5in", overflow: "auto", fontFamily: "Georgia, 'Times New Roman', serif" };
      const headerStyle = { borderBottom: "2pt solid #000", paddingBottom: 8, marginBottom: 12 };
      const titleStyle = { fontSize: "20pt", fontWeight: 700, margin: 0 };
      const subStyle = { fontSize: "10pt", fontStyle: "italic", marginTop: 4, color: "#444" };
      const sectionHead = { fontSize: "13pt", fontWeight: 700, borderBottom: "1pt solid #888", paddingBottom: 4, margin: "16px 0 8px" };
      const tableStyle = { width: "100%", borderCollapse: "collapse", fontSize: "10pt" };
      const cellStyle = { padding: "4px 8px", borderBottom: "1px solid #ddd" };

      const fmt = (n) => Number(n || 0).toLocaleString();
      const pct = (n) => Math.round(Number(n || 0));

      const titleFor = {
        "programming-mix":    "Programming Mix",
        "schedule-adherence": "Schedule Adherence",
        "curation-log":       "Curation Log",
        "program-recap":      "Program Recap",
      }[tab] || "Report";

      // Per-tab body builders. Each returns JSX for the printed page.
      const renderBody = () => {
        if (tab === "programming-mix") {
          const typeRows = Object.entries(data.yardsByType || {}).sort((a, b) => b[1] - a[1]);
          const strokeRows = Object.entries(data.yardsByStroke || {}).sort((a, b) => b[1] - a[1]);
          const sectionRows = Object.entries(data.sectionYards || {}).sort((a, b) => b[1] - a[1]);
          const srcTotal = Object.values(data.sourceCounts || {}).reduce((s, n) => s + n, 0);
          const RowTable = ({ rows, title }) => (
            <>
              <div style={sectionHead}>{title}</div>
              <table style={tableStyle}><tbody>
                {rows.map(([k, v]) => <tr key={k}><td style={cellStyle}>{k}</td><td style={{ ...cellStyle, textAlign: "right" }}>{fmt(v)}</td></tr>)}
              </tbody></table>
            </>
          );
          return <>
            <div style={sectionHead}>Summary</div>
            <table style={tableStyle}><tbody>
              <tr><td style={cellStyle}>Total yards</td><td style={{ ...cellStyle, textAlign: "right" }}>{fmt(data.totalYards)}</td></tr>
              <tr><td style={cellStyle}>Workouts</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.workoutCount}</td></tr>
              <tr><td style={cellStyle}>Distinct bank labels</td><td style={{ ...cellStyle, textAlign: "right" }}>{(data.bankLabels || []).length}</td></tr>
              <tr><td style={cellStyle}>Source mix</td><td style={{ ...cellStyle, textAlign: "right" }}>Bank {pct((data.sourceCounts?.bank || 0) / Math.max(1, srcTotal) * 100)}% · Engine {pct((data.sourceCounts?.engine || 0) / Math.max(1, srcTotal) * 100)}% · Mix {pct((data.sourceCounts?.mix || 0) / Math.max(1, srcTotal) * 100)}%</td></tr>
            </tbody></table>
            <RowTable rows={typeRows} title="Yards by type" />
            <RowTable rows={strokeRows} title="Yards by stroke" />
            <RowTable rows={sectionRows} title="Yards by section" />
          </>;
        }
        if (tab === "schedule-adherence") {
          return <>
            <div style={sectionHead}>Schedule completion</div>
            <table style={tableStyle}><tbody>
              <tr><td style={cellStyle}>Scheduled practices</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.scheduledCount}</td></tr>
              <tr><td style={cellStyle}>Completed</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.completedCount} ({pct(data.completionPct)}%)</td></tr>
              <tr><td style={cellStyle}>With attendance recorded</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.withAttendance}</td></tr>
              <tr><td style={cellStyle}>Without attendance</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.withoutAttendance}</td></tr>
              <tr><td style={cellStyle}>Avg attendance per practice</td><td style={{ ...cellStyle, textAlign: "right" }}>{pct(data.avgAttendancePct)}%</td></tr>
            </tbody></table>
            {data.rosterTrend && <>
              <div style={sectionHead}>Roster trend</div>
              <table style={tableStyle}><tbody>
                <tr><td style={cellStyle}>Joined</td><td style={{ ...cellStyle, textAlign: "right" }}>+{data.rosterTrend.added}</td></tr>
                <tr><td style={cellStyle}>Left</td><td style={{ ...cellStyle, textAlign: "right" }}>−{data.rosterTrend.removed}</td></tr>
              </tbody></table>
            </>}
          </>;
        }
        if (tab === "curation-log") {
          const Section = ({ title, favs, dis, fmtItem }) => <>
            <div style={sectionHead}>{title}</div>
            <table style={tableStyle}>
              <thead><tr><th style={{ ...cellStyle, textAlign: "left", fontWeight: 700 }}>★ Favorites ({favs.length})</th><th style={{ ...cellStyle, textAlign: "left", fontWeight: 700 }}>👎 Disfavorites ({dis.length})</th></tr></thead>
              <tbody>
                {Array.from({ length: Math.max(favs.length, dis.length) }).map((_, i) => (
                  <tr key={i}><td style={cellStyle}>{favs[i] ? fmtItem(favs[i]) : ""}</td><td style={cellStyle}>{dis[i] ? fmtItem(dis[i]) : ""}</td></tr>
                ))}
              </tbody>
            </table>
          </>;
          return <>
            <Section title="Bank labels" favs={data.bankLabels?.favorites || []} dis={data.bankLabels?.disfavorites || []} fmtItem={x => x.label} />
            <Section title="Sets" favs={data.sets?.favorites || []} dis={data.sets?.disfavorites || []} fmtItem={x => setIdToName(x.set_id)} />
            <Section title="Engine tuples (current state)" favs={data.engine?.favorites || []} dis={data.engine?.disfavorites || []} fmtItem={x => `${x.template_id} · ${x.stroke}`} />
          </>;
        }
        if (tab === "program-recap") {
          const typeRows = Object.entries(data.yardsByType || {}).sort((a, b) => b[1] - a[1]);
          const strokeRows = Object.entries(data.yardsByStroke || {}).sort((a, b) => b[1] - a[1]);
          return <>
            <div style={sectionHead}>Summary</div>
            <table style={tableStyle}><tbody>
              <tr><td style={cellStyle}>Total yards</td><td style={{ ...cellStyle, textAlign: "right" }}>{fmt(data.totalYards)}</td></tr>
              <tr><td style={cellStyle}>Workouts</td><td style={{ ...cellStyle, textAlign: "right" }}>{data.workoutCount}</td></tr>
              {data.multiLane?.generated > 0 && <tr><td style={cellStyle}>Multi-lane fit rate</td><td style={{ ...cellStyle, textAlign: "right" }}>{pct(data.multiLane.successPct)}% ({data.multiLane.withoutFallback}/{data.multiLane.generated})</td></tr>}
            </tbody></table>
            <div style={sectionHead}>Yards by type</div>
            <table style={tableStyle}><tbody>
              {typeRows.map(([k, v]) => <tr key={k}><td style={cellStyle}>{k}</td><td style={{ ...cellStyle, textAlign: "right" }}>{fmt(v)}</td></tr>)}
            </tbody></table>
            <div style={sectionHead}>Yards by stroke</div>
            <table style={tableStyle}><tbody>
              {strokeRows.map(([k, v]) => <tr key={k}><td style={cellStyle}>{k}</td><td style={{ ...cellStyle, textAlign: "right" }}>{fmt(v)}</td></tr>)}
            </tbody></table>
            {(data.strokeGaps || []).length > 0 && <>
              <div style={sectionHead}>Stroke gaps (30-day windows)</div>
              <table style={tableStyle}><tbody>
                {data.strokeGaps.map((g, i) => <tr key={i}><td style={cellStyle}>{g.windowStart} → {g.windowEnd}</td><td style={cellStyle}>Missing: {g.missingStrokes.join(", ")}</td></tr>)}
              </tbody></table>
            </>}
          </>;
        }
        return <div>Export not available for this report.</div>;
      };

      return (
        <div className="reports-print-overlay" style={pageStyle}>
          <button className="screen-only" onClick={onClose}
            style={{ position: "absolute", top: 12, right: 14, padding: "6px 12px", borderRadius: 6, border: "1px solid #888", background: "#f5f5f5", color: "#000", fontSize: 12, fontWeight: 700, cursor: "pointer", zIndex: 10001 }}>
            ✕ Close print view
          </button>
          <div style={headerStyle}>
            <div style={titleStyle}>SetForge · {titleFor}</div>
            <div style={subStyle}>{rangeLabel} · generated {new Date().toLocaleDateString()}</div>
          </div>
          {renderBody()}
          <div style={{ marginTop: 24, paddingTop: 8, borderTop: "1pt solid #888", fontSize: "9pt", color: "#666", textAlign: "center" }}>
            © 2026 Competition Aquatics, LLC · setforge.io
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  REPORTS VIEW — Reporting v1 Phase B (R1-R3 coach reports)
    //  Spec: REPORTING_SCOPE.md §2 + §5. Self-only (gated to current
    //  coach via server). Numbers + tables only — no charts in v1
    //  (deferred per Cap'n's scope call).
    // ═══════════════════════════════════════════════════════════════
    const REPORT_RANGES = [
      { id: "week",            label: "Last 7 days" },
      { id: "month",           label: "Last 30 days" },
      { id: "quarter",         label: "Last 90 days" },
      { id: "season-to-date",  label: "Season-to-date" },
    ];

    // Build a markdown export for any tab. Walks the report JSON and emits
    // headers + tables. Single shared helper — markdown is naturally flexible
    // enough that per-report templates aren't worth the maintenance.
    function reportToMarkdown(tab, data) {
      const lines = [];
      const fmt = (n) => Number(n || 0).toLocaleString();
      const pct = (n) => Math.round(Number(n || 0));
      const titleFor = {
        "programming-mix":    "Programming Mix",
        "schedule-adherence": "Schedule Adherence",
        "curation-log":       "Curation Log",
        "program-recap":      "Program Recap",
        "platform-health":    "Platform Health",
        "curation-support":   "Curation & Support",
      }[tab] || "Report";
      lines.push(`# SetForge · ${titleFor}`);
      lines.push(`*Range: ${data.startYmd} → ${data.endYmd} · generated ${new Date().toISOString().slice(0, 10)}*`);
      lines.push("");
      const table = (rows, headers) => {
        if (!rows.length) return ["_(no data)_", ""];
        const out = [`| ${headers.join(" | ")} |`, `| ${headers.map(() => "---").join(" | ")} |`];
        rows.forEach(r => out.push(`| ${r.join(" | ")} |`));
        out.push("");
        return out;
      };
      if (tab === "programming-mix") {
        lines.push("## Summary", "", `- **Total yards:** ${fmt(data.totalYards)}`, `- **Workouts:** ${data.workoutCount}`, `- **Distinct bank labels:** ${(data.bankLabels || []).length}`, "");
        const srcTotal = Object.values(data.sourceCounts || {}).reduce((s, n) => s + n, 0);
        lines.push(`- **Source mix:** Bank ${pct((data.sourceCounts?.bank || 0) / Math.max(1, srcTotal) * 100)}% · Engine ${pct((data.sourceCounts?.engine || 0) / Math.max(1, srcTotal) * 100)}% · Mix ${pct((data.sourceCounts?.mix || 0) / Math.max(1, srcTotal) * 100)}%`, "");
        lines.push("## Yards by type", "");
        lines.push(...table(Object.entries(data.yardsByType || {}).sort((a, b) => b[1] - a[1]).map(([k, v]) => [k, fmt(v)]), ["Type", "Yards"]));
        lines.push("## Yards by stroke", "");
        lines.push(...table(Object.entries(data.yardsByStroke || {}).sort((a, b) => b[1] - a[1]).map(([k, v]) => [k, fmt(v)]), ["Stroke", "Yards"]));
        lines.push("## Yards by section", "");
        lines.push(...table(Object.entries(data.sectionYards || {}).sort((a, b) => b[1] - a[1]).map(([k, v]) => [k, fmt(v)]), ["Section", "Yards"]));
      } else if (tab === "schedule-adherence") {
        lines.push("## Schedule completion", "", `- **Scheduled:** ${data.scheduledCount}`, `- **Completed:** ${data.completedCount} (${pct(data.completionPct)}%)`, `- **With attendance:** ${data.withAttendance}`, `- **Without attendance:** ${data.withoutAttendance}`, `- **Avg attendance per practice:** ${pct(data.avgAttendancePct)}%`, "");
        if (data.rosterTrend) lines.push("## Roster trend", "", `- **Joined:** +${data.rosterTrend.added}`, `- **Left:** −${data.rosterTrend.removed}`, "");
      } else if (tab === "curation-log") {
        const section = (title, favs, dis, fmtItem) => {
          lines.push(`## ${title}`, "", `### ★ Favorites (${favs.length})`, "");
          if (!favs.length) lines.push("_(none in range)_", "");
          else favs.forEach(f => lines.push(`- ${fmtItem(f)}`));
          lines.push("", `### 👎 Disfavorites (${dis.length})`, "");
          if (!dis.length) lines.push("_(none in range)_", "");
          else dis.forEach(d => lines.push(`- ${fmtItem(d)}`));
          lines.push("");
        };
        section("Bank labels", data.bankLabels?.favorites || [], data.bankLabels?.disfavorites || [], x => x.label);
        section("Sets", data.sets?.favorites || [], data.sets?.disfavorites || [], x => setIdToName(x.set_id));
        section("Engine tuples (current state)", data.engine?.favorites || [], data.engine?.disfavorites || [], x => `\`${x.template_id}\` · ${x.stroke}`);
      } else if (tab === "program-recap") {
        lines.push("## Summary", "", `- **Total yards:** ${fmt(data.totalYards)}`, `- **Workouts:** ${data.workoutCount}`);
        if (data.multiLane?.generated > 0) lines.push(`- **Multi-lane fit rate:** ${pct(data.multiLane.successPct)}% (${data.multiLane.withoutFallback}/${data.multiLane.generated})`);
        lines.push("");
        lines.push("## Yards by type", "");
        lines.push(...table(Object.entries(data.yardsByType || {}).sort((a, b) => b[1] - a[1]).map(([k, v]) => [k, fmt(v)]), ["Type", "Yards"]));
        lines.push("## Yards by stroke", "");
        lines.push(...table(Object.entries(data.yardsByStroke || {}).sort((a, b) => b[1] - a[1]).map(([k, v]) => [k, fmt(v)]), ["Stroke", "Yards"]));
        if ((data.strokeGaps || []).length > 0) {
          lines.push("## Stroke gaps (30-day windows)", "");
          lines.push(...table(data.strokeGaps.map(g => [`${g.windowStart} → ${g.windowEnd}`, g.missingStrokes.join(", ")]), ["Window", "Missing"]));
        }
      } else if (tab === "platform-health") {
        const ac = data.activeCoaches || {}; const fa = data.featureAdoption || {};
        lines.push("## Active coaches", "", `- **7 day:** ${ac.d7}`, `- **14 day:** ${ac.d14}`, `- **30 day:** ${ac.d30}`, "");
        lines.push("## Feature adoption", "", `- **Engine:** ${pct(fa.enginePct)}%`, `- **Mix:** ${pct(fa.mixPct)}%`, `- **Multi-lane:** ${pct(fa.multiLanePct)}%`, `- **Fallback rate:** ${pct(fa.fallbackPct)}%`, `- **Users with favs/disfavs:** ${fa.usersWithFavs}/${fa.usersWithDis} of ${fa.totalUsers}`, "");
        if ((data.weeklyByTeam || []).length) {
          lines.push("## Workouts per week", "");
          lines.push(...table(data.weeklyByTeam.map(w => [`Week of ${w.weekStart}`, fmt(w.total)]), ["Week", "Workouts"]));
        }
      } else if (tab === "curation-support") {
        if ((data.curationByTeam || []).length) {
          lines.push("## Propagating disfavor by team", "");
          lines.push(...table(data.curationByTeam.map(t => [t.team_name, t.coach_count, t.label_count, t.set_count, t.engine_count, t.total_propagating]), ["Team", "Coaches", "Labels", "Sets", "Engine", "Total"]));
        }
        if ((data.impersonationByActor || []).length) {
          lines.push("## Impersonation activity", "");
          lines.push(...table(data.impersonationByActor.map(a => [a.admin_sub.slice(0, 20), a.sessions, a.avg_minutes, a.distinct_targets]), ["Admin sub", "Sessions", "Avg min", "Distinct targets"]));
        }
      }
      return lines.join("\n");
    }

    // Phase 5 #2 — Swimmer Progress Dashboard. A swimmer-facing surface (not
    // buried in Reports/Profile) that assembles existing data: training volume
    // from R4 Program Recap (/api/reports/program-recap) + benchmark PRs/trend
    // (/api/benchmarks) + the existing BenchmarksSection logger. Free/ungated —
    // it's the free-tier retention lever. No server/DB changes.
    function ProgressDashboard({ onPaceUpdate }) {
      const [recap, setRecap]   = React.useState(null);   // null = loading
      const [bench, setBench]   = React.useState(null);   // null = loading

      React.useEffect(() => {
        const ymd = (d) => d.toISOString().slice(0, 10);
        const end = new Date();
        const start = new Date(); start.setDate(start.getDate() - 90);
        (async () => {
          try {
            const r = await fetch(`/api/reports/program-recap?start=${ymd(start)}&end=${ymd(end)}`, { cache: "no-store" });
            setRecap(r.ok ? await r.json() : {});
          } catch (_) { setRecap({}); }
          try {
            const b = await fetch("/api/benchmarks", { cache: "no-store" });
            setBench(b.ok ? await b.json() : []);
          } catch (_) { setBench([]); }
        })();
      }, []);

      const fmtSecs = (s) => { if (s == null) return "—"; const m = Math.floor(s / 60), ss = String(Math.round(s % 60)).padStart(2, "0"); return `${m}:${ss}`; };
      const KIND_LABEL = { t30: "T-30", tt500: "500 TT", broken500: "Broken 500" };

      // Group benchmarks by kind (API returns recorded_at DESC). PR = lowest
      // pace_100_secs; trend compares the two most recent (lower = improving).
      const byKind = {};
      (bench || []).forEach(b => { (byKind[b.kind] = byKind[b.kind] || []).push(b); });
      const prRows = Object.keys(KIND_LABEL).filter(k => byKind[k] && byKind[k].length).map(k => {
        const rows = byKind[k];
        const paces = rows.map(r => Number(r.pace_100_secs)).filter(n => !Number.isNaN(n));
        const pr = paces.length ? Math.min(...paces) : null;
        let trend = null;
        if (rows.length >= 2 && rows[0].pace_100_secs != null && rows[1].pace_100_secs != null) {
          const d = Number(rows[0].pace_100_secs) - Number(rows[1].pace_100_secs); // latest - prior
          trend = d < -0.5 ? "up" : d > 0.5 ? "down" : "flat";
        }
        return { kind: k, pr, count: rows.length, latest: rows[0], trend };
      });

      const card = { background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 10, padding: 16, marginBottom: 16 };
      const muted = { color: "var(--color-text-muted)", fontSize: 12 };
      const loading = recap === null || bench === null;

      // yards-by-stroke bars
      const stroke = (recap && recap.yardsByStroke) || {};
      const strokeMax = Math.max(1, ...Object.values(stroke).map(Number));

      return (
        <div style={{ maxWidth: 720, margin: "0 auto" }}>
          <h2 style={{ color: "var(--color-text)", fontSize: 20, fontWeight: 800, marginBottom: 4 }}>📈 Progress</h2>
          <p style={{ ...muted, marginBottom: 16 }}>Your last 90 days of training, plus your test-set bests. Log a time trial below to track your paces over time.</p>

          {loading ? (
            <div style={{ ...muted, padding: "24px 0" }}>Loading…</div>
          ) : (
            <>
              {/* Training volume (R4) */}
              <div style={card}>
                <div style={{ fontSize: 11, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Training volume · last 90 days</div>
                <div style={{ display: "flex", gap: 24, marginBottom: 12 }}>
                  <div><div style={{ fontSize: 22, fontWeight: 800, color: "var(--color-text)" }}>{(recap.totalYards || 0).toLocaleString()}</div><div style={muted}>yards</div></div>
                  <div><div style={{ fontSize: 22, fontWeight: 800, color: "var(--color-text)" }}>{recap.workoutCount || 0}</div><div style={muted}>workouts</div></div>
                </div>
                {Object.keys(stroke).length > 0 ? (
                  <div style={{ display: "grid", gap: 4 }}>
                    {Object.entries(stroke).sort((a, b) => b[1] - a[1]).map(([s, y]) => (
                      <div key={s} style={{ display: "flex", alignItems: "center", gap: 8 }}>
                        <div style={{ width: 64, fontSize: 12, color: "var(--color-text-muted)", textTransform: "capitalize" }}>{s}</div>
                        <div style={{ flex: 1, background: "var(--color-bg)", borderRadius: 4, overflow: "hidden", height: 14 }}>
                          <div style={{ width: `${Math.round(Number(y) / strokeMax * 100)}%`, background: "var(--color-primary)", height: "100%" }} />
                        </div>
                        <div style={{ width: 70, textAlign: "right", fontSize: 11, color: "var(--color-text-dim)", fontVariantNumeric: "tabular-nums" }}>{Number(y).toLocaleString()} yd</div>
                      </div>
                    ))}
                  </div>
                ) : <div style={muted}>No saved workouts in this window yet.</div>}
                {Array.isArray(recap.strokeGaps) && recap.strokeGaps.length > 0 && (
                  <div style={{ marginTop: 10, fontSize: 12, color: "var(--color-warn)" }}>
                    ⚠ Stroke gap: a 30-day window missing {recap.strokeGaps[recap.strokeGaps.length - 1].missingStrokes.join(", ")}.
                  </div>
                )}
              </div>

              {/* Personal bests (benchmarks) */}
              <div style={card}>
                <div style={{ fontSize: 11, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Test-set bests</div>
                {prRows.length === 0 ? (
                  <div style={muted}>No time trials logged yet. Log one below — you'll see your best pace and whether you're trending faster.</div>
                ) : (
                  <div style={{ display: "grid", gap: 8 }}>
                    {prRows.map(r => (
                      <div key={r.kind} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8, paddingBottom: 6, borderBottom: "1px solid var(--color-border)" }}>
                        <div style={{ color: "var(--color-text)", fontSize: 14, fontWeight: 600 }}>{KIND_LABEL[r.kind]}</div>
                        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
                          <span style={{ fontSize: 13, color: "var(--color-text)", fontVariantNumeric: "tabular-nums" }}>PR {fmtSecs(r.pr)}/100</span>
                          {r.trend && <span title="vs your previous result" style={{ fontSize: 13, color: r.trend === "up" ? "var(--color-positive)" : r.trend === "down" ? "var(--color-warn)" : "var(--color-text-dim)" }}>{r.trend === "up" ? "▲ faster" : r.trend === "down" ? "▼ slower" : "—"}</span>}
                          <span style={muted}>{r.count} logged</span>
                        </div>
                      </div>
                    ))}
                  </div>
                )}
              </div>

              {/* Logger (reuses the existing component) */}
              <div style={card}>
                <div style={{ fontSize: 11, fontWeight: 700, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Log a time trial</div>
                <BenchmarksSection onPaceUpdate={onPaceUpdate || (() => {})} />
              </div>
            </>
          )}
        </div>
      );
    }

    function ReportsView({ isCoach = false, isAdmin = false }) {
      // Non-coaches land on Program Recap (R4) — the only tab they can see.
      // Coaches default to Programming Mix (R1) as before. Admin gets two
      // extra tabs (R5 Platform Health, R6 Curation & Support).
      const [tab, setTab] = React.useState(isCoach ? "programming-mix" : "program-recap");
      const [range, setRange] = React.useState("month");
      const [groupId, setGroupId] = React.useState("");   // "" = all groups
      const [groups, setGroups] = React.useState([]);
      const [data, setData] = React.useState(null);
      const [err, setErr] = React.useState(null);
      const [loading, setLoading] = React.useState(false);
      // Phase E — print-view overlay state. null = not printing. Holds the
      // tab + data snapshot at the moment Print was clicked so the overlay
      // renders the same data even if filters change.
      const [printingData, setPrintingData] = React.useState(null);

      // Load coach's groups for the filter dropdown (coach-only endpoint).
      React.useEffect(() => {
        if (!isCoach) return;
        fetch("/api/picker/coach-targets", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(list => setGroups(Array.isArray(list) ? list : []))
          .catch(() => setGroups([]));
      }, [isCoach]);

      // Re-fetch the active report whenever tab / range / groupId changes.
      React.useEffect(() => {
        const qs = new URLSearchParams({ range });
        if (groupId) qs.set("group_id", groupId);
        const url = `/api/reports/${tab}?${qs}`;
        setLoading(true); setErr(null); setData(null);
        fetch(url, { cache: "no-store" })
          .then(r => r.ok ? r.json() : r.json().then(j => Promise.reject(new Error(j.error || `HTTP ${r.status}`))))
          .then(d => { setData(d); setLoading(false); })
          .catch(e => { setErr(e.message); setLoading(false); });
      }, [tab, range, groupId]);

      // Coach tabs gated to coaches; admin tabs gated to admins; Program
      // Recap is always visible.
      const tabs = [
        ...(isCoach ? [
          { id: "programming-mix",    label: "📈 Programming Mix" },
          { id: "schedule-adherence", label: "🗓 Schedule Adherence" },
          { id: "curation-log",       label: "🎚 Curation Log" },
        ] : []),
        { id: "program-recap",        label: "📖 Program Recap" },
        ...(isAdmin ? [
          { id: "platform-health",    label: "🩺 Platform Health" },
          { id: "curation-support",   label: "🛡 Curation & Support" },
        ] : []),
      ];

      return (
        <div>
          <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginBottom: 16, flexWrap: "wrap" }}>
            <h2 style={{ margin: 0, color: "var(--color-text)" }}>📊 Reports</h2>
            <span style={{ color: "var(--color-text-dim)", fontSize: 13 }}>
              Self-only coach view · numbers + tables (charts in a future phase)
            </span>
          </div>

          {/* Sticky filter row: range + group */}
          <div style={{
            display: "flex", flexWrap: "wrap", gap: 10, alignItems: "center",
            padding: 12, background: "var(--color-card)", border: "1px solid var(--color-border)",
            borderRadius: 8, marginBottom: 14,
          }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 12, fontWeight: 600 }}>RANGE</span>
            {REPORT_RANGES.map(r => {
              const active = range === r.id;
              return (
                <button key={r.id} onClick={() => setRange(r.id)}
                  style={{
                    padding: "5px 12px", borderRadius: 999,
                    border: `1px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                    background: active ? "#1e3a5f" : "var(--color-bg)",
                    color: active ? "var(--color-primary)" : "var(--color-text-dim)",
                    fontSize: 12, fontWeight: 700, cursor: "pointer",
                  }}>{r.label}</button>
              );
            })}
            {groups.length > 0 && (
              <>
                <span style={{ color: "var(--color-text-dim)", fontSize: 12, fontWeight: 600, marginLeft: 8 }}>GROUP</span>
                <select value={groupId} onChange={e => setGroupId(e.target.value)}
                  style={{
                    padding: "5px 10px", borderRadius: 6,
                    background: "var(--color-bg)", color: "var(--color-text)",
                    border: "1px solid var(--color-border-strong)", fontSize: 12, fontWeight: 600, cursor: "pointer",
                  }}>
                  <option value="">All groups</option>
                  {groups.map(g => <option key={g.id} value={g.id}>{g.name}</option>)}
                </select>
              </>
            )}
            {data && (
              <span style={{ marginLeft: "auto", color: "var(--color-text-muted)", fontSize: 11 }}>
                {data.startYmd} → {data.endYmd}
              </span>
            )}
            {/* Phase E export buttons. PDF print disabled for R5/R6 (admin-
                internal — no share use case). Markdown available for all. */}
            {data && (
              <div style={{ display: "flex", gap: 6 }}>
                {["programming-mix", "schedule-adherence", "curation-log", "program-recap"].includes(tab) && (
                  <button onClick={() => setPrintingData({ tab, data })}
                    title="Open a print-friendly view, then use your browser's print dialog to save as PDF"
                    style={{ padding: "5px 10px", borderRadius: 6, border: "1px solid var(--color-primary)", background: "transparent", color: "var(--color-primary)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                    📄 Print / PDF
                  </button>
                )}
                <button onClick={() => {
                  const md = reportToMarkdown(tab, data);
                  const filename = `setforge-${tab}-${data.startYmd}-to-${data.endYmd}.md`;
                  const blob = new Blob([md], { type: "text/markdown;charset=utf-8" });
                  const url = URL.createObjectURL(blob);
                  const a = document.createElement("a");
                  a.href = url; a.download = filename;
                  document.body.appendChild(a); a.click();
                  setTimeout(() => { URL.revokeObjectURL(url); a.remove(); }, 100);
                }}
                  title="Download a markdown file (paste into email, Slack, Notion, etc.)"
                  style={{ padding: "5px 10px", borderRadius: 6, border: "1px solid var(--color-border-strong)", background: "transparent", color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                  📋 Markdown
                </button>
              </div>
            )}
          </div>

          {/* Tab nav */}
          <div style={{ display: "flex", gap: 4, borderBottom: "1px solid var(--color-border)", marginBottom: 16 }}>
            {tabs.map(t => {
              const active = tab === t.id;
              return (
                <button key={t.id} onClick={() => setTab(t.id)}
                  style={{
                    padding: "10px 16px", border: "none", background: "transparent",
                    color: active ? "var(--color-primary)" : "var(--color-text-dim)",
                    fontSize: 13, fontWeight: 700, cursor: "pointer",
                    borderBottom: `3px solid ${active ? "var(--color-primary)" : "transparent"}`,
                    marginBottom: -1,
                  }}>{t.label}</button>
              );
            })}
          </div>

          {/* Tab body */}
          {loading && <div style={{ color: "var(--color-text-dim)" }}>Loading…</div>}
          {err && <div style={{ color: "var(--color-destructive)", padding: 12, background: "rgba(239,68,68,0.08)", borderRadius: 6 }}>⚠ {err}</div>}
          {!loading && !err && data && tab === "programming-mix"    && <R1ProgrammingMixTab data={data} />}
          {!loading && !err && data && tab === "schedule-adherence" && <R2ScheduleAdherenceTab data={data} hasGroup={!!groupId} />}
          {!loading && !err && data && tab === "curation-log"       && <R3CurationLogTab data={data} />}
          {!loading && !err && data && tab === "program-recap"      && <R4ProgramRecapTab data={data} />}
          {!loading && !err && data && tab === "platform-health"    && <R5PlatformHealthTab data={data} />}
          {!loading && !err && data && tab === "curation-support"   && <R6CurationSupportTab data={data} />}

          {/* Phase E print overlay. Mounted at the ReportsView root so
              the body class scoping works correctly. */}
          {printingData && (
            <ReportPrintView
              tab={printingData.tab}
              data={printingData.data}
              rangeLabel={`${printingData.data.startYmd} → ${printingData.data.endYmd}${printingData.data.groupId ? ` · group ${printingData.data.groupId}` : ""}`}
              onClose={() => setPrintingData(null)}
            />
          )}
        </div>
      );
    }

    // R2 — Schedule Adherence tab body.

    // R3 — Curation Log tab body. Three subsections (labels / sets / engine).

    // R4 — Program Recap (Reporting v1 Phase C). Solo/masters report.
    // Stat cards + reused tables + most/least-used templates + 4-stroke
    // balance grid + multi-lane fit success rate.

    // R5 — Platform Health (Reporting v1 Phase D, admin only).

    // R6 — Curation & Support Activity (admin only).

    // Reporting Phase A — modal for marking a scheduled practice done +
    // PracticesView — a dedicated coach screen that surfaces attendance/roll-call
    // outside the week grid. Lists scheduled practices over a date window
    // (default: this week + the next 3) and opens the SAME MarkPracticeDoneModal
    // the week view uses, so behavior stays identical. Additive: the week-view
    // "📋 Mark done" button is unchanged. Intent-mode rows (not yet generated)
    // can't take attendance, so they're shown disabled with a hint.
    function PracticesView() {
      const [list, setList]   = React.useState(null);   // null = loading
      const [err, setErr]     = React.useState(null);
      const [marking, setMarking] = React.useState(null);

      const fmtYmd = (d) => {
        const y = d.getFullYear(), m = String(d.getMonth() + 1).padStart(2, "0"), dd = String(d.getDate()).padStart(2, "0");
        return `${y}-${m}-${dd}`;
      };
      // Window: start of this week (local), 28 days forward.
      const { startStr, endStr } = React.useMemo(() => {
        const now = new Date();
        const start = new Date(now); start.setDate(now.getDate() - ((now.getDay() + 6) % 7)); // Monday
        const end = new Date(start); end.setDate(start.getDate() + 27);
        return { startStr: fmtYmd(start), endStr: fmtYmd(end) };
      }, []);

      const load = React.useCallback(async () => {
        setErr(null);
        try {
          const res = await fetch(`/api/scheduled-workouts?start=${startStr}&end=${endStr}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const rows = await res.json();
          rows.sort((a, b) => (a.scheduled_date || "").localeCompare(b.scheduled_date || ""));
          setList(rows);
        } catch (e) { setList([]); setErr(e.message); }
      }, [startStr, endStr]);
      React.useEffect(() => { load(); }, [load]);

      const prettyDate = (ymdStr) => {
        if (!ymdStr) return "";
        const [y, m, d] = ymdStr.split("-").map(Number);
        const dt = new Date(y, (m || 1) - 1, d || 1);
        return dt.toLocaleDateString(undefined, { weekday: "short", month: "short", day: "numeric" });
      };

      return (
        <div style={{ maxWidth: 720, margin: "0 auto" }}>
          <h2 style={{ color: "var(--color-text)", fontSize: 20, fontWeight: 800, marginBottom: 4 }}>📋 Practices</h2>
          <p style={{ color: "var(--color-text-muted)", fontSize: 13, marginBottom: 16 }}>
            Mark a practice done and record who attended. Shows your scheduled practices for the next four weeks.
          </p>
          {err && <div style={{ color: "var(--color-warn)", fontSize: 13, marginBottom: 12 }}>Couldn't load practices: {err}</div>}
          {list === null ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13 }}>Loading…</div>
          ) : list.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13, padding: "32px 0", textAlign: "center" }}>
              No scheduled practices in the next four weeks. Schedule one from the 📅 week view.
            </div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              {list.map(sw => {
                const isIntent = sw.mode === "intent";
                const done = !!sw.completed_at;
                return (
                  <div key={sw.id} className="card" style={{ display: "flex", alignItems: "center", gap: 12, padding: "10px 14px", borderRadius: 10 }}>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ color: "var(--color-text)", fontWeight: 700, fontSize: 14 }}>{prettyDate(sw.scheduled_date)}</div>
                      <div style={{ color: "var(--color-text-dim)", fontSize: 11 }}>
                        {sw.facility_name ? sw.facility_name + " · " : ""}{isIntent ? "Planned (generate first)" : (done ? "Attendance recorded" : "Not yet recorded")}
                      </div>
                    </div>
                    {isIntent ? (
                      <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>—</span>
                    ) : (
                      <button onClick={() => setMarking(sw)}
                        style={{ padding: "6px 12px", border: "1px solid var(--color-primary)", borderRadius: 6,
                          background: done ? "transparent" : "var(--color-primary)",
                          color: done ? "var(--color-primary)" : "var(--color-bg)",
                          fontSize: 12, fontWeight: 700, cursor: "pointer", whiteSpace: "nowrap" }}>
                        {done ? "Edit attendance" : "📋 Mark done"}
                      </button>
                    )}
                  </div>
                );
              })}
            </div>
          )}
          {marking && (
            <MarkPracticeDoneModal
              sw={marking}
              onClose={() => setMarking(null)}
              onSaved={() => { setMarking(null); load(); }}
            />
          )}
        </div>
      );
    }

    // recording attendance. Fetches /attendance-context to get roster
    // (point-in-time) + any existing attendance, renders roster pre-checked,
    // POSTs /complete on save. Group-less workouts (solo / no roster) get
    // a simpler "Just mark done" path with no checklist.
    function MarkPracticeDoneModal({ sw, onClose, onSaved }) {
      const [ctx, setCtx] = React.useState(null);    // { roster, attendance, group_id, completed_at, ... }
      const [absent, setAbsent] = React.useState(() => new Set()); // keys of swimmers marked absent
      const [busy, setBusy] = React.useState(false);
      const [err, setErr] = React.useState(null);

      // Stable key per roster row — swimmer_sub takes precedence; managed_id
      // prefixed to avoid collisions.
      const rowKey = (m) => m.member_swimmer_sub ? `s:${m.member_swimmer_sub}` : `m:${m.member_managed_id}`;

      React.useEffect(() => {
        let cancelled = false;
        fetch(`/api/scheduled-workouts/${sw.id}/attendance-context`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : Promise.reject(new Error(`HTTP ${r.status}`)))
          .then(data => {
            if (cancelled) return;
            setCtx(data);
            // Seed absent set from existing attendance rows (present=false).
            const seed = new Set();
            for (const a of (data.attendance || [])) {
              if (a.present) continue;
              const k = a.swimmer_sub ? `s:${a.swimmer_sub}` : `m:${a.managed_id}`;
              seed.add(k);
            }
            setAbsent(seed);
          })
          .catch(e => { if (!cancelled) setErr(e.message); });
        return () => { cancelled = true; };
      }, [sw.id]);

      const toggle = (k) => setAbsent(prev => {
        const n = new Set(prev);
        if (n.has(k)) n.delete(k); else n.add(k);
        return n;
      });

      const save = async () => {
        setBusy(true); setErr(null);
        try {
          const attendance = (ctx?.roster || []).map(m => {
            const k = rowKey(m);
            const present = !absent.has(k);
            return m.member_swimmer_sub
              ? { swimmer_sub: m.member_swimmer_sub, present }
              : { managed_id:  m.member_managed_id,  present };
          });
          const res = await fetch(`/api/scheduled-workouts/${sw.id}/complete`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ attendance }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          onSaved && onSaved();
        } catch (e) { setErr(e.message); }
        finally { setBusy(false); }
      };

      const presentCount = (ctx?.roster?.length || 0) - absent.size;

      return (
        <div onClick={onClose} className="modal-overlay" style={{ padding: 20 }}>
          <div onClick={e => e.stopPropagation()} style={{
            background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
            padding: 22, maxWidth: 480, width: "100%", maxHeight: "80vh", display: "flex", flexDirection: "column",
          }}>
            <div style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>
              {ctx?.completed_at ? "Edit attendance" : "Mark practice done"}
            </div>
            <div style={{ color: "var(--color-text)", fontSize: 15, marginBottom: 14 }}>
              {sw.scheduled_date} {ctx?.group_id ? `· group ${ctx.group_id}` : "· no group (solo)"}
            </div>
            {err && <div style={{ color: "var(--color-destructive)", fontSize: 12, marginBottom: 10 }}>⚠ {err}</div>}
            {!ctx ? (
              <div style={{ color: "var(--color-text-dim)", fontSize: 13 }}>Loading roster…</div>
            ) : ctx.roster.length === 0 ? (
              <div style={{ color: "var(--color-text-dim)", fontSize: 13, padding: "12px 0", lineHeight: 1.5 }}>
                No roster attached — clicking Save will just stamp the practice as done.
              </div>
            ) : (
              <>
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, marginBottom: 8 }}>
                  {presentCount}/{ctx.roster.length} present. Uncheck absentees.
                </div>
                <div style={{ overflowY: "auto", border: "1px solid var(--color-card)", borderRadius: 6, padding: 4, flex: 1 }}>
                  {ctx.roster.map(m => {
                    const k = rowKey(m);
                    const present = !absent.has(k);
                    return (
                      <label key={k} style={{
                        display: "flex", alignItems: "center", gap: 10,
                        padding: "8px 10px", borderRadius: 4, cursor: "pointer",
                        background: present ? "transparent" : "rgba(239,68,68,0.08)",
                      }}>
                        <input type="checkbox" checked={present} onChange={() => toggle(k)}
                          style={{ width: 18, height: 18, cursor: "pointer" }} />
                        <span style={{ color: present ? "var(--color-text)" : "var(--color-text-muted)", fontSize: 14, flex: 1, textDecoration: present ? "none" : "line-through" }}>
                          {m.display_name || m.initials || "(no name)"}
                        </span>
                        <span style={{ color: "var(--color-text-dim)", fontSize: 10 }}>
                          {m.role}{m.member_managed_id ? " · managed" : ""}
                        </span>
                      </label>
                    );
                  })}
                </div>
              </>
            )}
            <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
              <button onClick={onClose} disabled={busy}
                className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
              <button onClick={save} disabled={busy || !ctx}
                className="btn btn-lg btn-filled btn-primary">
                {busy ? "Saving…" : (ctx?.completed_at ? "Update" : "Mark done ✓")}
              </button>
            </div>
          </div>
        </div>
      );
    }

    function WeekView({ onRunScheduled, onEditScheduled, myConstraints = [] }) {
      // Compute the Monday of the current ISO week (Mon=1, Sun=7).
      function isoMondayOf(d) {
        const x = new Date(d);
        x.setHours(0, 0, 0, 0);
        const day = x.getDay(); // 0=Sun
        const diff = day === 0 ? -6 : 1 - day;
        x.setDate(x.getDate() + diff);
        return x;
      }
      function ymd(d) {
        const y = d.getFullYear(), m = String(d.getMonth() + 1).padStart(2, "0"), dd = String(d.getDate()).padStart(2, "0");
        return `${y}-${m}-${dd}`;
      }
      function fmtMonDay(d) {
        return d.toLocaleDateString(undefined, { weekday: "short", month: "short", day: "numeric" });
      }
      function isoWeekNumber(d) {
        // ISO week number for the given date.
        const x = new Date(Date.UTC(d.getFullYear(), d.getMonth(), d.getDate()));
        const dayNum = (x.getUTCDay() + 6) % 7; // 0=Mon
        x.setUTCDate(x.getUTCDate() - dayNum + 3);
        const firstThursday = x.valueOf();
        x.setUTCMonth(0, 1);
        if (x.getUTCDay() !== 4) {
          x.setUTCMonth(0, 1 + ((4 - x.getUTCDay()) + 7) % 7);
        }
        return 1 + Math.ceil((firstThursday - x) / 604800000);
      }

      const [weekStart, setWeekStart] = React.useState(() => isoMondayOf(new Date()));
      const [list,      setList]      = React.useState(null);
      const [err,       setErr]       = React.useState(null);
      const [copyTarget, setCopyTarget] = React.useState(null); // { sw, date }
      // Phase 2a — intent form + preview state.
      const [intentForm, setIntentForm] = React.useState(null);  // { date, intent, editingId } | null
      const [intentPreview, setIntentPreview] = React.useState(null); // { sw, workout } | null
      const [repeatBusy, setRepeatBusy] = React.useState(false);
      // Reporting Phase A — attendance modal target. null = closed; sw = open.
      const [markingPractice, setMarkingPractice] = React.useState(null);
      // I Phase 2b — coach targets cache (for group badge + Generate-from-intent
      // assignment_target stamping). Empty array when user isn't a coach.
      const [coachTargets, setCoachTargets] = React.useState([]);
      React.useEffect(() => {
        fetch("/api/picker/coach-targets", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(list => setCoachTargets(Array.isArray(list) ? list : []))
          .catch(() => setCoachTargets([]));
      }, []);

      const days = React.useMemo(() => {
        const arr = [];
        for (let i = 0; i < 7; i++) {
          const d = new Date(weekStart);
          d.setDate(d.getDate() + i);
          arr.push(d);
        }
        return arr;
      }, [weekStart]);

      const startStr = ymd(weekStart);
      const endStr   = ymd(days[6]);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/scheduled-workouts?start=${startStr}&end=${endStr}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setList(await res.json());
        } catch (e) { setList([]); setErr(e.message); }
      }, [startStr, endStr]);
      React.useEffect(() => { load(); }, [load]);

      const byDate = React.useMemo(() => {
        const m = {};
        for (const sw of (list || [])) {
          const k = sw.scheduled_date;
          (m[k] = m[k] || []).push(sw);
        }
        return m;
      }, [list]);

      const goPrev = () => setWeekStart(prev => { const d = new Date(prev); d.setDate(d.getDate() - 7); return d; });
      const goNext = () => setWeekStart(prev => { const d = new Date(prev); d.setDate(d.getDate() + 7); return d; });
      const goThis = () => setWeekStart(isoMondayOf(new Date()));

      const handleDelete = async (sw) => {
        if (!window.confirm(`Delete this scheduled workout? (${sw.payload?.typeLabel || sw.payload?.type || "workout"})`)) return;
        try {
          const res = await fetch(`/api/scheduled-workouts/${sw.id}`, { method: "DELETE", headers: { ...csrfHeaders() } });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      const handleCopy = async (sw, targetDate) => {
        try {
          // Copy preserves mode: intent-mode rows copy as intent; payload
          // copies as payload. (Phase 1 only knew payload; Phase 2a adds
          // intent.)
          const body = {
            scheduled_date: targetDate,
            notes: sw.notes,
          };
          if (sw.payload)       body.payload = sw.payload;
          if (sw.intent_params) body.intent_params = sw.intent_params;
          const res = await fetch("/api/scheduled-workouts", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify(body),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setCopyTarget(null);
          await load();
        } catch (e) { setErr(e.message); }
      };

      // Phase 2a — generate-from-intent. Invokes the in-app generateWorkout
      // with the intent params, opens IntentPreviewOverlay with the result.
      // User can Run / Regenerate / Cancel from the preview.
      const handleGenerateFromIntent = (sw) => {
        const ip = sw.intent_params || {};
        try {
          const generated = generateWorkout({
            typeId:       ip.type,
            maxYards:     Number(ip.maxYards) || 2400,
            equipment:    ip.equipment || {},
            favorites:    [],
            // P4 — default to the scheduled facility's course (25y/25m/50m)
            // when one is set; else the historical 25y default.
            poolMode:     sw.facility_course || "25y",
            userMin:      1200,
            pinnedBlocks: {},
            recentLabels: [],
            recoveryMode: !!ip.recovery,
            phase:        ip.phase || null,
            favoriteSetIds: null,
            sectionBias:  ip.mix || "balanced",
            // Section model A2 — honor the intent's section choices if present
            // (absent → generateWorkout defaults to all four).
            ...(Array.isArray(ip.includedSections) ? { includedSections: ip.includedSections } : {}),
            myConstraints,                                                       // Phase 3 PSC slice 2 — passed in from App so intent-generates honor active constraints
          });
          if (generated && generated.__generateFailure) {
            setErr(generated.error || "Couldn't generate from intent — try regenerating or editing the intent.");
            return;
          }
          // I Phase 2b — stamp assignment_target so the preview overlay's Run
          // handler can route through /api/log-workout's fanout path. Mirrors
          // R-D's handleGenerate stamping.
          if (ip.group_id) {
            const g = coachTargets.find(t => t.id === ip.group_id);
            if (g) {
              generated.assignment_target = {
                kind:         "group",
                group_id:     g.id,
                group_name:   g.name,
                member_count: g.member_count,
                phase:        g.current_phase || null,
              };
              if (ip.lane_plan_id) generated.assignment_target.lane_plan_id = ip.lane_plan_id;
            }
          }
          // Section model — attach the intent's dryland block (resolved from
          // the preset bank) at the chosen placement.
          if (ip.dryland?.preset && Array.isArray(generated.blocks)) {
            const opt = DRYLAND_OPTIONS.find(o => o.id === ip.dryland.preset);
            if (opt) {
              const blk = makeDrylandBlock(opt, ip.dryland.placement || "pre");
              generated.blocks = ip.dryland.placement === "post"
                ? [...generated.blocks, blk]
                : [blk, ...generated.blocks];
            }
          }
          setIntentPreview({ sw, workout: generated });
        } catch (e) {
          setErr(`Generate failed: ${e.message}`);
        }
      };

      // Phase 2a — bulk-copy this week to next.
      const handleRepeatWeek = async () => {
        const rowCount = (list || []).length;
        if (rowCount > 5 && !window.confirm(`Copy all ${rowCount} workouts to next week? Target days that already have rows will be skipped.`)) return;
        if (rowCount === 0) { setErr("Nothing to repeat — this week has no scheduled workouts."); return; }
        setRepeatBusy(true); setErr(null);
        try {
          const res = await fetch("/api/scheduled-workouts/repeat-week", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ start_date: startStr }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          // Jump to the new week and reload.
          const nextStart = new Date(weekStart); nextStart.setDate(nextStart.getDate() + 7);
          setWeekStart(nextStart);
          // load() will fire automatically via the useEffect dependency change.
          window.alert(`Copied ${j.copied} workout${j.copied === 1 ? "" : "s"} to next week.${j.skipped > 0 ? ` Skipped ${j.skipped} (target day already had rows).` : ""}`);
        } catch (e) { setErr(e.message); }
        finally { setRepeatBusy(false); }
      };

      const today = ymd(new Date());
      const showingThisWeek = startStr === ymd(isoMondayOf(new Date()));

      return (
        <div>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 14 }}>
            <div>
              <h2 style={{ margin: 0, color: "var(--color-text)", fontSize: 22 }}>📅 Plan your week</h2>
              <div style={{ color: "var(--color-text-muted)", fontSize: 12, marginTop: 4 }}>
                Week {isoWeekNumber(weekStart)} · {weekStart.toLocaleDateString(undefined, { month: "short", day: "numeric" })} – {days[6].toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" })}
              </div>
            </div>
            <div style={{ display: "flex", gap: 6, flexWrap: "wrap", justifyContent: "flex-end" }}>
              <button onClick={handleRepeatWeek} disabled={repeatBusy || (list || []).length === 0}
                title="Copy this week's scheduled workouts to next week (skips days that already have rows)"
                style={{ padding: "6px 12px", borderRadius: 6, border: `1px solid ${repeatBusy ? "var(--color-border)" : "var(--color-warn)"}`, background: repeatBusy ? "transparent" : "rgba(245,158,11,0.12)", color: repeatBusy ? "var(--color-text-dim)" : "var(--color-warn)", fontSize: 13, fontWeight: 700, cursor: repeatBusy || (list || []).length === 0 ? "not-allowed" : "pointer", opacity: (list || []).length === 0 ? 0.4 : 1 }}>{repeatBusy ? "…" : "🗓 Repeat next week"}</button>
              <button onClick={goPrev} title="Previous week"
                className="btn btn-md btn-outlined btn-neutral">‹ Prev</button>
              <button onClick={goThis} disabled={showingThisWeek}
                title="Jump to current ISO week"
                style={{ padding: "6px 12px", borderRadius: 6, border: `1px solid ${showingThisWeek ? "var(--color-border)" : "var(--color-primary)"}`, background: showingThisWeek ? "transparent" : "rgba(59,130,246,0.15)", color: showingThisWeek ? "var(--color-border-strong)" : "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: showingThisWeek ? "default" : "pointer" }}>This week</button>
              <button onClick={goNext} title="Next week"
                className="btn btn-md btn-outlined btn-neutral">Next ›</button>
            </div>
          </div>

          {err && <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>{err}</div>}

          <div style={{ display: "grid", gridTemplateColumns: "1fr", gap: 8 }}>
            {days.map((d) => {
              const dk = ymd(d);
              const rows = byDate[dk] || [];
              const isToday = dk === today;
              const isPast  = dk < today;
              return (
                <div key={dk} style={{
                  border: `1px solid ${isToday ? "var(--color-primary)" : "var(--color-border)"}`,
                  background: isToday ? "rgba(167,139,250,0.06)" : "var(--color-card)",
                  borderRadius: 8, padding: "10px 14px",
                }}>
                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 6 }}>
                    <div style={{ color: isToday ? "var(--color-primary)" : "var(--color-text)", fontSize: 13, fontWeight: 700 }}>
                      {fmtMonDay(d)}
                      {isToday && <span style={{ marginLeft: 8, fontSize: 10, color: "var(--color-primary)", textTransform: "uppercase", letterSpacing: "0.1em" }}>Today</span>}
                      {isPast && rows.length === 0 && <span style={{ marginLeft: 8, fontSize: 10, color: "var(--color-border-strong)", fontStyle: "italic" }}>(rest day)</span>}
                    </div>
                    <span style={{ fontSize: 10, color: "var(--color-border-strong)" }}>{rows.length} {rows.length === 1 ? "workout" : "workouts"}</span>
                  </div>
                  {rows.length === 0 && !isPast && (
                    <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "4px 0", flexWrap: "wrap" }}>
                      <span style={{ color: "var(--color-border-strong)", fontSize: 11, fontStyle: "italic" }}>Nothing scheduled.</span>
                      <button onClick={() => setIntentForm({ date: dk, intent: null, editingId: null })}
                        title="Plan a workout intent for this day — generate-from-intent on the day of"
                        style={{ padding: "3px 10px", border: "1px dashed var(--color-border-strong)", borderRadius: 5, background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>+ Plan intent</button>
                    </div>
                  )}
                  {rows.map(sw => {
                    const done       = !!sw.completed_workout_id;
                    // Reporting Phase A — practice completion stamp (separate
                    // from `done`/completed_workout_id, which is swimmer's
                    // run-mode log linkback). practiceDone captures "coach
                    // closed the practice + recorded attendance."
                    const practiceDone = !!sw.completed_at;
                    const isIntent   = sw.mode === "intent";
                    // Payload field shape: raw workout object from generateWorkout →
                    // has typeId + totalYards (no type/typeLabel/maxYardsCap/poolMode).
                    // Intent_params shape: { type, maxYards, mix, recovery }.
                    const typeId     = sw.payload?.typeId || sw.intent_params?.type || null;
                    const typeMeta   = typeId ? WORKOUT_TYPES.find(t => t.id === typeId) : null;
                    const typeLabel  = typeMeta ? `${typeMeta.emoji} ${typeMeta.label}` : "workout";
                    const yards      = sw.payload?.totalYards || sw.intent_params?.maxYards || 0;
                    const isMeters   = sw.payload?.poolMode === "25m" || sw.payload?.poolMode === "50m";
                    const unit       = isMeters ? "m" : "yds";
                    const borderClr  = done ? "var(--color-positive)" : (isIntent ? "var(--color-warn)" : "var(--color-border)");
                    // I Phase 2b — group fanout context. Look up group name
                    // from cached coach-targets (empty for non-coaches; old
                    // self-only rows have no group_id and skip rendering).
                    const groupId    = sw.intent_params?.group_id || sw.payload?.assignment_target?.group_id || null;
                    const groupName  = (() => {
                      if (!groupId) return null;
                      const g = coachTargets.find(t => t.id === groupId);
                      if (g) return g.name;
                      return sw.payload?.assignment_target?.group_name || "group";
                    })();
                    return (
                      <div key={sw.id} style={{ display: "flex", gap: 6, alignItems: "center", padding: "6px 8px", background: "var(--color-bg)", border: `1px solid ${borderClr}`, borderRadius: 5, marginBottom: 4, flexWrap: "wrap" }}>
                        <span style={{ color: done ? "var(--color-positive)" : (isIntent ? "var(--color-warn)" : "#cbd5e1"), fontSize: 13, fontWeight: 600, flex: 1, minWidth: 0 }}>
                          {done ? "✓ " : ""}{isIntent && !done && <span style={{ fontSize: 9, color: "var(--color-warn)", letterSpacing: "0.1em", marginRight: 6, textTransform: "uppercase" }}>Intent</span>}{typeLabel}
                          {groupName && (
                            <span title={`Coach-planned for group "${groupName}"`}
                              style={{ marginLeft: 6, fontSize: 10, color: "#86efac", background: "rgba(34,197,94,0.12)", border: "1px solid #16a34a", borderRadius: 4, padding: "1px 6px", fontWeight: 700, letterSpacing: "0.04em" }}>
                              → {groupName}
                            </span>
                          )}
                          {sw.facility_name && (
                            <span title={`At ${sw.facility_name}${sw.facility_course ? ` · ${poolModeLabel(sw.facility_course)}` : ""}`}
                              style={{ marginLeft: 6, fontSize: 10, color: "var(--color-primary)", background: "rgba(59,130,246,0.12)", border: "1px solid var(--color-primary)", borderRadius: 4, padding: "1px 6px", fontWeight: 700, letterSpacing: "0.04em" }}>
                              🏊 {sw.facility_name}
                            </span>
                          )}
                          <span style={{ color: "var(--color-text-dim)", fontWeight: 400, marginLeft: 6 }}>{yards.toLocaleString()} {unit}{isIntent && " (target)"}</span>
                          {isIntent && sw.notes && <span style={{ marginLeft: 8, fontSize: 11, color: "var(--color-text-muted)", fontStyle: "italic" }}>· {sw.notes}</span>}
                        </span>
                        {!done && isIntent && (
                          <>
                            <button onClick={() => handleGenerateFromIntent(sw)}
                              title="Generate a workout from this intent + preview it"
                              style={{ padding: "3px 10px", border: "1px solid var(--color-warn)", borderRadius: 5, background: "transparent", color: "var(--color-warn)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>▶ Generate</button>
                            <button onClick={() => setIntentForm({ date: sw.scheduled_date, intent: sw.intent_params, editingId: sw.id, facilityId: sw.facility_id })}
                              title="Edit this intent"
                              style={{ padding: "3px 10px", border: "1px solid var(--color-primary)", borderRadius: 5, background: "transparent", color: "var(--color-primary)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>✎ Edit intent</button>
                          </>
                        )}
                        {!done && !isIntent && (
                          <>
                            <button onClick={() => onRunScheduled && onRunScheduled(sw)}
                              title="Run this workout now (loads into Run mode)"
                              style={{ padding: "3px 10px", border: "1px solid #22c55e", borderRadius: 5, background: "transparent", color: "var(--color-positive)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>▶ Run</button>
                            <button onClick={() => onEditScheduled && onEditScheduled(sw)}
                              title="Open in the generator to edit / regenerate"
                              style={{ padding: "3px 10px", border: "1px solid var(--color-primary)", borderRadius: 5, background: "transparent", color: "var(--color-primary)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>✎ Edit</button>
                          </>
                        )}
                        {/* Reporting Phase A — practice attendance. Visible on
                            payload-mode rows (intent-mode rows need to be
                            generated first). Toggles between "Mark practice
                            done" and "Edit attendance" based on completed_at. */}
                        {!isIntent && (
                          <button onClick={() => setMarkingPractice(sw)}
                            title={practiceDone ? "Edit recorded attendance" : "Mark practice done + record attendance"}
                            style={{ padding: "3px 10px", border: `1px solid ${practiceDone ? "var(--color-positive)" : "var(--color-border-strong)"}`, borderRadius: 5, background: practiceDone ? "rgba(34,197,94,0.12)" : "transparent", color: practiceDone ? "var(--color-positive)" : "var(--color-text-muted)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                            {practiceDone ? "✓ Attendance" : "📋 Mark done"}
                          </button>
                        )}
                        <button onClick={() => setCopyTarget({ sw, date: dk })}
                          title="Copy this workout to another day"
                          style={{ padding: "3px 10px", border: "1px solid var(--color-border-strong)", borderRadius: 5, background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>🗐 Copy</button>
                        {!done && (
                          <button onClick={() => handleDelete(sw)}
                            title="Delete this scheduled workout"
                            style={{ padding: "3px 10px", border: "1px solid #ef4444", borderRadius: 5, background: "transparent", color: "var(--color-destructive)", fontSize: 11, cursor: "pointer" }}>🗑</button>
                        )}
                      </div>
                    );
                  })}
                </div>
              );
            })}
          </div>

          {/* Copy-to-day modal */}
          {copyTarget && (() => {
            const defaultDate = (() => {
              const d = new Date(copyTarget.date + "T00:00:00");
              d.setDate(d.getDate() + 1);
              return ymd(d);
            })();
            return (
              <div onClick={() => setCopyTarget(null)} className="modal-overlay" style={{ padding: 20 }}>
                <div onClick={(e) => e.stopPropagation()} style={{
                  background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
                  padding: 22, maxWidth: 420, width: "100%",
                }}>
                  <div style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>Copy workout</div>
                  <div style={{ color: "var(--color-text)", fontSize: 16, fontWeight: 700, marginBottom: 14 }}>
                    Copy "{copyTarget.sw.payload?.typeLabel || copyTarget.sw.payload?.type || "workout"}" to:
                  </div>
                  <input type="date" defaultValue={defaultDate} id="copy-target-date"
                    style={{ width: "100%", padding: "8px 10px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 14 }} />
                  <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
                    <button onClick={() => setCopyTarget(null)}
                      className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
                    <button onClick={() => {
                        const v = document.getElementById("copy-target-date")?.value;
                        if (v) handleCopy(copyTarget.sw, v);
                      }}
                      className="btn btn-lg btn-filled btn-primary">Copy ▸</button>
                  </div>
                </div>
              </div>
            );
          })()}

          {/* Phase 2a — IntentForm modal */}
          {intentForm && (
            <IntentForm
              initialDate={intentForm.date}
              initialIntent={intentForm.intent}
              editingId={intentForm.editingId}
              initialFacilityId={intentForm.facilityId}
              onClose={() => setIntentForm(null)}
              onSaved={() => { load(); }}
            />
          )}

          {/* Reporting Phase A — Mark Practice Done modal */}
          {markingPractice && (
            <MarkPracticeDoneModal
              sw={markingPractice}
              onClose={() => setMarkingPractice(null)}
              onSaved={() => { setMarkingPractice(null); load(); }}
            />
          )}

          {/* Phase 2a — IntentPreviewOverlay (Generate-from-intent flow) */}
          {intentPreview && (
            <IntentPreviewOverlay
              scheduledRow={intentPreview.sw}
              generatedWorkout={intentPreview.workout}
              onRegenerate={() => handleGenerateFromIntent(intentPreview.sw)}
              onSave={() => {
                // I Phase 2b — commit without entering Run mode. Refresh
                // week view so the row updates from intent → payload (and
                // shows ✓ done for group intents, which the log-workout
                // path linked + completed).
                setIntentPreview(null);
                load();
              }}
              onRun={(w) => {
                // Hand off to the parent's run-scheduled handler. The preview
                // overlay has already PATCHed the row's payload (self intent)
                // OR called /api/log-workout to create the snapshot + fanout +
                // link (group intent — I Phase 2b). Pass a synthesized sw so
                // onRunScheduled doesn't need to refetch.
                //
                // For group intents the schedule row is already linked to the
                // snapshot workout — null out completed_workout_id link
                // marker by tagging mode "payload_already_linked" so the
                // parent skips runScheduledId (avoids a second link attempt +
                // duplicate workout entry at finish-Run-mode time).
                const wasGroupIntent = !!(intentPreview.sw.intent_params && intentPreview.sw.intent_params.group_id);
                const swForRun = {
                  ...intentPreview.sw,
                  payload: w,
                  intent_params: null,
                  mode: "payload",
                  _alreadyLinked: wasGroupIntent,
                };
                setIntentPreview(null);
                load();
                if (onRunScheduled) onRunScheduled(swForRun);
              }}
              onClose={() => setIntentPreview(null)}
            />
          )}
        </div>
      );
    }

    function AssignedToMeView({ onRunAssignment }) {
      const [list,       setList]       = React.useState(null);
      const [stateFilter, setStateFilter] = React.useState("");                  // "" = all
      const [expandedId, setExpandedId] = React.useState(null);
      const [msg,        setMsg]        = React.useState(null);
      // Meet-anchored taper (cheap version). Fetched once on mount + refresh
      // when list reloads. Map of { group_id: anchor } so per-card lookup
      // by assignment.assigned_via_group_id is O(1).
      const [groupAnchors, setGroupAnchors] = React.useState({});
      // Phase 3 PSC slice 4 — swimmer's own active constraints for
      // computeSubstitutionsForSwimmer on each assigned workout. View-local
      // fetch (mirrors App-level myConstraints but scoped to this view's
      // lifecycle).
      const [myConstraints, setMyConstraints] = React.useState([]);

      const load = React.useCallback(async () => {
        try {
          const q = stateFilter ? `?state=${encodeURIComponent(stateFilter)}` : "";
          const [assignRes, anchorRes, constraintRes] = await Promise.all([
            fetch(`/api/me/assignments${q}`, { cache: "no-store" }),
            fetch(`/api/me/group-anchors`,   { cache: "no-store" }),
            fetch(`/api/me/constraints`,     { cache: "no-store" }),
          ]);
          if (!assignRes.ok) throw new Error(`HTTP ${assignRes.status}`);
          setList(await assignRes.json());
          if (anchorRes.ok) setGroupAnchors(await anchorRes.json());
          else setGroupAnchors({});
          if (constraintRes.ok) {
            const d = await constraintRes.json();
            setMyConstraints(Array.isArray(d?.constraints) ? d.constraints : []);
          } else { setMyConstraints([]); }
        } catch (e) { setList([]); setMsg(e.message); }
      }, [stateFilter]);
      React.useEffect(() => { load(); }, [load]);

      const markState = async (assignmentId, state, extra = {}) => {
        try {
          const res = await fetch(`/api/assignments/${assignmentId}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ completion_state: state, ...extra }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(e.message); }
      };

      const handleMarkComplete = (a) => {
        const diffStr = window.prompt(`Mark "${a.workout?.typeLabel || a.workout?.type}" complete. Difficulty 1-5 (optional, leave blank):`, "");
        if (diffStr === null) return;
        const extra = {};
        const d = parseInt(diffStr, 10);
        if (Number.isFinite(d) && d >= 1 && d <= 5) extra.difficulty = d;
        markState(a.id, "complete", extra);
      };
      const handleMarkPartial = (a) => {
        const note = window.prompt("Mark partial. Optional note (what you completed / why):", "");
        if (note === null) return;
        markState(a.id, "partial", note ? { focus_note: note.slice(0, 255) } : {});
      };
      const handleMarkMissed = (a) => {
        const note = window.prompt("Mark missed. Optional reason:", "");
        if (note === null) return;
        markState(a.id, "missed", note ? { focus_note: note.slice(0, 255) } : {});
      };

      const counts = list ? list.reduce((acc, a) => { acc[a.completion_state] = (acc[a.completion_state] || 0) + 1; return acc; }, {}) : {};

      return (
        <div>
          <h2 style={{ color: "var(--color-text)", marginTop: 0 }}>📥 Assigned to me</h2>

          <div style={{ display: "flex", gap: 6, marginBottom: 14, flexWrap: "wrap" }}>
            {[{ id: "", label: "All", n: list?.length || 0 }, ...Object.keys(COMPLETION_LABELS).map(k => ({ id: k, label: COMPLETION_LABELS[k].label, n: counts[k] || 0 }))].map(f => {
              const active = stateFilter === f.id;
              const color  = COMPLETION_LABELS[f.id]?.color || "var(--color-primary)";
              return (
                <button key={f.id || "all"} onClick={() => setStateFilter(f.id)}
                  className="pill"
                  style={{ border: `1px solid ${active ? color : "var(--color-border)"}`, background: active ? `${color}22` : "transparent", color: active ? color : "var(--color-text-muted)", fontSize: 12, cursor: "pointer" }}>
                  {f.label} ({f.n})
                </button>
              );
            })}
          </div>

          {msg && <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>{msg}</div>}

          {list === null ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13 }}>Loading…</div>
          ) : list.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13, fontStyle: "italic", padding: 20, textAlign: "center", border: "1px dashed var(--color-border)", borderRadius: 10 }}>
              No assigned workouts {stateFilter ? `in "${COMPLETION_LABELS[stateFilter]?.label}"` : "yet"}. When a coach generates a workout for a group you're in, it shows up here.
            </div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              {list.map(a => {
                const meta = COMPLETION_LABELS[a.completion_state] || COMPLETION_LABELS.not_started;
                const isExpanded = expandedId === a.id;
                const isCoachMarked = !!a.completed_by_coach_sub;
                // Meet-anchored taper countdown badge. Lookup the source
                // group's anchor (if any) from the pre-fetched map. Only
                // renders when an anchor exists for this card's group AND
                // the event is still in the future (weeks_out >= 0). Per
                // MEET_ANCHORED_TAPER_SCOPE §3.7. Cosmetic only.
                const anchor = groupAnchors[a.assigned_via_group_id] || groupAnchors[a.group_id];
                const showAnchorBadge = anchor && anchor.weeks_out != null && anchor.weeks_out >= 0;
                // Phase 3 PSC slice 4 — per-card substitution computation.
                // Substitutions are derived from the swimmer's OWN constraints
                // applied to the assigned workout. Coach-side workout content
                // doesn't carry sub annotations; the swimmer's view computes
                // them at render time.
                const subs = a.workout ? computeSubstitutionsForSwimmer(a.workout, myConstraints) : [];
                const subCount = subs.length;
                return (
                  <div key={a.id} style={{ background: "var(--color-card)", border: `1px solid ${meta.color}`, borderRadius: 10, overflow: "hidden" }}>
                    <button onClick={() => setExpandedId(isExpanded ? null : a.id)}
                      style={{ display: "block", width: "100%", textAlign: "left", padding: "12px 14px", background: "transparent", border: "none", color: "var(--color-text)", cursor: "pointer" }}>
                      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
                        <div>
                          <span style={{ fontSize: 14, fontWeight: 700 }}>
                            {isExpanded ? "▾" : "▸"} {a.workout?.typeLabel || a.workout?.type} · {a.workout?.totalYards} {a.workout?.poolMode === "25y" ? "yd" : "m"}
                          </span>
                          <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginTop: 3 }}>
                            From <strong>{a.coach_name || "(coach)"}</strong>
                            {a.group_name && <> · group <strong>{a.group_name}</strong></>}
                            {a.lane_label && <> · lane <strong>{a.lane_label}</strong>{a.lane_pace ? ` @ ${a.lane_pace}` : ""}</>}
                            · assigned {new Date(a.assigned_at).toLocaleDateString()}
                          </div>
                          {showAnchorBadge && (
                            <div style={{ fontSize: 11, color: "var(--color-warn)", marginTop: 4, fontWeight: 600 }}>
                              🎯 {anchor.weeks_out === 0 ? "This week" : `${anchor.weeks_out} wk${anchor.weeks_out === 1 ? "" : "s"} to`} <strong>{anchor.event_name}</strong> · suggested phase: <strong style={{ textTransform: "capitalize" }}>{anchor.suggested_phase}</strong>
                            </div>
                          )}
                          {subCount > 0 && (
                            <div style={{ fontSize: 11, color: "var(--color-warn)", marginTop: 4, fontWeight: 600 }}>
                              ⚠ {subCount} substitution{subCount === 1 ? "" : "s"} applied for your active constraint{myConstraints.length === 1 ? "" : "s"}
                            </div>
                          )}
                        </div>
                        <span style={{ fontSize: 10, padding: "3px 9px", borderRadius: 999, background: `${meta.color}22`, border: `1px solid ${meta.color}`, color: meta.color, fontWeight: 700 }}>
                          {meta.label}{isCoachMarked ? " (by coach)" : ""}
                        </span>
                      </div>
                    </button>
                    {isExpanded && (
                      <div style={{ padding: "0 14px 14px", borderTop: "1px solid var(--color-border)" }}>
                        {subCount > 0 && (
                          <div style={{ marginTop: 10, padding: 10, background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 6, fontSize: 11 }}>
                            <div style={{ color: "var(--color-warn)", fontWeight: 700, marginBottom: 6 }}>
                              ⚠ What was substituted ({subCount})
                            </div>
                            <div style={{ color: "var(--color-text-muted)", marginBottom: 6 }}>
                              These changes apply to YOUR copy because of your active constraints. Other swimmers in the group get the original.
                            </div>
                            <ul style={{ margin: 0, paddingLeft: 18, color: "var(--color-text)", lineHeight: 1.6 }}>
                              {subs.map((s, i) => {
                                const block = a.workout.blocks?.[s.block_idx];
                                const sectName = block?.section || "section";
                                if (s.kind === "stroke_sub") {
                                  return (
                                    <li key={i}>
                                      <em>{sectName}</em> set {s.set_idx + 1}: swap <strong>{s.from}</strong> → <strong>{s.to}</strong>
                                      <span style={{ color: "var(--color-text-muted)" }}> · {PSC_LABEL_MAP[s.constraint_type] || s.constraint_type}</span>
                                    </li>
                                  );
                                }
                                if (s.kind === "equip_drop") {
                                  return (
                                    <li key={i}>
                                      <em>{sectName}</em> set {s.set_idx + 1}: drop <strong>{s.eq}</strong> gear, swim unassisted
                                      <span style={{ color: "var(--color-text-muted)" }}> · {PSC_LABEL_MAP[s.constraint_type] || s.constraint_type}</span>
                                    </li>
                                  );
                                }
                                if (s.kind === "section_skip") {
                                  return (
                                    <li key={i}>
                                      Skip <em>{s.section}</em> — rest or own choice instead
                                      <span style={{ color: "var(--color-text-muted)" }}> · {PSC_LABEL_MAP[s.constraint_type] || s.constraint_type}</span>
                                    </li>
                                  );
                                }
                                if (s.kind === "cap_yardage") {
                                  return (
                                    <li key={i}>
                                      Yardage cap <strong>{s.value} yd</strong> — trim from the end if the workout runs long
                                    </li>
                                  );
                                }
                                if (s.kind === "cap_intensity") {
                                  return (
                                    <li key={i}>
                                      Intensity cap <strong>{s.value}</strong> — main set replaced with easy-pace equivalent volume
                                    </li>
                                  );
                                }
                                return null;
                              })}
                            </ul>
                          </div>
                        )}
                        {a.workout?.blocks && (
                          <div style={{ marginTop: 10 }}>
                            {a.workout.blocks.map((b, i) => b && b.kind === "dryland" ? (
                              <DrylandBlock key={i} block={b} />
                            ) : (
                              <WorkoutBlock key={i} block={b} equipment={a.workout.equipment || {}}
                                recentMainLabels={new Map()} poolMode={a.workout.poolMode || "25y"} />
                            ))}
                          </div>
                        )}
                        <div style={{ display: "flex", gap: 6, marginTop: 12, flexWrap: "wrap" }}>
                          {a.completion_state !== "complete" && (
                            <button onClick={() => { if (onRunAssignment) onRunAssignment(a); }}
                              style={{ padding: "7px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                              ▶ Run this workout
                            </button>
                          )}
                          <button onClick={() => handleMarkComplete(a)}
                            style={{ padding: "7px 12px", background: "transparent", color: "var(--color-positive)", border: "1px solid #22c55e", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>
                            ✓ Mark complete
                          </button>
                          <button onClick={() => handleMarkPartial(a)}
                            style={{ padding: "7px 12px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>
                            ◐ Mark partial
                          </button>
                          <button onClick={() => handleMarkMissed(a)}
                            style={{ padding: "7px 12px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>
                            ✕ Mark missed
                          </button>
                        </div>
                        {a.focus_note && (
                          <div style={{ marginTop: 10, padding: 8, background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 11, color: "#cbd5e1" }}>
                            <span style={{ color: "var(--color-text-dim)" }}>Note:</span> {a.focus_note}
                          </div>
                        )}
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          )}
        </div>
      );
    }

    // GroupAssignmentsPanel — coach-mark-on-behalf (Flow N, R-G). Lists
    // recent assignments fanned out from this group + lets the coach mark
    // any swimmer's completion state from the deck. Sets completed_by_coach_sub
    // so the swimmer sees "marked by coach" on their side.
    function GroupAssignmentsPanel({ groupId, isGroupCoach }) {
      const [list, setList] = React.useState(null);
      const [err,  setErr]  = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/groups/${groupId}/assignments`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setList(await res.json());
        } catch (e) { setErr(e.message); setList([]); }
      }, [groupId]);
      React.useEffect(() => { if (isGroupCoach) load(); }, [isGroupCoach, load]);

      const markState = async (assignmentId, state) => {
        try {
          const res = await fetch(`/api/assignments/${assignmentId}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ completion_state: state }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      if (!isGroupCoach) return null;

      return (
        <div style={{ marginTop: 14 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
            <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
              Recent assignments ({list ? list.length : "—"})
            </div>
          </div>
          {err && <div style={{ color: "#fca5a5", fontSize: 11, marginBottom: 6 }}>{err}</div>}
          {!list ? (
            <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>Loading…</div>
          ) : list.length === 0 ? (
            <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>No assignments yet. Generate a workout for this group to start the loop.</div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 4, maxHeight: 280, overflow: "auto" }}>
              {list.map(a => {
                const meta = COMPLETION_LABELS[a.completion_state] || COMPLETION_LABELS.not_started;
                const isCoachMarked = !!a.completed_by_coach_sub;
                return (
                  <div key={a.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "5px 9px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 4, fontSize: 11 }}>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ color: "#cbd5e1", fontWeight: 600, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                        {a.target_name || "(unknown)"} · {a.workout_type} {a.workout_total_yards}
                        {a.lane_label && <span style={{ color: "var(--color-text-dim)" }}> · {a.lane_label}</span>}
                      </div>
                      <div style={{ color: "var(--color-text-dim)", fontSize: 10 }}>
                        {new Date(a.assigned_at).toLocaleDateString()} · <span style={{ color: meta.color, fontWeight: 700 }}>{meta.label}</span>
                        {isCoachMarked && <span style={{ color: "var(--color-warn)" }}> (by coach)</span>}
                      </div>
                    </div>
                    <div style={{ display: "flex", gap: 3 }}>
                      {a.completion_state !== "complete" && <button onClick={() => markState(a.id, "complete")} title="Mark complete" style={{ padding: "2px 7px", background: "transparent", color: "var(--color-positive)", border: "1px solid #22c55e", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>✓</button>}
                      {a.completion_state !== "partial"  && <button onClick={() => markState(a.id, "partial")}  title="Mark partial"  style={{ padding: "2px 7px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>◐</button>}
                      {a.completion_state !== "missed"   && <button onClick={() => markState(a.id, "missed")}   title="Mark missed"   style={{ padding: "2px 7px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>✕</button>}
                    </div>
                  </div>
                );
              })}
            </div>
          )}
        </div>
      );
    }

    // JoinTokensPanel — coach-side invite-code issuer + outstanding list.
    // Inside GroupRow expanded view (R-F). Primary coach can issue + revoke;
    // any group coach can read.
    function JoinTokensPanel({ groupId, isPrimary, isGroupCoach }) {
      const [tokens, setTokens]      = React.useState(null);
      const [issuing, setIssuing]    = React.useState(false);
      const [issueRole, setIssueRole] = React.useState("primary");
      const [latestIssued, setLatestIssued] = React.useState(null);              // {token, expires_at, intended_role}
      const [copied, setCopied]      = React.useState(false);
      const [err, setErr]            = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/groups/${groupId}/join-tokens`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setTokens(await res.json());
        } catch (e) { setErr(e.message); setTokens([]); }
      }, [groupId]);
      React.useEffect(() => { if (isGroupCoach) load(); }, [isGroupCoach, load]);

      const issue = async () => {
        try {
          const res = await fetch(`/api/groups/${groupId}/join-tokens`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ intended_role: issueRole }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setLatestIssued({ token: j.token, expires_at: j.expires_at, intended_role: issueRole });
          setIssuing(false);
          setCopied(false);
          await load();
        } catch (e) { setErr(e.message); }
      };

      const revoke = async (token) => {
        if (!window.confirm("Revoke this code? Anyone who hasn't redeemed it yet will get an 'invalid token' error.")) return;
        try {
          const res = await fetch(`/api/join-tokens/${token}`, { method: "DELETE", headers: { ...csrfHeaders() } });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          if (latestIssued?.token === token) setLatestIssued(null);
          await load();
        } catch (e) { setErr(e.message); }
      };

      const copyToClipboard = (token) => {
        try {
          navigator.clipboard.writeText(token);
          setCopied(true);
          setTimeout(() => setCopied(false), 2000);
        } catch (_) {}
      };

      if (!isGroupCoach) return null;

      return (
        <div style={{ marginTop: 14 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
            <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
              Invite full-account swimmers
            </div>
            {isPrimary && !issuing && !latestIssued && (
              <button onClick={() => { setIssuing(true); setErr(null); }}
                style={{ padding: "3px 9px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                + Issue join code
              </button>
            )}
          </div>
          {err && <div style={{ color: "#fca5a5", fontSize: 11, marginBottom: 6 }}>{err}</div>}

          {issuing && (
            <div style={{ padding: 8, background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 5, marginBottom: 6 }}>
              <div style={{ display: "flex", gap: 6, alignItems: "center", marginBottom: 6 }}>
                <label style={{ fontSize: 11, color: "var(--color-text-muted)" }}>Role on join:</label>
                <select value={issueRole} onChange={e => setIssueRole(e.target.value)}
                  style={{ padding: "4px 8px", fontSize: 11, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 4 }}>
                  <option value="primary">primary (main training group)</option>
                  <option value="secondary">secondary (dryland / additional)</option>
                </select>
              </div>
              <div style={{ display: "flex", gap: 6 }}>
                <button onClick={issue}
                  style={{ padding: "4px 10px", background: "var(--color-warn)", color: "var(--color-bg)", border: "none", borderRadius: 4, fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                  Issue code
                </button>
                <button onClick={() => setIssuing(false)}
                  style={{ padding: "4px 10px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 4, fontSize: 11, cursor: "pointer" }}>
                  Cancel
                </button>
              </div>
            </div>
          )}

          {latestIssued && (
            <div style={{ padding: 10, background: "var(--color-bg)", border: "1px solid #22c55e", borderRadius: 5, marginBottom: 6 }}>
              <div style={{ fontSize: 11, color: "var(--color-positive)", marginBottom: 6 }}>
                ✓ Code created. Send this to the swimmer out-of-band (text, email, in person):
              </div>
              <div style={{ display: "flex", gap: 6, alignItems: "center", marginBottom: 6 }}>
                <code style={{ flex: 1, fontFamily: "monospace", fontSize: 16, fontWeight: 700, padding: "6px 10px", background: "var(--color-card)", color: "var(--color-warn)", border: "1px solid var(--color-border-strong)", borderRadius: 4, letterSpacing: "0.1em", textAlign: "center" }}>
                  {latestIssued.token}
                </code>
                <button onClick={() => copyToClipboard(latestIssued.token)}
                  style={{ padding: "5px 11px", background: copied ? "var(--color-positive)" : "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 4, fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                  {copied ? "Copied!" : "Copy"}
                </button>
              </div>
              <div style={{ fontSize: 10, color: "var(--color-text-dim)" }}>
                Role on join: <strong>{latestIssued.intended_role}</strong> · Expires {latestIssued.expires_at ? new Date(latestIssued.expires_at).toLocaleDateString() : "in 30 days"}
              </div>
              <button onClick={() => setLatestIssued(null)}
                style={{ marginTop: 6, padding: "3px 8px", background: "transparent", color: "var(--color-text-muted)", border: "none", borderRadius: 4, fontSize: 10, cursor: "pointer", textDecoration: "underline" }}>
                Dismiss
              </button>
            </div>
          )}

          {/* Outstanding tokens (unredeemed, unexpired) */}
          {tokens && tokens.length > 0 && (
            <div style={{ marginTop: 4 }}>
              <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginBottom: 4 }}>Outstanding codes (unredeemed):</div>
              {tokens.map(t => (
                <div key={t.token} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "4px 8px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 4, marginBottom: 3, fontSize: 11 }}>
                  <span>
                    <code style={{ fontFamily: "monospace", color: "var(--color-warn)", fontSize: 11 }}>{t.token}</code>
                    <span style={{ marginLeft: 8, color: "var(--color-text-muted)" }}>· {t.intended_role}</span>
                    <span style={{ marginLeft: 8, color: "var(--color-text-dim)", fontSize: 10 }}>expires {new Date(t.expires_at).toLocaleDateString()}</span>
                  </span>
                  {isPrimary && (
                    <button onClick={() => revoke(t.token)}
                      style={{ padding: "2px 7px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>Revoke</button>
                  )}
                </div>
              ))}
            </div>
          )}
        </div>
      );
    }

    // LanePlansPanel — inside GroupRow, renders the list of lane plans for
    // a group plus an in-place editor. Hidden for solo groups per decision
    // #17 (groups with member_count < 2). Read by any group coach; write by
    // group primary coach only. Plan structure JSON per scope §7.
    function LanePlansPanel({ groupId, members, isPrimary, onChanged }) {
      const [plans,      setPlans]      = React.useState(null);
      const [editingId,  setEditingId]  = React.useState(null);                 // plan id being edited, or "new"
      const [draft,      setDraft]      = React.useState(null);                 // { name, is_default, plan_data }
      const [err,        setErr]        = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/groups/${groupId}/lane-plans`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setPlans(await res.json());
        } catch (e) { setErr(e.message); setPlans([]); }
      }, [groupId]);
      React.useEffect(() => { load(); }, [load]);

      const startNew = () => {
        setEditingId("new");
        setDraft({ name: "", is_default: false, plan_data: { lanes: [{ idx: 0, label: "Lane 1", target_pace_100: "", members: [] }] } });
      };
      const startEdit = (p) => {
        setEditingId(p.id);
        setDraft({ name: p.name, is_default: p.is_default, plan_data: JSON.parse(JSON.stringify(p.plan_data || { lanes: [] })) });
      };
      const cancel = () => { setEditingId(null); setDraft(null); setErr(null); };

      const save = async () => {
        if (!draft.name.trim()) { setErr("Plan name required"); return; }
        try {
          const url    = editingId === "new" ? `/api/groups/${groupId}/lane-plans` : `/api/lane-plans/${editingId}`;
          const method = editingId === "new" ? "POST" : "PATCH";
          const body   = editingId === "new"
            ? JSON.stringify({ name: draft.name.trim(), is_default: !!draft.is_default, plan_data: draft.plan_data })
            : JSON.stringify({ name: draft.name.trim(), plan_data: draft.plan_data });
          const res = await fetch(url, { method, headers: { "Content-Type": "application/json", ...csrfHeaders() }, body });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || j.reason || `HTTP ${res.status}`);
          // is_default is set via separate endpoint on edit (not part of PATCH body).
          if (editingId !== "new" && draft.is_default) {
            await fetch(`/api/lane-plans/${editingId}/default`, { method: "POST", headers: { ...csrfHeaders() } });
          }
          cancel();
          await load();
          if (onChanged) onChanged();
        } catch (e) { setErr(e.message); }
      };

      const setDefault = async (planId) => {
        try {
          const res = await fetch(`/api/lane-plans/${planId}/default`, { method: "POST", headers: { ...csrfHeaders() } });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
          if (onChanged) onChanged();
        } catch (e) { setErr(e.message); }
      };

      const archivePlan = async (planId, name) => {
        if (!window.confirm(`Archive plan "${name}"?`)) return;
        try {
          const res = await fetch(`/api/lane-plans/${planId}/archive`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ archived: true }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
          if (onChanged) onChanged();
        } catch (e) { setErr(e.message); }
      };

      // ── Draft mutation helpers (operate on draft.plan_data.lanes) ────
      const updateDraftLane = (idx, patch) => {
        setDraft(d => ({ ...d, plan_data: { ...d.plan_data, lanes: d.plan_data.lanes.map((l, i) => i === idx ? { ...l, ...patch } : l) } }));
      };
      const addLane = () => {
        setDraft(d => {
          const nextIdx = d.plan_data.lanes.length;
          return { ...d, plan_data: { ...d.plan_data, lanes: [...d.plan_data.lanes, { idx: nextIdx, label: `Lane ${nextIdx + 1}`, target_pace_100: "", members: [] }] } };
        });
      };
      const removeLane = (idx) => {
        setDraft(d => ({ ...d, plan_data: { ...d.plan_data, lanes: d.plan_data.lanes.filter((_, i) => i !== idx).map((l, i) => ({ ...l, idx: i })) } }));
      };
      const addMemberToLane = (laneIdx, managedId) => {
        if (!managedId) return;
        setDraft(d => ({ ...d, plan_data: { ...d.plan_data, lanes: d.plan_data.lanes.map((l, i) => i === laneIdx ? { ...l, members: [...l.members, { managed_id: managedId }] } : l) } }));
      };
      const removeMemberFromLane = (laneIdx, mIdx) => {
        setDraft(d => ({ ...d, plan_data: { ...d.plan_data, lanes: d.plan_data.lanes.map((l, i) => i === laneIdx ? { ...l, members: l.members.filter((_, j) => j !== mIdx) } : l) } }));
      };

      // Members already placed in any lane (across the draft) — for the
      // add-member picker to exclude them.
      const placedManagedIds = new Set((draft?.plan_data?.lanes || []).flatMap(l => l.members.map(m => m.managed_id).filter(Boolean)));
      const memberOptions    = (members || []).filter(m => m.member_managed_id && !placedManagedIds.has(m.member_managed_id));
      const memberName       = (managedId) => (members || []).find(m => m.member_managed_id === managedId)?.display_name || "(unknown)";

      return (
        <div style={{ marginTop: 14 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
            <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
              Lane plans ({plans ? plans.length : "—"})
            </div>
            {isPrimary && !editingId && (
              <button onClick={startNew}
                style={{ padding: "3px 9px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                + New plan
              </button>
            )}
          </div>
          {err && <div style={{ color: "#fca5a5", fontSize: 11, marginBottom: 6 }}>{err}</div>}

          {/* Existing plans list (read mode) */}
          {!editingId && plans && plans.length === 0 && (
            <div style={{ fontSize: 11, color: "var(--color-text-dim)", fontStyle: "italic" }}>No lane plans yet. Coach generates use stored paces.</div>
          )}
          {!editingId && plans && plans.map(p => (
            <div key={p.id} style={{ padding: "6px 10px", background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 5, marginBottom: 5, fontSize: 12 }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
                <span style={{ color: "#cbd5e1", fontWeight: 700 }}>
                  {p.name}
                  {p.is_default && <span style={{ marginLeft: 6, fontSize: 9, padding: "1px 5px", borderRadius: 3, background: "#f59e0b22", border: "1px solid var(--color-warn)", color: "var(--color-warn)", fontWeight: 700 }}>default</span>}
                  <span style={{ marginLeft: 6, color: "var(--color-text-dim)", fontSize: 10 }}>· {(p.plan_data?.lanes || []).length} lanes · {(p.plan_data?.lanes || []).reduce((n, l) => n + (l.members?.length || 0), 0)} swimmers</span>
                </span>
                {isPrimary && (
                  <div style={{ display: "flex", gap: 4 }}>
                    {!p.is_default && <button onClick={() => setDefault(p.id)} style={{ padding: "2px 7px", background: "transparent", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>Set default</button>}
                    <button onClick={() => startEdit(p)} style={{ padding: "2px 7px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-primary)", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>Edit</button>
                    <button onClick={() => archivePlan(p.id, p.name)} style={{ padding: "2px 7px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>Archive</button>
                  </div>
                )}
              </div>
            </div>
          ))}

          {/* Editor (create or edit) */}
          {editingId && draft && (
            <div style={{ padding: 10, background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 5, marginBottom: 6 }}>
              <div style={{ display: "flex", gap: 6, marginBottom: 8, alignItems: "center" }}>
                <input value={draft.name} onChange={e => setDraft(d => ({ ...d, name: e.target.value }))} placeholder="Plan name (e.g. Tuesday config)"
                  style={{ flex: 1, padding: "5px 9px", fontSize: 12, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 4 }} />
                <label style={{ display: "flex", alignItems: "center", gap: 4, fontSize: 11, color: "#cbd5e1", cursor: "pointer" }}>
                  <input type="checkbox" checked={!!draft.is_default} onChange={e => setDraft(d => ({ ...d, is_default: e.target.checked }))} />
                  Default
                </label>
              </div>
              {draft.plan_data.lanes.map((lane, lIdx) => (
                <div key={lIdx} className="card" style={{ padding: 8, borderRadius: 4, marginBottom: 6 }}>
                  <div style={{ display: "flex", gap: 6, marginBottom: 6 }}>
                    <span style={{ fontSize: 10, color: "var(--color-text-dim)", padding: "5px 0" }}>#{lane.idx + 1}</span>
                    <input value={lane.label} onChange={e => updateDraftLane(lIdx, { label: e.target.value })} placeholder="Lane label" maxLength={40}
                      style={{ flex: 1, padding: "4px 8px", fontSize: 11, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 3 }} />
                    <input value={lane.target_pace_100 || ""} onChange={e => updateDraftLane(lIdx, { target_pace_100: e.target.value })} placeholder="pace e.g. 2:00" maxLength={8}
                      style={{ width: 80, padding: "4px 8px", fontSize: 11, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 3 }} />
                    <button onClick={() => removeLane(lIdx)} title="Remove lane"
                      style={{ padding: "3px 8px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 3, fontSize: 10, cursor: "pointer" }}>×</button>
                  </div>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 4, marginBottom: 6 }}>
                    {lane.members.map((m, mIdx) => (
                      <span key={mIdx} style={{ display: "inline-flex", alignItems: "center", gap: 4, padding: "2px 7px", background: "var(--color-bg)", border: "1px solid var(--color-border-strong)", borderRadius: 999, fontSize: 10, color: "#cbd5e1" }}>
                        {memberName(m.managed_id)}
                        <button onClick={() => removeMemberFromLane(lIdx, mIdx)} style={{ background: "transparent", border: "none", color: "var(--color-destructive)", cursor: "pointer", padding: 0, fontSize: 12, lineHeight: 1 }}>×</button>
                      </span>
                    ))}
                    {lane.members.length === 0 && <span style={{ fontSize: 10, color: "var(--color-text-dim)", fontStyle: "italic" }}>No swimmers yet</span>}
                  </div>
                  <select value="" onChange={e => { addMemberToLane(lIdx, e.target.value); e.target.value = ""; }}
                    style={{ width: "100%", padding: "4px 8px", fontSize: 11, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 3 }}>
                    <option value="">+ Add swimmer to this lane</option>
                    {memberOptions.map(m => (
                      <option key={m.member_managed_id} value={m.member_managed_id}>{m.display_name} ({m.age}y)</option>
                    ))}
                  </select>
                </div>
              ))}
              <button onClick={addLane}
                style={{ padding: "4px 10px", background: "transparent", color: "var(--color-warn)", border: "1px dashed var(--color-warn)", borderRadius: 4, fontSize: 11, cursor: "pointer", marginBottom: 8 }}>
                + Add lane
              </button>
              <div style={{ display: "flex", gap: 6 }}>
                <button onClick={save}
                  style={{ padding: "5px 11px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 4, fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                  Save plan
                </button>
                <button onClick={cancel}
                  style={{ padding: "5px 11px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 4, fontSize: 11, cursor: "pointer" }}>
                  Cancel
                </button>
              </div>
            </div>
          )}
        </div>
      );
    }

    // GroupRow — expandable component for a group within TeamsView's team
    // detail (R-C). Shows header (name, phase, member count) and expands to
    // members + coaches + phase setter + visibility/archive controls.
    const PHASE_OPTIONS = [
      { v: "",         label: "—", color: "var(--color-text-dim)" },
      { v: "base",     label: "Base 🌱",     color: "var(--color-positive)" },
      { v: "build",    label: "Build 🔨",    color: "var(--color-primary)" },
      { v: "peak",     label: "Peak ⛰️",     color: "var(--color-warn)" },
      { v: "taper",    label: "Taper 📉",    color: "var(--color-warn)" },
      { v: "recovery", label: "Recovery 🌿", color: "var(--color-primary)" },
    ];
    function phaseOption(v) {
      return PHASE_OPTIONS.find(p => p.v === (v || "")) || PHASE_OPTIONS[0];
    }

    function GroupRow({ group, coachPool, managedInTeam, onChanged }) {
      const [expanded, setExpanded] = React.useState(false);
      const [detail,   setDetail]   = React.useState(null);
      const [addMemberId, setAddMemberId] = React.useState("");
      const [addMemberRole, setAddMemberRole] = React.useState("primary");
      const [addCoachSub, setAddCoachSub] = React.useState("");
      const [err, setErr] = React.useState(null);

      const load = React.useCallback(async () => {
        if (!group?.id) return;
        try {
          const res = await fetch(`/api/groups/${group.id}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setDetail(await res.json());
          setErr(null);
        } catch (e) { setErr(e.message); }
      }, [group?.id]);
      React.useEffect(() => { if (expanded) load(); }, [expanded, load]);

      const isPrimary = detail?.viewer_role === "primary";
      const phase = phaseOption(group.current_phase);

      const setPhase = async (v) => {
        try {
          const res = await fetch(`/api/groups/${group.id}/phase`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ phase: v || null }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          if (onChanged) onChanged();                                          // refresh parent list (header reflects phase)
          await load();
        } catch (e) { setErr(e.message); }
      };

      const toggleVisibility = async () => {
        try {
          const res = await fetch(`/api/groups/${group.id}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ roster_visible_to_members: !detail.roster_visible_to_members }),
          });
          const j = await res.json();
          if (!res.ok) {
            // Decision #28: server returns reason "minors_present_visibility_locked_off"
            if (j.reason === "minors_present_visibility_locked_off") {
              throw new Error("Cannot enable roster visibility — group has under-18 members. Privacy gate is enforced.");
            }
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      const archive = async () => {
        if (!window.confirm(`Archive group "${group.name}"? Members will be stamped as left at archive time.`)) return;
        try {
          const res = await fetch(`/api/groups/${group.id}/archive`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ archived: true }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setExpanded(false);
          if (onChanged) onChanged();
        } catch (e) { setErr(e.message); }
      };

      const addMember = async () => {
        if (!addMemberId) { setErr("Pick a swimmer to add"); return; }
        try {
          const res = await fetch(`/api/groups/${group.id}/members`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ managed_id: addMemberId, role: addMemberRole }),
          });
          const j = await res.json();
          if (!res.ok) {
            // Decision #33: surface the conflict-group name if returned
            if (j.reason === "primary_in_other_group_same_coach") {
              throw new Error(`Already a primary in another of your groups: ${j.conflict_group_name || j.conflict_group_id}. Either change their role here to 'secondary' or remove them from the other group first.`);
            }
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setAddMemberId("");
          setErr(null);
          await load();
        } catch (e) { setErr(e.message); }
      };

      const removeMember = async (memberId, name) => {
        const reason = window.prompt(`Remove ${name} from ${group.name}? (Optional: enter a reason)`, "");
        if (reason === null) return;                                            // cancel
        try {
          const res = await fetch(`/api/groups/${group.id}/members/${memberId}`, {
            method:  "DELETE",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ reason: reason || null }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      const addCoach = async () => {
        if (!addCoachSub) return;
        try {
          const res = await fetch(`/api/groups/${group.id}/coaches`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ coach_sub: addCoachSub, role: "assistant" }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          setAddCoachSub("");
          await load();
        } catch (e) { setErr(e.message); }
      };

      const removeCoach = async (sub) => {
        if (!window.confirm("Remove this coach from the group?")) return;
        try {
          const res = await fetch(`/api/groups/${group.id}/coaches/${encodeURIComponent(sub)}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setErr(e.message); }
      };

      // Available managed swimmers for the add-member picker = team's managed
      // swimmers minus those already active in this group.
      const activeMemberIds = new Set((detail?.members || []).map(m => m.member_managed_id).filter(Boolean));
      const candidates      = (managedInTeam || []).filter(s => !activeMemberIds.has(s.id));
      const activeCoachSubs = new Set((detail?.coaches || []).map(c => c.coach_sub));
      const coachCandidates = (coachPool || []).filter(c => !activeCoachSubs.has(c.sub));

      return (
        <div style={{ background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 6 }}>
          <button onClick={() => setExpanded(e => !e)}
            style={{ display: "block", width: "100%", textAlign: "left", padding: "10px 12px", background: "transparent", border: "none", color: "var(--color-text)", cursor: "pointer" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8 }}>
              <div>
                <span style={{ fontSize: 13, fontWeight: 700 }}>
                  {expanded ? "▾" : "▸"} {group.name}
                  {group.archived && <span style={{ marginLeft: 6, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#64748b22", border: "1px solid #64748b", color: "var(--color-text-muted)" }}>archived</span>}
                </span>
                {group.pool_mode_default && <span style={{ marginLeft: 8, fontSize: 10, color: "var(--color-text-muted)" }}>· {group.pool_mode_default}</span>}
              </div>
              <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                {group.current_phase && (
                  <span title={`Phase set ${group.phase_set_at ? new Date(group.phase_set_at).toLocaleDateString() : ""}`}
                    style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: `${phase.color}22`, border: `1px solid ${phase.color}`, color: phase.color, fontWeight: 700 }}>
                    {phase.label}
                  </span>
                )}
              </div>
            </div>
          </button>
          {expanded && (
            <div style={{ padding: "0 14px 14px", borderTop: "1px solid var(--color-border)" }}>
              {err && <div style={{ color: "#fca5a5", fontSize: 11, padding: "8px 0" }}>{err}</div>}
              {!detail ? (
                <div style={{ color: "var(--color-text-dim)", fontSize: 12, padding: "10px 0" }}>Loading…</div>
              ) : (
                <>
                  {/* Members */}
                  <div style={{ marginTop: 10 }}>
                    <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                      Members ({(detail.members || []).length})
                    </div>
                    {(detail.members || []).length === 0 && <div style={{ fontSize: 12, color: "var(--color-text-dim)", fontStyle: "italic" }}>No members yet.</div>}
                    {(detail.members || []).map(m => (
                      <div key={m.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "5px 0", fontSize: 12 }}>
                        <span style={{ color: "#cbd5e1" }}>
                          {m.display_name}
                          <span style={{ marginLeft: 6, color: "var(--color-text-dim)" }}>· age {m.age}</span>
                          <span style={{ marginLeft: 6, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: m.role === "primary" ? "#22c55e22" : "#33415522", border: `1px solid ${m.role === "primary" ? "var(--color-positive)" : "var(--color-border-strong)"}`, color: m.role === "primary" ? "var(--color-positive)" : "var(--color-text-muted)", fontWeight: 700 }}>
                            {m.role}
                          </span>
                          {m.is_coppa_protected && <span style={{ marginLeft: 4, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#dc262622", border: "1px solid #dc2626", color: "#dc2626", fontWeight: 700 }}>under 13</span>}
                          {m.is_minor && !m.is_coppa_protected && <span style={{ marginLeft: 4, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#f59e0b22", border: "1px solid var(--color-warn)", color: "var(--color-warn)", fontWeight: 700 }}>minor</span>}
                        </span>
                        {detail.viewer_role && (
                          <button onClick={() => removeMember(m.id, m.display_name)}
                            style={{ padding: "2px 8px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>
                            Remove
                          </button>
                        )}
                      </div>
                    ))}
                    {detail.viewer_role && candidates.length > 0 && (
                      <div style={{ marginTop: 8, display: "flex", gap: 6, alignItems: "center" }}>
                        <select value={addMemberId} onChange={e => setAddMemberId(e.target.value)}
                          style={{ flex: 1, padding: "5px 9px", fontSize: 12, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                          <option value="">— pick a swimmer —</option>
                          {candidates.map(s => (
                            <option key={s.id} value={s.id}>{s.display_name} ({s.age}y)</option>
                          ))}
                        </select>
                        <select value={addMemberRole} onChange={e => setAddMemberRole(e.target.value)}
                          style={{ padding: "5px 9px", fontSize: 12, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                          <option value="primary">primary</option>
                          <option value="secondary">secondary</option>
                        </select>
                        <button onClick={addMember} disabled={!addMemberId}
                          style={{ padding: "5px 11px", background: addMemberId ? "var(--color-warn)" : "var(--color-border-strong)", color: addMemberId ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: addMemberId ? "pointer" : "not-allowed" }}>
                          Add
                        </button>
                      </div>
                    )}
                    {detail.viewer_role && candidates.length === 0 && (managedInTeam || []).length === 0 && (
                      <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 6, fontStyle: "italic" }}>
                        No team-affiliated managed swimmers available. Add some via 🏊 Managed swimmers.
                      </div>
                    )}
                  </div>

                  {/* Group coaches */}
                  <div style={{ marginTop: 14 }}>
                    <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                      Coaches ({(detail.coaches || []).length})
                    </div>
                    {(detail.coaches || []).map(c => (
                      <div key={c.coach_sub} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "4px 0", fontSize: 12 }}>
                        <span style={{ color: "#cbd5e1" }}>
                          {c.display_name || "(no name)"}
                          <span style={{ marginLeft: 6, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: c.role === "primary" ? "#3b82f622" : "#33415522", border: `1px solid ${c.role === "primary" ? "var(--color-primary)" : "var(--color-border-strong)"}`, color: c.role === "primary" ? "var(--color-primary)" : "var(--color-text-muted)", fontWeight: 700 }}>{c.role}</span>
                        </span>
                        {isPrimary && c.role !== "primary" && (
                          <button onClick={() => removeCoach(c.coach_sub)}
                            style={{ padding: "2px 8px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>
                            Remove
                          </button>
                        )}
                      </div>
                    ))}
                    {isPrimary && coachCandidates.length > 0 && (
                      <div style={{ marginTop: 6, display: "flex", gap: 6 }}>
                        <select value={addCoachSub} onChange={e => setAddCoachSub(e.target.value)}
                          style={{ flex: 1, padding: "5px 9px", fontSize: 12, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                          <option value="">— add an assistant coach —</option>
                          {coachCandidates.map(c => (
                            <option key={c.sub} value={c.sub}>{c.display_name || "(no name)"}{c.initials ? ` (${c.initials})` : ""} · …{c.sub.slice(-6)}</option>
                          ))}
                        </select>
                        <button onClick={addCoach} disabled={!addCoachSub}
                          style={{ padding: "5px 11px", background: addCoachSub ? "var(--color-primary)" : "var(--color-border-strong)", color: addCoachSub ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 5, fontSize: 12, fontWeight: 700, cursor: addCoachSub ? "pointer" : "not-allowed" }}>
                          Add
                        </button>
                      </div>
                    )}
                  </div>

                  {/* Phase + settings (primary only) */}
                  {isPrimary && (
                    <div style={{ marginTop: 14 }}>
                      <div style={{ fontSize: 11, color: "var(--color-text-muted)", fontWeight: 700, marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                        Training phase
                      </div>
                      <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
                        {PHASE_OPTIONS.map(p => {
                          const active = (group.current_phase || "") === p.v;
                          return (
                            <button key={p.v} onClick={() => setPhase(p.v)}
                              style={{ padding: "4px 10px", borderRadius: 999, border: `1px solid ${active ? p.color : "var(--color-border)"}`, background: active ? `${p.color}22` : "transparent", color: active ? p.color : "var(--color-text-muted)", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                              {p.label}
                            </button>
                          );
                        })}
                      </div>
                      <div style={{ marginTop: 14, display: "flex", gap: 8, flexWrap: "wrap", alignItems: "center" }}>
                        <button onClick={toggleVisibility}
                          style={{ padding: "5px 11px", background: detail.roster_visible_to_members ? "var(--color-positive)" : "var(--color-border)", color: detail.roster_visible_to_members ? "var(--color-bg)" : "#cbd5e1", border: "none", borderRadius: 5, fontSize: 11, fontWeight: 700, cursor: "pointer" }}>
                          Roster visible to members: {detail.roster_visible_to_members ? "ON" : "OFF"}
                        </button>
                        <button onClick={archive}
                          style={{ padding: "5px 11px", background: "transparent", color: "var(--color-destructive)", border: "1px solid #ef4444", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                          Archive group
                        </button>
                      </div>
                    </div>
                  )}

                  {/* Lane plans (R-E) — hidden for solo groups per #17 */}
                  {(detail.members || []).length >= 2 && (
                    <LanePlansPanel groupId={group.id} members={detail.members || []} isPrimary={isPrimary} onChanged={onChanged} />
                  )}

                  {/* Join tokens (R-F) — coach-side invite-code panel */}
                  {!group.archived && (
                    <JoinTokensPanel groupId={group.id} isPrimary={isPrimary} isGroupCoach={!!detail.viewer_role} />
                  )}

                  {/* Coach-mark-on-behalf (R-G, Flow N) — recent assignments */}
                  <GroupAssignmentsPanel groupId={group.id} isGroupCoach={!!detail.viewer_role} />
                </>
              )}
            </div>
          )}
        </div>
      );
    }

    // Managed Swimmers view (relationships scope, Stage 1 / R-B).
    // First-class top-level coach roster per decision #31 — shows ALL managed
    // swimmers regardless of group placement. Coach-gated. DOB required at
    // create per decision #27; minor status (#28) is DERIVED, never stored.

    // Bounds for the DOB date input. min: 1900-01-01 (sane lower bound);
    // max: today (no future-born). Both as YYYY-MM-DD strings for input[type=date].
    const DOB_MIN = "1900-01-01";
    const DOB_MAX_TODAY = () => new Date().toISOString().slice(0, 10);

    // ═══════════════════════════════════════════════════════════════
    //  Per-Swimmer Constraints (PSC) — slice 3 coach UX
    //  Spec: PER_SWIMMER_CONSTRAINTS_SCOPE.md §3.8
    //  Decisions (Cap'n 2026-05-26):
    //    - Managed Swimmers only for v1 (real-user swimmers via API)
    //    - Edit = delete + recreate (no PATCH route)
    // ═══════════════════════════════════════════════════════════════

    // Closed vocab — keep in sync with migration 037 ENUM and db.js PSC_TYPES.
    const PSC_TYPE_GROUPS = [
      {
        label: "Stroke (no fly / breast / back / free)",
        opts: [
          { v: "stroke_no_fly",    label: "No fly" },
          { v: "stroke_no_breast", label: "No breaststroke" },
          { v: "stroke_no_back",   label: "No backstroke" },
          { v: "stroke_no_free",   label: "No freestyle" },
        ],
      },
      {
        label: "Equipment (no paddles / fins / etc.)",
        opts: [
          { v: "equip_no_paddles",   label: "No paddles" },
          { v: "equip_no_fins",      label: "No fins" },
          { v: "equip_no_snorkel",   label: "No snorkel" },
          { v: "equip_no_kickboard", label: "No kickboard" },
          { v: "equip_no_buoy",      label: "No pull-buoy" },
        ],
      },
      {
        label: "Section (skip main / kick / drill)",
        opts: [
          { v: "section_no_main",  label: "Skip main set"  },
          { v: "section_no_kick",  label: "Skip kick section"  },
          { v: "section_no_drill", label: "Skip drill section" },
        ],
      },
      {
        label: "Caps (yardage / intensity)",
        opts: [
          { v: "cap_yardage",   label: "Yardage cap" },
          { v: "cap_intensity", label: "Intensity cap (easy-only)" },
        ],
      },
    ];

    // Flat lookup for label rendering.
    const PSC_LABEL_MAP = (() => {
      const m = {};
      for (const g of PSC_TYPE_GROUPS) for (const o of g.opts) m[o.v] = o.label;
      return m;
    })();

    function formatPscRow(c) {
      const base = PSC_LABEL_MAP[c.constraint_type] || c.constraint_type;
      const valueBit = c.value_num != null ? ` — ${c.value_num} yd` :
                       c.value_str != null ? ` — ${c.value_str}` : "";
      const expiryBit = c.expires_at
        ? ` · expires ${new Date(c.expires_at).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })}`
        : " · persistent";
      return `${base}${valueBit}${expiryBit}`;
    }

    // Add/Edit modal. Edit = caller passes prefill from a soft-deleted row.
    function ConstraintFormModal({ open, managedId, prefill, onClose, onSaved }) {
      const [type,     setType]     = React.useState(prefill?.constraint_type || "stroke_no_fly");
      const [valueNum, setValueNum] = React.useState(prefill?.value_num != null ? String(prefill.value_num) : "");
      const [valueStr, setValueStr] = React.useState(prefill?.value_str || "easy_only");
      const [persistent, setPersistent] = React.useState(prefill ? !prefill.expires_at : true);
      const [expiresYmd, setExpiresYmd] = React.useState(() => {
        if (prefill?.expires_at) return String(prefill.expires_at).slice(0, 10);
        // default: 2 weeks from now
        const d = new Date(); d.setDate(d.getDate() + 14);
        return d.toISOString().slice(0, 10);
      });
      const [busy, setBusy] = React.useState(false);
      const [err,  setErr]  = React.useState(null);

      // Re-init on open (prefill changes between Add and Edit invocations).
      React.useEffect(() => {
        if (!open) return;
        setType(prefill?.constraint_type || "stroke_no_fly");
        setValueNum(prefill?.value_num != null ? String(prefill.value_num) : "");
        setValueStr(prefill?.value_str || "easy_only");
        setPersistent(prefill ? !prefill.expires_at : true);
        if (prefill?.expires_at) setExpiresYmd(String(prefill.expires_at).slice(0, 10));
        setErr(null);
      }, [open, prefill]);

      if (!open) return null;

      const needsYd  = type === "cap_yardage";
      const needsStr = type === "cap_intensity";

      const submit = async () => {
        setErr(null);
        const body = { managed_id: managedId, constraint_type: type };
        if (needsYd) {
          const n = parseInt(valueNum, 10);
          if (!Number.isInteger(n) || n <= 0) { setErr("Yardage cap must be a positive integer"); return; }
          body.value_num = n;
        }
        if (needsStr) body.value_str = valueStr;
        if (!persistent) {
          // Build ISO with end-of-day UTC so the date inclusive feels right
          body.expires_at = `${expiresYmd}T23:59:59Z`;
        }
        setBusy(true);
        try {
          const res = await fetch("/api/swimmer-constraints", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify(body),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setBusy(false);
          onSaved && onSaved(j.constraint);
          onClose();
        } catch (e) { setBusy(false); setErr(e.message); }
      };

      return (
        <div onClick={onClose} className="modal-overlay"
          style={{ position: "fixed", inset: 0, zIndex: 9000, background: "rgba(2,6,23,0.7)", display: "flex", alignItems: "flex-start", justifyContent: "center", padding: "10vh 16px" }}>
          <div onClick={e => e.stopPropagation()}
            style={{ width: "100%", maxWidth: 460, background: "var(--color-bg)", border: "1px solid var(--color-card)", borderRadius: 14, padding: "22px 22px 18px", boxShadow: "0 24px 60px rgba(0,0,0,0.5)" }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 14 }}>
              <h3 style={{ color: "var(--color-text)", fontSize: 17, margin: 0, fontWeight: 700 }}>
                {prefill ? "Edit constraint" : "Add constraint"}
              </h3>
              <button onClick={onClose} aria-label="Close"
                style={{ background: "transparent", border: "none", color: "var(--color-text-dim)", fontSize: 22, cursor: "pointer", padding: 0, lineHeight: 1 }}>×</button>
            </div>

            <div style={{ marginBottom: 12 }}>
              <label style={{ display: "block", fontSize: 11, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 4 }}>Constraint type</label>
              <select value={type} onChange={e => setType(e.target.value)}
                style={{ width: "100%", padding: "8px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 14 }}>
                {PSC_TYPE_GROUPS.map(g => (
                  <optgroup key={g.label} label={g.label}>
                    {g.opts.map(o => <option key={o.v} value={o.v}>{o.label}</option>)}
                  </optgroup>
                ))}
              </select>
            </div>

            {needsYd && (
              <div style={{ marginBottom: 12 }}>
                <label style={{ display: "block", fontSize: 11, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 4 }}>Yardage cap</label>
                <input type="number" min="100" max="20000" step="100" value={valueNum}
                  onChange={e => setValueNum(e.target.value)} placeholder="e.g. 1500"
                  style={{ width: "100%", padding: "8px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 14 }} />
                <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginTop: 4 }}>
                  Workouts beyond this size get trimmed from the end (preserves warmup + main when possible).
                </div>
              </div>
            )}

            {needsStr && (
              <div style={{ marginBottom: 12, padding: 8, background: "var(--color-card)", borderRadius: 6, fontSize: 12, color: "var(--color-text-muted)" }}>
                Currently the only intensity cap is <strong>easy_only</strong> — replaces the main set with cooldown-equivalent volume at easy pace.
              </div>
            )}

            <div style={{ marginBottom: 14 }}>
              <label style={{ display: "block", fontSize: 11, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: 4 }}>Expiry</label>
              <div style={{ display: "flex", gap: 6, marginBottom: 6 }}>
                <button type="button" onClick={() => setPersistent(true)}
                  style={{ flex: 1, padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: persistent ? "var(--color-primary)" : "var(--color-card)", color: persistent ? "var(--color-bg)" : "var(--color-text-muted)", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                  Persistent (until removed)
                </button>
                <button type="button" onClick={() => setPersistent(false)}
                  style={{ flex: 1, padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: !persistent ? "var(--color-primary)" : "var(--color-card)", color: !persistent ? "var(--color-bg)" : "var(--color-text-muted)", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                  Until date
                </button>
              </div>
              {!persistent && (
                <input type="date" value={expiresYmd} onChange={e => setExpiresYmd(e.target.value)}
                  style={{ width: "100%", padding: "8px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text)", fontSize: 14 }} />
              )}
            </div>

            {err && <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>Error: {err}</div>}

            <div style={{ display: "flex", gap: 8 }}>
              <button onClick={submit} disabled={busy}
                style={{ flex: 1, padding: "9px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: busy ? "wait" : "pointer", opacity: busy ? 0.6 : 1 }}>
                {busy ? "Saving…" : (prefill ? "Save changes" : "Add constraint")}
              </button>
              <button onClick={onClose} disabled={busy}
                style={{ padding: "9px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 6, fontSize: 13, cursor: "pointer" }}>
                Cancel
              </button>
            </div>
          </div>
        </div>
      );
    }

    // Parents/Guardians subsection (Parent Portal MVP — 2026-05-27).
    // Slots into the managed-swimmer detail view alongside Constraints +
    // CoachNotes. Coach types a parent email → backend enqueues an
    // invite email; parent signs in via Apple/Google with that email and
    // the join completes server-side. Already-linked guardians can be
    // removed (one-way; the parent has to be re-invited if removed).
    // Pending invites can be revoked before the parent acts on them.
    function ParentsPanel({ swimmerRef, swimmerName, seedGuardians = null, seedInvites = null }) {
      const seeded = seedGuardians != null || seedInvites != null;
      const [data,   setData]   = React.useState(seeded
        ? { guardians: Array.isArray(seedGuardians) ? seedGuardians : [], invites: Array.isArray(seedInvites) ? seedInvites : [] }
        : null);                                                          // { guardians, invites }
      const [email,  setEmail]  = React.useState("");
      const [busy,   setBusy]   = React.useState(false);
      const [msg,    setMsg]    = React.useState(null);
      const [info,   setInfo]   = React.useState(null);

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/swimmers/${encodeURIComponent(swimmerRef)}/parents`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const j = await res.json();
          setData({
            guardians: Array.isArray(j.guardians) ? j.guardians : [],
            invites:   Array.isArray(j.invites)   ? j.invites   : [],
          });
        } catch (e) { setMsg(`Load failed: ${e.message}`); setData({ guardians: [], invites: [] }); }
      }, [swimmerRef]);
      // Seeded from the composite detail fetch — skip the initial load; still
      // refetch after invite/revoke/remove. Ref guards only the first run.
      const skipInitial = React.useRef(seeded);
      React.useEffect(() => { if (skipInitial.current) { skipInitial.current = false; return; } load(); }, [load]);

      const submitInvite = async () => {
        if (busy) return;
        const trimmed = email.trim();
        if (!trimmed) { setMsg("Enter the parent's email."); return; }
        if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(trimmed)) { setMsg("That doesn't look like a valid email."); return; }
        setBusy(true); setMsg(null); setInfo(null);
        try {
          const res = await fetch(`/api/swimmers/${encodeURIComponent(swimmerRef)}/parent-invite`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            // Server destructures `parent_email`; sending `email` 400s with
            // "parent_email required". Pre-2026-05-28 bug found in test.
            body:    JSON.stringify({ parent_email: trimmed }),
          });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setEmail("");
          setInfo(`Invite sent to ${trimmed}. They sign in with Apple or Google using that email to complete the link.`);
          await load();
        } catch (e) { setMsg(`Invite failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const revokeInvite = async (id) => {
        if (!window.confirm("Revoke this pending invite? You can re-invite later.")) return;
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/parent-invites/${id}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(`Revoke failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      const removeGuardian = async (id, who) => {
        if (!window.confirm(`Remove ${who || "this parent"} from ${swimmerName || "this swimmer"}?\n\nThey'll lose access to the parent view and the weekly digest. You can re-invite them later if needed.`)) return;
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`/api/guardians/${id}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
        } catch (e) { setMsg(`Remove failed: ${e.message}`); }
        finally { setBusy(false); }
      };

      if (data === null) {
        return (
          <div className="card" style={{ borderRadius: 10, padding: 14, marginBottom: 14, color: "var(--color-text-dim)", fontSize: 12 }}>
            Loading parents…
          </div>
        );
      }
      const { guardians, invites } = data;

      return (
        <div className="card" style={{ borderRadius: 10, padding: 14, marginBottom: 14 }}>
          <h3 style={{ color: "var(--color-text)", marginTop: 0, marginBottom: 10, fontSize: 14 }}>
            👪 Parents & Guardians
            <span style={{ marginLeft: 8, fontSize: 11, fontWeight: 500, color: "var(--color-text-muted)" }}>
              · they see a weekly recap + read-only view
            </span>
          </h3>

          {/* Current guardians */}
          {guardians.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic", marginBottom: 10 }}>
              No parents linked yet.
            </div>
          ) : (
            <div style={{ marginBottom: 12 }}>
              <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>Linked</div>
              {guardians.map(g => (
                <div key={g.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8, padding: "6px 8px", background: "var(--color-bg)", borderRadius: 6, marginBottom: 4 }}>
                  <div style={{ fontSize: 12, color: "var(--color-text)" }}>
                    <strong>{g.display_name || g.email || "Parent"}</strong>
                    {g.email && g.display_name && <span style={{ color: "var(--color-text-muted)", marginLeft: 6, fontSize: 11 }}>{g.email}</span>}
                  </div>
                  <button onClick={() => removeGuardian(g.id, g.display_name || g.email)} disabled={busy}
                    style={{ padding: "3px 9px", borderRadius: 4, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: busy ? "wait" : "pointer" }}>
                    Remove
                  </button>
                </div>
              ))}
            </div>
          )}

          {/* Pending invites */}
          {invites.length > 0 && (
            <div style={{ marginBottom: 12 }}>
              <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>Pending invites</div>
              {invites.map(inv => (
                <div key={inv.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8, padding: "6px 8px", background: "var(--color-bg)", borderRadius: 6, marginBottom: 4 }}>
                  <div style={{ fontSize: 12, color: "var(--color-text)" }}>
                    <span>{inv.parent_email}</span>
                    <span style={{ color: "var(--color-text-muted)", marginLeft: 8, fontSize: 11 }}>
                      sent {inv.created_at ? new Date(inv.created_at).toLocaleDateString() : "—"}
                    </span>
                  </div>
                  <button onClick={() => revokeInvite(inv.id)} disabled={busy}
                    style={{ padding: "3px 9px", borderRadius: 4, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: busy ? "wait" : "pointer" }}>
                    Revoke
                  </button>
                </div>
              ))}
            </div>
          )}

          {/* Add new parent */}
          <div style={{ borderTop: "1px solid var(--color-border)", paddingTop: 10 }}>
            <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginBottom: 6, textTransform: "uppercase", letterSpacing: "0.05em" }}>Invite a parent</div>
            <div style={{ display: "flex", gap: 6, alignItems: "stretch", flexWrap: "wrap" }}>
              <input
                type="email"
                value={email}
                onChange={e => { setEmail(e.target.value); setMsg(null); }}
                onKeyDown={e => { if (e.key === "Enter") submitInvite(); }}
                placeholder="parent@example.com"
                disabled={busy}
                style={{ flex: 1, minWidth: 180, padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-bg)", color: "var(--color-text)", fontSize: 13 }}
              />
              <button onClick={submitInvite} disabled={busy || !email.trim()}
                style={{ padding: "6px 14px", borderRadius: 6, border: "none", background: busy ? "var(--color-border)" : "var(--color-primary)", color: "var(--color-bg)", fontSize: 12, fontWeight: 700, cursor: busy ? "wait" : "pointer" }}>
                {busy ? "Sending…" : "Send invite"}
              </button>
            </div>
            <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 6, lineHeight: 1.5 }}>
              The parent gets an email with a link to SetForge. When they sign in with Apple or Google using this email, the link completes automatically — no token or password to manage.
            </div>
          </div>

          {info && <div style={{ color: "var(--color-positive)", fontSize: 12, marginTop: 8 }}>{info}</div>}
          {msg  && <div style={{ color: "var(--color-warn)",     fontSize: 12, marginTop: 8 }}>{msg}</div>}
        </div>
      );
    }

    // Per-managed-swimmer Constraints subsection. Slots into the swimmer
    // detail view between ClaimTokensPanel and CoachNotesPanel.
    function ConstraintsPanel({ managedId, seedConstraints = null }) {
      const [rows, setRows] = React.useState(Array.isArray(seedConstraints) ? seedConstraints : null);
      const [err,  setErr]  = React.useState(null);
      const [modalOpen, setModalOpen] = React.useState(false);
      const [editRow,   setEditRow]   = React.useState(null);  // prefill row for Edit

      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`/api/swimmer-constraints?managed_id=${encodeURIComponent(managedId)}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const j = await res.json();
          setRows(Array.isArray(j.constraints) ? j.constraints : []);
        } catch (e) { setErr(e.message); setRows([]); }
      }, [managedId]);
      // Seeded from the composite detail fetch — skip the initial load; still
      // refetch after add/edit/remove. Ref guards only the first run.
      const skipInitial = React.useRef(Array.isArray(seedConstraints));
      React.useEffect(() => { if (skipInitial.current) { skipInitial.current = false; return; } load(); }, [load]);

      const handleRemove = async (id, confirmMsg) => {
        if (confirmMsg && !window.confirm(confirmMsg)) return false;
        setErr(null);
        try {
          const res = await fetch(`/api/swimmer-constraints/${id}`, {
            method:  "DELETE",
            headers: { ...csrfHeaders() },
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await load();
          return true;
        } catch (e) { setErr(e.message); return false; }
      };

      const handleEdit = async (row) => {
        // Per fork 3 (delete + recreate): drop the existing row first, then
        // open the form prefilled. If delete fails, abort.
        const ok = await handleRemove(row.id, null);
        if (!ok) return;
        setEditRow(row);
        setModalOpen(true);
      };

      return (
        <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }}>
            <h3 style={{ color: "var(--color-text)", margin: 0, fontSize: 15 }}>⚠ Active constraints</h3>
            <button onClick={() => { setEditRow(null); setModalOpen(true); }}
              style={{ padding: "5px 11px", background: "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
              + Add constraint
            </button>
          </div>

          {rows === null ? (
            <div style={{ color: "var(--color-text-muted)", fontSize: 12 }}>Loading…</div>
          ) : rows.length === 0 ? (
            <div style={{ color: "var(--color-text-muted)", fontSize: 12, fontStyle: "italic", padding: "8px 0" }}>
              No active constraints. Use Add to record an injury restriction, equipment limit, or taper cap.
            </div>
          ) : (
            <div>
              {rows.map(r => (
                <div key={r.id} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "8px 0", borderBottom: "1px solid var(--color-card)", gap: 8 }}>
                  <div style={{ fontSize: 13, color: "var(--color-text)", flex: 1 }}>
                    <span style={{ marginRight: 6 }}>⚠</span>
                    {formatPscRow(r)}
                  </div>
                  <div style={{ display: "flex", gap: 4 }}>
                    <button onClick={() => handleEdit(r)} title="Edit (replaces the row)"
                      style={{ padding: "4px 9px", background: "var(--color-card)", color: "var(--color-text-muted)", border: "1px solid var(--color-border)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                      Edit
                    </button>
                    <button onClick={() => handleRemove(r.id, `Remove "${PSC_LABEL_MAP[r.constraint_type] || r.constraint_type}"?`)}
                      style={{ padding: "4px 9px", background: "var(--color-card)", color: "var(--color-warn)", border: "1px solid var(--color-warn)", borderRadius: 5, fontSize: 11, cursor: "pointer" }}>
                      Remove
                    </button>
                  </div>
                </div>
              ))}
            </div>
          )}

          {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 8 }}>Error: {err}</div>}

          <ConstraintFormModal
            open={modalOpen}
            managedId={managedId}
            prefill={editRow}
            onClose={() => { setModalOpen(false); setEditRow(null); }}
            onSaved={() => { load(); }}
          />
        </div>
      );
    }

    function ManagedSwimmersView({ mySub = null } = {}) {
      const [list,       setList]       = React.useState(null);
      const [showArch,   setShowArch]   = React.useState(false);
      const [selectedId, setSelectedId] = React.useState(null);
      const [detail,     setDetail]     = React.useState(null);
      // Composite payload from /api/managed-swimmers/:id/detail — seeds the
      // panels below so opening a swimmer is ONE request, not ~7 (the burst
      // that tripped 429s). Panels still keep their own fetch for post-edit
      // refresh; the seed just skips the initial load. null until loaded.
      const [bundle,     setBundle]     = React.useState(null);
      const [creating,   setCreating]   = React.useState(false);
      const [importing,  setImporting]  = React.useState(false);
      const [form,       setForm]       = React.useState(() => emptyForm());
      const [editing,    setEditing]    = React.useState(false);
      const [teams,      setTeams]      = React.useState(null);                // coach's active teams (for picker)
      const [msg,        setMsg]        = React.useState(null);

      function emptyForm() {
        return {
          display_name: "", initials: "", dob: "", gender: "", class_year: "", usa_swimming_id: "", team_id: "",
          pace_scy_100: "", pace_scm_100: "", pace_lcm_100: "",
          parental_contact: "", parent_managed_flag: false,
        };
      }

      const loadList = React.useCallback(async () => {
        try {
          const q = showArch ? "?archived=1" : "";
          const res = await fetch(`/api/managed-swimmers${q}`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setList(await res.json());
        } catch (err) {
          setList([]);
          setMsg(`Error loading roster: ${err.message}`);
        }
      }, [showArch]);

      const loadDetail = React.useCallback(async (id) => {
        if (!id) { setDetail(null); setBundle(null); return; }
        setDetail(null); setBundle(null);
        try {
          const res = await fetch(`/api/managed-swimmers/${id}/detail`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          const payload = await res.json();
          setBundle(payload);
          const d = payload.swimmer;
          setDetail(d);
          setForm({
            display_name:        d.display_name || "",
            initials:            d.initials || "",
            dob:                 d.dob || "",
            gender:              d.gender || "",
            class_year:          d.class_year ? String(d.class_year) : "",
            usa_swimming_id:     d.usa_swimming_id || "",
            team_id:             d.team_id || "",
            pace_scy_100:        d.pace_scy_100 || "",
            pace_scm_100:        d.pace_scm_100 || "",
            pace_lcm_100:        d.pace_lcm_100 || "",
            parental_contact:    d.parental_contact || "",
            parent_managed_flag: !!d.parent_managed_flag,
          });
        } catch (err) {
          setMsg(`Error loading swimmer: ${err.message}`);
        }
      }, []);

      React.useEffect(() => { loadList(); }, [loadList]);
      React.useEffect(() => { loadDetail(selectedId); }, [selectedId, loadDetail]);

      // Load the coach's active teams once on mount for the team-picker UI.
      // Filtered to non-archived so the picker only shows usable targets.
      React.useEffect(() => {
        fetch("/api/teams", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(ts => {
            const active = (Array.isArray(ts) ? ts : []).filter(t => !t.archived);
            setTeams(active);
            // If the coach has exactly one team, default the create form to it.
            // Only seed an empty form — never overwrite an in-progress edit or
            // a form the user has already touched.
            if (active.length === 1) {
              setForm(f => (f.display_name === "" && f.team_id === "" ? { ...f, team_id: active[0].id } : f));
            }
          })
          .catch(() => setTeams([]));
      }, []);

      const submitCreate = async () => {
        if (!form.display_name.trim()) { setMsg("Name required"); return; }
        if (!form.dob) { setMsg("DOB required (compliance gate)"); return; }
        try {
          const res = await fetch("/api/managed-swimmers", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({
              display_name:        form.display_name.trim(),
              initials:            form.initials.trim() || null,
              dob:                 form.dob,
              gender:              form.gender || null,
              class_year:          form.class_year ? Number(form.class_year) : null,
              usa_swimming_id:     form.usa_swimming_id.trim() || null,
              team_id:             form.team_id || null,
              pace_scy_100:        form.pace_scy_100.trim() || null,
              pace_scm_100:        form.pace_scm_100.trim() || null,
              pace_lcm_100:        form.pace_lcm_100.trim() || null,
              parental_contact:    form.parental_contact.trim() || null,
              parent_managed_flag: !!form.parent_managed_flag,
            }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setMsg(null);
          // Reset to empty form, but re-default team if coach has only one team.
          const reset = emptyForm();
          if (teams && teams.length === 1) reset.team_id = teams[0].id;
          setForm(reset);
          setCreating(false);
          await loadList();
          setSelectedId(j.id);
        } catch (err) { setMsg(`Error: ${err.message}`); }
      };

      const submitEdit = async () => {
        try {
          const res = await fetch(`/api/managed-swimmers/${detail.id}`, {
            method:  "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({
              display_name:        form.display_name.trim(),
              initials:            form.initials.trim() || null,
              dob:                 form.dob,
              gender:              form.gender || null,
              class_year:          form.class_year ? Number(form.class_year) : null,
              usa_swimming_id:     form.usa_swimming_id.trim() || null,
              team_id:             form.team_id || null,
              pace_scy_100:        form.pace_scy_100.trim() || null,
              pace_scm_100:        form.pace_scm_100.trim() || null,
              pace_lcm_100:        form.pace_lcm_100.trim() || null,
              parental_contact:    form.parental_contact.trim() || null,
              parent_managed_flag: !!form.parent_managed_flag,
            }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setEditing(false);
          setMsg(null);
          await loadDetail(detail.id);
          await loadList();
        } catch (err) { setMsg(`Error: ${err.message}`); }
      };

      const handleArchive = async () => {
        const next = !detail.archived;
        const verb = next ? "Archive" : "Unarchive";
        if (!window.confirm(`${verb} ${detail.display_name}?`)) return;
        try {
          const res = await fetch(`/api/managed-swimmers/${detail.id}/archive`, {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ archived: next }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          await loadDetail(detail.id);
          await loadList();
        } catch (err) { setMsg(`Error: ${err.message}`); }
      };

      // ── Minor-indicator pill ─────────────────────────────────────────
      function MinorPill({ swimmer }) {
        if (swimmer.is_coppa_protected) {
          return <span title="Under 13 — claim flow blocked per decision #28"
            style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: "#dc262622", border: "1px solid #dc2626", color: "#dc2626", fontWeight: 700 }}>under 13</span>;
        }
        if (swimmer.is_minor) {
          return <span title="Under 18 — minor protections apply"
            style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: "#f59e0b22", border: "1px solid var(--color-warn)", color: "var(--color-warn)", fontWeight: 700 }}>minor</span>;
        }
        return <span style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: "#33415522", border: "1px solid var(--color-border-strong)", color: "var(--color-text-muted)", fontWeight: 700 }}>adult</span>;
      }

      // ── Detail view ─────────────────────────────────────────────────
      if (selectedId && detail) {
        return (
          <div>
            <button onClick={() => { setSelectedId(null); setDetail(null); setEditing(false); setMsg(null); }}
              style={{ marginBottom: 12, padding: "5px 11px", borderRadius: 6, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text-muted)", fontSize: 12, fontWeight: 600, cursor: "pointer" }}>
              ← All swimmers
            </button>

            <div className="card" style={{ borderRadius: 10, padding: 16, marginBottom: 16 }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: 10, marginBottom: 12 }}>
                <div>
                  <h2 style={{ color: "var(--color-text)", margin: 0, fontSize: 20 }}>
                    {detail.display_name}
                    {detail.archived && <span style={{ marginLeft: 10, fontSize: 11, padding: "2px 8px", borderRadius: 999, background: "#64748b22", border: "1px solid #64748b", color: "var(--color-text-muted)" }}>archived</span>}
                  </h2>
                  <div style={{ display: "flex", gap: 8, marginTop: 6, alignItems: "center" }}>
                    <span style={{ fontSize: 12, color: "var(--color-text-muted)" }}>Age {detail.age}</span>
                    <MinorPill swimmer={detail} />
                    {detail.parent_managed_flag && (
                      <span title="Coach has flagged this profile as parent-managed; claim blocked even if 13+"
                        style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: "#3b82f622", border: "1px solid var(--color-primary)", color: "var(--color-primary)", fontWeight: 700 }}>parent-managed</span>
                    )}
                  </div>
                </div>
                {!editing && (
                  <button onClick={() => setEditing(true)}
                    style={{ padding: "5px 11px", background: "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    ✎ Edit
                  </button>
                )}
              </div>

              {editing ? (
                <ManagedSwimmerForm form={form} setForm={setForm} teams={teams} />
              ) : (
                <div style={{ fontSize: 13, color: "#cbd5e1", lineHeight: 1.7 }}>
                  <div>Initials: <strong>{detail.initials || "—"}</strong></div>
                  <div>DOB: <strong>{detail.dob}</strong></div>
                  <div>Gender: <strong>{genderLabel(detail.gender) || "—"}</strong></div>
                  <div>Class year: <strong>{detail.class_year || "—"}</strong>{detail.grade != null && detail.grade >= 1 && detail.grade <= 12 ? <span style={{ color: "var(--color-text-dim)", marginLeft: 6 }}>(Grade {detail.grade})</span> : detail.grade != null && detail.grade > 12 ? <span style={{ color: "var(--color-text-dim)", marginLeft: 6 }}>(Graduated)</span> : null}</div>
                  <div>Team: <strong>{detail.team_id ? ((teams || []).find(t => t.id === detail.team_id)?.name || detail.team_id) : "— none —"}</strong></div>
                  <div>Paces — SCY: <strong>{detail.pace_scy_100 || "—"}</strong> · SCM: <strong>{detail.pace_scm_100 || "—"}</strong> · LCM: <strong>{detail.pace_lcm_100 || "—"}</strong></div>
                  <div>Parental contact: <strong>{detail.parental_contact || "—"}</strong></div>
                  <div style={{ marginTop: 10, paddingTop: 10, borderTop: "1px solid var(--color-card)" }}>
                    {/* Home address — coach sees it only if a guardian shared it (consent). */}
                    <AddressManager key={detail.id} swimmerRef={detail.id} showConsent={false}
                      seedAddresses={bundle?.addresses} seedCanWrite={bundle?.address_can_write} />
                    {/* Siblings (P3) — consent-gated per sibling server-side. */}
                    <HouseholdSiblings key={"hh-" + detail.id} swimmerRef={detail.id}
                      seedHousehold={bundle?.household} />
                  </div>
                </div>
              )}

              <div style={{ marginTop: 14, display: "flex", gap: 8 }}>
                {editing ? (
                  <>
                    <button onClick={submitEdit}
                      style={{ padding: "6px 14px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>Save</button>
                    <button onClick={() => { setEditing(false); loadDetail(detail.id); }}
                      style={{ padding: "6px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 6, fontSize: 13, cursor: "pointer" }}>Cancel</button>
                  </>
                ) : (
                  <button onClick={handleArchive}
                    style={{ padding: "5px 11px", background: detail.archived ? "var(--color-positive)" : "var(--color-text-dim)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                    {detail.archived ? "Unarchive" : "Archive"}
                  </button>
                )}
              </div>

              <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 10 }}>
                ID <code style={{ background: "var(--color-bg)", padding: "1px 5px", borderRadius: 3 }}>{detail.id}</code>
                <span style={{ marginLeft: 8 }}>Owner sub ends in <code>{detail.owner_coach_sub.slice(-8)}</code></span>
              </div>
            </div>

            {/* R-I: claim token issuer for this managed profile */}
            {!detail.archived && (
              <ClaimTokensPanel
                key={"ct-" + detail.id}
                managedId={detail.id}
                isMinor={detail.is_minor}
                isCoppa={detail.is_coppa_protected}
                parentManaged={detail.parent_managed_flag}
                seedTokens={bundle?.claim_tokens}
              />
            )}

            {/* Phase 3 PSC slice 3 — per-swimmer constraints (injury / equipment / cap restrictions) */}
            {!detail.archived && (
              <ConstraintsPanel key={"con-" + detail.id} managedId={detail.id}
                seedConstraints={bundle?.constraints} />
            )}

            {/* Parent Portal MVP — invite/revoke parents on this profile */}
            {!detail.archived && (
              <ParentsPanel key={"pp-" + detail.id} swimmerRef={detail.id} swimmerName={detail.display_name}
                seedGuardians={bundle?.parents?.guardians} seedInvites={bundle?.parents?.invites} />
            )}

            {/* R-H: coach notes journal for this managed profile */}
            {!detail.archived && (
              <CoachNotesPanel
                key={"cn-" + detail.id}
                targetManagedId={detail.id}
                teamId={detail.team_id}
                mySub={mySub}
                seedNotes={bundle?.coach_notes}
              />
            )}

            {msg && <div style={{ color: "#fca5a5", fontSize: 12, padding: "8px 0" }}>{msg}</div>}
          </div>
        );
      }

      // ── List view ───────────────────────────────────────────────────
      const itemsByMinor = list ? {
        coppa:  list.filter(s => s.is_coppa_protected),
        minor:  list.filter(s => s.is_minor && !s.is_coppa_protected),
        adult:  list.filter(s => !s.is_minor),
      } : null;

      return (
        <div>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16, flexWrap: "wrap", gap: 8 }}>
            <h2 style={{ color: "var(--color-text)", margin: 0 }}>🏊 Managed swimmers</h2>
            <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
              <button onClick={() => setShowArch(v => !v)}
                style={{ padding: "5px 11px", borderRadius: 6, border: "1px solid var(--color-border)", background: showArch ? "var(--color-border)" : "transparent", color: "var(--color-text-muted)", fontSize: 11, cursor: "pointer" }}>
                {showArch ? "Hide archived" : "Show archived"}
              </button>
              <button onClick={() => setImporting(true)}
                style={{ padding: "6px 14px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-primary)", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                📥 Import
              </button>
              <button onClick={() => setCreating(c => !c)}
                style={{ padding: "6px 14px", background: creating ? "var(--color-border)" : "var(--color-positive)", color: creating ? "#cbd5e1" : "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                {creating ? "Cancel" : "+ New swimmer"}
              </button>
            </div>
          </div>

          {importing && (
            <BulkImportModal
              teams={teams || []}
              onClose={() => setImporting(false)}
              onImported={() => { loadList(); }}
            />
          )}

          {creating && (
            <div style={{ background: "var(--color-card)", border: "1px solid #22c55e", borderRadius: 10, padding: 14, marginBottom: 16 }}>
              <h3 style={{ color: "var(--color-text)", marginTop: 0, fontSize: 14 }}>Create managed swimmer</h3>
              <ManagedSwimmerForm form={form} setForm={setForm} teams={teams} />
              <button onClick={submitCreate}
                style={{ padding: "7px 16px", background: "var(--color-positive)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", marginTop: 8 }}>
                Create
              </button>
            </div>
          )}

          {msg && <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>{msg}</div>}

          {list === null ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13 }}>Loading…</div>
          ) : list.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 13, fontStyle: "italic", padding: 20, textAlign: "center", border: "1px dashed var(--color-border)", borderRadius: 10 }}>
              No managed swimmers yet. Add one above — DOB is required for compliance gates.
            </div>
          ) : (
            <div>
              <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginBottom: 8 }}>
                {list.length} swimmer{list.length === 1 ? "" : "s"}
                {itemsByMinor && (itemsByMinor.coppa.length + itemsByMinor.minor.length > 0) && (
                  <span> · {itemsByMinor.coppa.length} under 13 · {itemsByMinor.minor.length} 13–17 · {itemsByMinor.adult.length} adult</span>
                )}
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
                {list.map(s => {
                  const teamName = s.team_id ? ((teams || []).find(t => t.id === s.team_id)?.name) : null;
                  return (
                    <button key={s.id} onClick={() => setSelectedId(s.id)}
                      style={{ display: "block", textAlign: "left", width: "100%", padding: "10px 12px", background: s.archived ? "#1e293b88" : "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 8, cursor: "pointer", color: "var(--color-text)" }}>
                      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8 }}>
                        <div>
                          <span style={{ fontSize: 13, fontWeight: 700 }}>
                            {s.display_name}
                            {s.archived && <span style={{ marginLeft: 8, fontSize: 10, padding: "1px 6px", borderRadius: 4, background: "#64748b22", border: "1px solid #64748b", color: "var(--color-text-muted)" }}>archived</span>}
                          </span>
                          <div style={{ fontSize: 11, color: "var(--color-text-dim)", marginTop: 2 }}>
                            Age {s.age} · DOB {s.dob}
                            {s.pace_scy_100 && <span> · SCY {s.pace_scy_100}</span>}
                          </div>
                        </div>
                        <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                          {teamName && (
                            <span style={{ fontSize: 10, padding: "1px 7px", borderRadius: 999, background: "#3b82f622", border: "1px solid var(--color-primary)", color: "var(--color-primary)", fontWeight: 700 }} title="Team affiliation">
                              {teamName}
                            </span>
                          )}
                          <MinorPill swimmer={s} />
                        </div>
                      </div>
                    </button>
                  );
                })}
              </div>
            </div>
          )}
        </div>
      );
    }

    // Gender enum constants (matches db.js GENDER_VALUES). Labels used in
    // pickers; raw values are M / F / X / prefer_not_to_say.
    const GENDER_OPTIONS = [
      { v: "",                  label: "—" },
      { v: "M",                 label: "Male" },
      { v: "F",                 label: "Female" },
      { v: "X",                 label: "Non-binary / other" },
      { v: "prefer_not_to_say", label: "Prefer not to say" },
    ];
    function genderLabel(v) {
      const o = GENDER_OPTIONS.find(x => x.v === (v || ""));
      return o ? o.label : v;
    }

    // Shared form component for create + edit. Pure presentation.
    function ManagedSwimmerForm({ form, setForm, teams }) {
      const upd = (k, v) => setForm(f => ({ ...f, [k]: v }));
      const inputStyle = { width: "100%", padding: "5px 9px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 };
      const labelStyle = { display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 3, marginTop: 8 };
      const teamOptions = Array.isArray(teams) ? teams : [];
      return (
        <div>
          <label style={{ ...labelStyle, marginTop: 0 }}>Display name *</label>
          <input value={form.display_name} onChange={e => upd("display_name", e.target.value)}
            placeholder="e.g. Sarah Johnson" style={inputStyle} />
          {teamOptions.length > 0 && (
            <>
              <label style={labelStyle}>Team affiliation</label>
              <select value={form.team_id || ""} onChange={e => upd("team_id", e.target.value)} style={inputStyle}>
                <option value="">— no team —</option>
                {teamOptions.map(t => (
                  <option key={t.id} value={t.id}>{t.name}</option>
                ))}
              </select>
            </>
          )}
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 8 }}>
            <div>
              <label style={labelStyle}>Initials</label>
              <input value={form.initials} onChange={e => upd("initials", e.target.value)}
                placeholder="SJ" maxLength={4} style={inputStyle} />
            </div>
            <div>
              <label style={labelStyle}>DOB *</label>
              <input type="date" value={form.dob} onChange={e => upd("dob", e.target.value)}
                min={DOB_MIN} max={DOB_MAX_TODAY()} style={inputStyle} />
            </div>
            <div>
              <label style={labelStyle}>Gender</label>
              <select value={form.gender || ""} onChange={e => upd("gender", e.target.value)} style={inputStyle}>
                {GENDER_OPTIONS.map(o => <option key={o.v} value={o.v}>{o.label}</option>)}
              </select>
            </div>
          </div>
          <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 4 }}>
            DOB is required to derive minor-protection rules. Stored privately; only the swimmer's age and minor status are surfaced. Gender is optional; used for heat assignment when groups land.
          </div>

          <label style={labelStyle}>Class year (graduation year)</label>
          <input value={form.class_year} onChange={e => upd("class_year", e.target.value.replace(/\D/g, "").slice(0, 4))}
            placeholder="e.g. 2027" maxLength={4} inputMode="numeric" style={inputStyle} />
          <div style={{ fontSize: 10, color: "var(--color-text-dim)", marginTop: 4 }}>
            Optional. Grade level is derived automatically from the graduation year.
          </div>

          <label style={labelStyle}>USA Swimming ID</label>
          <input value={form.usa_swimming_id} onChange={e => upd("usa_swimming_id", e.target.value)}
            placeholder="USA-S member ID (optional)" maxLength={255} style={inputStyle} />

          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 8 }}>
            <div>
              <label style={labelStyle}>SCY 100 pace</label>
              <input value={form.pace_scy_100} onChange={e => upd("pace_scy_100", e.target.value)}
                placeholder="2:00" maxLength={8} style={inputStyle} />
            </div>
            <div>
              <label style={labelStyle}>SCM 100 pace</label>
              <input value={form.pace_scm_100} onChange={e => upd("pace_scm_100", e.target.value)}
                placeholder="2:10" maxLength={8} style={inputStyle} />
            </div>
            <div>
              <label style={labelStyle}>LCM 100 pace</label>
              <input value={form.pace_lcm_100} onChange={e => upd("pace_lcm_100", e.target.value)}
                placeholder="2:15" maxLength={8} style={inputStyle} />
            </div>
          </div>

          <label style={labelStyle}>Parental contact (email or phone, required for minors in high-school teams)</label>
          <input value={form.parental_contact} onChange={e => upd("parental_contact", e.target.value)}
            placeholder="parent@example.com" maxLength={255} style={inputStyle} />

          <label style={{ display: "flex", alignItems: "center", gap: 6, marginTop: 10, fontSize: 12, color: "#cbd5e1", cursor: "pointer" }}>
            <input type="checkbox" checked={!!form.parent_managed_flag}
              onChange={e => upd("parent_managed_flag", e.target.checked)} />
            <span>Parent-managed (blocks claim flow even for 13+)</span>
          </label>
        </div>
      );
    }

    // ── Bulk-import helpers (R-B') ────────────────────────────────────
    // Simple CSV/TSV parser — handles quoted strings with embedded delimiters
    // (Excel/Sheets export shape), auto-detects comma vs tab, requires a
    // header row. NOT a full RFC 4180 parser; handles 99% of coach exports.
    // I-H: names import as first_name + last_name (+ optional preferred_name).
    // The legacy single `name` column is still accepted and split on the last
    // space, with a per-row warning so the coach can verify the split.
    // Required: dob + a name (first_name OR legacy name).
    const IMPORT_NEW_HEADERS = ["first_name", "last_name", "preferred_name", "dob", "gender", "initials", "pace_scy_100", "pace_scm_100", "pace_lcm_100", "parental_contact"];
    // Every header the parser recognizes (superset; legacy `name` included).
    const IMPORT_RECOGNIZED = ["name", "first_name", "last_name", "preferred_name", "dob", "initials", "gender", "pace_scy_100", "pace_scm_100", "pace_lcm_100", "parental_contact"];
    // Accept several spellings of gender in CSV (case-insensitive). Map to enum.
    const GENDER_CSV_MAP = {
      "m": "M", "male": "M", "boy": "M",
      "f": "F", "female": "F", "girl": "F",
      "x": "X", "nb": "X", "non-binary": "X", "nonbinary": "X", "other": "X",
      "pnts": "prefer_not_to_say", "prefer not to say": "prefer_not_to_say", "decline": "prefer_not_to_say",
    };

    function csvAutoSplit(text) {
      // Strip BOM, normalize newlines.
      text = text.replace(/^﻿/, "").replace(/\r\n/g, "\n").replace(/\r/g, "\n");
      if (!text.trim()) return { rows: [], delim: "," };
      // Auto-detect delimiter from the first line: tab if there's at least one,
      // else comma. (Most coach exports are one or the other.)
      const firstLine = text.split("\n", 1)[0];
      const delim = firstLine.includes("\t") ? "\t" : ",";
      // Tokenize line by line. State machine handles quoted fields.
      const rows = [];
      for (const rawLine of text.split("\n")) {
        if (!rawLine.trim()) continue;
        const cells = [];
        let cur = "";
        let inQuotes = false;
        for (let i = 0; i < rawLine.length; i++) {
          const ch = rawLine[i];
          if (inQuotes) {
            if (ch === '"') {
              if (rawLine[i + 1] === '"') { cur += '"'; i++; }                // "" → "
              else inQuotes = false;
            } else cur += ch;
          } else {
            if (ch === '"') inQuotes = true;
            else if (ch === delim) { cells.push(cur); cur = ""; }
            else cur += ch;
          }
        }
        cells.push(cur);
        rows.push(cells.map(c => c.trim()));
      }
      return { rows, delim };
    }

    function csvParseSwimmers(text) {
      const { rows } = csvAutoSplit(text);
      if (!rows.length) return { ok: false, error: "No data found." };
      const headerCells = rows[0].map(h => h.toLowerCase().trim());
      // Validate required headers exist.
      if (!headerCells.includes("dob")) return { ok: false, error: `Missing required column: dob. Expected headers: ${IMPORT_NEW_HEADERS.join(", ")} (legacy single 'name' column also accepted).` };
      if (!headerCells.includes("first_name") && !headerCells.includes("name")) return { ok: false, error: "Missing a name column: provide first_name (+ optional last_name), or the legacy single 'name' column." };
      // Build column-index map for known headers (ignore unknown columns).
      const idx = {};
      for (const h of IMPORT_RECOGNIZED) {
        const i = headerCells.indexOf(h);
        if (i >= 0) idx[h] = i;
      }
      // Convert each data row into a raw object.
      const dataRows = [];
      for (let r = 1; r < rows.length; r++) {
        const obj = {};
        for (const [h, i] of Object.entries(idx)) obj[h] = rows[r][i] || "";
        // Skip empty rows (all known fields blank).
        if (Object.values(obj).every(v => !v.trim())) continue;
        dataRows.push(obj);
      }
      return { ok: true, dataRows };
    }

    // Normalize + validate one row. Returns {ok, normalized?, error?}.
    function normalizeImportRow(raw) {
      // I-H name resolution: explicit first_name (+ last_name) preferred; else
      // split the legacy single `name` column and flag a warning so the coach
      // can verify. preferred_name is optional in both paths.
      const first     = (raw.first_name || "").trim();
      const last      = (raw.last_name  || "").trim();
      const preferred = (raw.preferred_name || "").trim() || null;
      const legacy    = (raw.name || "").trim();
      let nameFields, warning = null;
      if (first) {
        if (first.length > 80) return { ok: false, error: "first_name too long (>80 chars)" };
        if (last.length  > 80) return { ok: false, error: "last_name too long (>80 chars)" };
        if (preferred && preferred.length > 80) return { ok: false, error: "preferred_name too long (>80 chars)" };
        nameFields = { first_name: first, last_name: last, preferred_name: preferred };
      } else if (legacy) {
        if (legacy.length > 120) return { ok: false, error: "name too long (>120 chars)" };
        nameFields = { display_name: legacy, preferred_name: preferred };
        warning = "split from single 'name' column — verify first/last";
      } else {
        return { ok: false, error: "first_name (or legacy 'name') is empty" };
      }
      const dobRaw = (raw.dob || "").trim();
      if (!dobRaw) return { ok: false, error: "dob is empty" };
      // Accept YYYY-MM-DD or MM/DD/YYYY (or M/D/YYYY).
      let dob = null;
      const isoMatch = dobRaw.match(/^(\d{4})-(\d{1,2})-(\d{1,2})$/);
      const usMatch  = dobRaw.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/);
      if (isoMatch) {
        dob = `${isoMatch[1]}-${String(isoMatch[2]).padStart(2, "0")}-${String(isoMatch[3]).padStart(2, "0")}`;
      } else if (usMatch) {
        dob = `${usMatch[3]}-${String(usMatch[1]).padStart(2, "0")}-${String(usMatch[2]).padStart(2, "0")}`;
      } else {
        return { ok: false, error: `dob format not recognized (use YYYY-MM-DD or MM/DD/YYYY): ${dobRaw}` };
      }
      // Sanity-check the date is real.
      const d = new Date(dob + "T00:00:00Z");
      if (Number.isNaN(d.getTime())) return { ok: false, error: `dob is not a real date: ${dobRaw}` };
      if (d > new Date()) return { ok: false, error: `dob is in the future: ${dobRaw}` };
      if (d.getUTCFullYear() < 1900) return { ok: false, error: `dob before 1900: ${dobRaw}` };

      const initials = (raw.initials || "").trim() || null;
      if (initials && initials.length > 4) return { ok: false, error: "initials too long (>4 chars)" };
      // Gender: tolerant CSV mapping (e.g. "Female" → "F"). Empty cell → null.
      let gender = null;
      const gRaw = (raw.gender || "").trim();
      if (gRaw) {
        const mapped = GENDER_CSV_MAP[gRaw.toLowerCase()];
        if (!mapped) return { ok: false, error: `gender not recognized (M/F/X/PNTS or full words): ${gRaw}` };
        gender = mapped;
      }
      const pace_scy_100 = (raw.pace_scy_100 || "").trim() || null;
      const pace_scm_100 = (raw.pace_scm_100 || "").trim() || null;
      const pace_lcm_100 = (raw.pace_lcm_100 || "").trim() || null;
      for (const [k, v] of [["pace_scy_100", pace_scy_100], ["pace_scm_100", pace_scm_100], ["pace_lcm_100", pace_lcm_100]]) {
        if (v && v.length > 8) return { ok: false, error: `${k} too long (>8 chars)` };
      }
      const parental_contact = (raw.parental_contact || "").trim() || null;
      if (parental_contact && parental_contact.length > 255) return { ok: false, error: "parental_contact too long (>255 chars)" };

      return { ok: true, warning, normalized: {
        ...nameFields, initials, dob, gender,
        pace_scy_100, pace_scm_100, pace_lcm_100,
        parental_contact,
      } };
    }

    function buildTemplateCSV() {
      const header = IMPORT_NEW_HEADERS.join(",");
      // Column order matches IMPORT_NEW_HEADERS:
      // first_name,last_name,preferred_name,dob,gender,initials,pace_scy_100,pace_scm_100,pace_lcm_100,parental_contact
      const example = "Sarah,Johnson,,2010-03-15,F,SJ,2:05,,,parent@example.com";
      return header + "\n" + example + "\n";
    }

    function downloadTemplate() {
      const blob = new Blob([buildTemplateCSV()], { type: "text/csv" });
      const url  = URL.createObjectURL(blob);
      const a    = document.createElement("a");
      a.href = url; a.download = "setforge-swimmer-import-template.csv";
      document.body.appendChild(a); a.click();
      setTimeout(() => { URL.revokeObjectURL(url); a.remove(); }, 0);
    }

    function BulkImportModal({ teams = [], onClose, onImported }) {
      const [text,    setText]    = React.useState("");
      const [parsed,  setParsed]  = React.useState(null);                    // null = unparsed; [] = empty; [{idx, raw, norm}] = parsed
      const [result,  setResult]  = React.useState(null);                    // server response after submit
      const [submitting, setSubmitting] = React.useState(false);
      const [err,     setErr]     = React.useState(null);
      // Batch-level team picker. Default: coach's only team if there's exactly
      // one; otherwise empty ("no team"). All imported swimmers in this run
      // get the same team affiliation. Per-row team_id NOT supported (would
      // require a column in the CSV; over-engineered for v1).
      const [teamId,  setTeamId]  = React.useState(() => (teams.length === 1 ? teams[0].id : ""));

      const onFile = (e) => {
        const f = e.target.files?.[0];
        if (!f) return;
        const reader = new FileReader();
        reader.onload = () => { setText(String(reader.result || "")); setParsed(null); setResult(null); setErr(null); };
        reader.onerror = () => setErr("Could not read file.");
        reader.readAsText(f);
      };

      const parse = () => {
        setErr(null); setResult(null);
        const p = csvParseSwimmers(text);
        if (!p.ok) { setErr(p.error); setParsed(null); return; }
        const rows = p.dataRows.map((raw, idx) => {
          const v = normalizeImportRow(raw);
          return { idx, raw, normalized: v.ok ? v.normalized : null, error: v.ok ? null : v.error, warning: v.ok ? (v.warning || null) : null };
        });
        setParsed(rows);
      };

      const submit = async () => {
        if (!parsed) return;
        const validRows = parsed.filter(r => !r.error).map(r => r.normalized);
        if (validRows.length === 0) { setErr("No valid rows to import."); return; }
        setSubmitting(true); setErr(null);
        try {
          const res = await fetch("/api/managed-swimmers/bulk", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ rows: validRows, team_id: teamId || null }),
          });
          const j = await res.json();
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setResult(j);
          if (onImported) onImported(j);
        } catch (e) { setErr(e.message); }
        finally { setSubmitting(false); }
      };

      const validCount   = parsed ? parsed.filter(r => !r.error).length : 0;
      const invalidCount = parsed ? parsed.filter(r =>  r.error).length : 0;
      const warnCount    = parsed ? parsed.filter(r => !r.error && r.warning).length : 0;

      return (
        <div style={{ position: "fixed", inset: 0, background: "rgba(15,23,42,0.85)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 200, padding: 16, overflow: "auto" }}>
          <div style={{ background: "var(--color-card)", border: "1px solid var(--color-border-strong)", borderRadius: 12, padding: 22, maxWidth: 720, width: "100%", maxHeight: "90vh", overflow: "auto", boxShadow: "0 20px 60px rgba(0,0,0,0.4)" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 10 }}>
              <h3 style={{ color: "var(--color-text)", margin: 0 }}>📥 Bulk import swimmers</h3>
              <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: "none", color: "var(--color-text-muted)", fontSize: 18, cursor: "pointer" }}>✕</button>
            </div>

            {result ? (
              <div>
                <div style={{ padding: "12px 14px", background: "#22c55e22", border: "1px solid #22c55e", borderRadius: 8, marginBottom: 12 }}>
                  <div style={{ color: "var(--color-positive)", fontWeight: 700, fontSize: 14 }}>Imported {result.inserted} swimmer{result.inserted === 1 ? "" : "s"}</div>
                  {result.errors && result.errors.length > 0 && (
                    <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 6 }}>
                      {result.errors.length} row{result.errors.length === 1 ? "" : "s"} rejected by server:
                      <ul style={{ margin: "4px 0 0 20px", padding: 0 }}>
                        {result.errors.map(e => (
                          <li key={e.row_idx}>Row {e.row_idx + 1}: {e.error}</li>
                        ))}
                      </ul>
                    </div>
                  )}
                </div>
                <button onClick={onClose}
                  style={{ padding: "7px 14px", background: "var(--color-primary)", color: "var(--color-bg)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer" }}>
                  Done
                </button>
              </div>
            ) : (
              <>
                <div style={{ fontSize: 12, color: "var(--color-text-muted)", marginBottom: 10, lineHeight: 1.5 }}>
                  Required: <code>first_name</code> + <code>dob</code> (<code>last_name</code> recommended). Optional: <code>preferred_name</code>, <code>initials</code>, <code>gender</code>, <code>pace_scy_100</code>, <code>pace_scm_100</code>, <code>pace_lcm_100</code>, <code>parental_contact</code>. A legacy single <code>name</code> column still works — it's split on the last space, flagged with a ⚠ so you can verify. Headers are case-insensitive; extra columns are ignored. DOB accepts <code>YYYY-MM-DD</code> or <code>MM/DD/YYYY</code>.
                </div>

                {teams.length > 0 && (
                  <div style={{ background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 6, padding: 10, marginBottom: 10 }}>
                    <label style={{ fontSize: 11, color: "var(--color-text-muted)", display: "block", marginBottom: 4 }}>Attach all imported swimmers to:</label>
                    <select value={teamId} onChange={e => setTeamId(e.target.value)}
                      style={{ width: "100%", padding: "6px 10px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                      <option value="">— no team (free-floating roster) —</option>
                      {teams.map(t => (
                        <option key={t.id} value={t.id}>{t.name}</option>
                      ))}
                    </select>
                  </div>
                )}

                <div style={{ display: "flex", gap: 8, marginBottom: 10, alignItems: "center", flexWrap: "wrap" }}>
                  <label style={{ padding: "5px 11px", background: "var(--color-border)", color: "#cbd5e1", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>
                    📁 Choose CSV file
                    <input type="file" accept=".csv,.tsv,.txt" onChange={onFile} style={{ display: "none" }} />
                  </label>
                  <button onClick={downloadTemplate}
                    style={{ padding: "5px 11px", background: "transparent", color: "var(--color-primary)", border: "1px solid var(--color-primary)", borderRadius: 6, fontSize: 12, cursor: "pointer" }}>
                    📄 Download template
                  </button>
                </div>

                <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginBottom: 4 }}>Or paste CSV/TSV here:</label>
                <textarea value={text} onChange={e => { setText(e.target.value); setParsed(null); setErr(null); }}
                  placeholder="first_name,last_name,preferred_name,dob,gender,initials,pace_scy_100,pace_scm_100,pace_lcm_100,parental_contact&#10;Sarah,Johnson,,2010-03-15,F,SJ,2:05,,,parent@example.com"
                  rows={8}
                  style={{ width: "100%", padding: "8px 10px", fontSize: 12, fontFamily: "monospace", background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, resize: "vertical", boxSizing: "border-box" }} />

                <div style={{ display: "flex", gap: 8, marginTop: 10, alignItems: "center" }}>
                  <button onClick={parse} disabled={!text.trim()}
                    style={{ padding: "6px 14px", background: text.trim() ? "var(--color-primary)" : "var(--color-border-strong)", color: text.trim() ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: text.trim() ? "pointer" : "not-allowed" }}>
                    Parse
                  </button>
                  {parsed && (
                    <span style={{ fontSize: 12, color: "var(--color-text-muted)" }}>
                      {validCount} valid · {invalidCount > 0 ? <span style={{ color: "#fca5a5" }}>{invalidCount} invalid</span> : "0 invalid"}
                      {warnCount > 0 && <span style={{ color: "#f59e0b" }}> · {warnCount} name-split ⚠</span>}
                    </span>
                  )}
                </div>

                {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 8 }}>{err}</div>}

                {parsed && parsed.length > 0 && (
                  <div style={{ marginTop: 14, maxHeight: 260, overflow: "auto", border: "1px solid var(--color-border)", borderRadius: 6 }}>
                    <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                      <thead style={{ background: "var(--color-bg)", position: "sticky", top: 0 }}>
                        <tr>
                          <th style={{ padding: "6px 8px", textAlign: "left", color: "var(--color-text-muted)", fontWeight: 700 }}>#</th>
                          <th style={{ padding: "6px 8px", textAlign: "left", color: "var(--color-text-muted)", fontWeight: 700 }}>Status</th>
                          <th style={{ padding: "6px 8px", textAlign: "left", color: "var(--color-text-muted)", fontWeight: 700 }}>Name</th>
                          <th style={{ padding: "6px 8px", textAlign: "left", color: "var(--color-text-muted)", fontWeight: 700 }}>DOB</th>
                          <th style={{ padding: "6px 8px", textAlign: "left", color: "var(--color-text-muted)", fontWeight: 700 }}>Notes</th>
                        </tr>
                      </thead>
                      <tbody>
                        {parsed.map(row => (
                          <tr key={row.idx} style={{ background: row.error ? "#dc262611" : "transparent", borderTop: "1px solid var(--color-border)" }}>
                            <td style={{ padding: "5px 8px", color: "var(--color-text-dim)" }}>{row.idx + 1}</td>
                            <td style={{ padding: "5px 8px" }}>
                              {row.error
                                ? <span style={{ color: "var(--color-destructive)" }}>✗</span>
                                : row.warning
                                  ? <span style={{ color: "#f59e0b" }}>⚠</span>
                                  : <span style={{ color: "var(--color-positive)" }}>✓</span>}
                            </td>
                            <td style={{ padding: "5px 8px", color: "var(--color-text)" }}>
                              {row.raw.first_name ? `${row.raw.first_name} ${row.raw.last_name || ""}`.trim() : (row.raw.name || "—")}
                            </td>
                            <td style={{ padding: "5px 8px", color: "#cbd5e1", fontFamily: "monospace" }}>
                              {row.normalized?.dob || row.raw.dob || "—"}
                            </td>
                            <td style={{ padding: "5px 8px", color: row.error ? "#fca5a5" : (row.warning ? "#f59e0b" : "var(--color-text-dim)"), fontSize: 11 }}>
                              {row.error || row.warning || (row.normalized?.initials ? `initials: ${row.normalized.initials}` : "")}
                            </td>
                          </tr>
                        ))}
                      </tbody>
                    </table>
                  </div>
                )}

                {parsed && parsed.length > 0 && (
                  <div style={{ display: "flex", gap: 8, marginTop: 12 }}>
                    <button onClick={submit} disabled={submitting || validCount === 0}
                      style={{ padding: "7px 14px", background: validCount > 0 ? "var(--color-positive)" : "var(--color-border-strong)", color: validCount > 0 ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: (validCount > 0 && !submitting) ? "pointer" : "not-allowed" }}>
                      {submitting ? "Importing…" : `Import ${validCount} swimmer${validCount === 1 ? "" : "s"}`}
                    </button>
                    <button onClick={onClose}
                      style={{ padding: "7px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 6, fontSize: 13, cursor: "pointer" }}>
                      Cancel
                    </button>
                  </div>
                )}
              </>
            )}
          </div>
        </div>
      );
    }

    // DOB soft-prompt modal (decision #37). Surfaces when authenticated and
    // me.dob is null. Dismissable; sessionStorage prevents re-nagging within
    // a single browser session. Hard gates (group join, coach invite accept)
    // will re-prompt unconditionally when they ship in R-F.
    const DOB_PROMPT_DISMISS_KEY = "setforge.dob_prompt_dismissed_v1";

    function DobPromptModal({ onSaved, onDismiss }) {
      const [dob, setDob] = React.useState("");
      const [err, setErr] = React.useState(null);
      const [saving, setSaving] = React.useState(false);
      const save = async () => {
        if (!dob) { setErr("Please enter your DOB or tap 'Remind me later'."); return; }
        setSaving(true);
        try {
          const res = await fetch("/api/me/dob", {
            method:  "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body:    JSON.stringify({ dob }),
          });
          if (!res.ok) {
            const j = await res.json().catch(() => ({}));
            throw new Error(j.error || `HTTP ${res.status}`);
          }
          onSaved(dob);
        } catch (e) {
          setErr(e.message);
        } finally { setSaving(false); }
      };
      return (
        <div style={{ position: "fixed", inset: 0, background: "rgba(15,23,42,0.85)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 200, padding: 16 }}>
          <div style={{ background: "var(--color-card)", border: "1px solid var(--color-border-strong)", borderRadius: 12, padding: 24, maxWidth: 460, width: "100%", boxShadow: "0 20px 60px rgba(0,0,0,0.4)" }}>
            <h3 style={{ color: "var(--color-text)", marginTop: 0 }}>Quick question — your date of birth</h3>
            <p style={{ color: "#cbd5e1", fontSize: 13, lineHeight: 1.6 }}>
              We're rolling out coaching features (teams, groups, workouts assigned to you). To make sure the right compliance rules apply — especially for minor athletes — we need your DOB on file.
            </p>
            <p style={{ color: "var(--color-text-muted)", fontSize: 12, lineHeight: 1.5 }}>
              Your DOB is private. Only your age and a minor/adult status indicator are visible to other coaches; the raw date is only visible to you.
            </p>
            <label style={{ display: "block", fontSize: 11, color: "var(--color-text-muted)", marginTop: 14, marginBottom: 4 }}>Date of birth</label>
            <input type="date" value={dob} onChange={e => setDob(e.target.value)}
              min={DOB_MIN} max={DOB_MAX_TODAY()}
              style={{ width: "100%", padding: "7px 11px", fontSize: 14, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6 }} />
            {err && <div style={{ color: "#fca5a5", fontSize: 12, marginTop: 8 }}>{err}</div>}
            <div style={{ display: "flex", gap: 8, marginTop: 16, justifyContent: "flex-end" }}>
              <button onClick={onDismiss} disabled={saving}
                style={{ padding: "7px 14px", background: "var(--color-border)", color: "#cbd5e1", border: "none", borderRadius: 6, fontSize: 13, cursor: "pointer" }}>
                Remind me later
              </button>
              <button onClick={save} disabled={saving || !dob}
                style={{ padding: "7px 14px", background: dob ? "var(--color-positive)" : "var(--color-border-strong)", color: dob ? "var(--color-bg)" : "var(--color-text-muted)", border: "none", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: dob ? "pointer" : "not-allowed" }}>
                {saving ? "Saving…" : "Save"}
              </button>
            </div>
          </div>
        </div>
      );
    }

    function CatalogView({ favorites, onToggleFavorite, favoriteSets, disfavorSets, onCycleSetStatus, ugcOverlay }) {
      const [poolMode,    setPoolMode]    = React.useState("25y");
      const [section,     setSection]     = React.useState("main");
      const [typeId,      setTypeId]      = React.useState("all");
      const [searchQuery, setSearchQuery] = React.useState("");
      const [equipFilter, setEquipFilter] = React.useState(() => new Set()); // empty = no filter
      const [zoneFilter,  setZoneFilter]  = React.useState(() => new Set()); // empty = no filter
      const [expandedKey, setExpandedKey] = React.useState(null);
      const [printAll,    setPrintAll]    = React.useState(false);
      // Phase II — flag-for-review: stores prefill values for FeedbackModal.
      // null = modal closed; object with { subject, body } = modal open.
      const [flagging,    setFlagging]    = React.useState(null);

      const isTyped = (section === "drill" || section === "main");

      // 1. Get authored options for the selected (section, typeId, poolMode).
      // 2. Filter by search query, equipment, zone — combined with AND.
      const filtered = React.useMemo(() => {
        const list = getCatalogList(section, isTyped ? typeId : null, poolMode, ugcOverlay);
        const q = searchQuery.trim().toLowerCase();
        return list.filter(opt => {
          // Label search
          if (q) {
            const labelText = (opt.label || `${section} #${opt._idx}`).toLowerCase();
            if (!labelText.includes(q)) return false;
          }
          // Equipment filter — option must satisfy at least one of the selected
          // equipment items (OR within the equipment chips).
          if (equipFilter.size > 0) {
            let any = false;
            for (const eq of equipFilter) if (catalogOptionUsesEquip(opt, eq)) { any = true; break; }
            if (!any) return false;
          }
          // Zone filter — option's inferred block zone must be in the set.
          if (zoneFilter.size > 0) {
            const z = inferBlockZone({ section, sets: opt.sets, primary_zone: opt.primary_zone });
            if (!zoneFilter.has(z)) return false;
          }
          return true;
        });
      }, [section, typeId, poolMode, searchQuery, equipFilter, zoneFilter, isTyped]);

      const tabStyle = (active, color = "var(--color-primary)") => ({
        padding: "6px 12px", border: "none", borderRadius: 6,
        background: active ? color : "var(--color-card)",
        color: active ? "var(--color-bg)" : "var(--color-text-muted)",
        fontSize: 12, fontWeight: 700, cursor: "pointer",
        marginRight: 6, marginBottom: 6,
      });

      const toggleSetMember = (set, key, setter) => {
        const next = new Set(set);
        if (next.has(key)) next.delete(key); else next.add(key);
        setter(next);
      };

      // Phase II — open feedback modal prefilled with the option's identity.
      // Coach can append "what to review" detail before submitting.
      const openFlag = React.useCallback((opt) => {
        const labelText = opt.label || `(unlabeled ${section} #${opt._idx})`;
        const subject = `Catalog flag: ${labelText} [${poolMode}/${section}${opt._typeId ? "/" + opt._typeId : ""}]`;
        const body = [
          "Flagged from catalog browse.",
          "",
          `Label:       ${opt.label || "(unlabeled)"}`,
          `Pool mode:   ${poolMode}`,
          `Section:     ${section}`,
          ...(opt._typeId ? [`Type:        ${opt._typeId}`] : []),
          `Index:       ${opt._idx}`,
          `Total:       ${opt.totalYards} ${poolMode === "25y" ? "yd" : "m"}`,
          `Sets:        ${(opt.sets || []).length}`,
          "",
          "What to review (please fill in):",
          "",
        ].join("\n");
        setFlagging({ subject, body });
      }, [section, poolMode]);

      // Print: expand everything, trigger window.print, then collapse back.
      const handlePrint = React.useCallback(() => {
        setPrintAll(true);
        setTimeout(() => {
          window.print();
          setTimeout(() => setPrintAll(false), 250);
        }, 100);
      }, []);

      const unitLabel = (poolMode === "25y") ? "yd" : "m";

      return (
        <div>
          <div className="screen-only">
            <h2 style={{ color: "var(--color-text)", marginTop: 0, display: "inline-block", marginRight: 16 }}>📚 Workout Catalog</h2>
            <button onClick={handlePrint}
              style={{ padding: "6px 12px", borderRadius: 6, border: "1px solid var(--color-border)",
                       background: "var(--color-card)", color: "#cbd5e1", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
              🖨 Print catalog (expanded)
            </button>
            <p style={{ color: "var(--color-text-muted)", fontSize: 13, marginTop: 8, marginBottom: 8 }}>
              Read-only browse of the workout-set bank as authored. No cross-unit conversion or fallback — see what's actually in each bucket per pool mode.
            </p>
            <p className="card" style={{ color: "var(--color-warn)", fontSize: 12, marginTop: 0, marginBottom: 16, padding: "8px 12px", borderRadius: 6 }}>
              ⚠️ All intervals are calibrated for a <strong>masters baseline of 2:00/100yd</strong> (≈2:11/100m for SCM).
              On the workout page, set your actual pace and click <em>Rescale</em> to scale every interval proportionally.
              Faster swimmers tighten; slower swimmers loosen.
            </p>
          </div>
          <div className="print-only" style={{ marginBottom: 16 }}>
            <h2>Workout Catalog — {poolMode} · {section}{isTyped ? ` · ${typeId}` : ""}</h2>
          </div>

          {/* Pool mode toggle */}
          <div className="screen-only" style={{ marginBottom: 12 }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Mode</span>
            {["25y", "25m", "50m"].map(m => (
              <button key={m} onClick={() => { setPoolMode(m); setExpandedKey(null); }} style={tabStyle(poolMode === m, "var(--color-primary)")}>{m}</button>
            ))}
          </div>

          {/* Section filter */}
          <div className="screen-only" style={{ marginBottom: 12 }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Section</span>
            {CATALOG_SECTIONS.map(s => (
              <button key={s} onClick={() => { setSection(s); setExpandedKey(null); }} style={tabStyle(section === s, "var(--color-positive)")}>
                {s.charAt(0).toUpperCase() + s.slice(1)}
              </button>
            ))}
          </div>

          {/* Type filter — only meaningful for drill/main */}
          {isTyped && (
            <div className="screen-only" style={{ marginBottom: 12 }}>
              <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Type</span>
              {CATALOG_TYPES.map(t => (
                <button key={t} onClick={() => { setTypeId(t); setExpandedKey(null); }} style={tabStyle(typeId === t, "var(--color-primary)")}>{t}</button>
              ))}
            </div>
          )}

          {/* Equipment filter chips */}
          <div className="screen-only" style={{ marginBottom: 12 }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Equipment</span>
            {EQUIPMENT_LIST.map(eq => (
              <button key={eq.id}
                onClick={() => toggleSetMember(equipFilter, eq.id, setEquipFilter)}
                title={eq.description}
                style={tabStyle(equipFilter.has(eq.id), "var(--color-warn)")}>
                {eq.icon} {eq.label}
              </button>
            ))}
            {equipFilter.size > 0 && (
              <button onClick={() => setEquipFilter(new Set())}
                style={{ ...tabStyle(false), color: "var(--color-warn)", fontStyle: "italic" }}>
                clear
              </button>
            )}
          </div>

          {/* Zone filter chips */}
          <div className="screen-only" style={{ marginBottom: 12 }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Zone</span>
            {ZONE_ORDER.map(zid => {
              const z = ZONES[zid];
              return (
                <button key={zid}
                  onClick={() => toggleSetMember(zoneFilter, zid, setZoneFilter)}
                  style={{ ...tabStyle(zoneFilter.has(zid), z.color),
                           color: zoneFilter.has(zid) ? "var(--color-bg)" : z.color,
                           border: `1px solid ${z.color}` }}>
                  {z.label}
                </button>
              );
            })}
            {zoneFilter.size > 0 && (
              <button onClick={() => setZoneFilter(new Set())}
                style={{ ...tabStyle(false), color: "var(--color-text-muted)", fontStyle: "italic" }}>
                clear
              </button>
            )}
          </div>

          {/* Search input */}
          <div className="screen-only" style={{ marginBottom: 16 }}>
            <span style={{ color: "var(--color-text-dim)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.08em", marginRight: 10 }}>Search label</span>
            <input
              type="text" value={searchQuery}
              onChange={e => setSearchQuery(e.target.value)}
              onKeyDown={e => { if (e.key === "Escape") setSearchQuery(""); }}
              placeholder="e.g. CSS, IM, sprint…"
              style={{ padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)",
                       background: "var(--color-bg)", color: "var(--color-text)", fontSize: 12, width: 220 }}
            />
          </div>

          {/* Result count */}
          <div className="screen-only" style={{ color: "var(--color-text-dim)", fontSize: 12, marginBottom: 12 }}>
            {filtered.length} option{filtered.length === 1 ? "" : "s"}
          </div>

          {/* List */}
          {filtered.length === 0 ? (
            <div style={{ color: "var(--color-text-muted)", fontStyle: "italic", padding: 20, textAlign: "center", border: "1px dashed var(--color-border)", borderRadius: 8 }}>
              No options match. (Try clearing filters or switching section/mode.)
            </div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
              {filtered.map(opt => {
                const key       = `${opt._typeId || section}-${opt._idx}`;
                const expanded  = printAll || expandedKey === key;
                const declared  = opt.totalYards || 0;
                const actual    = (opt.sets || []).reduce((s, st) => s + (st.reps || 1) * (st.dist || 0), 0);
                const mismatch  = declared !== actual;
                const labelText = opt.label || `(unlabeled ${section} #${opt._idx})`;
                const blockZ    = inferBlockZone({ section, sets: opt.sets, primary_zone: opt.primary_zone });
                const zoneInfo  = ZONES[blockZ] || null;

                return (
                  <div key={key} className="catalog-option"
                    className="card" style={{ borderRadius: 8 }}>
                    {/* Row header — clickable to expand */}
                    <div onClick={() => setExpandedKey(expanded && !printAll ? null : key)}
                      style={{
                        padding: "10px 14px", display: "flex", alignItems: "center", gap: 12,
                        flexWrap: "wrap", cursor: printAll ? "default" : "pointer",
                      }}>
                      <span className="screen-only" style={{ color: "var(--color-text-dim)", fontSize: 12, width: 12 }}>{expanded ? "▾" : "▸"}</span>
                      <strong style={{ color: "var(--color-text)", fontSize: 14 }}>{labelText}</strong>
                      {opt._typeId && (
                        <span style={{ background: "var(--color-border)", color: "var(--color-primary)", padding: "1px 7px", borderRadius: 4, fontSize: 10, fontWeight: 700 }}>
                          {opt._typeId}
                        </span>
                      )}
                      {zoneInfo && (
                        <span style={{ background: zoneInfo.color, color: "var(--color-bg)", padding: "1px 7px", borderRadius: 4, fontSize: 10, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.04em" }}>
                          {zoneInfo.label}
                        </span>
                      )}
                      <span style={{ color: "var(--color-text-muted)", fontSize: 12, fontFamily: "monospace" }}>
                        {declared.toLocaleString()} {unitLabel}
                      </span>
                      <span style={{ color: "var(--color-text-dim)", fontSize: 12 }}>
                        · {(opt.sets || []).length} set{(opt.sets || []).length === 1 ? "" : "s"}
                      </span>
                      {mismatch && (
                        <span title={`Σ reps×dist = ${actual} but totalYards = ${declared}`}
                          style={{ background: "#7f1d1d", color: "#fee2e2", padding: "1px 7px", borderRadius: 4, fontSize: 10, fontWeight: 700 }}>
                          ⚠ sum mismatch (actual {actual})
                        </span>
                      )}
                      {/* Favorite ★ — toggles the existing label-favorite. Hidden
                          when the option has no label or user not authenticated.
                          stopPropagation so click doesn't also toggle row expand. */}
                      {opt.label && onToggleFavorite && (() => {
                        const isFav = favorites && favorites.includes(opt.label);
                        return (
                          <button className="screen-only"
                            onClick={e => { e.stopPropagation(); onToggleFavorite(opt.label); }}
                            title={isFav ? "Remove from favorites" : "Save as favorite — generator will pick it more often (×3 weight)"}
                            style={{ marginLeft: "auto", padding: "2px 6px",
                                     background: "transparent", border: "none",
                                     color: isFav ? "var(--color-warn)" : "var(--color-text-muted)",
                                     fontSize: 18, lineHeight: 1, cursor: "pointer",
                                     opacity: isFav ? 1 : 0.6 }}>
                            {isFav ? "★" : "☆"}
                          </button>
                        );
                      })()}
                      {/* Phase II — flag for review. Pushes the option's identity
                          into a prefilled feedback submission. stopPropagation so
                          the click doesn't also toggle row expand/collapse. */}
                      <button className="screen-only"
                        onClick={e => { e.stopPropagation(); openFlag(opt); }}
                        title="Flag this option for review"
                        style={{ marginLeft: opt.label && onToggleFavorite ? 0 : "auto",
                                 padding: "4px 8px", borderRadius: 6,
                                 border: "1px solid var(--color-border-strong)", background: "transparent",
                                 color: "var(--color-text-muted)", fontSize: 12, cursor: "pointer", lineHeight: 1 }}>
                        🚩 Flag
                      </button>
                    </div>

                    {/* Expanded detail — full sets */}
                    {expanded && (
                      <div style={{ borderTop: "1px solid var(--color-border)", padding: "10px 14px", background: "var(--color-bg)" }}>
                        <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12, color: "#cbd5e1" }}>
                          <thead>
                            <tr style={{ color: "var(--color-text-dim)", textAlign: "left", fontSize: 10, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                              {onCycleSetStatus && <th style={{ padding: "4px 8px", width: 24 }}></th>}
                              <th style={{ padding: "4px 8px", whiteSpace: "nowrap" }}>Reps × Dist</th>
                              <th style={{ padding: "4px 8px", whiteSpace: "nowrap" }}>Interval</th>
                              <th style={{ padding: "4px 8px" }}>Description</th>
                              <th style={{ padding: "4px 8px" }}>Focus</th>
                            </tr>
                          </thead>
                          <tbody>
                            {opt.sets.map((s, i) => {
                              const eqItems = equipmentForSet(s, CATALOG_ALL_EQUIP);
                              // Gap-2 (2026-05-22) — per-set cycling button.
                              // Only fires when set has an `id` and user is
                              // authenticated. Mirrors SetRow's tri-state.
                              const isFav = !!(s.id && favoriteSets && favoriteSets.has(s.id));
                              const isDis = !!(s.id && disfavorSets && disfavorSets.has(s.id));
                              const cycleIcon  = isFav ? "★" : (isDis ? "👎" : "☆");
                              const cycleColor = isFav ? "var(--color-warn)" : (isDis ? "#ef4444" : "var(--color-text-dim)");
                              const cycleTitle = isFav
                                ? "Favorited — click to disfavor"
                                : (isDis ? "Disfavored — click to clear" : "Click to favorite");
                              return (
                                <tr key={i} style={{ borderTop: "1px solid var(--color-card)" }}>
                                  {onCycleSetStatus && (
                                    <td style={{ padding: "6px 4px", textAlign: "center" }}>
                                      {s.id ? (
                                        <button
                                          type="button"
                                          onClick={() => onCycleSetStatus(s.id)}
                                          title={cycleTitle}
                                          style={{
                                            background: "transparent", border: "none",
                                            color: cycleColor, fontSize: 14, lineHeight: 1,
                                            cursor: "pointer", padding: 2,
                                          }}>
                                          {cycleIcon}
                                        </button>
                                      ) : (
                                        <span style={{ color: "var(--color-text-dim)", opacity: 0.3, fontSize: 10 }} title="No set ID — cannot favorite">·</span>
                                      )}
                                    </td>
                                  )}
                                  <td style={{ padding: "6px 8px", whiteSpace: "nowrap", fontFamily: "monospace", fontWeight: 700, color: "var(--color-text)" }}>
                                    {s.reps > 1 ? `${s.reps}×${s.dist}` : `${s.dist}`} {unitLabel}
                                  </td>
                                  <td style={{ padding: "6px 8px", whiteSpace: "nowrap", fontFamily: "monospace", color: "var(--color-text-muted)" }}>
                                    {s.interval || "—"}
                                  </td>
                                  <td style={{ padding: "6px 8px" }}>
                                    {s.desc || "—"}
                                    {eqItems.length > 0 && (
                                      <span style={{ marginLeft: 6 }}>
                                        {eqItems.map(name => <EquipmentBadge key={name} name={name} />)}
                                      </span>
                                    )}
                                  </td>
                                  <td style={{ padding: "6px 8px", color: "var(--color-text-muted)", fontStyle: "italic" }}>
                                    {s.focus || "—"}
                                  </td>
                                </tr>
                              );
                            })}
                          </tbody>
                        </table>
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          )}

          {/* Phase II — flag-for-review modal (reuses FeedbackModal with prefill) */}
          {flagging && (
            <FeedbackModal
              onClose={() => setFlagging(null)}
              initialCategory="catalog-flag"
              initialSubject={flagging.subject}
              initialBody={flagging.body}
            />
          )}
        </div>
      );
    }

    // ─── My Sets (UGC authoring, Phase C) ────────────────────────────────
    // List + create + edit + delete coach-authored UGC bank options.
    // Phase C scope: visibility forced to 'private' server-side.
    // Spec: UGC_COACH_SETS_SCOPE.md §3.A + §3.C.
    function MySetsView({ onChanged }) {
      const [rows, setRows] = React.useState([]);
      const [loading, setLoading] = React.useState(true);
      const [err, setErr] = React.useState(null);
      const [formState, setFormState] = React.useState(null); // null | { mode: 'create' } | { mode: 'edit', option }
      const [deleting, setDeleting] = React.useState(null);

      const reload = React.useCallback(() => {
        setLoading(true);
        fetch(`${API_BASE}/bank-options/mine`, { cache: "no-store", credentials: "include" })
          .then(r => r.ok ? r.json() : Promise.reject(new Error(`HTTP ${r.status}`)))
          .then(data => {
            setRows(Array.isArray(data) ? data : []);
            setErr(null);
          })
          .catch(e => setErr(e.message || String(e)))
          .finally(() => setLoading(false));
      }, []);
      React.useEffect(() => { reload(); }, [reload]);

      const handleSaved = () => {
        setFormState(null);
        reload();
        if (onChanged) onChanged();  // refresh the App's overlay state
      };

      const handleDelete = async (option) => {
        if (!window.confirm(`Delete "${option.label}"? This cannot be undone.`)) return;
        setDeleting(option.id);
        try {
          const csrf = (await (await fetch('/api/auth/csrf')).json()).token;
          const r = await fetch(`${API_BASE}/bank-options/${option.id}`, {
            method: 'DELETE',
            headers: { 'X-CSRF-Token': csrf },
            credentials: 'include',
          });
          const j = await r.json();
          if (!r.ok) throw new Error(j.error || `HTTP ${r.status}`);
          reload();
          if (onChanged) onChanged();
        } catch (e) {
          window.alert(`Delete failed: ${e.message || String(e)}`);
        } finally {
          setDeleting(null);
        }
      };

      const handleEdit = async (option) => {
        // Fetch full option (with sets) before opening the form.
        try {
          const r = await fetch(`${API_BASE}/bank-options/${option.id}`, { credentials: 'include' });
          if (!r.ok) throw new Error(`HTTP ${r.status}`);
          const full = await r.json();
          setFormState({ mode: 'edit', option: full });
        } catch (e) {
          window.alert(`Load failed: ${e.message || String(e)}`);
        }
      };

      const fmtDate = (d) => {
        if (!d) return "—";
        const dt = new Date(d);
        return dt.toLocaleDateString() + " " + dt.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
      };

      return (
        <div style={{ padding: "16px 12px" }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 12 }}>
            <h2 style={{ margin: 0, fontSize: 18 }}>📝 My Sets</h2>
            <button
              onClick={() => setFormState({ mode: 'create' })}
              style={{
                padding: "8px 14px", borderRadius: 6, border: "1px solid var(--color-primary)",
                background: "var(--color-primary)", color: "white", fontWeight: 600, cursor: "pointer",
              }}
            >+ New set</button>
          </div>
          <p style={{ fontSize: 12, color: "var(--color-muted)", marginTop: 0, marginBottom: 16 }}>
            Author your own bank options. Currently private (only you see them in your picker).
            Team-sharing + public submission are coming in later phases.
          </p>
          {err && (
            <div style={{ padding: 8, marginBottom: 8, background: "rgba(239,68,68,0.1)", border: "1px solid #ef4444", borderRadius: 4, color: "#ef4444", fontSize: 13 }}>
              {err}
            </div>
          )}
          {loading && <p style={{ color: "var(--color-muted)" }}>Loading…</p>}
          {!loading && rows.length === 0 && (
            <div style={{ padding: 24, textAlign: "center", color: "var(--color-muted)", border: "1px dashed var(--color-border)", borderRadius: 8 }}>
              <p style={{ margin: 0 }}>No UGC sets yet. Tap <strong>+ New set</strong> to author your first one,</p>
              <p style={{ margin: "4px 0 0 0" }}>or use the 📥 snapshot button on a generated workout block.</p>
            </div>
          )}
          {!loading && rows.length > 0 && (
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
              <thead>
                <tr style={{ borderBottom: "1px solid var(--color-border)", textAlign: "left" }}>
                  <th style={{ padding: "6px 4px" }}>Label</th>
                  <th style={{ padding: "6px 4px" }}>Section</th>
                  <th style={{ padding: "6px 4px" }}>Pool</th>
                  <th style={{ padding: "6px 4px" }}>Type/Stroke</th>
                  <th style={{ padding: "6px 4px", textAlign: "right" }}>Yards</th>
                  <th style={{ padding: "6px 4px" }}>Visibility</th>
                  <th style={{ padding: "6px 4px" }}>Updated</th>
                  <th style={{ padding: "6px 4px", textAlign: "right" }}>Actions</th>
                </tr>
              </thead>
              <tbody>
                {rows.map(r => (
                  <tr key={r.id} style={{ borderBottom: "1px solid var(--color-border)" }}>
                    <td style={{ padding: "6px 4px", fontWeight: 500 }}>{r.label}</td>
                    <td style={{ padding: "6px 4px" }}>{r.section}</td>
                    <td style={{ padding: "6px 4px" }}>{r.pool_mode}</td>
                    <td style={{ padding: "6px 4px" }}>{[...(r.type_ids || []), ...(r.stroke_ids || [])].join(", ") || r.type_id || r.stroke_id || "—"}</td>
                    <td style={{ padding: "6px 4px", textAlign: "right" }}>{r.total_yards}</td>
                    <td style={{ padding: "6px 4px" }}>
                      {r.promoted_at ? (
                        <span title={`Promoted to JS at ${fmtDate(r.promoted_at)}`} style={{ color: "var(--color-success, #10b981)" }}>✅ promoted</span>
                      ) : r.visibility === "private" ? (
                        <span title="Only you see this in your picker">📝 private</span>
                      ) : r.visibility === "team" ? (
                        <span title="Shared with teams you coach">👥 team</span>
                      ) : r.visibility === "public" ? (
                        <span title="Approved by admin; visible to all SetForge coaches" style={{ color: "var(--color-primary)" }}>🌐 public</span>
                      ) : r.visibility === "pending" ? (
                        <span title="Submitted for admin review" style={{ color: "var(--color-warn)" }}>⏳ pending review</span>
                      ) : r.visibility === "rejected" ? (
                        <span title={r.latest_review_reason ? `Rejected: ${r.latest_review_reason}` : "Rejected by admin"} style={{ color: "#ef4444" }}>❌ rejected</span>
                      ) : (
                        <span>{r.visibility}</span>
                      )}
                    </td>
                    <td style={{ padding: "6px 4px", color: "var(--color-muted)" }}>{fmtDate(r.updated_at)}</td>
                    <td style={{ padding: "6px 4px", textAlign: "right" }}>
                      {!r.promoted_at && (
                        <>
                          <button
                            onClick={() => handleEdit(r)}
                            style={{ marginRight: 4, padding: "4px 8px", border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text)", borderRadius: 4, cursor: "pointer", fontSize: 12 }}
                          >Edit</button>
                          <button
                            disabled={deleting === r.id}
                            onClick={() => handleDelete(r)}
                            style={{ padding: "4px 8px", border: "1px solid #ef4444", background: "transparent", color: "#ef4444", borderRadius: 4, cursor: "pointer", fontSize: 12 }}
                          >{deleting === r.id ? "…" : "Delete"}</button>
                        </>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
          {formState && (
            <UgcFormModal
              option={formState.mode === 'edit' ? formState.option : null}
              onSave={handleSaved}
              onClose={() => setFormState(null)}
            />
          )}
        </div>
      );
    }

    // UGC author/edit form modal. Phase C: visibility forced to private
    // server-side regardless of selection (no team/public flow yet).
    // option=null → create; option={...} → edit (with pre-fill).
    // onSave called after a successful POST/PATCH (parent re-fetches list).
    function UgcFormModal({ option, onSave, onClose }) {
      // isEdit only if we have an option WITH a server id. A pre-filled
      // snapshot from the 📥 button passes a partial option (no id), which
      // should still be treated as create.
      const isEdit = !!(option && option.id);
      const [section, setSection] = React.useState(option?.section || "warmup");
      const [poolMode, setPoolMode] = React.useState(option?.pool_mode || "25y");
      // Phase H — multi-tag. Type + Stroke are arrays; an option can live
      // in multiple type/stroke buckets simultaneously. Fall back to
      // singleton type_id / stroke_id for snapshot pre-fills + edits of
      // pre-migration rows.
      const [typeIds, setTypeIds] = React.useState(() => {
        if (Array.isArray(option?.type_ids)) return [...option.type_ids];
        if (option?.type_id) return [option.type_id];
        return [];
      });
      const [strokeIds, setStrokeIds] = React.useState(() => {
        if (Array.isArray(option?.stroke_ids)) return [...option.stroke_ids];
        if (option?.stroke_id) return [option.stroke_id];
        return [];
      });
      const [label, setLabel] = React.useState(option?.label || "");
      const [totalYards, setTotalYards] = React.useState(option?.total_yards || 0);
      const [sets, setSets] = React.useState(option?.sets?.length
        ? option.sets.map(s => ({ ...s }))
        : [{ reps: 1, dist: 100, desc: "", interval: "On 2:00", focus: "" }]);
      // Phase D — visibility + team picker
      const [visibility, setVisibility] = React.useState(option?.visibility || "private");
      const [teamIds, setTeamIds] = React.useState(Array.isArray(option?.team_ids) ? [...option.team_ids] : []);
      const [allTeams, setAllTeams] = React.useState([]);
      React.useEffect(() => {
        fetch(`${API_BASE}/teams`, { credentials: "include", cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setAllTeams(data); })
          .catch(() => {});
      }, []);
      const [saving, setSaving] = React.useState(false);
      const [err, setErr] = React.useState(null);

      const needsType = (section === "drill" || section === "main");
      const computedTotal = sets.reduce((acc, s) => acc + (Number(s.reps) || 0) * (Number(s.dist) || 0), 0);

      // Auto-sync total_yards to the computed total. User can override but
      // the form recomputes on each set edit to avoid surprises at save.
      React.useEffect(() => {
        setTotalYards(computedTotal);
      }, [computedTotal]);

      const updateSet = (i, patch) => {
        setSets(prev => prev.map((s, j) => j === i ? { ...s, ...patch } : s));
      };
      // Autofocus the freshly-added row's first input after render. Counter
      // bumps on addSet; useEffect on the counter fires post-render.
      const newRowRepsRef = React.useRef(null);
      const [addCount, setAddCount] = React.useState(0);
      const addSet = () => {
        setSets(prev => [...prev, { reps: 1, dist: 100, desc: "", interval: "On 2:00", focus: "" }]);
        setAddCount(n => n + 1);
      };
      React.useEffect(() => {
        if (addCount > 0 && newRowRepsRef.current) {
          newRowRepsRef.current.focus();
          newRowRepsRef.current.select();  // select the "1" so user can type-replace
        }
      }, [addCount]);
      const removeSet = (i) => {
        if (sets.length === 1) return;  // keep at least 1
        setSets(prev => prev.filter((_, j) => j !== i));
      };

      // Interval normalizer: server requires `On M:SS` or `No interval...`.
      // Forgive common shapes: bare `1:30` → `On 1:30`; bare `none` → `No interval`.
      const normalizeInterval = (raw) => {
        const s = String(raw || "").trim();
        if (!s) return s;
        if (/^\d+:\d{2}$/.test(s)) return `On ${s}`;
        if (/^on \d+:\d{2}$/i.test(s)) return `On ${s.replace(/^on\s+/i, "")}`;  // case-fix
        if (/^(none|no interval)$/i.test(s)) return "No interval";
        return s;  // pass through; server validation will surface if still bad
      };
      const handleSubmit = async () => {
        setErr(null);
        // Client-side guard: team visibility requires at least one team.
        if (visibility === "team" && teamIds.length === 0) {
          setErr("Pick at least one team to share with, or change visibility to Private.");
          return;
        }
        setSaving(true);
        try {
          const csrf = (await (await fetch('/api/auth/csrf')).json()).token;
          // Client-side guard: drill/main need at least one type or stroke checked.
          if (needsType && typeIds.length === 0 && strokeIds.length === 0) {
            setErr("Pick at least one type or stroke for drill/main sections.");
            setSaving(false);
            return;
          }
          const payload = {
            section, pool_mode: poolMode,
            type_ids:   needsType ? typeIds   : [],
            stroke_ids: needsType ? strokeIds : [],
            label,
            total_yards: computedTotal,
            visibility,
            team_ids: visibility === "team" ? teamIds : [],
            sets: sets.map(s => ({
              reps: Number(s.reps), dist: Number(s.dist),
              desc: s.desc, interval: normalizeInterval(s.interval),
              focus: s.focus || null, stroke: s.stroke || null, eq: s.eq || null,
            })),
          };
          const url = isEdit
            ? `${API_BASE}/bank-options/${option.id}`
            : `${API_BASE}/bank-options`;
          const r = await fetch(url, {
            method: isEdit ? 'PATCH' : 'POST',
            headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': csrf },
            credentials: 'include',
            body: JSON.stringify(payload),
          });
          const j = await r.json();
          if (!r.ok) throw new Error(j.error || `HTTP ${r.status}`);
          onSave(j);
        } catch (e) {
          setErr(e.message || String(e));
        } finally {
          setSaving(false);
        }
      };

      const fieldStyle = { padding: "6px 8px", border: "1px solid var(--color-border)", borderRadius: 4, fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", minWidth: 0, boxSizing: "border-box" };
      const labelStyle = { display: "block", fontSize: 11, fontWeight: 600, color: "var(--color-muted)", marginBottom: 4, textTransform: "uppercase", letterSpacing: 0.5 };

      return (
        <div onClick={onClose} style={{
          position: "fixed", inset: 0, background: "rgba(0,0,0,0.6)", zIndex: 100,
          display: "flex", alignItems: "center", justifyContent: "center", padding: 16,
        }}>
          <div onClick={(e) => e.stopPropagation()} style={{
            background: "var(--color-bg)", color: "var(--color-text)", borderRadius: 8,
            padding: 20, maxWidth: 720, width: "100%", maxHeight: "90vh", overflow: "auto",
            border: "1px solid var(--color-border)",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
              <h3 style={{ margin: 0, fontSize: 16 }}>{isEdit ? "Edit set" : "New set"}</h3>
              <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: "none", color: "var(--color-muted)", fontSize: 18, cursor: "pointer" }}>✕</button>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10, marginBottom: 12 }}>
              <div>
                <label style={labelStyle}>Section</label>
                <select value={section} onChange={(e) => setSection(e.target.value)} style={{ ...fieldStyle, width: "100%" }}>
                  <option value="warmup">warmup</option>
                  <option value="drill">drill / pre-main</option>
                  <option value="main">main</option>
                  <option value="cooldown">cooldown</option>
                </select>
              </div>
              <div>
                <label style={labelStyle}>Pool mode</label>
                <select value={poolMode} onChange={(e) => setPoolMode(e.target.value)} style={{ ...fieldStyle, width: "100%" }}>
                  <option value="25y">25y</option>
                  <option value="25m">25m</option>
                  <option value="50m">50m</option>
                </select>
              </div>
            </div>
            {/* Phase H — multi-tag type + stroke. Check any combination
                so a single set (e.g. a kick set) can land in distance
                AND sprint AND endurance buckets without duplication. */}
            {needsType && (
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginBottom: 12 }}>
                <div>
                  <label style={labelStyle}>Type (multi-select)</label>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 8, padding: 6, border: "1px solid var(--color-border)", borderRadius: 4, background: "var(--color-card)" }}>
                    {["im","distance","sprint","endurance","mixed","technique"].map(t => (
                      <label key={t} style={{ display: "flex", alignItems: "center", gap: 3, fontSize: 12, cursor: "pointer" }}>
                        <input type="checkbox" checked={typeIds.includes(t)}
                          onChange={(e) => {
                            if (e.target.checked) setTypeIds(prev => prev.includes(t) ? prev : [...prev, t]);
                            else setTypeIds(prev => prev.filter(x => x !== t));
                          }} />
                        {t}
                      </label>
                    ))}
                  </div>
                </div>
                <div>
                  <label style={labelStyle}>Stroke (multi-select)</label>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 8, padding: 6, border: "1px solid var(--color-border)", borderRadius: 4, background: "var(--color-card)" }}>
                    {["back","breast","fly","free","im"].map(s => (
                      <label key={s} style={{ display: "flex", alignItems: "center", gap: 3, fontSize: 12, cursor: "pointer" }}>
                        <input type="checkbox" checked={strokeIds.includes(s)}
                          onChange={(e) => {
                            if (e.target.checked) setStrokeIds(prev => prev.includes(s) ? prev : [...prev, s]);
                            else setStrokeIds(prev => prev.filter(x => x !== s));
                          }} />
                        {s}
                      </label>
                    ))}
                  </div>
                </div>
              </div>
            )}
            <div style={{ marginBottom: 12 }}>
              <label style={labelStyle}>Label</label>
              <input value={label} onChange={(e) => setLabel(e.target.value)} maxLength={120}
                style={{ ...fieldStyle, width: "100%" }} placeholder="e.g. Build 8×50" />
            </div>
            {/* Phase D + E — visibility picker. Public goes through admin
                moderation queue (server coerces to 'pending'). */}
            <div style={{ marginBottom: 12 }}>
              <label style={labelStyle}>Visibility</label>
              <div style={{ display: "flex", flexWrap: "wrap", gap: 16, alignItems: "center", fontSize: 13 }}>
                <label style={{ display: "flex", alignItems: "center", gap: 4, cursor: "pointer" }}>
                  <input type="radio" name="ugc-visibility" checked={visibility === "private"}
                    onChange={() => setVisibility("private")} /> 📝 Private (only you)
                </label>
                <label style={{ display: "flex", alignItems: "center", gap: 4, cursor: allTeams.length === 0 ? "not-allowed" : "pointer", opacity: allTeams.length === 0 ? 0.4 : 1 }}
                  title={allTeams.length === 0 ? "You need to coach at least one team to share." : ""}>
                  <input type="radio" name="ugc-visibility" checked={visibility === "team"}
                    disabled={allTeams.length === 0}
                    onChange={() => setVisibility("team")} /> 👥 Team-shared
                </label>
                <label style={{ display: "flex", alignItems: "center", gap: 4, cursor: "pointer" }}>
                  <input type="radio" name="ugc-visibility" checked={visibility === "public" || visibility === "pending"}
                    onChange={() => setVisibility("public")} /> 🌐 Public (admin review)
                </label>
              </div>
              {(visibility === "public" || visibility === "pending") && (
                <p style={{ fontSize: 11, color: "var(--color-muted)", margin: "6px 0 0 0", fontStyle: "italic" }}>
                  Public submissions wait in an admin review queue before going live to all SetForge coaches.
                </p>
              )}
              {visibility === "team" && (
                <div style={{ marginTop: 8, padding: 8, border: "1px solid var(--color-border)", borderRadius: 4, background: "var(--color-card)" }}>
                  <div style={{ ...labelStyle, marginBottom: 6 }}>Share with teams</div>
                  {allTeams.length === 0 ? (
                    <p style={{ fontSize: 12, color: "var(--color-muted)", margin: 0 }}>(no teams)</p>
                  ) : (
                    <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
                      {allTeams.map(t => (
                        <label key={t.id} style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 13, cursor: "pointer" }}>
                          <input type="checkbox"
                            checked={teamIds.includes(t.id)}
                            onChange={(e) => {
                              if (e.target.checked) setTeamIds(prev => prev.includes(t.id) ? prev : [...prev, t.id]);
                              else setTeamIds(prev => prev.filter(id => id !== t.id));
                            }} />
                          <span>{t.name}</span>
                          <span style={{ fontSize: 11, color: "var(--color-muted)" }}>· {t.team_type}</span>
                        </label>
                      ))}
                    </div>
                  )}
                  {teamIds.length === 0 && allTeams.length > 0 && (
                    <p style={{ fontSize: 11, color: "#ef4444", margin: "6px 0 0 0" }}>Pick at least one team.</p>
                  )}
                </div>
              )}
            </div>
            <div style={{ marginBottom: 6, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
              <label style={{ ...labelStyle, marginBottom: 0 }}>Sets (total {computedTotal} yd)</label>
              <button onClick={addSet} style={{ padding: "4px 10px", fontSize: 12, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text)", borderRadius: 4, cursor: "pointer" }}>+ Add row</button>
            </div>
            <p style={{ fontSize: 11, color: "var(--color-muted)", margin: "0 0 6px 0" }}>
              Interval format: <code>On 1:30</code> (we'll add "On" if you skip it) or <code>No interval</code> for untimed sets.
            </p>
            <div style={{ marginBottom: 16 }}>
              {sets.map((s, i) => (
                <div key={i} className="editor-row" style={{ display: "grid", gridTemplateColumns: "60px 60px 1fr 100px 1fr 90px 30px", gap: 6, marginBottom: 6, alignItems: "start" }}>
                  <input type="number" min="1" value={s.reps} onChange={(e) => updateSet(i, { reps: e.target.value })} placeholder="reps" style={fieldStyle}
                    ref={i === sets.length - 1 ? newRowRepsRef : null} />
                  <input type="number" min="1" value={s.dist} onChange={(e) => updateSet(i, { dist: e.target.value })} placeholder="dist" style={fieldStyle} />
                  <input className="editor-row-wide" value={s.desc} onChange={(e) => updateSet(i, { desc: e.target.value })} placeholder="Description" style={fieldStyle} maxLength={500} />
                  <input className="editor-row-wide" value={s.interval} onChange={(e) => updateSet(i, { interval: e.target.value })} placeholder="On 2:00" style={fieldStyle} />
                  <input className="editor-row-wide" value={s.focus || ""} onChange={(e) => updateSet(i, { focus: e.target.value })} placeholder="Focus (optional)" style={fieldStyle} maxLength={240} />
                  <select value={s.eq || ""} onChange={(e) => updateSet(i, { eq: e.target.value || null })} style={fieldStyle} title="Equipment required for this set">
                    <option value="">— eq —</option>
                    <option value="kick">kick</option>
                    <option value="pull">pull</option>
                    <option value="snorkel">snorkel</option>
                    <option value="fins">fins</option>
                    <option value="underkick">underkick</option>
                  </select>
                  <button onClick={() => removeSet(i)} disabled={sets.length === 1} style={{ background: "transparent", border: "none", color: sets.length === 1 ? "var(--color-border)" : "#ef4444", cursor: sets.length === 1 ? "default" : "pointer", fontSize: 16 }}>✕</button>
                </div>
              ))}
            </div>
            {err && (
              <div style={{ padding: 8, marginBottom: 12, background: "rgba(239,68,68,0.1)", border: "1px solid #ef4444", borderRadius: 4, color: "#ef4444", fontSize: 13 }}>
                {err}
              </div>
            )}
            <div style={{ display: "flex", justifyContent: "flex-end", gap: 8 }}>
              <button onClick={onClose} disabled={saving}
                style={{ padding: "8px 16px", border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text)", borderRadius: 6, cursor: "pointer" }}>
                Cancel
              </button>
              <button onClick={handleSubmit} disabled={saving || !label.trim() || sets.length === 0}
                style={{ padding: "8px 16px", border: "1px solid var(--color-primary)", background: "var(--color-primary)", color: "white", borderRadius: 6, cursor: saving ? "default" : "pointer", opacity: saving ? 0.7 : 1 }}>
                {saving ? "Saving…" : isEdit ? "Save changes" : "Create"}
              </button>
            </div>
          </div>
        </div>
      );
    }


    // Phase E — admin moderation queue for public UGC submissions.
    // Lists visibility='pending' rows with author info + preview +
    // approve/reject controls. Reject requires a reason.

    // Phase F — Graduate-to-JS admin tab. Lists visibility='public' AND
    // promoted_at IS NULL rows. Per-row Graduate button opens UgcGraduateModal
    // which shows the JS snippet + paste instructions. Admin pastes locally,
    // commits, pushes, then confirms → server stamps promoted_at and the
    // row leaves this list (also leaves the overlay endpoint per visibility
    // scoping).

    // Two-step graduate modal. Fetches snippet on open; admin copies +
    // commits + pushes locally + waits for Hyperlift redeploy, then
    // clicks "Confirm" which POSTs the graduate route → stamps promoted_at.

    // Session 5: admin feedback tab. Lists feedback rows, filterable by status.
    // Each row has inline status pills + admin note edit. Default filter "new".




    // Audit log group chips. Each maps to the server-side AUDIT_EVENT_GROUPS
    // patterns. Default ON for everything except impersonation (the
    // impersonation.access per-request rows are extremely high volume and
    // bury other events; admin can re-enable when investigating support
    // sessions). Saved per-session in component state.
    // Admin-only email-test surface. Fires POST /api/admin/email/test which
    // routes through enqueueEmail (lib/email.js) — same path as the real
    // sign-up welcome. The worker picks the row up within ~30s on its next
    // tick. Result display surfaces whichever branch enqueueEmail returned:
    //   { id }              — queued; email arrives within ~30s
    //   { bypassed: ... }   — minor-bypass triggered; check your DOB on file
    //   { skipped: ... }    — EMAIL_ACTIVE=false, no email, etc

    // Admin-only Stripe-config diagnostic. Hits GET /api/admin/billing/config
    // which returns billingConfigState() from lib/billing.js — booleans for
    // each field's presence (NOT the secret values). Lets us confirm
    // at-a-glance whether STRIPE_CONFIG parsed cleanly + every field landed
    // without grepping Hyperlift logs or echoing secrets to stdout.

    // Phase 3 vendor paper kit (deliverable 3 of 4).
    // Per VENDOR_PAPER_KIT_SCOPE.md §3.5. Server route POST
    // /api/admin/vendor-kit/send returns the rendered cover-letter +
    // attachment filenames; the client opens a mailto: with subject +
    // body pre-filled. Cap'n attaches PDFs from vendor-kit/build/
    // manually in his mail client. Audit log captures the send.


    function RestPickerModal({ restSecs, onChange, onStart, onCancel, audioCues, onAudioCuesChange, lapButton, onLapButtonChange }) {
      return (
        <div style={{
          position: "fixed", inset: 0, zIndex: 9998,
          background: "rgba(0,0,0,0.75)",
          display: "flex", alignItems: "center", justifyContent: "center",
          padding: 24,
          fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
        }}>
          <div style={{
            background: "var(--color-card)", borderRadius: 20, padding: "28px 24px",
            width: "100%", maxWidth: 360, border: "1px solid var(--color-border)",
          }}>
            <div style={{ fontSize: 20, fontWeight: 800, color: "var(--color-text)", marginBottom: 4 }}>
              🏊 Start Workout
            </div>
            <div style={{ fontSize: 13, color: "var(--color-text-dim)", marginBottom: 20 }}>
              Rest between sets
            </div>
            <div style={{ display: "flex", gap: 8, marginBottom: 18 }}>
              {REST_OPTIONS.map(o => {
                const active = restSecs === o.value;
                return (
                  <button key={String(o.value)} onClick={() => onChange(o.value)} style={{
                    flex: 1, padding: "10px 0", borderRadius: 10,
                    border: `2px solid ${active ? "var(--color-primary)" : "var(--color-border)"}`,
                    background: active ? "#1e3a5f" : "var(--color-bg)",
                    color: active ? "var(--color-primary)" : "var(--color-text-dim)",
                    fontSize: 13, fontWeight: 700, cursor: "pointer",
                  }}>
                    {o.label}
                  </button>
                );
              })}
            </div>
            {/* W1: audio-cue toggle */}
            {onAudioCuesChange && (
              <button onClick={() => {
                // Prime the audio context on the user gesture before toggling on,
                // and play a one-beat preview so the user hears what they enabled.
                if (!audioCues) { primeAudioCtx(); playRestCue("beep"); }
                onAudioCuesChange(!audioCues);
              }}
                title="Three beeps at T-3/T-2/T-1, distinct tone at go"
                style={{
                  width: "100%", marginBottom: 22,
                  display: "flex", alignItems: "center", justifyContent: "space-between",
                  padding: "10px 14px", borderRadius: 10,
                  border: `1px solid ${audioCues ? "var(--color-primary)" : "var(--color-border)"}`,
                  background: audioCues ? "#1e3a5f" : "var(--color-bg)",
                  color: audioCues ? "var(--color-primary)" : "var(--color-text-dim)",
                  fontSize: 13, fontWeight: 700, cursor: "pointer",
                }}>
                <span>🔔 Audio cues</span>
                <span style={{ fontSize: 11, color: audioCues ? "var(--color-primary)" : "var(--color-border-strong)" }}>
                  {audioCues ? "ON · preview ▶" : "OFF"}
                </span>
              </button>
            )}
            {/* Run-screen v1: lap-button toggle. When OFF, PaceClockView drops
                the ✓ Lap action and the splits feedback pill; ⏸ Pause becomes
                the primary action. Default ON preserves existing UX. */}
            {onLapButtonChange && (
              <button onClick={() => onLapButtonChange(!lapButton)}
                title="✓ Lap button during sets — tap each rep to record split times. Off = timer auto-advances on hit, no manual lap input."
                style={{
                  width: "100%", marginBottom: 22,
                  display: "flex", alignItems: "center", justifyContent: "space-between",
                  padding: "10px 14px", borderRadius: 10,
                  border: `1px solid ${lapButton ? "var(--color-primary)" : "var(--color-border)"}`,
                  background: lapButton ? "#1e3a5f" : "var(--color-bg)",
                  color: lapButton ? "var(--color-primary)" : "var(--color-text-dim)",
                  fontSize: 13, fontWeight: 700, cursor: "pointer",
                }}>
                <span>✋ Lap button</span>
                <span style={{ fontSize: 11, color: lapButton ? "var(--color-primary)" : "var(--color-border-strong)" }}>
                  {lapButton ? "ON · per-rep splits" : "OFF · auto-advance only"}
                </span>
              </button>
            )}
            <div style={{ display: "flex", gap: 10 }}>
              <button onClick={onCancel} style={{
                flex: 1, padding: "14px 0", borderRadius: 12,
                border: "1px solid var(--color-border)", background: "transparent",
                color: "var(--color-text-dim)", fontSize: 15, fontWeight: 600, cursor: "pointer",
              }}>
                Cancel
              </button>
              <button onClick={() => {
                // Prime audio context on the user gesture — first play after this
                // (the first T-3 beep) will work even on iOS Safari.
                if (audioCues) primeAudioCtx();
                onStart();
              }} style={{
                flex: 2, padding: "14px 0", borderRadius: 12,
                border: "none", background: "var(--color-primary)",
                color: "#fff", fontSize: 15, fontWeight: 700, cursor: "pointer",
              }}>
                ▶ Start
              </button>
            </div>
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  RUN WORKOUT OVERLAY
    //  Full-screen step-through: one section at a time, iPhone-sized.
    // ═══════════════════════════════════════════════════════════════

    const SECTION_EMOJIS = { warmup: "🌊", drill: "🎯", kick: "🦵", main: "💪", cooldown: "🧊" };

    function RunWorkoutOverlay({ workout, restSecs, onClose, onLogAsToday, personalFirstName, audioCues = false, lapButton = true, unit = "yds" }) {
      const [idx, setIdx]               = useState(0);
      const [paceIdx, setPaceIdx]       = useState(null);
      const [elapsed, setElapsed]       = useState(0); // seconds since workout started
      const [autoStartNext, setAutoStartNext] = useState(false);
      const [showFinish, setShowFinish] = useState(false);
      const [finishDifficulty, setFinishDifficulty] = useState(null);
      const [logState, setLogState]     = useState("idle"); // idle | saving | saved | error
      const [logError, setLogError]     = useState(null);
      // E: flat array of all splits captured this session — {blockIdx, setIdx, rep, actual_secs, expected_secs, delta}
      const [allSplits, setAllSplits]   = useState([]);
      // Section model B3 — dryland checklist tick state (keyed "blockIdx:exIdx").
      const [drylandChecked, setDrylandChecked] = useState(() => new Set());
      const blocks  = workout.blocks;

      // Wall-clock elapsed timer — pauses once the workout is finished
      useEffect(() => {
        if (showFinish) return;
        const id = setInterval(() => setElapsed(e => e + 1), 1000);
        return () => clearInterval(id);
      }, [showFinish]);

      const allSets = useMemo(() =>
        blocks.flatMap((block, bIdx) =>
          (block.sets || []).map((set, sIdx) => ({ set, blockIdx: bIdx, setIdx: sIdx }))  // dryland: no swim sets
        ), [blocks]);

      // Marks the workout as completed — show the Finish screen instead of closing.
      const finishWorkout = useCallback(() => {
        setPaceIdx(null);
        setAutoStartNext(false);
        setShowFinish(true);
      }, []);

      const handlePaceNext = useCallback(() => {
        if (paceIdx + 1 < allSets.length) {
          const next = allSets[paceIdx + 1];
          setIdx(next.blockIdx);
          setPaceIdx(paceIdx + 1);
          setAutoStartNext(restSecs !== null); // auto-start if not manual
        } else {
          finishWorkout();
        }
      }, [paceIdx, allSets, restSecs, finishWorkout]);

      const handleLogClick = useCallback(async () => {
        if (!onLogAsToday) { onClose(); return; }
        setLogState("saving"); setLogError(null);
        const res = await onLogAsToday(workout, {
          difficulty: finishDifficulty || undefined,
          // E: bundle any captured splits into the new entry
          actualSplits: allSplits.length > 0 ? allSplits : undefined,
        });
        if (res.ok) {
          setLogState("saved");
          setTimeout(() => onClose(), 900);
        } else {
          setLogState("error");
          setLogError(res.error || "Save failed");
        }
      }, [onLogAsToday, workout, finishDifficulty, onClose, allSplits]);


      // Manual tap on a set card — never auto-starts
      function handleSetTap(globalIdx) {
        setPaceIdx(globalIdx);
        setAutoStartNext(false);
      }
      const block   = blocks[idx];
      const total   = blocks.length;
      const isFirst = idx === 0;
      const isLast  = idx === total - 1;
      const s       = SECTION_STYLES[block.section] || SECTION_STYLES.main;
      const emoji   = SECTION_EMOJIS[block.section] || "🏊";

      return (
        <div style={{
          position: "fixed", inset: 0, zIndex: 9999,
          background: "var(--color-bg)", overflowY: "auto",
          display: "flex", flexDirection: "column",
          fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
        }}>
          {/* Top bar: close + progress dots + counter */}
          <div style={{
            display: "flex", justifyContent: "space-between", alignItems: "center",
            padding: "16px 20px", borderBottom: "1px solid var(--color-card)",
            position: "sticky", top: 0, background: "var(--color-bg)", zIndex: 1,
          }}>
            <button onClick={onClose} style={{
              background: "transparent", border: "none", color: "var(--color-text-muted)",
              fontSize: 28, lineHeight: 1, cursor: "pointer",
              padding: "4px 8px", borderRadius: 8, minWidth: 44, minHeight: 44,
            }}>✕</button>
            <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
              {blocks.map((_, i) => (
                <div key={i} style={{
                  width: i === idx ? 28 : 8, height: 8, borderRadius: 999,
                  background: i === idx ? "var(--color-primary)" : "var(--color-border)",
                  transition: "width 0.2s",
                }} />
              ))}
            </div>
            <div style={{ fontSize: 13, color: "var(--color-text-dim)", fontWeight: 600, minWidth: 44, textAlign: "right" }}>
              {idx + 1}/{total}
            </div>
          </div>

          {/* Section model B3 — dryland blocks render a tick-off checklist
              instead of the pace-card sets. */}
          {block.kind === "dryland" ? (
          <>
            <div style={{ background: "#f0e2cc", padding: "24px 20px 20px" }}>
              <div style={{ fontSize: 12, fontWeight: 700, color: "#7a4a12", textTransform: "uppercase", letterSpacing: "0.12em", opacity: 0.7, marginBottom: 6 }}>
                Section {idx + 1} of {total} · Dryland
              </div>
              <div style={{ fontSize: 34, fontWeight: 900, color: "#7a4a12", lineHeight: 1.1, marginBottom: 6 }}>
                🏋 {block.name}
              </div>
              <div style={{ fontSize: 15, color: "#7a4a12", opacity: 0.85, fontWeight: 600 }}>
                {block.placement === "post" ? "After pool" : "Before pool"} · tap to check off
              </div>
            </div>
            <div style={{ flex: 1, padding: "16px 20px", display: "flex", flexDirection: "column", gap: 10 }}>
              {(block.exercises || []).map((ex, i) => {
                const ck = `${idx}:${i}`;
                const done = drylandChecked.has(ck);
                return (
                  <div key={i}
                    onClick={() => setDrylandChecked(prev => { const n = new Set(prev); n.has(ck) ? n.delete(ck) : n.add(ck); return n; })}
                    style={{ display: "flex", alignItems: "center", gap: 14, background: "var(--color-card)", borderRadius: 14,
                      border: `1px solid ${done ? "#16a34a" : "var(--color-border)"}`, padding: "16px 18px", cursor: "pointer",
                      WebkitTapHighlightColor: "transparent", opacity: done ? 0.6 : 1, minHeight: 54 }}>
                    <div style={{ width: 28, height: 28, borderRadius: 8, flexShrink: 0,
                      border: `2px solid ${done ? "#16a34a" : "var(--color-border-strong)"}`,
                      background: done ? "#16a34a" : "transparent", color: "#fff",
                      display: "flex", alignItems: "center", justifyContent: "center", fontSize: 16 }}>{done ? "✓" : ""}</div>
                    <div style={{ flex: 1 }}>
                      <div style={{ fontSize: 22, fontWeight: 800, color: "#fff", lineHeight: 1.1, textDecoration: done ? "line-through" : "none" }}>
                        {ex.sets > 1 ? `${ex.sets} × ${ex.reps}` : ex.reps}
                      </div>
                      <div style={{ fontSize: 16, color: "var(--color-text)", marginTop: 2 }}>
                        {ex.name}{ex.rest ? <span style={{ color: "var(--color-text-dim)", fontSize: 13 }}> · rest {ex.rest}</span> : null}
                      </div>
                    </div>
                  </div>
                );
              })}
            </div>
          </>
          ) : (
          <>
          {/* Section header */}
          <div style={{ background: s.headerBg, padding: "24px 20px 20px" }}>
            <div style={{ fontSize: 12, fontWeight: 700, color: s.headerText, textTransform: "uppercase", letterSpacing: "0.12em", opacity: 0.7, marginBottom: 6 }}>
              Section {idx + 1} of {total}
            </div>
            <div style={{ fontSize: 34, fontWeight: 900, color: s.headerText, lineHeight: 1.1, marginBottom: 6 }}>
              {emoji} {block.name}
            </div>
            <div style={{ fontSize: 15, color: s.headerText, opacity: 0.85, fontWeight: 600 }}>
              {block.totalYards.toLocaleString()} {unit}
            </div>
            {workout.focusNote && (
              <div style={{ marginTop: 10, fontSize: 14, color: s.headerText, opacity: 0.92, fontStyle: "italic", fontWeight: 600 }}>
                🎯 {workout.focusNote}
              </div>
            )}
          </div>

          {/* Sets — tap any card to open the pace clock */}
          <div style={{ flex: 1, padding: "16px 20px", display: "flex", flexDirection: "column", gap: 12 }}>
            {(block.sets || []).map((set, i) => (
              <div key={i}
                onClick={() => handleSetTap(blocks.slice(0, idx).reduce((acc, b) => acc + (b.sets ? b.sets.length : 0), 0) + i)}
                style={{
                  background: "var(--color-card)", borderRadius: 14,
                  border: `1px solid ${s.border}`, padding: "18px 18px 14px",
                  cursor: "pointer", WebkitTapHighlightColor: "transparent",
                }}>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
                  <div style={{ fontSize: 30, fontWeight: 900, color: "#fff", marginBottom: 8, letterSpacing: "-0.02em", lineHeight: 1 }}>
                    {set.reps} <span style={{ color: "var(--color-border-strong)", fontWeight: 400 }}>×</span> {set.dist}
                    <span style={{ fontSize: 16, fontWeight: 500, color: "var(--color-text-dim)", marginLeft: 6 }}>{unit}</span>
                  </div>
                  <span style={{ fontSize: 11, color: "var(--color-border)", fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.08em", paddingTop: 4 }}>
                    ▶ pace
                  </span>
                </div>
                <div style={{ fontSize: 19, color: "var(--color-text)", lineHeight: 1.45, marginBottom: 10 }}>
                  {set.desc}
                </div>
                <div style={{ display: "flex", flexWrap: "wrap", gap: 8, alignItems: "baseline" }}>
                  <span style={{
                    background: "var(--color-bg)", color: "#38bdf8",
                    padding: "4px 12px", borderRadius: 999,
                    fontSize: 16, fontWeight: 700, fontFamily: "monospace",
                    whiteSpace: "nowrap",
                  }}>
                    ⏱ {set.interval}
                  </span>
                  <span style={{ color: "var(--color-text-muted)", fontSize: 16, fontStyle: "italic", lineHeight: 1.4 }}>
                    {set.focus}
                  </span>
                </div>
              </div>
            ))}
          </div>
          </>
          )}

          {/* Pace clock — rendered over this overlay when a set is tapped */}
          {paceIdx !== null && (
            <PaceClockView
              key={paceIdx}
              set={allSets[paceIdx].set}
              sectionName={workout.blocks[allSets[paceIdx].blockIdx].name}
              focusNote={workout.focusNote}
              onClose={() => setPaceIdx(null)}
              onNext={handlePaceNext}
              onWorkoutFinish={finishWorkout}
              onLap={(split) => setAllSplits(prev => [...prev, {
                blockIdx: allSets[paceIdx].blockIdx,
                setIdx:   allSets[paceIdx].setIdx,
                ...split,
              }])}
              isLastSet={paceIdx === allSets.length - 1}
              restSecs={restSecs}
              autoStart={autoStartNext}
              elapsed={elapsed}
              audioCues={audioCues}
              lapButton={lapButton}
              unit={unit}
            />
          )}

          {/* Navigation */}
          <div style={{
            display: "flex", gap: 12, padding: "16px 20px 36px",
            borderTop: "1px solid var(--color-card)", background: "var(--color-bg)",
            position: "sticky", bottom: 0,
          }}>
            {!isFirst ? (
              <button onClick={() => setIdx(i => i - 1)} style={{
                flex: 1, padding: "17px 0", borderRadius: 14,
                border: "1px solid var(--color-border)", background: "var(--color-card)",
                color: "var(--color-text)", fontSize: 17, fontWeight: 700, cursor: "pointer",
                minHeight: 54,
              }}>
                ← Previous
              </button>
            ) : (
              <div style={{ flex: 1 }} />
            )}
            <button onClick={() => isLast ? finishWorkout() : setIdx(i => i + 1)} style={{
              flex: isFirst ? 2 : 1, padding: "17px 0", borderRadius: 14,
              border: "none",
              background: isLast ? "#16a34a" : "var(--color-primary)",
              color: "#fff", fontSize: 17, fontWeight: 700, cursor: "pointer",
              boxShadow: "0 4px 16px rgba(0,0,0,0.35)",
              minHeight: 54,
            }}>
              {isLast ? "✓ Finish" : "Next →"}
            </button>
          </div>

          {/* Finish screen — appears after the user reaches the last set / clicks Finish */}
          {showFinish && (
            <div style={{
              position: "fixed", inset: 0, zIndex: 10001,
              background: "rgba(2,6,23,0.96)",
              display: "flex", alignItems: "center", justifyContent: "center",
              padding: 20,
              fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
            }}>
              <div style={{
                width: "100%", maxWidth: 380,
                background: "var(--color-bg)", border: "1px solid var(--color-card)",
                borderRadius: 16, padding: "28px 22px 22px",
                textAlign: "center",
              }}>
                <div style={{ fontSize: 56, marginBottom: 8 }}>🎉</div>
                <div style={{ fontSize: 22, fontWeight: 900, color: "#fff", marginBottom: 4 }}>
                  {personalFirstName ? `Nice work, ${personalFirstName}!` : "Workout complete!"}
                </div>
                <div style={{ fontSize: 13, color: "var(--color-text-dim)", marginBottom: 20 }}>
                  {workout.totalYards.toLocaleString()} {unit} · {Math.floor(elapsed / 60)}:{String(elapsed % 60).padStart(2, "0")}
                  {allSplits.length > 0 && (
                    <> · <span style={{ color: "#86efac", fontWeight: 700 }}>{allSplits.length} split{allSplits.length === 1 ? "" : "s"} captured</span></>
                  )}
                </div>

                {/* Difficulty rating — optional, captured right after the swim while it's fresh */}
                <div style={{ marginBottom: 18 }}>
                  <div style={{ fontSize: 11, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 6 }}>
                    How hard was it? <span style={{ color: "var(--color-border-strong)", textTransform: "none", letterSpacing: 0 }}>(optional)</span>
                  </div>
                  <div style={{ display: "inline-flex" }}>
                    <StarRating
                      value={finishDifficulty}
                      onChange={setFinishDifficulty}
                      size={28}
                      allowToggleClear
                    />
                  </div>
                </div>

                {logState === "saved" ? (
                  <div style={{ color: "#4ade80", fontSize: 15, fontWeight: 700, padding: "10px 0" }}>
                    ✓ Logged to your history{allSplits.length > 0 ? " (with splits)" : ""}
                  </div>
                ) : (
                  <>
                    <button
                      onClick={handleLogClick}
                      disabled={logState === "saving"}
                      style={{
                        width: "100%", padding: "14px 0", borderRadius: 12, border: "none",
                        background: logState === "saving" ? "#1e3a5f" : "var(--color-primary)",
                        color: "#fff", fontSize: 15, fontWeight: 800,
                        cursor: logState === "saving" ? "default" : "pointer",
                        marginBottom: 10,
                      }}>
                      {logState === "saving"
                        ? "Saving…"
                        : allSplits.length > 0
                          ? `Log as today's session (with ${allSplits.length} split${allSplits.length === 1 ? "" : "s"})`
                          : "Log as today's session"}
                    </button>
                    {logState === "error" && (
                      <div style={{ color: "#fca5a5", fontSize: 12, marginBottom: 10 }}>
                        {logError}
                      </div>
                    )}
                    <button
                      onClick={onClose}
                      style={{
                        background: "transparent", border: "none",
                        color: "var(--color-text-dim)", fontSize: 13, cursor: "pointer",
                        padding: "6px 12px",
                      }}>
                      Close without logging
                    </button>
                  </>
                )}
              </div>
            </div>
          )}
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  ACCESS GATE
    // ═══════════════════════════════════════════════════════════════

    // SignInGate is the public-facing surface for setforge.io. It serves three
    // audiences in one page: (1) existing users with invites who just want to
    // sign in, (2) cold prospects who landed without context, (3) prospects who
    // bounced off a coach's recommendation and need to request access.
    //
    // Phase 1 item 5 (PHASED_PLAN_2026-05-25.md): expanded from "logo + sign-in
    // button on a gradient" into a real marketing landing. Cap'n homework: the
    // screenshot below is a stylized mock — swap for a real screenshot when
    // available (PHASED_PLAN §8 open follow-up).
    //
    // Copy decisions locked as defaults (revise freely):
    //   - Hero tagline: "Swim workouts in seconds." (matches og-image.png)
    //   - Subtitle: "Generator and pace clock for coaches and their swimmers."
    //   - 3 value props: free-for-swimmers-forever / no-ads-trackers-passwords /
    //     coach-pays-$10 + solo-founder. Marketing's "lean-in" set.
    //   - Primary CTA: Sign in with Apple (existing). Secondary: "No invite?
    //     Request one" mailto. Google OAuth slot pre-wired but commented out.
    function SignInGate({ authError }) {
      // Provider-neutral invite landing: an invite URL of the form
      // setforge.io/?invite=CODE lets the user pick Apple OR Google,
      // and we forward the code to whichever auth start route they tap.
      // Used by AdminInvites "Copy link" (which now emits /?invite=CODE
      // instead of provider-specific /api/auth/apple?invite=CODE).
      const inviteParam = (typeof window !== "undefined")
        ? (new URLSearchParams(window.location.search).get("invite") || "")
        : "";
      const appleHref  = inviteParam ? `/api/auth/apple?invite=${encodeURIComponent(inviteParam)}`   : "/api/auth/apple";
      const googleHref = inviteParam ? `/api/auth/google?invite=${encodeURIComponent(inviteParam)}`  : "/api/auth/google";
      return (
        <div style={{
          minHeight: "100vh",
          background: "linear-gradient(135deg, var(--color-bg) 0%, #1e3a5f 50%, var(--color-bg) 100%)",
          fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
          display: "flex", flexDirection: "column",
        }}>
          {/* ─── Top bar: wordmark left, nav right ─── */}
          <header style={{
            display: "flex", alignItems: "center", justifyContent: "space-between",
            padding: "20px 32px",
            maxWidth: 1200, width: "100%", margin: "0 auto", boxSizing: "border-box",
          }}>
            <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
              <img src="/icons/icon-512.svg" alt="SetForge logo" width="36" height="36" />
              <span style={{
                fontFamily: '"Inter Tight", Inter, sans-serif', fontSize: 22, fontWeight: 800,
                color: "#fff", letterSpacing: "-0.02em",
              }}>SetForge</span>
            </div>
            <nav aria-label="Marketing pages" style={{ display: "flex", gap: 18, fontSize: 14 }}>
              <a href="/pricing.html" style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Pricing</a>
              <a href="/about.html"   style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>About</a>
              <a href="/security.html"style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Security</a>
              <a href="/manual.html"  style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Manual</a>
            </nav>
          </header>

          {/* ─── Hero: 2-col on desktop, 1-col on mobile ─── */}
          <main style={{
            flex: 1, display: "grid", gap: 48, alignItems: "center",
            padding: "32px 32px 64px", maxWidth: 1200, width: "100%",
            margin: "0 auto", boxSizing: "border-box",
            gridTemplateColumns: "minmax(0, 1fr) minmax(0, 1fr)",
          }} className="signin-hero-grid">
            {/* ─── Left col: value prop + CTAs ─── */}
            <div>
              <h1 style={{
                fontFamily: '"Inter Tight", Inter, sans-serif',
                fontSize: "clamp(40px, 6vw, 64px)", fontWeight: 800,
                color: "#fff", margin: "0 0 16px", letterSpacing: "-0.03em",
                lineHeight: 1.05,
              }}>
                Swim workouts<br />in seconds.
              </h1>
              <p style={{
                color: "var(--color-text)", fontSize: 18, lineHeight: 1.5,
                margin: "0 0 32px", maxWidth: 480,
              }}>
                Generator and pace clock for coaches and their swimmers. Built by one swim coach; honest about what it is and isn't.
              </p>

              {/* Three value props */}
              <ul style={{
                listStyle: "none", padding: 0, margin: "0 0 36px",
                display: "flex", flexDirection: "column", gap: 12,
              }}>
                {[
                  { icon: "🆓", text: <><strong style={{color:"#fff"}}>Free for swimmers, forever.</strong> Written into our Terms — not just a marketing promise.</> },
                  { icon: "🛡️", text: <><strong style={{color:"#fff"}}>No ads, no trackers, no passwords.</strong> OAuth sign-in only; we can't lose what we never had.</> },
                  { icon: "✉️", text: <><strong style={{color:"#fff"}}>Coach pays $10/mo. One person answers the email.</strong> No support queue, no enterprise sales call.</> },
                ].map((vp, i) => (
                  <li key={i} style={{
                    display: "flex", gap: 12, alignItems: "flex-start",
                    color: "var(--color-text-muted)", fontSize: 15, lineHeight: 1.5,
                  }}>
                    <span aria-hidden="true" style={{ fontSize: 18, flexShrink: 0 }}>{vp.icon}</span>
                    <span>{vp.text}</span>
                  </li>
                ))}
              </ul>

              {/* Auth-error surface — preserved from prior SignInGate */}
              {authError && (
                <div role="alert" style={{
                  color: "#f87171", fontSize: 13, marginBottom: 20,
                  background: "rgba(239,68,68,0.1)", borderRadius: 8, padding: "10px 14px",
                  maxWidth: 360,
                }}>
                  {authError === "not_authorized"
                    ? "Your account isn't on the access list. Email hello@competitionaquatics.com to request an invite."
                    : "Sign-in failed — please try again."}
                </div>
              )}

              {/* CTAs: primary Sign in (existing flow), secondary Request invite */}
              <div style={{ display: "flex", flexWrap: "wrap", gap: 12, alignItems: "center" }}>
                <a href={appleHref} style={{ textDecoration: "none" }} aria-label="Sign in with Apple">
                  <div style={{
                    display: "flex", alignItems: "center", justifyContent: "center", gap: 10,
                    background: "#fff", color: "#000", borderRadius: 12,
                    padding: "14px 28px", fontSize: 17, fontWeight: 600,
                    cursor: "pointer", userSelect: "none",
                    boxShadow: "0 1px 3px rgba(0,0,0,0.3)",
                    minHeight: 48,
                  }}>
                    <svg width="20" height="20" viewBox="0 0 814 1000" fill="currentColor" aria-hidden="true">
                      <path d="M788.1 340.9c-5.8 4.5-108.2 62.2-108.2 190.5 0 148.4 130.3 200.9 134.2 202.2-.6 3.2-20.7 71.9-68.7 141.9-42.8 61.6-87.5 123.1-155.5 123.1s-85.5-39.5-164-39.5c-76.5 0-103.7 40.8-165.9 40.8s-105-37.3-155.8-109.3C107.3 729.3 75 608.4 75 490.5c0-173.5 113.4-265.5 224.8-265.5 58.2 0 106.8 38.9 142.5 38.9 33.8 0 87.4-41.2 154.5-41.2 24.5 0 108.2 3.9 161.7 59.4zm-326.3-191.4c30.6-35.8 52.4-85.6 52.4-135.4 0-6.9-.6-13.9-1.9-19.5-49.6 1.9-108.9 33.1-145.5 73.8-27.5 30.1-53.1 79.7-53.1 130.3 0 7.5 1.3 15 1.9 17.4 3.2.6 8.4 1.3 13.6 1.3 44.6 0 100.7-29.5 132.6-67.9z"/>
                    </svg>
                    Sign in with Apple
                  </div>
                </a>
                {/* Sign in with Google (Phase 2 · GOOGLE_OAUTH_SCOPE.md).
                    Same redirect pattern as Apple. White button with the
                    multicolor "G" mark — Google's official identity guidelines
                    permit the white-on-white variant when the wordmark is
                    "Sign in with Google" verbatim. */}
                <a href={googleHref} style={{ textDecoration: "none" }} aria-label="Sign in with Google">
                  <div style={{
                    display: "flex", alignItems: "center", justifyContent: "center", gap: 10,
                    background: "#fff", color: "#1f1f1f", borderRadius: 12,
                    padding: "14px 28px", fontSize: 17, fontWeight: 500,
                    fontFamily: '"Roboto", "Helvetica Neue", Arial, sans-serif',
                    cursor: "pointer", userSelect: "none",
                    boxShadow: "0 1px 3px rgba(0,0,0,0.3)",
                    minHeight: 48,
                  }}>
                    <svg width="20" height="20" viewBox="0 0 48 48" aria-hidden="true">
                      <path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/>
                      <path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"/>
                      <path fill="#FBBC05" d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"/>
                      <path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/>
                      <path fill="none" d="M0 0h48v48H0z"/>
                    </svg>
                    Sign in with Google
                  </div>
                </a>
                {/* Tertiary CTA: mailto fallback for users who don't have an
                    invite yet. After Google OAuth lands, the invite-gate stays
                    in place (per GOOGLE_OAUTH_SCOPE.md decision 5). */}
                <a href="mailto:hello@competitionaquatics.com?subject=SetForge%20invite%20request&body=Hi%20%E2%80%94%20I'd%20like%20an%20invite%20to%20SetForge.%20A%20bit%20about%20me%3A%20"
                   style={{
                     color: "var(--color-text)", fontSize: 14, fontWeight: 600,
                     textDecoration: "none", padding: "14px 16px",
                     border: "1px solid var(--color-border)", borderRadius: 12,
                     minHeight: 48, display: "inline-flex", alignItems: "center",
                   }}>
                  No invite? Request one →
                </a>
              </div>

              <div style={{ marginTop: 24, fontSize: 12, color: "var(--color-text-dim)" }}>
                By signing in you agree to our{" "}
                <a href="/terms.html" style={{ color: "var(--color-text-muted)", textDecoration: "underline" }}>Terms</a>
                {" "}and{" "}
                <a href="/privacy.html" style={{ color: "var(--color-text-muted)", textDecoration: "underline" }}>Privacy Policy</a>.
              </div>
            </div>

            {/* ─── Right col: visual proof. STYLIZED MOCK — Cap'n homework:
                 replace with a real screenshot when available. PHASED_PLAN
                 §8 open follow-up. ─── */}
            <div style={{
              background: "var(--color-card)",
              border: "1px solid var(--color-border)",
              borderRadius: 16,
              padding: 20,
              boxShadow: "0 20px 60px rgba(0,0,0,0.4)",
              fontFamily: "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
              fontSize: 13,
              color: "var(--color-text)",
              lineHeight: 1.6,
            }}>
              {/* Fake browser/window chrome */}
              <div style={{ display: "flex", gap: 6, marginBottom: 14 }}>
                <span style={{ width: 11, height: 11, borderRadius: 999, background: "#ef4444" }} aria-hidden="true"></span>
                <span style={{ width: 11, height: 11, borderRadius: 999, background: "#f59e0b" }} aria-hidden="true"></span>
                <span style={{ width: 11, height: 11, borderRadius: 999, background: "#22c55e" }} aria-hidden="true"></span>
                <span style={{ marginLeft: "auto", fontSize: 11, color: "var(--color-text-dim)" }}>setforge.io · Generate</span>
              </div>
              {/* Header pills row */}
              <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12, fontSize: 11 }}>
                <span style={{ padding: "3px 10px", borderRadius: 999, background: "rgba(59,130,246,0.18)", color: "var(--color-primary)", fontWeight: 700 }}>🏊 Distance</span>
                <span style={{ padding: "3px 10px", borderRadius: 999, background: "rgba(34,197,94,0.18)", color: "var(--color-positive)", fontWeight: 700 }}>3,200 yd · ~52 min</span>
                <span style={{ padding: "3px 10px", borderRadius: 999, background: "rgba(245,158,11,0.18)", color: "var(--color-warn)", fontWeight: 700 }}>🏊‍♂️ 3 lanes</span>
              </div>
              {/* Generated workout block */}
              <div style={{ borderTop: "1px solid var(--color-border)", paddingTop: 12 }}>
                <div style={{ color: "#fff", fontWeight: 700, marginBottom: 8 }}>
                  Main · Threshold ladder ⚡
                  <span style={{ color: "var(--color-text-dim)", fontWeight: 400, fontSize: 11, marginLeft: 8 }}>1,800 yd</span>
                </div>
                <div style={{ color: "var(--color-text-muted)", fontSize: 12 }}>
                  3 × 200 free <span style={{color:"var(--color-text-dim)"}}>@ 3:00</span> · descend 1→3<br />
                  4 × 100 free <span style={{color:"var(--color-text-dim)"}}>@ 1:30</span> · hold 200 pace<br />
                  8 × 50 free  <span style={{color:"var(--color-text-dim)"}}>@&nbsp;:45</span> · build each<br />
                  4 × 25 sprint<span style={{color:"var(--color-text-dim)"}}>@&nbsp;:30</span> · all out
                </div>
              </div>
              <div style={{ marginTop: 14, paddingTop: 12, borderTop: "1px solid var(--color-border)",
                            display: "flex", justifyContent: "space-between", fontSize: 11, color: "var(--color-text-dim)" }}>
                <span>★ Favorite</span>
                <span>⇄ Regenerate</span>
                <span>📥 Save to My Sets</span>
              </div>
            </div>
          </main>

          {/* ─── Footer micro-nav ─── */}
          <footer style={{
            borderTop: "1px solid var(--color-border)",
            padding: "20px 32px",
            color: "var(--color-text-dim)",
            fontSize: 12,
            display: "flex", flexWrap: "wrap", gap: 16,
            justifyContent: "space-between", alignItems: "center",
            maxWidth: 1200, width: "100%", margin: "0 auto", boxSizing: "border-box",
          }}>
            <div>© 2026 Competition Aquatics, LLC</div>
            <nav aria-label="Footer" style={{ display: "flex", flexWrap: "wrap", gap: 16 }}>
              <a href="/changelog.html"      style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Changelog</a>
              <a href="/sub-processors.html" style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Sub-processors</a>
              <a href="/privacy.html"        style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Privacy</a>
              <a href="/terms.html"          style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>Terms</a>
              <a href="mailto:hello@competitionaquatics.com" style={{ color: "var(--color-text-muted)", textDecoration: "none" }}>hello@competitionaquatics.com</a>
            </nav>
          </footer>

          {/* Hero collapses to single column on narrow viewports. Inline so
              we don't have to wire a separate stylesheet for one rule. */}
          <style>{`
            @media (max-width: 880px) {
              .signin-hero-grid { grid-template-columns: 1fr !important; }
            }
          `}</style>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  ONBOARDING TOUR OVERLAY
    // ═══════════════════════════════════════════════════════════════
    // Spotlight coachmark over a data-tour anchor + a tooltip card.
    // No external libraries. The dim ring uses a large box-shadow "hole"
    // and is pointer-events:none so the highlighted control stays
    // clickable (so the final "tap Generate" step actually works). If an
    // anchor is missing, the card falls back to screen-center so the tour
    // never dead-ends.
    function TourOverlay({ steps, stepIndex, onNext, onBack, onSkip, onFinish }) {
      const step = steps[stepIndex];
      const isLast = stepIndex === steps.length - 1;
      const [rect, setRect] = React.useState(null);
      const cardRef = React.useRef(null);

      React.useLayoutEffect(() => {
        if (!step) return;
        let raf = 0;
        const measure = () => {
          const el = document.querySelector(`[data-tour="${step.sel}"]`);
          if (!el) { setRect(null); return; }
          const r = el.getBoundingClientRect();
          setRect({ top: r.top, left: r.left, width: r.width, height: r.height });
        };
        const el = document.querySelector(`[data-tour="${step.sel}"]`);
        if (el && el.scrollIntoView) el.scrollIntoView({ block: "center", behavior: "smooth" });
        raf = requestAnimationFrame(measure);
        window.addEventListener("resize", measure);
        window.addEventListener("scroll", measure, true);
        return () => {
          cancelAnimationFrame(raf);
          window.removeEventListener("resize", measure);
          window.removeEventListener("scroll", measure, true);
        };
      }, [step]);

      React.useEffect(() => {
        const onKey = (e) => {
          if (e.key === "Escape") { onSkip(); }
          else if (e.key === "ArrowRight" || e.key === "Enter") { isLast ? onFinish() : onNext(); }
          else if (e.key === "ArrowLeft") { if (stepIndex > 0) onBack(); }
        };
        window.addEventListener("keydown", onKey);
        return () => window.removeEventListener("keydown", onKey);
      }, [stepIndex, isLast, onNext, onBack, onSkip, onFinish]);

      React.useEffect(() => { if (cardRef.current) cardRef.current.focus(); }, [stepIndex]);

      if (!step) return null;

      const pad = 6;
      const vw = window.innerWidth, vh = window.innerHeight;
      const CARD_W = Math.min(320, vw - 24);
      // Card placement: below the anchor if it fits, else above, else centered.
      let cardStyle;
      if (rect) {
        const below = rect.top + rect.height + 150 < vh;
        const top = below ? rect.top + rect.height + pad + 8 : Math.max(12, rect.top - 8 - 150);
        let left = rect.left + rect.width / 2 - CARD_W / 2;
        left = Math.max(12, Math.min(left, vw - CARD_W - 12));
        cardStyle = { position: "fixed", top, left, width: CARD_W };
      } else {
        cardStyle = { position: "fixed", top: "50%", left: "50%", width: CARD_W, transform: "translate(-50%, -50%)" };
      }

      return (
        <div role="dialog" aria-modal="true" aria-label={`Tour: ${step.title}`}>
          {/* Dim + spotlight ring (or full dim when anchor missing) */}
          {rect ? (
            <div style={{
              position: "fixed", top: rect.top - pad, left: rect.left - pad,
              width: rect.width + pad * 2, height: rect.height + pad * 2,
              borderRadius: 10, border: "2px solid var(--color-primary)",
              boxShadow: "0 0 0 9999px rgba(0,0,0,0.62)",
              pointerEvents: "none", zIndex: 9000, transition: "all 0.18s ease",
            }} />
          ) : (
            <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.62)", pointerEvents: "none", zIndex: 9000 }} />
          )}

          {/* Tooltip card */}
          <div ref={cardRef} tabIndex={-1} style={{
            ...cardStyle, zIndex: 9001, background: "var(--color-card)",
            border: "1px solid var(--color-border-strong)", borderRadius: 12,
            padding: 16, boxShadow: "0 8px 28px rgba(0,0,0,0.4)", outline: "none",
          }}>
            <div style={{ fontSize: 11, fontWeight: 700, color: "var(--color-text-dim)", marginBottom: 4 }}>
              Step {stepIndex + 1} of {steps.length}
            </div>
            <div style={{ fontSize: 16, fontWeight: 800, color: "var(--color-text)", marginBottom: 6 }}>
              {step.title}
            </div>
            <div style={{ fontSize: 13, lineHeight: 1.5, color: "var(--color-text-dim)", marginBottom: 14 }}>
              {step.body}
            </div>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 }}>
              <button onClick={onSkip} style={{
                padding: "6px 10px", borderRadius: 8, border: "none", background: "transparent",
                color: "var(--color-text-dim)", fontSize: 12, fontWeight: 600, cursor: "pointer",
              }}>Skip tour</button>
              <div style={{ display: "flex", gap: 8 }}>
                {stepIndex > 0 && (
                  <button onClick={onBack} style={{
                    padding: "7px 12px", borderRadius: 8, border: "1px solid var(--color-border)",
                    background: "var(--color-bg)", color: "var(--color-text)", fontSize: 13, fontWeight: 700, cursor: "pointer",
                  }}>Back</button>
                )}
                <button onClick={isLast ? onFinish : onNext} style={{
                  padding: "7px 14px", borderRadius: 8, border: "none",
                  background: "var(--color-primary)", color: "var(--color-bg)", fontSize: 13, fontWeight: 800, cursor: "pointer",
                }}>{isLast ? "Done" : "Next"}</button>
              </div>
            </div>
          </div>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  TEAM PRACTICE FACILITIES (Locations P1)
    // ═══════════════════════════════════════════════════════════════
    // One-or-more pools per team (generalizes the old single "school"
    // field). Read for any team member; add/edit/archive for owner/admin.
    const FACILITY_COURSES = [
      { v: "",    label: "—" },
      { v: "25y", label: "SCY (25 yd)" },
      { v: "25m", label: "SCM (25 m)" },
      { v: "50m", label: "LCM (50 m)" },
    ];
    function courseLabel(v) { const o = FACILITY_COURSES.find(x => x.v === (v || "")); return o ? o.label : v; }
    function TeamFacilitiesSection({ teamId, canWrite }) {
      const [list, setList] = React.useState(null);
      const [busy, setBusy] = React.useState(false);
      const [msg, setMsg]   = React.useState(null);
      const [adding, setAdding] = React.useState(false);
      const emptyF = () => ({ name: "", course: "", lanes: "", is_primary: false, line1: "", city: "", region: "", postal_code: "" });
      const [form, setForm] = React.useState(emptyF());
      const upd = (k, v) => setForm(f => ({ ...f, [k]: v }));
      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`${API_BASE}/teams/${teamId}/facilities`, { cache: "no-store" });
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setList(await res.json());
        } catch (e) { setMsg(`Load failed: ${e.message}`); setList([]); }
      }, [teamId]);
      React.useEffect(() => { load(); }, [load]);
      const body = (f) => ({
        name: f.name.trim(), course: f.course || null, lanes: f.lanes === "" ? null : Number(f.lanes), is_primary: !!f.is_primary,
        address: (f.line1 || f.city || f.region || f.postal_code) ? { line1: f.line1 || null, city: f.city || null, region: f.region || null, postal_code: f.postal_code || null } : null,
      });
      const add = async () => {
        if (!form.name.trim()) { setMsg("Facility name required."); return; }
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`${API_BASE}/teams/${teamId}/facilities`, { method: "POST", headers: { "Content-Type": "application/json", ...csrfHeaders() }, body: JSON.stringify(body(form)) });
          const j = await res.json().catch(() => ({}));
          if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
          setForm(emptyF()); setAdding(false); await load();
        } catch (e) { setMsg(`Add failed: ${e.message}`); } finally { setBusy(false); }
      };
      const makePrimary = async (id) => {
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`${API_BASE}/teams/${teamId}/facilities/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json", ...csrfHeaders() }, body: JSON.stringify({ is_primary: true }) });
          if (!res.ok) { const j = await res.json().catch(() => ({})); throw new Error(j.error || `HTTP ${res.status}`); }
          await load();
        } catch (e) { setMsg(`Update failed: ${e.message}`); } finally { setBusy(false); }
      };
      const archive = async (id, name) => {
        if (!window.confirm(`Remove the facility "${name}"?`)) return;
        setBusy(true); setMsg(null);
        try {
          const res = await fetch(`${API_BASE}/teams/${teamId}/facilities/${id}`, { method: "DELETE", headers: { ...csrfHeaders() } });
          if (!res.ok) { const j = await res.json().catch(() => ({})); throw new Error(j.error || `HTTP ${res.status}`); }
          await load();
        } catch (e) { setMsg(`Remove failed: ${e.message}`); } finally { setBusy(false); }
      };
      const inputStyle = { padding: "5px 9px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 };
      return (
        <div style={{ borderTop: "1px solid var(--color-border)", paddingTop: 14, marginTop: 6 }}>
          <div style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 10 }}>Practice facilities</div>
          {list === null ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12 }}>Loading…</div>
          ) : list.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12, marginBottom: 8 }}>No facilities yet.</div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 6, marginBottom: 10 }}>
              {list.map(f => (
                <div key={f.id} style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", padding: "8px 10px", background: "var(--color-bg)", borderRadius: 6 }}>
                  <span style={{ fontWeight: 700, color: "var(--color-text)", fontSize: 13 }}>{f.name}</span>
                  {f.is_primary && <span style={{ fontSize: 10, fontWeight: 700, color: "var(--color-primary)", border: "1px solid var(--color-primary)", borderRadius: 4, padding: "1px 6px" }}>PRIMARY</span>}
                  {f.course && <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>{courseLabel(f.course)}</span>}
                  {f.lanes != null && <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>{f.lanes} lanes</span>}
                  {f.address && (f.address.city || f.address.line1) && <span style={{ fontSize: 11, color: "var(--color-text-dim)" }}>· {[f.address.line1, f.address.city, f.address.region].filter(Boolean).join(", ")}</span>}
                  {canWrite && (
                    <span style={{ marginLeft: "auto", display: "flex", gap: 6 }}>
                      {!f.is_primary && <button onClick={() => makePrimary(f.id)} disabled={busy} style={{ fontSize: 11, padding: "3px 8px", borderRadius: 4, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", cursor: "pointer" }}>Make primary</button>}
                      <button onClick={() => archive(f.id, f.name)} disabled={busy} style={{ fontSize: 11, padding: "3px 8px", borderRadius: 4, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", cursor: "pointer" }}>Remove</button>
                    </span>
                  )}
                </div>
              ))}
            </div>
          )}
          {canWrite && (adding ? (
            <div style={{ display: "flex", flexDirection: "column", gap: 6, padding: "10px", background: "var(--color-bg)", borderRadius: 6 }}>
              <input value={form.name} onChange={e => upd("name", e.target.value)} placeholder="Facility name (e.g. Lincoln HS Natatorium)" maxLength={120} style={inputStyle} />
              <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                <select value={form.course} onChange={e => upd("course", e.target.value)} style={{ ...inputStyle, flex: 1 }}>
                  {FACILITY_COURSES.map(o => <option key={o.v} value={o.v}>{o.label}</option>)}
                </select>
                <input value={form.lanes} onChange={e => upd("lanes", e.target.value.replace(/\D/g, "").slice(0, 3))} placeholder="Lanes" inputMode="numeric" style={{ ...inputStyle, width: 80 }} />
                <label style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 12, color: "var(--color-text-dim)" }}>
                  <input type="checkbox" checked={form.is_primary} onChange={e => upd("is_primary", e.target.checked)} /> Primary
                </label>
              </div>
              <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                <input value={form.line1} onChange={e => upd("line1", e.target.value)} placeholder="Address (optional)" maxLength={160} style={{ ...inputStyle, flex: 2, minWidth: 160 }} />
                <input value={form.city} onChange={e => upd("city", e.target.value)} placeholder="City" maxLength={80} style={{ ...inputStyle, flex: 1, minWidth: 90 }} />
                <input value={form.region} onChange={e => upd("region", e.target.value)} placeholder="State" maxLength={80} style={{ ...inputStyle, width: 70 }} />
                <input value={form.postal_code} onChange={e => upd("postal_code", e.target.value)} placeholder="ZIP" maxLength={20} style={{ ...inputStyle, width: 80 }} />
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <button onClick={add} disabled={busy || !form.name.trim()} style={{ padding: "6px 14px", borderRadius: 6, border: "none", background: (busy || !form.name.trim()) ? "var(--color-border)" : "var(--color-primary)", color: "#fff", fontSize: 12, fontWeight: 700, cursor: (busy || !form.name.trim()) ? "not-allowed" : "pointer" }}>{busy ? "Saving…" : "Add facility"}</button>
                <button onClick={() => { setAdding(false); setForm(emptyF()); setMsg(null); }} disabled={busy} style={{ padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-dim)", fontSize: 12, cursor: "pointer" }}>Cancel</button>
              </div>
            </div>
          ) : (
            <button onClick={() => setAdding(true)} style={{ padding: "6px 12px", borderRadius: 6, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-primary)", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>+ Add facility</button>
          ))}
          {msg && <div style={{ color: "var(--color-warn)", fontSize: 12, marginTop: 8 }}>{msg}</div>}
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  HOME ADDRESS MANAGER (Locations P2)
    // ═══════════════════════════════════════════════════════════════
    // Manages a person's home address(es) via /api/swimmers/:ref/addresses.
    // The server gates access (self+guardians always; coach only with the
    // consent toggle on) — a 403 just renders "not shared". showConsent adds
    // the guardian/self consent toggle ("coaches may view this address").
    function AddressManager({ swimmerRef, showConsent, seedAddresses = null, seedCanWrite = null }) {
      // Seeded from the composite detail fetch (owner-coach context, where
      // access is always granted) — skip the initial GET; mutations still
      // refetch. noaccess only arises from a real 403 on the fallback fetch.
      const [data, setData] = React.useState(Array.isArray(seedAddresses)
        ? { addresses: seedAddresses, can_write: !!seedCanWrite, coach_visible: false }
        : null);
      const [adding, setAdding] = React.useState(false);
      const emptyF = () => ({ line1: "", city: "", region: "", postal_code: "" });
      const [form, setForm] = React.useState(emptyF());
      const [busy, setBusy] = React.useState(false);
      const [msg, setMsg] = React.useState(null);
      const upd = (k, v) => setForm(f => ({ ...f, [k]: v }));
      const load = React.useCallback(async () => {
        try {
          const res = await fetch(`${API_BASE}/swimmers/${encodeURIComponent(swimmerRef)}/addresses`, { cache: "no-store" });
          if (res.status === 403) { setData({ addresses: [], can_write: false, coach_visible: false, noaccess: true }); return; }
          if (!res.ok) throw new Error(`HTTP ${res.status}`);
          setData(await res.json());
        } catch (e) { setMsg(`Load failed: ${e.message}`); setData({ addresses: [], can_write: false, coach_visible: false }); }
      }, [swimmerRef]);
      const skipInitial = React.useRef(Array.isArray(seedAddresses));
      React.useEffect(() => { if (skipInitial.current) { skipInitial.current = false; return; } load(); }, [load]);
      const reqJson = async (url, method, payload) => {
        const res = await fetch(`${API_BASE}${url}`, { method, headers: { "Content-Type": "application/json", ...csrfHeaders() }, body: payload ? JSON.stringify(payload) : undefined });
        if (!res.ok) { const j = await res.json().catch(() => ({})); throw new Error(j.error || `HTTP ${res.status}`); }
      };
      const add = async () => {
        if (!form.line1.trim() && !form.city.trim()) { setMsg("Enter at least a street or city."); return; }
        setBusy(true); setMsg(null);
        try { await reqJson(`/swimmers/${encodeURIComponent(swimmerRef)}/addresses`, "POST", { kind: "home", is_primary: true, address: { line1: form.line1 || null, city: form.city || null, region: form.region || null, postal_code: form.postal_code || null } }); setForm(emptyF()); setAdding(false); await load(); }
        catch (e) { setMsg(`Save failed: ${e.message}`); } finally { setBusy(false); }
      };
      const remove = async (id) => {
        if (!window.confirm("Remove this address?")) return;
        setBusy(true); setMsg(null);
        try { await reqJson(`/swimmers/${encodeURIComponent(swimmerRef)}/addresses/${id}`, "DELETE"); await load(); }
        catch (e) { setMsg(`Remove failed: ${e.message}`); } finally { setBusy(false); }
      };
      const setConsent = async (next) => {
        setBusy(true); setMsg(null);
        try { await reqJson(`/swimmers/${encodeURIComponent(swimmerRef)}/address-consent`, "PATCH", { coach_visible: next }); await load(); }
        catch (e) { setMsg(`Update failed: ${e.message}`); } finally { setBusy(false); }
      };
      const inputStyle = { padding: "5px 9px", fontSize: 13, background: "var(--color-bg)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 };
      if (data === null) return <div style={{ color: "var(--color-text-dim)", fontSize: 12 }}>Loading address…</div>;
      if (data.noaccess) return <div style={{ color: "var(--color-text-dim)", fontSize: 12, fontStyle: "italic" }}>🏠 Home address not shared.</div>;
      const addr = (a) => [a.line1, a.city, a.region, a.postal_code].filter(Boolean).join(", ");
      return (
        <div>
          <div style={{ color: "var(--color-text-muted)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em", marginBottom: 6 }}>🏠 Home address</div>
          {data.addresses.length === 0 ? (
            <div style={{ color: "var(--color-text-dim)", fontSize: 12, marginBottom: 6 }}>No address on file.</div>
          ) : (
            <div style={{ display: "flex", flexDirection: "column", gap: 4, marginBottom: 8 }}>
              {data.addresses.map(a => (
                <div key={a.id} style={{ display: "flex", alignItems: "center", gap: 8, padding: "6px 8px", background: "var(--color-bg)", borderRadius: 6, fontSize: 13, color: "var(--color-text)" }}>
                  <span style={{ flex: 1 }}>{addr(a) || "—"}{a.is_primary && data.addresses.length > 1 ? <span style={{ marginLeft: 6, fontSize: 10, color: "var(--color-primary)" }}>primary</span> : null}</span>
                  {data.can_write && <button onClick={() => remove(a.id)} disabled={busy} style={{ fontSize: 11, padding: "2px 8px", borderRadius: 4, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-muted)", cursor: "pointer" }}>Remove</button>}
                </div>
              ))}
            </div>
          )}
          {data.can_write && (adding ? (
            <div style={{ display: "flex", flexDirection: "column", gap: 6, marginBottom: 8 }}>
              <input value={form.line1} onChange={e => upd("line1", e.target.value)} placeholder="Street address" maxLength={160} style={inputStyle} />
              <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                <input value={form.city} onChange={e => upd("city", e.target.value)} placeholder="City" maxLength={80} style={{ ...inputStyle, flex: 1, minWidth: 90 }} />
                <input value={form.region} onChange={e => upd("region", e.target.value)} placeholder="State" maxLength={80} style={{ ...inputStyle, width: 70 }} />
                <input value={form.postal_code} onChange={e => upd("postal_code", e.target.value)} placeholder="ZIP" maxLength={20} style={{ ...inputStyle, width: 80 }} />
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <button onClick={add} disabled={busy} style={{ padding: "6px 14px", borderRadius: 6, border: "none", background: "var(--color-primary)", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>{busy ? "Saving…" : "Save address"}</button>
                <button onClick={() => { setAdding(false); setForm(emptyF()); setMsg(null); }} disabled={busy} style={{ padding: "6px 10px", borderRadius: 6, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-dim)", fontSize: 12, cursor: "pointer" }}>Cancel</button>
              </div>
            </div>
          ) : (
            <button onClick={() => setAdding(true)} style={{ padding: "5px 11px", borderRadius: 6, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-primary)", fontSize: 12, fontWeight: 700, cursor: "pointer", marginBottom: 8 }}>+ {data.addresses.length ? "Add another" : "Add address"}</button>
          ))}
          {showConsent && (
            <label style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12, color: "var(--color-text-dim)", marginTop: 4 }}>
              <input type="checkbox" checked={!!data.coach_visible} disabled={busy} onChange={e => setConsent(e.target.checked)} />
              Let this swimmer's coaches view the home address
            </label>
          )}
          {msg && <div style={{ color: "var(--color-warn)", fontSize: 12, marginTop: 6 }}>{msg}</div>}
        </div>
      );
    }

    // Household/siblings (Locations P3) — read-only derived display. Server
    // gates it (shared address+guardian, per-sibling consent), so we just
    // render whatever names come back; nothing if none/not-authorized.
    function HouseholdSiblings({ swimmerRef, seedHousehold = null }) {
      const [sibs, setSibs] = React.useState(Array.isArray(seedHousehold) ? seedHousehold : null);
      React.useEffect(() => {
        // Seeded from the composite detail fetch — skip the initial GET.
        if (Array.isArray(seedHousehold)) return;
        let cancelled = false;
        fetch(`${API_BASE}/swimmers/${encodeURIComponent(swimmerRef)}/household`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : { siblings: [] })
          .then(d => { if (!cancelled) setSibs(Array.isArray(d.siblings) ? d.siblings : []); })
          .catch(() => { if (!cancelled) setSibs([]); });
        return () => { cancelled = true; };
      }, [swimmerRef]);
      if (!sibs || sibs.length === 0) return null;
      return (
        <div style={{ fontSize: 12, color: "var(--color-text-dim)", marginTop: 8 }}>
          👪 Household: <span style={{ color: "var(--color-text)" }}>{sibs.map(s => s.display_name).join(", ")}</span>
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  PARENT INVITE ACCEPT CARDS
    // ═══════════════════════════════════════════════════════════════
    // Explicit-consent surface: a parent with pending invites (from
    // bootstrap.pending_invites) sees one card per invite and must Accept
    // (creates the guardian link) or Decline. Replaces the old silent
    // auto-consume. Renders for any signed-in user with a pending invite,
    // even before they're a linked parent.
    function ParentInviteCards({ invites, onResolved }) {
      const [busyId, setBusyId] = React.useState(null);
      const [err, setErr] = React.useState(null);
      if (!Array.isArray(invites) || invites.length === 0) return null;
      const act = async (id, action) => {
        setBusyId(id); setErr(null);
        try {
          const res = await fetch(`${API_BASE}/parent/invites/${id}/${action}`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
          });
          if (!res.ok) { const j = await res.json().catch(() => ({})); throw new Error(j.error || `HTTP ${res.status}`); }
          if (onResolved) onResolved();
        } catch (e) { setErr(e.message || String(e)); }
        finally { setBusyId(null); }
      };
      return (
        <div className="screen-only" style={{ display: "flex", flexDirection: "column", gap: 8, padding: "10px 16px", background: "var(--color-bg)" }}>
          {invites.map(inv => (
            <div key={inv.id} style={{
              display: "flex", flexWrap: "wrap", alignItems: "center", gap: 12,
              background: "var(--color-card)", border: "1px solid var(--color-primary)",
              borderRadius: 10, padding: "12px 14px",
            }}>
              <div style={{ flex: 1, minWidth: 200, fontSize: 13, color: "var(--color-text)" }}>
                👪 <strong>{inv.coach_name}</strong> invited you to follow <strong>{inv.swimmer_name}</strong> on SetForge as a parent/guardian.
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <button onClick={() => act(inv.id, "accept")} disabled={busyId === inv.id}
                  style={{ padding: "7px 14px", borderRadius: 8, border: "none", background: "var(--color-primary)", color: "var(--color-bg)", fontSize: 13, fontWeight: 800, cursor: busyId === inv.id ? "default" : "pointer" }}>
                  {busyId === inv.id ? "…" : "Accept"}
                </button>
                <button onClick={() => act(inv.id, "decline")} disabled={busyId === inv.id}
                  style={{ padding: "7px 12px", borderRadius: 8, border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-dim)", fontSize: 13, fontWeight: 600, cursor: busyId === inv.id ? "default" : "pointer" }}>
                  Decline
                </button>
              </div>
              {err && <div style={{ flexBasis: "100%", color: "#fca5a5", fontSize: 12 }}>{err}</div>}
            </div>
          ))}
        </div>
      );
    }

    // ═══════════════════════════════════════════════════════════════
    //  MAIN APP
    // ═══════════════════════════════════════════════════════════════

    function App() {
      const [selectedType, setSelectedType] = useState(null);
      const [maxYards, setMaxYards]         = useState(2400);
      const [poolMode, setPoolMode]         = useState("25y");
      const [paceInput, setPaceInput]       = useState("2:00");
      // Onboarding tour. tourStep -1 = inactive, 0..N-1 = active step.
      // tourSeen defaults true so the tour never flashes before settings
      // resolve; applySettings sets the real value (unset → false → auto-run
      // once). tourSeenWriteRef guards the one-time persist.
      const [tourStep, setTourStep]         = useState(-1);
      const [tourSeen, setTourSeen]         = useState(true);
      const tourAutoRef     = React.useRef(false);
      const tourSeenWriteRef = React.useRef(false);
      // v2.0 — multi-lane mode (coach-only). When ON, picker filters
      // options to those whose intervals fit ALL lane paces, and the
      // generate flow auto-routes to MultiPacePrintView. manualLanesPace
      // holds { lane_label, pace } rows; user types or seeds from a plan.
      const [multiLaneMode, setMultiLaneMode] = useState(false);
      const [manualLanesPace, setManualLanesPace] = useState([
        { lane_label: "Lane 1", pace: "1:45" },
        { lane_label: "Lane 2", pace: "2:00" },
      ]);
      const [sliderMin, setSliderMin]       = useState(1900);
      const [sliderMax, setSliderMax]       = useState(5000);
      const [workout, setWorkout]           = useState(null);
      const [hover, setHover]               = useState(null);
      // F: equipment is tri-state per item — "off" | "preferred" | "required".
      // Initial state is all "off"; values are loaded from settings.extra.equipment_modes
      // on mount (legacy booleans are normalized via equipMode()).
      const [equipment, setEquipment]       = useState({
        kickboard: "off", fins: "off", paddles: "off", pullBuoy: "off", snorkel: "off",
      });

      // History state
      const [view, setView]                       = useState("generator"); // "generator" | "history" | "admin"
      const [history, setHistory]                 = useState(() => loadLocalHistory());
      const [historyLoaded, setHistoryLoaded]     = useState(false);
      const [favorites, setFavorites]             = useState([]);
      // v1.2 — Disfavorites: labels with 0.25× pick weight (inverse of fav).
      // Loaded on mount alongside favorites; toggled via /api/disfavorites.
      // Mutex with favorites enforced at the DB layer (dbAdd* helpers clear
      // the opposite table on insert).
      const [disfavorites, setDisfavorites]       = useState([]);
      // v1.3 — Engine disfavorites: array of { template_id, stroke } pairs
      // the user has marked as disfavored in engine output. 0.25× weight in
      // the engine template picker. Persisted to settings.extra (no DB table).
      const [engineDisfavorites, setEngineDisfavorites] = useState([]);
      // v1.8 — Disfavor mode: "downweight" (0.25× weight, default) or
      // "exclude" (hard-exclude, weight 0 with silent fallback if pool
      // empties). Applies to ALL disfavor types (label, set, engine).
      // Persisted to settings.extra.disfavor_mode.
      const [disfavorMode, setDisfavorMode] = useState("downweight");
      // Set-level per-user favorites — Set<set_id> for O(1) lookup. Loaded
      // alongside label-favorites; toggled via /api/favorite-sets endpoints.
      const [favoriteSets, setFavoriteSets]       = useState(() => new Set());
      // v1.5 — Set-level disfavorites: Set<set_id>. 0.25× pick weight per
      // matching set inside an option (vs favorite-sets' 3×). Mutex with
      // favorite-sets enforced at the DB layer. Loaded on mount; no UI
      // in v1.5 (mirrors favorite-sets' API-only setup) — future catalog
      // or coach view can wire the toggle handler.
      const [disfavorSets, setDisfavorSets]       = useState(() => new Set());
      // v1.7 — Effective disfavorites (own + coach propagation). Fetched
      // from /api/effective-disfavorites on mount. Used by the picker so
      // primary coach's disfavorites silently apply to swimmers in their
      // group. The audit panel + tri-state UI still read the OWN lists
      // (disfavorites + disfavorSets + engineDisfavorites) so swimmers
      // don't see coach contributions in their UI.
      const [effectiveDisfavorLabels, setEffectiveDisfavorLabels] = useState([]);
      const [effectiveDisfavorSetIds, setEffectiveDisfavorSetIds] = useState(() => new Set());
      const [effectiveEngineDisfavorites, setEffectiveEngineDisfavorites] = useState([]);
      // v1.13 — Effective favorites mirror. Own favorites + coach (primary
      // + assistant) favorites silently boost picks. Audit panel still
      // own-only via /api/favorites + /api/favorite-sets.
      const [effectiveFavoriteLabels, setEffectiveFavoriteLabels] = useState([]);
      const [effectiveFavoriteSetIds, setEffectiveFavoriteSetIds] = useState(() => new Set());
      const [effectiveEngineFavorites, setEffectiveEngineFavorites] = useState([]);
      // v1.13 — Own engine favorites (separate from effective so the
      // audit panel can list user's own without coach contributions).
      // Lives in settings.extra.engine_favorites, loaded on mount.
      const [engineFavorites, setEngineFavorites]               = useState([]);
      // Phase 3 PSC slice 2 — caller's own active per-swimmer constraints.
      // Fetched from /api/me/constraints on mount + 5-min poll. Fed into
      // generateWorkout/regenerateSection as step-0 hard-exclude. Per Cap'n
      // fork 2 (2026-05-26): we apply the caller's OWN PSC to every Generate
      // — solo, coach-Generate, etc. Slice 3 adds coach UX + per-practice
      // checklist for assignments targeting specific swimmers.
      const [myConstraints, setMyConstraints] = useState([]);
      // Phase 3 PSC slice 3 — per-practice checklist.
      // groupActiveConstraints: { [swimmerKey: sub|managed_id]: [constraint rows] }
      // tonightSelected: Set<constraint_id> of rows the coach checked for THIS Generate.
      // groupActiveConstraints refetches on target group change. tonightSelected
      // resets to empty (default to NOT applying — coach opts in per practice).
      const [groupActiveConstraints, setGroupActiveConstraints] = useState({});
      const [tonightSelected, setTonightSelected] = useState(() => new Set());
      // UGC Phase B — bank overlay. Mirrors the 12 JS bank constants;
      // populated by /api/bank/my-overlay (own + team-shared + public
      // UGC, excluding promoted_at). Picker merges with simple concat
      // per constant. Empty 12-key shape until authoring lands (Phase C).
      const [ugcOverlay, setUgcOverlay] = useState(() => ({
        WARMUP_OPTIONS: [], COOLDOWN_OPTIONS: [], DRILL_OPTIONS: {}, MAIN_OPTIONS: {},
        WARMUP_OPTIONS_50M: [], COOLDOWN_OPTIONS_50M: [], DRILL_OPTIONS_50M: {}, MAIN_OPTIONS_50M: {},
        WARMUP_OPTIONS_SCM: [], COOLDOWN_OPTIONS_SCM: [], DRILL_OPTIONS_SCM: {}, MAIN_OPTIONS_SCM: {},
      }));
      // UGC Phase C — snapshot button on WorkoutBlock opens UgcFormModal
      // pre-filled with the block's data. null = closed, object = open
      // with that pseudo-"option" pre-populated.
      const [snapshotOption, setSnapshotOption] = useState(null);
      // Sub IDs used to classify a 'selectedType' value as a TYPE vs STROKE
      // when building the snapshot pre-fill (drill/main need one of the two).
      const UGC_TYPE_KEYS_CLIENT   = React.useMemo(() => new Set(["im","distance","sprint","endurance","mixed","technique"]), []);
      const UGC_STROKE_KEYS_CLIENT = React.useMemo(() => new Set(["back","breast","fly","free","im"]), []);
      // Pre-built Map of UGC overlay set IDs → { _is_own, _visibility }
      // for the badge derivation in WorkoutBlock. Phase D needs the
      // metadata (not just presence) to pick the right badge variant:
      //   📝 = your own (any visibility)
      //   👥 = team-shared from another coach (_visibility === 'team')
      //   🌐 = admin-approved public from another coach (Phase E)
      const ugcSetIdMeta = React.useMemo(() => {
        const out = new Map();
        if (!ugcOverlay) return out;
        const walkOptionList = (list) => {
          if (!Array.isArray(list)) return;
          for (const opt of list) {
            if (opt && Array.isArray(opt.sets)) {
              const meta = {
                _is_own:     !!opt._is_own,
                _visibility: opt._visibility || null,
              };
              for (const s of opt.sets) if (s && s.id) out.set(s.id, meta);
            }
          }
        };
        for (const key of Object.keys(ugcOverlay)) {
          if (key.startsWith("_")) continue;
          const val = ugcOverlay[key];
          if (Array.isArray(val)) {
            walkOptionList(val);
          } else if (val && typeof val === "object") {
            for (const subKey of Object.keys(val)) walkOptionList(val[subKey]);
          }
        }
        return out;
      }, [ugcOverlay]);

      const handleSnapshotBlock = useCallback((block) => {
        if (!block || !block.sets) return;
        const needsType = (block.section === "drill" || block.section === "main");
        const sel       = selectedType || "";
        const isStroke  = UGC_STROKE_KEYS_CLIENT.has(sel);
        const isType    = UGC_TYPE_KEYS_CLIENT.has(sel);
        setSnapshotOption({
          // No id → UgcFormModal treats this as a "create" with pre-fill.
          section:     block.section,
          pool_mode:   poolMode,
          // Phase H — arrays so the snapshot pre-fill seeds the multi-select
          // checkboxes correctly.
          type_ids:    needsType && isType   ? [sel] : [],
          stroke_ids:  needsType && isStroke ? [sel] : [],
          label:       block.label ? `${block.label} (copy)` : "Snapshot",
          total_yards: block.sets.reduce((acc, s) => acc + (s.reps || 0) * (s.dist || 0), 0),
          sets:        block.sets.map(s => ({
            reps: s.reps, dist: s.dist, desc: s.desc, interval: s.interval,
            focus: s.focus || null, stroke: s.stroke || null, eq: s.eq || null,
          })),
        });
      }, [selectedType, poolMode, UGC_STROKE_KEYS_CLIENT, UGC_TYPE_KEYS_CLIENT]);
      const [goals, setGoals]                     = useState([]);
      const [nextEvent, setNextEvent]             = useState(null); // { name, date } | null
      const [phase, setPhase]                     = useState(null); // N5: "base"|"build"|"peak"|"taper"|"recovery"|null
      // B (bootstrap extension) — lifted to App so ProfileModal can read from
      // props instead of re-fetching /api/settings just to get `level`.
      const [level, setLevel]                     = useState(null); // J — swimmer-level preset id | null
      // B — sessions + team-defaults + billing-status arrive in bootstrap so
      // ProfileModal-open doesn't need their three GETs anymore.
      const [sessions, setSessions]               = useState([]);   // auth/sessions
      const [teamDefaults, setTeamDefaults]       = useState([]);   // team-defaults inherited
      const [pendingInvites, setPendingInvites]   = useState([]);   // parent invites awaiting accept
      const [billingStatus, setBillingStatus]     = useState(null); // { tier, ... } | null
      const [audioCues, setAudioCues]             = useState(true); // W1: rest-timer beeps default ON
      const [lapButton, setLapButton]             = useState(true); // Run-screen v1: ✓ Lap button default ON
      const [loadedFromHistoryId, setLoadedFromHistoryId] = useState(null);
      const [saveStatus, setSaveStatus]           = useState(null); // null | "saving" | "saved" | "error"
      const [saveError, setSaveError]             = useState(null);
      const [dateDraft, setDateDraft]             = useState(() => new Date().toISOString().slice(0, 10));
      const [noteDraft, setNoteDraft]             = useState("");
      const [difficultyDraft, setDifficultyDraft] = useState(null);    // 1–5 stars, null = unrated
      const [focusNoteDraft, setFocusNoteDraft]   = useState("");      // pre-workout intention
      const [initialsDraft, setInitialsDraft]     = useState(() => {
        try { return normalizeInitials(localStorage.getItem(USER_INITIALS_KEY) || ""); }
        catch (_) { return ""; }
      });
      // Pinned sections: { warmup?: bool, drill?: bool, main?: bool, cooldown?: bool }
      // Pinned blocks are held fixed on "Generate New Workout"; unpinned sections get fresh picks.
      const [pinnedSections, setPinnedSections]   = useState({});
      const [recoveryMode, setRecoveryMode]       = useState(false); // C: per-generation toggle, not persisted
      const [sectionBias, setSectionBias]         = useState("balanced"); // Section-proportion bias: balanced / warmup_heavy / drill_heavy / long_main. Per-generation, session-only.
      // Section model A2 — which sections to include. `main` always on. Skipping
      // a section drops it from the workout (a correspondingly shorter, honest
      // total). Per-generation, session-only.
      const [skipWarmup,   setSkipWarmup]   = useState(false);
      const [skipDrill,    setSkipDrill]    = useState(false);
      const [skipCooldown, setSkipCooldown] = useState(false);
      // Kick is the opt-IN 5th section (default off), so it's tracked as
      // addKick rather than a skip flag. Order in includedSections must match
      // the engine's block order: warmup → drill → kick → main → cooldown.
      const [addKick,      setAddKick]      = useState(false);
      const includedSections = React.useMemo(() => {
        const out = [];
        if (!skipWarmup) out.push("warmup");
        if (!skipDrill)  out.push("drill");
        if (addKick)     out.push("kick");
        out.push("main");
        if (!skipCooldown) out.push("cooldown");
        return out;
      }, [skipWarmup, skipDrill, addKick, skipCooldown]);
      // Section model B2 — dryland insert picker (post-generation add).
      const [drylandPickerOpen, setDrylandPickerOpen] = useState(false);
      const [drylandPresetId,   setDrylandPresetId]   = useState(DRYLAND_OPTIONS[0].id);
      const [drylandPlacement,  setDrylandPlacement]  = useState("pre");
      // S3 — per-section engine source (bank | engine | mix), default all bank.
      // Persisted to settings.extra.engine_section_sources on change. Read on
      // mount via the settings fetch elsewhere in this component.
      const [sectionSources, setSectionSources]   = useState({ warmup: "bank", drill: "bank", main: "bank", cooldown: "bank" });
      // S3 — anti-repeat memory for engine (last 10 { template_id, stroke, ts }).
      // Persisted as settings.extra.engine_recent_templates. Loaded on mount,
      // updated client-side after each successful engine generate.
      const [recentEngineTemplates, setRecentEngineTemplates] = useState([]);

      // Per-section regeneration error: { section: "warmup"|"drill"|"main"|"cooldown", message } | null
      const [regenError, setRegenError]           = useState(null);
      // S3 #3 — top-level generate error (e.g. required equipment unsatisfiable).
      // Surfaced as a banner above the workout area; cleared on successful generate.
      const [generateError, setGenerateError]     = useState(null);
      // S4 #7 — session-local anti-repeat: every successful generate / main-regen
      // records the picked main-set label here. Unioned with recentMainLabels (from
      // saved history) when computing weights, so consecutive generates don't
      // repeat the same main even before any save has happened.
      const [sessionRecentLabels, setSessionRecentLabels] = useState(() => new Set());
      // Run Workout overlay: null when inactive, or the workout/entry object being stepped through
      const [runWorkout, setRunWorkout]           = useState(null);
      const [showRestPicker, setShowRestPicker]   = useState(false);
      // R-G: when set, Run-mode finish writes to this assignment row instead
      // of creating a new history entry. Cleared after the run completes or
      // when the swimmer exits Run mode.
      const [runAssignmentId, setRunAssignmentId] = useState(null);
      // I — week-view planning. runScheduledId is set when a scheduled
      // workout is launched into Run mode so the log-workout call links
      // the completion back via dbLinkCompletedToSchedule. editingScheduledId
      // is set when a scheduled row is loaded into the workout slot for
      // edit/regen — handleSave PATCHes the schedule row instead of
      // creating a new history entry.
      const [runScheduledId,     setRunScheduledId]     = useState(null);
      const [editingScheduledId, setEditingScheduledId] = useState(null);
      // I — date picker shown when user clicks "📅 Schedule for…" on the
      // workout display. holds the target date string (YYYY-MM-DD) and is
      // null when the picker is closed.
      const [scheduleDate, setScheduleDate] = useState(null);
      // I Phase 2a — save-as-intent date picker. Opens when "Save as intent
      // for later…" is clicked under the Generate button. POSTs intent_params
      // built from the *current* form state (type/maxYards/mix/recovery) —
      // generation deferred to the scheduled day.
      const [intentDate, setIntentDate] = useState(null);
      const [intentSaveBusy, setIntentSaveBusy] = useState(false);
      const [showProfile, setShowProfile]         = useState(false);
      // Setforge rebrand 2026-05-20 — Coach dropdown (REBRAND_SCOPE §8.1).
      // Collapses Teams / Swimmers / Catalog into a single nav entry.
      const [coachMenuOpen, setCoachMenuOpen] = useState(false);
      const [showFeedback, setShowFeedback]       = useState(false);
      const [showIntentParser, setShowIntentParser] = useState(false);
      // DOB soft-prompt dismissal (sessionStorage-backed, per decision #37).
      // Initialized lazily from sessionStorage so the modal doesn't re-pop
      // after dismissal within the same tab.
      const [dobPromptDismissed, setDobPromptDismissed] = useState(() => {
        try { return sessionStorage.getItem(DOB_PROMPT_DISMISS_KEY) === "1"; }
        catch (_) { return false; }
      });
      const [restSecs,      setRestSecs]          = useState(30); // null=manual, 0/30/45/60=auto
      // Print dialog: show "save before printing?" prompt for unsaved workouts
      const [showPrintDialog, setShowPrintDialog] = useState(false);
      // N6 — Multi-pace export. showMultiPace gates the modal; multiPaceLanes
      // is non-null while the print overlay is mounted ({lanes, mode}).
      const [showMultiPace,   setShowMultiPace]   = useState(false);
      const [multiPaceLanes,  setMultiPaceLanes]  = useState(null);
      const [copyFlash,       setCopyFlash]       = useState(false);
      // Auth state: null = checking, false = not signed in, true = signed in
      const [authenticated, setAuthenticated] = useState(null);
      const [authMode,      setAuthMode]      = useState(null); // "open" | "apple"
      const [authError,     setAuthError]     = useState(null);
      const [me,            setMe]            = useState(null); // /api/me payload (for is_admin gate, etc.)
      // View-as: admin-only QA mode that temporarily overrides role flags
      // for UI gating purposes. Real `me` stays unchanged so API permission
      // checks (server-side) and the view-as switcher's own visibility
      // continue to use real role. localStorage-backed so it survives reload.
      // Values: "self" (no override) | "solo" (no coach, no admin) | "coach"
      // (coach only). Switcher only renders to actual admins.
      const [viewAsRole, setViewAsRoleState] = useState(() => {
        try { return localStorage.getItem("setforge_view_as") || "self"; } catch (_) { return "self"; }
      });
      const setViewAsRole = useCallback((role) => {
        const v = ["self", "solo", "coach"].includes(role) ? role : "self";
        setViewAsRoleState(v);
        try { localStorage.setItem("setforge_view_as", v); } catch (_) {}
      }, []);
      // Orthogonal "+ parent" toggle (2026-05-27). Composes with the role
      // picker: when on AND role != self, effectiveMe also flips is_parent
      // true so the 👪 nav + ParentDashboard are reachable in preview. Only
      // meaningful for admins (mirrors the rest of view-as v1 semantics).
      const [viewAsParent, setViewAsParentState] = useState(() => {
        try { return localStorage.getItem("setforge_view_as_parent") === "1"; } catch (_) { return false; }
      });
      const setViewAsParent = useCallback((on) => {
        const v = !!on;
        setViewAsParentState(v);
        try { localStorage.setItem("setforge_view_as_parent", v ? "1" : "0"); } catch (_) {}
      }, []);
      // effectiveMe — same as me, but with role flags overridden when
      // viewAsRole !== "self". UI gates should read effectiveMe; the
      // view-as switcher itself reads me.is_admin (real) for visibility.
      const effectiveMe = React.useMemo(() => {
        if (!me) return me;
        if (viewAsRole === "self" && !viewAsParent) return me;
        if (!me.is_admin) return me;  // non-admins can't view-as; ignore the stored value
        let next = me;
        if      (viewAsRole === "solo")  next = { ...me, is_admin: false, is_coach: false };
        else if (viewAsRole === "coach") next = { ...me, is_admin: false, is_coach: true  };
        // Parent flag is orthogonal — composes on top of solo/coach (and
        // even self, though the banner is hidden in self mode so it has
        // no UX effect there).
        if (viewAsParent) next = { ...next, is_parent: true };
        return next;
      }, [me, viewAsRole, viewAsParent]);

      // View-as v2 (2026-05-23): persona simulation. When admin is viewing-as
      // another role, the read view shows a fresh-new-user empty state AND
      // writes are blocked. Different from v1 which only hid UI surfaces.
      // Different from v3 impersonation (server-side, real target user) —
      // this is pure client-side preview using admin's own session.
      const isViewingAsOther = !!(me?.is_admin && (viewAsRole !== "self" || viewAsParent));
      // Ref mirror — write handlers use useCallback with non-view-as deps, so
      // reading isViewingAsOther directly in the handler closure would stale.
      // The ref is updated on every render so handlers always see the current
      // value without forcing a re-memoize across every dep array.
      const isViewingAsOtherRef = React.useRef(isViewingAsOther);
      isViewingAsOtherRef.current = isViewingAsOther;
      const [personaBlockMsg, setPersonaBlockMsg] = useState(null);
      // Auto-clear the persona-block toast after 2.5s.
      useEffect(() => {
        if (!personaBlockMsg) return;
        const id = setTimeout(() => setPersonaBlockMsg(null), 2500);
        return () => clearTimeout(id);
      }, [personaBlockMsg]);

      // Billing v1 — ?upgrade=success / ?upgrade=cancelled banner when
      // the user returns from Stripe Checkout. Clears the query param
      // from the URL after read so refresh doesn't re-show the banner.
      // Also fires a delayed refreshBootstrap() so the new tier (set
      // server-side by the Stripe webhook arriving asynchronously)
      // propagates into App state without requiring a manual reload.
      const [upgradeBanner, setUpgradeBanner] = useState(null);
      useEffect(() => {
        try {
          const params = new URLSearchParams(window.location.search);
          const u = params.get("upgrade");
          if (u === "success") {
            setUpgradeBanner({ kind: "success", text: "Welcome to Coach tier — your 14-day free trial is active. Manage your subscription anytime from Profile → Subscription." });
            // Webhook arrives async (~1-2s after Stripe redirect). Refresh
            // bootstrap twice with a small delay so we usually catch the
            // tier flip without forcing the user to wait or reload. First
            // call is best-effort right away (covers fast webhooks); second
            // covers the typical webhook latency window. If both miss, the
            // 5-min poll OR next ProfileModal-open will pick it up.
            refreshBootstrap();
            setTimeout(() => { refreshBootstrap(); }, 3000);
          } else if (u === "cancelled") {
            setUpgradeBanner({ kind: "info", text: "Checkout cancelled. Your account is still on the Swimmer (Free) tier. Subscribe from Profile → Subscription whenever you're ready." });
          }
          if (u) {
            // Clear the param so refresh doesn't re-trigger.
            params.delete("upgrade");
            const q = params.toString();
            window.history.replaceState({}, "", window.location.pathname + (q ? "?" + q : ""));
          }
        } catch (_) { /* URLSearchParams unsupported — skip */ }
      }, []);
      // Auto-clear upgrade banner after 8s (longer than persona-block since
      // it's success-flow not error-flow).
      useEffect(() => {
        if (!upgradeBanner) return;
        const id = setTimeout(() => setUpgradeBanner(null), 8000);
        return () => clearTimeout(id);
      }, [upgradeBanner]);
      // v2 entry/exit handler. On entering view-as, wipe data state for a
      // fresh-new-user simulation. On exit, force window.location.reload()
      // to restore admin's real data cleanly — avoids racing N fetch chains.
      // Brutal but bulletproof for an admin-only QA tool.
      const prevViewingRef = React.useRef(isViewingAsOther);
      useEffect(() => {
        const wasViewing = prevViewingRef.current;
        prevViewingRef.current = isViewingAsOther;
        if (!wasViewing && isViewingAsOther) {
          setHistory([]);
          setFavorites([]);
          setDisfavorites([]);
          setEngineDisfavorites([]);
          setFavoriteSets(new Set());
          setDisfavorSets(new Set());
          setEffectiveDisfavorLabels([]);
          setEffectiveDisfavorSetIds(new Set());
          setEffectiveEngineDisfavorites([]);
          setEffectiveFavoriteLabels([]);
          setEffectiveFavoriteSetIds(new Set());
          setEffectiveEngineFavorites([]);
          setEngineFavorites([]);
          setGoals([]);
          setNextEvent(null);
        } else if (wasViewing && !isViewingAsOther) {
          // Restore real data cleanly.
          window.location.reload();
        }
      }, [isViewingAsOther]);

      const [showImpersonationModal, setShowImpersonationModal] = useState(false);
      // v3 impersonation state. Mirrors module-scope `impersonation.active`
      // for React render purposes. setImpersonation() (module-scope) updates
      // the fetch-wrapper's view; setImpersonationState here triggers React
      // re-renders for the banner, write-control gating, etc.
      const [impersonationState, setImpersonationState] = useState(null);
      // Keep module-scope mirror in sync with React state.
      useEffect(() => { setImpersonation(impersonationState); }, [impersonationState]);

      // On mount: try to recover impersonation state across page reload.
      // GET /api/impersonation/active returns the caller's currently-active
      // session if any. If found, restore + auto-end on expiry.
      useEffect(() => {
        if (!authenticated) return;
        fetch(`${API_BASE}/impersonation/active`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(data => {
            if (data && data.active) {
              // We don't know target name/email from this endpoint; fetch /api/me
              // (which under the new header will return the target's profile) to
              // populate the banner label. Temporarily set the module-scope
              // wrapper so the next /api/me request carries the header.
              setImpersonation({ target_sub: data.active.target_sub, target_name: "" });
              fetch(`${API_BASE}/me`, { cache: "no-store" })
                .then(r => r.ok ? r.json() : null)
                .then(targetMe => {
                  setImpersonationState({
                    target_sub: data.active.target_sub,
                    target_name: (targetMe && (targetMe.display_name || targetMe.initials || targetMe.email)) || data.active.target_sub.slice(0, 8),
                    target_email: targetMe && targetMe.email,
                    expires_at: data.active.expires_at,
                  });
                })
                .catch(() => {});
            }
          })
          .catch(() => {});
      }, [authenticated]);

      // Start an impersonation session. Returns a promise that resolves on
      // success (with the populated state) or rejects with an error string.
      const handleStartImpersonation = React.useCallback(async (targetSub, targetName, targetEmail) => {
        const res = await fetch(`${API_BASE}/impersonation/start`, {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ target_sub: targetSub }),
        });
        if (!res.ok) {
          const err = await res.json().catch(() => ({}));
          throw new Error(err.error || `start failed (${res.status})`);
        }
        const data = await res.json();
        setImpersonationState({
          target_sub: data.target_sub,
          target_name: targetName || data.target_sub.slice(0, 8),
          target_email: targetEmail || null,
          expires_at: data.expires_at,
        });
        return data;
      }, []);

      // End the current impersonation session. Safe to call even if no
      // session is active (server returns ended: 0).
      const handleEndImpersonation = React.useCallback(async () => {
        try {
          await fetch(`${API_BASE}/impersonation/end`, {
            method: "POST",
            headers: { ...csrfHeaders() },
          });
        } catch (_) { /* swallow — clear local state anyway */ }
        setImpersonationState(null);
        setImpersonation(null);
        // Force a reload-ish behavior so any cached data from the target's
        // perspective gets re-fetched as the admin. Simplest: hard reload.
        // (Alternative: re-fetch /api/me + invalidate all caches. Reload
        // is one line and bullet-proof.)
        if (typeof window !== "undefined") window.location.reload();
      }, []);

      // Bootstrap composite (2026-05-28). Replaces ~13 parallel /api/* reads
      // on app load with a single /api/me/bootstrap request. Mitigates 429s
      // from Spaceship Hyperlift's platform-level inbound rate limit. The
      // ref starts true so the ~13 individual mount-time useEffects below
      // can check it as a "skip the duplicate fetch" guard. Bootstrap
      // completion (success OR failure) flips the ref to false so the
      // post-mount 5-min polls and post-mutation refreshes still work.
      const bootstrapHydratingRef = React.useRef(true);

      // On mount: check auth status via session cookie (set by /auth/callback)
      useEffect(() => {
        const params = new URLSearchParams(window.location.search);
        if (params.get("auth") === "error") {
          setAuthError(params.get("reason") || "unknown");
          window.history.replaceState({}, "", "/");
        }
        fetch("/api/auth/status")
          .then(r => r.json())
          .then(data => {
            setAuthenticated(data.authenticated);
            setAuthMode(data.mode === "open" ? "open" : "apple");
            if (data.authenticated) {
              refreshCsrf();
              // Don't fetch /api/me here — bootstrap useEffect below handles it.
            } else {
              // Not authed; nothing to bootstrap, free the ref so any
              // post-sign-in fetches run normally.
              bootstrapHydratingRef.current = false;
            }
          })
          .catch(() => { setAuthenticated(false); setAuthMode(null); bootstrapHydratingRef.current = false; });
      }, []);

      // Bootstrap fetch — fires when auth confirms. Hydrates 13 sections in
      // one round-trip. Each section that comes back populates its state
      // directly; the original individual mount-time useEffects skip via the
      // bootstrapHydratingRef guard. If bootstrap fails entirely, the ref
      // flips to false and the individual useEffects... won't re-fire (they
      // had [] deps and already mounted). In that case, the 5-min polls
      // catch up. Per-section _errors are tolerated — that section keeps
      // its initial state until the next poll.
      // Apply a /api/me/bootstrap payload to App-level state. Extracted from
      // the mount useEffect so refreshBootstrap (post-mutation refresh) and
      // ProfileModal close/save handlers can reuse the same hydration path
      // without duplicating setters.
      const applyBootstrap = useCallback((d) => {
        if (!d || typeof d !== "object") return;
        // me
        if (d.me) setMe(d.me);
        // workouts (history) — merge with local cache like the original useEffect did
        if (Array.isArray(d.workouts)) {
          const merged = mergeById(loadLocalHistory(), d.workouts);
          setHistory(merged);
          saveLocalHistory(merged);
          setHistoryLoaded(true);
        }
        // favorites + sets (Set wrappers)
        if (Array.isArray(d.favorites))     setFavorites(d.favorites);
        if (Array.isArray(d.disfavorites))  setDisfavorites(d.disfavorites);
        if (Array.isArray(d.favoriteSets))  setFavoriteSets(new Set(d.favoriteSets));
        if (Array.isArray(d.disfavorSets))  setDisfavorSets(new Set(d.disfavorSets));
        // effective favorites/disfavorites (three states each)
        if (d.effectiveDisfavorites && typeof d.effectiveDisfavorites === "object") {
          if (Array.isArray(d.effectiveDisfavorites.labels))  setEffectiveDisfavorLabels(d.effectiveDisfavorites.labels);
          if (Array.isArray(d.effectiveDisfavorites.set_ids)) setEffectiveDisfavorSetIds(new Set(d.effectiveDisfavorites.set_ids));
          if (Array.isArray(d.effectiveDisfavorites.engine))  setEffectiveEngineDisfavorites(d.effectiveDisfavorites.engine);
        }
        if (d.effectiveFavorites && typeof d.effectiveFavorites === "object") {
          if (Array.isArray(d.effectiveFavorites.labels))  setEffectiveFavoriteLabels(d.effectiveFavorites.labels);
          if (Array.isArray(d.effectiveFavorites.set_ids)) setEffectiveFavoriteSetIds(new Set(d.effectiveFavorites.set_ids));
          if (Array.isArray(d.effectiveFavorites.engine))  setEffectiveEngineFavorites(d.effectiveFavorites.engine);
        }
        // PSC constraints
        if (d.myConstraints && Array.isArray(d.myConstraints.constraints)) {
          setMyConstraints(d.myConstraints.constraints);
        }
        // UGC overlay — only set if shape looks right
        if (d.ugcOverlay && typeof d.ugcOverlay === "object" && "WARMUP_OPTIONS" in d.ugcOverlay) {
          setUgcOverlay(d.ugcOverlay);
        }
        // Goals
        if (Array.isArray(d.goals)) setGoals(d.goals);
        // B — sessions (auth devices list), team-defaults (inherited
        // settings preview), billing-status (subscription tier). All read
        // by ProfileModal; lifting to App-level state means the modal can
        // seed from props instead of fetching on open.
        if (Array.isArray(d.sessions))     setSessions(d.sessions);
        if (Array.isArray(d.teamDefaults)) setTeamDefaults(d.teamDefaults);
        if (Array.isArray(d.pendingInvites)) setPendingInvites(d.pendingInvites);
        if (d.billing && d.billing.status) setBillingStatus(d.billing.status);
        // Settings — applied through the shared applySettings helper so
        // we don't refetch /api/settings post-bootstrap. applySettings is
        // a stable useCallback defined later in this component; deliberately
        // not in the deps array (it's in TDZ at this point in source order,
        // and the closure resolves at call time which is post-mount).
        if (d.settings) applySettings(d.settings);
      }, []);

      // Programmatic bootstrap re-fetch. Replaces post-mutation refresh
      // bursts (5-8 GETs after ProfileModal close) with a single composite
      // GET. Returns the promise so callers can chain. Same stable-callback
      // pattern as applyBootstrap — applyBootstrap is intentionally not in deps.
      const refreshBootstrap = useCallback(() => {
        return fetch(`${API_BASE}/me/bootstrap`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(d => { applyBootstrap(d); return d; })
          .catch(() => null);
      }, []);

      useEffect(() => {
        if (!authenticated) return;
        let cancelled = false;
        fetch(`${API_BASE}/me/bootstrap`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(d => { if (!cancelled) applyBootstrap(d); })
          .catch(() => { /* tolerated; ref flip in finally */ })
          .finally(() => {
            if (!cancelled) bootstrapHydratingRef.current = false;
          });
        return () => { cancelled = true; };
      }, [authenticated]);

      // Refresh me whenever the view changes — keeps the admin feedback-count
      // badge accurate after triage (mark new → reviewed/resolved/dismissed
      // happens inside AdminView; we re-pull here when the user navigates
      // back so the badge reflects the new count). Cheap GET, indexed query.
      // Suppressed on first load by bootstrap ref — bootstrap hydrates `me`.
      useEffect(() => {
        if (!authenticated) return;
        if (bootstrapHydratingRef.current) return;
        fetch("/api/me").then(r => r.ok ? r.json() : null).then(d => { if (d) setMe(d); }).catch(() => {});
      }, [view, authenticated]);

      // On mount: fetch live history from the API, merge with localStorage cache.
      // Suppressed on first load by bootstrap ref — bootstrap hydrates history.
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        let cancelled = false;
        fetch(`${API_BASE}/workouts`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(remote => {
            if (cancelled) return;
            const merged = mergeById(loadLocalHistory(), Array.isArray(remote) ? remote : []);
            setHistory(merged);
            saveLocalHistory(merged);
            setHistoryLoaded(true);
          })
          .catch(() => { if (!cancelled) setHistoryLoaded(true); });
        return () => { cancelled = true; };
      }, []);

      // On mount: fetch favorites — suppressed on first load by bootstrap.
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        fetch(`${API_BASE}/favorites`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setFavorites(data); })
          .catch(() => {});
      }, []);

      // v1.2 — On mount: fetch disfavorites — suppressed on first load by bootstrap.
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        fetch(`${API_BASE}/disfavorites`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setDisfavorites(data); })
          .catch(() => {});
      }, []);

      // On mount: fetch per-user set-favorites — suppressed on first load by bootstrap.
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        fetch(`${API_BASE}/favorite-sets`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setFavoriteSets(new Set(data)); })
          .catch(() => {});
      }, []);

      // v1.5 — On mount: fetch per-user set-disfavorites — suppressed on first load by bootstrap.
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        fetch(`${API_BASE}/disfavor-sets`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setDisfavorSets(new Set(data)); })
          .catch(() => {});
      }, []);

      // v1.7 — On mount: fetch effective disfavorites (own + coach
      // propagation). Updated separately from the own-only lists so the
      // audit panel keeps showing only the user's own picks.
      const refreshEffectiveDisfavorites = useCallback(() => {
        return fetch(`${API_BASE}/effective-disfavorites`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(data => {
            if (!data || typeof data !== "object") return;
            if (Array.isArray(data.labels))  setEffectiveDisfavorLabels(data.labels);
            if (Array.isArray(data.set_ids)) setEffectiveDisfavorSetIds(new Set(data.set_ids));
            if (Array.isArray(data.engine))  setEffectiveEngineDisfavorites(data.engine);
          })
          .catch(() => {});
      }, []);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshEffectiveDisfavorites();
      }, [refreshEffectiveDisfavorites]);
      // Periodic refresh (2026-05-22) — coach updates while swimmer is logged
      // in stay stale until next page load. 5-min poll keeps the swimmer's
      // picker effectively current without forcing a reload. Cheap relative
      // to the picker's already-on-mount fetch; the server-side helper
      // (dbGetEffectiveDisfavorites) is a small 3-query SELECT.
      useEffect(() => {
        const id = setInterval(refreshEffectiveDisfavorites, 5 * 60 * 1000);
        return () => clearInterval(id);
      }, [refreshEffectiveDisfavorites]);

      // v1.13 — Mirror of refreshEffectiveDisfavorites for the favorite side.
      const refreshEffectiveFavorites = useCallback(() => {
        return fetch(`${API_BASE}/effective-favorites`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(data => {
            if (!data || typeof data !== "object") return;
            if (Array.isArray(data.labels))  setEffectiveFavoriteLabels(data.labels);
            if (Array.isArray(data.set_ids)) setEffectiveFavoriteSetIds(new Set(data.set_ids));
            if (Array.isArray(data.engine))  setEffectiveEngineFavorites(data.engine);
          })
          .catch(() => {});
      }, []);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshEffectiveFavorites();
      }, [refreshEffectiveFavorites]);
      // Periodic refresh mirror of the disfavor side.
      useEffect(() => {
        const id = setInterval(refreshEffectiveFavorites, 5 * 60 * 1000);
        return () => clearInterval(id);
      }, [refreshEffectiveFavorites]);

      // Phase 3 PSC slice 2 — fetch own active constraints + 5-min poll.
      // Mirrors the effective-disfavor refresh pattern. Coach updates on
      // a swimmer's constraints reach the swimmer's UI within 5 min without
      // requiring a reload (slice 3 adds coach UX; slice 4 adds visible
      // swimmer UI like ProfileModal section + AssignedToMe details).
      const refreshMyConstraints = useCallback(() => {
        return fetch(`${API_BASE}/me/constraints`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(data => {
            if (data && Array.isArray(data.constraints)) setMyConstraints(data.constraints);
          })
          .catch(() => {});
      }, []);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshMyConstraints();
      }, [refreshMyConstraints]);
      useEffect(() => {
        const id = setInterval(refreshMyConstraints, 5 * 60 * 1000);
        return () => clearInterval(id);
      }, [refreshMyConstraints]);

      // UGC Phase B — pull the UGC bank overlay on mount + 5-min poll.
      // Empty 12-key shape arrives as { WARMUP_OPTIONS: [], DRILL_OPTIONS: {}, ... }
      // until Phase C+ populates rows. Picker merges via getBankOptions concat.
      const refreshUgcOverlay = useCallback(() => {
        return fetch(`${API_BASE}/bank/my-overlay`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : null)
          .then(data => {
            if (!data || typeof data !== "object") return;
            // Validate shape minimally — must have at least one of the
            // expected keys to avoid clobbering state with garbage.
            if (!("WARMUP_OPTIONS" in data)) return;
            setUgcOverlay(data);
          })
          .catch(() => {});
      }, []);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshUgcOverlay();
      }, [refreshUgcOverlay]);
      useEffect(() => {
        const id = setInterval(refreshUgcOverlay, 5 * 60 * 1000);
        return () => clearInterval(id);
      }, [refreshUgcOverlay]);

      // On mount: fetch goals (recurring targets for stats progress bars)
      const refreshGoals = useCallback(() => {
        fetch(`${API_BASE}/goals`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => { if (Array.isArray(data)) setGoals(data); })
          .catch(() => {});
      }, []);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshGoals();
      }, [refreshGoals]);

      // Apply a settings payload to the matching React state. Extracted from
      // refreshSettings so the bootstrap useEffect can hydrate settings from
      // /api/me/bootstrap without firing an extra /api/settings GET.
      const applySettings = useCallback((s) => {
        if (!s || typeof s !== "object") return;
        if (s.sliderMin) setSliderMin(s.sliderMin);
        if (s.sliderMax) { setSliderMax(s.sliderMax); setMaxYards(v => Math.min(v, s.sliderMax)); }
        if (s.paceInput) setPaceInput(s.paceInput);
        if (s.initials)  setInitialsDraft(normalizeInitials(s.initials));
        // Q: next-event countdown lives in settings.extra; dbGetSettings
        // spreads extra into the top-level response.
        setNextEvent(s.next_event && s.next_event.name && s.next_event.date ? s.next_event : null);
        // N5: phase also in settings.extra (single string id).
        setPhase(s.phase && PHASES[s.phase] ? s.phase : null);
        // J: level preset id (settings.extra). Validated against LEVEL_PRESETS.
        setLevel(s.level && LEVEL_PRESETS[s.level] ? s.level : null);
        // W1: audio_cues persisted per-user; default ON when unset.
        if (typeof s.audio_cues === "boolean") setAudioCues(s.audio_cues);
        // Run-screen v1: lap_button persisted per-user; default ON when unset.
        if (typeof s.lap_button === "boolean") setLapButton(s.lap_button);
        // Onboarding tour: tour_seen persisted per-user. Unset (new user) →
        // false → the auto-start effect runs the tour once.
        setTourSeen(s.tour_seen === true);
        // F: equipment_modes is a per-item map { kickboard: "off"|"preferred"|"required", ... }
        if (s.equipment_modes && typeof s.equipment_modes === "object") {
          setEquipment(prev => {
            const next = { ...prev };
            for (const k of Object.keys(prev)) {
              // Normalize unknown values through equipMode() (handles legacy booleans).
              next[k] = equipMode(s.equipment_modes, k);
            }
            return next;
          });
        }
        // S3 — template engine per-section toggle + anti-repeat memory
        if (s.engine_section_sources && typeof s.engine_section_sources === "object") {
          setSectionSources(prev => ({ ...prev, ...s.engine_section_sources }));
        }
        if (Array.isArray(s.engine_recent_templates)) {
          setRecentEngineTemplates(s.engine_recent_templates.slice(0, 10));
        }
        // v1.3 — engine disfavorites
        if (Array.isArray(s.engine_disfavorites)) {
          setEngineDisfavorites(s.engine_disfavorites.slice(0, 50));
        }
        // v1.13 — own engine favorites (settings.extra.engine_favorites)
        if (Array.isArray(s.engine_favorites)) {
          setEngineFavorites(s.engine_favorites.slice(0, 50));
        }
        // v2.0 polish — multi-lane persisted state (enabled + lanes)
        if (s.multi_lane && typeof s.multi_lane === "object") {
          if (typeof s.multi_lane.enabled === "boolean") {
            setMultiLaneMode(s.multi_lane.enabled);
          }
          if (Array.isArray(s.multi_lane.lanes) && s.multi_lane.lanes.length > 0) {
            setManualLanesPace(s.multi_lane.lanes.slice(0, 12).map((l, i) => ({
              lane_label: l.lane_label || `Lane ${i + 1}`,
              pace: l.pace || "",
            })));
          }
        }
        // v1.8 — disfavor mode (downweight | exclude)
        if (s.disfavor_mode === "downweight" || s.disfavor_mode === "exclude") {
          setDisfavorMode(s.disfavor_mode);
        }
      }, []);

      // On mount: fetch user settings (slider range + default pace). Suppressed
      // on first load by the bootstrap ref guard — bootstrap calls applySettings
      // directly. Post-bootstrap (programmatic refresh) it still GETs.
      const refreshSettings = useCallback(() => {
        return fetch(`${API_BASE}/settings`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : {})
          .then(applySettings)
          .catch(() => {});
      }, [applySettings]);
      useEffect(() => {
        if (bootstrapHydratingRef.current) return;
        refreshSettings();
      }, [refreshSettings]);

      // W1: persist the audio_cues toggle to settings.extra without disturbing
      // other extra keys. Optimistic local update; best-effort save.
      const handleAudioCuesToggle = useCallback((next) => {
        setAudioCues(next);
        fetch(`${API_BASE}/settings/extra`, {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ audio_cues: next }),
        }).catch(() => {});
      }, []);

      // Run-screen v1: persist the lap_button toggle. Mirror of audio_cues
      // pattern — optimistic local update + best-effort POST.
      const handleLapButtonToggle = useCallback((next) => {
        setLapButton(next);
        fetch(`${API_BASE}/settings/extra`, {
          method: "POST",
          headers: { "Content-Type": "application/json", ...csrfHeaders() },
          body: JSON.stringify({ lap_button: next }),
        }).catch(() => {});
      }, []);

      // Onboarding tour controls. Auto-start once for users whose tour_seen
      // is unset (resolves to false in applySettings). startTour replays it
      // on demand. finishTour ends it and persists tour_seen=true so it
      // won't auto-run again (best-effort; one write per session via ref).
      useEffect(() => {
        if (!tourAutoRef.current && tourSeen === false) {
          tourAutoRef.current = true;
          setTourStep(0);
        }
      }, [tourSeen]);
      const startTour = useCallback(() => { setTourStep(0); }, []);
      const finishTour = useCallback(() => {
        setTourStep(-1);
        setTourSeen(true);
        if (!tourSeenWriteRef.current) {
          tourSeenWriteRef.current = true;
          fetch(`${API_BASE}/settings/extra`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ tour_seen: true }),
          }).catch(() => {});
        }
      }, []);

      // v2.0 polish — Persist multi-lane state (enabled + lanes) to
      // settings.extra. Debounced 800ms so per-keystroke pace edits coalesce
      // into one POST. The ref skips the first effect run (initial render
      // with defaults) so the second (post-refreshSettings) is the first
      // server write. Harmless round-trip if user had persisted state.
      const isFirstMultiLaneEffect = React.useRef(true);
      const multiLaneSaveTimerRef = React.useRef(null);
      useEffect(() => {
        if (isFirstMultiLaneEffect.current) {
          isFirstMultiLaneEffect.current = false;
          return;
        }
        clearTimeout(multiLaneSaveTimerRef.current);
        multiLaneSaveTimerRef.current = setTimeout(() => {
          fetch(`${API_BASE}/settings/extra`, {
            method: "POST",
            credentials: "include",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({
              multi_lane: { enabled: multiLaneMode, lanes: manualLanesPace },
            }),
          }).catch(() => {});
        }, 800);
      }, [multiLaneMode, manualLanesPace]);

      // Debounced settings save — fires 1.5s after the last change
      const saveSettingsTimerRef = React.useRef(null);
      function saveSettings(min, max, pace, initials) {
        clearTimeout(saveSettingsTimerRef.current);
        saveSettingsTimerRef.current = setTimeout(() => {
          fetch(`${API_BASE}/settings`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ sliderMin: min, sliderMax: max, paceInput: pace, initials }),
          }).catch(() => {});
        }, 1500);
      }

      // Range change: update bounds, clamp current value, persist
      const handleRangeChange = useCallback((newMin, newMax) => {
        setSliderMin(newMin);
        setSliderMax(newMax);
        setMaxYards(v => Math.max(newMin, Math.min(v, newMax)));
        setWorkout(null);
        setSaveStatus(null);
        saveSettings(newMin, newMax, paceInput, initialsDraft);
      }, [paceInput, initialsDraft]);

      // Pace change with settings persistence
      const handlePaceChange = useCallback((val) => {
        setPaceInput(val);
        saveSettings(sliderMin, sliderMax, val, initialsDraft);
      }, [sliderMin, sliderMax, initialsDraft]);

      // Initials change with settings persistence
      const handleInitialsChange = useCallback((val) => {
        const normalized = normalizeInitials(val);
        setInitialsDraft(normalized);
        saveSettings(sliderMin, sliderMax, paceInput, normalized);
      }, [sliderMin, sliderMax, paceInput]);

      // Toggle a main-set label in/out of favorites
      const handleToggleFavorite = useCallback(async (label) => {
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Favorites disabled in view-as mode"); return; }
        const isFav = favorites.includes(label);
        // Optimistic update
        setFavorites(prev => isFav ? prev.filter(x => x !== label) : [...prev, label]);
        // v1.2 — adding favorite implicitly removes disfavor (server-side mutex
        // in dbAddFavorite). Reflect locally so the UI doesn't briefly show both.
        if (!isFav) setDisfavorites(prev => prev.filter(x => x !== label));
        try {
          if (isFav) {
            await fetch(`${API_BASE}/favorites/${encodeURIComponent(label)}`, {
              method: "DELETE",
              headers: { ...csrfHeaders() },
            });
          } else {
            await fetch(`${API_BASE}/favorites`, {
              method: "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body: JSON.stringify({ label }),
            });
          }
        } catch (_) {
          // Revert optimistic update on error
          setFavorites(prev => isFav ? [...prev, label] : prev.filter(x => x !== label));
        }
      }, [favorites]);

      // v1.3 — Toggle an engine (template_id, stroke) pair in/out of
      // engine_disfavorites (settings.extra JSON array). 0.25× weight in
      // the engine template picker.
      // v1.13 — Now mutex-enforced with engine_favorites: adding a
      // disfavor strips any matching favorite, both updated in a single
      // /api/settings/extra patch.
      const handleToggleEngineDisfavor = useCallback(async (meta) => {
        if (!meta || !meta.template_id || !meta.stroke) return;
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Engine curation disabled in view-as mode"); return; }
        const key = `${meta.template_id}:${meta.stroke}`;
        const isDis = engineDisfavorites.some(e => `${e.template_id}:${e.stroke}` === key);
        const nextDis = isDis
          ? engineDisfavorites.filter(e => `${e.template_id}:${e.stroke}` !== key)
          : [...engineDisfavorites, { template_id: meta.template_id, stroke: meta.stroke }].slice(0, 50);
        // v1.13 — mutex: if we're ADDING a disfavor, clear any matching favorite
        const nextFav = (!isDis)
          ? engineFavorites.filter(e => `${e.template_id}:${e.stroke}` !== key)
          : engineFavorites;
        setEngineDisfavorites(nextDis);
        if (!isDis) setEngineFavorites(nextFav);
        if (typeof fetch === "function" && csrf.token) {
          const body = { engine_disfavorites: nextDis };
          if (!isDis) body.engine_favorites = nextFav;
          fetch(`${API_BASE}/settings/extra`, {
            method: "POST",
            credentials: "include",
            headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf.token },
            body: JSON.stringify(body),
          }).catch(() => {
            // Revert optimistic update on network error
            setEngineDisfavorites(engineDisfavorites);
            setEngineFavorites(engineFavorites);
          });
        }
      }, [engineDisfavorites, engineFavorites]);

      // v1.13 — Mirror of handleToggleEngineDisfavor for the favorite side.
      // 3× weight in the engine template picker. Mutex with disfavorites.
      const handleToggleEngineFavorite = useCallback(async (meta) => {
        if (!meta || !meta.template_id || !meta.stroke) return;
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Engine curation disabled in view-as mode"); return; }
        const key = `${meta.template_id}:${meta.stroke}`;
        const isFav = engineFavorites.some(e => `${e.template_id}:${e.stroke}` === key);
        const nextFav = isFav
          ? engineFavorites.filter(e => `${e.template_id}:${e.stroke}` !== key)
          : [...engineFavorites, { template_id: meta.template_id, stroke: meta.stroke }].slice(0, 50);
        // Mutex: adding a favorite strips any matching disfavor
        const nextDis = (!isFav)
          ? engineDisfavorites.filter(e => `${e.template_id}:${e.stroke}` !== key)
          : engineDisfavorites;
        setEngineFavorites(nextFav);
        if (!isFav) setEngineDisfavorites(nextDis);
        if (typeof fetch === "function" && csrf.token) {
          const body = { engine_favorites: nextFav };
          if (!isFav) body.engine_disfavorites = nextDis;
          fetch(`${API_BASE}/settings/extra`, {
            method: "POST",
            credentials: "include",
            headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf.token },
            body: JSON.stringify(body),
          }).catch(() => {
            setEngineFavorites(engineFavorites);
            setEngineDisfavorites(engineDisfavorites);
          });
        }
      }, [engineFavorites, engineDisfavorites]);

      // v1.2 — Toggle a main-set label in/out of disfavorites. Mirror of
      // handleToggleFavorite with 0.25× pick weight in the picker. Server-
      // side dbAddDisfavorite removes any matching favorite on insert; we
      // reflect that locally for instant UI consistency.
      const handleToggleDisfavorite = useCallback(async (label) => {
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Disfavorites disabled in view-as mode"); return; }
        const isDis = disfavorites.includes(label);
        setDisfavorites(prev => isDis ? prev.filter(x => x !== label) : [...prev, label]);
        if (!isDis) setFavorites(prev => prev.filter(x => x !== label));
        try {
          if (isDis) {
            await fetch(`${API_BASE}/disfavorites/${encodeURIComponent(label)}`, {
              method: "DELETE",
              headers: { ...csrfHeaders() },
            });
          } else {
            await fetch(`${API_BASE}/disfavorites`, {
              method: "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body: JSON.stringify({ label }),
            });
          }
        } catch (_) {
          setDisfavorites(prev => isDis ? [...prev, label] : prev.filter(x => x !== label));
        }
      }, [disfavorites]);

      // Toggle a single set in/out of per-user favorites. Optimistic update
      // mirrors the label-favorite handler. Caller must pass a valid set_id
      // ("s_xxxxxx") — UI hides the star button on un-IDed sets.
      const handleToggleFavoriteSet = useCallback(async (setId) => {
        if (!setId) return;
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Set favorites disabled in view-as mode"); return; }
        const isFav = favoriteSets.has(setId);
        setFavoriteSets(prev => {
          const next = new Set(prev);
          if (isFav) next.delete(setId); else next.add(setId);
          return next;
        });
        // v1.6 — mirror server-side mutex locally: adding to favorites
        // removes from disfavor-sets for the same set_id.
        if (!isFav) {
          setDisfavorSets(prev => {
            if (!prev.has(setId)) return prev;
            const next = new Set(prev); next.delete(setId); return next;
          });
        }
        try {
          if (isFav) {
            await fetch(`${API_BASE}/favorite-sets/${encodeURIComponent(setId)}`, {
              method: "DELETE",
              headers: { ...csrfHeaders() },
            });
          } else {
            await fetch(`${API_BASE}/favorite-sets`, {
              method: "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body: JSON.stringify({ setId }),
            });
          }
        } catch (_) {
          // Revert optimistic update on error
          setFavoriteSets(prev => {
            const next = new Set(prev);
            if (isFav) next.add(setId); else next.delete(setId);
            return next;
          });
        }
      }, [favoriteSets]);

      // v1.6 — Toggle a single set in/out of per-user disfavor-sets.
      // Mirror of handleToggleFavoriteSet. Local mutex with favoriteSets
      // (server-side dbAddDisfavorSet already enforces this).
      const handleToggleDisfavorSet = useCallback(async (setId) => {
        if (!setId) return;
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Set disfavorites disabled in view-as mode"); return; }
        const isDis = disfavorSets.has(setId);
        setDisfavorSets(prev => {
          const next = new Set(prev);
          if (isDis) next.delete(setId); else next.add(setId);
          return next;
        });
        if (!isDis) {
          setFavoriteSets(prev => {
            if (!prev.has(setId)) return prev;
            const next = new Set(prev); next.delete(setId); return next;
          });
        }
        try {
          if (isDis) {
            await fetch(`${API_BASE}/disfavor-sets/${encodeURIComponent(setId)}`, {
              method: "DELETE",
              headers: { ...csrfHeaders() },
            });
          } else {
            await fetch(`${API_BASE}/disfavor-sets`, {
              method: "POST",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body: JSON.stringify({ setId }),
            });
          }
        } catch (_) {
          setDisfavorSets(prev => {
            const next = new Set(prev);
            if (isDis) next.add(setId); else next.delete(setId);
            return next;
          });
        }
      }, [disfavorSets]);

      // v1.6 — Cycle a set's status: neutral → favorite → disfavor → neutral.
      // Used by the SetRow's per-set status button. Both fav and disfavor
      // handlers carry server-side AND local mutex, so a single call per
      // transition suffices.
      const handleCycleSetStatus = useCallback((setId) => {
        if (!setId) return;
        const isFav = favoriteSets.has(setId);
        const isDis = disfavorSets.has(setId);
        if (!isFav && !isDis) handleToggleFavoriteSet(setId);  // neutral → favorite
        else if (isFav)       handleToggleDisfavorSet(setId);  // favorite → disfavor (server mutex clears favorite)
        else                  handleToggleDisfavorSet(setId);  // disfavor → neutral (just untoggle)
      }, [favoriteSets, disfavorSets, handleToggleFavoriteSet, handleToggleDisfavorSet]);

      const [openSwapKey, setOpenSwapKey] = useState(null);

      const handleToggleSwap = useCallback((blockIdx, setIdx) => {
        const key = `${blockIdx}-${setIdx}`;
        setOpenSwapKey(prev => prev === key ? null : key);
      }, []);

      const handleApplySwap = useCallback((blockIdx, setIdx, newSet) => {
        setWorkout(prev => {
          const blocks = prev.blocks.map((b, bi) => {
            if (bi !== blockIdx) return b;
            const sets = b.sets.map((s, si) => si === setIdx ? newSet : s);
            const totalYards = sets.reduce((sum, s) => sum + s.reps * s.dist, 0) * (b.rounds || 1);
            return { ...b, sets, totalYards };
          });
          return { ...prev, blocks };
        });
        setOpenSwapKey(null);
      }, []);

      // ── Pace rescaling ────────────────────────────────────────────
      const PACE_BASELINE_SECS = 120; // app intervals calibrated for 2:00/100yd

      const handleApplyPace = useCallback(() => {
        const parts = paceInput.trim().split(":");
        if (parts.length !== 2) return;
        const userSecs = parseInt(parts[0], 10) * 60 + parseInt(parts[1], 10);
        if (!userSecs || userSecs < 30 || userSecs > 300) return;
        const ratio = userSecs / PACE_BASELINE_SECS;
        setWorkout(prev => {
          const newBlocks = prev.blocks.map(b => Array.isArray(b.sets) ? ({
            ...b,
            sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, ratio) })),
          }) : b);   // Section model B — dryland blocks have no intervals; pass through.
          return { ...prev, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
        });
      }, [paceInput]);

      // ── Per-set interval editing ──────────────────────────────────
      const [editIntervalKey,   setEditIntervalKey]   = useState(null);
      const [editIntervalDraft, setEditIntervalDraft] = useState("");
      // S2 — inline rejection message when normalizeIntervalInput can't parse
      // the draft. Cleared on successful commit, cancel, or next keystroke.
      const [editIntervalError, setEditIntervalError] = useState(null);

      const handleStartEditInterval = useCallback((bIdx, sIdx, current) => {
        setEditIntervalKey(`${bIdx}-${sIdx}`);
        setEditIntervalDraft(current);
        setEditIntervalError(null);
        setOpenSwapKey(null);
      }, []);

      // S2 — commit goes through normalizeIntervalInput. Empty draft closes
      // the editor without changes; unparseable input keeps the editor open
      // and surfaces an inline error so the user can fix it; valid input is
      // stored as the canonical string (math always sees canonical form).
      const handleCommitInterval = useCallback((bIdx, sIdx, draft) => {
        const norm = normalizeIntervalInput(draft);
        if (norm.error) {
          setEditIntervalError(norm.error);
          return; // keep editor open so user can correct
        }
        if (norm.value != null) {
          setWorkout(prev => {
            const newBlocks = prev.blocks.map((b, bi) => bi !== bIdx ? b : {
              ...b,
              sets: b.sets.map((s, si) => si !== sIdx ? s : { ...s, interval: norm.value }),
            });
            return { ...prev, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
          });
        }
        setEditIntervalKey(null);
        setEditIntervalDraft("");
        setEditIntervalError(null);
      }, []);

      // S2 — ⊘ button: clears the set's interval to the canonical no-interval
      // sentinel. calcEstimatedMin will fall back to yd/min for that set.
      const handleClearInterval = useCallback((bIdx, sIdx) => {
        setWorkout(prev => {
          const newBlocks = prev.blocks.map((b, bi) => bi !== bIdx ? b : {
            ...b,
            sets: b.sets.map((s, si) => si !== sIdx ? s : { ...s, interval: NO_INTERVAL_CANONICAL }),
          });
          return { ...prev, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
        });
        setEditIntervalKey(null);
        setEditIntervalDraft("");
        setEditIntervalError(null);
      }, []);

      const handleCancelInterval = useCallback(() => {
        setEditIntervalKey(null);
        setEditIntervalDraft("");
        setEditIntervalError(null);
      }, []);

      // ── Round rest editing ───────────────────────────────────────
      const handleRoundRestChange = useCallback((blockIdx, newSecs) => {
        const secs = Math.max(0, Math.min(300, parseInt(newSecs, 10) || 0));
        setWorkout(prev => {
          const newBlocks = prev.blocks.map((b, i) =>
            i !== blockIdx ? b : { ...b, roundRestSecs: secs }
          );
          return { ...prev, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
        });
      }, []);

      // ── Per-set description editing ───────────────────────────────
      const [editDescKey,   setEditDescKey]   = useState(null);
      const [editDescDraft, setEditDescDraft] = useState("");

      const handleStartEditDesc = useCallback((bIdx, sIdx, current) => {
        setEditDescKey(`${bIdx}-${sIdx}`);
        setEditDescDraft(current);
        setOpenSwapKey(null);
        setEditIntervalKey(null);
      }, []);

      const handleCommitDesc = useCallback((bIdx, sIdx, draft) => {
        const val = draft.trim();
        if (val) {
          setWorkout(prev => {
            const newBlocks = prev.blocks.map((b, bi) => bi !== bIdx ? b : {
              ...b,
              sets: b.sets.map((s, si) => si !== sIdx ? s : { ...s, desc: val }),
            });
            return { ...prev, blocks: newBlocks };
          });
        }
        setEditDescKey(null);
        setEditDescDraft("");
      }, []);

      const handleCancelDesc = useCallback(() => {
        setEditDescKey(null);
        setEditDescDraft("");
      }, []);

      // W2 — Apply parsed-intent tokens to generator form state. Each token
      // category maps to a setter; tokens with no setter (e.g. zone) are
      // silent no-ops by design (visible in the chip preview only). When
      // duration AND distance both present, distance wins (it's more direct).
      // Multiple type tokens: first wins.
      const handleApplyIntent = useCallback((parsed) => {
        if (!parsed || !parsed.tokens || !parsed.tokens.length) return;
        const isMeters = poolMode === "25m" || poolMode === "50m";

        // Pre-pass: capture pace before computing distance from duration
        // (duration → maxYards needs paceSecs). Apply pace immediately so
        // the input is consistent if Generate fires next.
        const paceTok = parsed.tokens.find(t => t.kind === "pace");
        if (paceTok) {
          const m  = Math.floor(paceTok.value / 60);
          const ss = String(paceTok.value % 60).padStart(2, "0");
          setPaceInput(`${m}:${ss}`);
        }
        const effectivePaceSecs = paceTok
          ? paceTok.value
          : (() => {
              const parts = (paceInput || "").trim().split(":");
              if (parts.length !== 2) return 120;
              const m = parseInt(parts[0], 10), s = parseInt(parts[1], 10);
              return (isNaN(m) || isNaN(s)) ? 120 : m * 60 + s;
            })();

        // Track which categories have already applied so multi-token kinds
        // honor the "first wins" rule (e.g. "im technique" → im selected).
        let appliedType = false;
        let appliedDistance = false;

        for (const tok of parsed.tokens) {
          switch (tok.kind) {
            case "type":
              if (!appliedType) {
                setSelectedType(tok.value);
                appliedType = true;
              }
              break;
            case "distance": {
              if (appliedDistance) break;
              let yards = tok.value;
              if (tok.unit === "m" && !isMeters)      yards = Math.round(tok.value * 1.0936 / 50) * 50;
              else if (tok.unit === "y" && isMeters)  yards = Math.round(tok.value * 0.9144 / 50) * 50;
              const lo = isMeters ? 1300 : 1200;
              const hi = 5000;
              setMaxYards(Math.max(lo, Math.min(hi, yards)));
              appliedDistance = true;
              break;
            }
            case "duration": {
              if (appliedDistance) break;  // explicit distance wins
              const y = Math.round(2400 * (tok.value / 60) * (120 / effectivePaceSecs) / 50) * 50;
              const lo = isMeters ? 2000 : 1900;
              const hi = 5000;
              setMaxYards(Math.max(lo, Math.min(hi, y)));
              appliedDistance = true;  // treat duration as having set the budget
              break;
            }
            case "recovery":
              setRecoveryMode(true);
              break;
            case "equipment":
              setEquipment(prev => ({ ...prev, [tok.value]: "preferred" }));
              break;
            case "pace":
              // Already applied in the pre-pass above.
              break;
            case "zone":
              // No direct form control — visible in chip preview only.
              // Future: a "preferred zone" hint could bias the picker.
              break;
            default:
              break;
          }
        }
      }, [poolMode, paceInput]);

      // ── Late-declared deps moved up so the useCallback dep arrays below see
      // real values (not the var-hoisted undefined). These previously sat near
      // line 15017+ which caused stale-closure risk in handleGenerate /
      // handleRegenerateSection / handleSave. ────────────────────────────

      // R-D: generate-for picker. State = group_id string OR "myself".
      // coachTargets is the loaded list of groups the user coaches (with
      // member counts + current_phase). Loaded once when authenticated and
      // is_coach. Defaults to "myself" — solo training is the default flow.
      const [generateForId, setGenerateForId] = useState("myself");
      const [coachTargets,   setCoachTargets]  = useState([]);
      // R-E: lane plans for the currently-selected group (loaded on demand).
      // generateForPlanId is "" = no plan (stored paces) or plan id.
      const [lanePlansForTarget, setLanePlansForTarget] = useState([]);
      const [generateForPlanId,  setGenerateForPlanId]  = useState("");
      useEffect(() => {
        if (!authenticated || !effectiveMe?.is_coach) { setCoachTargets([]); return; }
        fetch("/api/picker/coach-targets", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => setCoachTargets(Array.isArray(data) ? data : []))
          .catch(() => setCoachTargets([]));
      }, [authenticated, effectiveMe?.is_coach, view]);
      // R-E: when a multi-member group is picked, load its lane plans. Reset
      // selection to default plan if one exists (so "Generate" with the
      // default plan is a one-click flow on each visit).
      useEffect(() => {
        if (generateForId === "myself" || !generateForId) { setLanePlansForTarget([]); setGenerateForPlanId(""); return; }
        const target = coachTargets.find(t => t.id === generateForId);
        if (!target || target.member_count < 2) { setLanePlansForTarget([]); setGenerateForPlanId(""); return; }
        fetch(`/api/groups/${generateForId}/lane-plans`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(plans => {
            const arr = Array.isArray(plans) ? plans : [];
            setLanePlansForTarget(arr);
            const def = arr.find(p => p.is_default);
            setGenerateForPlanId(def ? def.id : "");
          })
          .catch(() => { setLanePlansForTarget([]); setGenerateForPlanId(""); });
      }, [generateForId, coachTargets]);
      // Resolve the currently-selected target (or null for "myself").
      const generateForTarget = useMemo(() => {
        if (generateForId === "myself" || !generateForId) return null;
        return coachTargets.find(t => t.id === generateForId) || null;
      }, [generateForId, coachTargets]);
      // Effective phase: group's current_phase overrides the user's personal
      // phase when generating for a group (decision #39).
      const effectivePhase = generateForTarget?.current_phase || phase;

      // Phase 3 PSC slice 3 — per-practice checklist data fetch.
      // When Generate target switches to a group, fetch its active constraints
      // roll-up so the coach can opt in (per-practice) to additional restrictions.
      // Selection resets per target switch (no leaky-state between groups).
      useEffect(() => {
        setTonightSelected(new Set());
        if (!generateForTarget || !generateForTarget.id) {
          setGroupActiveConstraints({});
          return;
        }
        fetch(`${API_BASE}/groups/${encodeURIComponent(generateForTarget.id)}/active-constraints`, { cache: "no-store" })
          .then(r => r.ok ? r.json() : {})
          .then(map => { if (map && typeof map === "object") setGroupActiveConstraints(map); else setGroupActiveConstraints({}); })
          .catch(() => setGroupActiveConstraints({}));
      }, [generateForTarget?.id]);

      // Derived: flat list of (swimmerKey, constraint) pairs for the checklist.
      // swimmerKey is either users.sub (real swimmer) or ms_xxxx (managed).
      const tonightChecklistRows = useMemo(() => {
        const out = [];
        for (const [swimmerKey, list] of Object.entries(groupActiveConstraints || {})) {
          if (!Array.isArray(list)) continue;
          for (const c of list) out.push({ swimmerKey, c });
        }
        return out;
      }, [groupActiveConstraints]);

      // The subset of checked constraints, expanded into the picker-shape array.
      const tonightOverrides = useMemo(() => {
        if (!tonightSelected.size) return [];
        const out = [];
        for (const { c } of tonightChecklistRows) {
          if (tonightSelected.has(c.id)) out.push(c);
        }
        return out;
      }, [tonightChecklistRows, tonightSelected]);

      // N1: recently-used main-set labels for the signed-in user (last 7 days).
      // Returns Map<label, daysAgo> — used for (a) generator weight down-bias and
      // (b) "last seen Nd ago" badges in the favorites panel + main-set blocks.
      const recentMainLabels = useMemo(() => {
        const mySub = me?.sub;
        const map = new Map();
        if (!mySub || !history.length) return map;
        const today = new Date().toISOString().slice(0, 10);
        const cutoffMs = Date.now() - 7 * 24 * 60 * 60 * 1000;
        for (const e of history) {
          if (e.sub !== mySub) continue;
          if (e.completed === false) continue;
          if (!e.dateCompleted) continue;
          const ts = new Date(`${e.dateCompleted}T00:00:00Z`).getTime();
          if (ts < cutoffMs) continue;
          if (!Array.isArray(e.blocks)) continue;
          const daysAgo = Math.max(0, Math.round((new Date(today) - new Date(e.dateCompleted)) / 86400000));
          for (const b of e.blocks) {
            const label = extractMainLabel(b);
            if (!label) continue;
            const prev = map.get(label);
            if (prev === undefined || daysAgo < prev) map.set(label, daysAgo);
          }
        }
        return map;
      }, [history, me]);

      const handleGenerate = useCallback(() => {
        if (!selectedType) return;
        // Build pinned block map from current workout state
        const pinned = {};
        if (workout) {
          for (const b of workout.blocks) {
            if (pinnedSections[b.section]) pinned[b.section] = b;
          }
        }
        // S4 #7 — union saved-history labels with this session's just-picked labels.
        const recentLabels = Array.from(new Set([
          ...recentMainLabels.keys(),
          ...sessionRecentLabels,
        ]));
        // v2.0 — multi-lane: parse rows into pace-seconds array. Invalid rows
        // are dropped silently (the input box turns red so the coach sees it).
        // If multiLaneMode is OFF or no valid lanes, fall back to single-pace.
        const lanesPaceSecs = multiLaneMode
          ? manualLanesPace.map(l => parsePaceMSS(l.pace)).filter(s => s !== null)
          : null;
        const useMultiLane = multiLaneMode && Array.isArray(lanesPaceSecs) && lanesPaceSecs.length > 0;
        let newWorkout = generateWorkout({
          typeId:         selectedType,
          maxYards,
          // v1.13 — same union pattern as disfavor: own + coach-propagated.
          favorites:      Array.from(new Set([...favorites, ...effectiveFavoriteLabels])),
          equipment,
          poolMode,
          userMin:        sliderMin,
          pinnedBlocks:   pinned,
          recentLabels,
          recoveryMode,
          phase: effectivePhase,                                               // R-D: group-context override (#39)
          favoriteSetIds: new Set([...favoriteSets, ...effectiveFavoriteSetIds]),
          sectionBias,                                                          // Real-shift section-proportion controls (Mix pills)
          includedSections,                                                     // Section model A2 — skip warmup/drill/cooldown per workout
          sectionSources,                                                       // S3 — Bank/Engine/Mix per section
          recentEngineTemplates,                                                // S3 — anti-repeat memory for engine
          // v1.13 — engine favorites union (own + coach-propagated)
          engineFavorites: (() => {
            const seen = new Set();
            const out = [];
            for (const e of [...engineFavorites, ...effectiveEngineFavorites]) {
              const k = `${e.template_id}:${e.stroke}`;
              if (!seen.has(k)) { seen.add(k); out.push(e); }
            }
            return out;
          })(),
          // v1.7 — union user's OWN disfavorites with effective coach
          // propagation. Union (not effective alone) so optimistic toggle
          // updates apply immediately even before refreshEffectiveDisfavorites
          // re-fetches. Coach contributions stay invisible to the swimmer's
          // audit panel (which reads own-only state).
          disfavorites: Array.from(new Set([...disfavorites, ...effectiveDisfavorLabels])),
          engineDisfavorites: (() => {
            const seen = new Set();
            const out = [];
            for (const e of [...engineDisfavorites, ...effectiveEngineDisfavorites]) {
              const k = `${e.template_id}:${e.stroke}`;
              if (!seen.has(k)) { seen.add(k); out.push(e); }
            }
            return out;
          })(),
          disfavorSetIds: new Set([...disfavorSets, ...effectiveDisfavorSetIds]),
          disfavorMode,                                                         // v1.8 — downweight or exclude
          lanesPaceSecs: useMultiLane ? lanesPaceSecs : null,                   // v2.0 — multi-lane fit filter
          ugcOverlay,                                                           // UGC Phase B — bank overlay merged in getBankOptions
          myConstraints,                                                         // Phase 3 PSC slice 2 — step-0 hard-exclude (skipped in multi-lane)
          tonightOverrides,                                                       // Phase 3 PSC slice 3 — per-practice checklist selections (in-memory, never persisted)
        });
        // S3 #3 — generateWorkout returns a failure sentinel when required equipment
        // can't be satisfied. Surface the message and bail before any post-processing.
        if (newWorkout && newWorkout.__generateFailure) {
          setGenerateError(newWorkout.error || "Could not build a workout matching your constraints.");
          return;
        }
        // R-D: stamp assignment target onto the workout so the pill renders in
        // the workout-display header AND survives to history (the payload JSON
        // round-trips this field).
        if (generateForTarget) {
          newWorkout.assignment_target = {
            kind:         "group",
            group_id:     generateForTarget.id,
            group_name:   generateForTarget.name,
            member_count: generateForTarget.member_count,
            phase:        generateForTarget.current_phase || null,
          };
        }
        setGenerateError(null);
        // Apply user's pace to intervals at generation time (not just on "Rescale all")
        const parts = paceInput.trim().split(":");
        if (parts.length === 2) {
          const userSecs = parseInt(parts[0], 10) * 60 + parseInt(parts[1], 10);
          if (userSecs && userSecs >= 30 && userSecs <= 300 && userSecs !== PACE_BASELINE_SECS) {
            const ratio = userSecs / PACE_BASELINE_SECS;
            const newBlocks = newWorkout.blocks.map(b => ({
              ...b,
              sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, ratio) })),
            }));
            newWorkout = { ...newWorkout, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
          }
        }
        // C: recovery mode → stretch every interval +10% on top of any pace
        // scaling, and mark the workout so the UI can render the green badge.
        if (recoveryMode) {
          const newBlocks = newWorkout.blocks.map(b => ({
            ...b,
            sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, 1.10) })),
          }));
          newWorkout = { ...newWorkout, blocks: newBlocks, recovery: true, estimatedMin: calcEstimatedMin(newBlocks) };
        }
        // N5: stamp the current training phase on the workout so the soft
        // badge renders alongside the summary bar.
        if (phase && PHASES[phase]) {
          newWorkout = { ...newWorkout, phase };
        }
        // v2.1 — stamp multi-lane state onto the workout so save-to-history
        // round-trips it. Workout payload is JSON server-side so no migration.
        if (useMultiLane) {
          const lanesForPersist = manualLanesPace
            .filter(l => parsePaceMSS(l.pace) !== null)
            .map(l => ({ lane_label: l.lane_label || "Lane", pace: l.pace }));
          if (lanesForPersist.length) {
            newWorkout = { ...newWorkout, multi_lane: { lanes: lanesForPersist } };
          }
        }
        setWorkout(newWorkout);
        // v2.0 — multi-lane: auto-route to MultiPacePrintView. The print view
        // takes the canonical workout + lane paces and renders per-lane pages.
        // setMultiPaceLanes mounts the print overlay; coach can dismiss to
        // return to the editor with the workout still loaded.
        if (useMultiLane && newWorkout.multi_lane?.lanes?.length) {
          setMultiPaceLanes({ lanes: newWorkout.multi_lane.lanes, mode: "per_lane" });
        }
        // S3 — write side: prepend engine entries to recent[], cap at 10,
        // post to settings.extra. Fire-and-forget; errors don't block UI.
        const newEngineEntries = [];
        for (const b of newWorkout.blocks || []) {
          if (b && b.__engineMeta && b.__engineMeta.template_id) {
            newEngineEntries.push({
              template_id: b.__engineMeta.template_id,
              stroke: b.__engineMeta.stroke || "free",
              ts: Date.now(),
            });
          }
        }
        if (newEngineEntries.length) {
          setRecentEngineTemplates(prev => {
            const next = [...newEngineEntries, ...prev].slice(0, 10);
            if (typeof fetch === "function" && csrf.token) {
              fetch(`${API_BASE}/settings/extra`, {
                method: "POST",
                credentials: "include",
                headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf.token },
                body: JSON.stringify({ engine_recent_templates: next }),
              }).catch(() => {});
            }
            return next;
          });
        }
        // S4 #7 — record this session's main label for future anti-repeat.
        const mainLabel = newWorkout.blocks && newWorkout.blocks[2] && newWorkout.blocks[2].label;
        if (mainLabel) setSessionRecentLabels(prev => new Set([...prev, mainLabel]));
        setLoadedFromHistoryId(null);
        setSaveStatus(null); setSaveError(null);
        setRegenError(null);
        setDateDraft(new Date().toISOString().slice(0, 10));
        setNoteDraft("");
        setFocusNoteDraft("");
        setDifficultyDraft(null);
        setOpenSwapKey(null);
        setEditIntervalKey(null);
        setEditDescKey(null);
      }, [selectedType, maxYards, equipment, favorites, poolMode, paceInput, sliderMin, pinnedSections, workout, recentMainLabels, sessionRecentLabels, recoveryMode, phase, favoriteSets, effectivePhase, generateForTarget, sectionBias, sectionSources, recentEngineTemplates, disfavorites, engineDisfavorites, disfavorSets, effectiveDisfavorLabels, effectiveDisfavorSetIds, effectiveEngineDisfavorites, disfavorMode, engineFavorites, effectiveFavoriteLabels, effectiveFavoriteSetIds, effectiveEngineFavorites, multiLaneMode, manualLanesPace, includedSections]);

      // Regenerate one section in place, holding the other three fixed.
      // On failure (no valid alternative), keep the workout untouched and
      // surface a per-section inline error.
      const handleRegenerateSection = useCallback((sectionKey, sourceOverride = null) => {
        if (!workout || !selectedType) return;
        if (pinnedSections[sectionKey]) return; // pinned sections can't be individually regenerated
        const isRecovery = !!workout.recovery;
        // S4 #7 — same union as handleGenerate so main regenerations also avoid repeats.
        const recentLabels = Array.from(new Set([
          ...recentMainLabels.keys(),
          ...sessionRecentLabels,
        ]));
        // v1.3 fix — sourceOverride lets callers (e.g. the section-source
        // toggle's setTimeout) pass the new source EXPLICITLY, bypassing
        // stale-closure issues where sectionSources hadn't repropagated yet
        // when the regen was queued. If no override, read current state.
        const effectiveSource = sourceOverride || sectionSources[sectionKey] || "bank";
        const result = regenerateSection({
          workout,
          typeId:       selectedType,
          sectionKey,
          maxYards,
          equipment,
          poolMode,
          userMin:      sliderMin,
          recentLabels,
          recoveryMode: isRecovery,
          phase,
          // v1.13 — union own + coach-propagated favorites (mirror v1.7).
          // Gap-1 (2026-05-22) — set-level (favoriteSetIds/disfavorSetIds)
          // now wired in regenerateSection's main-section picker.
          favorites: Array.from(new Set([...favorites, ...effectiveFavoriteLabels])),
          favoriteSetIds: new Set([...favoriteSets, ...effectiveFavoriteSetIds]),
          engineFavorites: (() => {
            const seen = new Set();
            const out = [];
            for (const e of [...engineFavorites, ...effectiveEngineFavorites]) {
              const k = `${e.template_id}:${e.stroke}`;
              if (!seen.has(k)) { seen.add(k); out.push(e); }
            }
            return out;
          })(),
          sectionSource: effectiveSource,
          recentEngineTemplates,
          // v1.7 — union own + effective coach propagation (see handleGenerate)
          disfavorites: Array.from(new Set([...disfavorites, ...effectiveDisfavorLabels])),
          disfavorSetIds: new Set([...disfavorSets, ...effectiveDisfavorSetIds]),
          engineDisfavorites: (() => {
            const seen = new Set();
            const out = [];
            for (const e of [...engineDisfavorites, ...effectiveEngineDisfavorites]) {
              const k = `${e.template_id}:${e.stroke}`;
              if (!seen.has(k)) { seen.add(k); out.push(e); }
            }
            return out;
          })(),
          disfavorMode,                                                         // v1.8
          ugcOverlay,                                                           // UGC Phase B — overlay merged into bank picker
          myConstraints,                                                         // Phase 3 PSC slice 2 — step-0 hard-exclude (skipped in multi-lane)
          tonightOverrides,                                                       // Phase 3 PSC slice 3 — per-practice checklist (regen honors the same selections)
        });
        if (result.error) {
          setRegenError({ section: sectionKey, message: result.error });
          return;
        }
        // Scale new section's intervals to match current pace
        let regenWorkout = result.workout;
        const parts = paceInput.trim().split(":");
        if (parts.length === 2) {
          const userSecs = parseInt(parts[0], 10) * 60 + parseInt(parts[1], 10);
          if (userSecs && userSecs >= 30 && userSecs <= 300 && userSecs !== PACE_BASELINE_SECS) {
            const ratio = userSecs / PACE_BASELINE_SECS;
            const newBlocks = regenWorkout.blocks.map((b, bi) => {
              const origBlock = workout.blocks[bi];
              // Only rescale the regenerated section; leave others as-is
              if (b.section !== sectionKey) return b;
              return { ...b, sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, ratio) })) };
            });
            regenWorkout = { ...regenWorkout, blocks: newBlocks, estimatedMin: calcEstimatedMin(newBlocks) };
          }
        }
        // C: if the existing workout is a recovery-day workout, apply the +10%
        // interval stretch to the regenerated section so it matches its siblings.
        if (isRecovery) {
          const newBlocks = regenWorkout.blocks.map(b => {
            if (b.section !== sectionKey) return b;
            return { ...b, sets: b.sets.map(s => ({ ...s, interval: scaleInterval(s.interval, 1.10) })) };
          });
          regenWorkout = { ...regenWorkout, blocks: newBlocks, recovery: true, estimatedMin: calcEstimatedMin(newBlocks) };
        }
        setWorkout(regenWorkout);
        // S4 #7 — if main was regenerated, record the new label for anti-repeat.
        if (sectionKey === "main") {
          const newMainLabel = regenWorkout.blocks && regenWorkout.blocks[2] && regenWorkout.blocks[2].label;
          if (newMainLabel) setSessionRecentLabels(prev => new Set([...prev, newMainLabel]));
        }
        // v1.1 — write side for section regen: if the regenerated section
        // used the engine, prepend its template+stroke to recentEngineTemplates
        // (same shape as handleGenerate's write path).
        const regenBlock = regenWorkout.blocks.find(b => b.section === sectionKey);
        if (regenBlock && regenBlock.__engineMeta && regenBlock.__engineMeta.template_id) {
          const newEntry = {
            template_id: regenBlock.__engineMeta.template_id,
            stroke: regenBlock.__engineMeta.stroke || "free",
            ts: Date.now(),
          };
          setRecentEngineTemplates(prev => {
            const next = [newEntry, ...prev].slice(0, 10);
            if (typeof fetch === "function" && csrf.token) {
              fetch(`${API_BASE}/settings/extra`, {
                method: "POST",
                credentials: "include",
                headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf.token },
                body: JSON.stringify({ engine_recent_templates: next }),
              }).catch(() => {});
            }
            return next;
          });
        }
        setRegenError(null);
        setEditDescKey(null);
        setEditIntervalKey(null);
        // A single-section regen makes this a fresh, unsaved workout — even if
        // it was previously loaded from history.
        setLoadedFromHistoryId(null);
        setSaveStatus(null); setSaveError(null);
      }, [workout, selectedType, maxYards, equipment, poolMode, paceInput, sliderMin, pinnedSections, recentMainLabels, sessionRecentLabels, phase, favorites, favoriteSets, sectionSources, recentEngineTemplates, disfavorites, engineDisfavorites, disfavorSets, effectiveDisfavorLabels, effectiveDisfavorSetIds, effectiveEngineDisfavorites, disfavorMode, engineFavorites, effectiveFavoriteLabels, effectiveFavoriteSetIds, effectiveEngineFavorites]);

      const handleTogglePin = useCallback((sectionKey) => {
        setPinnedSections(prev => ({ ...prev, [sectionKey]: !prev[sectionKey] }));
      }, []);

      const handleTypeSelect = (id) => { setSelectedType(id); setWorkout(null); setLoadedFromHistoryId(null); setSaveStatus(null); setRegenError(null); setGenerateError(null); setPinnedSections({}); };
      const handleMaxChange  = (v)  => { setMaxYards(v); setWorkout(null); setLoadedFromHistoryId(null); setSaveStatus(null); setRegenError(null); setGenerateError(null); };
      const handlePoolModeChange = (mode) => {
        setPoolMode(mode);
        setWorkout(null);
        setLoadedFromHistoryId(null);
        setSaveStatus(null);
        setRegenError(null);
        setGenerateError(null);
        setPinnedSections({});
        setMaxYards(mode === "50m" || mode === "25m" ? 2500 : 2400);
      };
      // F: `mode` is "off" | "preferred" | "required" (legacy booleans accepted).
      const handleEquipChange = (id, mode) => {
        const next = mode === true ? "required" :
                     mode === false || mode == null ? "off" :
                     mode;
        setEquipment(prev => {
          const updated = { ...prev, [id]: next };
          // Persist to settings.extra.equipment_modes (best-effort).
          fetch(`${API_BASE}/settings/extra`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ equipment_modes: updated }),
          }).catch(() => {});
          return updated;
        });
        setWorkout(null);
        setLoadedFromHistoryId(null);
        setSaveStatus(null);
        setRegenError(null);
        setGenerateError(null);
      };

      // Save the current workout to history (POST to worker, optimistic local update)
      const handleSave = useCallback(async () => {
        if (!workout || !selectedType) return;
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Save disabled in view-as mode"); return; }
        setSaveStatus("saving"); setSaveError(null);
        // I — if we're editing a scheduled-workout row (loaded via the
        // ✎ Edit action in WeekView), PATCH the schedule row's payload
        // instead of creating a new history entry. Schedule row keeps
        // its date; only payload changes.
        if (editingScheduledId) {
          try {
            // Enrich payload with form context (matches Phase 1 schedule POST
            // — see comment there).
            const enrichedPayload = {
              ...workout,
              maxYardsCap: workout.maxYardsCap || maxYards,
              poolMode:    workout.poolMode    || poolMode,
              equipment:   workout.equipment   || { ...equipment },
              focusNote:   workout.focusNote   || focusNoteDraft || undefined,
            };
            const res = await fetch(`/api/scheduled-workouts/${editingScheduledId}`, {
              method:  "PATCH",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({ payload: enrichedPayload }),
            });
            const j = await res.json();
            if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
            setSaveStatus("scheduled");
            setEditingScheduledId(null);
            setTimeout(() => setSaveStatus(null), 2500);
            return;
          } catch (err) {
            setSaveStatus("error");
            setSaveError(`Schedule update failed: ${err.message || String(err)}`);
            return;
          }
        }
        const meta = WORKOUT_TYPES.find(t => t.id === selectedType);
        const cleanInitials = normalizeInitials(initialsDraft);
        // Remember the last-used initials for next time (best-effort).
        try {
          if (cleanInitials) localStorage.setItem(USER_INITIALS_KEY, cleanInitials);
        } catch (_) {}
        const entry = {
          id: makeEntryId(),
          savedAt: new Date().toISOString(),
          dateCompleted: dateDraft,
          type: selectedType,
          typeLabel: meta ? meta.label : selectedType,
          totalYards: workout.totalYards,
          estimatedMin: workout.estimatedMin,
          maxYardsCap: maxYards,
          poolMode: poolMode,
          equipment: { ...equipment },
          blocks: workout.blocks.map(b => b.kind === "dryland" ? ({
            // Section model B — dryland blocks persist exercises, not swim sets.
            kind: "dryland", section: "dryland", name: b.name,
            placement: b.placement || "pre",
            exercises: (b.exercises || []).map(e => ({ ...e })),
            totalYards: 0,
          }) : ({
            name: b.name, section: b.section, totalYards: b.totalYards,
            rounds: b.rounds, roundRestSecs: b.roundRestSecs,
            label: b.label,
            sets: b.sets.map(s => ({ ...s })),
          })),
          userInitials: cleanInitials,
          notes: (noteDraft || "").trim(),
          focusNote: (focusNoteDraft || "").trim() || undefined,
          difficulty: difficultyDraft || undefined,
          recovery: workout.recovery ? true : undefined,
          phase: workout.phase || undefined,
          completed: dateDraft <= new Date().toISOString().slice(0, 10),
          // R-D: assignment metadata snapshot persists in payload JSON so
          // history loads can render the pill without an extra fetch.
          assignment_target: workout.assignment_target || undefined,
          // v2.1 — multi-lane snapshot for history. Carries the lanes the
          // workout was generated for. Load-from-history restores App state.
          multi_lane: workout.multi_lane || undefined,
        };
        // R-D: include assign_to for fanout when generate-for picker is set.
        // Server validates caller has access to the group + writes assignment
        // rows in the same /api/log-workout call.
        // R-E: include lane_plan_id when one was selected — server uses the
        // plan's lanes as the authoritative member list with per-lane pace.
        if (generateForTarget?.id) {
          entry.assign_to = { group_id: generateForTarget.id };
          if (generateForPlanId) entry.assign_to.lane_plan_id = generateForPlanId;
        }
        // Optimistic: add to local cache and state immediately
        setHistory(prev => {
          const next = mergeById(prev, [entry]);
          saveLocalHistory(next);
          return next;
        });
        try {
          const res = await fetch(`${API_BASE}/log-workout`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify(entry),
          });
          // S5 S7 — 409 means the server already has this entry id (a prior
          // attempt of the same submission succeeded; this is a network retry).
          // The optimistic local state is correct as-is; treat it as success
          // rather than surfacing a confusing "duplicate id" error.
          if (res.status === 409) {
            setSaveStatus("saved");
            setNoteDraft("");
            setFocusNoteDraft("");
            setDifficultyDraft(null);
            return;
          }
          if (!res.ok) {
            const body = await res.json().catch(() => ({}));
            throw new Error(body.error || `HTTP ${res.status}`);
          }
          const saved = await res.json().catch(() => ({}));
          // Replace optimistic entry with server's version (which has sub stamped)
          if (saved.entry) {
            setHistory(prev => {
              const next = prev.map(e => e.id === entry.id ? saved.entry : e);
              saveLocalHistory(next);
              return next;
            });
          }
          setSaveStatus("saved");
          setNoteDraft("");
          setFocusNoteDraft("");
          setDifficultyDraft(null);
        } catch (err) {
          setSaveStatus("error");
          setSaveError(err.message || String(err));
        }
      }, [workout, selectedType, maxYards, equipment, dateDraft, noteDraft, initialsDraft, poolMode, focusNoteDraft, difficultyDraft, generateForTarget, generateForPlanId, editingScheduledId]);

      // Log a completed Run-mode workout as today's session.
      // Used by RunWorkoutOverlay's finish screen — POSTs a NEW entry dated today,
      // not a duplicate of any source entry's id/sub. Returns true on success.
      const handleLogAsToday = useCallback(async (sourceWorkout, opts = {}) => {
        if (!sourceWorkout || !sourceWorkout.blocks || !sourceWorkout.type) {
          return { ok: false, error: "missing workout fields" };
        }
        if (isViewingAsOtherRef.current) { setPersonaBlockMsg("Log disabled in view-as mode"); return { ok: false, error: "view-as preview" }; }
        // R-G: if this run was launched from an assignment ("Run this workout"
        // on AssignedToMeView), write completion back to the assignment row
        // instead of creating a new workout in the swimmer's history. The
        // canonical workout already exists in the coach's history; we just
        // stamp completion + splits on the assignment.
        if (runAssignmentId) {
          try {
            const res = await fetch(`/api/assignments/${runAssignmentId}`, {
              method:  "PATCH",
              headers: { "Content-Type": "application/json", ...csrfHeaders() },
              body:    JSON.stringify({
                completion_state: "complete",
                splits_payload:   (opts.actualSplits && opts.actualSplits.length) ? opts.actualSplits : null,
                difficulty:       opts.difficulty || null,
                focus_note:       (opts.notes || "").trim() || null,
              }),
            });
            const j = await res.json().catch(() => ({}));
            if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
            // Clear the assignment tag so subsequent solo runs save normally.
            setRunAssignmentId(null);
            return { ok: true, assignment: true };
          } catch (err) {
            return { ok: false, error: err.message || String(err) };
          }
        }
        const meta = WORKOUT_TYPES.find(t => t.id === sourceWorkout.type);
        const today = new Date().toISOString().slice(0, 10);
        const cleanInitials = normalizeInitials(initialsDraft) || sourceWorkout.userInitials || "";
        const entry = {
          id: makeEntryId(),
          savedAt: new Date().toISOString(),
          dateCompleted: today,
          type: sourceWorkout.type,
          typeLabel: sourceWorkout.typeLabel || (meta ? meta.label : sourceWorkout.type),
          totalYards: sourceWorkout.totalYards,
          estimatedMin: sourceWorkout.estimatedMin,
          maxYardsCap: sourceWorkout.maxYardsCap,
          poolMode: sourceWorkout.poolMode || "25y",
          equipment: { ...(sourceWorkout.equipment || {}) },
          blocks: sourceWorkout.blocks.map(b => b.kind === "dryland" ? ({
            kind: "dryland", section: "dryland", name: b.name,
            placement: b.placement || "pre",
            exercises: (b.exercises || []).map(e => ({ ...e })),
            totalYards: 0,
          }) : ({
            name: b.name, section: b.section, totalYards: b.totalYards,
            rounds: b.rounds, roundRestSecs: b.roundRestSecs,
            label: b.label,
            sets: b.sets.map(s => ({ ...s })),
          })),
          userInitials: cleanInitials,
          notes: (opts.notes || "").trim(),
          focusNote: sourceWorkout.focusNote || undefined,
          difficulty: opts.difficulty || undefined,
          recovery: sourceWorkout.recovery ? true : undefined,
          phase: sourceWorkout.phase || undefined,
          actual_splits: opts.actualSplits && opts.actualSplits.length > 0 ? opts.actualSplits : undefined,
          completed: true,
        };
        // I — if this run was launched from a scheduled-workout row, tag the
        // log call so the server stamps completed_workout_id on the schedule
        // row. Cleared after the request returns (success or failure).
        if (runScheduledId) entry.scheduled_id = runScheduledId;
        setHistory(prev => {
          const next = mergeById(prev, [entry]);
          saveLocalHistory(next);
          return next;
        });
        try {
          const res = await fetch(`${API_BASE}/log-workout`, {
            method: "POST",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify(entry),
          });
          // S5 S7 — same idempotent treatment as handleSave: 409 means the
          // entry was already accepted by a prior attempt. Optimistic add stays.
          if (res.status === 409) {
            return { ok: true };
          }
          if (!res.ok) {
            const body = await res.json().catch(() => ({}));
            throw new Error(body.error || `HTTP ${res.status}`);
          }
          const saved = await res.json().catch(() => ({}));
          if (saved.entry) {
            setHistory(prev => {
              const next = prev.map(e => e.id === entry.id ? saved.entry : e);
              saveLocalHistory(next);
              return next;
            });
          }
          // I — clear the scheduled-id tag once the log succeeds.
          if (runScheduledId) setRunScheduledId(null);
          return { ok: true };
        } catch (err) {
          // Roll back optimistic add
          setHistory(prev => {
            const next = prev.filter(e => e.id !== entry.id);
            saveLocalHistory(next);
            return next;
          });
          return { ok: false, error: err.message || String(err) };
        }
      }, [initialsDraft, runAssignmentId, runScheduledId]);

      const handleUpdateCompleted = useCallback(async (id, completed) => {
        setHistory(prev => {
          const next = prev.map(e => e.id === id ? { ...e, completed } : e);
          saveLocalHistory(next);
          return next;
        });
        try {
          await fetch(`${API_BASE}/workouts/${id}`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ completed }),
          });
        } catch (err) { console.error("Failed to update completed:", err); }
      }, []);

      const handleUpdateNotes = useCallback(async (id, notes) => {
        setHistory(prev => {
          const next = prev.map(e => e.id === id ? { ...e, notes } : e);
          saveLocalHistory(next);
          return next;
        });
        try {
          await fetch(`${API_BASE}/workouts/${id}`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ notes }),
          });
        } catch (err) { console.error("Failed to update notes:", err); }
      }, []);

      // PATCH difficulty (1–5 or null). Optimistic; reverts on failure.
      const handleUpdateDifficulty = useCallback(async (id, difficulty) => {
        const prevHistory = history;
        setHistory(prev => {
          const next = prev.map(e => e.id === id ? { ...e, difficulty } : e);
          saveLocalHistory(next);
          return next;
        });
        try {
          const res = await fetch(`${API_BASE}/workouts/${id}`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", ...csrfHeaders() },
            body: JSON.stringify({ difficulty }),
          });
          if (!res.ok) throw new Error(`PATCH failed: ${res.status}`);
        } catch (err) {
          console.error("Failed to update difficulty:", err);
          setHistory(prevHistory);
          saveLocalHistory(prevHistory);
        }
      }, [history]);

      const handleDelete = useCallback(async (id) => {
        setHistory(prev => {
          const next = prev.filter(e => e.id !== id);
          saveLocalHistory(next);
          return next;
        });
        try {
          await fetch(`${API_BASE}/workouts/${id}`, { method: "DELETE", headers: { ...csrfHeaders() } });
        } catch (err) { console.error("Failed to delete entry:", err); }
      }, []);

      // Load a past workout into the generator view and trigger Print
      const handleLoadAndPrint = useCallback((entry) => {
        setSelectedType(entry.type);
        setMaxYards(entry.maxYardsCap || 2400);
        setPoolMode(entry.poolMode || "25y");
        setEquipment(entry.equipment || { kickboard: "off", fins: "off", paddles: "off", pullBuoy: "off", snorkel: "off" });
        // Reconstruct a workout-shaped object from the saved entry. v2.1:
        // carry multi_lane through so the print view can re-render per-lane.
        setWorkout({
          blocks: entry.blocks,
          totalYards: entry.totalYards,
          estimatedMin: entry.estimatedMin,
          typeId: entry.type,
          multi_lane: entry.multi_lane || undefined,
        });
        // v2.1 — restore multi-lane App state when entry carries lanes.
        if (entry.multi_lane && Array.isArray(entry.multi_lane.lanes) && entry.multi_lane.lanes.length) {
          setMultiLaneMode(true);
          setManualLanesPace(entry.multi_lane.lanes.map((l, i) => ({
            lane_label: l.lane_label || `Lane ${i + 1}`,
            pace: l.pace || "",
          })));
        }
        setLoadedFromHistoryId(entry.id);
        setFocusNoteDraft(entry.focusNote || "");
        setSaveStatus(null);
        setRegenError(null);
        setView("generator");
        // Wait for the next paint, then open the print dialog
        setTimeout(() => window.print(), 150);
      }, []);

      // Repeat a historical workout as a fresh, unsaved one. Same blocks/type/
      // equipment as the source entry, but loadedFromHistoryId stays null so
      // the Save form appears with today's date and empty notes. Per-section
      // regenerate buttons remain enabled — user can shuffle pieces before
      // logging the repeat as its own history entry.
      const handleRepeatFromHistory = useCallback((entry) => {
        setSelectedType(entry.type);
        setMaxYards(entry.maxYardsCap || 2400);
        setPoolMode(entry.poolMode || "25y");
        setEquipment(entry.equipment || { kickboard: "off", fins: "off", paddles: "off", pullBuoy: "off", snorkel: "off" });
        setWorkout({
          blocks: entry.blocks,
          totalYards: entry.totalYards,
          estimatedMin: entry.estimatedMin,
          typeId: entry.type,
          multi_lane: entry.multi_lane || undefined,
        });
        // v2.1 — restore multi-lane state for the repeat path too.
        if (entry.multi_lane && Array.isArray(entry.multi_lane.lanes) && entry.multi_lane.lanes.length) {
          setMultiLaneMode(true);
          setManualLanesPace(entry.multi_lane.lanes.map((l, i) => ({
            lane_label: l.lane_label || `Lane ${i + 1}`,
            pace: l.pace || "",
          })));
        }
        setLoadedFromHistoryId(null);     // treat as fresh / unsaved
        setSaveStatus(null); setSaveError(null);
        setRegenError(null);
        setDateDraft(new Date().toISOString().slice(0, 10));
        setNoteDraft("");
        setView("generator");
      }, []);

      // Q: derive countdown state for the next event chip. Returns null when no
      // event is set OR when the event date is more than 1 day in the past.
      // Past day-of (i.e. yesterday) is treated as cleared from display — the
      // underlying settings.extra.next_event row stays put until the user edits it.
      const nextEventCountdown = useMemo(() => {
        if (!nextEvent || !nextEvent.date) return null;
        const today = new Date(); today.setHours(0,0,0,0);
        const target = new Date(`${nextEvent.date}T00:00:00`);
        if (isNaN(target)) return null;
        const days = Math.round((target - today) / 86400000);
        if (days < 0) return null; // past — hide display, keep data
        return { name: nextEvent.name || "Your event", days };
      }, [nextEvent]);

      // R-C / decision #38: upcoming team events (across all the user's teams
      // they're a coach on or have a group membership in). Refreshed when the
      // user navigates between views to absorb event creates/deletes promptly.
      const [upcomingTeamEvents, setUpcomingTeamEvents] = useState([]);
      useEffect(() => {
        if (!authenticated) return;
        fetch("/api/events/upcoming", { cache: "no-store" })
          .then(r => r.ok ? r.json() : [])
          .then(data => setUpcomingTeamEvents(Array.isArray(data) ? data : []))
          .catch(() => setUpcomingTeamEvents([]));
      }, [authenticated, view]);
      // Pick the most-imminent team event for the pill. Server returns events
      // in date-ascending order, so we just take the first non-past one.
      const teamEventPill = useMemo(() => {
        if (!upcomingTeamEvents.length) return null;
        const today = new Date(); today.setHours(0,0,0,0);
        for (const ev of upcomingTeamEvents) {
          const target = new Date(`${ev.date}T00:00:00`);
          if (isNaN(target)) continue;
          const days = Math.round((target - today) / 86400000);
          if (days >= 0) return { ...ev, days };
        }
        return null;
      }, [upcomingTeamEvents]);

      // P: derive the display name used for personalization throughout the app.
      // Falls back display_name → initials → null. Possessive needs the first
      // word only, so "Patrick James" → "Patrick" → "Patrick's workout."
      const personalFirstName = useMemo(() => {
        const dn = (me?.display_name || "").trim();
        if (dn) {
          const first = dn.split(/\s+/)[0];
          if (first) return first;
        }
        const ini = (me?.initials || "").trim();
        return ini || null;
      }, [me]);

      // Quick-launch — last 3 completed workouts belonging to the signed-in user.
      // Powers the pre-pool "do yesterday's again" cards above the Generate button.
      const quickLaunch = useMemo(() => {
        const mySub = me?.sub;
        if (!mySub) return [];
        const mine = history.filter(e =>
          e.sub === mySub && e.completed !== false && e.dateCompleted && e.blocks
        );
        if (!mine.length) return [];
        mine.sort((a, b) => b.dateCompleted.localeCompare(a.dateCompleted));
        const today = new Date().toISOString().slice(0, 10);
        return mine.slice(0, 3).map(e => {
          const diffDays = Math.round((new Date(today) - new Date(e.dateCompleted)) / 86400000);
          let when;
          if      (diffDays === 0) when = "Today";
          else if (diffDays === 1) when = "Yesterday";
          else if (diffDays <  7)  when = `${diffDays} days ago`;
          else if (diffDays < 14)  when = "1 week ago";
          else                     when = `${Math.floor(diffDays / 7)} weeks ago`;
          const unit = (e.poolMode === "50m" || e.poolMode === "25m") ? "m" : "yds";
          const typeMeta = WORKOUT_TYPES.find(t => t.id === e.type);
          return {
            id: e.id,
            entry: e,
            emoji: typeMeta ? typeMeta.emoji : "🏊",
            label: e.typeLabel || (typeMeta ? typeMeta.label : e.type),
            when,
            totalYards: e.totalYards || 0,
            estimatedMin: e.estimatedMin,
            unit,
          };
        });
      }, [history, me]);

      // Auth gate — render nothing while checking, show gate if no valid code
      if (authenticated === null) return null; // still checking
      if (!authenticated) return <SignInGate authError={authError} />;

      const meta = WORKOUT_TYPES.find(t => t.id === selectedType);
      const sectionColors = ["#38bdf8", "#2dd4bf", "var(--color-primary)", "var(--color-text-muted)"];
      const unit = (poolMode === "50m" || poolMode === "25m") ? "m" : "yds";
      const typeMin = selectedType ? minYardsForType(selectedType, poolMode) : 0;
      const canGenerate = selectedType && maxYards >= typeMin;

      return (
        <div style={{ minHeight: "100vh", background: "linear-gradient(135deg, var(--color-bg) 0%, #1e3a5f 50%, var(--color-bg) 100%)" }}>
          {/* Skip-link — keyboard-only users can jump straight to main content.
              Visually hidden until focused (Phase 1 item 8, 2026-05-26). */}
          <a href="#main-content"
             style={{
               position: "absolute", left: "-9999px", top: "auto",
               width: 1, height: 1, overflow: "hidden",
             }}
             onFocus={(e) => {
               Object.assign(e.target.style, {
                 position: "fixed", left: "8px", top: "8px",
                 width: "auto", height: "auto", overflow: "visible",
                 padding: "10px 16px", background: "var(--color-primary)",
                 color: "#fff", borderRadius: 6, textDecoration: "none",
                 fontSize: 14, fontWeight: 700, zIndex: 99999,
               });
             }}
             onBlur={(e) => {
               Object.assign(e.target.style, {
                 position: "absolute", left: "-9999px", top: "auto",
                 width: 1, height: 1, overflow: "hidden",
                 padding: 0, background: "transparent", color: "inherit",
                 borderRadius: 0, fontSize: "inherit", fontWeight: "inherit", zIndex: "auto",
               });
             }}
          >Skip to main content</a>
          {/* v3 impersonation banner — sticky at top (above view-as banner)
              when an active impersonation session exists. Persistent and
              dismiss-resistant on purpose: this is a customer-support session
              acting as another user, NOT a UI preview. Hard-red background
              to distinguish from the amber view-as v1 banner. Countdown
              ticks every 5s. */}
          {impersonationState && (
            <ImpersonationBanner state={impersonationState} onExit={handleEndImpersonation} />
          )}
          {/* View-as banner — sticky at top when admin has flipped into a
              role-override mode. Only renders to actual admins (real me)
              who picked something other than "self". One-click exit. */}
          {me?.is_admin && (viewAsRole !== "self" || viewAsParent) && (
            <div className="screen-only" style={{
              position: "sticky", top: 0, zIndex: 9000,
              background: "var(--color-warn)", color: "#1e293b",
              padding: "6px 16px", fontSize: 12, fontWeight: 700,
              display: "flex", justifyContent: "center", alignItems: "center", gap: 12,
              boxShadow: "0 2px 6px rgba(0,0,0,0.3)",
            }}>
              <span>👁 PREVIEW MODE — viewing as <strong style={{ textTransform: "uppercase", letterSpacing: "0.04em" }}>{viewAsRole}{viewAsParent ? " + parent" : ""}</strong> · fresh-user view · writes blocked</span>
              <button
                type="button"
                onClick={() => { setViewAsParent(false); setViewAsRole("self"); }}
                style={{
                  background: "rgba(30, 41, 59, 0.85)", color: "#fff",
                  border: "none", borderRadius: 4,
                  padding: "3px 12px", fontSize: 11, fontWeight: 700,
                  cursor: "pointer",
                }}>
                Exit (reload)
              </button>
            </div>
          )}
          {/* Parent invite accept cards — explicit consent for any pending
              invites matching this user's verified email (from bootstrap). */}
          {authenticated && pendingInvites.length > 0 && (
            <ParentInviteCards invites={pendingInvites} onResolved={refreshBootstrap} />
          )}
          {/* Billing v1 — post-Stripe-redirect banner. Auto-clears in 8s.
              Renders above the persona-block toast (higher z-index). */}
          {upgradeBanner && (
            <div className="screen-only" style={{
              position: "fixed", top: 16, left: "50%", transform: "translateX(-50%)",
              zIndex: 9060, maxWidth: 560, width: "calc(100% - 32px)",
              background: upgradeBanner.kind === "success" ? "var(--color-positive)" : "rgba(30, 41, 59, 0.95)",
              color: "#fff",
              padding: "12px 18px", borderRadius: 8,
              fontSize: 13, fontWeight: 600, lineHeight: 1.5,
              boxShadow: "0 4px 12px rgba(0,0,0,0.4)",
              border: upgradeBanner.kind === "success" ? "none" : "1px solid var(--color-primary)",
              display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12,
            }}>
              <span>{upgradeBanner.kind === "success" ? "✓ " : "ⓘ "}{upgradeBanner.text}</span>
              <button onClick={() => setUpgradeBanner(null)}
                aria-label="Dismiss"
                style={{ background: "transparent", border: "none", color: "inherit", fontSize: 18, cursor: "pointer", padding: 0, lineHeight: 1 }}>
                ×
              </button>
            </div>
          )}
          {/* v2 persona-block toast — surfaces a brief reason when a write
              is intercepted while viewing-as. Auto-clears in 2.5s. */}
          {personaBlockMsg && (
            <div className="screen-only" style={{
              position: "fixed", top: 60, left: "50%", transform: "translateX(-50%)",
              zIndex: 9050,
              background: "rgba(30, 41, 59, 0.95)", color: "#fff",
              padding: "10px 18px", borderRadius: 999,
              fontSize: 13, fontWeight: 600,
              boxShadow: "0 4px 12px rgba(0,0,0,0.4)",
              border: "1px solid var(--color-warn)",
            }}>
              ⚠ {personaBlockMsg}
            </div>
          )}
          {/* Header */}
          <header className="screen-only" role="banner" style={{ background: "var(--color-bg)", borderBottom: "1px solid #1e40af", boxShadow: "0 4px 20px rgba(0,0,0,0.4)" }}>
            <div style={{ maxWidth: 940, margin: "0 auto", padding: "20px 24px", display: "flex", alignItems: "center", gap: 12 }}>
              <div style={{ flex: 1, display: "flex", alignItems: "center", gap: 10 }}>
                {/* SetForge logo. /icons/icon-192.png is served via the
                    public/icons/ static folder. 36px target gives a crisp
                    render on standard DPI; the 192px source covers 2x. */}
                <img src="/icons/icon-192.png" alt="SetForge" width="36" height="36"
                  style={{ display: "block", borderRadius: 6, flexShrink: 0 }} />
                <div>
                  <h1 style={{ fontFamily: '"Inter Tight", Inter, sans-serif', fontSize: 28, fontWeight: 800, color: "var(--color-text)", margin: 0, letterSpacing: "-0.02em" }}>SetForge</h1>
                {/* Banner tagline (2026-05-27): when signed in, show provider
                    icon(s) + the user's full display_name. Hidden entirely when
                    signed out. Helps disambiguate Apple vs Google account when
                    a user has both registered (which today produces two
                    separate user rows per GOOGLE_OAUTH_SCOPE decision 3). */}
                {me?.display_name && (
                  <p style={{ color: "var(--color-text-muted)", fontSize: 13, margin: "2px 0 0", display: "flex", alignItems: "center", gap: 6 }}>
                    {(me.providers || []).map(p => (
                      p.provider === 'apple' ? (
                        <svg key="apple" width="13" height="13" viewBox="0 0 24 24" fill="currentColor" aria-label="Apple" style={{ flexShrink: 0 }}>
                          <path d="M17.05 20.28c-.98.95-2.05.8-3.08.35-1.09-.46-2.09-.48-3.24 0-1.44.62-2.2.44-3.06-.35C2.79 15.25 3.51 7.59 9.05 7.31c1.35.07 2.29.74 3.08.8 1.18-.24 2.31-.93 3.57-.84 1.51.12 2.65.72 3.4 1.8-3.12 1.87-2.38 5.98.48 7.13-.57 1.5-1.31 2.99-2.54 4.09zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
                        </svg>
                      ) : p.provider === 'google' ? (
                        <svg key="google" width="13" height="13" viewBox="0 0 24 24" aria-label="Google" style={{ flexShrink: 0 }}>
                          <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                          <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                          <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/>
                          <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                        </svg>
                      ) : null
                    ))}
                    <span>{me.display_name}</span>
                  </p>
                )}
                </div>
              </div>
              <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 6 }}>
                <nav aria-label="Primary" style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", justifyContent: "flex-end" }}>
                  <button onClick={() => setView(v => v === "generator" ? "history" : "generator")}
                    style={{
                      padding: "8px 14px", borderRadius: 8,
                      border: "1px solid var(--color-border)", background: "var(--color-card)",
                      color: "var(--color-text)", fontSize: 13, fontWeight: 600,
                      cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 6,
                    }}>
                    {view === "generator" ? "📜 History" : "⏎ Generator"}
                    {view === "generator" && history.length > 0 && (
                      <span style={{ background: "var(--color-primary)", color: "#fff", borderRadius: 999, padding: "1px 8px", fontSize: 10, fontWeight: 700 }}>
                        {history.length}
                      </span>
                    )}
                  </button>
                  {/* R-G: 📥 Assigned to me — visible to all auth users (any
                      account may be a swimmer in someone's group). */}
                  {authenticated && (
                    <button onClick={() => setView(v => v === "assigned" ? "generator" : "assigned")}
                      title={view === "assigned" ? "Back to generator" : "Workouts assigned to you"}
                      aria-label={view === "assigned" ? "Back to generator" : "Workouts assigned to you"}
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-primary)", background: view === "assigned" ? "var(--color-primary)" : "var(--color-card)", color: view === "assigned" ? "var(--color-bg)" : "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      {view === "assigned" ? "⏎" : "📥"}
                    </button>
                  )}
                  {/* I — 📅 Week view (planning grid). Visible to all auth users. */}
                  {authenticated && (
                    <button onClick={() => setView(v => v === "week" ? "generator" : "week")}
                      title={view === "week" ? "Back to generator" : "Plan your week"}
                      aria-label={view === "week" ? "Back to generator" : "Plan your week"}
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-primary)", background: view === "week" ? "var(--color-primary)" : "var(--color-card)", color: view === "week" ? "var(--color-bg)" : "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      {view === "week" ? "⏎" : "📅"}
                    </button>
                  )}
                  {/* 👪 Parent view — visible when this user has at least one
                      active guardian row (effectiveMe.is_parent — so the admin
                      view-as +parent flag also surfaces the button). Toggleable
                      so users who are also a swimmer/coach can still reach
                      their other views. */}
                  {effectiveMe?.is_parent && (
                    <button onClick={() => setView(v => v === "parent" ? "generator" : "parent")}
                      title={view === "parent" ? "Back to generator" : "Parent view (your swimmers)"}
                      aria-label={view === "parent" ? "Back to generator" : "Parent view"}
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-primary)", background: view === "parent" ? "var(--color-primary)" : "var(--color-card)", color: view === "parent" ? "var(--color-bg)" : "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      {view === "parent" ? "⏎" : "👪"}
                    </button>
                  )}
                  {/* Setforge rebrand 2026-05-20 — Coach dropdown collapses
                      Teams / Swimmers / Catalog (REBRAND_SCOPE §8.1). Visible
                      only to coaches. Clicking the trigger toggles a small
                      menu; clicking an item navigates + closes. */}
                  {effectiveMe?.is_coach && (
                    <div style={{ position: "relative", display: "inline-flex" }}>
                      <button onClick={() => setCoachMenuOpen(v => !v)}
                        title="Coach tools (Teams, Swimmers, Catalog)"
                        aria-label="Coach tools menu"
                        aria-haspopup="menu"
                        aria-expanded={coachMenuOpen}
                        style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-primary)",
                          background: ["teams","swimmers","practices","catalog","my-sets"].includes(view) ? "var(--color-primary)" : "var(--color-card)",
                          color: ["teams","swimmers","practices","catalog","my-sets"].includes(view) ? "var(--color-bg)" : "var(--color-primary)",
                          fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1,
                          display: "inline-flex", alignItems: "center", gap: 4 }}>
                        🔧 <span style={{ fontSize: 10 }}>▾</span>
                      </button>
                      {coachMenuOpen && (
                        <>
                          {/* Click-outside catcher */}
                          <div onClick={() => setCoachMenuOpen(false)}
                            style={{ position: "fixed", inset: 0, zIndex: 50 }} />
                          <div className="card" style={{
                            position: "absolute", top: "calc(100% + 4px)", right: 0,
                            borderRadius: 8,
                            boxShadow: "0 8px 24px rgba(0,0,0,0.4)", minWidth: 180, zIndex: 51,
                            padding: 4,
                          }}>
                            {[
                              { id: "teams",     emoji: "👥", label: "Teams" },
                              { id: "swimmers",  emoji: "🏊", label: "Managed swimmers" },
                              { id: "practices", emoji: "📋", label: "Practices" },
                              { id: "catalog",   emoji: "📚", label: "Catalog" },
                              { id: "my-sets",  emoji: "📝", label: "My Sets" },
                            ].map(item => {
                              const active = view === item.id;
                              return (
                                <button key={item.id}
                                  onClick={() => { setView(item.id); setCoachMenuOpen(false); }}
                                  style={{
                                    display: "flex", width: "100%", alignItems: "center", gap: 8,
                                    padding: "8px 12px", borderRadius: 5, border: "none",
                                    background: active ? "rgba(59,130,246,0.12)" : "transparent",
                                    color: active ? "var(--color-primary)" : "#cbd5e1",
                                    fontSize: 13, fontWeight: active ? 700 : 500, cursor: "pointer",
                                    textAlign: "left",
                                  }}>
                                  <span>{item.emoji}</span><span>{item.label}</span>
                                </button>
                              );
                            })}
                          </div>
                        </>
                      )}
                    </div>
                  )}
                  {/* Reports — top-nav (was under 🔧 coach dropdown in Phase B).
                      Promoted out 2026-05-23 with Phase C so solo users get
                      access to their own Program Recap. ReportsView itself
                      gates the per-tab visibility by role. */}
                  {authenticated && (
                    <button onClick={() => setView(v => v === "reports" ? "generator" : "reports")}
                      title={view === "reports" ? "Back to generator" : "Reports"}
                      aria-label={view === "reports" ? "Back to generator" : "Reports"}
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-border)",
                        background: view === "reports" ? "var(--color-border)" : "var(--color-card)",
                        color: view === "reports" ? "var(--color-text)" : "var(--color-text-dim)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      {view === "reports" ? "⏎" : "📊"}
                    </button>
                  )}
                  {/* Progress dashboard — top-nav, all authed users (free-tier
                      retention surface; Phase 5 #2). */}
                  {authenticated && (
                    <button onClick={() => setView(v => v === "progress" ? "generator" : "progress")}
                      title={view === "progress" ? "Back to generator" : "Progress (your training + bests)"}
                      aria-label={view === "progress" ? "Back to generator" : "Progress"}
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-border)",
                        background: view === "progress" ? "var(--color-border)" : "var(--color-card)",
                        color: view === "progress" ? "var(--color-text)" : "var(--color-text-dim)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      {view === "progress" ? "⏎" : "📈"}
                    </button>
                  )}
                  {effectiveMe?.is_admin && (() => {
                    const pendingFb = view === "admin" ? 0 : (me?.pending_feedback_count || 0);
                    return (
                      <button onClick={() => setView(v => v === "admin" ? "generator" : "admin")}
                        title={view === "admin"
                          ? "Back to generator"
                          : pendingFb > 0
                            ? `Admin — ${pendingFb} new feedback item${pendingFb === 1 ? "" : "s"}`
                            : "Admin"}
                        aria-label={view === "admin"
                          ? "Back to generator"
                          : pendingFb > 0
                            ? `Admin console — ${pendingFb} new feedback item${pendingFb === 1 ? "" : "s"}`
                            : "Admin console"}
                        style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-primary)", background: view === "admin" ? "var(--color-primary)" : "var(--color-card)", color: view === "admin" ? "var(--color-bg)" : "var(--color-primary)", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1, position: "relative" }}>
                        {view === "admin" ? "⏎" : "🛡"}
                        {/* Feedback-count badge — only when not on admin view AND
                            count > 0. Hidden when viewing admin to avoid stale
                            "look here" signal while you're already there. */}
                        {pendingFb > 0 && (
                          <span style={{
                            position: "absolute",
                            top: -6, right: -6,
                            minWidth: 16, height: 16,
                            padding: "0 4px",
                            borderRadius: 8,
                            background: "var(--color-warn)",
                            color: "var(--color-bg)",
                            fontSize: 10, fontWeight: 800,
                            display: "inline-flex", alignItems: "center", justifyContent: "center",
                            border: "1px solid var(--color-bg)",
                            lineHeight: 1,
                          }}>
                            {pendingFb > 99 ? "99+" : pendingFb}
                          </span>
                        )}
                      </button>
                    );
                  })()}
                  {(me?.is_admin || me?.support_role) && !impersonationState && (
                    <button onClick={() => setShowImpersonationModal(true)}
                      title="Start impersonation (read-only customer-support session)"
                      style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid #dc2626", background: "transparent", color: "#fca5a5", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                      🛂
                    </button>
                  )}
                  <button onClick={() => setShowProfile(true)} title="Account (also: Send feedback, Sign out)"
                    data-tour="step-swimmer-level"
                    style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text-dim)", fontSize: 13, fontWeight: 700, cursor: "pointer", lineHeight: 1 }}>
                    👤
                  </button>
                  <a href="/manual.html" target="_blank" rel="noopener" title="User manual"
                    style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--color-border)", background: "var(--color-card)", color: "var(--color-text-dim)", fontSize: 13, fontWeight: 700, cursor: "pointer", textDecoration: "none", lineHeight: 1 }}>
                    ?
                  </a>
                </nav>
                <span style={{ fontSize: 10, color: "var(--color-border)", fontFamily: "monospace" }}>{BUILD_ID}</span>
              </div>
            </div>
          </header>

          <main id="main-content" style={{ maxWidth: 940, margin: "0 auto", padding: "32px 24px" }}>

            {view === "admin"    && <AdminView me={me} onStartImpersonation={handleStartImpersonation} />}
            {view === "parent"   && <ParentDashboard me={me} />}
            {view === "teams"    && <TeamsView />}
            {view === "swimmers" && <ManagedSwimmersView mySub={me?.sub} />}
            {view === "practices" && <PracticesView />}
            {view === "week"     && (
              <WeekView
                myConstraints={myConstraints}
                onRunScheduled={(sw) => {
                  // Same pattern as AssignedToMeView's onRunAssignment: load
                  // the workout into Run mode and tag the schedule id so the
                  // finish-flow writes the completion back via /api/log-workout
                  // with scheduled_id set, which stamps completed_workout_id.
                  //
                  // I Phase 2b — group intents reach Run mode already linked
                  // (the Run handler created the snapshot + ran fanout + linked
                  // the schedule row in one log-workout call). Skip
                  // runScheduledId so the coach's actual-swim completion
                  // doesn't attempt a second link / duplicate workout entry.
                  setRunWorkout(sw.payload);
                  if (!sw._alreadyLinked) setRunScheduledId(sw.id);
                  setShowRestPicker(true);
                  setView("generator");
                }}
                onEditScheduled={(sw) => {
                  // Load the scheduled workout back into the generator workout
                  // slot so the user can regenerate / tweak / re-save. Tag
                  // so handleSave can offer "Update schedule" instead of "Save as new."
                  //
                  // CRITICAL: must restore selectedType from the payload. The
                  // workout-display block reads
                  //   meta = WORKOUT_TYPES.find(t => t.id === selectedType)
                  // and crashes on meta.badge if selectedType is null (bug
                  // surfaced 2026-05-20 when Edit was clicked on a fresh page
                  // load without first selecting a type → "undefined is not
                  // an object 'meta.badge'").
                  //
                  // Restore full form context from the enriched payload. The
                  // schedule POST/PATCH sites enrich `payload` with
                  // maxYardsCap/poolMode/equipment/focusNote alongside the
                  // raw workout fields. Pre-enrichment rows (created before
                  // 2026-05-20 hotfix) just have the raw workout — those
                  // conditionals fall through and leave current form state
                  // untouched.
                  const p = sw.payload || {};
                  if (p.typeId)      setSelectedType(p.typeId);
                  if (p.maxYardsCap) setMaxYards(p.maxYardsCap);
                  if (p.poolMode)    setPoolMode(p.poolMode);
                  if (p.equipment)   setEquipment(p.equipment);
                  setFocusNoteDraft(p.focusNote || "");
                  setWorkout(sw.payload);
                  setEditingScheduledId(sw.id);
                  setLoadedFromHistoryId(null);
                  setRunScheduledId(null);
                  setView("generator");
                }}
              />
            )}
            {view === "assigned" && (
              <AssignedToMeView
                onRunAssignment={(a) => {
                  // Load the coach-snapshot workout into Run mode and tag the
                  // assignment so the finish-flow writes back to it instead
                  // of creating a new history entry.
                  setRunWorkout(a.workout);
                  setRunAssignmentId(a.id);
                  setShowRestPicker(true);
                  setView("generator");
                }}
              />
            )}
            {view === "catalog"  && (
              <CatalogView
                favorites={favorites}
                onToggleFavorite={authenticated ? handleToggleFavorite : null}
                favoriteSets={favoriteSets}
                disfavorSets={disfavorSets}
                onCycleSetStatus={authenticated ? handleCycleSetStatus : null}
                ugcOverlay={ugcOverlay}
              />
            )}
            {view === "my-sets"  && (
              <MySetsView
                onChanged={refreshUgcOverlay}
              />
            )}
            {view === "reports"  && (
              <ReportsView isCoach={!!effectiveMe?.is_coach} isAdmin={!!effectiveMe?.is_admin} />
            )}
            {view === "progress" && (
              <ProgressDashboard onPaceUpdate={handlePaceChange} />
            )}

            {view === "history" && (
              <HistoryView
                history={history}
                historyLoaded={historyLoaded}
                onUpdateNotes={handleUpdateNotes}
                onUpdateCompleted={handleUpdateCompleted}
                onUpdateDifficulty={handleUpdateDifficulty}
                recentMainLabels={recentMainLabels}
                goals={goals}
                mySub={me?.sub}
                onDelete={handleDelete}
                onLoadAndPrint={handleLoadAndPrint}
                onRepeat={handleRepeatFromHistory}
                onRun={entry => { setRunWorkout(entry); setShowRestPicker(true); }}
                favorites={favorites}
                onToggleFavorite={handleToggleFavorite}
              />
            )}

            {view === "generator" && <>

            {/* Pool mode toggle + next-event countdown (Q, right-justified) */}
            {/* Setforge rebrand 2026-05-20 — pool-row-wrap class lets the mobile
                breakpoint force pool buttons + label onto row 1 alone, with
                modifiers (Recovery, Phase, Mix pills) wrapping to row 2 cleanly
                (REBRAND_SCOPE §8.3). */}
            <div className="screen-only pool-mode-row" style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 20, flexWrap: "wrap" }}>
              <span style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-dim)", textTransform: "uppercase", letterSpacing: "0.1em" }}>Pool</span>
              <div className="pool-mode-buttons" data-tour="step-pool-mode" style={{ display: "flex", background: "var(--color-bg)", borderRadius: 8, border: "1px solid var(--color-border)", overflow: "hidden" }}>
                {[["25y","SCY"],["25m","SCM"],["50m","LCM"]].map(([mode, label]) => (
                  <button key={mode} onClick={() => handlePoolModeChange(mode)}
                    style={{
                      padding: "6px 16px", border: "none", cursor: "pointer", fontSize: 13, fontWeight: 700,
                      background: poolMode === mode ? "var(--color-primary)" : "transparent",
                      color: poolMode === mode ? "#fff" : "var(--color-text-dim)",
                      transition: "all 0.15s",
                    }}>
                    {label}
                  </button>
                ))}
              </div>
              {/* Team event pill (decision #38) — most-imminent team event
                  across all teams the user is in. Renders to the left of the
                  personal next_event pill. marginLeft: auto on the FIRST one
                  pushes the pair right; subsequent siblings flow naturally. */}
              {teamEventPill && (
                <div title={`${teamEventPill.team_name}: ${teamEventPill.name} — ${teamEventPill.date}`}
                  className="pill"
                  style={{
                    marginLeft: nextEventCountdown ? 0 : "auto",
                    display: "inline-flex", alignItems: "center", gap: 6,
                    background: teamEventPill.days === 0 ? "#f59e0b22" : "#1e3a5f",
                    border: `1px solid ${teamEventPill.days === 0 ? "var(--color-warn)" : "var(--color-warn)"}`,
                    color: teamEventPill.days === 0 ? "#fde68a" : "var(--color-warn)",
                    fontSize: 12,
                  }}>
                  📅
                  <span style={{ whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", maxWidth: 200 }}>
                    {teamEventPill.team_name}: {teamEventPill.name}
                  </span>
                  <span style={{ color: "var(--color-text-dim)", fontWeight: 400 }}>·</span>
                  <span style={{ fontVariantNumeric: "tabular-nums" }}>
                    {teamEventPill.days === 0 ? "today!" : teamEventPill.days === 1 ? "1 day" : `${teamEventPill.days} days`}
                  </span>
                </div>
              )}
              {nextEventCountdown && (
                <div title={`${nextEventCountdown.name} — ${nextEvent.date}`}
                  className="pill"
                  style={{
                    marginLeft: teamEventPill ? 6 : "auto",
                    display: "inline-flex", alignItems: "center", gap: 6,
                    background: nextEventCountdown.days === 0 ? "#f59e0b22" : "#1e3a5f",
                    border: `1px solid ${nextEventCountdown.days === 0 ? "var(--color-warn)" : "var(--color-primary)"}`,
                    color: nextEventCountdown.days === 0 ? "#fde68a" : "var(--color-primary)",
                    fontSize: 12,
                  }}>
                  🏁
                  <span style={{ whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", maxWidth: 200 }}>
                    {nextEventCountdown.name}
                  </span>
                  <span style={{ color: "var(--color-text-dim)", fontWeight: 400 }}>·</span>
                  <span style={{ fontVariantNumeric: "tabular-nums" }}>
                    {nextEventCountdown.days === 0
                      ? "today!"
                      : nextEventCountdown.days === 1
                        ? "1 day"
                        : `${nextEventCountdown.days} days`}
                  </span>
                </div>
              )}
            </div>

            {/* C: Recovery mode toggle (Easy day / Recovery day ON) + section-
                proportion bias (Mix:) — merged onto one flex row 2026-05-27 per
                Cap'n's "save vertical space if room permits" request. Both
                groups flex-wrap as a unit so narrow viewports split them onto
                two lines cleanly. Outer gap 14 = old marginBottom between the
                two stacks. */}
            <div className="screen-only" style={{ display: "flex", alignItems: "center", gap: 14, marginBottom: 14, flexWrap: "wrap" }}>
              {/* Easy day / Recovery day ON toggle + helper text */}
              <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
                <button onClick={() => setRecoveryMode(v => !v)}
                  title={recoveryMode
                    ? "Recovery day ON — next generate keeps things easy/aerobic"
                    : "Toggle recovery day — easy/aerobic main only, intervals +10%, no repeat sets"}
                  style={{
                    display: "inline-flex", alignItems: "center", gap: 8,
                    padding: "6px 14px", borderRadius: 999,
                    border: `1px solid ${recoveryMode ? "var(--color-positive)" : "var(--color-border)"}`,
                    background: recoveryMode ? "rgba(34,197,94,0.15)" : "transparent",
                    color: recoveryMode ? "#86efac" : "var(--color-text-dim)",
                    fontSize: 12, fontWeight: 700, cursor: "pointer",
                    transition: "all 0.15s",
                  }}>
                  <span style={{
                    width: 8, height: 8, borderRadius: "50%",
                    background: recoveryMode ? "var(--color-positive)" : "var(--color-border-strong)",
                  }} />
                  {recoveryMode ? "Recovery day ON" : "Easy day"}
                </button>
                {recoveryMode && (
                  <span style={{ fontSize: 11, color: "var(--color-border-strong)", fontStyle: "italic" }}>
                    Easy/aerobic mains · intervals +10% · no repeat sets · floor 1,200 {(poolMode === "50m" || poolMode === "25m") ? "m" : "yds"}
                  </span>
                )}
              </div>

              {/* Section-proportion bias (real-shift). Per-generation, session-
                  only. Shifts yardage allocations between warmup / drill /
                  main / cooldown beyond the baseline ~13/17/65/5 split. */}
              <div style={{ display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap" }}>
                <span style={{ fontSize: 11, color: "var(--color-border-strong)", marginRight: 4 }}>Mix:</span>
                {[
                  { id: "balanced",     label: "Balanced",     desc: "Current allocator (warmup ~13% / drill ~17% / main ~65% / cooldown ~5%)" },
                  { id: "warmup_heavy", label: "Warmup-heavy", desc: "Bigger warmup (~22%); main reduced (~62%)" },
                  { id: "drill_heavy",  label: "Drill-heavy",  desc: "Bigger drill section (~26%); main reduced (~61%)" },
                  { id: "long_main",    label: "Long main",    desc: "Most yardage on the main set (~83%); warmup + drill suppressed" },
                ].map(m => {
                  const active = sectionBias === m.id;
                  return (
                    <button key={m.id} onClick={() => setSectionBias(m.id)} title={m.desc}
                      className="pill"
                      style={{
                        border: `1px solid ${active ? "var(--color-warn)" : "var(--color-border)"}`,
                        background: active ? "rgba(245,158,11,0.15)" : "transparent",
                        color: active ? "var(--color-warn)" : "var(--color-text-dim)",
                        fontSize: 11, cursor: "pointer",
                        transition: "all 0.15s",
                      }}>
                      {m.label}
                    </button>
                  );
                })}
              </div>

              {/* Section model A2 — include/skip per workout. Main is always on.
                  Skipping drops the section (a shorter, honest total). */}
              <div style={{ display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap" }}>
                <span style={{ fontSize: 11, color: "var(--color-border-strong)", marginRight: 4 }}>Sections:</span>
                {[
                  { key: "warmup",   label: "Warmup",   skip: skipWarmup,   toggle: () => setSkipWarmup(v => !v) },
                  { key: "drill",    label: "Drill",    skip: skipDrill,    toggle: () => setSkipDrill(v => !v) },
                  { key: "kick",     label: "Kick",     skip: !addKick,     toggle: () => setAddKick(v => !v) },
                  { key: "main",     label: "Main",     locked: true },
                  { key: "cooldown", label: "Cooldown", skip: skipCooldown, toggle: () => setSkipCooldown(v => !v) },
                ].map(s => {
                  const included = s.locked || !s.skip;
                  return (
                    <button key={s.key} onClick={s.locked ? undefined : s.toggle}
                      title={s.locked ? "Main set is always included" : (included ? `Skip ${s.label.toLowerCase()} on next generate` : `Include ${s.label.toLowerCase()}`)}
                      className="pill"
                      style={{
                        border: `1px solid ${included ? "var(--color-primary)" : "var(--color-border)"}`,
                        background: included ? "rgba(59,130,246,0.12)" : "transparent",
                        color: included ? "var(--color-primary)" : "var(--color-text-dim)",
                        textDecoration: included ? "none" : "line-through",
                        fontSize: 11, cursor: s.locked ? "default" : "pointer", opacity: s.locked ? 0.85 : 1,
                      }}>
                      {included ? "✓ " : ""}{s.label}
                    </button>
                  );
                })}
              </div>
            </div>

            {/* Workout type selector */}
            <p className="screen-only" style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.15em", marginBottom: 12, marginTop: 0 }}>
              Select Workout Type
            </p>
            <div className="screen-only type-card-grid" data-tour="step-type-cards" style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 12, marginBottom: 20 }}>
              {WORKOUT_TYPES.map(t => {
                const isSel = selectedType === t.id;
                const isHov = hover === t.id;
                return (
                  <button key={t.id}
                    onClick={() => handleTypeSelect(t.id)}
                    onMouseEnter={() => setHover(t.id)}
                    onMouseLeave={() => setHover(null)}
                    style={{
                      borderRadius: 12, border: `2px solid ${isSel ? t.selBg : t.border}`,
                      padding: 16, textAlign: "left", cursor: "pointer",
                      background: isSel ? t.selBg : "var(--color-card)",
                      color: isSel ? "#fff" : "var(--color-text)",
                      transform: isSel ? "scale(1.02)" : isHov ? "scale(1.01)" : "scale(1)",
                      transition: "all 0.15s",
                      boxShadow: isSel ? "0 8px 24px rgba(0,0,0,0.35)" : "none",
                      outline: "none",
                    }}>
                    <div style={{ fontSize: 24, marginBottom: 4 }}>{t.emoji}</div>
                    <div style={{ fontWeight: 700, fontSize: 13 }}>{t.label}</div>
                    <div style={{ fontSize: 11, marginTop: 2, opacity: 0.75 }}>{t.description}</div>
                  </button>
                );
              })}
            </div>

            <div className="screen-only">

              {/* Equipment checkboxes */}
              <div data-tour="step-equipment"><EquipmentPicker equipment={equipment} onChange={handleEquipChange} /></div>

              {/* Max Yardage / Distance Slider */}
              <div data-tour="step-yardage-slider"><YardageSlider value={maxYards} onChange={handleMaxChange} selectedType={selectedType} poolMode={poolMode} paceInput={paceInput} onPaceChange={handlePaceChange} sliderMin={sliderMin} sliderMax={sliderMax} onRangeChange={handleRangeChange} /></div>

              {/* v2.0 — Multi-lane multi-pace generate. Coach-only. When ON, the
                  picker filters options that fit ALL lane paces and the generate
                  flow auto-opens MultiPacePrintView. Prefills from the selected
                  lane plan when one is in play; otherwise the coach edits rows. */}
              {effectiveMe?.is_coach && (
                <MultiLaneControl
                  multiLaneMode={multiLaneMode}
                  setMultiLaneMode={setMultiLaneMode}
                  manualLanesPace={manualLanesPace}
                  setManualLanesPace={setManualLanesPace}
                  lanePlansForTarget={lanePlansForTarget}
                  generateForPlanId={generateForPlanId}
                />
              )}

              {/* Quick-launch — last 3 of your own completed workouts (pre-pool "do yesterday's again") */}
              {!workout && quickLaunch.length > 0 && (
                <div style={{ marginBottom: 12 }}>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 6 }}>
                    Pick up where you left off
                  </div>
                  <div style={{
                    display: "grid",
                    gridTemplateColumns: "repeat(auto-fit, minmax(240px, 1fr))",
                    gap: 6,
                  }}>
                    {quickLaunch.map(ql => (
                      <div key={ql.id}
                        onClick={() => { setRunWorkout(ql.entry); setShowRestPicker(true); }}
                        className="card"
                        title="Run this workout"
                        style={{
                          display: "flex", alignItems: "center", gap: 8,
                          borderRadius: 8,
                          padding: "6px 8px 6px 10px", cursor: "pointer", minHeight: 44,
                          transition: "border-color 0.15s, background 0.15s",
                          WebkitTapHighlightColor: "transparent",
                        }}
                        onMouseEnter={e => { e.currentTarget.style.borderColor = "var(--color-primary)"; }}
                        onMouseLeave={e => { e.currentTarget.style.borderColor = "var(--color-border)"; }}>
                        <div style={{ fontSize: 18, lineHeight: 1, flexShrink: 0 }}>{ql.emoji}</div>
                        <div style={{ flex: 1, minWidth: 0 }}>
                          <div style={{ fontSize: 13, color: "#fff", fontWeight: 700, lineHeight: 1.25, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                            {ql.label}
                          </div>
                          <div style={{ fontSize: 11, color: "var(--color-text-dim)", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
                            {ql.when} · {ql.totalYards.toLocaleString()} {ql.unit}
                            {ql.estimatedMin ? ` · ~${ql.estimatedMin} min` : ""}
                          </div>
                        </div>
                        {/* Actions, inline + right-aligned */}
                        <button
                          onClick={(e) => { e.stopPropagation(); handleRepeatFromHistory(ql.entry); }}
                          title="Load into generator (don't auto-start)"
                          style={{
                            padding: "8px 9px", borderRadius: 6,
                            border: "1px solid var(--color-border)", background: "transparent",
                            color: "#86efac", fontSize: 13, cursor: "pointer", flexShrink: 0, lineHeight: 1,
                          }}>
                          🔁
                        </button>
                        <div style={{
                          flexShrink: 0, padding: "8px 10px", borderRadius: 6,
                          background: "#1e3a5f", color: "var(--color-primary)",
                          fontSize: 12, fontWeight: 700,
                          display: "inline-flex", alignItems: "center", gap: 4, lineHeight: 1,
                        }}>
                          ▶ Run
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              )}

              {/* W2 — Parse Intent button (smaller, sits above Generate). Opens
                  the IntentParserModal so coaches can fill the form from a
                  natural-language one-liner. Always visible — coaches and
                  swimmers alike can use it; vocabulary is documented in manual. */}
              <button
                onClick={() => setShowIntentParser(true)}
                style={{
                  width: "100%", padding: "9px 0", borderRadius: 10,
                  border: "1px solid var(--color-border)", background: "transparent",
                  color: "var(--color-text-muted)", fontSize: 13, fontWeight: 600,
                  cursor: "pointer", marginBottom: 10, outline: "none",
                }}>
                ✏️ Quick input…
              </button>

              {/* R-D: generate-for picker (coach-only). Lists solo private
                  students separately from multi-member groups per decision
                  #35. State sticks across generations. Phase from the
                  selected group overrides personal phase (decision #39). */}
              {effectiveMe?.is_coach && coachTargets.length > 0 && (() => {
                const solos  = coachTargets.filter(t => t.member_count === 1);
                const groups = coachTargets.filter(t => t.member_count >= 2);
                return (
                  <div style={{ marginBottom: 10, padding: "10px 12px", background: "var(--color-bg)", border: "1px solid var(--color-warn)", borderRadius: 8 }}>
                    <label style={{ display: "block", fontSize: 11, color: "var(--color-warn)", marginBottom: 4, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                      Generate for
                    </label>
                    <select value={generateForId} onChange={e => setGenerateForId(e.target.value)}
                      style={{ width: "100%", padding: "6px 10px", fontSize: 13, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                      <option value="myself">Myself (solo training, no assignment)</option>
                      {solos.length > 0 && (
                        <optgroup label="Private students">
                          {solos.map(t => (
                            <option key={t.id} value={t.id}>{t.name}{t.team_name ? ` · ${t.team_name}` : ""}</option>
                          ))}
                        </optgroup>
                      )}
                      {groups.length > 0 && (
                        <optgroup label="Groups">
                          {groups.map(t => (
                            <option key={t.id} value={t.id}>{t.name} ({t.member_count}{t.team_name ? ` · ${t.team_name}` : ""}{t.current_phase ? ` · ${t.current_phase}` : ""})</option>
                          ))}
                        </optgroup>
                      )}
                    </select>
                    {/* R-E: lane plan picker. Only shown when target is a
                        multi-member group AND it has plans. Defaults to the
                        group's default plan if any. */}
                    {generateForTarget && generateForTarget.member_count >= 2 && lanePlansForTarget.length > 0 && (
                      <div style={{ marginTop: 6 }}>
                        <label style={{ display: "block", fontSize: 10, color: "var(--color-text-muted)", marginBottom: 3, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.05em" }}>
                          Lane plan
                        </label>
                        <select value={generateForPlanId} onChange={e => setGenerateForPlanId(e.target.value)}
                          style={{ width: "100%", padding: "5px 9px", fontSize: 12, background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 5 }}>
                          <option value="">— No plan (each swimmer uses stored pace) —</option>
                          {lanePlansForTarget.map(p => (
                            <option key={p.id} value={p.id}>
                              {p.name}{p.is_default ? " (default)" : ""} · {(p.plan_data?.lanes || []).length} lanes · {(p.plan_data?.lanes || []).reduce((n, l) => n + (l.members?.length || 0), 0)} swimmers
                            </option>
                          ))}
                        </select>
                      </div>
                    )}
                    {generateForTarget && (
                      <div style={{ fontSize: 11, color: "var(--color-text-muted)", marginTop: 6 }}>
                        {generateForPlanId
                          ? (() => {
                              const p = lanePlansForTarget.find(pl => pl.id === generateForPlanId);
                              const swimmerCount = (p?.plan_data?.lanes || []).reduce((n, l) => n + (l.members?.length || 0), 0);
                              return <>Workout saves to <strong>your history</strong> and fans out as <strong>{swimmerCount} per-lane assignment{swimmerCount === 1 ? "" : "s"}</strong> using plan <strong>{p?.name}</strong>.</>;
                            })()
                          : <>Workout will save to <strong>your history</strong> and fan out as <strong>{generateForTarget.member_count} assignment{generateForTarget.member_count === 1 ? "" : "s"}</strong> to {generateForTarget.name}'s members.</>
                        }
                        {generateForTarget.current_phase && <span> Phase override: <strong>{generateForTarget.current_phase}</strong>.</span>}
                      </div>
                    )}
                  </div>
                );
              })()}

              {/* Phase 3 PSC slice 3 — per-practice constraint checklist.
                  Shown only when generating for a group whose roster has
                  active constraints. Each checked row gets unioned into
                  tonightOverrides for THIS Generate only (in-memory). */}
              {generateForTarget && tonightChecklistRows.length > 0 && (
                <div style={{ marginBottom: 12, padding: 12, background: "var(--color-card)", border: "1px solid var(--color-border)", borderRadius: 8 }}>
                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}>
                    <div style={{ fontSize: 12, color: "var(--color-text)", fontWeight: 700 }}>
                      Tonight's constraints
                      <span style={{ color: "var(--color-text-muted)", fontWeight: 400, marginLeft: 6 }}>
                        ({tonightChecklistRows.length} active across roster)
                      </span>
                    </div>
                    {tonightSelected.size > 0 && (
                      <button onClick={() => setTonightSelected(new Set())}
                        style={{ padding: "3px 8px", background: "transparent", color: "var(--color-text-muted)", border: "1px solid var(--color-border)", borderRadius: 4, fontSize: 10, cursor: "pointer" }}>
                        Clear ({tonightSelected.size})
                      </button>
                    )}
                  </div>
                  <div style={{ fontSize: 10, color: "var(--color-text-muted)", marginBottom: 8, lineHeight: 1.4 }}>
                    Check any constraint to apply to <em>tonight's</em> generated workout only. Persistent constraints already apply automatically — these are extras for one practice.
                  </div>
                  <div style={{ maxHeight: 180, overflowY: "auto", paddingRight: 4 }}>
                    {tonightChecklistRows.map(({ swimmerKey, c }) => {
                      const checked = tonightSelected.has(c.id);
                      const swimmerTag = swimmerKey.startsWith("ms_")
                        ? swimmerKey
                        : swimmerKey.slice(0, 12) + "…";
                      return (
                        <label key={c.id} style={{ display: "flex", alignItems: "center", gap: 8, padding: "5px 0", borderBottom: "1px solid var(--color-bg)", fontSize: 12, cursor: "pointer", color: "var(--color-text)" }}>
                          <input type="checkbox" checked={checked}
                            onChange={() => {
                              setTonightSelected(prev => {
                                const next = new Set(prev);
                                if (checked) next.delete(c.id); else next.add(c.id);
                                return next;
                              });
                            }}
                            style={{ accentColor: "var(--color-primary)" }} />
                          <code style={{ fontFamily: "monospace", fontSize: 10, color: "var(--color-text-muted)", minWidth: 80 }}>{swimmerTag}</code>
                          <span style={{ flex: 1 }}>{formatPscRow(c)}</span>
                        </label>
                      );
                    })}
                  </div>
                  {tonightSelected.size > 0 && (
                    <div style={{ fontSize: 11, color: "var(--color-primary)", marginTop: 8, fontWeight: 700 }}>
                      → {tonightSelected.size} constraint{tonightSelected.size === 1 ? "" : "s"} will apply to this Generate.
                    </div>
                  )}
                </div>
              )}

              {/* Generate Button */}
              <button
                onClick={handleGenerate}
                disabled={!canGenerate}
                data-tour="step-generate-button"
                style={{
                  width: "100%", padding: "14px 0", borderRadius: 12, border: "none",
                  fontWeight: 700, fontSize: 15, cursor: canGenerate ? "pointer" : "not-allowed",
                  background: canGenerate ? meta.selBg : "var(--color-border)",
                  color: canGenerate ? "#fff" : "var(--color-text-dim)",
                  boxShadow: canGenerate ? "0 8px 24px rgba(0,0,0,0.3)" : "none",
                  transition: "all 0.15s", marginBottom: 32, outline: "none",
                }}>
                {workout ? "🔄 Generate New Workout" : "🏊 Generate Workout"}
              </button>

              {/* I Phase 2a — save current form state as a deferred intent.
                  Visible whenever the form is ready to generate AND the user
                  is authenticated. Useful when planning ahead without
                  committing to a specific shuffle. */}
              {canGenerate && authenticated && (
                <div style={{ marginTop: -22, marginBottom: 28, textAlign: "center" }}>
                  <button onClick={() => {
                      const t = new Date(); t.setDate(t.getDate() + 1);
                      const y = t.getFullYear(), m = String(t.getMonth() + 1).padStart(2, "0"), d = String(t.getDate()).padStart(2, "0");
                      setIntentDate(`${y}-${m}-${d}`);
                    }}
                    title="Save these settings as an intent for a future day — generate the actual workout on the day of"
                    style={{
                      background: "transparent", border: "none", color: "var(--color-warn)",
                      fontSize: 12, fontWeight: 600, cursor: "pointer", padding: 4,
                      textDecoration: "underline", textDecorationStyle: "dotted",
                    }}>
                    💾 Save as intent for later…
                  </button>
                </div>
              )}

              {/* S3 #3 — generate-level error (e.g. required equipment unsatisfiable) */}
              {generateError && (
                <div role="alert" style={{
                  background: "#fef2f2", border: "1px solid #fecaca",
                  color: "#991b1b", borderRadius: 10, padding: "12px 16px",
                  marginTop: -16, marginBottom: 24,
                  display: "flex", alignItems: "flex-start", gap: 10, fontSize: 13, lineHeight: 1.4,
                }}>
                  <span style={{ fontSize: 16, lineHeight: 1 }}>⚠️</span>
                  <span>{generateError}</span>
                </div>
              )}
            </div>

            {/* Workout display */}
            {workout && (
              <div>
                {/* Summary bar (screen) — green-bordered when recovery day */}
                <div className="screen-only" data-tour="step-celebrate" style={{
                  background: "var(--color-card)",
                  border: `${workout.recovery ? 2 : 1}px solid ${workout.recovery ? "var(--color-positive)" : "var(--color-border)"}`,
                  borderRadius: 12, padding: 20, marginBottom: 16,
                  display: "flex", flexWrap: "wrap", gap: 24, alignItems: "center",
                }}>
                  <span style={{ background: meta.badge, color: meta.badgeText, borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 700 }}>
                    {meta.emoji} {meta.label}
                  </span>
                  {workout.recovery && (
                    <span title="Generated as a recovery day — easy/aerobic main, intervals stretched +10%, no repeat sets"
                      style={{
                        background: "rgba(34,197,94,0.18)",
                        border: "1px solid #22c55e",
                        color: "#86efac",
                        borderRadius: 999, padding: "4px 12px",
                        fontSize: 12, fontWeight: 700,
                        display: "inline-flex", alignItems: "center", gap: 6,
                      }}>
                      🌿 Recovery day
                    </span>
                  )}
                  {workout.phase && PHASES[workout.phase] && (() => {
                    const p = PHASES[workout.phase];
                    return (
                      <span title={`Phase: ${p.label} — ${p.description}. Change in Account → Goals.`}
                        style={{
                          background: `${p.color}1f`,
                          border: `1px solid ${p.color}`,
                          color: p.color,
                          borderRadius: 999, padding: "4px 12px",
                          fontSize: 12, fontWeight: 700,
                          display: "inline-flex", alignItems: "center", gap: 6,
                        }}>
                        {p.emoji} {p.label}
                      </span>
                    );
                  })()}
                  {workout.assignment_target && (
                    <span title={`Generated for ${workout.assignment_target.group_name} — ${workout.assignment_target.member_count} swimmer${workout.assignment_target.member_count === 1 ? "" : "s"}`}
                      style={{
                        background: "#f59e0b1f",
                        border: "1px solid var(--color-warn)",
                        color: "var(--color-warn)",
                        borderRadius: 999, padding: "4px 12px",
                        fontSize: 12, fontWeight: 700,
                        display: "inline-flex", alignItems: "center", gap: 6,
                      }}>
                      → {workout.assignment_target.group_name} ({workout.assignment_target.member_count})
                    </span>
                  )}
                  {[
                    [(poolMode === "50m" || poolMode === "25m") ? "Total Distance" : "Total Yardage", `${workout.totalYards.toLocaleString()} ${unit}`],
                    ["Est. Duration",   `~${workout.estimatedMin} min`],
                    ["Target Range",   `${typeMin.toLocaleString()} – ${maxYards.toLocaleString()} ${unit}`],
                  ].map(([label, val]) => (
                    <div key={label}>
                      <div style={{ fontSize: 10, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>{label}</div>
                      <div style={{ fontSize: 20, fontWeight: 900, color: "#fff" }}>
                        {val.split(" ")[0]}
                        <span style={{ fontSize: 12, fontWeight: 400, color: "var(--color-text-muted)" }}> {val.split(" ").slice(1).join(" ")}</span>
                      </div>
                    </div>
                  ))}
                  <div style={{ marginLeft: "auto", display: "flex", gap: 8 }}>
                    <button
                      onClick={() => {
                        const focus = (focusNoteDraft || "").trim();
                        // Enrich workout with the surrounding App state so that
                        // "Log as today's session" can build a valid entry from
                        // a freshly-generated (not-yet-saved) workout.
                        setRunWorkout({
                          ...workout,
                          type: selectedType,
                          typeLabel: meta ? meta.label : selectedType,
                          poolMode,
                          equipment: { ...equipment },
                          maxYardsCap: maxYards,
                          ...(focus ? { focusNote: focus } : {}),
                        });
                        setShowRestPicker(true);
                      }}
                      style={{
                        padding: "10px 16px", borderRadius: 8,
                        border: "1px solid var(--color-primary)", background: "#1e3a5f", color: "var(--color-primary)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer",
                        display: "inline-flex", alignItems: "center", gap: 6,
                      }}
                      title="Step through this workout section by section">
                      ▶ Run
                    </button>
                    <button
                      onClick={() => {
                        const text = workoutToText(workout, meta, paceInput, unit);
                        navigator.clipboard.writeText(text).then(() => {
                          setCopyFlash(true);
                          setTimeout(() => setCopyFlash(false), 2000);
                        });
                      }}
                      style={{
                        padding: "10px 16px", borderRadius: 8,
                        border: `1px solid ${copyFlash ? "var(--color-positive)" : "var(--color-border-strong)"}`,
                        background: copyFlash ? "#052e16" : "var(--color-bg)",
                        color: copyFlash ? "#86efac" : "var(--color-text)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer",
                        display: "inline-flex", alignItems: "center", gap: 6,
                        transition: "all 0.2s",
                      }}
                      title="Copy workout as plain text for texting or pasting into Notes">
                      {copyFlash ? "✓ Copied!" : "📋 Copy Text"}
                    </button>
                    <button
                      onClick={() => {
                        if (loadedFromHistoryId) {
                          // Already saved — print directly
                          window.print();
                        } else {
                          // Unsaved fresh workout — ask first
                          setShowPrintDialog(true);
                        }
                      }}
                      style={{
                        padding: "10px 16px", borderRadius: 8,
                        border: "1px solid var(--color-border-strong)", background: "var(--color-bg)", color: "var(--color-text)",
                        fontSize: 13, fontWeight: 700, cursor: "pointer",
                        display: "inline-flex", alignItems: "center", gap: 6,
                      }}
                      title="Print a clean B&W 8.5×11 version of this workout">
                      🖨 Print Workout
                    </button>
                    {/* N6 — Multi-pace export. Coach-only, group-only. Visible only
                        when generating for a group with ≥2 members (lane plans can't
                        exist for groups <2 per decision #17). */}
                    {effectiveMe?.is_coach && generateForTarget && generateForTarget.member_count >= 2 && (
                      <button
                        onClick={() => setShowMultiPace(true)}
                        style={{
                          padding: "10px 16px", borderRadius: 8,
                          border: "1px solid var(--color-warn)", background: "rgba(245,158,11,0.15)", color: "var(--color-warn)",
                          fontSize: 13, fontWeight: 700, cursor: "pointer",
                          display: "inline-flex", alignItems: "center", gap: 6,
                        }}
                        title="Render this workout at multiple paces — one page per lane (or a side-by-side matrix). Coach + group only.">
                        🏊 Multi-pace print…
                      </button>
                    )}
                    {/* I — Schedule this workout for a future day. Available
                        whenever there's a workout on screen and the user is
                        authenticated. */}
                    {workout && authenticated && (
                      <button
                        onClick={() => {
                          // Default: tomorrow.
                          const t = new Date(); t.setDate(t.getDate() + 1);
                          const y = t.getFullYear(), m = String(t.getMonth() + 1).padStart(2, "0"), d = String(t.getDate()).padStart(2, "0");
                          setScheduleDate(`${y}-${m}-${d}`);
                        }}
                        style={{
                          padding: "10px 16px", borderRadius: 8,
                          border: "1px solid var(--color-primary)", background: "rgba(59,130,246,0.15)", color: "var(--color-primary)",
                          fontSize: 13, fontWeight: 700, cursor: "pointer",
                          display: "inline-flex", alignItems: "center", gap: 6,
                        }}
                        title="Schedule this workout for a future date (planning, not logging)">
                        📅 Schedule for…
                      </button>
                    )}
                  </div>
                </div>

                {/* Focus note — pre-workout intention (screen, edit-in-place) */}
                {loadedFromHistoryId ? (
                  focusNoteDraft && (
                    <div className="screen-only card" style={{
                      marginBottom: 12, padding: "8px 12px",
                      borderRadius: 8, fontSize: 13, color: "var(--color-primary)", fontStyle: "italic",
                    }}>
                      🎯 Focus: {focusNoteDraft}
                    </div>
                  )
                ) : (
                  <div className="screen-only" style={{ marginBottom: 12 }}>
                    <input type="text" value={focusNoteDraft}
                      onChange={e => setFocusNoteDraft(e.target.value.slice(0, 255))}
                      placeholder="🎯 Focus for this workout (optional) — e.g. hip rotation, strong turns…"
                      style={{
                        width: "100%", padding: "8px 12px",
                        background: "var(--color-bg)", color: "var(--color-text)",
                        border: `1px solid ${focusNoteDraft ? "var(--color-primary)" : "var(--color-border)"}`,
                        borderRadius: 8, fontSize: 13, fontFamily: "inherit",
                        transition: "border-color 0.15s",
                      }} />
                  </div>
                )}

                {/* Print-only header */}
                <div className="print-only print-header">
                  <h1 className="print-title">{meta.label} Swim Workout</h1>
                  <p className="print-subtitle">{meta.description}</p>
                  {focusNoteDraft && (
                    <p style={{ fontSize: 14, fontStyle: "italic", color: "#000", marginTop: 6 }}>
                      🎯 Focus: {focusNoteDraft}
                    </p>
                  )}
                  <div className="print-meta">
                    <span><b>Date:</b> {new Date().toLocaleDateString(undefined, { weekday: "long", year: "numeric", month: "long", day: "numeric" })}</span>
                    <span><b>Total:</b> {workout.totalYards.toLocaleString()} {unit}</span>
                    <span><b>Duration:</b> ~{workout.estimatedMin} min</span>
                    <span><b>Pace target:</b> ~{paceInput} / 100 {unit}</span>
                  </div>
                </div>

                {/* Section breakdown bar (screen only) */}
                <div className="screen-only" style={{ display: "flex", gap: 3, height: 10, borderRadius: 999, overflow: "hidden", marginBottom: 8 }}>
                  {workout.blocks.map((b, i) => (
                    <div key={i}
                      style={{ width: `${(b.totalYards / workout.totalYards * 100).toFixed(1)}%`, background: sectionColors[i] }}
                      title={`${b.name}: ${b.totalYards} ${unit}`}
                    />
                  ))}
                </div>
                <div className="screen-only" style={{ display: "flex", gap: 16, marginBottom: 20, flexWrap: "wrap" }}>
                  {workout.blocks.map((b, i) => (
                    <div key={i} style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 11, color: "var(--color-text-muted)" }}>
                      <span style={{ width: 8, height: 8, borderRadius: "50%", background: sectionColors[i], display: "inline-block" }} />
                      <span style={{ color: sectionColors[i] }}>{b.name}</span>
                      <span>({b.totalYards} {unit})</span>
                    </div>
                  ))}
                </div>

                {/* v2.0 polish — Lane-fit fallback banner. Shows when the
                    multi-lane picker couldn't find lane-friendly options for
                    one or more sections and fell back to the unfiltered pool.
                    Tells the coach the workout may not fit every lane's pace
                    as cleanly as they'd expect. */}
                {Array.isArray(workout.__laneFitFallback) && workout.__laneFitFallback.length > 0 && (
                  <div className="screen-only" style={{
                    marginBottom: 16, padding: "10px 14px",
                    background: "rgba(234, 179, 8, 0.10)",
                    border: "1px solid rgba(234, 179, 8, 0.45)",
                    borderRadius: 8,
                    display: "flex", alignItems: "flex-start", gap: 10,
                  }}>
                    <span style={{ fontSize: 16, lineHeight: 1 }}>⚠️</span>
                    <div style={{ fontSize: 12, color: "var(--color-text)", lineHeight: 1.5 }}>
                      <strong>Lane range is wide</strong> — the picker couldn't find options that fit every lane's pace for: <em>{workout.__laneFitFallback.join(", ")}</em>. Those sections used the unfiltered pool, so intervals may be tight for your slowest lane or loose for your fastest. Tighten the lane range or relax intervals to remove this warning.
                    </div>
                  </div>
                )}

                {/* Pace control (screen only) */}
                <div className="screen-only" style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 16, padding: "10px 16px", background: "var(--color-card)", borderRadius: 10, border: "1px solid var(--color-border)", flexWrap: "wrap" }}>
                  <span style={{ fontSize: 11, fontWeight: 600, color: "var(--color-text-muted)", textTransform: "uppercase", letterSpacing: "0.08em" }}>100 {unit} pace</span>
                  <input
                    type="text"
                    value={paceInput}
                    onChange={e => handlePaceChange(e.target.value)}
                    onKeyDown={e => e.key === "Enter" && handleApplyPace()}
                    placeholder="M:SS"
                    style={{ width: 58, fontFamily: "monospace", fontSize: 13, padding: "4px 7px", borderRadius: 6, border: "1px solid var(--color-border-strong)", background: "var(--color-bg)", color: "var(--color-text)" }}
                  />
                  <button onClick={handleApplyPace}
                    style={{ fontSize: 12, fontWeight: 600, padding: "4px 13px", borderRadius: 6, border: "1px solid var(--color-primary)", background: "#1d4ed8", color: "#fff", cursor: "pointer" }}>
                    Rescale all
                  </button>
                  <span style={{ fontSize: 11, color: "var(--color-border-strong)" }}>· Click any interval or description to edit directly</span>
                </div>

                {/* Workout blocks */}
                {workout.blocks.map((block, i) => block && block.kind === "dryland" ? (
                  <DrylandBlock
                    key={i}
                    block={block}
                    onRemove={() => setWorkout(w => w ? { ...w, blocks: w.blocks.filter((_, j) => j !== i) } : w)}
                    onChange={(nb) => setWorkout(w => w ? { ...w, blocks: w.blocks.map((b, j) => j === i ? nb : b) } : w)}
                  />
                ) : (
                  <WorkoutBlock
                    key={i}
                    block={block}
                    equipment={equipment}
                    onRegenerate={handleRegenerateSection}
                    regenError={regenError && regenError.section === block.section ? regenError.message : null}
                    blockIdx={i}
                    openSwapKey={openSwapKey}
                    onToggleSwap={handleToggleSwap}
                    onApplySwap={handleApplySwap}
                    editIntervalKey={editIntervalKey}
                    editIntervalDraft={editIntervalDraft}
                    setEditIntervalDraft={(v) => { setEditIntervalDraft(v); setEditIntervalError(null); }}
                    editIntervalError={editIntervalError}
                    onStartEditInterval={handleStartEditInterval}
                    onCommitInterval={handleCommitInterval}
                    onClearInterval={handleClearInterval}
                    onCancelInterval={handleCancelInterval}
                    editDescKey={editDescKey}
                    editDescDraft={editDescDraft}
                    setEditDescDraft={setEditDescDraft}
                    onStartEditDesc={handleStartEditDesc}
                    onCommitDesc={handleCommitDesc}
                    onCancelDesc={handleCancelDesc}
                    isFavorited={!!(block.label && favorites.includes(block.label))}
                    onToggleFavorite={handleToggleFavorite}
                    isDisfavorited={!!(block.label && disfavorites.includes(block.label))}
                    onToggleDisfavorite={handleToggleDisfavorite}
                    isEngineDisfavorited={!!(block.__engineMeta && engineDisfavorites.some(e => e.template_id === block.__engineMeta.template_id && e.stroke === block.__engineMeta.stroke))}
                    onToggleEngineDisfavor={handleToggleEngineDisfavor}
                    isEngineFavorited={!!(block.__engineMeta && engineFavorites.some(e => e.template_id === block.__engineMeta.template_id && e.stroke === block.__engineMeta.stroke))}
                    onToggleEngineFavorite={handleToggleEngineFavorite}
                    favoriteSets={favoriteSets}
                    disfavorSets={disfavorSets}
                    onCycleSetStatus={handleCycleSetStatus}
                    onRoundRestChange={handleRoundRestChange}
                    unit={unit}
                    isPinned={!!pinnedSections[block.section]}
                    onTogglePin={handleTogglePin}
                    recentMainLabels={recentMainLabels}
                    poolMode={poolMode}
                    isCoach={!!effectiveMe?.is_coach}
                    onSnapshot={authenticated ? handleSnapshotBlock : null}
                    ugcSetIdMeta={ugcSetIdMeta}
                    sectionSource={sectionSources[block.section] || "bank"}
                    onSectionSourceChange={(newSource) => {
                      setSectionSources(prev => {
                        const next = { ...prev, [block.section]: newSource };
                        // S3 — persist to settings.extra (fire-and-forget, errors
                        // surface in console but don't block UI). Header CSRF + auth
                        // wrappers piggy-back on the existing /api/settings/extra
                        // route added by S2.5.
                        if (typeof fetch === "function" && csrf.token) {
                          fetch(`${API_BASE}/settings/extra`, {
                            method: "POST",
                            credentials: "include",
                            headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf.token },
                            body: JSON.stringify({ engine_section_sources: next }),
                          }).catch(() => {});
                        }
                        return next;
                      });
                      // Trigger a regen of this section under the new source — only
                      // if it's not pinned (pinned sections stay fixed).
                      if (!pinnedSections[block.section]) {
                        // v1.3 fix — pass newSource explicitly. The setTimeout
                        // closure captures the OLD handleRegenerateSection which
                        // reads the OLD sectionSources. The sourceOverride arg
                        // bypasses that staleness so the regen uses the source
                        // the user JUST clicked.
                        setTimeout(() => handleRegenerateSection(block.section, newSource), 0);
                      }
                    }}
                  />
                ))}

                {/* Section model B2 — add a dryland block (before / after pool). */}
                <div className="screen-only" style={{ marginTop: 12, marginBottom: 4 }}>
                  {!drylandPickerOpen ? (
                    <button onClick={() => setDrylandPickerOpen(true)}
                      style={{ padding: "6px 12px", border: "1px dashed #a3702c", borderRadius: 8, background: "transparent", color: "#a3702c", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>
                      🏋 + Add dryland
                    </button>
                  ) : (
                    <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", padding: "10px 12px", border: "1px solid #a3702c", borderRadius: 8, background: "#fbf6ee" }}>
                      <select value={drylandPresetId} onChange={e => setDrylandPresetId(e.target.value)}
                        style={{ padding: "5px 8px", fontSize: 13, borderRadius: 5, border: "1px solid #d8c19a", background: "#fff", color: "#5b4326" }}>
                        {DRYLAND_OPTIONS.map(o => <option key={o.id} value={o.id}>{o.name}</option>)}
                      </select>
                      <select value={drylandPlacement} onChange={e => setDrylandPlacement(e.target.value)}
                        style={{ padding: "5px 8px", fontSize: 13, borderRadius: 5, border: "1px solid #d8c19a", background: "#fff", color: "#5b4326" }}>
                        <option value="pre">Before pool</option>
                        <option value="post">After pool</option>
                      </select>
                      <button onClick={() => {
                        const opt = DRYLAND_OPTIONS.find(o => o.id === drylandPresetId) || DRYLAND_OPTIONS[0];
                        const blk = makeDrylandBlock(opt, drylandPlacement);
                        setWorkout(w => w ? { ...w, blocks: drylandPlacement === "pre" ? [blk, ...w.blocks] : [...w.blocks, blk] } : w);
                        setDrylandPickerOpen(false);
                      }}
                        style={{ padding: "6px 14px", border: "none", borderRadius: 6, background: "#a3702c", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>Add</button>
                      <button onClick={() => setDrylandPickerOpen(false)}
                        style={{ padding: "6px 12px", border: "1px solid #d8c19a", borderRadius: 6, background: "transparent", color: "#9a7b4f", fontSize: 12, cursor: "pointer" }}>Cancel</button>
                    </div>
                  )}
                </div>

                {/* Coach note (screen only — user opted not to include in print) */}
                <div className="screen-only" style={{ marginTop: 20, background: "rgba(30,41,59,0.6)", borderRadius: 12, padding: 16, border: "1px solid var(--color-border)", color: "var(--color-text-muted)", fontSize: 13 }}>
                  <span style={{ fontWeight: 600, color: "#cbd5e1" }}>📋 Coach's Note: </span>
                  {poolMode === "50m"
                    ? "Intervals are scaled from your pace setting. Adjust ±10–15 sec to match your base in long-course."
                    : poolMode === "25m"
                    ? "Intervals are scaled from your pace setting. Adjust ±10–15 sec to match your base in short-course meters."
                    : "Intervals are calibrated for a masters pace of 2:00–2:15/100 yds. Adjust ±10–15 sec to match your base."
                  }
                  Always prioritize stroke mechanics over hitting a split — especially on drill and technique sets.
                </div>

                {/* Save-to-history form (only for freshly generated workouts) */}
                {!loadedFromHistoryId && (
                  <SaveToHistoryForm
                    dateDraft={dateDraft} setDateDraft={setDateDraft}
                    initialsDraft={initialsDraft} setInitialsDraft={handleInitialsChange}
                    noteDraft={noteDraft} setNoteDraft={setNoteDraft}
                    difficultyDraft={difficultyDraft} setDifficultyDraft={setDifficultyDraft}
                    saveStatus={saveStatus} saveError={saveError}
                    onSave={handleSave}
                  />
                )}

                {/* Banner for workouts loaded from history */}
                {loadedFromHistoryId && (
                  <div className="screen-only" style={{ marginTop: 20, background: "var(--color-card)", border: "1px dashed var(--color-border-strong)", borderRadius: 8, padding: "10px 14px", fontSize: 12, color: "var(--color-text-muted)" }}>
                    📜 Loaded from history. To save a new workout, click <b>Generate New Workout</b> above.
                  </div>
                )}
              </div>
            )}

            {/* Empty state */}
            {!workout && (
              <div className="screen-only" style={{ textAlign: "center", padding: "60px 0", color: "var(--color-border-strong)" }}>
                <div style={{ fontSize: 56, marginBottom: 12 }}>🌊</div>
                <p style={{ fontSize: 18, fontWeight: 500, color: "var(--color-text-muted)", margin: 0 }}>Pick a type, set your max yardage, and generate</p>
                <p style={{ fontSize: 13, marginTop: 4, color: "var(--color-border-strong)" }}>Each press produces a fresh workout within your range</p>
              </div>
            )}

            </>}
          </main>
          {showPrintDialog && (
            <div className="screen-only" style={{
              position: "fixed", inset: 0, background: "rgba(0,0,0,0.7)",
              display: "flex", alignItems: "center", justifyContent: "center", zIndex: 1000,
            }}
              onClick={() => setShowPrintDialog(false)}>
              <div className="card" style={{
                borderRadius: 12,
                padding: "28px 32px", maxWidth: 400, width: "90%", boxShadow: "0 20px 60px rgba(0,0,0,0.6)",
              }}
                onClick={e => e.stopPropagation()}>
                <div style={{ fontSize: 22, marginBottom: 8 }}>🖨 Print Workout</div>
                <p style={{ color: "#cbd5e1", fontSize: 14, margin: "0 0 24px 0", lineHeight: 1.5 }}>
                  Would you like to save this workout to your history before printing?
                </p>
                <div style={{ display: "flex", gap: 10, flexWrap: "wrap" }}>
                  <button
                    onClick={async () => {
                      setShowPrintDialog(false);
                      await handleSave();
                      setTimeout(() => window.print(), 150);
                    }}
                    style={{
                      flex: 1, padding: "10px 16px", borderRadius: 8,
                      border: "none", background: "#2563eb", color: "#fff",
                      fontSize: 13, fontWeight: 700, cursor: "pointer",
                    }}>
                    Save &amp; Print
                  </button>
                  <button
                    onClick={() => { setShowPrintDialog(false); setTimeout(() => window.print(), 150); }}
                    style={{
                      flex: 1, padding: "10px 16px", borderRadius: 8,
                      border: "1px solid var(--color-border-strong)", background: "transparent", color: "#cbd5e1",
                      fontSize: 13, fontWeight: 600, cursor: "pointer",
                    }}>
                    Print Without Saving
                  </button>
                  <button
                    onClick={() => setShowPrintDialog(false)}
                    style={{
                      width: "100%", padding: "8px 16px", borderRadius: 8,
                      border: "1px solid var(--color-border)", background: "transparent", color: "var(--color-text-dim)",
                      fontSize: 13, cursor: "pointer", marginTop: 2,
                    }}>
                    Cancel
                  </button>
                </div>
              </div>
            </div>
          )}
          {showRestPicker && runWorkout && (
            <RestPickerModal
              restSecs={restSecs}
              onChange={setRestSecs}
              onStart={() => setShowRestPicker(false)}
              onCancel={() => { setRunWorkout(null); setShowRestPicker(false); setRunAssignmentId(null); }}
              audioCues={audioCues}
              onAudioCuesChange={handleAudioCuesToggle}
              lapButton={lapButton}
              onLapButtonChange={handleLapButtonToggle}
            />
          )}
          {!showRestPicker && runWorkout && (
            <RunWorkoutOverlay
              workout={runWorkout}
              restSecs={restSecs}
              onClose={() => { setRunWorkout(null); setRunAssignmentId(null); }}
              onLogAsToday={handleLogAsToday}
              personalFirstName={personalFirstName}
              audioCues={audioCues}
              lapButton={lapButton}
              unit={unit}
            />
          )}
          {showProfile && (
            <ProfileModal
              onClose={() => {
                setShowProfile(false);
                // Burst mitigation — was 3 parallel GETs (me, goals,
                // settings). Replaced with a single bootstrap re-fetch which
                // refreshes me + goals + settings + everything else in one
                // round-trip.
                refreshBootstrap();
              }}
              onProfileChange={() => {
                // Burst mitigation — was 8 parallel GETs (me, settings,
                // disfavorites, disfavor-sets, effective-disfavorites,
                // favorites, favorite-sets, effective-favorites). Replaced
                // with a single bootstrap re-fetch which covers all of them.
                refreshBootstrap();
              }}
              onPaceUpdate={(newPace) => {
                // J — LevelRow pushes a preset pace up to App when a level
                // is picked. Use handlePaceChange so the debounced save
                // path persists it to settings alongside the local update.
                handlePaceChange(newPace);
              }}
              authMode={authMode}
              onSendFeedback={() => setShowFeedback(true)}
              onStartTour={startTour}
              appEffectiveMe={effectiveMe}
              appViewAsRole={viewAsRole}
              appSetViewAsRole={setViewAsRole}
              appViewAsParent={viewAsParent}
              appSetViewAsParent={setViewAsParent}
              /* Burst-mitigation — pass App-level state already populated by
                 bootstrap so ProfileModal can seed its local state without
                 re-fetching. Each prop replaces one GET in the open burst. */
              appMe={me}
              appGoals={goals}
              appFavorites={favorites}
              appDisfavorites={disfavorites}
              appFavoriteSets={favoriteSets}
              appDisfavorSets={disfavorSets}
              appMyConstraints={myConstraints}
              /* B (bootstrap extension) — sessions + team-defaults +
                 billing-status + level + settings-derived bits passed in so
                 ProfileModal can drop the /api/settings + /api/auth/sessions
                 + /api/me/team-defaults fetches on open. */
              appSessions={sessions}
              appTeamDefaults={teamDefaults}
              appBillingStatus={billingStatus}
              appLevel={level}
              appNextEvent={nextEvent}
              appPhase={phase}
              appDisfavorMode={disfavorMode}
              appEngineDisfavorites={engineDisfavorites}
              appEngineFavorites={engineFavorites} />
          )}
          {showFeedback && (
            <FeedbackModal onClose={() => setShowFeedback(false)} />
          )}
          {tourStep >= 0 && (
            <TourOverlay
              steps={TOUR_STEPS}
              stepIndex={tourStep}
              onNext={() => setTourStep(s => Math.min(TOUR_STEPS.length - 1, s + 1))}
              onBack={() => setTourStep(s => Math.max(0, s - 1))}
              onSkip={finishTour}
              onFinish={finishTour}
            />
          )}
          {showImpersonationModal && (
            <ImpersonationStartModal
              me={me}
              onClose={() => setShowImpersonationModal(false)}
              onStartImpersonation={handleStartImpersonation}
            />
          )}
          {showIntentParser && (
            <IntentParserModal
              onClose={() => setShowIntentParser(false)}
              onApply={handleApplyIntent}
            />
          )}
          {/* UGC Phase C — snapshot-from-WorkoutBlock opens the same
              UgcFormModal used by My Sets, pre-filled with block data.
              No id on the pre-fill → modal treats as create. */}
          {snapshotOption && (
            <UgcFormModal
              option={snapshotOption}
              onSave={() => { setSnapshotOption(null); refreshUgcOverlay(); }}
              onClose={() => setSnapshotOption(null)}
            />
          )}
          {/* N6 — Multi-pace export. Modal first; on Preview, modal closes
              and the print overlay mounts (multiPaceLanes !== null). */}
          {showMultiPace && workout && generateForTarget && (
            <MultiPaceModal
              workout={workout}
              generateForTarget={generateForTarget}
              lanePlansForTarget={lanePlansForTarget}
              onClose={() => setShowMultiPace(false)}
              onPreview={(payload) => { setShowMultiPace(false); setMultiPaceLanes(payload); }}
            />
          )}
          {multiPaceLanes && workout && (
            <MultiPacePrintView
              workout={workout}
              lanes={multiPaceLanes.lanes}
              mode={multiPaceLanes.mode}
              groupActiveConstraints={groupActiveConstraints}
              onClose={() => setMultiPaceLanes(null)}
            />
          )}
          {/* I — schedule-for-day picker. Opens when scheduleDate state is
              non-null (set by the 📅 Schedule for… button). Picks a date,
              POSTs to /api/scheduled-workouts with the current workout. */}
          {scheduleDate !== null && workout && (
            <div onClick={() => setScheduleDate(null)} className="modal-overlay" style={{ padding: 20 }}>
              <div onClick={(e) => e.stopPropagation()} style={{
                background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
                padding: 22, maxWidth: 420, width: "100%",
              }}>
                <div style={{ color: "var(--color-primary)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>Schedule workout</div>
                <div style={{ color: "var(--color-text)", fontSize: 16, fontWeight: 700, marginBottom: 14 }}>Pick a date</div>
                <input type="date" value={scheduleDate} onChange={(e) => setScheduleDate(e.target.value)}
                  style={{ width: "100%", padding: "8px 10px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 14 }} />
                <div style={{ color: "var(--color-text-muted)", fontSize: 11, marginTop: 6 }}>
                  Saves the current workout to your week-view planner. Run it later from the 📅 Week tab — your completion logs back to history and links to the schedule row.
                </div>
                <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
                  <button onClick={() => setScheduleDate(null)}
                    className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
                  <button onClick={async () => {
                      if (!/^\d{4}-\d{2}-\d{2}$/.test(scheduleDate)) { setSaveError("Pick a valid date"); return; }
                      try {
                        // Enrich payload with form context so ✎ Edit on the
                        // scheduled row can fully restore the generator
                        // (otherwise the raw workout object doesn't carry
                        // maxYards/poolMode/equipment).
                        const enrichedPayload = {
                          ...workout,
                          maxYardsCap: workout.maxYardsCap || maxYards,
                          poolMode:    workout.poolMode    || poolMode,
                          equipment:   workout.equipment   || { ...equipment },
                          focusNote:   workout.focusNote   || focusNoteDraft || undefined,
                        };
                        const res = await fetch("/api/scheduled-workouts", {
                          method:  "POST",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body:    JSON.stringify({ scheduled_date: scheduleDate, payload: enrichedPayload }),
                        });
                        const j = await res.json();
                        if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
                        setScheduleDate(null);
                        setSaveStatus("scheduled");
                        setTimeout(() => setSaveStatus(null), 2500);
                      } catch (e) {
                        setSaveError(`Schedule failed: ${e.message}`);
                        setScheduleDate(null);
                      }
                    }}
                    className="btn btn-lg btn-filled btn-primary">Schedule ▸</button>
                </div>
              </div>
            </div>
          )}
          {/* I Phase 2a — save-as-intent date picker. Captures the current
              generator form state as an intent_params row; generation runs
              on the scheduled day via WeekView's ▶ Generate button. */}
          {intentDate !== null && (
            <div onClick={() => setIntentDate(null)} className="modal-overlay" style={{ padding: 20 }}>
              <div onClick={(e) => e.stopPropagation()} style={{
                background: "var(--color-bg)", border: "1px solid var(--color-border)", borderRadius: 12,
                padding: 22, maxWidth: 460, width: "100%",
              }}>
                <div style={{ color: "var(--color-warn)", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: "0.12em", marginBottom: 4 }}>Save as intent</div>
                <div style={{ color: "var(--color-text)", fontSize: 16, fontWeight: 700, marginBottom: 14 }}>Plan these settings for…</div>
                <input type="date" value={intentDate} onChange={(e) => setIntentDate(e.target.value)}
                  style={{ width: "100%", padding: "8px 10px", background: "var(--color-card)", color: "var(--color-text)", border: "1px solid var(--color-border-strong)", borderRadius: 6, fontSize: 14 }} />
                <div style={{ color: "var(--color-text-muted)", fontSize: 11, marginTop: 8, lineHeight: 1.5 }}>
                  Saves these settings (type, yardage, mix, recovery) as an <strong>intent</strong> for the chosen day. On the day, generate the actual workout from the latest bank state via 📅 Week.
                </div>
                <div style={{ display: "flex", justifyContent: "flex-end", gap: 6, marginTop: 14 }}>
                  <button onClick={() => setIntentDate(null)} disabled={intentSaveBusy}
                    className="btn btn-lg btn-outlined btn-neutral">Cancel</button>
                  <button onClick={async () => {
                      if (!/^\d{4}-\d{2}-\d{2}$/.test(intentDate)) { setSaveError("Pick a valid date"); return; }
                      setIntentSaveBusy(true);
                      try {
                        const intent_params = {
                          type:     selectedType,
                          maxYards: Number(maxYards),
                          mix:      sectionBias,
                          recovery: !!recoveryMode,
                        };
                        const res = await fetch("/api/scheduled-workouts", {
                          method:  "POST",
                          headers: { "Content-Type": "application/json", ...csrfHeaders() },
                          body:    JSON.stringify({ scheduled_date: intentDate, intent_params }),
                        });
                        const j = await res.json();
                        if (!res.ok) throw new Error(j.error || `HTTP ${res.status}`);
                        setIntentDate(null);
                        setSaveStatus("intent_saved");
                        setTimeout(() => setSaveStatus(null), 2500);
                      } catch (e) {
                        setSaveError(`Save intent failed: ${e.message}`);
                        setIntentDate(null);
                      } finally { setIntentSaveBusy(false); }
                    }}
                    disabled={intentSaveBusy}
                    className="btn btn-lg btn-filled btn-warn">{intentSaveBusy ? "…" : "Save intent ▸"}</button>
                </div>
              </div>
            </div>
          )}
          {/* DOB soft-prompt (decision #37). Surfaces once per session when
              authenticated and me.dob is null. The check is gated by
              dobPromptDismissed (sessionStorage) so dismissal sticks for
              the rest of the browser tab life. */}
          {authenticated && me && me.dob === null && !dobPromptDismissed && (
            <DobPromptModal
              onSaved={(newDob) => {
                // Compute age correctly: year diff minus 1 if birthday hasn't passed this year.
                // (Server is authoritative via /api/me; this just keeps the local `me` consistent
                // until the next refresh.) Year-only subtraction misclassified anyone whose
                // birthday hadn't occurred yet — e.g. Nov-2008 evaluated in May 2026.
                setMe(prev => {
                  if (!prev) return prev;
                  const today = new Date();
                  const dob   = new Date(newDob);
                  let age = today.getFullYear() - dob.getFullYear();
                  const monthDiff = today.getMonth() - dob.getMonth();
                  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < dob.getDate())) age--;
                  return { ...prev, dob: newDob, is_minor: age < 18 };
                });
                setDobPromptDismissed(true);
              }}
              onDismiss={() => {
                try { sessionStorage.setItem(DOB_PROMPT_DISMISS_KEY, "1"); } catch (_) {}
                setDobPromptDismissed(true);
              }}
            />
          )}
          {/* Footer — quiet copyright stamp at the bottom of every view. */}
          <footer className="screen-only" style={{
            textAlign: "center", padding: "20px 16px 32px",
            color: "var(--color-border-strong)", fontSize: 11, fontFamily: "system-ui, -apple-system, sans-serif",
            borderTop: "1px solid var(--color-card)", marginTop: 40,
          }}>
            © 2026 Competition Aquatics, LLC · All rights reserved.
          </footer>
        </div>
      );
    }

    ReactDOM.createRoot(document.getElementById("root")).render(<App />);
