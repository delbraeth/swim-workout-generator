# Secrets + local config — never commit
.env
.env.*
*.env

# SSH keys + credentials
swimdb
swimdb.pub
*.pem
*.key
id_rsa*

# macOS noise
.DS_Store
**/.DS_Store

# Local operator scripts (run by Cap'n, not shipped)
_checkpoint.py
_deploy.py
_COMMIT_MSG.txt

# Historical snapshots — kept locally, not in git
checkpoints/

# Build artifacts
node_modules/
vendor-kit/build/
tools/__pycache__/
# SPA-split: src/app.jsx is now the committed source of truth (cut over 2026-06-03;
# index.html is a thin shell). The esbuild OUTPUT stays out of git — _deploy.py
# ships public/assets/app.js directly from disk (it reads files, not git).
public/assets/

# Misc working dirs not for prod
SwimWorkout
SwimApp-Sources/
"SetForge Icons/"
"new watch app/"
png/

# Local preview helper (Claude Preview launch configs) — not shipped
.claude/

# Local SSH config + python bytecode (stray, never deploy)
/config
__pycache__/

# Local IAP review assets (paywall mockup) — not deployed app code
/ios-app/iap-assets/
